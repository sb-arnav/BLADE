---
phase: 05-agents-knowledge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tauri/agents.ts
  - src/lib/tauri/knowledge.ts
  - src/lib/tauri/index.ts
  - src/features/agents/index.tsx
  - src/features/knowledge/index.tsx
  - src/features/agents/types.ts
  - src/features/knowledge/types.ts
  - src/features/agents/agents.css
  - src/features/knowledge/knowledge.css
  - src/styles/tokens.css
autonomous: true
requirements:
  - AGENT-01  # AgentDashboard — requires agentList, getActiveAgents
  - AGENT-02  # AgentDetail — requires agentGet + event constants (05-01)
  - AGENT-03  # AgentFactory — requires factoryCreate/Deploy/List/Pause/Delete
  - AGENT-04  # AgentTeam — requires agentList grouping by role
  - AGENT-05  # AgentTimeline — requires agentList + events
  - AGENT-06  # BackgroundAgents — requires agentSpawn, agentListBackground, agentGetOutput
  - AGENT-07  # TaskAgents — requires agentList + task filter
  - AGENT-08  # SwarmView — requires swarmList, swarmGet, swarmGetProgress
  - AGENT-09  # AgentPixelWorld — requires agentList grouping by role
  - KNOW-01   # KnowledgeBase — requires dbSearchKnowledge, dbListKnowledge, semanticSearch
  - KNOW-02   # KnowledgeGraph — requires graphSearchNodes, graphGetStats
  - KNOW-03   # MemoryPalace — requires memorySearch, memoryRecallCategory, memoryGetAllTyped
  - KNOW-04   # ScreenTimeline — requires timelineSearchCmd, timelineBrowseCmd, timelineGetScreenshot
  - KNOW-05   # RewindTimeline — requires timelineBrowseCmd, timelineGetScreenshot
  - KNOW-06   # LiveNotes — requires dbListKnowledge + memoryAddManual
  - KNOW-07   # DailyLog — requires memorySearch or dbListKnowledge filtered by date
  - KNOW-08   # ConversationInsights — requires dbListConversations + semanticSearch
  - KNOW-09   # CodebaseExplorer — requires docList, docSearch, semanticSearch
must_haves:
  truths:
    - "src/lib/tauri/agents.ts exports ~33 camelCase wrapper functions, one per registered Rust #[tauri::command] in agent_commands.rs + background_agent.rs + swarm_commands.rs + agent_factory.rs + managed_agents.rs (D-119 inventory)"
    - "src/lib/tauri/knowledge.ts exports ~52 camelCase wrapper functions covering db_commands.rs (knowledge + templates), embeddings.rs, knowledge_graph.rs, memory_palace.rs, typed_memory.rs, screen_timeline_commands.rs, document_intelligence.rs, memory.rs (D-119 inventory)"
    - "Both wrapper files use invokeTyped<TReturn, TArgs>(command, args); raw invoke NEVER used (ESLint no-raw-tauri passes)"
    - "Each wrapper has a JSDoc comment with @see src-tauri/src/<file>.rs:<line> citation matching the Rust #[tauri::command] (D-38 naming discipline)"
    - "src/features/agents/index.tsx REWRITTEN — 9 lazy imports of per-route files (AgentDashboard.tsx, AgentDetail.tsx, AgentFactory.tsx, AgentTeam.tsx, AgentTimeline.tsx, BackgroundAgents.tsx, TaskAgents.tsx, SwarmView.tsx, AgentPixelWorld.tsx) — each component file created with a minimal placeholder export (Plans 05-03/05-04 fill in the bodies)"
    - "src/features/knowledge/index.tsx REWRITTEN — 9 lazy imports of per-route files (KnowledgeBase.tsx, KnowledgeGraph.tsx, MemoryPalace.tsx, ScreenTimeline.tsx, RewindTimeline.tsx, LiveNotes.tsx, DailyLog.tsx, ConversationInsights.tsx, CodebaseExplorer.tsx) + 1 Documents.tsx file created since KNOW-09 CodebaseExplorer may need it adjacent — each component minimal placeholder"
    - "Both cluster index.tsx files preserve route id order and RouteDefinition[] shape (D-122 single-writer discipline)"
    - "Per-route files are MINIMAL placeholders — each exports a named component that renders a GlassPanel wrapper with route label + 'Ships in Plan 05-NN' placeholder text. Plans 05-03..06 REPLACE these placeholder bodies (not edit around them)."
    - "src/features/agents/types.ts + src/features/knowledge/types.ts re-export cluster-local types from the wrappers + cluster-only UI types (filter/sort/selection) — D-128"
    - "src/features/agents/agents.css + src/features/knowledge/knowledge.css created with shared layer discipline (D-132) + rgba-bg card classes (D-70) + status-color modifiers; sub-plans only extend, never recreate"
    - "src/styles/tokens.css has --status-running, --status-idle, --status-success, --status-error tokens added (used by agents.css card status border)"
    - "src/lib/tauri/index.ts barrel re-exports both new modules for convenience (consistent with Phase 1..4 pattern)"
    - "npx tsc --noEmit passes; ESLint passes; npm run verify:all passes; npm run dev builds without error (route lazy imports resolve)"
    - "Zero Rust changes (D-119 + D-123)"
  artifacts:
    - path: "src/lib/tauri/agents.ts"
      provides: "Typed wrappers for ~33 agent commands"
      exports: ["agentCreate", "agentList", "agentGet", "agentPause", "agentResume", "agentCancel", "agentSpawn", "agentListBackground", "agentGetBackground", "agentCancelBackground", "agentDetectAvailable", "agentGetOutput", "getActiveAgents", "agentAutoSpawn", "agentSpawnCodex", "swarmCreate", "swarmList", "swarmGet", "swarmPause", "swarmResume", "swarmCancel", "swarmGetProgress", "swarmWriteScratchpad", "swarmReadScratchpad", "factoryCreateAgent", "factoryDeployAgent", "factoryListAgents", "factoryPauseAgent", "factoryDeleteAgent", "runManagedAgent", "agentCreateDesktop", "agentRespondDesktopAction"]
    - path: "src/lib/tauri/knowledge.ts"
      provides: "Typed wrappers for ~52 knowledge commands"
      exports: ["dbListKnowledge", "dbGetKnowledge", "dbAddKnowledge", "dbUpdateKnowledge", "dbDeleteKnowledge", "dbSearchKnowledge", "dbKnowledgeByTag", "dbKnowledgeTags", "dbKnowledgeStats", "dbListTemplates", "dbAddTemplate", "dbDeleteTemplate", "dbIncrementTemplateUsage", "embedAndStore", "semanticSearch", "vectorStoreSize", "graphAddNode", "graphSearchNodes", "graphTraverse", "graphFindPath", "graphExtractFromText", "graphAnswer", "graphGetStats", "graphDeleteNode", "memorySearch", "memoryGetRecent", "memoryRecall", "memoryAddManual", "memoryDelete", "memoryConsolidateNow", "memoryStoreTyped", "memoryRecallCategory", "memoryGetAllTyped", "memoryDeleteTyped", "memoryGenerateUserSummary", "timelineSearchCmd", "timelineBrowseCmd", "timelineGetScreenshot", "timelineGetThumbnail", "timelineGetConfig", "timelineSetConfig", "timelineGetStatsCmd", "timelineCleanup", "timelineSearchEverything", "timelineGetAudio", "timelineMeetingSummary", "timelineGetActionItems", "timelineSetAudioCapture", "timelineDetectMeeting", "docIngest", "docSearch", "docGet", "docList", "docDelete", "docAnswerQuestion", "docCrossSynthesis", "docGenerateStudyNotes", "getMemoryLog", "getMemoryBlocks", "setMemoryBlock", "runWeeklyMemoryConsolidation"]
    - path: "src/features/agents/index.tsx"
      provides: "9-route RouteDefinition[] with lazy imports of real components"
    - path: "src/features/knowledge/index.tsx"
      provides: "9-route RouteDefinition[] with lazy imports of real components"
    - path: "src/features/agents/types.ts"
      provides: "Cluster-local re-exports + UI types"
      exports: ["AgentFilterStatus", "Agent", "BackgroundAgent", "Swarm"]
    - path: "src/features/knowledge/types.ts"
      provides: "Cluster-local re-exports + UI types"
      exports: ["KnowledgeEntry", "GraphNode", "GraphEdge", "TimelineEntry", "DocumentEntry"]
    - path: "src/features/agents/agents.css"
      provides: "Cluster-scoped CSS — shared base + agent-card classes"
      contains: ".agents-surface"
    - path: "src/features/knowledge/knowledge.css"
      provides: "Cluster-scoped CSS — shared base + knowledge-card classes"
      contains: ".knowledge-surface"
    - path: "src/styles/tokens.css"
      provides: "4 new status-color CSS custom properties"
      contains: "--status-running"
    - path: "src/lib/tauri/index.ts"
      provides: "Extended barrel exports with agents + knowledge re-exports"
  key_links:
    - from: "src/lib/tauri/agents.ts"
      to: "src-tauri/src/lib.rs generate_handler!"
      via: "invokeTyped('agent_*' / 'swarm_*' / 'factory_*', ...) — all registered"
      pattern: "invokeTyped\\('agent_|invokeTyped\\('swarm_|invokeTyped\\('factory_"
    - from: "src/lib/tauri/knowledge.ts"
      to: "src-tauri/src/lib.rs generate_handler!"
      via: "invokeTyped('db_*' / 'graph_*' / 'memory_*' / 'timeline_*' / 'doc_*' / 'embed_*' / 'semantic_search' / 'vector_*', ...)"
      pattern: "invokeTyped\\('db_|invokeTyped\\('graph_|invokeTyped\\('memory_|invokeTyped\\('timeline_|invokeTyped\\('doc_"
    - from: "src/features/agents/index.tsx"
      to: "src/features/agents/AgentDashboard.tsx etc."
      via: "React.lazy + dynamic import"
      pattern: "lazy\\(\\(\\) => import"
    - from: "src/features/knowledge/index.tsx"
      to: "src/features/knowledge/KnowledgeBase.tsx etc."
      via: "React.lazy + dynamic import"
      pattern: "lazy\\(\\(\\) => import"
---

<objective>
Create the two cluster-level Tauri wrapper modules + rewrite both cluster feature index files with real lazy imports + seed 18 minimal per-route component files that Plans 05-03..06 will fill in. No behavioral UI work in this plan — only plumbing.

Purpose: ROADMAP Phase 5 description "each cluster wires its own lib/tauri module" + D-118 cluster-scoped wrapper discipline + D-122 single-writer index.tsx invariant. After this plan lands, Plans 05-03..06 can ship their route bodies with zero import-path surprises and zero chance of stepping on each other's index.tsx edits.

Output: ~85 camelCase wrapper exports across 2 new TS files; 2 rewritten index.tsx files; 18 new per-route placeholder files; 2 cluster CSS files; 2 type barrel files; +4 CSS tokens; 1 barrel edit. Roughly 1500-2000 net new lines — mostly mechanical wrapper declarations. Plan completes under 45% context — large file count but each file is tightly templated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-agents-knowledge/05-CONTEXT.md
@.planning/phases/05-agents-knowledge/05-PATTERNS.md
@.planning/RECOVERY_LOG.md
@CLAUDE.md
@src/lib/tauri/_base.ts
@src/lib/tauri/chat.ts
@src/lib/tauri/config.ts
@src/lib/tauri/index.ts
@src/lib/router.ts
@src/features/agents/index.tsx
@src/features/knowledge/index.tsx
@src/styles/tokens.css
@src-tauri/src/lib.rs
@src-tauri/src/agent_commands.rs
@src-tauri/src/background_agent.rs
@src-tauri/src/swarm_commands.rs
@src-tauri/src/agent_factory.rs
@src-tauri/src/managed_agents.rs
@src-tauri/src/db_commands.rs
@src-tauri/src/embeddings.rs
@src-tauri/src/knowledge_graph.rs
@src-tauri/src/memory_palace.rs
@src-tauri/src/typed_memory.rs
@src-tauri/src/screen_timeline_commands.rs
@src-tauri/src/document_intelligence.rs
@src-tauri/src/memory.rs

<interfaces>
<!-- Existing wrapper pattern to follow (from src/lib/tauri/chat.ts + config.ts) -->
```ts
// src/lib/tauri/_base.ts
export async function invokeTyped<TReturn, TArgs extends Record<string, unknown> = Record<string, unknown>>(
  command: string,
  args?: TArgs,
): Promise<TReturn>;

// Pattern (src/lib/tauri/chat.ts):
/** @see src-tauri/src/commands.rs:559 send_message_stream */
export async function sendMessageStream(messages: ChatMessage[]): Promise<void> {
  return invokeTyped<void, {messages: ChatMessage[]}>('send_message_stream', { messages });
}
```

<!-- Existing route definition shape (D-39) -->
```ts
// src/lib/router.ts
export interface RouteDefinition {
  id: string;
  label: string;
  section: 'core' | 'agents' | 'knowledge' | 'life' | 'identity' | 'dev' | 'admin' | 'body' | 'hive';
  component: React.LazyExoticComponent<React.ComponentType<any>>;
  icon?: React.ComponentType;
  shortcut?: string;
  paletteHidden?: boolean;
  description?: string;
  phase?: number;
}
```

<!-- Rust command surface summary (from lib.rs generate_handler!) — counts are nominal -->
```
Agents cluster commands (present in lib.rs):
  agent_commands::agent_create, agent_create_desktop, agent_respond_desktop_action
  agent_commands::agent_list, agent_get, agent_pause, agent_resume, agent_cancel
  background_agent::agent_spawn, agent_list_background, agent_get_background, agent_cancel_background
  background_agent::agent_detect_available, agent_get_output, agent_auto_spawn, agent_spawn_codex, get_active_agents
  swarm_commands::swarm_create, swarm_list, swarm_get, swarm_pause, swarm_resume, swarm_cancel
  swarm_commands::swarm_write_scratchpad, swarm_write_scratchpad_entry, swarm_read_scratchpad, swarm_get_progress
  agent_factory::factory_create_agent, factory_deploy_agent, factory_list_agents, factory_pause_agent, factory_delete_agent
  managed_agents::run_managed_agent

Knowledge cluster commands (present in lib.rs):
  db_commands::db_list_knowledge, db_get_knowledge, db_add_knowledge, db_update_knowledge, db_delete_knowledge
  db_commands::db_search_knowledge, db_knowledge_by_tag, db_knowledge_tags, db_knowledge_stats
  db_commands::db_list_templates, db_add_template, db_delete_template, db_increment_template_usage
  embeddings::embed_and_store, semantic_search, vector_store_size
  knowledge_graph::graph_add_node, graph_search_nodes, graph_traverse, graph_find_path
  knowledge_graph::graph_extract_from_text, graph_answer, graph_get_stats, graph_delete_node
  memory_palace::memory_search, memory_get_recent, memory_recall, memory_add_manual, memory_delete, memory_consolidate_now
  typed_memory::memory_store_typed, memory_recall_category, memory_get_all_typed, memory_delete_typed, memory_generate_user_summary
  screen_timeline_commands::timeline_search_cmd, timeline_browse_cmd, timeline_get_screenshot, timeline_get_thumbnail
  screen_timeline_commands::timeline_get_config, timeline_set_config, timeline_get_stats_cmd, timeline_cleanup
  screen_timeline_commands::timeline_search_everything, timeline_get_audio, timeline_meeting_summary
  screen_timeline_commands::timeline_get_action_items, timeline_set_audio_capture, timeline_detect_meeting
  document_intelligence::doc_ingest, doc_search, doc_get, doc_list, doc_delete
  document_intelligence::doc_answer_question, doc_cross_synthesis, doc_generate_study_notes
  memory::get_memory_log, get_memory_blocks, set_memory_block, run_weekly_memory_consolidation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/tauri/agents.ts with 33 typed wrappers + src/features/agents/types.ts barrel</name>
  <files>src/lib/tauri/agents.ts, src/features/agents/types.ts</files>
  <read_first>
    - /home/arnav/blade/src/lib/tauri/chat.ts (complete — recipe reference)
    - /home/arnav/blade/src/lib/tauri/config.ts (complete — recipe reference)
    - /home/arnav/blade/src/lib/tauri/_base.ts (complete)
    - /home/arnav/blade/src-tauri/src/agent_commands.rs (read the 8 #[tauri::command] signatures — line numbers 228, 295, 2605, 2611, 2622, 2632, 2702, 2712)
    - /home/arnav/blade/src-tauri/src/background_agent.rs (read the 9 #[tauri::command] signatures — line numbers 152, 377, 386, 393, 405, 411, 644, 711, 721)
    - /home/arnav/blade/src-tauri/src/swarm_commands.rs (read the 10 #[tauri::command] signatures — line numbers 470, 541, 546, 551, 557, 574, 580, 589, 609, 617)
    - /home/arnav/blade/src-tauri/src/agent_factory.rs (grep #[tauri::command] for the 5 factory_* signatures)
    - /home/arnav/blade/src-tauri/src/managed_agents.rs (line 23)
    - /home/arnav/blade/.planning/phases/05-agents-knowledge/05-PATTERNS.md §1
  </read_first>
  <action>
    Create `src/lib/tauri/agents.ts` (~450 lines expected). Structure:

    ```ts
    // src/lib/tauri/agents.ts
    //
    // Typed wrappers for the Agents cluster (~33 registered commands).
    // Each wrapper cites @see src-tauri/src/<file>.rs:<line>.
    //
    // D-118: per-cluster wrapper module — agents cluster lives HERE.
    // D-119: zero Rust expansion in Phase 5 — every command below already registered in lib.rs.
    // D-126: camelCase JS API, snake_case at invoke boundary. No raw invoke.
    // D-38: @see Rust cite in JSDoc; invokeTyped only; ESLint no-raw-tauri.
    //
    // @see .planning/phases/05-agents-knowledge/05-CONTEXT.md §D-118..D-128
    // @see .planning/phases/05-agents-knowledge/05-PATTERNS.md §1

    import { invokeTyped } from './_base';

    // ═══════════════════════════════════════════════════════════════════════
    // Types (mirror Rust Serialize shape verbatim — snake_case preserved)
    // ═══════════════════════════════════════════════════════════════════════

    export type AgentStatus = 'pending' | 'running' | 'complete' | 'failed' | 'paused';

    export interface Agent {
      id: string;
      role: string;
      status: AgentStatus;
      task_description: string;
      started_at?: number;
      progress?: number;
      [k: string]: unknown;
    }

    export interface BackgroundAgent {
      id: string;
      kind: string;             // e.g. "claude-code" | "aider" | "goose"
      task: string;
      status: AgentStatus;
      output?: string;
      started_at?: number;
      completed_at?: number;
      [k: string]: unknown;
    }

    export interface SwarmStep {
      id: string;
      agent_role: string;
      task: string;
      deps: string[];
      status?: AgentStatus;
      [k: string]: unknown;
    }

    export interface Swarm {
      id: string;
      goal: string;
      steps: SwarmStep[];
      status: 'pending' | 'running' | 'paused' | 'complete' | 'failed';
      created_at?: number;
      [k: string]: unknown;
    }

    export interface SwarmProgress {
      completed: number;
      total: number;
      current_step_id?: string;
      [k: string]: unknown;
    }

    export interface FactoryAgent {
      id: string;
      name: string;
      role: string;
      [k: string]: unknown;
    }

    // ═══════════════════════════════════════════════════════════════════════
    // agent_commands.rs wrappers
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/agent_commands.rs:228 agent_create */
    export function agentCreate(args: {
      agentType: string;
      taskDescription: string;
      priority?: 'low' | 'normal' | 'high';
    }): Promise<Agent> {
      return invokeTyped<Agent>('agent_create', {
        agent_type: args.agentType,
        task_description: args.taskDescription,
        priority: args.priority,
      });
    }

    /** @see src-tauri/src/agent_commands.rs:295 agent_create_desktop */
    export function agentCreateDesktop(args: {
      taskDescription: string;
      tools?: string[];
    }): Promise<Agent> {
      return invokeTyped<Agent>('agent_create_desktop', {
        task_description: args.taskDescription,
        tools: args.tools,
      });
    }

    /** @see src-tauri/src/agent_commands.rs:2605 agent_list */
    export function agentList(): Promise<Agent[]> {
      return invokeTyped<Agent[]>('agent_list', {});
    }

    /** @see src-tauri/src/agent_commands.rs:2611 agent_get */
    export function agentGet(id: string): Promise<Agent | null> {
      return invokeTyped<Agent | null>('agent_get', { id });
    }

    /** @see src-tauri/src/agent_commands.rs:2622 agent_pause */
    export function agentPause(id: string): Promise<void> {
      return invokeTyped<void>('agent_pause', { id });
    }

    /** @see src-tauri/src/agent_commands.rs:2632 agent_resume */
    export function agentResume(id: string): Promise<void> {
      return invokeTyped<void>('agent_resume', { id });
    }

    /** @see src-tauri/src/agent_commands.rs:2702 agent_cancel */
    export function agentCancel(id: string): Promise<void> {
      return invokeTyped<void>('agent_cancel', { id });
    }

    /** @see src-tauri/src/agent_commands.rs:2712 agent_respond_desktop_action */
    export function agentRespondDesktopAction(args: {
      actionId: string;
      response: 'approve' | 'deny';
    }): Promise<void> {
      return invokeTyped<void>('agent_respond_desktop_action', {
        action_id: args.actionId,
        response: args.response,
      });
    }

    // ═══════════════════════════════════════════════════════════════════════
    // background_agent.rs wrappers
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/background_agent.rs:152 agent_spawn */
    export function agentSpawn(args: {
      agentKind: string;     // "claude-code" | "aider" | "goose" | ...
      task: string;
      workingDir?: string;
    }): Promise<BackgroundAgent> {
      return invokeTyped<BackgroundAgent>('agent_spawn', {
        agent_kind: args.agentKind,
        task: args.task,
        working_dir: args.workingDir,
      });
    }

    /** @see src-tauri/src/background_agent.rs:377 agent_list_background */
    export function agentListBackground(): Promise<BackgroundAgent[]> {
      return invokeTyped<BackgroundAgent[]>('agent_list_background', {});
    }

    /** @see src-tauri/src/background_agent.rs:386 agent_get_background */
    export function agentGetBackground(id: string): Promise<BackgroundAgent | null> {
      return invokeTyped<BackgroundAgent | null>('agent_get_background', { id });
    }

    /** @see src-tauri/src/background_agent.rs:393 agent_cancel_background */
    export function agentCancelBackground(id: string): Promise<void> {
      return invokeTyped<void>('agent_cancel_background', { id });
    }

    /** @see src-tauri/src/background_agent.rs:405 agent_detect_available */
    export function agentDetectAvailable(): Promise<string[]> {
      return invokeTyped<string[]>('agent_detect_available', {});
    }

    /** @see src-tauri/src/background_agent.rs:411 agent_get_output */
    export function agentGetOutput(id: string): Promise<string> {
      return invokeTyped<string>('agent_get_output', { id });
    }

    /** @see src-tauri/src/background_agent.rs:644 get_active_agents */
    export function getActiveAgents(): Promise<BackgroundAgent[]> {
      return invokeTyped<BackgroundAgent[]>('get_active_agents', {});
    }

    /** @see src-tauri/src/background_agent.rs:711 agent_auto_spawn */
    export function agentAutoSpawn(task: string): Promise<BackgroundAgent> {
      return invokeTyped<BackgroundAgent>('agent_auto_spawn', { task });
    }

    /** @see src-tauri/src/background_agent.rs:721 agent_spawn_codex */
    export function agentSpawnCodex(task: string): Promise<BackgroundAgent> {
      return invokeTyped<BackgroundAgent>('agent_spawn_codex', { task });
    }

    // ═══════════════════════════════════════════════════════════════════════
    // swarm_commands.rs wrappers
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/swarm_commands.rs:470 swarm_create */
    export function swarmCreate(args: { goal: string; maxSteps?: number }): Promise<Swarm> {
      return invokeTyped<Swarm>('swarm_create', { goal: args.goal, max_steps: args.maxSteps });
    }

    /** @see src-tauri/src/swarm_commands.rs:541 swarm_list */
    export function swarmList(limit?: number): Promise<Swarm[]> {
      return invokeTyped<Swarm[]>('swarm_list', { limit });
    }

    /** @see src-tauri/src/swarm_commands.rs:546 swarm_get */
    export function swarmGet(swarmId: string): Promise<Swarm | null> {
      return invokeTyped<Swarm | null>('swarm_get', { swarm_id: swarmId });
    }

    /** @see src-tauri/src/swarm_commands.rs:551 swarm_pause */
    export function swarmPause(swarmId: string): Promise<void> {
      return invokeTyped<void>('swarm_pause', { swarm_id: swarmId });
    }

    /** @see src-tauri/src/swarm_commands.rs:557 swarm_resume */
    export function swarmResume(swarmId: string): Promise<void> {
      return invokeTyped<void>('swarm_resume', { swarm_id: swarmId });
    }

    /** @see src-tauri/src/swarm_commands.rs:574 swarm_cancel */
    export function swarmCancel(swarmId: string): Promise<void> {
      return invokeTyped<void>('swarm_cancel', { swarm_id: swarmId });
    }

    /** @see src-tauri/src/swarm_commands.rs:580 swarm_write_scratchpad */
    export function swarmWriteScratchpad(args: {
      swarmId: string;
      key: string;
      value: string;
    }): Promise<void> {
      return invokeTyped<void>('swarm_write_scratchpad', {
        swarm_id: args.swarmId,
        key: args.key,
        value: args.value,
      });
    }

    /** @see src-tauri/src/swarm_commands.rs:589 swarm_write_scratchpad_entry */
    export function swarmWriteScratchpadEntry(args: {
      swarmId: string;
      key: string;
      value: string;
      appendedBy?: string;
    }): Promise<void> {
      return invokeTyped<void>('swarm_write_scratchpad_entry', {
        swarm_id: args.swarmId,
        key: args.key,
        value: args.value,
        appended_by: args.appendedBy,
      });
    }

    /** @see src-tauri/src/swarm_commands.rs:609 swarm_read_scratchpad */
    export function swarmReadScratchpad(args: {
      swarmId: string;
      key: string;
    }): Promise<string | null> {
      return invokeTyped<string | null>('swarm_read_scratchpad', {
        swarm_id: args.swarmId,
        key: args.key,
      });
    }

    /** @see src-tauri/src/swarm_commands.rs:617 swarm_get_progress */
    export function swarmGetProgress(swarmId: string): Promise<SwarmProgress | null> {
      return invokeTyped<SwarmProgress | null>('swarm_get_progress', { swarm_id: swarmId });
    }

    // ═══════════════════════════════════════════════════════════════════════
    // agent_factory.rs wrappers
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/agent_factory.rs factory_create_agent (exact line — verify on read) */
    export function factoryCreateAgent(args: {
      name: string;
      role: string;
      description?: string;
    }): Promise<FactoryAgent> {
      return invokeTyped<FactoryAgent>('factory_create_agent', {
        name: args.name,
        role: args.role,
        description: args.description,
      });
    }

    /** @see src-tauri/src/agent_factory.rs factory_deploy_agent */
    export function factoryDeployAgent(id: string): Promise<void> {
      return invokeTyped<void>('factory_deploy_agent', { id });
    }

    /** @see src-tauri/src/agent_factory.rs factory_list_agents */
    export function factoryListAgents(): Promise<FactoryAgent[]> {
      return invokeTyped<FactoryAgent[]>('factory_list_agents', {});
    }

    /** @see src-tauri/src/agent_factory.rs factory_pause_agent */
    export function factoryPauseAgent(id: string): Promise<void> {
      return invokeTyped<void>('factory_pause_agent', { id });
    }

    /** @see src-tauri/src/agent_factory.rs factory_delete_agent */
    export function factoryDeleteAgent(id: string): Promise<void> {
      return invokeTyped<void>('factory_delete_agent', { id });
    }

    // ═══════════════════════════════════════════════════════════════════════
    // managed_agents.rs wrappers
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/managed_agents.rs:23 run_managed_agent */
    export function runManagedAgent(args: {
      kind: string;
      task: string;
    }): Promise<string> {
      return invokeTyped<string>('run_managed_agent', { kind: args.kind, task: args.task });
    }
    ```

    **Adjust wrapper signatures to MATCH the actual Rust signatures** — when reading `agent_commands.rs`, `background_agent.rs`, `swarm_commands.rs`, `agent_factory.rs`, `managed_agents.rs` in the `<read_first>` step, preserve the Rust parameter names (converted snake_case → invoke) and return types. If any Rust fn signature differs from the draft above, the ACTUAL Rust signature wins — update the wrapper verbatim and add a note in the JSDoc explaining the correction.

    Then create `src/features/agents/types.ts`:

    ```ts
    // src/features/agents/types.ts
    // Cluster-local barrel — re-exports Tauri wrapper types + UI-only types.
    // @see .planning/phases/05-agents-knowledge/05-CONTEXT.md §D-128

    export type { Agent, AgentStatus, BackgroundAgent, Swarm, SwarmStep, SwarmProgress, FactoryAgent } from '@/lib/tauri/agents';

    export type AgentFilterStatus = 'all' | 'running' | 'idle' | 'failed';

    export interface TimelineRow {
      seq: number;
      ts: number;
      event: string;
      agentId: string;
      preview: string;
    }
    ```

    **CRITICAL — CLAUDE.md rules:**
    - Raw `invoke()` import is BANNED in this file (ESLint no-raw-tauri). Every wrapper routes through `invokeTyped`.
    - Argument key conversion camelCase → snake_case happens AT EACH wrapper's invoke call — the `invokeTyped` base does not transform keys.
    - When a Rust return type is complex, prefer `[k: string]: unknown` index signature on the interface to stay forward-compatible (matches D-38-payload accepted risk).
  </action>
  <verify>
    <automated>grep -c "export function" src/lib/tauri/agents.ts && grep -c "@see src-tauri" src/lib/tauri/agents.ts && grep -c "invokeTyped" src/lib/tauri/agents.ts && test -f src/features/agents/types.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    agents.ts has ≥30 export function declarations, ≥30 @see JSDoc lines, and ≥30 invokeTyped call sites. types.ts exists. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/tauri/knowledge.ts with 52 typed wrappers + src/features/knowledge/types.ts barrel</name>
  <files>src/lib/tauri/knowledge.ts, src/features/knowledge/types.ts</files>
  <read_first>
    - /home/arnav/blade/src-tauri/src/db_commands.rs (lines 111-220 for db_knowledge + db_templates #[tauri::command] signatures)
    - /home/arnav/blade/src-tauri/src/embeddings.rs (lines 444-490)
    - /home/arnav/blade/src-tauri/src/knowledge_graph.rs (lines 856-914)
    - /home/arnav/blade/src-tauri/src/memory_palace.rs (grep #[tauri::command] to find all 6 signatures)
    - /home/arnav/blade/src-tauri/src/typed_memory.rs (lines 544-581)
    - /home/arnav/blade/src-tauri/src/screen_timeline_commands.rs (grep #[tauri::command] — 14 commands per lib.rs:923-936)
    - /home/arnav/blade/src-tauri/src/document_intelligence.rs (grep #[tauri::command] — 8 commands per lib.rs:1115-1122)
    - /home/arnav/blade/src-tauri/src/memory.rs (lines 439-800 — 4 #[tauri::command] entries)
    - /home/arnav/blade/src/lib/tauri/agents.ts (just-created file from Task 1 — same recipe)
  </read_first>
  <action>
    Create `src/lib/tauri/knowledge.ts` (~700-800 lines expected, single file). Structure mirrors Task 1's agents.ts: section headers per Rust module, one exported camelCase function per `#[tauri::command]`, JSDoc cite, `invokeTyped` call, camelCase→snake_case arg keys.

    Types section at the top:
    ```ts
    export interface KnowledgeEntry {
      id: string;
      title: string;
      content: string;
      tags: string[];
      created_at: number;
      updated_at: number;
      [k: string]: unknown;
    }

    export interface KnowledgeTemplate {
      id: string;
      name: string;
      body: string;
      usage_count: number;
      [k: string]: unknown;
    }

    export interface SemanticHit {
      id: string;
      score: number;
      content: string;
      metadata?: Record<string, unknown>;
    }

    export interface GraphNode {
      id: string;
      label: string;
      concept?: string;
      tag?: string;
      [k: string]: unknown;
    }

    export interface GraphEdge {
      from: string;
      to: string;
      weight?: number;
      [k: string]: unknown;
    }

    export interface GraphStats {
      node_count: number;
      edge_count: number;
      top_concepts?: Array<{ concept: string; count: number }>;
      [k: string]: unknown;
    }

    export type MemoryCategory = 'Fact' | 'Preference' | 'Decision' | 'Skill' | 'Goal' | 'Routine' | 'Relationship';

    export interface TypedMemory {
      id: string;
      category: MemoryCategory;
      content: string;
      confidence?: number;
      created_at: number;
      [k: string]: unknown;
    }

    export interface MemoryEntry {
      id: string;
      content: string;
      category?: string;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface TimelineEntry {
      id: string;
      timestamp: number;
      screenshot_path?: string;
      thumbnail_path?: string;
      ocr_text?: string;
      active_app?: string;
      window_title?: string;
      [k: string]: unknown;
    }

    export interface TimelineStats {
      total_screenshots: number;
      total_bytes: number;
      oldest_ts?: number;
      newest_ts?: number;
      [k: string]: unknown;
    }

    export interface DocumentEntry {
      id: string;
      title: string;
      path: string;
      ingested_at: number;
      chunk_count?: number;
      [k: string]: unknown;
    }
    ```

    Wrapper sections (one section per Rust module, matching lib.rs order):

    ```ts
    // ═══════════════════════════════════════════════════════════════════════
    // db_commands.rs — knowledge entries + templates
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/db_commands.rs:111 db_list_knowledge */
    export function dbListKnowledge(limit?: number): Promise<KnowledgeEntry[]> {
      return invokeTyped<KnowledgeEntry[]>('db_list_knowledge', { limit });
    }

    /** @see src-tauri/src/db_commands.rs:119 db_get_knowledge */
    export function dbGetKnowledge(id: string): Promise<KnowledgeEntry | null> {
      return invokeTyped<KnowledgeEntry | null>('db_get_knowledge', { id });
    }

    // ... (continue for all 9 db_*_knowledge + 4 db_*_template commands)

    // ═══════════════════════════════════════════════════════════════════════
    // embeddings.rs
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/embeddings.rs:444 embed_and_store */
    export function embedAndStore(args: { text: string; metadata?: Record<string, unknown> }): Promise<void> {
      return invokeTyped<void>('embed_and_store', { text: args.text, metadata: args.metadata });
    }

    /** @see src-tauri/src/embeddings.rs:472 semantic_search */
    export function semanticSearch(args: { query: string; limit?: number }): Promise<SemanticHit[]> {
      return invokeTyped<SemanticHit[]>('semantic_search', { query: args.query, limit: args.limit });
    }

    /** @see src-tauri/src/embeddings.rs:488 vector_store_size */
    export function vectorStoreSize(): Promise<number> {
      return invokeTyped<number>('vector_store_size', {});
    }

    // ═══════════════════════════════════════════════════════════════════════
    // knowledge_graph.rs
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/knowledge_graph.rs:856 graph_add_node */
    export function graphAddNode(args: {
      id: string;
      label: string;
      concept?: string;
      tags?: string[];
    }): Promise<void> { /* ... */ }

    // ... etc for all 8 graph_* commands

    // ═══════════════════════════════════════════════════════════════════════
    // memory_palace.rs (6 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/memory_palace.rs memory_search */
    export function memorySearch(args: { query: string; limit?: number }): Promise<MemoryEntry[]> {
      return invokeTyped<MemoryEntry[]>('memory_search', { query: args.query, limit: args.limit });
    }

    // ... etc for memory_get_recent, memory_recall, memory_add_manual, memory_delete, memory_consolidate_now

    // ═══════════════════════════════════════════════════════════════════════
    // typed_memory.rs (5 commands — D-125 AgentDetail does NOT use these, but MemoryPalace does)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/typed_memory.rs:544 memory_store_typed */
    export function memoryStoreTyped(args: {
      category: MemoryCategory;
      content: string;
      confidence?: number;
    }): Promise<void> { /* ... */ }

    /** @see src-tauri/src/typed_memory.rs:557 memory_recall_category */
    export function memoryRecallCategory(args: { category: MemoryCategory; limit?: number }): Promise<TypedMemory[]> {
      return invokeTyped<TypedMemory[]>('memory_recall_category', {
        category: args.category,
        limit: args.limit,
      });
    }

    // ... memory_get_all_typed, memory_delete_typed, memory_generate_user_summary

    // ═══════════════════════════════════════════════════════════════════════
    // screen_timeline_commands.rs (14 commands — per lib.rs:923-936)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/screen_timeline_commands.rs timeline_search_cmd */
    export function timelineSearchCmd(args: { query: string; limit?: number }): Promise<TimelineEntry[]> { /* ... */ }

    /** @see src-tauri/src/screen_timeline_commands.rs timeline_browse_cmd */
    export function timelineBrowseCmd(args: {
      cursor?: number;
      limit?: number;
    }): Promise<TimelineEntry[]> { /* ... */ }

    // ... 12 more timeline_* wrappers

    // ═══════════════════════════════════════════════════════════════════════
    // document_intelligence.rs (8 commands — per lib.rs:1115-1122)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/document_intelligence.rs doc_ingest */
    export function docIngest(args: { path: string; title?: string }): Promise<DocumentEntry> { /* ... */ }

    /** @see src-tauri/src/document_intelligence.rs doc_search */
    export function docSearch(args: { query: string; limit?: number }): Promise<DocumentEntry[]> { /* ... */ }

    // ... doc_get, doc_list, doc_delete, doc_answer_question, doc_cross_synthesis, doc_generate_study_notes

    // ═══════════════════════════════════════════════════════════════════════
    // memory.rs (4 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/memory.rs:576 get_memory_log */
    export function getMemoryLog(): Promise<unknown[]> { /* ... */ }

    /** @see src-tauri/src/memory.rs:787 get_memory_blocks */
    export function getMemoryBlocks(): Promise<Record<string, unknown>> { /* ... */ }

    /** @see src-tauri/src/memory.rs:798 set_memory_block */
    export function setMemoryBlock(args: { block: string; content: string }): Promise<void> { /* ... */ }

    /** @see src-tauri/src/memory.rs:781 run_weekly_memory_consolidation */
    export function runWeeklyMemoryConsolidation(): Promise<string> { /* ... */ }
    ```

    **Adjust signatures based on actual Rust reads.** If any Rust signature differs, the ACTUAL Rust wins — update the wrapper and note the correction in JSDoc.

    Then create `src/features/knowledge/types.ts`:

    ```ts
    // src/features/knowledge/types.ts
    // Cluster-local barrel.

    export type {
      KnowledgeEntry, KnowledgeTemplate, SemanticHit, GraphNode, GraphEdge, GraphStats,
      MemoryCategory, TypedMemory, MemoryEntry, TimelineEntry, TimelineStats, DocumentEntry,
    } from '@/lib/tauri/knowledge';

    export type KnowledgeGroupSource = 'knowledge' | 'memory' | 'timeline';

    export interface KnowledgeSearchGroup {
      source: KnowledgeGroupSource;
      label: string;
      results: Array<{ id: string; title: string; preview: string; score?: number }>;
    }
    ```

    **CRITICAL — CLAUDE.md rules:**
    - Raw invoke BANNED.
    - camelCase JS API; snake_case wire args.
    - Every wrapper: one @see JSDoc line + one invokeTyped call.
    - File stays in ONE FILE (no splitting into sub-modules) — makes Plan 05-05/05-06 imports deterministic.
  </action>
  <verify>
    <automated>grep -c "export function" src/lib/tauri/knowledge.ts && grep -c "@see src-tauri" src/lib/tauri/knowledge.ts && grep -c "invokeTyped" src/lib/tauri/knowledge.ts && test -f src/features/knowledge/types.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    knowledge.ts has ≥48 export function declarations (allow ±5 for signature merge), ≥48 @see JSDoc lines, and ≥48 invokeTyped call sites. types.ts exists. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite feature index files + seed 18 per-route placeholder components + create cluster CSS + add status tokens + update tauri barrel</name>
  <files>src/features/agents/index.tsx, src/features/knowledge/index.tsx, src/features/agents/AgentDashboard.tsx, src/features/agents/AgentDetail.tsx, src/features/agents/AgentFactory.tsx, src/features/agents/AgentTeam.tsx, src/features/agents/AgentTimeline.tsx, src/features/agents/BackgroundAgents.tsx, src/features/agents/TaskAgents.tsx, src/features/agents/SwarmView.tsx, src/features/agents/AgentPixelWorld.tsx, src/features/agents/agents.css, src/features/knowledge/KnowledgeBase.tsx, src/features/knowledge/KnowledgeGraph.tsx, src/features/knowledge/MemoryPalace.tsx, src/features/knowledge/ScreenTimeline.tsx, src/features/knowledge/RewindTimeline.tsx, src/features/knowledge/LiveNotes.tsx, src/features/knowledge/DailyLog.tsx, src/features/knowledge/ConversationInsights.tsx, src/features/knowledge/CodebaseExplorer.tsx, src/features/knowledge/knowledge.css, src/styles/tokens.css, src/lib/tauri/index.ts</files>
  <read_first>
    - /home/arnav/blade/src/features/agents/index.tsx (current 9-stub version — 29 lines)
    - /home/arnav/blade/src/features/knowledge/index.tsx (current 9-stub version — 28 lines)
    - /home/arnav/blade/src/lib/router.ts (RouteDefinition interface)
    - /home/arnav/blade/src/design-system/primitives/index.ts (to import GlassPanel + ComingSoonSkeleton)
    - /home/arnav/blade/src/styles/tokens.css (see where to add --status-* tokens)
    - /home/arnav/blade/src/lib/tauri/index.ts (barrel — see current exports)
    - /home/arnav/blade/.planning/phases/05-agents-knowledge/05-PATTERNS.md §5, §10
    - /home/arnav/blade/.planning/phases/05-agents-knowledge/05-CONTEXT.md §D-122, §D-131, §D-132, §D-133
  </read_first>
  <action>
    **Sub-task 3a — Rewrite src/features/agents/index.tsx (exactly per Pattern §5):**

    ```tsx
    // src/features/agents/index.tsx — Phase 5 rewrite (Plan 05-02, D-122 single-writer)
    // Phase 1 ComingSoonSkeleton stubs replaced with lazy imports of real route components.
    // @see .planning/phases/05-agents-knowledge/05-CONTEXT.md §D-122
    // @see .planning/REQUIREMENTS.md §AGENT-01..09

    import { lazy } from 'react';
    import type { RouteDefinition } from '@/lib/router';

    const AgentDashboard   = lazy(() => import('./AgentDashboard').then((m) => ({ default: m.AgentDashboard })));
    const AgentDetail      = lazy(() => import('./AgentDetail').then((m) => ({ default: m.AgentDetail })));
    const AgentFactory     = lazy(() => import('./AgentFactory').then((m) => ({ default: m.AgentFactory })));
    const AgentTeam        = lazy(() => import('./AgentTeam').then((m) => ({ default: m.AgentTeam })));
    const AgentTimeline    = lazy(() => import('./AgentTimeline').then((m) => ({ default: m.AgentTimeline })));
    const BackgroundAgents = lazy(() => import('./BackgroundAgents').then((m) => ({ default: m.BackgroundAgents })));
    const TaskAgents       = lazy(() => import('./TaskAgents').then((m) => ({ default: m.TaskAgents })));
    const SwarmView        = lazy(() => import('./SwarmView').then((m) => ({ default: m.SwarmView })));
    const AgentPixelWorld  = lazy(() => import('./AgentPixelWorld').then((m) => ({ default: m.AgentPixelWorld })));

    export const routes: RouteDefinition[] = [
      { id: 'agents',            label: 'Agents',            section: 'agents', component: AgentDashboard,   phase: 5, description: 'Running + idle agents' },
      { id: 'agent-detail',      label: 'Agent Detail',      section: 'agents', component: AgentDetail,      phase: 5 },
      { id: 'agent-factory',     label: 'Agent Factory',     section: 'agents', component: AgentFactory,     phase: 5 },
      { id: 'agent-team',        label: 'Agent Team',        section: 'agents', component: AgentTeam,        phase: 5 },
      { id: 'agent-timeline',    label: 'Agent Timeline',    section: 'agents', component: AgentTimeline,    phase: 5 },
      { id: 'background-agents', label: 'Background Agents', section: 'agents', component: BackgroundAgents, phase: 5 },
      { id: 'task-agents',       label: 'Task Agents',       section: 'agents', component: TaskAgents,       phase: 5 },
      { id: 'swarm-view',        label: 'Swarm',             section: 'agents', component: SwarmView,        phase: 5 },
      { id: 'agent-pixel-world', label: 'Pixel World',       section: 'agents', component: AgentPixelWorld,  phase: 5 },
    ];
    ```

    **Sub-task 3b — Rewrite src/features/knowledge/index.tsx** (same pattern, 9 routes):

    ```tsx
    // src/features/knowledge/index.tsx — Phase 5 rewrite (Plan 05-02)
    import { lazy } from 'react';
    import type { RouteDefinition } from '@/lib/router';

    const KnowledgeBase        = lazy(() => import('./KnowledgeBase').then((m) => ({ default: m.KnowledgeBase })));
    const KnowledgeGraph       = lazy(() => import('./KnowledgeGraph').then((m) => ({ default: m.KnowledgeGraph })));
    const MemoryPalace         = lazy(() => import('./MemoryPalace').then((m) => ({ default: m.MemoryPalace })));
    const ScreenTimeline       = lazy(() => import('./ScreenTimeline').then((m) => ({ default: m.ScreenTimeline })));
    const RewindTimeline       = lazy(() => import('./RewindTimeline').then((m) => ({ default: m.RewindTimeline })));
    const LiveNotes            = lazy(() => import('./LiveNotes').then((m) => ({ default: m.LiveNotes })));
    const DailyLog             = lazy(() => import('./DailyLog').then((m) => ({ default: m.DailyLog })));
    const ConversationInsights = lazy(() => import('./ConversationInsights').then((m) => ({ default: m.ConversationInsights })));
    const CodebaseExplorer     = lazy(() => import('./CodebaseExplorer').then((m) => ({ default: m.CodebaseExplorer })));

    export const routes: RouteDefinition[] = [
      { id: 'knowledge-base',        label: 'Knowledge Base',        section: 'knowledge', component: KnowledgeBase,        phase: 5 },
      { id: 'knowledge-graph',       label: 'Knowledge Graph',       section: 'knowledge', component: KnowledgeGraph,       phase: 5 },
      { id: 'memory-palace',         label: 'Memory Palace',         section: 'knowledge', component: MemoryPalace,         phase: 5 },
      { id: 'screen-timeline',       label: 'Screen Timeline',       section: 'knowledge', component: ScreenTimeline,       phase: 5 },
      { id: 'rewind-timeline',       label: 'Rewind',                section: 'knowledge', component: RewindTimeline,       phase: 5 },
      { id: 'live-notes',            label: 'Live Notes',            section: 'knowledge', component: LiveNotes,            phase: 5 },
      { id: 'daily-log',             label: 'Daily Log',             section: 'knowledge', component: DailyLog,             phase: 5 },
      { id: 'conversation-insights', label: 'Conversation Insights', section: 'knowledge', component: ConversationInsights, phase: 5 },
      { id: 'codebase-explorer',     label: 'Codebase Explorer',     section: 'knowledge', component: CodebaseExplorer,     phase: 5 },
    ];
    ```

    **Sub-task 3c — Create 18 per-route placeholder files** (9 agents + 9 knowledge). Each file is a minimal named export that Plans 05-03..06 will fill in. Template per file (substitute NAME + LABEL + PLAN):

    ```tsx
    // src/features/agents/AgentDashboard.tsx — placeholder shipped by Plan 05-02
    // Real body ships in Plan 05-03.
    import { GlassPanel } from '@/design-system/primitives';
    import './agents.css';

    export function AgentDashboard() {
      return (
        <GlassPanel tier={1} className="agents-surface">
          <div className="agents-placeholder" data-testid="agent-dashboard-placeholder">
            <h2>Agents</h2>
            <p className="agents-placeholder-hint">Ships in Plan 05-03.</p>
          </div>
        </GlassPanel>
      );
    }
    ```

    Create a placeholder for each route:
    - agents: AgentDashboard (Plan 05-03), AgentDetail (05-03), AgentTeam (05-03), BackgroundAgents (05-03), AgentFactory (05-04), AgentTimeline (05-04), TaskAgents (05-04), SwarmView (05-04), AgentPixelWorld (05-04)
    - knowledge: KnowledgeBase (05-05), KnowledgeGraph (05-05), ScreenTimeline (05-05), RewindTimeline (05-05 — shares logic), MemoryPalace (05-06), LiveNotes (05-06), DailyLog (05-06), ConversationInsights (05-06), CodebaseExplorer (05-06)

    The placeholder hint must identify which plan ships the body so Plans 05-03..06 replace it cleanly.

    **Sub-task 3d — Create src/features/agents/agents.css:**

    ```css
    /* src/features/agents/agents.css — Plan 05-02 shared base
       Plans 05-03 and 05-04 EXTEND (append) this file; never replace.
       @see .planning/phases/05-agents-knowledge/05-PATTERNS.md §10, §D-132 */

    @layer features {
      .agents-surface {
        padding: var(--sp-4);
        height: 100%;
        overflow-y: auto;
      }

      .agents-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--sp-2);
        padding: var(--sp-6);
        text-align: center;
      }

      .agents-placeholder-hint {
        color: var(--t-3);
        font-size: 14px;
      }

      .agent-card {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-3);
        display: flex;
        flex-direction: column;
        gap: var(--sp-2);
        transition: border-color 180ms var(--ease-out);
      }

      .agent-card:hover {
        border-color: rgba(255, 255, 255, 0.18);
      }

      .agent-card[data-status="running"]  { border-left: 3px solid var(--status-running); }
      .agent-card[data-status="idle"]     { border-left: 3px solid var(--status-idle); }
      .agent-card[data-status="complete"] { border-left: 3px solid var(--status-success); }
      .agent-card[data-status="failed"]   { border-left: 3px solid var(--status-error); }
    }
    ```

    **Sub-task 3e — Create src/features/knowledge/knowledge.css** (parallel structure — substitute "knowledge" for "agents"):

    ```css
    /* src/features/knowledge/knowledge.css — Plan 05-02 shared base */

    @layer features {
      .knowledge-surface {
        padding: var(--sp-4);
        height: 100%;
        overflow-y: auto;
      }

      .knowledge-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--sp-2);
        padding: var(--sp-6);
        text-align: center;
      }

      .knowledge-placeholder-hint { color: var(--t-3); font-size: 14px; }

      .knowledge-card {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-3);
      }
    }
    ```

    Also update the knowledge placeholders to import './knowledge.css' and use the `.knowledge-surface` + `.knowledge-placeholder` classes (mirror the agents placeholder template).

    **Sub-task 3f — Add status tokens to src/styles/tokens.css.** Find the existing status/color token section (or add one near the bottom, just before any `@theme` block) and add:

    ```css
    /* Agent + Swarm step status colors (Phase 5 Plan 05-02 addition) */
    :root {
      --status-idle:    rgba(255, 255, 255, 0.3);
      --status-running: #8affc7;
      --status-success: #a6ffd2;
      --status-error:   #ff6b6b;
    }
    ```

    (If tokens.css is organized by concern per Phase 1 D-22, add these to whichever file is canonical for color tokens — verify by reading the file first.)

    **Sub-task 3g — Update src/lib/tauri/index.ts barrel** to re-export the new modules:

    ```ts
    // Append to existing barrel (before or after existing re-exports — match the file's convention)
    export * as agents from './agents';
    export * as knowledge from './knowledge';
    ```

    (If the file's convention is individual re-exports rather than namespace wildcards, match that convention instead.)

    **CRITICAL — CLAUDE.md + Phase discipline:**
    - D-122: This plan is the ONLY plan that edits `src/features/agents/index.tsx` and `src/features/knowledge/index.tsx`. Downstream plans replace the BODY of the 18 per-route files — they do NOT touch the 2 index.tsx files.
    - D-131: Per-route file names match the index.tsx imports exactly. Case-sensitive.
    - D-132: Cluster CSS files are created ONCE here; Plans 05-03..06 append their own rules using `@layer features`.
    - All primitives imported from `@/design-system/primitives` (the barrel export) — not from individual files. This keeps Phase 1 D-35 location contract intact.
  </action>
  <verify>
    <automated>test -f src/features/agents/AgentDashboard.tsx && test -f src/features/agents/AgentDetail.tsx && test -f src/features/agents/AgentFactory.tsx && test -f src/features/agents/AgentTeam.tsx && test -f src/features/agents/AgentTimeline.tsx && test -f src/features/agents/BackgroundAgents.tsx && test -f src/features/agents/TaskAgents.tsx && test -f src/features/agents/SwarmView.tsx && test -f src/features/agents/AgentPixelWorld.tsx && test -f src/features/agents/agents.css && test -f src/features/knowledge/KnowledgeBase.tsx && test -f src/features/knowledge/KnowledgeGraph.tsx && test -f src/features/knowledge/MemoryPalace.tsx && test -f src/features/knowledge/ScreenTimeline.tsx && test -f src/features/knowledge/RewindTimeline.tsx && test -f src/features/knowledge/LiveNotes.tsx && test -f src/features/knowledge/DailyLog.tsx && test -f src/features/knowledge/ConversationInsights.tsx && test -f src/features/knowledge/CodebaseExplorer.tsx && test -f src/features/knowledge/knowledge.css && grep -q "status-running" src/styles/tokens.css && npx tsc --noEmit</automated>
  </verify>
  <done>
    All 18 per-route placeholder files exist; both cluster index.tsx rewritten with lazy imports; both CSS files created; status tokens added; barrel updated; `npx tsc --noEmit` passes; `npm run verify:all` passes (migration-ledger + entries checks both still green).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes.
- `npm run verify:all` passes — migration-ledger still consistent (routes unchanged), entries check still passes, no-raw-tauri rule still green (every wrapper uses invokeTyped).
- ESLint passes (no-raw-tauri rule enforced).
- `npm run dev` builds the frontend — lazy imports resolve at first load (browser won't error on the placeholder routes).
- Route count unchanged in migration-ledger (9+9 = 18 Phase 5 routes).
</verification>

<success_criteria>
- `src/lib/tauri/agents.ts` exports ≥30 camelCase wrapper functions, each with JSDoc @see citation.
- `src/lib/tauri/knowledge.ts` exports ≥48 camelCase wrapper functions, each with JSDoc @see citation.
- Both cluster feature `index.tsx` files rewritten to lazy-import 9 real components each.
- 18 per-route placeholder files exist and render a glass panel + placeholder hint naming the follow-up plan.
- Two cluster CSS files exist with shared base classes; 4 CSS status tokens added.
- Types barrel files exist for both clusters.
- Tauri library barrel re-exports both new wrapper modules.
- Zero Rust file edits.
- Zero behavior regressions in existing Phase 1..4 tests.
</success_criteria>

<threat_model>
## Trust Boundaries

- **JS → Rust invoke boundary**: every wrapper crosses this. Args flow JS → serde → Rust param. Untrusted input originates in user-facing components (Plans 05-03..06).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | camelCase → snake_case arg mis-conversion | mitigate | Per-wrapper explicit mapping (not auto-derived). Code review catches drift. Each wrapper tested by Plan 05-07 Playwright specs. |
| T-05-02-02 | Information Disclosure | raw invoke bypasses wrappers | mitigate | ESLint `no-raw-tauri` rule enforces invokeTyped-only outside `src/lib/tauri/`. `scripts/verify-no-raw-tauri.sh` is CI backstop. |
| T-05-02-03 | Denial of Service | wrapper throws uncaught rejection under load | accept | TauriError class propagates; consumer components surface to ToastContext. Low risk — all commands are local IPC. |
| T-05-02-04 | Tampering | route placeholder could be left shipped if Plans 05-03..06 never run | mitigate | Plan 05-07 Playwright `cluster-routes` spec asserts real components mount (placeholder hint text absent). Regression caught in CI. |
</threat_model>

<output>
After completion, create `.planning/phases/05-agents-knowledge/05-02-SUMMARY.md` with:
- Exact count of exported wrappers per cluster (agents.ts + knowledge.ts).
- Which Rust module each wrapper group maps to.
- List of the 18 per-route files created (confirming placeholder content).
- Any Rust signature corrections made while reading the source (each correction noted so Plans 05-03..06 match reality).
- Barrel export update confirmed; status tokens added; CSS files created.
- Confirmation that `src/features/agents/index.tsx` and `src/features/knowledge/index.tsx` were the only index files touched (D-122 single-writer invariant held).
</output>
