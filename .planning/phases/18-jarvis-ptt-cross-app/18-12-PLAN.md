---
phase: 18
plan: 12
type: execute
wave: 5
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06", "18-07", "18-08", "18-09", "18-10", "18-11"]
files_modified:
  - .planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md
  - docs/testing ss/jarvis-cold-install-demo.png
autonomous: false
requirements:
  - JARVIS-03
  - JARVIS-04
  - JARVIS-05
  - JARVIS-06
  - JARVIS-07
  - JARVIS-08
  - JARVIS-09
  - JARVIS-10
  - JARVIS-11
  - JARVIS-12
tags: [verification, uat, cold-install-demo, blocking-checkpoint]
must_haves:
  truths:
    - "All static gates green: cargo check + cargo test --lib (full suite) + npx tsc --noEmit + npm run verify:all"
    - "Cold-install demo (JARVIS-12 e2e) executed: text chat → ConsentDialog → real cross-app write → ActivityStrip emission → screenshot saved + read back"
    - "Demo target = Linear (preferred per RESEARCH § Cold-Install Demo Viability — guaranteed creds path) OR Slack if operator has Slack MCP installed"
    - "Operator confirms the action is visible in the target service (issue created in Linear OR message posted in Slack)"
    - "UI-polish UAT (ConsentDialog visual fidelity, JarvisPill positioning) explicitly DEFERRED to v1.3 per chat-first pivot — recorded in 18-VERIFICATION.md"
    - "research/questions.md Q1 status closed (verified by grep)"
    - "10-WIRING-AUDIT.json gate green for all 7 new modules (verify-wiring-audit-shape passes)"
  artifacts:
    - path: ".planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md"
      provides: "Phase 18 verification report — must-haves matrix + REQ-to-evidence map + cold-install demo screenshot citation"
      min_lines: 100
    - path: "docs/testing ss/jarvis-cold-install-demo.png"
      provides: "Screenshot proving the e2e demo (Linear issue or Slack message visible after BLADE chat-driven action)"
  key_links:
    - from: "18-VERIFICATION.md must-haves matrix"
      to: "JARVIS-01..12 + STATE.md status update"
      via: "Phase close criteria"
      pattern: "All must-haves green"

threat_model_required: true
---

<objective>
Wave 5 Plan 12 — Verification + cold-install demo (the JARVIS-12 e2e success criterion).

**This plan is a BLOCKING CHECKPOINT** (autonomous: false). The cold-install demo MUST be operator-driven because:
- Real cross-app writes require real credentials in keyring
- Screenshot evidence MUST be visually verified by operator
- The chat-first pivot DEFERS UI-polish UAT but NOT the e2e write proof per CONTEXT § canonical_refs

**Purpose:**
- Static gates prove static correctness; the demo proves end-to-end behavior.
- Per CLAUDE.md Verification Protocol: "Static gates ≠ done." The v1.1 retraction lesson is load-bearing here.
- Per chat-first pivot operator framing: the cold-install demo is the SC, not polish.

**Output:**
- `18-VERIFICATION.md` with must-haves matrix + REQ-to-evidence map
- `docs/testing ss/jarvis-cold-install-demo.png` screenshot saved (literal-space directory) + Read-tool readback citation
- Static-gate evidence (cargo check + cargo test + tsc + verify:all)
- KNOWN-GAP documentation (UI-polish UAT deferred; one-shot consent path deferred)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-jarvis-ptt-cross-app/18-CONTEXT.md
@.planning/phases/18-jarvis-ptt-cross-app/18-RESEARCH.md
@.planning/phases/18-jarvis-ptt-cross-app/18-VALIDATION.md
@.planning/phases/18-jarvis-ptt-cross-app/18-01-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-02-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-03-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-04-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-05-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-06-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-07-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-08-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-09-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-10-SUMMARY.md
@.planning/phases/18-jarvis-ptt-cross-app/18-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all static gates and capture evidence</name>
  <read_first>
    - All Plan 01-11 SUMMARYs (loaded above) — confirm each shipped green
    - .planning/phases/18-jarvis-ptt-cross-app/18-VALIDATION.md (sampling rate + per-task verification map)
    - CLAUDE.md § Verification Protocol
  </read_first>
  <files>
    .planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md
  </files>
  <behavior>
    - Run cargo check, cargo test --lib (full crate), tsc, npm run verify:all
    - Capture exit codes + line counts of pass/fail per gate
    - Document each REQ → evidence mapping
    - Confirm 10-WIRING-AUDIT.json gate green
    - Confirm verify-emit-policy gate green (single-window emit_to throughout — no allowlist additions)
  </behavior>
  <action>
**Step 1.** Create `.planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md` with the static-gate evidence section. Run each command and capture output:

```bash
cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -5
cd /home/arnav/blade/src-tauri && cargo test --lib -- --nocapture --test-threads=1 2>&1 | tail -10
cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -5
cd /home/arnav/blade && npm run verify:all 2>&1 | tail -20
cd /home/arnav/blade && bash scripts/verify-eval.sh 2>&1 | tail -5
```

**Step 2.** Write `18-VERIFICATION.md` with this structure:

```markdown
---
phase: 18
status: pending  # → "verified" once cold-install demo passes (Task 2)
gate_count: 4    # cargo check + cargo test --lib + tsc --noEmit + verify:all (+ verify:eval already in chain)
created: 2026-04-30
---

# Phase 18 — Verification Report

## Static Gates (Task 1)

| Gate | Command | Status | Evidence |
|------|---------|--------|----------|
| cargo check | `cd src-tauri && cargo check` | ✅ / ❌ | exit code; last 5 lines of output |
| cargo test --lib | `cd src-tauri && cargo test --lib` | ✅ / ❌ | NN tests passed / NN failed |
| tsc --noEmit | `npx tsc --noEmit` | ✅ / ❌ | exit code |
| verify:all (chain count 31+) | `npm run verify:all` | ✅ / ❌ | each sub-gate's status |
| verify:eval | `bash scripts/verify-eval.sh` | ✅ / ❌ | (Phase 16 chain entry — Phase 18 doesn't change) |

## Requirement Coverage Matrix

| Req ID | Description | Plan(s) | Evidence | Status |
|--------|-------------|---------|----------|--------|
| JARVIS-01 | PTT global hotkey — DEFERRED to v1.3 per D-01 | Plan 13 (deferral doc) | "JARVIS-01 deferred to v1.3" — see 18-13-PLAN.md | DEFERRED |
| JARVIS-02 | Whisper STT — DEFERRED to v1.3 per D-01 | Plan 13 (deferral doc) | "JARVIS-02 deferred to v1.3" — see 18-13-PLAN.md | DEFERRED |
| JARVIS-03 | Intent classification | Plans 01, 06, 10 | intent_router::classify_intent unit tests + commands.rs wiring | ✅ |
| JARVIS-04 | Cross-app dispatch reuses observer creds | Plans 03, 07, 08, 09 | tentacle outbound bodies + dispatcher consent gate | ✅ |
| JARVIS-05 | Per-action consent dialog | Plans 04, 06, 09, 11 | consent_decisions table + consent_check + ConsentDialog component | ✅ |
| JARVIS-06 | Ego refusal regex (≥5 patterns) | Plans 01, 05 | 9 patterns in ego.rs::refusal_patterns + ≥6 unit tests | ✅ |
| JARVIS-07 | Capability_gap → log + auto_install + retry | Plans 02, 05, 10 | handle_refusal calls evolution_log_capability_gap + self_upgrade::auto_install | ✅ |
| JARVIS-08 | Retry cap = 1 per turn | Plans 05, 10 | RETRY_COUNT atomic + reset_retry_for_turn at function entry | ✅ |
| JARVIS-09 | research/questions.md Q1 closed | Plan 10 | grep "Status: closed" + verdict block | ✅ |
| JARVIS-10 | ActivityStrip emission per action turn | Plan 09 | emit_jarvis_activity D-17 LOCKED format | ✅ |
| JARVIS-11 | Inline JARVIS pill on intercept | Plans 04, 11 | JARVIS_INTERCEPT registry + JarvisPill.tsx + MessageList subscriber | ✅ |
| JARVIS-12 | Cold-install demo e2e | Plan 12 (this plan, Task 2) | docs/testing ss/jarvis-cold-install-demo.png + readback citation | ✅ (after Task 2) |

## Must-Haves Matrix

(populated from must_haves blocks across Plans 01-11)

| Truth | Source Plan | Evidence | Status |
|-------|-------------|----------|--------|
| 4 new Rust modules compile clean as skeletons | Plan 01 | cargo check exit 0 | ✅ |
| Per-tentacle WriteScope + 30s TTL | Plan 02 | ecosystem.rs WRITE_UNLOCKS + write_scope_drops_on_drop test | ✅ |
| 3 outbound tentacle skeletons | Plan 03 | slack/github/gmail_outbound.rs files exist | ✅ |
| BLADE_EVENTS gains JARVIS_INTERCEPT + CONSENT_REQUEST | Plan 04 | grep evidence | ✅ |
| 9 refusal patterns + disjunction post-check | Plan 05 | ego::refusal_patterns + tests | ✅ |
| intent_router heuristic-first body | Plan 06 | intent_router::classify_intent + 6 tests | ✅ |
| consent SQLite CRUD complete | Plan 06 | consent_set/get/revoke + tests | ✅ |
| slack_outbound + github_outbound bodies | Plan 07 | MCP-first + HTTP fallback patterns landed | ✅ |
| gmail_outbound body | Plan 08 | base64url + 401 handling | ✅ |
| jarvis_dispatch consent gate + WriteScope + 3-tier | Plan 09 | dispatch_action body + tests | ✅ |
| commands.rs ego wrap at l.~1517 | Plan 10 | grep "ego::intercept_assistant_output" | ✅ |
| 6 typed Tauri wrappers + JarvisPill + ConsentDialog | Plan 11 | admin.ts + 2 component files | ✅ |
| research/questions.md Q1 closed | Plan 10 | grep "Status: closed" | ✅ |

## Threat Model Status

| Threat ID | Severity | Disposition | Plan(s) | Status |
|-----------|----------|-------------|---------|--------|
| T-18-01 | HIGH | mitigate | Plans 06, 09 | ✅ — consent_check gate enforced before any outbound |
| T-18-02 | HIGH | mitigate | Plans 02, 09 | ✅ — WriteScope RAII + 30s TTL + Drop verified |
| T-18-03 | HIGH | mitigate | Plans 09, 11 | ✅ — safe_slice + plain-text render |
| T-18-04 | MEDIUM | mitigate | Plan 02 | ✅ — Integration kind early-return prevents nonsense installs |
| T-18-05 | MEDIUM | mitigate | Plans 05, 10 | ✅ — 9 patterns + disjunction post-check |
| T-18-06 | LOW | mitigate | Plans 04, 11 | ✅ — payload schema excludes token fields |

## Deferred Per Chat-First Pivot (Operator-Approved)

- **UI-polish runtime UAT for ConsentDialog visual fidelity** — deferred to v1.3 per operator pivot; UI-only. The functional path (dialog opens, buttons work, decision propagates) is exercised by the cold-install demo (Task 2) but visual polish is NOT screenshot-validated.
- **JarvisPill positioning fidelity** — deferred to v1.3; same rationale.
- **One-shot "Allow once" dispatch path** — Wave 4 simplification treats it as re-invoke; next dispatch finds NeedsPrompt again. v1.3 wires tokio oneshot channel for true one-shot semantics.
- **Fast-streaming branch ego coverage** — RESEARCH Pitfall 3; out-of-scope for v1.2 per architectural-correction lock. Documented inline in commands.rs at l.~1166.

## Cold-Install Demo Status (Task 2 — this plan)

(populated by Task 2 below; Task 1 leaves this section as `pending`)

```

**Step 3.** Capture the actual gate output as inline evidence. If any gate is RED, STOP — do not proceed to Task 2 until static gates are green. Investigate, return to the failing plan, fix, then re-run.

**Note on the test-threads flag:** BLADE convention is `--test-threads=1` to avoid SQLite test interference. Some Plans (06, 09) noted this in their test design.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -5 && cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -5 && cd /home/arnav/blade && npm run verify:all 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md` exists with all 5 sections (Static Gates, Requirement Coverage, Must-Haves, Threat Model, Deferred).
    - All 5 static-gate cells in the table are ✅.
    - All 12 JARVIS REQ rows have a status (✅ or DEFERRED for JARVIS-01/02).
    - `cd src-tauri && cargo check` exits 0.
    - `cd src-tauri && cargo test --lib --test-threads=1 -- --nocapture` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `npm run verify:all` exits 0 (chain count ≥31 — Phase 18 doesn't add new gates).
    - `bash scripts/verify-eval.sh` exits 0 (Phase 16 chain entry unchanged).
    - 10-WIRING-AUDIT.json gate confirms 7 new module entries (Plan 04 task 2).
    - verify-emit-policy gate green (single-window emit_to throughout — no allowlist additions).
    - `grep "Status: closed" .planning/research/questions.md` returns at least 1 line.
  </acceptance_criteria>
  <done>All static gates green and documented. 18-VERIFICATION.md must-haves matrix + REQ-to-evidence map populated. Ready for Task 2 (cold-install demo).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The full Phase 18 chat → cross-app action pipeline:
- Type "create a Linear issue: test JARVIS demo from Phase 18" (OR "post 'shipping doctor module today' to #team in Slack" if Slack MCP is installed) into the chat input.
- BLADE classifies intent → ActionRequired{linear, create} (or {slack, post}).
- jarvis_dispatch checks consent → no row → emits consent_request → ConsentDialog opens.
- Operator clicks "Allow once" (or "Allow always").
- jarvis_dispatch acquires WriteScope(30s) → calls linear_create_issue (or slack_outbound_post_message).
- Real cross-app write happens (issue visible in Linear OR message visible in Slack).
- ActivityStrip emits "[JARVIS] action_required: linear → executed" (or slack).
- Screenshot saved at `docs/testing ss/jarvis-cold-install-demo.png` (literal space).
  </what-built>
  <how-to-verify>
**This is the JARVIS-12 e2e success criterion. UI-polish UAT is DEFERRED per chat-first pivot, but the functional cross-app write is NOT.**

1. **Pre-check (operator):**
   - Confirm Linear PAT is in keyring (preferred demo target — guaranteed creds path).
     Test: `cd src-tauri && cargo run --bin blade -- --test-keyring linear` (or equivalent BLADE command).
     If empty: connect Linear via Integrations tab BEFORE running the demo.
   - Alternative: if Linear creds missing, confirm Slack MCP server is installed.
     Run BLADE → check Diagnostics → MCP servers — `slack` should appear.
   - If neither path is available, run the demo with a "Connect via Integrations tab → Linear" hard-fail check (validates D-10) and capture that as the screenshot — but this only proves D-10, not the full e2e write. **Strong recommendation: ensure Linear creds are in place before running.**

2. **Run BLADE in dev mode:**
   ```bash
   cd /home/arnav/blade && npm run tauri dev
   ```
   Wait for the app to mount cleanly (no Rust compile errors; main window renders).

3. **Trigger the demo:**
   - Open the chat surface (default route).
   - Type into the chat input: `create a Linear issue: test JARVIS demo from Phase 18`
   - Press Enter.

4. **Observe:**
   - Within ~3 seconds, ConsentDialog opens with:
     - Target service: "linear"
     - Action: "create on linear" (or similar action_verb)
     - Content preview: the safe_slice'd args (a serde JSON snippet)
     - 3 buttons: "Allow once" (default focus), "Allow always", "Deny"
   - Click "Allow once".
   - Watch the chat — within ~3 seconds, the JARVIS pill should NOT appear (because the dispatch path is happy-case — no refusal, no capability gap). The action just executes.
   - The ActivityStrip (bottom of the window) shows: `[JARVIS] action_required: linear → executed`

5. **Confirm the cross-app write:**
   - Open Linear in a browser tab.
   - The issue "test JARVIS demo from Phase 18" should appear in the operator's default Linear team (or wherever linear_create_issue routes).

6. **Capture the screenshot:**
   - Take a screenshot of the BLADE window showing:
     - The chat with the user's prompt + (if rendered) the assistant's confirmation
     - The ActivityStrip with the [JARVIS] line visible
   - AND/OR a screenshot of the Linear issue page showing the issue was created.
   - Save to `docs/testing ss/jarvis-cold-install-demo.png` (literal space in the directory name).

7. **Read-back citation (CLAUDE.md Verification Protocol):**
   - After saving, use the Read tool on `docs/testing ss/jarvis-cold-install-demo.png`.
   - Add a one-line observation citing what's visible. Example: "Linear issue page shows 'test JARVIS demo from Phase 18' created at 2026-04-30 12:34 by operator's account. BLADE window shows [JARVIS] action_required: linear → executed in ActivityStrip."
   - Add this observation to the "Cold-Install Demo Status" section of `18-VERIFICATION.md`.

8. **Update 18-VERIFICATION.md:**
   - Set frontmatter `status: verified`
   - Update the "Cold-Install Demo Status" section with:
     - Demo target chosen (Linear or Slack)
     - Pre-check results (creds confirmed)
     - Action taken (issue created / message posted)
     - Cross-app write confirmed (issue/message visible in target service)
     - Screenshot path: `docs/testing ss/jarvis-cold-install-demo.png`
     - Read-back observation (one line)

9. **Optional but recommended:** Test ONE refusal path to validate the ego pipeline.
   - Type a message designed to trigger a refusal (e.g. ask BLADE to do something it doesn't have a tool for).
   - Confirm: a JarvisPill appears with the correct state (intercepting → installing/retrying → hard_refused).
   - Confirm: ActivityStrip shows the corresponding `[JARVIS] action_required: X → hard_refused` (or similar) entry.
   - This is informational — not a blocking SC for Phase 18.
  </how-to-verify>
  <resume-signal>
Type "verified" with:
- Demo target chosen
- Cross-app write confirmed (1-2 line description)
- Screenshot path
- One-line read-back observation

OR describe issues encountered. If the cold-install demo fails (real cross-app write does NOT land), STOP — do not mark Phase 18 verified. Open a follow-up plan to investigate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-driven UAT -> production credentials | Real Linear/Slack writes happen with operator's real creds; verified manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-CARRY-36 | Information Disclosure | Demo screenshot may capture creds in Diagnostics tab | accept | Operator verifies the screenshot frame before saving — only the chat surface + ActivityStrip + Linear page are captured. Diagnostics is not in frame. |
| T-18-CARRY-37 | Spoofing | Test issue title in real Linear team | accept | Operator can delete the test issue post-demo. Title "test JARVIS demo from Phase 18" is unambiguous. |

This plan COMPLETES all phase-level threat-model dispositions through verification of the mitigations from earlier plans.
</threat_model>

<verification>
- 18-VERIFICATION.md exists with all sections populated
- Static gates: cargo check + cargo test --lib + tsc + verify:all all green
- Cold-install demo screenshot saved to `docs/testing ss/jarvis-cold-install-demo.png`
- Operator types "verified" with demo target + cross-app write evidence + screenshot path + read-back observation
- All 12 JARVIS-XX REQs accounted for (JARVIS-01/02 deferred per Plan 13; JARVIS-03..12 verified)
</verification>

<success_criteria>
- All static gates green (4 Phase 18 + 27 inherited from v1.1)
- Cold-install demo executed end-to-end: text chat → consent → real cross-app write → ActivityStrip emission → screenshot + readback
- 18-VERIFICATION.md frontmatter status: verified
- All HIGH-severity threats (T-18-01/02/03) confirmed mitigated through plan trace
- Phase 18 ready for `/gsd-transition` to Phase 19
</success_criteria>

<output>
After completion, update `.planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md`:
- Frontmatter `status: verified`
- Cold-Install Demo Status section populated with operator's evidence
- All REQ rows ✅ or DEFERRED

Also create `.planning/phases/18-jarvis-ptt-cross-app/18-12-SUMMARY.md` with:
- Static-gate results (per-gate exit code + notes)
- Cold-install demo evidence (target chosen + screenshot path + read-back citation)
- KNOWN GAPS recorded (UI-polish UAT, one-shot consent, fast-path coverage — all deferred per chat-first pivot)
- Phase 18 status: shipped (code complete + e2e demo passed) — operator decides whether to mark fully closed or carry deferred items forward

Update STATE.md:
- `progress.completed_phases: 13` (was 12)
- `progress.completed_plans: 79 + 12 = 91`
- `Current Focus`: "Phase 18 ✅ closed YYYY-MM-DD; cold-install demo executed; UI-polish UAT deferred to v1.3 per chat-first pivot. Next: Phase 19 (Operator UAT Close) or Phase 20 (Polish)."

Update ROADMAP.md to mark Phase 18 complete (`✅`).
</output>
