---
phase: 18
plan: 13
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md
autonomous: true
deferred: true
requirements:
  - JARVIS-01
  - JARVIS-02
tags: [deferral, traceability, voice, ptt, whisper]
must_haves:
  truths:
    - "JARVIS-01 (PTT global hotkey) traceability: marked DEFERRED to v1.3 per CONTEXT D-01"
    - "JARVIS-02 (Whisper STT) traceability: marked DEFERRED to v1.3 per CONTEXT D-01"
    - "v1.3 hand-off documents the dispatcher signature is voice-source-agnostic — zero rework needed when wiring v1.3 voice path"
    - "voice_global.rs PTT primitive remains in tree, NOT wired into Phase 18 dispatcher"
  artifacts:
    - path: ".planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md"
      provides: "Phase 18 deferred-REQ traceability + v1.3 hand-off shape"
      min_lines: 60

threat_model_required: false
---

<objective>
Wave 0 Plan 13 — Document the deferral of JARVIS-01 (PTT) and JARVIS-02 (Whisper STT) to v1.3.

**Purpose:**
- Phase 18 was reinterpreted under the chat-first pivot (operator 2026-04-30) — voice input is OUT.
- ROADMAP.md still lists JARVIS-01..12 as Phase 18 REQs; this plan ensures the deferred ones have FORMAL traceability (not just a missing checkbox).
- Per CONTEXT D-01: "the dispatcher takes a transcript string, the source of that transcript is irrelevant" — this plan documents the v1.3 hand-off shape so resurrection is zero-rework.

**Output:**
- `18-DEFERRAL.md` with: which REQs are deferred, why, the v1.3 hand-off shape, and the existing voice_global.rs primitive's state (not wired)
- Cross-reference from 18-VERIFICATION.md (Plan 12) so deferral is traceable from the verification report
- No code changes — pure documentation

**This plan can run in Wave 0 (parallel-safe with all other plans) — no file overlap.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-jarvis-ptt-cross-app/18-CONTEXT.md
@/home/arnav/.claude/projects/-home-arnav-blade/memory/feedback_chat_first_pivot.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 18-DEFERRAL.md documenting JARVIS-01/02 deferral + v1.3 hand-off shape</name>
  <read_first>
    - .planning/REQUIREMENTS.md § JARVIS-01..12 (lines 46-59 — original wording before chat-first reinterpretation)
    - .planning/phases/18-jarvis-ptt-cross-app/18-CONTEXT.md D-01, D-02 (deferral lock + rename)
    - src-tauri/src/voice_global.rs (full file overview — confirm it remains in tree, not wired into Phase 18)
    - .planning/STATE.md § Current Focus (chat-first pivot context)
  </read_first>
  <files>.planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md</files>
  <behavior>
    - File exists at `.planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md`
    - Lists JARVIS-01 + JARVIS-02 as deferred with reasoning citing D-01
    - Documents v1.3 hand-off shape — specifically that the dispatcher accepts a transcript string regardless of source
    - Confirms voice_global.rs remains in tree (not deleted) and ready for v1.3 resurrection
    - Cross-links 18-VERIFICATION.md so the deferred REQs trace to documented evidence
  </behavior>
  <action>
Create `.planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md`:

```markdown
---
phase: 18
plan: 13
status: deferred
created: 2026-04-30
deferred_reqs:
  - JARVIS-01
  - JARVIS-02
target_milestone: v1.3
---

# Phase 18 — Deferred Requirements (JARVIS-01, JARVIS-02)

## Why deferred

Phase 18 was reinterpreted under the **chat-first pivot** (operator decision 2026-04-30). Per CONTEXT D-01:

> "Text chat is the only input surface for Phase 18. PTT (JARVIS-01) and Whisper STT (JARVIS-02) are deferred to v1.3. JARVIS-12 (cold-install demo) is rewritten as type a command in chat → BLADE prompts consent → executes real cross-app action → action visible in target service."

The high-leverage half of Phase 18 is the **action + ego loop**, not the voice input. Voice path adds STT latency + accuracy issues, and the dispatcher is voice-source-agnostic — wiring voice in v1.3 is zero rework.

## REQs deferred

| REQ ID | Original wording (REQUIREMENTS.md:48-49) | Reason for deferral | Target milestone |
|--------|------------------------------------------|---------------------|------------------|
| JARVIS-01 | "Push-to-talk global hotkey registered (configurable; default `Ctrl+Alt+Space` on Win / `Cmd+Opt+Space` on Mac)" | Chat-first pivot: voice input out of Phase 18 scope. Existing PTT primitive (`src-tauri/src/voice_global.rs`) remains in tree, available for v1.3 resurrection. | v1.3 |
| JARVIS-02 | "PTT flow captures audio → Whisper STT (existing `voice.rs` or `whisper_local` if feature flag set) → text" | Same chat-first pivot. Existing `whisper_local.rs` (gated behind `local-whisper` feature flag) remains in tree, available for v1.3. | v1.3 |

## v1.3 hand-off shape (zero-rework guarantee)

The Phase 18 dispatcher (`src-tauri/src/jarvis_dispatch.rs::jarvis_dispatch_action`) accepts an `IntentClass` parameter, which itself is derived from a `transcript: String` via `intent_router::classify_intent(transcript)`. The transcript's SOURCE is irrelevant to the dispatcher.

Pseudo-pipeline (chat-first, Phase 18):
```
chat input → useChat send → commands.rs send_message_stream
  → intent_router::classify_intent(message)  ← transcript IS the user's typed message
  → jarvis_dispatch::dispatch_action(intent)
  → outbound write
```

Pseudo-pipeline (voice-resurrected, v1.3):
```
PTT hotkey → voice_global.rs captures audio → whisper_local.rs returns transcript: String
  → intent_router::classify_intent(transcript)  ← same fn, voice transcript instead of typed
  → jarvis_dispatch::dispatch_action(intent)    ← UNCHANGED
  → outbound write                              ← UNCHANGED
```

**Wiring deltas required in v1.3 (estimated scope):**
1. Re-enable `voice_global.rs` PTT registration (already present; just toggle off the chat-first guard).
2. Wire `whisper_local.rs::transcribe(audio_bytes)` → string (already present behind `local-whisper` feature flag).
3. Hand the resulting transcript to `intent_router::classify_intent(...)` — the existing function; no changes.

**Estimated v1.3 effort:** 1 plan, 1-2 tasks. The chat-first dispatcher carries voice for free.

## Files NOT wired in Phase 18 (preserved for v1.3)

| File | State | v1.3 action |
|------|-------|-------------|
| `src-tauri/src/voice_global.rs` | In tree, not wired into JARVIS dispatcher | Wire to `intent_router::classify_intent` |
| `src-tauri/src/whisper_local.rs` | Behind `local-whisper` feature flag | Build with the flag in v1.3; pipe audio_bytes → transcribe → dispatcher |
| `src-tauri/src/voice.rs` | (verify presence) — fallback STT path | Keep available; v1.3 chooses whisper_local OR voice.rs based on user preference |

## JARVIS-12 reinterpretation (chat-first)

Per CONTEXT D-21, JARVIS-12 (cold-install demo) is rewritten:
- **Original:** PTT activated → user speaks command → BLADE prompts consent → executes real cross-app action
- **Phase 18 chat-first:** Operator types into chat → BLADE prompts consent → executes real cross-app action → screenshot saved at `docs/testing ss/jarvis-cold-install-demo.png`

This rewriting is operator-blessed (chat-first pivot 2026-04-30) and is the e2e SC for Phase 18. See Plan 12 for the e2e demo execution.

## Cross-reference

- See `.planning/phases/18-jarvis-ptt-cross-app/18-VERIFICATION.md` (Plan 12) for the per-REQ status table.
- See `.planning/phases/18-jarvis-ptt-cross-app/18-CONTEXT.md` D-01 for the operator's chat-first lock.
- See `~/.claude/projects/-home-arnav-blade/memory/feedback_chat_first_pivot.md` for the pivot memory (load-bearing).

## Status

| Plan | Status |
|------|--------|
| 18-13 | ✅ Documented (this file) |
| JARVIS-01 / JARVIS-02 | DEFERRED to v1.3 — re-enabled by a small follow-up plan (1-2 tasks) when v1.3 voice surface is on the roadmap |
```
  </action>
  <verify>
    <automated>test -f /home/arnav/blade/.planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md && grep -c "DEFERRED" /home/arnav/blade/.planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` exists.
    - `grep -n "JARVIS-01" .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` returns at least 2 lines.
    - `grep -n "JARVIS-02" .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` returns at least 2 lines.
    - `grep -n "voice_global.rs" .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` returns at least 1 line.
    - `grep -n "transcript: String" .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` returns at least 1 line.
    - `grep -n "v1.3 hand-off" .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` returns at least 1 line.
    - `grep -n "zero rework\|zero-rework" .planning/phases/18-jarvis-ptt-cross-app/18-DEFERRAL.md` returns at least 1 line.
    - Frontmatter `status: deferred` + `deferred_reqs: [JARVIS-01, JARVIS-02]` present.
  </acceptance_criteria>
  <done>JARVIS-01 + JARVIS-02 formally documented as deferred with v1.3 hand-off shape; voice_global.rs preserved in tree.</done>
</task>

</tasks>

<verification>
- File exists at the correct path
- All grep checks return expected counts
- Frontmatter parseable (deferred: true / deferred_reqs list)
- Cross-references match (18-VERIFICATION.md, 18-CONTEXT.md, feedback_chat_first_pivot.md)
</verification>

<success_criteria>
- 18-DEFERRAL.md documents JARVIS-01 + JARVIS-02 deferral with operator-blessed reasoning
- v1.3 hand-off shape clarifies the dispatcher is voice-source-agnostic
- Existing voice files (voice_global.rs, whisper_local.rs) preserved in tree for v1.3
- Cross-referenced from Plan 12 verification report
- All 12 JARVIS REQs accounted for in the plan set (Plans 01-12 cover JARVIS-03..12; Plan 13 covers JARVIS-01..02)
</success_criteria>

<output>
After completion, the deferral doc is committed alongside the rest of Phase 18. Plan 12 (verification) cross-references this file in the REQ coverage matrix for JARVIS-01 and JARVIS-02 rows.

No SUMMARY needed for this small plan — the doc IS the deliverable. Plan 12's SUMMARY can note "Plan 13 documented the JARVIS-01/02 deferral; see 18-DEFERRAL.md."
</output>
