---
phase: 03-dashboard-chat-settings
plan: 07
type: execute
wave: 5
depends_on: ['03-04', '03-05', '03-06']
files_modified:
  - tests/e2e/chat-stream.spec.ts
  - tests/e2e/chat-tool-approval.spec.ts
  - tests/e2e/dashboard-paint.spec.ts
  - tests/e2e/settings-provider.spec.ts
  - scripts/verify-chat-rgba.sh
  - package.json
autonomous: false
requirements:
  - CHAT-01
  - CHAT-04
  - CHAT-06
  - CHAT-08
  - CHAT-09
  - DASH-07
  - DASH-08
  - SET-01
must_haves:
  truths:
    - "Playwright spec chat-stream.spec.ts asserts ≤60 rAF commits during 50 synthetic chat_token events over 1s (D-91 / SC-2)"
    - "Playwright spec chat-tool-approval.spec.ts asserts buttons disabled at t=100ms and enabled by t=600ms; respond_tool_approval invoked with approved=true (D-91 / SC-2)"
    - "Playwright spec dashboard-paint.spec.ts asserts measure(boot, dashboard-paint) < 400ms headless (D-91 / SC-5)"
    - "Playwright spec settings-provider.spec.ts asserts Test → Save flow stores key + key persists (mocked) (D-91 / SC-4)"
    - "Bash script verify-chat-rgba.sh greps src/features/chat for backdrop-filter and fails if any found (D-70 / SC-5 invariant)"
    - "package.json scripts include verify:chat-rgba; verify:all updated to include it"
    - "Operator smoke checkpoint task (D-92) blocks phase completion — manual walk-through of dashboard / chat / settings"
  artifacts:
    - path: "tests/e2e/chat-stream.spec.ts"
      provides: "Profiler ≤60 commits / 1s assertion (SC-2)"
    - path: "tests/e2e/chat-tool-approval.spec.ts"
      provides: "500ms delay assertion (SC-2)"
    - path: "tests/e2e/dashboard-paint.spec.ts"
      provides: "performance.measure(boot, dashboard-paint) < 400ms assertion (SC-5)"
    - path: "tests/e2e/settings-provider.spec.ts"
      provides: "Provider key save round-trip assertion (SC-4)"
    - path: "scripts/verify-chat-rgba.sh"
      provides: "Backdrop-filter grep regression check (SC-5 D-70 invariant)"
    - path: "package.json"
      provides: "verify:chat-rgba script wired into verify:all aggregate"
  key_links:
    - from: "tests/e2e/*.spec.ts"
      to: "playwright.config.ts"
      via: "Phase 1+2 harness — no new deps"
      pattern: "@playwright/test"
    - from: "scripts/verify-chat-rgba.sh"
      to: "package.json verify:all"
      via: "npm run verify:chat-rgba added to combined script"
      pattern: "verify:chat-rgba"
---

<objective>
Land the falsifiable test surface for Phase 3 SC-2 / SC-3 / SC-4 / SC-5 — 4 Playwright specs + 1 bash regression script + the non-autonomous operator smoke checkpoint (D-92). All specs reuse the Phase 1+2 `@tauri-apps/test` harness; no new test deps.

Purpose: every Phase 3 SC has a specific assertion that fails loudly when violated. Operator smoke (Task 3) catches what automation cannot — visual coherence, ambient strip changing on real activity, end-to-end provider key persistence across restart.

Output: 4 specs + 1 script + 1 package.json edit + 1 checkpoint task. Plan completes under 35% context (specs themselves are tight; the bulk of work is wiring + the manual walk-through).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-dashboard-chat-settings/03-CONTEXT.md
@.planning/phases/03-dashboard-chat-settings/03-PATTERNS.md
@CLAUDE.md
@playwright.config.ts
@tests/e2e/listener-leak.spec.ts
@package.json

<interfaces>
Existing Phase 1+2 Playwright harness:
- `playwright.config.ts` already configured (Plan 01-09)
- `tests/e2e/listener-leak.spec.ts` (Plan 01-09 spec — model for Phase 3 specs)
- `tests/e2e/onboarding-boot.spec.ts` + `tests/e2e/shell.spec.ts` (Plan 02-07 specs — model for Phase 3 specs)
- Mock-invoke shim pattern already established: tests `await page.evaluate` to install `(window as any).__TAURI_INVOKE_HOOK__` for command interception

Existing scripts in `package.json`:
- `verify:no-raw-tauri`
- `verify:entries`
- `verify:emit-policy`
- `verify:migration-ledger`
- `verify:contrast`
- `verify:html-entries`
- `verify:all` (composite)

Phase 3 specs follow the SAME pattern: each is an ESM `.spec.ts` under `tests/e2e/`. No new harness, no new deps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship 4 Playwright specs covering SC-2, SC-3, SC-4, SC-5</name>
  <files>tests/e2e/chat-stream.spec.ts, tests/e2e/chat-tool-approval.spec.ts, tests/e2e/dashboard-paint.spec.ts, tests/e2e/settings-provider.spec.ts</files>
  <read_first>
    - /home/arnav/blade/tests/e2e/listener-leak.spec.ts (Phase 1 model spec — copy harness pattern)
    - /home/arnav/blade/tests/e2e/onboarding-boot.spec.ts (Phase 2 model — TAURI_INVOKE_HOOK mock pattern)
    - /home/arnav/blade/tests/e2e/shell.spec.ts (Phase 2 model — keyboard + UI assertions)
    - /home/arnav/blade/playwright.config.ts (current config)
    - /home/arnav/blade/.planning/phases/03-dashboard-chat-settings/03-PATTERNS.md §11, §12, §13, §14
  </read_first>
  <action>
    Sub-task 1a — Create `tests/e2e/chat-stream.spec.ts` per pattern §11. Boot dev build via Playwright; navigate to `/chat` (call `(window as any).__BLADE_OPEN_ROUTE__('chat')` if exposed, else use the ⌘K palette pattern from shell.spec). Install a rAF render counter shim on `window`. Dispatch 50 synthetic `chat_token` events (each carrying a tiny string) over 1000ms via `__TAURI_EMIT__` (or the fallback CustomEvent bridge). After the burst settles, read `window.__RENDER_COUNT__` and assert ≤60. The exact count threshold is the SC-2 falsifier; document the 60-frame ceiling in a comment.

    Sub-task 1b — Create `tests/e2e/chat-tool-approval.spec.ts` per pattern §12. Navigate to chat. Emit a synthetic `tool_approval_needed` event with payload `{ tool_name: 'shell_exec', args: { cmd: 'ls' }, context: 'list current dir', approval_id: 'req-1', risk: 'Ask' }` (use `approval_id` per Rust source — Plan 03-04 reads both keys defensively). Assert the Approve button exists AND is disabled. Wait 550ms (clear of the 500ms delay). Assert the Approve button is now enabled. Click Approve. Assert that `__TAURI_INVOKE_CALLS__` array contains an entry with `cmd: 'respond_tool_approval'` + `args.approval_id: 'req-1'` + `args.approved: true`. Mock `respond_tool_approval` to return undefined.

    Sub-task 1c — Create `tests/e2e/dashboard-paint.spec.ts` per pattern §13. Mock `perception_get_latest` to return a small synthetic PerceptionState immediately; mock `homeostasis_get` to return a synthetic HormoneState. Boot, navigate to `/dashboard`. `await page.waitForFunction(() => performance.getEntriesByName('dashboard-paint').length > 0, { timeout: 5000 })`. Compute `performance.getEntriesByName('dashboard-paint')[0].startTime - performance.getEntriesByName('boot')[0].startTime`. Assert < 400ms (D-77 headless 2× metal budget).

    Sub-task 1d — Create `tests/e2e/settings-provider.spec.ts` per pattern §14. Navigate to `/settings/providers`. Mock `test_provider` → 'OK'; mock `store_provider_key` → undefined; mock `switch_provider` → empty BladeConfig stub; mock `get_all_provider_keys` → reflective: first call returns `{ providers: [], active_provider: '' }`; subsequent calls return `{ providers: [{ provider: 'groq', has_key: true, masked: 'gsk_***1234', is_active: true }], active_provider: 'groq · llama-3.3-70b-versatile' }`. Find the Groq card (filter by text 'Groq'); fill the input with 'gsk_test_1234'; click Save & Switch. Assert the card now shows 'Key stored: gsk_***1234' (matches the masked-key text from the second mock). Optionally simulate a reload (`page.reload()`) and assert the masked-key persists in the rendered DOM (will pass because the mock still returns the stored shape).

    Each spec includes: descriptive `test('summary', async ({ page }) => { ... })`; uses `expect(...)` from `@playwright/test`; no new imports beyond `@playwright/test`; comments cite the SC and decision IDs verifying.
  </action>
  <verify>
    <automated>test -f tests/e2e/chat-stream.spec.ts && test -f tests/e2e/chat-tool-approval.spec.ts && test -f tests/e2e/dashboard-paint.spec.ts && test -f tests/e2e/settings-provider.spec.ts; grep -q "approval_id" tests/e2e/chat-tool-approval.spec.ts; grep -q "dashboard-paint" tests/e2e/dashboard-paint.spec.ts; grep -q "store_provider_key" tests/e2e/settings-provider.spec.ts; grep -q "RENDER_COUNT\|requestAnimationFrame" tests/e2e/chat-stream.spec.ts</automated>
  </verify>
  <done>
    - 4 Playwright spec files exist under tests/e2e/.
    - Each spec maps to one Phase 3 SC (chat-stream→SC-2, chat-tool-approval→SC-2, dashboard-paint→SC-5, settings-provider→SC-4).
    - No new test dependencies (uses existing @playwright/test from Phase 1+2 harness).
    - Specs don't have to PASS in this plan's verify (operator runs `npm run test:e2e` during smoke per Task 3); the spec FILES must exist + parse + import correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship verify-chat-rgba.sh + wire into package.json verify:all</name>
  <files>scripts/verify-chat-rgba.sh, package.json</files>
  <read_first>
    - /home/arnav/blade/scripts/verify-no-raw-tauri.sh (model — bash style + exit codes)
    - /home/arnav/blade/scripts/verify-entries.mjs (alternative model — Node ESM)
    - /home/arnav/blade/package.json (current verify:* scripts + verify:all composite)
  </read_first>
  <action>
    Sub-task 2a — Create `scripts/verify-chat-rgba.sh` (executable bash):
    ```bash
    #!/usr/bin/env bash
    # verify-chat-rgba.sh — D-70 / SC-5 invariant: chat bubbles MUST use rgba()
    # backgrounds, NEVER backdrop-filter. The dashboard already uses 3 backdrop-filter
    # layers (NavRail + TitleBar + glass shell) per D-07; adding more on chat bubbles
    # collapses first paint.
    #
    # Fails if any backdrop-filter property is found in src/features/chat/*.css.
    #
    # @see .planning/phases/03-dashboard-chat-settings/03-CONTEXT.md §D-70

    set -e

    if grep -rnE "backdrop-filter" src/features/chat/*.css 2>/dev/null; then
      echo ""
      echo "FAIL: backdrop-filter property detected in src/features/chat/*.css"
      echo "      D-70: chat bubbles MUST use rgba() backgrounds — never backdrop-filter."
      echo "      D-07 blur cap (3 per viewport) is already used by NavRail / TitleBar / shell."
      exit 1
    fi

    echo "OK: src/features/chat/*.css free of backdrop-filter (D-70 invariant preserved)"
    exit 0
    ```
    Make it executable via comment in code: `# Run: chmod +x scripts/verify-chat-rgba.sh` (operator runs once; `npm run` invocation will work either way via `bash`).

    Sub-task 2b — Patch `package.json`:
    - Add a new script entry: `"verify:chat-rgba": "bash scripts/verify-chat-rgba.sh"`
    - Update the `"verify:all"` composite script to include it. Current shape (read from file): `"verify:all": "npm run verify:entries && npm run verify:no-raw-tauri && npm run verify:emit-policy && npm run verify:contrast && npm run verify:migration-ledger"` (or similar).
    - New shape: append ` && npm run verify:chat-rgba` to the chain.
    - Be CAREFUL not to disturb other entries; use Edit (with prior Read) instead of Write for package.json.
  </action>
  <verify>
    <automated>test -f scripts/verify-chat-rgba.sh && grep -q "backdrop-filter" scripts/verify-chat-rgba.sh; grep -q "verify:chat-rgba" package.json; chmod +x scripts/verify-chat-rgba.sh 2>/dev/null || true; bash scripts/verify-chat-rgba.sh && echo "verify-chat-rgba PASS"</automated>
  </verify>
  <done>
    - scripts/verify-chat-rgba.sh exists, executable, exits 0 against current chat.css (which has no backdrop-filter per Plans 03-03/04).
    - package.json verify:chat-rgba script entry exists.
    - package.json verify:all composite includes verify:chat-rgba.
    - Running `npm run verify:chat-rgba` succeeds.
    - Running `npm run verify:all` still passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator smoke checkpoint — Phase 3 end-to-end (D-92)</name>
  <what-built>
    - Phase 3 substrate is COMPLETE: 6 WIRE Rust emits live, 14 new TS wrappers + 5 DTOs, full Chat (streaming + tool approval + thinking + compacting), full Dashboard (Right Now hero + Ambient strip + 3 sub-cards), full Settings (10 tabs).
    - 4 Playwright specs land for SC-2/3/4/5 falsifiers.
    - `verify-chat-rgba.sh` enforces D-70 invariant in CI.

    What's left: a real-world smoke session on a desktop with libclang + Tauri capability that the planning sandbox can't run.
  </what-built>
  <how-to-verify>
    On a desktop session:
    1. `cd src-tauri && cargo check` → expect zero errors (validates Plan 03-01 Rust changes).
    2. `npm install && npx playwright install chromium` (if not already done).
    3. `npm run tauri dev`. Verify:
       - All 5 windows still launch (Phase 1+2 substrate intact).
       - Onboarding either runs (fresh config) or skipped (returning user) — confirm the Phase 2 gate still works.
       - Land on the Dashboard. Confirm:
         a. Right Now hero displays the OS active app + window title (e.g. "Code · MainShell.tsx — blade").
         b. RAM / disk / top CPU chips show real numbers.
         c. Visible errors collapsible only appears if there are errors.
         d. AmbientStrip shows 5 hormone chips with the dominant one styled larger.
         e. DEV console shows `[perf] dashboard-first-paint: <Xms>` — record X.
         f. 3 ComingSoonCards visible (Hive / Calendar / Integrations).
       - Open Chat (⌘+/). Send "Hi BLADE — what's 2+2?". Confirm:
         g. Routing pill shows your active provider · model.
         h. Streaming bubble appears, content fills progressively (no jank, no full-page flash).
         i. Open React DevTools Profiler (or measure `__RENDER_COUNT__` if instrumented). Sustained tokens for 2-3s should show smooth commits ≤60/s.
         j. Trigger a tool by asking "ls my home directory" (BLADE may call shell tool). Confirm the approval Dialog appears with the 500ms countdown ring; buttons disabled then enabled. Approve → see tool result.
         k. If running a reasoning-capable model (Anthropic), open a complex query → confirm the "Thinking" collapsible <details> appears above the answer.
         l. If conversation gets long (>40 turns), confirm the "Compacting… N%" pill appears.
       - Open Settings (⌘+,). Confirm:
         m. 10 tabs visible in the left nav: Providers, Models, Routing, Voice, Personality, Appearance, IoT, Privacy, Diagnostics, About.
         n. Each tab opens its pane without error.
         o. ProvidersPane: enter a (real) Groq key → Test → see "OK" toast → Save & Switch → see masked key stored. Restart the app. Re-open Settings → Providers. Confirm the Groq key persists.
         p. RoutingPane: change "code" to Anthropic → Save → toast success.
         q. PersonalityPane: enter a name → Save (per-field) → toast.
         r. AboutPane: version + GitHub link visible.
       - Run `npm run test:e2e`. Confirm 4 new Phase 3 specs pass (chat-stream, chat-tool-approval, dashboard-paint, settings-provider) alongside Phase 1+2 specs.
       - Run `npm run verify:all`. Confirm zero failures (including new verify:chat-rgba gate).
       - `npm run tauri build`. Confirm prod build completes; spot-check the prod bundle by running it once.
  </how-to-verify>
  <resume-signal>
    Type "approved" once all 18 verifications above pass.
    If any fail, describe the failure (e.g. "j: tool approval buttons enabled at t=200ms — delay not enforced") so the planner can route a follow-up plan via /gsd-plan-phase --gaps.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright spec → DOM assertions | Specs assume specific data-attributes / button text; refactor drift could falsify the test |
| verify-chat-rgba.sh → src/features/chat | Bash regex matches "backdrop-filter" anywhere in the file (including comments) |
| Operator smoke verifications | Manual; cannot be replayed; relies on operator integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Playwright spec relies on data-attributes (data-route-id, data-message-id) | mitigate | Plan 03-03/04/05/06 already produce these attrs; spec failures point at refactor regressions early |
| T-03-07-02 | Spoofing | __TAURI_INVOKE_HOOK__ mock could differ from real backend behavior | accept | SC-4 spec uses mock for setting; D-92 operator smoke runs against real backend for ground-truth |
| T-03-07-03 | Tampering | verify-chat-rgba.sh false positive on commented-out backdrop-filter | accept | Comments naming the property are fine; CI grep is naive but acceptable for this single invariant |
| T-03-07-04 | Repudiation | Operator smoke checkpoint not run (rubber-stamped) | mitigate | Checkpoint task requires explicit "approved" with 18 enumerated verifications; failure path documents what to fix |
| T-03-07-05 | DoS | dashboard-paint.spec.ts timeout (5s) on slow CI runner | accept | 5s headless budget vs 200ms metal target; if flaky, raise timeout in Phase 9 polish |
| T-03-07-06 | Info disclosure | Operator smoke 'o' uses real Groq key | accept | Operator's own key; not committed; standard practice |
</threat_model>

<verification>
End-to-end checks for this plan:
- 4 spec files exist under tests/e2e/.
- scripts/verify-chat-rgba.sh exists and exits 0 against current chat.css.
- `grep -q "verify:chat-rgba" package.json` returns true.
- `npm run verify:all` passes (includes the new chat-rgba check).
- The checkpoint task is `autonomous: false` and waits for operator "approved".
- Plans 03-01..06 prerequisites checked off (the smoke depends on those landing; this plan ships the test surface + the checkpoint).
</verification>

<success_criteria>
- 4 Playwright specs exist + parse + import successfully.
- verify-chat-rgba.sh wired into verify:all.
- Operator smoke checkpoint blocks Phase 3 closure until 18 verifications complete.
- Plan completes under 35% context.
- Phase 3 closes upon operator "approved" signal; STATE.md update (separate from this plan) records Phase 3 substrate complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-chat-settings/03-07-SUMMARY.md` documenting test specs landed, operator smoke result, any deferred follow-ups (e.g. if a spec fails on the operator desktop, document and route via gap-closure planning).
</output>
