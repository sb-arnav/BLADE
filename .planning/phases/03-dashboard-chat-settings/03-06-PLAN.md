---
phase: 03-dashboard-chat-settings
plan: 06
type: execute
wave: 3
depends_on: ['03-02']
files_modified:
  - src/features/settings/index.tsx
  - src/features/settings/SettingsShell.tsx
  - src/features/settings/settings.css
  - src/features/settings/panes/ProvidersPane.tsx
  - src/features/settings/panes/ModelsPane.tsx
  - src/features/settings/panes/RoutingPane.tsx
  - src/features/settings/panes/VoicePane.tsx
  - src/features/settings/panes/PersonalityPane.tsx
  - src/features/settings/panes/AppearancePane.tsx
  - src/features/settings/panes/IoTPane.tsx
  - src/features/settings/panes/PrivacyPane.tsx
  - src/features/settings/panes/DiagnosticsEntryPane.tsx
  - src/features/settings/panes/AboutPane.tsx
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05
  - SET-06
  - SET-07
  - SET-08
  - SET-09
  - SET-10
must_haves:
  truths:
    - "Settings route 'settings' renders SettingsShell with a 10-tab vertical nav: Providers, Models, Routing, Voice, Personality, Appearance, IoT, Privacy, Diagnostics, About (D-79)"
    - "Each tab maps 1:1 to a Pane component lazy-imported on demand"
    - "ProvidersPane reuses PROVIDERS registry from src/features/onboarding/providers.ts (D-81); 6 cards with Test + Save&Switch buttons"
    - "ModelsPane shows current provider + model + per-provider model dropdown; Save calls switchProvider (D-82)"
    - "RoutingPane reads getTaskRouting + writes setTaskRouting; 5-row grid (code/vision/fast/creative/fallback) with provider dropdowns (D-83)"
    - "VoicePane writes voice_mode/tts_voice/voice_shortcut/quick_ask_shortcut via setConfig (per Rust set_config signature at commands.rs:1944); wake_word_enabled is read-only display in Phase 3 (D-84 + D-66 — no new Rust commands)"
    - "PersonalityPane updates user_name/work_mode/response_style/blade_email via setConfig partial-update; Re-run onboarding button → resetOnboarding wrapper (D-85)"
    - "AppearancePane is a readout — locked dark theme info + Reset prefs button (D-86)"
    - "IoTPane wraps iotListEntities + iotSetState + iotSpotifyNowPlaying; Connect HA section saves ha_base_url via save_config_field; HA token NOT settable from Phase 3 (no Rust setter exists — pane shows 'Configure HA_TOKEN env var' instructional notice) (D-87)"
    - "PrivacyPane lists local-first promises + Clear conversation history button → historyDeleteConversation loop (D-88)"
    - "DiagnosticsEntryPane is a doorway — DEV: openRoute('diagnostics-dev'); PROD: 'Phase 7 admin' notice (D-89)"
    - "AboutPane is static content — version + Tauri + GitHub link (D-90)"
    - "settings.css uses tokens; zero new backdrop-filter layers (D-07 cap)"
    - "10 child route ids registered in settings/index.tsx routes export; matches the 10 SettingsShell tabs"
    - "npx tsc --noEmit passes; npm run verify:all passes"
  artifacts:
    - path: "src/features/settings/index.tsx"
      provides: "11 RouteDefinition entries (settings + 10 child) all mapping to SettingsShell"
      exports: ["routes"]
    - path: "src/features/settings/SettingsShell.tsx"
      provides: "Tabbed shell — left tab nav + right pane via lazy Suspense"
      exports: ["SettingsShell"]
    - path: "src/features/settings/panes/ProvidersPane.tsx"
      provides: "SET-01 — 6 provider cards reusing PROVIDERS registry"
      exports: ["ProvidersPane"]
    - path: "src/features/settings/panes/ModelsPane.tsx"
      provides: "SET-02 — provider/model picker"
      exports: ["ModelsPane"]
    - path: "src/features/settings/panes/RoutingPane.tsx"
      provides: "SET-03 — 5-row routing grid"
      exports: ["RoutingPane"]
    - path: "src/features/settings/panes/VoicePane.tsx"
      provides: "SET-04 — voice settings via setConfig (NOT save_config_field — voice fields aren't in save_config_field's switch table)"
      exports: ["VoicePane"]
    - path: "src/features/settings/panes/PersonalityPane.tsx"
      provides: "SET-05 — personality + re-run onboarding"
      exports: ["PersonalityPane"]
    - path: "src/features/settings/panes/AppearancePane.tsx"
      provides: "SET-06 — locked dark theme readout"
      exports: ["AppearancePane"]
    - path: "src/features/settings/panes/IoTPane.tsx"
      provides: "SET-07 — Home Assistant + Spotify (HA URL via save_config_field; HA token via env-var instructional notice)"
      exports: ["IoTPane"]
    - path: "src/features/settings/panes/PrivacyPane.tsx"
      provides: "SET-08 — local-first readout + history clear"
      exports: ["PrivacyPane"]
    - path: "src/features/settings/panes/DiagnosticsEntryPane.tsx"
      provides: "SET-09 — diagnostics entry doorway"
      exports: ["DiagnosticsEntryPane"]
    - path: "src/features/settings/panes/AboutPane.tsx"
      provides: "SET-10 — version + about"
      exports: ["AboutPane"]
    - path: "src/features/settings/settings.css"
      provides: "Tabbed layout + pane typography"
      contains: "settings-shell"
  key_links:
    - from: "src/features/settings/SettingsShell.tsx"
      to: "src/windows/main/useRouter.ts"
      via: "useRouterCtx().openRoute(tabId)"
      pattern: "useRouterCtx"
    - from: "src/features/settings/panes/ProvidersPane.tsx"
      to: "src/features/onboarding/providers.ts (PROVIDERS)"
      via: "import"
      pattern: "PROVIDERS"
    - from: "src/features/settings/panes/RoutingPane.tsx"
      to: "src/lib/tauri/config.ts (getTaskRouting/setTaskRouting)"
      via: "wrapper invoke"
      pattern: "getTaskRouting|setTaskRouting"
    - from: "src/features/settings/panes/PersonalityPane.tsx"
      to: "src/lib/tauri/config.ts (resetOnboarding)"
      via: "Re-run onboarding action"
      pattern: "resetOnboarding"
    - from: "src/features/settings/panes/VoicePane.tsx"
      to: "src/lib/tauri/config.ts (setConfig)"
      via: "voice_mode/tts_voice/voice_shortcut/quick_ask_shortcut updates"
      pattern: "setConfig"
---

<objective>
Replace the Phase 1 ComingSoonSkeleton stubs at all 10 settings child routes with a working SettingsShell + 10 functional panes. Hits SET-01..10 verbatim from ROADMAP. Renames 2 child routes (`settings-integrations` → IoT; `settings-ambient` → Personality) and adds 5 (`settings-models`, `settings-routing`, `settings-appearance`, `settings-privacy`, `settings-diagnostics`) per D-79 to align with ROADMAP labels.

Purpose: SC-4 (Settings saves a provider key; persists after restart; routing grid reflects updated config) requires a real ProvidersPane + RoutingPane. Plan 03-07 spec asserts the save round-trip. The other 8 panes are minimum-viable surfaces — each delivers the SET-* outcome but defers polish to Phase 9.

Output: 13 files (1 shell + 1 css + 1 index + 10 panes). All saves through existing Rust commands (D-66 + D-80 — no new backend). Honors actual Rust surface: VoicePane uses `setConfig` (not `save_config_field`) because voice fields are in set_config's signature at commands.rs:1944; IoTPane saves `ha_base_url` only (HA token not exposed via any Rust setter — env-var fallback). PROVIDERS registry reused from onboarding (D-81). Plan completes under 40% context — multi-file but each pane is a small, isolated component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-dashboard-chat-settings/03-CONTEXT.md
@.planning/phases/03-dashboard-chat-settings/03-PATTERNS.md
@CLAUDE.md
@src/lib/tauri/index.ts
@src/lib/context/index.ts
@src/features/onboarding/providers.ts
@src/types/provider.ts
@src/types/routing.ts
@src/types/iot.ts
@src/types/history.ts
@src/types/config.ts
@src/design-system/primitives/index.ts
@src/windows/main/useRouter.ts
@src/hooks/usePrefs.ts

<interfaces>
Phase 3 wrappers consumed (Plan 03-02 ships them):

```ts
import {
  getConfig, setConfig, getAllProviderKeys, storeProviderKey, switchProvider, testProvider,
  getTaskRouting, setTaskRouting, saveConfigField, resetOnboarding,
  iotListEntities, iotSetState, iotSpotifyNowPlaying,
  historyListConversations, historyDeleteConversation,
  debugConfig,
} from '@/lib/tauri';
```

Phase 1+2 utilities consumed:
- `useToast()` from `@/lib/context` for save feedback
- `useConfig()` from `@/lib/context` for current BladeConfig + reload()
- `usePrefs()` from `@/hooks/usePrefs` for prefs reset (Appearance pane)
- `useRouterCtx()` from `@/windows/main/useRouter` for tab navigation
- `PROVIDERS` from `@/features/onboarding/providers` (Phase 2)

Phase 1 primitives: `Button, Card, Input, Pill, Badge, GlassPanel, GlassSpinner, Dialog`.

**Rust write-surface reality (verified at planning time):**
- `set_config` (commands.rs:1944) — accepts: provider, api_key, model, token_efficient, user_name, work_mode, response_style, blade_email, base_url, god_mode, god_mode_tier, voice_mode, obsidian_vault_path, tts_voice, quick_ask_shortcut, voice_shortcut. Use this for VoicePane voice fields + PersonalityPane name/work_mode/style/email.
- `save_config_field` (config.rs:728) — accepts ONLY: blade_source_path, user_name, obsidian_vault_path, work_mode, response_style, trusted_ai_delegate, ha_base_url, screen_timeline_enabled, timeline_capture_interval, timeline_retention_days. Use for HA URL.
- NO Rust setter for: ha_token, wake_word_enabled, tts voices beyond config field. wake_word_enabled is a public BladeConfig field but no setter command exists; in Phase 3 it is **read-only** in the VoicePane (toggle UI deferred to Phase 4 with a new Rust command).
- HA token: passed via `HA_TOKEN` env var or fetched from keyring; Phase 3 IoTPane shows an instructional notice in the Connect HA section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship SettingsShell + index.tsx route registration + 5 admin-style panes (Models, Routing, Voice, Personality, Appearance)</name>
  <files>src/features/settings/index.tsx, src/features/settings/SettingsShell.tsx, src/features/settings/settings.css, src/features/settings/panes/ModelsPane.tsx, src/features/settings/panes/RoutingPane.tsx, src/features/settings/panes/VoicePane.tsx, src/features/settings/panes/PersonalityPane.tsx, src/features/settings/panes/AppearancePane.tsx</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/03-dashboard-chat-settings/03-PATTERNS.md §9 (SettingsShell), §10 (ProvidersPane reference)
    - /home/arnav/blade/.planning/phases/03-dashboard-chat-settings/03-CONTEXT.md §D-79..D-86
    - /home/arnav/blade/src/features/settings/index.tsx (current 10 stubs — replace per D-79 rename plan)
    - /home/arnav/blade/src/types/config.ts (BladeConfig field shape — verify field names match wrapper expectations)
    - /home/arnav/blade/src/types/routing.ts (TaskRouting shape — Plan 03-02 ships)
    - /home/arnav/blade/src/lib/tauri/index.ts (verify getTaskRouting / setTaskRouting / saveConfigField / resetOnboarding / setConfig exports)
    - /home/arnav/blade/src/lib/context/index.ts (verify useToast / useConfig export)
    - /home/arnav/blade/src/windows/main/useRouter.ts (verify useRouterCtx + openRoute signature)
    - /home/arnav/blade/src/features/onboarding/providers.ts (PROVIDERS for Models/Routing dropdowns)
    - /home/arnav/blade/src-tauri/src/commands.rs lines 1944-1986 (set_config exact param list)
    - /home/arnav/blade/src-tauri/src/config.rs lines 728-752 (save_config_field allowed keys)
  </read_first>
  <action>
    Sub-task 1a — Create `src/features/settings/SettingsShell.tsx` per pattern §9. The shell:
    - Reads `routeId` + `openRoute` from `useRouterCtx()`
    - Constant `TABS` array of 10 entries `{ id: 'settings-providers', label: 'Providers' }` etc. in the order required by D-79
    - Constant `PANES` map id → lazy import (one per Pane component file)
    - When activeId === 'settings' (the parent), default to 'settings-providers'
    - Renders left nav `<nav className="settings-tabs">` with 10 buttons, each `aria-current="page"` when active
    - Renders right `<div className="settings-pane">` with `<Suspense fallback={<GlassSpinner size={28} />}><Pane /></Suspense>`

    Sub-task 1b — Create `src/features/settings/settings.css`:
    - `.settings-shell { display: grid; grid-template-columns: 220px 1fr; gap: var(--sp-4); height: 100%; padding: var(--sp-4); }`
    - `.settings-tabs { display: flex; flex-direction: column; gap: var(--sp-1); border-right: 1px solid rgba(255,255,255,0.08); padding-right: var(--sp-2); }`
    - `.settings-tab { background: transparent; border: 0; padding: 8px 12px; text-align: left; border-radius: var(--r-card); color: var(--t-2); cursor: pointer; }`
    - `.settings-tab:hover { background: rgba(255,255,255,0.04); }`
    - `.settings-tab.is-active { background: rgba(255,255,255,0.10); color: var(--t-1); border-left: 2px solid #fff; }`
    - `.settings-pane { overflow-y: auto; padding: var(--sp-2); }`
    - `.settings-grid { display: grid; gap: var(--sp-3); grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); }`
    - `.settings-actions { display: flex; gap: var(--sp-2); margin-top: var(--sp-2); justify-content: flex-end; }`
    - `.provider-tagline { color: var(--t-3); font-size: 13px; margin: 4px 0; }`
    - NO `backdrop-filter` properties (D-07 cap)

    Sub-task 1c — Replace `src/features/settings/index.tsx`:
    - Lazy-load SettingsShell once
    - Import settings.css at top
    - Export `routes: RouteDefinition[]` with 11 entries — `settings` (parent, shortcut Mod+,) plus 10 child ids in this exact order: `settings-providers`, `settings-models`, `settings-routing`, `settings-voice`, `settings-personality`, `settings-appearance`, `settings-iot`, `settings-privacy`, `settings-diagnostics`, `settings-about`
    - Each route's component points to the SAME lazy-loaded SettingsShell (the shell switches panes by routeId)
    - Labels exactly as in TABS in 1a
    - `paletteHidden` for none (all 10 user-facing tabs surface in ⌘K palette per D-57)
    - `phase: 3` for all entries
    - Migration ledger note: rename `settings-integrations` → `settings-iot` and `settings-ambient` → `settings-personality`. The Phase 1 ledger must record these renames as cross-references; if the executor finds the migration-ledger script complaining, run `node scripts/seed-migration-ledger.mjs --update` (or document in summary if not available)

    Sub-task 1d — Create `src/features/settings/panes/ModelsPane.tsx` (D-82). Reads `useConfig().config` for current provider+model. Inline per-provider model list (small array of strings per provider id; if not in list, use config.model as the only option). Save calls `switchProvider(providerId, modelId)` then `useConfig().reload()`. Token-efficient toggle: checkbox bound to `config.token_efficient`; on change calls `setConfig({ provider, apiKey: '', model, tokenEfficient: !current })` (apiKey: '' preserves keyring per Plan 02 D-50). Uses Card + Input + Button primitives.

    Sub-task 1e — Create `src/features/settings/panes/RoutingPane.tsx` (D-83). On mount: `getTaskRouting()` → local state. 5 dropdowns (one per row: code/vision/fast/creative/fallback). Each dropdown options = PROVIDERS map (id + name). Save button calls `setTaskRouting(routing)` then toast success. Display "No key stored — configure in Providers" inline if the chosen provider in any row has `getAllProviderKeys()` reporting `has_key: false`. Uses Card + native `<select>` + Button.

    Sub-task 1f — Create `src/features/settings/panes/VoicePane.tsx` (D-84). Reads `useConfig().config` for current voice_mode + tts_voice + voice_shortcut + quick_ask_shortcut + wake_word_enabled. Each editable field: Input or `<select>` (for voice_mode + tts_voice with hardcoded option lists: voice_mode = ['off', 'push_to_talk', 'always_on']; tts_voice = inline list).

    Save button calls `setConfig({ provider, apiKey: '', model, voiceMode, ttsVoice, quickAskShortcut, voiceShortcut })` per Rust set_config signature at commands.rs:1944. Use the existing `useConfig().config` for provider/model/api_key (apiKey: '' preserves keyring). Toast success.

    `wake_word_enabled` is **read-only display** in Phase 3 — no Rust setter command exists (config.rs:728 save_config_field doesn't accept it; commands.rs:1944 set_config doesn't accept it). Show the current value as a Pill ("Wake word: enabled" / "disabled") with a small notice: "Toggle wired in Phase 4 (needs new Rust command)." DO NOT attempt `saveConfigField('wake_word_enabled', ...)` — it will throw 'Unknown config field'.

    Pane header note: "Live TTS preview ships in Phase 4 (voice orb wiring)."

    Sub-task 1g — Create `src/features/settings/panes/PersonalityPane.tsx` (D-85). Reads `useConfig().config` for user_name, work_mode, response_style, blade_email. Each as Input. Save button (one per pane, not per field) calls `setConfig({ provider, apiKey: '', model, userName, workMode, responseStyle, bladeEmail })` then `useConfig().reload()`. Toast success. Below: a "Re-run persona onboarding" Button — on click, opens a confirmation `<Dialog>` (Phase 1 primitive); on confirm calls `resetOnboarding()` then triggers `useConfig().reload()`. The user is bounced back to onboarding by MainShell's gate (the next render of MainShell observes `getOnboardingStatus()` returning false).

    Sub-task 1h — Create `src/features/settings/panes/AppearancePane.tsx` (D-86). Pure readout. Sections:
    - "Theme: Liquid Glass dark (locked per D-15)" pill
    - "Typography: 4 self-hosted fonts (Syne / Bricolage / Fraunces / JetBrains Mono)" with size readout from tokens.css (or hardcoded list)
    - "Reset preferences" Button → `usePrefs().resetPrefs()` with confirmation dialog. Toast on success.
    - Note: "Accent picker / theme switcher permanently out of scope per D-15."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -q "error TS" && exit 1 || true; test -f src/features/settings/SettingsShell.tsx && test -f src/features/settings/settings.css && test -f src/features/settings/panes/ModelsPane.tsx && test -f src/features/settings/panes/RoutingPane.tsx && test -f src/features/settings/panes/VoicePane.tsx && test -f src/features/settings/panes/PersonalityPane.tsx && test -f src/features/settings/panes/AppearancePane.tsx; grep -q "settings-iot" src/features/settings/index.tsx; grep -q "settings-personality" src/features/settings/index.tsx; grep -q "settings-models" src/features/settings/index.tsx; grep -q "resetOnboarding" src/features/settings/panes/PersonalityPane.tsx; grep -q "setTaskRouting" src/features/settings/panes/RoutingPane.tsx; grep -q "setConfig" src/features/settings/panes/VoicePane.tsx; grep -nE "saveConfigField.*wake_word" src/features/settings/panes/VoicePane.tsx && { echo FAIL wake_word_enabled saveConfigField will throw; exit 1; } || true; grep -nE "backdrop-filter" src/features/settings/settings.css && { echo FAIL backdrop-filter; exit 1; } || true</automated>
  </verify>
  <done>
    - SettingsShell + settings.css + index.tsx replaced.
    - 5 of 10 panes shipped (Models, Routing, Voice, Personality, Appearance).
    - VoicePane uses setConfig (NOT saveConfigField for voice fields) and treats wake_word_enabled as read-only.
    - Route ids include the 5 renames/adds; legacy ids `settings-integrations` and `settings-ambient` removed (replaced).
    - npx tsc --noEmit passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship remaining 5 panes (Providers, IoT, Privacy, Diagnostics, About)</name>
  <files>src/features/settings/panes/ProvidersPane.tsx, src/features/settings/panes/IoTPane.tsx, src/features/settings/panes/PrivacyPane.tsx, src/features/settings/panes/DiagnosticsEntryPane.tsx, src/features/settings/panes/AboutPane.tsx</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/03-dashboard-chat-settings/03-PATTERNS.md §10 (ProvidersPane skeleton)
    - /home/arnav/blade/.planning/phases/03-dashboard-chat-settings/03-CONTEXT.md §D-81, D-87, D-88, D-89, D-90
    - /home/arnav/blade/src/features/onboarding/providers.ts (PROVIDERS registry)
    - /home/arnav/blade/src/types/iot.ts (IoTEntity / SpotifyTrack shapes)
    - /home/arnav/blade/src/types/history.ts (ConversationSummary shape)
    - /home/arnav/blade/src/lib/tauri/index.ts (verify iot/history wrappers landed)
    - /home/arnav/blade/package.json (read version for AboutPane)
    - /home/arnav/blade/src-tauri/src/iot_bridge.rs (verify HA env-var pattern; HA token sourcing)
  </read_first>
  <action>
    Sub-task 2a — Create `src/features/settings/panes/ProvidersPane.tsx` per pattern §10. Imports PROVIDERS from `@/features/onboarding/providers`. State: pending key per provider (`Record<string, string>`). On mount: `getAllProviderKeys()` populates which providers have stored keys. For each provider card: name + tagline + "Key stored: <masked>" pill OR "No key" pill + masked-input + Test button + "Save & switch" button. Test calls `testProvider({provider, apiKey: pending[id], model: defaultModel})` → toast success/error. Save calls `storeProviderKey(id, pending[id])` → `switchProvider(id, defaultModel)` → refresh → toast. After save, clear the pending[id] entry. Use Card + Input + Pill + Button + useToast. Failure path catches TauriError and shows the rustMessage in toast.

    Sub-task 2b — Create `src/features/settings/panes/IoTPane.tsx` (D-87). Reads `useConfig().config.ha_base_url`. Three sections:
    - Section 1: Connect Home Assistant — collapsible with ONE Input field (URL) + Save button. Save calls `saveConfigField('ha_base_url', url)`. If `ha_base_url` is empty on mount, show only the connect form. Below the URL field, an instructional notice: "Set the HA_TOKEN environment variable before launching BLADE — Phase 3 doesn't expose a token setter (no Rust command)." Link to docs.
    - Section 2: Entities (only when ha_base_url is set) — on mount calls `iotListEntities()`. Renders a list of entities with friendly_name + state. For switchable entities (domain === 'switch' or 'light'), include a toggle button calling `iotSetState(entity_id, newState)`.
    - Section 3: Spotify Now Playing — calls `iotSpotifyNowPlaying()` on mount. Renders title/artist/album if `is_playing`, else "Not playing".
    - All catches: if `iotListEntities` throws TauriError("not_found"), the wrapper isn't registered → show "Home Assistant integration not available" notice. Same for Spotify wrapper.

    Sub-task 2c — Create `src/features/settings/panes/PrivacyPane.tsx` (D-88). Static readout sections + one action:
    - "Local-first" header + paragraph: "BLADE runs entirely on your machine. Zero telemetry. All data lives at ~/.blade."
    - "API keys" section: read `getAllProviderKeys()` and list "Stored in OS keyring" rows for each provider with `has_key: true`. The masked field already comes from the Rust call.
    - "Audit": link/anchor `<a href="docs/architecture/2026-04-16-blade-body-architecture-design.md" target="_blank">Architecture audit</a>` + "Open BLADE config dir" button (Phase 4 may wire to a tauri shell command; Phase 3 just shows the path text).
    - "Conversation history": Button "Clear all conversation history" → confirmation Dialog → on confirm: `historyListConversations()` → loop `historyDeleteConversation(id)` for each → toast count. Catch errors and show partial-success toast.

    Sub-task 2d — Create `src/features/settings/panes/DiagnosticsEntryPane.tsx` (D-89). DEV-only path:
    - If `import.meta.env.DEV`: Button "Open full Diagnostics" → `useRouterCtx().openRoute('diagnostics-dev')` (Phase 1 dev route).
    - Show event listener counter from `window.__BLADE_LISTENERS_COUNT__` (DEV only; might be undefined → guard).
    - Show last `debugConfig()` output (collapsed JSON). Refreshes on a button click.
    - PROD path: card with "Diagnostics is part of the Admin cluster (ships in Phase 7)."
    - Both paths: small "Run wrapper smoke test" Button → `openRoute('wrapper-smoke')` (DEV only).

    Sub-task 2e — Create `src/features/settings/panes/AboutPane.tsx` (D-90). Pure static content:
    - BLADE wordmark + version (read from package.json — use Vite's `import.meta.env.PACKAGE_VERSION` if exposed, else hardcode the current version string and document staleness)
    - Tauri version (read from `@tauri-apps/api` package.json or hardcode)
    - Build date (use `import.meta.env.BUILD_DATE` if defined in vite.config.ts as a `define`; otherwise omit or hardcode)
    - Link: `<a href="https://github.com/anthropics/blade" target="_blank" rel="noopener noreferrer">GitHub</a>` (use the actual repo URL — operator confirms in summary)
    - "Built by Arnav" credit line
    - Use Card primitive.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -q "error TS" && exit 1 || true; test -f src/features/settings/panes/ProvidersPane.tsx && test -f src/features/settings/panes/IoTPane.tsx && test -f src/features/settings/panes/PrivacyPane.tsx && test -f src/features/settings/panes/DiagnosticsEntryPane.tsx && test -f src/features/settings/panes/AboutPane.tsx; grep -q "PROVIDERS" src/features/settings/panes/ProvidersPane.tsx; grep -q "iotListEntities\|iotSetState" src/features/settings/panes/IoTPane.tsx; grep -q "historyDeleteConversation\|historyListConversations" src/features/settings/panes/PrivacyPane.tsx; grep -q "diagnostics-dev\|wrapper-smoke" src/features/settings/panes/DiagnosticsEntryPane.tsx; grep -nE "saveConfigField.*ha_token" src/features/settings/panes/IoTPane.tsx && { echo FAIL ha_token saveConfigField will throw; exit 1; } || true; npm run verify:all 2>&1 | grep -q "FAIL" && exit 1 || exit 0</automated>
  </verify>
  <done>
    - All 5 remaining panes created with correct wrappers wired.
    - ProvidersPane reuses PROVIDERS registry (no duplication).
    - IoTPane saves only ha_base_url (NOT ha_token); shows env-var instructional notice for token.
    - IoTPane fail-soft when iot wrappers throw not_found.
    - PrivacyPane history-clear loop has confirmation dialog.
    - DiagnosticsEntryPane DEV/PROD paths separated.
    - AboutPane static; renders without error.
    - npx tsc --noEmit passes; npm run verify:all passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend save UI → Rust write commands | User intent crosses to provider key storage, routing config, voice/personality config |
| ProvidersPane masked-input → keyring | Store_provider_key writes to OS keyring |
| PrivacyPane "Clear conversation history" | Bulk delete is destructive |
| IoTPane → Home Assistant local network | URL entered crosses to HA instance via Rust HTTP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | Provider key entered + tested but never saved | accept | Save & switch is single-button path; Test is informational |
| T-03-06-02 | Info disclosure | API key visible in pending state in memory | mitigate | Cleared from state immediately after storeProviderKey; not persisted to localStorage |
| T-03-06-03 | Elevation | Re-run onboarding without confirmation | mitigate | PersonalityPane shows confirmation Dialog before resetOnboarding |
| T-03-06-04 | Tampering | RoutingPane saves with provider that has no key | mitigate | Inline "No key stored" notice + getAllProviderKeys cross-check; user warned |
| T-03-06-05 | Repudiation | History clear with no audit trail | accept | User initiated; toast confirms count |
| T-03-06-06 | DoS | historyListConversations + N delete calls | mitigate | Confirmation dialog; runs sequentially with toast; failed deletes surfaced |
| T-03-06-07 | Tampering | IoT wrapper not registered (Plan 03-02 noted possible) | mitigate | Pane catches TauriError("not_found") and shows fail-soft notice; SET-07 still satisfied |
| T-03-06-08 | Info disclosure | DiagnosticsEntryPane debugConfig() output may include sensitive paths | accept | DEV-only display; PROD path hides; debugConfig already redacts keys |
| T-03-06-09 | Tampering | settings.css backdrop-filter regression | mitigate | grep verify in CI |
| T-03-06-10 | DoS | All 10 panes lazy — initial route mount may be slow | accept | Suspense fallback shows GlassSpinner; first navigation completes in single tick |
| T-03-06-11 | Tampering | wake_word_enabled toggle attempt via saveConfigField throws | mitigate | Pane treats wake_word_enabled as read-only; comment cites D-66 + Rust setter absence; verify grep blocks regression |
| T-03-06-12 | Info disclosure | HA_TOKEN env var in process environment | accept | User-controlled launch env; standard practice for Home Assistant clients |
</threat_model>

<verification>
End-to-end checks for this plan:
- `npx tsc --noEmit` exits 0.
- 13 files created/replaced.
- `grep -nE "backdrop-filter" src/features/settings/settings.css` returns nothing (D-07 cap protected).
- `grep -nE "id: 'settings-(models|routing|appearance|privacy|diagnostics)'" src/features/settings/index.tsx` returns 5 matches (5 new tabs).
- `grep -nE "id: 'settings-(integrations|ambient)'" src/features/settings/index.tsx` returns nothing (legacy ids removed).
- `grep -nE "PROVIDERS" src/features/settings/panes/ProvidersPane.tsx src/features/settings/panes/RoutingPane.tsx` returns matches in both (registry reuse).
- `grep -nE "resetOnboarding" src/features/settings/panes/PersonalityPane.tsx` returns one match.
- `grep -nE "historyDeleteConversation" src/features/settings/panes/PrivacyPane.tsx` returns one match.
- `grep -nE "saveConfigField.*wake_word" src/features/settings/panes/VoicePane.tsx` returns nothing (would throw at runtime).
- `grep -nE "saveConfigField.*ha_token" src/features/settings/panes/IoTPane.tsx` returns nothing (would throw at runtime).
- `npm run verify:all` passes.
- `npm run verify:migration-ledger` passes (or executor documents the rename in summary if the script needs manual update).
</verification>

<success_criteria>
- All 13 files created/modified.
- 10 settings tabs functional and route-able from ⌘K palette.
- ProvidersPane Test + Save round-trip works against Rust (verified by Plan 03-07 spec).
- RoutingPane reads + writes TaskRouting.
- VoicePane updates voice fields via setConfig (correct Rust surface; saveConfigField doesn't accept voice fields).
- PersonalityPane "Re-run onboarding" successfully resets and the next MainShell render bounces user back to onboarding.
- PrivacyPane Clear history confirmation dialog works.
- IoTPane fails soft when iot wrappers absent; HA URL via save_config_field; HA token via env var instructional notice.
- AboutPane + AppearancePane + DiagnosticsEntryPane render without error.
- Plan completes under 40% context.
- Phase 3 SC-4 substrate ready for Plan 03-07 spec assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-chat-settings/03-06-SUMMARY.md` following the standard summary template.
</output>
