---
phase: 00-pre-rebuild-audit
plan: 02
type: execute
wave: 2
depends_on: [00-pre-rebuild-audit/00-01]
files_modified:
  - .planning/RECOVERY_LOG.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "`.planning/RECOVERY_LOG.md` exists at the `.planning/` root (per D-19, matches ROADMAP.md literal path) and has 5 top-level sections + 2 appendices"
    - "Section 1 documents the QuickAsk → Main bridge contract (invoke name, event name, payload shape, conversation persistence path), derived from commands.rs + docs/design/quickask.html"
    - "Section 2 documents the Voice Orb 4-phase state machine with driving events cited from voice_global.rs / wake_word.rs / voice*.rs"
    - "Section 3 documents the onboarding backend call sequence (`get_onboarding_status` → provider pick → key entry → `complete_onboarding` → `deep_scan_*`)"
    - "Section 4 is the full event catalog (every Rust emitter Phase 1's `useTauriEvent` will subscribe to)"
    - "Section 5 is the emit_all classification table copied verbatim from 00-EMIT-AUDIT.md — resolves WIRE-08"
    - "Appendix A is the 11-prototype flow map (copied/distilled from 00-PROTO-FLOW.md)"
    - "Appendix B is the Liquid Glass token set (extracted from shared.css / proto.css / orb.css via 00-PROTO-FLOW.md)"
    - "ROADMAP.md Phase 0 success criteria are reworded per CONTEXT.md §Success Criteria Reframe (no references to `src.bak/src/quickask.tsx`)"
    - "STATE.md no longer references the wrong `src.bak/src/quickask.tsx` path"
    - "Phase 0 audit bundle is committed to git in a single commit"
  artifacts:
    - path: ".planning/RECOVERY_LOG.md"
      provides: "Singular monolithic audit output — the contract map Phase 1 consumes"
      min_lines: 400
      contains: "QuickAsk"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 0 success criteria reframed to backend/prototype sources"
      contains: "docs/design/quickask.html"
    - path: ".planning/STATE.md"
      provides: "No references to the dead src.bak/src/quickask.tsx path"
      contains: "Phase 0"
  key_links:
    - from: ".planning/RECOVERY_LOG.md"
      to: "Phase 1 (FOUND-03/04 typed Tauri wrapper, FOUND-05 event registry, FOUND-06 `useTauriEvent`, WIRE-08)"
      via: "Phase 1 planning reads RECOVERY_LOG.md as authoritative contract map"
      pattern: "RECOVERY_LOG.md §1-§5 + Appendix B feed Phase 1 directly"
    - from: ".planning/ROADMAP.md Phase 0 Success Criteria"
      to: "CONTEXT.md §Success Criteria Reframe"
      via: "Rewording criteria #1-#5 to cite `commands.rs` + `docs/design/*.html` instead of `src.bak/`"
      pattern: "No criterion mentions `src.bak/`"
---

<objective>
Synthesize the three Wave-1 extracts into the singular deliverable per D-19: `.planning/RECOVERY_LOG.md`. Patch the two nearby project docs (ROADMAP.md Phase 0 success criteria + STATE.md) to match the D-17 source pivot. Commit the Phase 0 bundle.

Purpose: The three intermediate files from Plan 01 are not the deliverable — a single monolithic log that a reviewer can read in one sitting is. This plan also closes the documentation inconsistencies introduced by the source pivot.

Output:
- `.planning/RECOVERY_LOG.md` (new)
- `.planning/ROADMAP.md` (patched — Phase 0 criteria)
- `.planning/STATE.md` (patched — src.bak path reference)
- A single git commit capturing the Phase 0 audit bundle
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/00-pre-rebuild-audit/00-CONTEXT.md
@.planning/phases/00-pre-rebuild-audit/00-BACKEND-EXTRACT.md
@.planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md
@.planning/phases/00-pre-rebuild-audit/00-PROTO-FLOW.md

## Guardrails

1. Per **D-17**: `src.bak/` is DEAD. Nothing in the output or patches may cite `src.bak/` as a source.
2. Per **D-19**: The deliverable is `.planning/RECOVERY_LOG.md` (singular, at `.planning/` root — NOT inside the phase dir). The phase dir contains the intermediate extracts; the root contains the log.
3. No code. Do not touch anything under `src/` or `src-tauri/`.
4. Use the Edit tool for patches to existing files; use the Write tool for the new RECOVERY_LOG.md.
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Synthesize .planning/RECOVERY_LOG.md from the 3 extracts</name>
  <files>.planning/RECOVERY_LOG.md</files>
  <read_first>
    - .planning/phases/00-pre-rebuild-audit/00-BACKEND-EXTRACT.md
    - .planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md
    - .planning/phases/00-pre-rebuild-audit/00-PROTO-FLOW.md
    - .planning/phases/00-pre-rebuild-audit/00-CONTEXT.md (for D-19 structure: 5 sections + Appendix A + Appendix B)
    - .planning/STATE.md (for WIRE table to cross-reference "STATUS: WIRE REQUIRED" flags)
  </read_first>
  <action>
Read the three extract files in full. Do NOT re-read backend source files — the extracts already cite them. Synthesize into `.planning/RECOVERY_LOG.md` with this exact D-19 structure. Use the Write tool.

### Required structure of `.planning/RECOVERY_LOG.md`

```
# BLADE Skin Rebuild — Pre-Rebuild Audit Recovery Log

**Phase:** 00 — Pre-Rebuild Audit
**Created:** <today's date>
**Sources:**
- `src-tauri/src/*.rs` (backend contracts)
- `docs/design/*.html` + `*.css` (prototype flows + visual tokens)
- `.planning/research/*.md` + `.planning/codebase/*.md` (already-synthesized context)

**Per D-17, `src.bak/` was NOT read for this audit.**
**Per D-19, this is the singular deliverable for Phase 0.**

---

## 1. QuickAsk ↔ Main Bridge Contract

Derived from `src-tauri/src/commands.rs` + `docs/design/quickask.html` + `docs/design/quickask-voice.html`.

### 1.1 Submission path
- **Invoke name:** `quickask_submit` (STATUS: STUB REQUIRED per WIRE-01) — or, if already present, the file:line from 00-BACKEND-EXTRACT.md
- **Signature:** `async fn quickask_submit(app: AppHandle, text: String, ...) -> Result<String, String>` (from extract)
- **Payload:** <exact fields>
- **Submission UX:** from quickask.html — what the user sees, what keys submit, what visually confirms submission

### 1.2 Bridge event
- **Event name:** `blade_quickask_bridged` (per D-11)
- **Payload shape:** <list>
- **Emitter:** <file:line from extract, or STATUS: WIRE REQUIRED>
- **Consumer:** Main window (per D-14 this event is single-window → should use `emit_to("main", ...)`)

### 1.3 Conversation persistence path
- How the submitted conversation appears in the main window's history drawer
- Which Rust memory module stores it (cite from extract)

### 1.4 Open contract risks
- E.g., shortcut conflicts with CJK IME on macOS (per P-09 mentioned in ROADMAP.md Phase 4 SC #5)

## 2. Voice Orb Driving-Event State Machine

Derived from `voice_global.rs` + `wake_word.rs` + `voice*.rs` + `docs/design/voice-orb-states.html`.
OpenClaw math is already locked in PRIOR_ART.md via D-08 — do NOT re-document the math here; reference it.

### 2.1 4-Phase state map
| Phase state | Visual (prototype) | Driving Rust events (file:line) |
|-------------|--------------------|----------------------------------|
| Idle        | <from voice-orb-states.html> | <events from extract> |
| Listening   | <visual>           | <events> |
| Thinking    | <visual>           | <events> |
| Speaking    | <visual>           | <events> |

### 2.2 Transitions
<state-machine diagram in Mermaid or prose>

### 2.3 Wake word path
- `hey_blade_detected` (or whatever wake_word.rs emits) → transition to Listening
- Cite file:line from extract.

## 3. Onboarding Backend Wiring

Derived from `commands.rs` + `config.rs` + the 3 onboarding prototypes.

### 3.1 Boot check
- `get_onboarding_status` signature + return shape (from extract)
- Frontend behavior: if `completed == false` → show Provider Picker; if `true` → boot to default dashboard route.

### 3.2 Call sequence
1. Screen 1 (onboarding-01-provider.html): user picks provider → local state only
2. Screen 2 (onboarding-02-apikey.html): user enters API key → validated via provider check command (cite)
3. Screen 3 (onboarding-03-ready.html): `complete_onboarding({provider, key})` writes config + triggers `deep_scan_*` sequence
4. Deep scan: list each `deep_scan_*` command in order with its payload (from extract)
5. Enter main shell: dashboard route mounts

### 3.3 Event sequence during deep scan
<table of event name | payload | when fired | frontend handler>

## 4. Event Catalog (Phase 1 `useTauriEvent` Subscription Surface)

This is the definitive list of Rust-side emitters. Phase 1's `useTauriEvent` hook will subscribe to these. Any "STATUS: WIRE REQUIRED" row is a backend-wiring gap scoped to Phase 3 / Phase 4 (see .planning/STATE.md WIRE table).

| Event name | Rust file:line | Payload type | Consumer window | Status |
|------------|----------------|--------------|-----------------|--------|
| blade_stream_chunk | <cite> | <type> | main (chat) | LIVE or WIRE REQUIRED |
| blade_message_start | <cite> | <type> | main (chat) | WIRE REQUIRED (WIRE-03) |
| blade_thinking_chunk | <cite> | <type> | main (chat) | WIRE REQUIRED (WIRE-04) |
| blade_token_ratio | <cite> | <type> | main (chat) | WIRE REQUIRED (WIRE-06) |
| blade_agent_event | <cite> | <type> | main (agents) | WIRE REQUIRED (WIRE-05) |
| blade_quickask_bridged | <cite> | <type> | main | WIRE REQUIRED (WIRE-01) |
| hormone_update | <cite> | <type> | main + hud + body | WIRE REQUIRED (WIRE-02) |
| <all voice events>   | <cite> | <type> | voice overlay | LIVE |
| <all wake_word events> | <cite> | <type> | main + voice overlay | LIVE |
| <all deep_scan_* events> | <cite> | <type> | main (onboarding) | LIVE |
| <every other emitter from extract> | ... | ... | ... | ... |

## 5. emit_all Classification (WIRE-08 Resolution)

Copied verbatim from `.planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md` Classification Table. Phase 1's WIRE-08 task executes the `single-window` replacements.

### 5.1 Summary
- Total emit sites: N
- cross-window (keep emit_all): N
- single-window (convert to emit_to): N
- ambiguous (manual review in Phase 1): N

### 5.2 Full table
<paste the full classification table from 00-EMIT-AUDIT.md — do not elide>

### 5.3 Ambiguous rows — synthesis notes
For each `ambiguous` row, Claude's synthesis call:
- Row X: cross-window because <reason> | single-window because <reason>
- Resolve where possible; leave the rest for Phase 1 review.

---

## Appendix A — Prototype User-Flow Map

Distilled from `.planning/phases/00-pre-rebuild-audit/00-PROTO-FLOW.md` §A.

<11 short flow-contract entries, one per prototype, max ~10 lines each>

---

## Appendix B — Liquid Glass Token Set

Extracted from `docs/design/shared.css` + `proto.css` + `orb.css`. This seeds Phase 1's `src/styles/tokens.css` (FOUND-01).

### B.1 Color tokens
<table>

### B.2 Blur + backdrop-filter (per D-06, D-07: window-vibrancy for chrome; max 3 backdrop-filter per viewport; blur caps 20/12/8px)
<table>

### B.3 Motion tokens (per D-02: CSS-only; no Framer Motion)
<table>

### B.4 Spacing / radii / shadows
<table>

### B.5 Typography scale
<table>

### B.6 Conflicts / duplications between shared.css, proto.css, orb.css
<list — Phase 1 must resolve>

---

## Handoff to Phase 1

Phase 1 consumes this log for:
- **FOUND-01** (tokens.css) → Appendix B
- **FOUND-03/04** (typed Tauri wrapper) → §1 + §3 signatures
- **FOUND-05** (event registry) → §4
- **FOUND-06** (`useTauriEvent`) → §4 subscription surface
- **WIRE-08** (emit_all audit) → §5

*End of Recovery Log.*
```

### Synthesis rules

- **No `src.bak/` references** anywhere.
- **Every contract row / event row must cite a Rust file:line** (pulled from 00-BACKEND-EXTRACT.md). If the extract flagged a row as `STATUS: WIRE REQUIRED`, preserve that status in the log — do not invent signatures.
- **§5 full table must be copied from 00-EMIT-AUDIT.md verbatim** (not a sample).
- **Appendix B tokens must come from 00-PROTO-FLOW.md** (not re-read CSS).
- Target length: ≥ 400 lines of actual content.
  </action>
  <acceptance_criteria>
    - `test -f .planning/RECOVERY_LOG.md`
    - `wc -l < .planning/RECOVERY_LOG.md` is ≥ 400
    - `grep -c "^## " .planning/RECOVERY_LOG.md` is ≥ 5 (five top-level sections)
    - `grep -q "^## 1\." .planning/RECOVERY_LOG.md && grep -q "^## 2\." .planning/RECOVERY_LOG.md && grep -q "^## 3\." .planning/RECOVERY_LOG.md && grep -q "^## 4\." .planning/RECOVERY_LOG.md && grep -q "^## 5\." .planning/RECOVERY_LOG.md`
    - `grep -q "QuickAsk" .planning/RECOVERY_LOG.md && grep -q "Voice Orb" .planning/RECOVERY_LOG.md && grep -q "Onboarding" .planning/RECOVERY_LOG.md`
    - `grep -q "Appendix A" .planning/RECOVERY_LOG.md && grep -q "Appendix B" .planning/RECOVERY_LOG.md`
    - `grep -q "blade_quickask_bridged" .planning/RECOVERY_LOG.md` (bridge event named)
    - `grep -c "^| src-tauri/src/" .planning/RECOVERY_LOG.md` is ≥ 50 (emit audit table pulled in intact)
    - `! grep -q "src\.bak" .planning/RECOVERY_LOG.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/RECOVERY_LOG.md && [ $(wc -l < .planning/RECOVERY_LOG.md) -ge 400 ] && [ $(grep -c "^## " .planning/RECOVERY_LOG.md) -ge 5 ] && grep -q "QuickAsk" .planning/RECOVERY_LOG.md && grep -q "Voice Orb" .planning/RECOVERY_LOG.md && grep -q "Appendix A" .planning/RECOVERY_LOG.md && grep -q "Appendix B" .planning/RECOVERY_LOG.md && [ $(grep -c "^| src-tauri/src/" .planning/RECOVERY_LOG.md) -ge 50 ] && ! grep -q "src\.bak" .planning/RECOVERY_LOG.md</automated>
  </verify>
  <done>RECOVERY_LOG.md exists at `.planning/` root with ≥ 400 lines, 5 top-level sections (1-5), both appendices, ≥ 50 emit-audit rows, zero `src.bak` references.</done>
</task>

<task type="auto">
  <name>Task 2.2: Patch ROADMAP.md Phase 0 success criteria (reframe per D-17)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the section `### Phase 0: Pre-Rebuild Audit`)
    - .planning/phases/00-pre-rebuild-audit/00-CONTEXT.md §"Success Criteria Reframe" (authoritative wording)
  </read_first>
  <action>
Use the Edit tool. Replace the 5 success criteria under `### Phase 0: Pre-Rebuild Audit` with the reframed wording from CONTEXT.md §Success Criteria Reframe. The new criteria text to place under `**Success Criteria** (what must be TRUE):` is:

```
  1. `RECOVERY_LOG.md` exists in `.planning/` with explicit QuickAsk → Main bridge contract (invoke name, event name, payload shape, conversation persistence path) derived from `src-tauri/src/commands.rs` + `docs/design/quickask.html`.
  2. Voice Orb state machine documented — which Rust events (`voice_conversation_*`, `wake_word_detected`, etc.) drive each of the 4 phase states — derived from `src-tauri/src/voice_global.rs`, `src-tauri/src/wake_word.rs`, and `docs/design/voice-orb-states.html` (OpenClaw math locked via D-08; see PRIOR_ART.md).
  3. All Rust event emitters catalogued — every `emit_all` / `emit_to` site in `src-tauri/src/` with event name and payload type — forming the subscription surface for Phase 1's `useTauriEvent`.
  4. Onboarding backend wiring documented — `get_onboarding_status`, `complete_onboarding`, and `deep_scan_*` call sequence and payloads — derived from `src-tauri/src/commands.rs` + the 3 onboarding prototype screens.
  5. `emit_all` audit complete — every `app.emit_all(...)` classified as cross-window (keep) or single-window (convert to `emit_to`); proposed `emit_to(label, ...)` replacement inline for every single-window row.
```

Do NOT modify any other phase or any other part of ROADMAP.md. Only the Phase 0 Success Criteria block.

After editing, verify with grep that no criterion still mentions `src.bak`.
  </action>
  <acceptance_criteria>
    - `grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "docs/design/quickask.html"`
    - `grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "voice-orb-states.html"`
    - `grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "useTauriEvent"`
    - `! grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "src\.bak/src/quickask"` (dead path removed)
    - `! grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "43 event-listener sites"` (old forward-reference from src.bak removed)
    - Phase 1 Success Criteria block is UNCHANGED: `grep -A 25 "### Phase 1: Foundation" .planning/ROADMAP.md | grep -q "P-01"`
  </acceptance_criteria>
  <verify>
    <automated>grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "docs/design/quickask.html" && grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "useTauriEvent" && ! (grep -A 25 "### Phase 0: Pre-Rebuild Audit" .planning/ROADMAP.md | grep -q "src\.bak/src/quickask") && grep -A 25 "### Phase 1: Foundation" .planning/ROADMAP.md | grep -q "P-01"</automated>
  </verify>
  <done>ROADMAP.md Phase 0 criteria reworded to cite backend/prototype sources; no `src.bak/src/quickask` reference anywhere in the Phase 0 block; Phase 1+ blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2.3: Patch STATE.md (remove src.bak references per D-17)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (locate the src.bak references)
    - .planning/phases/00-pre-rebuild-audit/00-CONTEXT.md §Claude's Discretion (recommend remove not fix)
  </read_first>
  <action>
Use the Edit tool. Remove or reframe the `src.bak/` references in STATE.md. The specific lines to address:

1. In `## Current Position > **Current Focus:**` block: the text `Document the QuickAsk bridge, voice orb patterns, event listeners, and onboarding wiring from \`src.bak/\` before a single line of new code is written.` Replace `from \`src.bak/\`` with `from backend (\`src-tauri/src/\`) + prototypes (\`docs/design/*.html\`)` — matches D-17.

2. In `### Active Todos`:
   - Remove the bullet `- [ ] Read \`src.bak/src/quickask.tsx\` and document bridge contract` (this is superseded — the bridge contract is derived from commands.rs + quickask.html now).
   - Replace `- [ ] Audit \`src.bak/\` for voice orb, event listeners, onboarding patterns` with `- [ ] Extract voice orb driving events from \`src-tauri/src/voice_global.rs\` + \`wake_word.rs\``.

3. In `## Session Continuity > **Next action:**`: replace `read \`src.bak/src/quickask.tsx\`, \`src.bak/src/VoiceOrb.tsx\` (or equivalent)` with `read \`src-tauri/src/commands.rs\` + \`voice_global.rs\` and the prototype set under \`docs/design/\``.

4. In `## Session Continuity > **Context cliff notes:**`: keep the bullet `- \`src.bak/\` is the backup of the old \`src/\` — read-only reference, never import from it` AS-IS (it's correct guidance; D-17 just says don't mine it for Phase 0 audit patterns, not that it doesn't exist on disk).

After editing, grep to confirm no remaining reference to `src.bak/src/quickask.tsx` (the wrong path) and no remaining Phase-0-instruction telling the reader to read `src.bak/` for pattern recovery.
  </action>
  <acceptance_criteria>
    - `! grep -q "src\.bak/src/quickask" .planning/STATE.md` (wrong path gone)
    - `! grep -q "Audit \`src\.bak/\` for voice orb" .planning/STATE.md` (old todo replaced)
    - `! grep -q "Read \`src\.bak/src/quickask\.tsx\`" .planning/STATE.md` (old todo removed)
    - `grep -q "src-tauri/src/" .planning/STATE.md` (new backend reference present)
    - `grep -q "docs/design/" .planning/STATE.md` OR `grep -q "prototypes" .planning/STATE.md` (prototypes reference present)
    - `grep -q "src\.bak" .planning/STATE.md` — STILL TRUE (the "do not import" cliff-note bullet remains; D-17 did not delete src.bak from disk)
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "src\.bak/src/quickask" .planning/STATE.md && grep -q "src-tauri/src/" .planning/STATE.md && grep -q "src\.bak" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md no longer contains the wrong `src.bak/src/quickask.tsx` path; Phase 0 todos reframed to backend + prototypes; the legit "do-not-import" cliff-note remains.</done>
</task>

<task type="auto">
  <name>Task 2.4: Commit the Phase 0 audit bundle</name>
  <files>
    - .planning/RECOVERY_LOG.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/00-pre-rebuild-audit/00-BACKEND-EXTRACT.md
    - .planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md
    - .planning/phases/00-pre-rebuild-audit/00-PROTO-FLOW.md
    - .planning/phases/00-pre-rebuild-audit/00-01-PLAN.md
    - .planning/phases/00-pre-rebuild-audit/00-02-PLAN.md
    - .planning/phases/00-pre-rebuild-audit/00-01-SUMMARY.md (if Plan 01 produced one)
    - .planning/phases/00-pre-rebuild-audit/00-02-SUMMARY.md (this plan's summary)
  </files>
  <read_first>
    - git status (via Bash)
  </read_first>
  <action>
Run `git status` first to confirm all Phase 0 audit files are present and untracked/modified. Then stage the files explicitly by name (never `git add -A`):

```bash
git add .planning/RECOVERY_LOG.md \
        .planning/ROADMAP.md \
        .planning/STATE.md \
        .planning/phases/00-pre-rebuild-audit/00-BACKEND-EXTRACT.md \
        .planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md \
        .planning/phases/00-pre-rebuild-audit/00-PROTO-FLOW.md \
        .planning/phases/00-pre-rebuild-audit/00-01-PLAN.md \
        .planning/phases/00-pre-rebuild-audit/00-02-PLAN.md \
        .planning/phases/00-pre-rebuild-audit/00-01-SUMMARY.md \
        .planning/phases/00-pre-rebuild-audit/00-02-SUMMARY.md
```

(Skip any missing SUMMARY files — they're written by each plan's own output step. Do not fail the task if one is absent.)

Commit with the message:

```
docs(00): pre-rebuild audit — RECOVERY_LOG + extracts + doc patches
```

No `Co-Authored-By` line (per BLADE CLAUDE.md: Arnav is the author). No `--no-verify`, no `--amend`, no force-push. One fresh commit.

Verify the commit landed:
```bash
git log -1 --oneline
git show --stat HEAD | head -30
```
  </action>
  <acceptance_criteria>
    - `git log -1 --pretty=%s | grep -q "pre-rebuild audit"` (commit message contains phrase)
    - `git log -1 --stat | grep -q ".planning/RECOVERY_LOG.md"` (RECOVERY_LOG.md in latest commit)
    - `git log -1 --stat | grep -q "00-BACKEND-EXTRACT.md"` (extract files in latest commit)
    - `git log -1 --stat | grep -q "00-EMIT-AUDIT.md"`
    - `git log -1 --stat | grep -q "00-PROTO-FLOW.md"`
    - `git log -1 --stat | grep -q "ROADMAP.md"` (patch in commit)
    - `git log -1 --stat | grep -q "STATE.md"` (patch in commit)
    - `! git log -1 --pretty=%B | grep -q "Co-Authored-By"` (per BLADE CLAUDE.md — Arnav is the author)
    - `git status --porcelain | grep -v "^??" | wc -l` is `0` (no staged/unstaged Phase 0 files remain)
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "pre-rebuild audit" && git log -1 --stat | grep -q ".planning/RECOVERY_LOG.md" && git log -1 --stat | grep -q "00-BACKEND-EXTRACT.md" && git log -1 --stat | grep -q "00-EMIT-AUDIT.md" && git log -1 --stat | grep -q "00-PROTO-FLOW.md" && ! git log -1 --pretty=%B | grep -q "Co-Authored-By"</automated>
  </verify>
  <done>Single commit on master with RECOVERY_LOG.md + the 3 extracts + ROADMAP/STATE patches + both PLAN files; no Co-Authored-By line; subject line contains "pre-rebuild audit".</done>
</task>

</tasks>

<verification>
- `.planning/RECOVERY_LOG.md` exists and passes all Task 2.1 acceptance criteria.
- ROADMAP.md Phase 0 success criteria reworded; no `src.bak/src/quickask` reference.
- STATE.md Phase 0 todos reframed to backend + prototypes.
- One git commit captures the whole audit bundle.
- Reframed Phase 0 success criteria (per CONTEXT.md §Success Criteria Reframe) are now the canonical gating criteria; they hold:
  1. RECOVERY_LOG.md present with QuickAsk bridge contract (§1) derived from commands.rs + quickask.html ✓
  2. Voice Orb state machine documented (§2) with events from voice_global.rs / wake_word.rs / voice-orb-states.html ✓
  3. All Rust event emitters catalogued (§4) ✓
  4. Onboarding wiring documented (§3) ✓
  5. emit_all audit complete (§5) ✓
</verification>

<success_criteria>
Phase 0 is complete. Phase 1 is unblocked — its Foundation plans can now consume `.planning/RECOVERY_LOG.md` as authoritative contract input (typed Tauri wrappers from §1+§3, event registry from §4, useTauriEvent subscription surface from §4, WIRE-08 resolution from §5, tokens.css seeds from Appendix B).
</success_criteria>

<output>
After completion, create `.planning/phases/00-pre-rebuild-audit/00-02-SUMMARY.md`.
</output>
