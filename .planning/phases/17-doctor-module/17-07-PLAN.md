---
phase: 17-doctor-module
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/17-doctor-module/17-VERIFICATION.md
  - docs/testing ss/17-doctor-1280.png
  - docs/testing ss/17-doctor-1100.png
  - docs/testing ss/17-doctor-drawer-1280.png
  - docs/testing ss/17-doctor-drawer-1100.png
autonomous: false
requirements:
  - DOCTOR-01
  - DOCTOR-02
  - DOCTOR-03
  - DOCTOR-04
  - DOCTOR-05
  - DOCTOR-06
  - DOCTOR-07
  - DOCTOR-08
  - DOCTOR-09
  - DOCTOR-10
tags:
  - verification
  - blade-uat
  - phase-17

must_haves:
  truths:
    - "All static gates green: cargo check + cargo test --lib doctor + cargo test --lib evals + npx tsc --noEmit + npm run verify:all + bash scripts/verify-eval.sh"
    - "/blade-uat checklist (UI-SPEC § 17) executed in full — all 16 boxes ticked"
    - "Screenshots saved to docs/testing ss/ at both 1280×800 and 1100×700 for the Doctor pane and the drawer-open state"
    - "Each screenshot Read back by the agent + cited with a one-line observation in 17-VERIFICATION.md"
    - "Artificially failing eval flips Eval Scores row to Red without page reload (validates doctor_event push + useTauriEvent subscription)"
    - "ActivityStrip emission test shows '[Doctor] EvalScores → Red: ...' line"
    - "VERIFICATION.md exists with REQ → evidence table for DOCTOR-01..10"
  artifacts:
    - path: ".planning/phases/17-doctor-module/17-VERIFICATION.md"
      provides: "Phase 17 verification record + REQ→evidence map + UAT checklist results"
      contains: "DOCTOR-01"
      min_lines: 80
    - path: "docs/testing ss/17-doctor-1280.png"
      provides: "Doctor pane at 1280×800 viewport"
    - path: "docs/testing ss/17-doctor-1100.png"
      provides: "Doctor pane at 1100×700 viewport"
    - path: "docs/testing ss/17-doctor-drawer-1280.png"
      provides: "Drawer-open state at 1280×800"
    - path: "docs/testing ss/17-doctor-drawer-1100.png"
      provides: "Drawer-open state at 1100×700"
  key_links:
    - from: ".planning/phases/17-doctor-module/17-VERIFICATION.md"
      to: "docs/testing ss/ screenshots"
      via: "screenshot read-back observations cited in markdown"
      pattern: "docs/testing ss"
    - from: "/blade-uat checklist (UI-SPEC § 17)"
      to: "VERIFICATION.md UAT section"
      via: "16 checkboxes ticked + evidence per box"
      pattern: "UAT"
---

<objective>
Run the full Phase 17 verification gate. Static gates first (cargo + tsc + verify:all + verify:eval), then the mandatory `/blade-uat` runtime UAT per CLAUDE.md Verification Protocol — including the artificially-failing-eval test that proves end-to-end the `doctor_event` push + ActivityStrip emission flow. Capture screenshots at both required viewports, Read each one back, and write `17-VERIFICATION.md` with a REQ → evidence table.

Purpose: Phase 17 close. Per CLAUDE.md: "static gates ≠ done." This plan is the runtime checkpoint that v1.1 lacked. Without /blade-uat green + screenshots read back, Phase 17 cannot be marked complete.

Output: A green VERIFICATION.md with all 10 DOCTOR-XX requirements evidenced (mix of automated cargo tests + runtime UAT screenshots) plus 4 screenshots saved to `docs/testing ss/` (note literal space).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-doctor-module/17-CONTEXT.md
@.planning/phases/17-doctor-module/17-RESEARCH.md
@.planning/phases/17-doctor-module/17-PATTERNS.md
@.planning/phases/17-doctor-module/17-UI-SPEC.md
@.planning/phases/17-doctor-module/17-VALIDATION.md
@.planning/phases/17-doctor-module/17-01-SUMMARY.md
@.planning/phases/17-doctor-module/17-02-SUMMARY.md
@.planning/phases/17-doctor-module/17-03-SUMMARY.md
@.planning/phases/17-doctor-module/17-04-SUMMARY.md
@.planning/phases/17-doctor-module/17-05-SUMMARY.md
@.planning/phases/17-doctor-module/17-06-SUMMARY.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all static gates and capture results</name>
  <files>.planning/phases/17-doctor-module/17-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DOCTOR-01..10 — verification phrases for each REQ)
    - .planning/ROADMAP.md lines 86-100 (Phase 17 success criteria — 5 SCs + ROADMAP wording)
    - .planning/phases/17-doctor-module/17-VALIDATION.md (full validation strategy — sampling, manual-only items, Wave 0 requirements)
    - CLAUDE.md "Verification Protocol" — runtime UAT mandatory
  </read_first>
  <action>
    Run the full static gate chain. For each command, capture exit code + output tail (last ~10 lines). Record results in a temporary log so Task 3 can build VERIFICATION.md.

    Run in order (each command must exit 0):

    ```bash
    # 1. Rust unit tests — Doctor module
    cd /home/arnav/blade/src-tauri && cargo test --lib doctor -- --test-threads=1 --nocapture

    # 2. Rust unit tests — Evals (regression check after Plan 03 record_eval_run wiring)
    cd /home/arnav/blade/src-tauri && cargo test --lib evals -- --test-threads=1 --nocapture

    # 3. Rust unit tests — full lib (catches any cross-module regression)
    cd /home/arnav/blade/src-tauri && cargo test --lib -- --test-threads=1

    # 4. Rust compile check
    cd /home/arnav/blade/src-tauri && cargo check

    # 5. TypeScript compile check
    cd /home/arnav/blade && npx tsc --noEmit

    # 6. Static verify chain — covers all 31 verify:* gates
    cd /home/arnav/blade && npm run verify:all

    # 7. Eval gate (Phase 16 invariant; Plan 03 wired record_eval_run, must still pass)
    cd /home/arnav/blade && bash scripts/verify-eval.sh
    ```

    Confirm `tests/evals/history.jsonl` was populated by step 7 (Plan 03's record_eval_run wiring should have written 5 lines per eval run):
    ```bash
    wc -l /home/arnav/blade/tests/evals/history.jsonl
    head -1 /home/arnav/blade/tests/evals/history.jsonl
    ```

    Should show ≥ 5 lines (one per Phase 16 eval module). The first line should be valid JSON with fields `timestamp`, `module`, `top1`, `top3`, `mrr`, `floor_passed`, `asserted_count`, `relaxed_count`.

    If any command fails, STOP and report the specific failure to the user — do NOT proceed to Task 2 (`/blade-uat` is meaningless if static gates are red).

    Capture command outputs to `/tmp/phase-17-static-gates.log`:
    ```bash
    {
      echo "=== cargo test --lib doctor ==="; cd /home/arnav/blade/src-tauri && cargo test --lib doctor -- --test-threads=1 2>&1
      echo "=== cargo test --lib evals ==="; cd /home/arnav/blade/src-tauri && cargo test --lib evals -- --test-threads=1 2>&1
      echo "=== cargo test --lib (full) ==="; cd /home/arnav/blade/src-tauri && cargo test --lib -- --test-threads=1 2>&1 | tail -30
      echo "=== cargo check ==="; cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
      echo "=== tsc --noEmit ==="; cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -10
      echo "=== verify:all ==="; cd /home/arnav/blade && npm run verify:all 2>&1 | tail -30
      echo "=== verify-eval.sh ==="; cd /home/arnav/blade && bash scripts/verify-eval.sh 2>&1 | tail -30
      echo "=== history.jsonl ==="; wc -l /home/arnav/blade/tests/evals/history.jsonl 2>&1
    } | tee /tmp/phase-17-static-gates.log
    ```

    **Watch out (CLAUDE.md):**
    - DO NOT skip step 6 (verify:all) — it includes verify:contrast (DOCTOR-08 gate), verify:emit-policy, verify:tokens-consistency, and verify:migration-ledger.
    - DO NOT run `cargo build --release` or `npm run tauri build` — wastes ~5 min and is not part of static gates.
    - If a test is flaky (per VALIDATION.md "Status: ⚠️ flaky"), re-run with --test-threads=1 once. If still failing, treat as red.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri &amp;&amp; cargo test --lib doctor -- --test-threads=1 &amp;&amp; cargo test --lib evals -- --test-threads=1 &amp;&amp; cargo check &amp;&amp; cd /home/arnav/blade &amp;&amp; npx tsc --noEmit &amp;&amp; npm run verify:all &amp;&amp; bash scripts/verify-eval.sh &amp;&amp; test -s /home/arnav/blade/tests/evals/history.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/phase-17-static-gates.log` exists and contains 8 section headers
    - `grep -c "^=== " /tmp/phase-17-static-gates.log` returns at least 7
    - All Doctor unit tests pass (Plans 02–05 added ≥ 22 tests; sum: 4 enum/exhaustive (Plan 02) + 4 eval (Plan 03) + 3 capgap + 4 autoupdate (Plan 03) + 5 tentacle + 6 drift + 1 verbatim (Plan 04) + 6 transition + 1 format (Plan 05) ≈ 34 tests minimum)
    - `cd /home/arnav/blade/src-tauri && cargo test --lib doctor -- --test-threads=1 2>&1 | grep -E "test result:" | head -1` shows "ok" with `0 failed`
    - `cd /home/arnav/blade && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
    - `cd /home/arnav/blade && npm run verify:all` exits 0
    - `cd /home/arnav/blade && bash scripts/verify-eval.sh` exits 0
    - `test -s /home/arnav/blade/tests/evals/history.jsonl` exits 0 (file exists and is non-empty)
    - `wc -l /home/arnav/blade/tests/evals/history.jsonl | awk '{print $1}'` is >= 5
    - First line of history.jsonl parses as JSON: `head -1 /home/arnav/blade/tests/evals/history.jsonl | python3 -c "import json, sys; json.loads(sys.stdin.read())"` exits 0
  </acceptance_criteria>
  <done>All 7 static gates green; history.jsonl populated by verify:eval run; results captured in /tmp/phase-17-static-gates.log for VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Runtime UAT — /blade-uat checklist + 4 screenshots + artificially failing eval</name>
  <what-built>
    Phase 17 backend + frontend: doctor.rs module with 3 Tauri commands + doctor_event emission + ActivityStrip integration; DoctorPane.tsx with 5 severity-striped rows + Dialog drill-down; new "Doctor" sub-tab inside Diagnostics.tsx route. Static gates green per Task 1.
  </what-built>
  <how-to-verify>
    Run the full `/blade-uat` checklist per UI-SPEC § 17 (16 boxes). Specifically:

    **Setup:**
    1. Start dev server: `npm run tauri dev` from /home/arnav/blade
    2. Wait for Tauri window to mount (typically 10–30s on first launch)
    3. Confirm no Rust panic in stdout in first 10 seconds

    **A. Reachability (DOCTOR-07):**
    - Open Diagnostics route (sidebar → "Diagnostics" or whichever entry; or use ⌘K → "Diagnostics")
    - Click the "Doctor" tab pill (7th tab in the tab strip — should be after "Config")
    - Confirm DoctorPane mounts (rows render, no white screen)

    **B. Initial render (DOCTOR-02..05, DOCTOR-08, DOCTOR-10):**
    - All 5 rows render in locked order: "Eval Scores", "Capability Gaps", "Tentacle Health", "Config Drift", "Auto-Update"
    - On a fresh install:
      - Eval Scores: should be Green (history.jsonl was populated by Task 1's verify:eval run, all assert floors passed → floor_passed: true)
      - Capability Gaps: likely Green (no recent gaps in this dev session)
      - Tentacle Health: likely Green or Amber depending on supervisor state
      - Config Drift: Green if migration ledger clean + scan profile fresh; Amber/Red otherwise
      - Auto-Update: Green (Cargo.toml + lib.rs anchors both present per RESEARCH § I1)
    - Confirm severity stripe colors: green = `--status-success` (light green), amber = `--a-warm` (light orange), red = `--status-error` (red)
    - 4 distinct stripe colors should be visible across the 5 rows on a typical dev install

    **C. Drill-down drawer (DOCTOR-09):**
    - Click each row (5 clicks total)
    - For each: drawer opens centered, payload renders as formatted JSON (`<pre>` with indented JSON), suggested-fix text matches UI-SPEC § 15 verbatim for that (class, severity)
    - Press `Esc` → drawer closes; confirm focus returns to the row that was clicked (Pitfall 5 mitigation)
    - Click backdrop → drawer closes
    - Click "Close" button in footer → drawer closes

    **D. Manual refresh (DOCTOR-01):**
    - Click "Re-run all checks" button at top of pane
    - Button text changes to "Re-checking…"; button disabled
    - After ~500ms-1s (Node shell-out for ledger check is the slowest), button returns to "Re-run all checks"
    - Rows re-render with refreshed timestamps

    **E. Artificially failing eval (DOCTOR-06, DOCTOR-08, ROADMAP SC-3):**
    - In a separate terminal, with the dev server still running, append a synthetic FAILING line to history.jsonl:
      ```bash
      echo '{"timestamp":"'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'","module":"hybrid_search_eval","top1":2,"top3":4,"mrr":0.4,"floor_passed":false,"asserted_count":8,"relaxed_count":0}' >> /home/arnav/blade/tests/evals/history.jsonl
      ```
    - Click "Re-run all checks" in Doctor pane
    - The Eval Scores row should flip to Red (border-left: red, badge: RED)
    - Confirm `doctor_event` was emitted: ActivityStrip should show a new line `[Doctor] EvalScores → Red: ...` (D-21 / M-07 contract)
    - Click the Red Eval Scores row → drawer opens; suggested_fix string starts with "An eval module breached its asserted floor (top-3 below 80% or MRR below 0.6)." (UI-SPEC § 15 verbatim)
    - Cleanup: remove the synthetic line so it doesn't pollute future runs:
      ```bash
      head -n -1 /home/arnav/blade/tests/evals/history.jsonl > /tmp/h.jsonl && mv /tmp/h.jsonl /home/arnav/blade/tests/evals/history.jsonl
      ```

    **F. Screenshots (CLAUDE.md Verification Protocol):**
    - Save 4 screenshots (use the OS screenshot tool, save to `docs/testing ss/` — LITERAL SPACE in path):
      1. `docs/testing ss/17-doctor-1280.png` — Doctor pane at 1280×800 (resize the Tauri window to 1280×800)
      2. `docs/testing ss/17-doctor-1100.png` — Doctor pane at 1100×700
      3. `docs/testing ss/17-doctor-drawer-1280.png` — drawer open (any row clicked) at 1280×800
      4. `docs/testing ss/17-doctor-drawer-1100.png` — drawer open at 1100×700
    - For each PNG saved, the agent will Read it back (Task 3) and cite a one-line observation per CLAUDE.md "Screenshot read back" rule

    **G. Static checklist** (no UI):
    - Run `npm run verify:contrast` again post-`/blade-uat` — confirm no contrast regressions detected against the rendered DOM
    - Confirm keyboard tab-walk: Tab from page-level "Re-run all checks" button cycles through 5 rows in DOM order; Enter opens drawer; Esc closes; focus returns to row

    Report findings clearly. If any check fails, capture the specific symptom (e.g., "drawer opens but focus returns to tab pill, not row" — Pitfall 5 regression) so Plan 08 (gap closure) or revision can address.
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 UI-SPEC § 17 boxes are green AND 4 screenshots saved AND ActivityStrip line confirmed. Otherwise describe what failed (which box, what symptom).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read back screenshots + write VERIFICATION.md with REQ→evidence map</name>
  <files>.planning/phases/17-doctor-module/17-VERIFICATION.md</files>
  <read_first>
    - /tmp/phase-17-static-gates.log (Task 1 output)
    - docs/testing ss/17-doctor-1280.png (Read tool — multimodal PNG observation)
    - docs/testing ss/17-doctor-1100.png
    - docs/testing ss/17-doctor-drawer-1280.png
    - docs/testing ss/17-doctor-drawer-1100.png
    - .planning/REQUIREMENTS.md (DOCTOR-01..10 — verification phrases)
    - .planning/ROADMAP.md (Phase 17 5 success criteria)
    - .planning/phases/16-eval-scaffolding-expansion/16-VERIFICATION.md (template — REQ→evidence table format)
  </read_first>
  <action>
    First, Read each of the 4 screenshots using the Read tool (multimodal — Claude can describe PNG contents). For each screenshot, capture a 1-line observation describing what's visible (per CLAUDE.md: "Cite a one-line observation in the response").

    Example observations (you'll generate these for real after Reading):
    - "1280×800 Doctor pane: 5 rows visible (Eval Scores → Auto-Update), 4 distinct severity stripe colors (1 green, 2 amber, 1 red, 1 green), Re-run button top-right shows 'Re-run all checks' label, ActivityStrip mounted at bottom showing 1 [Doctor] line"
    - "1100×700 Doctor pane: 5 rows visible, layout intact, no overflow, tab strip wraps gracefully"
    - "Drawer-open at 1280×800 viewing Eval Scores Red: drawer centered, 'SUGGESTED FIX' label visible, suggested-fix text starts 'An eval module breached its asserted floor', 'RAW PAYLOAD' label visible, JSON formatted with 2-space indent"
    - "Drawer-open at 1100×700: drawer width clamped to 720px max, ~190px margin per side, payload pre scrollable, footer buttons 'Re-check this signal' and 'Close' both rendered"

    Then write `.planning/phases/17-doctor-module/17-VERIFICATION.md` with this structure:

    ```markdown
    ---
    phase: 17-doctor-module
    status: complete
    verified_at: <ISO date>
    static_gates_green: true
    runtime_uat_green: true
    screenshots_count: 4
    requirements_count: 10
    requirements_evidenced: 10
    ---

    # Phase 17 Verification — Doctor Module

    **Phase:** 17-doctor-module
    **Verified:** 2026-04-30 (or actual date)
    **Verification gate:** static (cargo + tsc + verify:all + verify:eval) + runtime UAT (`/blade-uat`)

    ## Static Gates (Task 1)

    | Gate | Command | Exit | Notes |
    |------|---------|------|-------|
    | cargo test --lib doctor | `cd src-tauri && cargo test --lib doctor -- --test-threads=1` | 0 | NN tests passed (cite count from log) |
    | cargo test --lib evals | `cd src-tauri && cargo test --lib evals -- --test-threads=1` | 0 | 5 modules @ MRR 1.000 (Phase 16 invariant preserved) |
    | cargo check | `cd src-tauri && cargo check` | 0 | clean |
    | tsc --noEmit | `npx tsc --noEmit` | 0 | clean |
    | verify:all | `npm run verify:all` | 0 | 31 gates green (count from log) |
    | verify-eval.sh | `bash scripts/verify-eval.sh` | 0 | history.jsonl populated (NN lines) |

    ## Runtime UAT (Task 2)

    `/blade-uat` checklist — 16 boxes per UI-SPEC § 17:

    - [x] Dev server runs cleanly (no panic in first 10s)
    - [x] Doctor sub-tab reachable from Diagnostics
    - [x] All 5 rows render in locked order
    - [x] Severity stripes match data-severity (4 colors visible across 5 rows)
    - [x] Click each row → drawer opens centered + payload + suggested-fix
    - [x] Esc closes drawer; focus returns to row
    - [x] Manual "Re-run all checks" button text changes to "Re-checking…"
    - [x] Artificially failing eval flips Eval Scores to Red without page reload
    - [x] Screenshot at 1280×800 saved + read back
    - [x] Screenshot at 1100×700 saved + read back
    - [x] Drawer screenshot at 1280×800 saved + read back
    - [x] Drawer screenshot at 1100×700 saved + read back
    - [x] verify:contrast PASS for all 4 severity stripes
    - [x] tsc --noEmit clean
    - [x] cargo check clean
    - [x] Keyboard tab-walk works (Tab through rows, Enter opens drawer, Esc closes, focus restores)
    - [x] ActivityStrip emission test: `[Doctor] EvalScores → Red: ...` line confirmed

    ## Screenshot Read-Back Observations

    ### 17-doctor-1280.png (1280×800)
    > <one-line observation generated from Read tool>

    ### 17-doctor-1100.png (1100×700)
    > <one-line observation>

    ### 17-doctor-drawer-1280.png (drawer open, 1280×800)
    > <one-line observation>

    ### 17-doctor-drawer-1100.png (drawer open, 1100×700)
    > <one-line observation>

    ## Requirement → Evidence Map

    | REQ | Description | Evidence |
    |-----|-------------|----------|
    | DOCTOR-01 | 3 Tauri commands callable end-to-end | `cargo test --lib doctor::tests` PASS; UAT step D ("Re-run all checks") confirms doctor_run_full_check; UAT step C confirms doctor_get_signal via drawer payload render |
    | DOCTOR-02 | Eval-score history surfaces | `cargo test --lib doctor::tests::eval_signal_*` (4 tests) PASS; UAT step E (artificially failing eval) flips Eval Scores to Red via history.jsonl read |
    | DOCTOR-03 | Capability-gap log aggregation | `cargo test --lib doctor::tests::capgap_signal_*` (3 tests) PASS; UAT step B confirms Capability Gaps row renders with severity from activity_timeline aggregation |
    | DOCTOR-04 | Tentacle health signal source | `cargo test --lib doctor::tests::tentacle_classify_*` (5 tests) PASS; UAT step B confirms Tentacle Health row renders with worst-of severity across supervisor + MCP integrations |
    | DOCTOR-05 | Config drift detection | `cargo test --lib doctor::tests::drift_classify_*` (6 tests) PASS; UAT step B confirms Config Drift row renders; verify:migration-ledger gate green |
    | DOCTOR-06 | doctor_event emission on regression | `cargo test --lib doctor::tests::transition_gate_*` (5 tests) PASS; UAT step E confirms event delivery (row updates without page reload + ActivityStrip line appears) |
    | DOCTOR-07 | Diagnostics admin tab Doctor pane | UAT step A (sub-tab reachable from Diagnostics route, click "Doctor" pill); persistence via prefs['admin.activeTab'] (verified via Diagnostics.tsx 5 surgical edits + tab survives page reload) |
    | DOCTOR-08 | Severity-tiered visual hierarchy (green/amber/red) | UAT step B + screenshots show 4 distinct stripe colors; verify:contrast PASS |
    | DOCTOR-09 | Per-signal drill-down | UAT step C (5 row-clicks, drawer opens with raw payload + suggested-fix); focus restoration verified |
    | DOCTOR-10 | Auto-update presence check | `cargo test --lib doctor::tests::autoupdate_classify_*` (4 tests) PASS; UAT step B confirms Auto-Update row Green on stock install (Cargo.toml + lib.rs anchors both present, classify_autoupdate runs LIVE per D-09 — not hardcoded) |

    ## Phase 17 Success Criteria (per ROADMAP.md)

    | SC | Description | Status |
    |----|-------------|--------|
    | SC-1 | 3 Tauri commands callable end-to-end | ✅ DOCTOR-01 evidence |
    | SC-2 | Doctor pane renders ≥5 distinct signal classes on fresh install | ✅ UAT step B confirms 5 rows visible |
    | SC-3 | Artificially failing eval lights up doctor surface red | ✅ UAT step E confirms Red transition |
    | SC-4 | doctor_event Tauri event emitted on regression | ✅ DOCTOR-06 evidence |
    | SC-5 | Auto-update presence check folded in as amber if not wired | ✅ DOCTOR-10 evidence (Green on stock install — flips to Amber if dep removed) |

    ## Test Count

    - Doctor unit tests: NN passing (target ≥ 30)
    - Phase 16 eval modules: 5/5 still PASS (record_eval_run insertion did not break asserted floors)
    - verify:all chain: 31/31 gates green

    ## Verification Protocol Sign-Off

    - [x] Static gates green
    - [x] Dev server ran cleanly
    - [x] All 4 screenshots saved to `docs/testing ss/`
    - [x] All 4 screenshots Read back + cited above
    - [x] Cross-viewport check (1280×800 + 1100×700)
    - [x] Round-trip exercised (artificially failing eval → red transition → ActivityStrip line)
    - [x] No raw `listen()` imports added
    - [x] No ghost CSS tokens (every var() reference grep-verified)

    **Phase 17 status: COMPLETE.**

    ## Open Items / Tech Debt

    - (none currently — list any minor items observed during UAT that should not block phase close)
    ```

    Replace `<one-line observation>` placeholders with the actual observations from Reading each screenshot. Replace `NN` with actual test counts from `/tmp/phase-17-static-gates.log`. Update `verified_at` with the actual ISO date.

    **Watch out:**
    - The screenshots MUST be Read back via the Read tool (Claude is multimodal; PNG content can be observed). Do NOT skip this step. Do NOT fabricate observations — Read each PNG and describe what's actually there.
    - The path to screenshots has a LITERAL SPACE: `docs/testing ss/` (NOT `docs/testing-ss/` and NOT `docs/testing_ss/`). Per CLAUDE.md "What NOT to Do": quote the path in any bash command.
    - VERIFICATION.md lives at `.planning/phases/17-doctor-module/17-VERIFICATION.md` (mirrors Phase 16's location pattern).
    - Do NOT include Co-Authored-By in any commit (CLAUDE.md "What NOT to Do").
    - If any UAT box failed, the phase is NOT complete — set frontmatter `status: tech_debt` (mirroring Phase 14/15 v1.1 pattern) with explicit gap notes.
  </action>
  <verify>
    <automated>test -f /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md &amp;&amp; test -f "/home/arnav/blade/docs/testing ss/17-doctor-1280.png" &amp;&amp; test -f "/home/arnav/blade/docs/testing ss/17-doctor-1100.png" &amp;&amp; test -f "/home/arnav/blade/docs/testing ss/17-doctor-drawer-1280.png" &amp;&amp; test -f "/home/arnav/blade/docs/testing ss/17-doctor-drawer-1100.png" &amp;&amp; grep -q "Phase 17 status: COMPLETE" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` exits 0
    - `wc -l /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` >= 80
    - `grep -c "DOCTOR-0[0-9]\|DOCTOR-10" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` >= 10 (all 10 REQs cited)
    - All 4 screenshots exist:
      - `test -f "/home/arnav/blade/docs/testing ss/17-doctor-1280.png"` exits 0
      - `test -f "/home/arnav/blade/docs/testing ss/17-doctor-1100.png"` exits 0
      - `test -f "/home/arnav/blade/docs/testing ss/17-doctor-drawer-1280.png"` exits 0
      - `test -f "/home/arnav/blade/docs/testing ss/17-doctor-drawer-1100.png"` exits 0
    - Each screenshot is non-empty: `for f in "/home/arnav/blade/docs/testing ss/17-doctor-1280.png" "/home/arnav/blade/docs/testing ss/17-doctor-1100.png" "/home/arnav/blade/docs/testing ss/17-doctor-drawer-1280.png" "/home/arnav/blade/docs/testing ss/17-doctor-drawer-1100.png"; do test -s "$f" || echo "EMPTY: $f"; done | grep -c "EMPTY:"` returns 0
    - `grep -F "Phase 17 status: COMPLETE" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` returns at least 1 match
    - `grep -F "/blade-uat" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` returns at least 1 match
    - `grep -F "ActivityStrip" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` returns at least 1 match
    - `grep -F "[Doctor] EvalScores → Red" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` returns at least 1 match (UAT step E confirmed)
    - All 5 SCs cited: `grep -c "SC-[1-5]" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` >= 5
    - Each screenshot has a read-back observation: `grep -c "17-doctor-1280.png\|17-doctor-1100.png\|17-doctor-drawer-1280.png\|17-doctor-drawer-1100.png" /home/arnav/blade/.planning/phases/17-doctor-module/17-VERIFICATION.md` >= 4
  </acceptance_criteria>
  <done>VERIFICATION.md ships with all 10 DOCTOR-XX REQs evidenced, 5 SCs green, 4 screenshots saved + Read back + cited, /blade-uat checklist 16/16 boxes ticked, ActivityStrip emission confirmed, Phase 17 status: COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri dev server → user inspection | Standard dev surface |
| Test fixture line append → history.jsonl | Synthetic data appended for UAT step E; cleanup step removes it |
| Screenshot file save → docs/testing ss/ | Local filesystem; no remote upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Persistence of test data | UAT step E synthetic history.jsonl line | mitigate | UAT step E explicit cleanup: `head -n -1 ... > /tmp/h.jsonl && mv` removes the synthetic Red-trigger line. Verifier confirms post-UAT that history.jsonl no longer contains the synthetic record. If skipped, the next `/blade-uat` run starts in Red state — not a security issue but a developer-experience issue documented in the cleanup step. |
| T-17-02 | Information disclosure | Screenshot content | accept | Screenshots may show developer's local paths in raw payload (e.g., autoupdate signal payload `cargo_toml_path`). Acceptable for /blade-uat artifacts; /testing\ ss/ is the sanctioned testing directory. |
| T-17-03 | Verification gap regression | Static gates pass + UAT skipped | mitigate | Plan 07 makes the human-verify checkpoint a HARD gate (`gate="blocking"`). Per CLAUDE.md "Verification Protocol": Phase 17 has UI surface, runtime UAT mandatory. Without screenshots saved + read back, the phase is NOT closed — agent reports tech_debt status, not COMPLETE. |
</threat_model>

<verification>
- All 7 static gates exit 0 (Task 1 acceptance criteria)
- All 4 screenshots saved with non-empty file sizes
- VERIFICATION.md exists with all 10 DOCTOR-XX REQs evidenced + 5 SCs + UAT 16-box checklist
- /blade-uat step E artificially-failing-eval test confirms doctor_event push + ActivityStrip line
</verification>

<success_criteria>
1. All 7 static gates green (cargo test/check, tsc, verify:all, verify-eval.sh, history.jsonl populated)
2. /blade-uat checklist 16/16 boxes ticked (UI-SPEC § 17)
3. 4 screenshots saved at correct paths (literal-space `docs/testing ss/`) + Read back + observations cited in VERIFICATION.md
4. Artificially failing eval test (UAT step E) confirms end-to-end: history.jsonl write → doctor_run_full_check → severity transition → doctor_event emit → ActivityStrip line render
5. VERIFICATION.md status: COMPLETE; all 10 DOCTOR-XX REQs evidenced; all 5 ROADMAP SCs green
</success_criteria>

<output>
After completion, the executor should also:
1. Create `.planning/phases/17-doctor-module/17-07-SUMMARY.md` (the per-plan summary)
2. Update `.planning/STATE.md` to reflect Phase 17 status (`status: phase_complete`, `completed_phases: 2`, `last_activity` line)
3. Update `.planning/ROADMAP.md` Phase 17 row to `[x]` with shipped date

These housekeeping updates close the phase per BLADE conventions.
</output>
