---
phase: 07-dev-tools-admin
plan: 05
type: execute
wave: 2
depends_on: ['07-01', '07-02']
files_modified:
  - src/features/admin/Analytics.tsx
  - src/features/admin/CapabilityReports.tsx
  - src/features/admin/Reports.tsx
  - src/features/admin/DecisionLog.tsx
  - src/features/admin/SecurityDashboard.tsx
  - src/features/admin/SecurityAlertsTab.tsx
  - src/features/admin/SecurityScansTab.tsx
  - src/features/admin/SecurityPoliciesTab.tsx
  - src/features/admin/SecurityPentestTab.tsx
  - src/features/admin/admin-rich-a.css
autonomous: true
requirements:
  - ADMIN-01 # Analytics — KPI summary + events feed + prune
  - ADMIN-02 # CapabilityReports — evolution + self_upgrade + self_critique + tool_forge
  - ADMIN-03 # (synthetic Reports route per P-03) — reports CRUD + webhook config
  - ADMIN-04 # DecisionLog — decision gate history + feedback + authority audit + global audit
  - ADMIN-05 # SecurityDashboard — overview + scans + audit + policies + pentest (5 tabs)
must_haves:
  truths:
    - "Analytics renders 4-panel dashboard (D-182): dbAnalyticsSummary hero KPIs (total events, by_type breakdown), dbEventsSince feed (last 100 events with timestamp + type), 'Track event' debug form invoking dbTrackEvent, 'Prune analytics' Dialog-confirm invoking dbPruneAnalytics"
    - "CapabilityReports renders sections (D-182): 'Level' hero (evolutionGetLevel), 'Suggestions' list (evolutionGetSuggestions + Install/Dismiss buttons → evolutionInstallSuggestion/evolutionDismissSuggestion Dialog-confirm), 'Catalog' (selfUpgradeCatalog + Install → selfUpgradeInstall Dialog-confirm), 'Audit' (selfUpgradeAudit read), 'Gaps' (evolutionLogCapabilityGap entry form + immuneResolveGap action), 'Self-critique' (selfCritiqueHistory + 'Run deep roast' → selfCritiqueDeepRoast + 'Weekly meta' → selfCritiqueWeeklyMeta), 'Forge' (forgeListTools + 'New tool' → forgeNewTool + forgeDeleteTool + forgeTestTool)"
    - "Reports (P-03 synthetic) renders reports list from getReports() as reverse-chrono cards each with source column (D-185 Discretion); detail pane for selected report; 'Mark resolved' → updateReportStatus({id, status: 'resolved'}) + toast; 'Log gap' form → reportGap({summary}) to manually add; webhook config in footer (getReportWebhook + setReportWebhook Dialog-gated)"
    - "DecisionLog renders getDecisionLog({limit: 100}) as reverse-chrono list (D-182); each row: decision_type + confidence + outcome chip + 'Feedback' action → Dialog → decisionFeedback({decisionId, wasCorrect, note}); 'Evaluate' panel → decisionEvaluate({signal}) free-form debug input"
    - "DecisionLog sub-tabs persisted via prefs.admin.activeTab with key-prefix 'dlog:': 'Decisions' (main list), 'Authority' (authorityGetAuditLog + authorityGetDelegations), 'Audit' (auditGetLog — global audit)"
    - "SecurityDashboard hero card (D-183): securityOverview() status traffic-light (ok/warn/critical via status tokens); active alert count chip"
    - "SecurityDashboard 5-tab layout (persisted via prefs.admin.activeTab prefix 'sec:'): Alerts / Scans / Audit / Policies / Pentest — each is a separate sub-component file for isolation (D-183)"
    - "SecurityAlertsTab: derives alerts from securityOverview + securityCheckBreach + securityCheckUrl form input; alert cards with severity status token"
    - "SecurityScansTab: 'Run network scan' → securityScanNetwork; 'Scan sensitive files' → securityScanSensitiveFiles; results as findings table with severity-colored rows"
    - "SecurityAuditTab (part of same tab layout, may be merged with Scans per SUMMARY choice): 'Run full audit' → securityRunAudit; 'Deps audit' → securityAuditDeps; 'Code scan' → securityScanCode (with code path input)"
    - "SecurityPoliciesTab: symbolicListPolicies list; 'Add policy' Dialog → symbolicAddPolicy; per-policy 'Check' → symbolicCheckPolicy input; 'Verify plan' → symbolicVerifyPlan with plan JSON textarea"
    - "SecurityPentestTab (DANGER ZONE D-183): ALL-CAPS warning banner; pentestListAuth current authorizations; 'Authorize' Dialog (target + rationale required; variant=danger) → pentestAuthorize; 'Revoke' per auth → pentestRevoke; 'Check auth' → pentestCheckAuth; 'Check model safety' → pentestCheckModelSafety"
    - "data-testid attributes for Plan 07-07 spec: analytics-root, analytics-kpi, capability-reports-root, reports-root, report-row, decision-log-root, decision-row, decision-feedback-button, security-dashboard-root, security-hero, security-tab, security-pentest-warning, danger-banner"
    - "No raw invoke / listen (ESLint no-raw-tauri passes); no ChatProvider re-hoist (D-134); no files_modified overlap with Plans 07-03 / 07-04 / 07-06"
    - "npx tsc --noEmit passes; npm run verify:all passes"
  artifacts:
    - path: "src/features/admin/Analytics.tsx"
      provides: "ADMIN-01 — KPI dashboard + events feed + prune"
      exports: ["Analytics"]
    - path: "src/features/admin/CapabilityReports.tsx"
      provides: "ADMIN-02 — evolution + self_upgrade + self_critique + tool_forge sections"
      exports: ["CapabilityReports"]
    - path: "src/features/admin/Reports.tsx"
      provides: "P-03 synthetic — reports CRUD + webhook config"
      exports: ["Reports"]
    - path: "src/features/admin/DecisionLog.tsx"
      provides: "ADMIN-04 — decision gate history + feedback + authority audit + global audit"
      exports: ["DecisionLog"]
    - path: "src/features/admin/SecurityDashboard.tsx"
      provides: "ADMIN-05 — 5-tab security surface"
      exports: ["SecurityDashboard"]
    - path: "src/features/admin/SecurityAlertsTab.tsx"
      provides: "Security Alerts sub-tab"
      exports: ["SecurityAlertsTab"]
    - path: "src/features/admin/SecurityScansTab.tsx"
      provides: "Security Scans + Audit sub-tab (merged for density)"
      exports: ["SecurityScansTab"]
    - path: "src/features/admin/SecurityPoliciesTab.tsx"
      provides: "Security Policies sub-tab (symbolic::*)"
      exports: ["SecurityPoliciesTab"]
    - path: "src/features/admin/SecurityPentestTab.tsx"
      provides: "Security Pentest sub-tab (DANGER ZONE, D-183)"
      exports: ["SecurityPentestTab"]
    - path: "src/features/admin/admin-rich-a.css"
      provides: "Plan 07-05 scoped CSS — KPI grid + alerts list + pentest banner + reports list"
  key_links:
    - from: "src/features/admin/Analytics.tsx"
      to: "src/lib/tauri/admin.ts (dbAnalyticsSummary, dbEventsSince, dbTrackEvent, dbPruneAnalytics)"
      via: "invokeTyped via wrappers"
      pattern: "dbAnalyticsSummary|dbEventsSince|dbTrackEvent|dbPruneAnalytics"
    - from: "src/features/admin/CapabilityReports.tsx"
      to: "src/lib/tauri/admin.ts (evolutionGetLevel, evolutionGetSuggestions, evolutionInstallSuggestion, evolutionDismissSuggestion, evolutionRunNow, evolutionLogCapabilityGap, immuneResolveGap, selfUpgradeCatalog, selfUpgradeInstall, selfUpgradeAudit, selfCritiqueHistory, selfCritiqueDeepRoast, selfCritiqueWeeklyMeta, forgeListTools, forgeNewTool, forgeDeleteTool, forgeTestTool)"
      via: "invokeTyped via wrappers"
      pattern: "evolution|selfUpgrade|selfCritique|forge|immuneResolveGap"
    - from: "src/features/admin/Reports.tsx"
      to: "src/lib/tauri/admin.ts (getReports, reportGap, updateReportStatus, setReportWebhook, getReportWebhook)"
      via: "invokeTyped"
      pattern: "getReports|reportGap|updateReportStatus|ReportWebhook"
    - from: "src/features/admin/DecisionLog.tsx"
      to: "src/lib/tauri/admin.ts (getDecisionLog, decisionFeedback, decisionEvaluate, authorityGetAuditLog, authorityGetDelegations, auditGetLog)"
      via: "invokeTyped"
      pattern: "getDecisionLog|decisionFeedback|decisionEvaluate|authorityGetAuditLog|authorityGetDelegations|auditGetLog"
    - from: "src/features/admin/SecurityDashboard.tsx"
      to: "src/lib/tauri/admin.ts (securityOverview, securityScanNetwork, securityCheckBreach, securityCheckUrl, securityScanSensitiveFiles, securityRunAudit, securityAuditDeps, securityScanCode, symbolicListPolicies, symbolicAddPolicy, symbolicCheckPolicy, symbolicVerifyPlan, pentestListAuth, pentestAuthorize, pentestRevoke, pentestCheckAuth, pentestCheckModelSafety)"
      via: "invokeTyped"
      pattern: "security|symbolic|pentest"
---

<objective>
Ship Admin cluster subset A — the 5 high-value surfaces: Analytics (ADMIN-01), CapabilityReports (ADMIN-02), Reports (P-03 synthetic), DecisionLog (SC-3), SecurityDashboard (SC-4 alerts). Each route becomes a real glass-native surface backed by Plan 07-02 wrappers + Plan 07-01 Prefs.

Purpose: ROADMAP Phase 7 SC-3 — "DecisionLog reads decision-gate history from `decision_gate_*` commands" — directly falsified by this plan's DecisionLog. SC-4 — "SecurityDashboard surfaces active alerts from `security_monitor.rs`" — directly falsified by this plan's SecurityDashboard hero + alerts tab. Plan closes ADMIN-01..05 for 5 of the 11 Admin routes. Plan 07-06 closes the remaining 6.

Output: 5 replaced per-route files + 4 security-tab sub-components + 1 scoped CSS partial. ~1200-1500 net new lines. Plan completes under 48% context (higher than typical due to security-dashboard sub-component count, but within Phase 6 06-05 precedent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-dev-tools-admin/07-CONTEXT.md
@.planning/phases/07-dev-tools-admin/07-PATTERNS.md
@.planning/phases/07-dev-tools-admin/07-02-SUMMARY.md
@.planning/phases/06-life-os-identity/06-05-PLAN.md
@.planning/RECOVERY_LOG.md
@CLAUDE.md
@src/lib/tauri/admin.ts
@src/lib/events/index.ts
@src/hooks/usePrefs.ts
@src/design-system/primitives/index.ts
@src/features/admin/index.tsx
@src/features/admin/types.ts
@src/features/admin/admin.css
@src/lib/context/ToastContext.tsx

<interfaces>
```ts
// Surfaces consumed from Plan 07-02:
import {
  dbAnalyticsSummary, dbEventsSince, dbTrackEvent, dbPruneAnalytics,
  getReports, reportGap, updateReportStatus, setReportWebhook, getReportWebhook,
  evolutionGetLevel, evolutionGetSuggestions, evolutionDismissSuggestion, evolutionInstallSuggestion, evolutionRunNow, evolutionLogCapabilityGap,
  immuneResolveGap,
  selfUpgradeCatalog, selfUpgradeInstall, selfUpgradeAudit,
  selfCritiqueResponse, selfCritiqueHistory, selfCritiqueDeepRoast, selfCritiqueWeeklyMeta,
  forgeNewTool, forgeListTools, forgeDeleteTool, forgeTestTool,
  getDecisionLog, decisionFeedback, decisionEvaluate,
  authorityGetAgents, authorityGetAuditLog, authorityGetDelegations,
  auditGetLog,
  securityOverview, securityScanNetwork, securityCheckBreach, securityCheckPasswordHash, securityScanSensitiveFiles, securityCheckUrl, securityRunAudit, securityAuditDeps, securityScanCode,
  symbolicListPolicies, symbolicAddPolicy, symbolicCheckPolicy, symbolicVerifyPlan,
  pentestAuthorize, pentestCheckAuth, pentestRevoke, pentestListAuth, pentestCheckModelSafety,
} from '@/lib/tauri/admin';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship Analytics + CapabilityReports + Reports (ADMIN-01..02 + P-03 synthetic)</name>
  <files>src/features/admin/Analytics.tsx, src/features/admin/CapabilityReports.tsx, src/features/admin/Reports.tsx, src/features/admin/admin-rich-a.css</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §4, §9
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-182, §D-185
    - /home/arnav/blade/src/lib/tauri/admin.ts (Plan 07-02 output)
    - /home/arnav/blade/src/features/admin/admin.css (base from Plan 07-02)
    - /home/arnav/blade/src/features/life-os/FinanceView.tsx (Phase 6 KPI row pattern reference)
  </read_first>
  <action>
    **Analytics.tsx** — per D-182:
    - On mount: `Promise.all([dbAnalyticsSummary(), dbEventsSince({since: Date.now() - 24*3600*1000, limit: 100})])`.
    - Panel 1 (hero KPI row): `AnalyticsSummary.total_events` big number + `by_type` breakdown as 4-card grid (top 4 event types by count). Uses `.admin-kpi-grid` from admin.css.
    - Panel 2 (events feed): reverse-chrono list of `AnalyticsEvent[]` — each row: timestamp + type + payload preview (JSON.stringify truncated to 100 chars). Scrollable.
    - Panel 3 (track event debug form): type input + payload JSON textarea + "Track" button → `dbTrackEvent({type, payload: JSON.parse(payload)})` + toast + refresh feed.
    - Panel 4 (prune): "Prune analytics older than N days" input + Dialog-confirm → `dbPruneAnalytics({olderThanDays})` + toast.
    - data-testid: `analytics-root`, `analytics-kpi`, `analytics-events-feed`, `analytics-event-row`, `analytics-track-form`, `analytics-prune-button`.

    **CapabilityReports.tsx** — per D-182:
    - Sections rendered in order:
      1. "Level" hero card: `evolutionGetLevel()` → big number / text.
      2. "Suggestions" section: `evolutionGetSuggestions()` → cards with title + description + 2 buttons: "Install" → `evolutionInstallSuggestion(id)` Dialog-confirm; "Dismiss" → `evolutionDismissSuggestion(id)`. "Run evolution now" top-action → `evolutionRunNow()`.
      3. "Catalog" section: `selfUpgradeCatalog()` → entries; per-entry "Install" Dialog-confirm → `selfUpgradeInstall({id})`.
      4. "Audit" section: `selfUpgradeAudit()` → read-only list.
      5. "Gaps" section: `evolutionLogCapabilityGap({description, category})` inline form + "Log gap" button; "Resolve" button per gap → `immuneResolveGap({gapId})`.
      6. "Self-critique" section: `selfCritiqueHistory({limit: 20})` → list; "Run deep roast" button → `selfCritiqueDeepRoast({subject})` Dialog; "Weekly meta" button → `selfCritiqueWeeklyMeta()`.
      7. "Forge" section: `forgeListTools()` → tool cards; "New tool" Dialog → `forgeNewTool({name, definition})`; per-tool "Test" → `forgeTestTool({id})`; "Delete" Dialog-confirm → `forgeDeleteTool({id})`.
    - data-testid: `capability-reports-root`, `evolution-level`, `evolution-suggestion`, `capability-catalog-entry`, `capability-gap-form`, `self-critique-row`, `forge-tool-card`.

    **Reports.tsx** — P-03 synthetic:
    - On mount: `getReports()`.
    - Main list: reverse-chrono cards — each row shows `summary`, `status` chip (open/investigating/resolved via status tokens), `source` column (per D-185 Discretion — show source = 'capability_gap_detected' or manual).
    - Detail pane (if row selected): full card + "Mark investigating" / "Mark resolved" Dialog-confirm → `updateReportStatus({id, status})` + refresh.
    - Top action: "Log gap" inline form (summary input + "Log" button) → `reportGap({summary})` + toast + refresh. This is the MANUAL entry point; the automatic entry point is backend `capability_gap_detected` events (Phase 1 P-03).
    - Footer: webhook config — `getReportWebhook()` read; "Set webhook" Dialog-gated → `setReportWebhook({url})`. URL input validated client-side (regex for http(s)).
    - data-testid: `reports-root`, `reports-list`, `report-row`, `report-detail`, `report-status-button`, `report-log-gap-form`, `report-webhook-input`.

    **admin-rich-a.css** — scoped partial:
    ```css
    /* src/features/admin/admin-rich-a.css — Plan 07-05 scoped partial */

    @layer features {
      .analytics-events-feed {
        max-height: 400px;
        overflow-y: auto;
        display: flex;
        flex-direction: column;
        gap: var(--sp-1);
      }

      .analytics-event-row {
        padding: var(--sp-2);
        background: rgba(255, 255, 255, 0.02);
        border-bottom: 1px solid var(--line);
        font-size: 12px;
      }

      .capability-section {
        padding: var(--sp-3) 0;
        border-bottom: 1px solid var(--line);
      }

      .capability-section h3 {
        font-size: 16px;
        margin-bottom: var(--sp-2);
        color: var(--t-1);
      }

      .capability-forge-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: var(--sp-2);
      }

      .reports-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--sp-2);
      }

      .report-row {
        padding: var(--sp-2);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        cursor: pointer;
        margin-bottom: var(--sp-1);
      }

      .report-row[data-selected="true"] {
        background: rgba(255, 255, 255, 0.08);
      }

      .report-source-chip {
        font-size: 10px;
        padding: 2px 6px;
        background: rgba(255, 255, 255, 0.08);
        border-radius: var(--radius-pill);
        color: var(--t-3);
      }
    }
    ```

    **CRITICAL:**
    - P-03 discipline: Reports route MUST be reachable via `openRoute('reports')` backend → frontend wiring (already present in Phase 1).
    - All destructive operations (Install, Prune, Resolve, Dismiss, Delete forge tool, Set webhook) Dialog-confirmed per Pattern §4.
    - Status tokens used for all state colors (no hex).
  </action>
  <verify>
    <automated>test -f src/features/admin/Analytics.tsx && test -f src/features/admin/CapabilityReports.tsx && test -f src/features/admin/Reports.tsx && test -f src/features/admin/admin-rich-a.css && grep -q "dbAnalyticsSummary" src/features/admin/Analytics.tsx && grep -q "evolutionGetSuggestions\|selfUpgradeCatalog\|forgeListTools" src/features/admin/CapabilityReports.tsx && grep -q "getReports\|reportGap" src/features/admin/Reports.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>
    3 real route components + 1 CSS partial shipped; each invokes at least one admin wrapper; `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship DecisionLog + SecurityDashboard (+ 4 security sub-tabs) (ADMIN-04..05 — SC-3 + SC-4)</name>
  <files>src/features/admin/DecisionLog.tsx, src/features/admin/SecurityDashboard.tsx, src/features/admin/SecurityAlertsTab.tsx, src/features/admin/SecurityScansTab.tsx, src/features/admin/SecurityPoliciesTab.tsx, src/features/admin/SecurityPentestTab.tsx</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §4, §10
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-182, §D-183
    - /home/arnav/blade/src/lib/tauri/admin.ts
    - /home/arnav/blade/src/lib/events/index.ts (Plan 07-01 — check if DECISION_GATE_EVENT / SECURITY_ALERT exist)
    - /home/arnav/blade/src/features/admin/admin.css (danger-banner already defined)
    - /home/arnav/blade/src/features/admin/admin-rich-a.css (just-created Task 1)
    - /home/arnav/blade/src/features/identity/PersonaView.tsx (Phase 6 tabbed-surface reference)
  </read_first>
  <action>
    **DecisionLog.tsx** — per D-182:
    - Tabs (persisted via prefs.admin.activeTab prefix 'dlog:'): "Decisions" / "Authority" / "Audit".
    - **Decisions tab:** on mount `getDecisionLog({limit: 100})` → reverse-chrono list; each row shows decision_type + confidence bar + outcome chip (act/ask/queue/ignore) + timestamp; "Feedback" button per row opens Dialog (was_correct radio + optional note) → `decisionFeedback({decisionId, wasCorrect, note})` + toast. Optional live subscription if DECISION_GATE_EVENT exists (Plan 07-01 audit).
    - Evaluate panel (always visible at top of Decisions tab): signal input textarea → "Evaluate" → `decisionEvaluate({signal})` → result card.
    - **Authority tab:** on mount `Promise.all([authorityGetAuditLog(), authorityGetDelegations()])` → two lists: audit entries + active delegations.
    - **Audit tab:** on mount `auditGetLog({limit: 100})` → global audit entry list (category + message + timestamp).
    - data-testid: `decision-log-root`, `decision-log-tab`, `decision-row`, `decision-feedback-button`, `decision-evaluate-input`, `authority-audit-row`, `audit-row`.

    **SecurityDashboard.tsx** — per D-183:
    - On mount: `securityOverview()` → hero card with traffic-light status (`data-status="complete"` if ok, `"running"` if warn, `"failed"` if critical via status tokens); active alert count chip; last scan timestamp.
    - Tabs row (persisted via prefs.admin.activeTab prefix 'sec:'): "Alerts" / "Scans" / "Policies" / "Pentest". Audit merged into Scans tab per Task 1 decision (cleaner IA).
    - Render appropriate sub-tab component. Tabs use `.admin-tab-pill` with `data-active` per Pattern §10.
    - data-testid: `security-dashboard-root`, `security-hero`, `security-tab`.

    **SecurityAlertsTab.tsx** — per D-183:
    - Derives alerts from `securityOverview()` (already mounted by parent; receive via props or re-fetch). Each alert card with severity (info/warn/critical via status tokens).
    - Input form: "Check URL" input → `securityCheckUrl({url})` → result chip. "Check breach" email input → `securityCheckBreach({email})`. "Check password hash" — prefill `securityCheckPasswordHash({hash})` (warn in UI: never enter real password).
    - data-testid: `security-alerts-root`, `security-alert-card`, `security-check-url-input`.

    **SecurityScansTab.tsx** — per D-183:
    - Scans section: "Run network scan" → `securityScanNetwork()` → findings table; "Scan sensitive files" → `securityScanSensitiveFiles()` → findings table (with severity-colored rows).
    - Audit section (merged): "Run full audit" → `securityRunAudit()` → result card; "Deps audit" → `securityAuditDeps()` → findings list; "Code scan" — path input + "Scan" → `securityScanCode({path})` → findings.
    - data-testid: `security-scans-root`, `security-scan-button`, `security-findings-table`.

    **SecurityPoliciesTab.tsx** — per D-183:
    - On mount: `symbolicListPolicies()` → policies list as `.admin-card`s.
    - "Add policy" Dialog → name + rule inputs → `symbolicAddPolicy({name, rule})`.
    - Per-policy "Check" inline input (context JSON) → `symbolicCheckPolicy({id, context})` → result chip.
    - "Verify plan" panel: plan JSON textarea + "Verify" → `symbolicVerifyPlan({plan})` → violations list.
    - data-testid: `security-policies-root`, `security-policy-card`, `security-policy-check`, `security-verify-plan`.

    **SecurityPentestTab.tsx** — per D-183 (DANGER ZONE):
    - Top: `<div className="danger-banner" data-testid="security-pentest-warning">AUTHORIZING PENTEST FOR A TARGET YOU DO NOT OWN IS ILLEGAL. CONFIRMED AUTHORIZATIONS ARE LOGGED AND ATTRIBUTABLE.</div>`
    - On mount: `pentestListAuth()` → current authorizations list with target + expiry.
    - "Authorize" button opens AuthorizeDialog (from Pattern §4 recipe): target input + rationale textarea (both required) + `variant="danger"` Authorize button → `pentestAuthorize({target, rationale})`.
    - Per-auth "Revoke" → `pentestRevoke({authId})` Dialog-confirm.
    - "Check auth" input (target) → `pentestCheckAuth({target})` → boolean chip.
    - "Check model safety" input (model name) → `pentestCheckModelSafety({model})` → safety report card.
    - data-testid: `security-pentest-root`, `security-pentest-warning`, `pentest-authorize-button`, `pentest-authorization-row`, `pentest-check-auth-input`.

    **CRITICAL:**
    - SC-3 "DecisionLog reads decision-gate history from `decision_gate_*` commands" — DecisionLog.tsx directly falsifies via `getDecisionLog`.
    - SC-4 "SecurityDashboard surfaces active alerts from `security_monitor.rs`" — SecurityDashboard.tsx hero + Alerts tab directly falsifies.
    - ALL pentest actions MUST use Dialog-confirm with `variant="danger"` button AND ALL-CAPS warning banner per D-183.
    - Tab components are split across files (one per tab) for maintainability and so Plan 07-07 spec can import the right isolation harness.
  </action>
  <verify>
    <automated>test -f src/features/admin/DecisionLog.tsx && test -f src/features/admin/SecurityDashboard.tsx && test -f src/features/admin/SecurityAlertsTab.tsx && test -f src/features/admin/SecurityScansTab.tsx && test -f src/features/admin/SecurityPoliciesTab.tsx && test -f src/features/admin/SecurityPentestTab.tsx && grep -q "getDecisionLog\|decisionFeedback\|decisionEvaluate" src/features/admin/DecisionLog.tsx && grep -q "securityOverview" src/features/admin/SecurityDashboard.tsx && grep -q "pentestAuthorize\|pentestListAuth" src/features/admin/SecurityPentestTab.tsx && grep -q "danger-banner\|security-pentest-warning" src/features/admin/SecurityPentestTab.tsx && npx tsc --noEmit && npm run verify:all</automated>
  </verify>
  <done>
    DecisionLog + SecurityDashboard + 4 security sub-tabs shipped; SC-3 + SC-4 falsified; pentest tab gated with ALL-CAPS warning + Dialog confirm; `npx tsc --noEmit` + `npm run verify:all` pass.
  </done>
</task>

</tasks>

<verification>
- 5 real route components + 4 security sub-tabs + 1 CSS partial shipped.
- Every route invokes at least one admin wrapper (verified via grep per-file).
- `npx tsc --noEmit` passes.
- `npm run verify:all` passes — no regressions.
- ESLint passes (no-raw-tauri rule enforced).
- `files_modified` list is disjoint from Plans 07-03 / 07-04 / 07-06.
</verification>

<success_criteria>
- SC-3 "DecisionLog reads decision-gate history from `decision_gate_*` commands" — DecisionLog.tsx directly falsifies via `getDecisionLog`.
- SC-4 "SecurityDashboard surfaces active alerts from `security_monitor.rs`" — SecurityDashboard.tsx hero + Alerts tab directly falsifies via `securityOverview`.
- ADMIN-01..05 + P-03 synthetic mapped to real UI surfaces.
- No route renders a 404 fallback (D-168 policy).
- Pentest tab gated with danger banner + Dialog confirm (D-183).
- Zero Rust file edits.
</success_criteria>

<threat_model>
## Trust Boundaries

- **User input → pentest authorize boundary**: SecurityPentestTab authorization inputs flow directly into `pentestAuthorize`. Rust-side logs + enforces scope. Frontend's only job is user consent gating.
- **User input → decision feedback boundary**: DecisionLog feedback note is stored in Rust for learning. Privacy: user's own note → user's own DB.
- **User input → reports webhook boundary**: Reports webhook URL flows into `setReportWebhook` — if malicious, Rust is responsible for outbound-request sandboxing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Elevation of Privilege | SecurityPentestTab authorizes arbitrary pentest targets | mitigate | ALL-CAPS warning banner + required rationale + Dialog-confirm (D-183). Rust-side `pentest_authorize` audits all requests. |
| T-07-05-02 | Tampering | Analytics prune deletes historical data | mitigate | Dialog-confirm with "older than N days" required input. |
| T-07-05-03 | Information Disclosure | SecurityAlertsTab password hash input field | mitigate | UI label warns "never enter real password"; field is for hash input only. |
| T-07-05-04 | Denial of Service | securityRunAudit runs expensive scan | accept | User-initiated via button; frontend disables button until promise resolves. |
| T-07-05-05 | Tampering | CapabilityReports `forgeDeleteTool` removes registered tool | mitigate | Dialog-confirm required. |
| T-07-05-06 | Information Disclosure | Reports webhook URL could exfiltrate | mitigate | URL validated client-side; Rust outbound gating assumed; user's own webhook — authorized by user click. |
</threat_model>

<output>
After completion, create `.planning/phases/07-dev-tools-admin/07-05-SUMMARY.md` with:
- List of 5 real routes + 4 security sub-tabs + 1 CSS partial shipped.
- Command wiring table (which wrapper each route uses).
- SC-3 + SC-4 falsification summary (exact code path).
- Any Rust signature mismatches encountered vs. Plan 07-02 types.
- Confirmation data-testid coverage is complete for Plan 07-07 spec.
- Prefs integration verified (admin.activeTab for DecisionLog + SecurityDashboard tabs).
- Any divergences from D-182..D-183 with rationale.
</output>
