---
phase: 07-dev-tools-admin
plan: 04
type: execute
wave: 2
depends_on: ['07-01', '07-02']
files_modified:
  - src/features/dev-tools/WebAutomation.tsx
  - src/features/dev-tools/EmailAssistant.tsx
  - src/features/dev-tools/DocumentGenerator.tsx
  - src/features/dev-tools/CodeSandbox.tsx
  - src/features/dev-tools/ComputerUse.tsx
  - src/features/dev-tools/AutomationTab.tsx
  - src/features/dev-tools/UiAutomationTab.tsx
  - src/features/dev-tools/dev-tools-rich-b.css
autonomous: true
requirements:
  - DEV-06  # WebAutomation — browser_agent_loop + page describe/action + session status
  - DEV-07  # EmailAssistant — auto_reply_draft + reminder follow-up
  - DEV-08  # DocumentGenerator — doc_list + ingest + generate study notes + cross-synthesis + Q&A
  - DEV-09  # CodeSandbox — sandbox_run + run_explain + fix_and_run + detect_language
  - DEV-10  # ComputerUse — computer_use_task + screenshot + automation::* + uia::*
must_haves:
  truths:
    - "WebAutomation renders browser session status chip (browserSessionStatus) + 'Connect' button (connectToUserBrowser); main goal textarea + 'Run' invokes browserAgentLoop({goal, maxSteps}); live trace panel renders returned steps OR subscribes via useTauriEvent(BROWSER_AGENT_EVENT) if Plan 07-01 added the constant (D-177)"
    - "WebAutomation tool panel (pill tabs): 'Click' / 'Describe page' / 'Navigate' — each invokes browserAction or browserDescribePage or webAction with inline input + inline result; latest page screenshot rendered as base64 img if returned (D-177)"
    - "EmailAssistant top input: recipient + incoming email textarea + intent radio (reply/followup/introduce); 'Draft' invokes autoReplyDraft({recipient, message, intent}); right panel 'Learn from edit' — after user edits the draft, 'Save learning' invokes autoReplyLearnFromEdit({original, edited}) (D-178)"
    - "EmailAssistant 'Batch' tab: csv-paste input → autoReplyDraftBatch({items}); 'Schedule follow-up reminder' button invokes reminderAddNatural({text}) with parsed-time preview via reminderParseTime (D-178)"
    - "DocumentGenerator top: doc_list rendered as sidebar; 'Ingest' button opens Dialog with Tauri file-picker → docIngest({path}); main = 3 pill tabs 'Study notes' / 'Cross-synthesis' / 'Q&A' persisted via prefs.devTools.activeTab (D-179)"
    - "DocumentGenerator 'Study notes' tab: pick single doc → docGenerateStudyNotes({docId}) → rendered in glass card; 'Cross-synthesis' tab: checkbox multi-select ≥2 docs → docCrossSynthesis({docIds}); 'Q&A' tab: pick doc + question input → docAnswerQuestion({docId, question}) → answer card (D-179)"
    - "DocumentGenerator sidebar 'Search docs' input → docSearch({query}) — dedicated to DocumentGenerator, DOES NOT overlap with Phase 5 KnowledgeBase consumer (D-179)"
    - "CodeSandbox renders language select (auto-detect via sandboxDetectLanguage({code}) on code change) + textarea (mono font) + 3 action buttons 'Run' (sandboxRun) / 'Run + explain' (sandboxRunExplain) / 'Fix + run' (sandboxFixAndRun); output panels colored by status token; exit code chip; history sidebar = last 10 client-side runs (D-180)"
    - "ComputerUse top: active-task card (if computer_use_task running, shows live status + 'Stop' invokes computerUseStop); screenshot pane (computerUseScreenshot on mount + 'Refresh'); main 'Start task' textarea + 'Run' invokes computerUseTask({goal}) (D-181)"
    - "ComputerUse tabs 'Automation' / 'UI Automation' persisted via prefs.devTools.activeTab — AutomationTab wraps the 15 automation::* commands as inline mini-forms + Dialog-confirm for destructive ones (launch_app, auto_open_path, auto_open_url, mouse_click); UiAutomationTab wraps the 7 uia_* commands with snapshot-tree + per-element actions (D-181)"
    - "All 5 routes render without 404 fallback (SC-1); WebAutomation shows goal → live trace (SC-2)"
    - "data-testid attributes for Plan 07-07 spec: web-automation-root, web-automation-goal-input, web-automation-run-button, web-automation-trace, email-assistant-root, email-assistant-draft-button, document-generator-root, doc-list-sidebar, doc-tab, code-sandbox-root, code-sandbox-run-button, code-sandbox-stdout, computer-use-root, computer-use-tab, computer-use-action-danger"
    - "No raw invoke / listen (ESLint no-raw-tauri passes); no ChatProvider re-hoist (D-134); no files_modified overlap with Plans 07-03 / 07-05 / 07-06"
    - "npx tsc --noEmit passes; npm run verify:all passes"
  artifacts:
    - path: "src/features/dev-tools/WebAutomation.tsx"
      provides: "DEV-06 — browser agent + tool panel + screenshot"
      exports: ["WebAutomation"]
    - path: "src/features/dev-tools/EmailAssistant.tsx"
      provides: "DEV-07 — draft/learn/batch + reminder follow-up"
      exports: ["EmailAssistant"]
    - path: "src/features/dev-tools/DocumentGenerator.tsx"
      provides: "DEV-08 — ingest + study notes + cross-synthesis + Q&A"
      exports: ["DocumentGenerator"]
    - path: "src/features/dev-tools/CodeSandbox.tsx"
      provides: "DEV-09 — run/explain/fix + language detect + history"
      exports: ["CodeSandbox"]
    - path: "src/features/dev-tools/ComputerUse.tsx"
      provides: "DEV-10 — task + screenshot + 2-tab automation surface"
      exports: ["ComputerUse"]
    - path: "src/features/dev-tools/AutomationTab.tsx"
      provides: "ComputerUse Automation sub-tab (15 automation::* commands)"
      exports: ["AutomationTab"]
    - path: "src/features/dev-tools/UiAutomationTab.tsx"
      provides: "ComputerUse UI Automation sub-tab (7 uia_* commands)"
      exports: ["UiAutomationTab"]
    - path: "src/features/dev-tools/dev-tools-rich-b.css"
      provides: "Plan 07-04 scoped CSS — trace panel + doc sidebar + computer-use screenshot"
  key_links:
    - from: "src/features/dev-tools/WebAutomation.tsx"
      to: "src/lib/tauri/dev_tools.ts (browserAgentLoop, browserAction, browserDescribePage, browserSessionStatus, connectToUserBrowser, webAction)"
      via: "invokeTyped via wrappers"
      pattern: "browserAgentLoop|browserAction|browserDescribePage|browserSessionStatus"
    - from: "src/features/dev-tools/EmailAssistant.tsx"
      to: "src/lib/tauri/dev_tools.ts (autoReplyDraft, autoReplyLearnFromEdit, autoReplyDraftBatch, reminderAddNatural, reminderParseTime)"
      via: "invokeTyped via wrappers"
      pattern: "autoReply|reminderAddNatural|reminderParseTime"
    - from: "src/features/dev-tools/DocumentGenerator.tsx"
      to: "src/lib/tauri/dev_tools.ts (docList, docIngest, docSearch, docGet, docGenerateStudyNotes, docCrossSynthesis, docAnswerQuestion, docDelete)"
      via: "invokeTyped"
      pattern: "docList|docIngest|docSearch|docGenerate|docCrossSynthesis|docAnswerQuestion"
    - from: "src/features/dev-tools/CodeSandbox.tsx"
      to: "src/lib/tauri/dev_tools.ts (sandboxRun, sandboxRunExplain, sandboxFixAndRun, sandboxDetectLanguage)"
      via: "invokeTyped"
      pattern: "sandboxRun|sandboxRunExplain|sandboxFixAndRun|sandboxDetectLanguage"
    - from: "src/features/dev-tools/ComputerUse.tsx"
      to: "src/lib/tauri/dev_tools.ts (computerUseTask, computerUseStop, computerUseScreenshot + automation::* + uia_*)"
      via: "invokeTyped"
      pattern: "computerUse|auto(TypeText|PressKey|KeyCombo|Mouse|Scroll|OpenUrl|OpenPath|LaunchApp|CopyToClipboard|PasteClipboard)|uia(GetActive|Describe|Click|Invoke|Focus|SetElement|WaitForElement)"
---

<objective>
Ship Dev Tools cluster subset B — 5 more surfaces: WebAutomation (SC-2 browser agent), EmailAssistant (DEV-07), DocumentGenerator (DEV-08), CodeSandbox (DEV-09), ComputerUse (DEV-10). Each route becomes a real glass-native surface backed by Plan 07-02 wrappers + Plan 07-01 Prefs.

Purpose: ROADMAP Phase 7 SC-2 — "WebAutomation accepts a goal, calls `browser_agent_*` commands, and displays live screen feedback" — directly falsified by this plan's WebAutomation. Plan 07-04 closes DEV-06..10. Parallel with Plan 07-03 (no files_modified overlap).

Output: 5 replaced per-route files + 2 sub-components (AutomationTab, UiAutomationTab for ComputerUse's 25-command surface) + 1 scoped CSS partial. ~1100-1400 net new lines. Plan completes under 45% context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-dev-tools-admin/07-CONTEXT.md
@.planning/phases/07-dev-tools-admin/07-PATTERNS.md
@.planning/phases/07-dev-tools-admin/07-02-SUMMARY.md
@.planning/phases/06-life-os-identity/06-04-PLAN.md
@.planning/RECOVERY_LOG.md
@CLAUDE.md
@src/lib/tauri/dev_tools.ts
@src/lib/events/index.ts
@src/hooks/usePrefs.ts
@src/design-system/primitives/index.ts
@src/features/dev-tools/index.tsx
@src/features/dev-tools/types.ts
@src/features/dev-tools/dev-tools.css
@src/lib/context/ToastContext.tsx

<interfaces>
```ts
// Surfaces consumed from Plan 07-02:
import {
  browserAction, browserAgentLoop, browserDescribePage, browserSessionStatus, connectToUserBrowser, webAction,
  autoReplyDraft, autoReplyLearnFromEdit, autoReplyDraftBatch,
  reminderAdd, reminderAddNatural, reminderList, reminderDelete, reminderParseTime,
  docIngest, docSearch, docGet, docList, docDelete, docAnswerQuestion, docCrossSynthesis, docGenerateStudyNotes,
  sandboxRun, sandboxRunExplain, sandboxFixAndRun, sandboxDetectLanguage,
  computerUseTask, computerUseStop, computerUseScreenshot,
  autoTypeText, autoPressKey, autoKeyCombo, autoMouseMove, autoGetMousePosition,
  autoMouseClick, autoMouseClickRelative, autoMouseDoubleClick, autoMouseDrag, autoScroll,
  autoOpenUrl, autoOpenPath, autoLaunchApp, autoCopyToClipboard, autoPasteClipboard,
  uiaGetActiveWindowSnapshot, uiaDescribeActiveWindow, uiaClickElement, uiaInvokeElement,
  uiaFocusElement, uiaSetElementValue, uiaWaitForElement,
} from '@/lib/tauri/dev_tools';

// Plan 07-01 Prefs:
//   prefs['devTools.activeTab']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship WebAutomation + EmailAssistant + DocumentGenerator (DEV-06..08)</name>
  <files>src/features/dev-tools/WebAutomation.tsx, src/features/dev-tools/EmailAssistant.tsx, src/features/dev-tools/DocumentGenerator.tsx, src/features/dev-tools/dev-tools-rich-b.css</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §4, §7, §9
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-177, §D-178, §D-179
    - /home/arnav/blade/src/lib/tauri/dev_tools.ts (Plan 07-02 output)
    - /home/arnav/blade/src/lib/events/index.ts (Plan 07-01 — check if BROWSER_AGENT_EVENT exists)
    - /home/arnav/blade/src/features/agents/useAgentTimeline.ts (rAF-flush reference for potential live-trace upgrade)
    - /home/arnav/blade/src.bak/components/{WebAutomation,EmailAssistant,DocumentIntelligence}.tsx (READ-ONLY layout reference)
  </read_first>
  <action>
    **WebAutomation.tsx** — per D-177:
    - On mount: `browserSessionStatus()` → status chip (`connected: boolean`). If not connected, show "Connect" button → `connectToUserBrowser()` + refetch status.
    - Main: goal textarea + "Max steps" input (default 10) + "Run" button → `browserAgentLoop({goal, maxSteps})` → either:
      - **Sync render:** loop returns full trace; render `BrowserAgentStep[]` as timeline cards.
      - **Live stream (conditional):** if `BROWSER_AGENT_EVENT` constant exists (Plan 07-01 added after audit), subscribe via `useTauriEvent<BrowserAgentEventPayload>(BLADE_EVENTS.BROWSER_AGENT_EVENT, handler)` — append steps in real time. Use rAF-flush pattern from `useAgentTimeline.ts` if events fire >5/sec.
    - Tool panel (pill tabs, persisted via `prefs.devTools.activeTab` prefix `web:`): "Click" / "Describe page" / "Navigate".
      - Click: selector input + "Click" button → `browserAction({action: 'click', selector})`.
      - Describe page: "Describe" button → `browserDescribePage()` → result in card.
      - Navigate: URL input + "Go" button → `webAction({action: 'navigate', url})`.
    - Screenshot pane: if last action returned `screenshot_b64`, render as `<img src={`data:image/png;base64,${b64}`} />`.
    - data-testid: `web-automation-root`, `web-automation-goal-input`, `web-automation-run-button`, `web-automation-trace`, `web-automation-step-row`, `web-automation-screenshot`.

    **EmailAssistant.tsx** — per D-178:
    - State: `recipient`, `message`, `intent: 'reply'|'followup'|'introduce'`, `draft`, `editedDraft`, `busy`.
    - Top input: recipient name input + incoming email textarea + intent radio pills.
    - "Draft" button → `autoReplyDraft({recipient, message, intent})` → sets `draft`; user can edit in `editedDraft` textarea.
    - Right panel: "Save learning" button (enabled when `editedDraft !== draft`) → `autoReplyLearnFromEdit({original: draft, edited: editedDraft})` + toast.
    - Tabs (persisted via prefs): "Single" / "Batch" / "Follow-up".
      - Single: above.
      - Batch: csv-paste input → parse client-side to `items: Array<{recipient, message, intent}>` → `autoReplyDraftBatch({items})` → results list.
      - Follow-up: natural-language input → on debounce call `reminderParseTime({text})` for a preview chip → "Schedule" button → `reminderAddNatural({text})` + toast.
    - data-testid: `email-assistant-root`, `email-assistant-draft-button`, `email-assistant-draft-output`, `email-assistant-tab`, `email-assistant-batch-items`.

    **DocumentGenerator.tsx** — per D-179:
    - Layout: left sidebar (doc list from `docList()`) + main pane (generation mode tabs).
    - Sidebar top: search input → `docSearch({query})`; "Ingest" button opens Dialog → Tauri `@tauri-apps/plugin-dialog` `open({filters: [{name: 'Docs', extensions: ['pdf','md','txt','docx']}]})` → `docIngest({path})` → refresh list.
    - Sidebar rows: each doc card = title + "Delete" button Dialog-confirm → `docDelete({id})`.
    - Main tabs (persisted via prefs.devTools.activeTab prefix `doc:`): "Study notes" / "Cross-synthesis" / "Q&A".
      - Study notes: select single doc from sidebar → "Generate" → `docGenerateStudyNotes({docId})` → rendered in glass card.
      - Cross-synthesis: multi-select (checkboxes in sidebar) ≥2 docs → "Synthesize" → `docCrossSynthesis({docIds})` → rendered.
      - Q&A: select doc + question textarea → "Ask" → `docAnswerQuestion({docId, question})` → answer card.
    - data-testid: `document-generator-root`, `doc-list-sidebar`, `doc-sidebar-row`, `doc-tab`, `doc-generate-button`, `doc-synthesis-output`.

    **dev-tools-rich-b.css** — scoped partial:
    ```css
    /* src/features/dev-tools/dev-tools-rich-b.css — Plan 07-04 scoped partial */

    @layer features {
      .web-automation-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--sp-2);
      }

      .web-automation-trace {
        display: flex;
        flex-direction: column;
        gap: var(--sp-1);
        max-height: 400px;
        overflow-y: auto;
      }

      .web-automation-step-row {
        padding: var(--sp-2);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        font-size: 12px;
      }

      .web-automation-screenshot img {
        width: 100%;
        border-radius: var(--radius-card);
        border: 1px solid var(--line);
      }

      .email-assistant-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--sp-2);
      }

      .doc-generator-layout {
        display: grid;
        grid-template-columns: 280px 1fr;
        gap: var(--sp-2);
        height: calc(100vh - 180px);
      }

      .doc-sidebar-row {
        padding: var(--sp-2);
        border-bottom: 1px solid var(--line);
        cursor: pointer;
      }

      .doc-sidebar-row[data-selected="true"] {
        background: rgba(255, 255, 255, 0.06);
      }

      .computer-use-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--sp-2);
      }

      .computer-use-screenshot img {
        width: 100%;
        border-radius: var(--radius-card);
        border: 1px solid var(--line);
      }

      .code-sandbox-output {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--sp-2);
        margin-top: var(--sp-2);
      }

      .code-sandbox-output pre {
        font-family: var(--font-mono);
        font-size: 12px;
        background: rgba(0, 0, 0, 0.3);
        padding: var(--sp-2);
        border-radius: var(--radius-card);
        overflow: auto;
        max-height: 300px;
        white-space: pre-wrap;
      }
    }
    ```

    **CRITICAL:**
    - WebAutomation live-trace subscription is conditional on Plan 07-01 adding BROWSER_AGENT_EVENT; if absent, synchronous rendering is the default + adequate per D-177 trade-off.
    - DocumentGenerator's `docSearch` is SEPARATE from Phase 5 KnowledgeBase's search (D-179 dual-home); they share backend but surface different intents.
    - All Dialog-confirms for destructive ops (Delete doc, Schedule reminder overwrite).
  </action>
  <verify>
    <automated>test -f src/features/dev-tools/WebAutomation.tsx && test -f src/features/dev-tools/EmailAssistant.tsx && test -f src/features/dev-tools/DocumentGenerator.tsx && test -f src/features/dev-tools/dev-tools-rich-b.css && grep -q "browserAgentLoop" src/features/dev-tools/WebAutomation.tsx && grep -q "autoReplyDraft" src/features/dev-tools/EmailAssistant.tsx && grep -q "docList\|docIngest\|docGenerateStudyNotes" src/features/dev-tools/DocumentGenerator.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>
    3 real route components + 1 CSS partial shipped; each invokes at least one dev_tools wrapper; `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship CodeSandbox + ComputerUse (+ AutomationTab + UiAutomationTab sub-components) (DEV-09..10)</name>
  <files>src/features/dev-tools/CodeSandbox.tsx, src/features/dev-tools/ComputerUse.tsx, src/features/dev-tools/AutomationTab.tsx, src/features/dev-tools/UiAutomationTab.tsx</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §4
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-180, §D-181
    - /home/arnav/blade/src/lib/tauri/dev_tools.ts (Plan 07-02 output)
    - /home/arnav/blade/src/features/dev-tools/dev-tools-rich-b.css (just-created Task 1)
    - /home/arnav/blade/src.bak/components/{CodeSandbox,ComputerUseView}.tsx (READ-ONLY layout reference)
  </read_first>
  <action>
    **CodeSandbox.tsx** — per D-180:
    - State: `code: string`, `language: string` (auto-detected via debounced `sandboxDetectLanguage({code})` or manual pill-select), `result: SandboxResult | null`, `busy: boolean`, `history: SandboxResult[]` (client-side ring buffer, last 10, not persisted).
    - Language pill-select: common languages (python/javascript/typescript/bash/rust/go). "Auto-detect" pill calls `sandboxDetectLanguage({code})` and sets `language` to result.
    - Code textarea: mono font, 20 rows.
    - 3 action buttons: "Run" → `sandboxRun({language, code})`; "Run + explain" → `sandboxRunExplain({language, code})`; "Fix + run" → `sandboxFixAndRun({language, code, error: result?.stderr})` (only enabled if last run had stderr).
    - On each run, push result into `history` (keep last 10), set as `result`.
    - Output: two `<pre>` panels (stdout + stderr) with status token accent (`data-status="complete"` if exit_code===0 else `"failed"`); exit-code chip.
    - History sidebar: click entry → restore its code + language + result.
    - data-testid: `code-sandbox-root`, `code-sandbox-code`, `code-sandbox-run-button`, `code-sandbox-stdout`, `code-sandbox-stderr`, `code-sandbox-exit-code`, `code-sandbox-history`.

    **ComputerUse.tsx** — per D-181 (main route + 2 tabs):
    - State: `activeTask: ComputerUseTask | null`, `screenshot_b64: string | null`, `goal: string`, `tab: 'automation' | 'ui-automation'` (persisted via prefs.devTools.activeTab prefix `cu:`).
    - Top active-task card: if `activeTask` running, show status + "Stop" button → `computerUseStop()`.
    - Screenshot pane: on mount `computerUseScreenshot()` → set `screenshot_b64`. "Refresh" button re-invokes.
    - Main action: goal textarea + "Start task" → `computerUseTask({goal})` → set `activeTask`.
    - Tabs row:
      - "Automation" tab → `<AutomationTab />`
      - "UI Automation" tab → `<UiAutomationTab />`
    - data-testid: `computer-use-root`, `computer-use-task-card`, `computer-use-stop-button`, `computer-use-screenshot`, `computer-use-tab`, `computer-use-goal-input`.

    **AutomationTab.tsx** — 15 automation::* commands (sub-tab):
    - Sections grouped by concern: "Keyboard" (auto_type_text, auto_press_key, auto_key_combo), "Mouse" (auto_mouse_move, auto_get_mouse_position, auto_mouse_click, auto_mouse_click_relative, auto_mouse_double_click, auto_mouse_drag, auto_scroll), "Apps & Paths" (auto_open_url, auto_open_path, auto_launch_app), "Clipboard" (auto_copy_to_clipboard, auto_paste_clipboard).
    - Each command = inline mini-form + button.
    - Destructive/OS-action commands (auto_launch_app, auto_open_path, auto_open_url, auto_mouse_click) require Dialog-confirm with Pattern §4; benign (auto_get_mouse_position, auto_copy_to_clipboard) plain button.
    - data-testid: `automation-tab-root`, `automation-section`, `automation-action-danger` (only on Dialog-gated actions).

    **UiAutomationTab.tsx** — 7 uia_* commands (sub-tab):
    - Top action: "Get active window snapshot" → `uiaGetActiveWindowSnapshot()` → render `UiaSnapshot` as expandable JSON tree.
    - Per-element actions: given an element id (from snapshot), invoke `uiaClickElement`, `uiaInvokeElement`, `uiaFocusElement`, `uiaSetElementValue`, `uiaWaitForElement` via inline forms.
    - "Describe active window" button → `uiaDescribeActiveWindow()` → text card.
    - data-testid: `ui-automation-tab-root`, `uia-snapshot`, `uia-element-action`.

    **CRITICAL:**
    - D-181: Destructive automation ops Dialog-confirmed.
    - Every run of `runShell`-style or `computerUseTask` is logged by Rust in execution_memory (accepted risk — frontend doesn't log).
    - 25 commands across AutomationTab + UiAutomationTab is the MAJORITY of ComputerUse's surface — splitting into sub-components keeps parent file readable.
  </action>
  <verify>
    <automated>test -f src/features/dev-tools/CodeSandbox.tsx && test -f src/features/dev-tools/ComputerUse.tsx && test -f src/features/dev-tools/AutomationTab.tsx && test -f src/features/dev-tools/UiAutomationTab.tsx && grep -q "sandboxRun\|sandboxRunExplain\|sandboxFixAndRun" src/features/dev-tools/CodeSandbox.tsx && grep -q "computerUseTask\|computerUseScreenshot" src/features/dev-tools/ComputerUse.tsx && grep -q "autoTypeText\|autoMouseClick\|autoLaunchApp" src/features/dev-tools/AutomationTab.tsx && grep -q "uiaGetActiveWindowSnapshot\|uiaClickElement" src/features/dev-tools/UiAutomationTab.tsx && npx tsc --noEmit && npm run verify:all</automated>
  </verify>
  <done>
    CodeSandbox + ComputerUse (+ 2 sub-tabs) shipped; ComputerUse covers all 25 automation + uia commands; Dialog-confirm applied to destructive ops; `npx tsc --noEmit` + `npm run verify:all` pass.
  </done>
</task>

</tasks>

<verification>
- 5 real route components + 2 sub-components + 1 CSS partial shipped.
- Every route invokes at least one dev_tools wrapper (verified via grep per-file).
- `npx tsc --noEmit` passes.
- `npm run verify:all` passes — no regressions.
- ESLint passes (no-raw-tauri rule enforced).
- `files_modified` list is disjoint from Plans 07-03 / 07-05 / 07-06.
</verification>

<success_criteria>
- SC-2 "WebAutomation accepts a goal, calls `browser_agent_*` commands, and displays live screen feedback" — WebAutomation.tsx directly falsifies via `browserAgentLoop` + trace rendering.
- DEV-06..10 mapped to real UI surfaces.
- No route renders a 404 fallback (D-168 policy).
- ComputerUse exposes all 25 automation + uia commands with Dialog-confirm on destructive ops (D-181).
- Zero Rust file edits.
</success_criteria>

<threat_model>
## Trust Boundaries

- **User input → browser_agent_loop boundary**: WebAutomation goal input flows into `browser_agent_loop` — agent may navigate to arbitrary URLs. Rust-side sandbox/auth is assumed.
- **User input → automation::* boundary**: ComputerUse AutomationTab actions invoke OS-level automation (launch apps, mouse clicks, URL open). User-clicked Dialog confirm IS authorization.
- **User input → computer_use_task boundary**: ComputerUse goal → full autonomous computer use loop. Same authorization model.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation of Privilege | AutomationTab `auto_launch_app` / `auto_open_url` / `auto_open_path` launches arbitrary apps/paths | mitigate | Dialog confirm required per D-181. User click IS authorization (local-first single-user). |
| T-07-04-02 | Elevation of Privilege | ComputerUse `computer_use_task` acts autonomously across apps | mitigate | Visible screenshot + live status card + "Stop" button gives user direct observability + cancel. |
| T-07-04-03 | Information Disclosure | Screenshot pane may capture sensitive content from active window | accept | Single-user local-first; user controls when to refresh. No screenshot exfiltrated. |
| T-07-04-04 | Tampering | DocumentGenerator ingest of untrusted file crashes parser | accept | Rust-side `doc_ingest` is responsible for safe parsing; frontend passes absolute path only. |
| T-07-04-05 | Denial of Service | CodeSandbox long-running code blocks | accept | `sandbox_run` is sandboxed in Rust; frontend disables "Run" until promise resolves. |
</threat_model>

<output>
After completion, create `.planning/phases/07-dev-tools-admin/07-04-SUMMARY.md` with:
- List of 5 real routes + 2 sub-components + 1 CSS partial shipped.
- Command wiring table (which wrapper each route uses).
- WebAutomation live-trace decision — subscribed via useTauriEvent or synchronous render (based on Plan 07-01 audit outcome).
- Any Rust signature mismatches encountered vs. the Plan 07-02 types.
- Confirmation data-testid coverage is complete for Plan 07-07 spec.
- Prefs integration verified (activeTab).
- Any divergences from D-177..D-181 with rationale.
</output>
