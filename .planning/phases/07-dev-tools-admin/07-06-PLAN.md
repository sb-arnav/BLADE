---
phase: 07-dev-tools-admin
plan: 06
type: execute
wave: 2
depends_on: ['07-01', '07-02']
files_modified:
  - src/features/admin/Temporal.tsx
  - src/features/admin/Diagnostics.tsx
  - src/features/admin/IntegrationStatus.tsx
  - src/features/admin/McpSettings.tsx
  - src/features/admin/ModelComparison.tsx
  - src/features/admin/KeyVault.tsx
  - src/features/admin/DiagnosticsSysadminTab.tsx
  - src/features/admin/admin-rich-b.css
autonomous: true
requirements:
  - ADMIN-06 # Temporal — daily standup + patterns + meeting prep + execution memory
  - ADMIN-07 # Diagnostics — supervisor health + traces + deep scan + sysadmin (SC-4 "module health")
  - ADMIN-08 # IntegrationStatus — integration_bridge state + mcp servers
  - ADMIN-09 # McpSettings — mcp CRUD + tool trust
  - ADMIN-10 # ModelComparison — task routing + switch provider + test provider
  # KeyVault labeled synthetic ADMIN-11 orphan per D-185 — ships real, retrospective flags
must_haves:
  truths:
    - "Temporal hero card: temporalDailyStandup() summary + highlights + blockers (D-184)"
    - "Temporal tabs persisted via prefs.admin.activeTab prefix 'temp:': 'What was I doing' (temporalWhatWasIDoing with window_hours input, default 24), 'Patterns' (temporalDetectPatterns as list), 'Meeting prep' (temporalMeetingPrep with meeting_id input — shared read with Phase 6 MeetingsView per D-184), 'Execution memory' (exmemRecent + exmemSearch + exmemRecord debug form)"
    - "Diagnostics hero = supervisorGetHealth() 'services' grid (D-184 SC-4 falsifier) — each service card with name + status chip + uptime_seconds via data-status attribute; responsive grid via .admin-health-grid"
    - "Diagnostics tabs persisted via prefs.admin.activeTab prefix 'diag:': 'Health' (hero), 'Traces' (getRecentTraces), 'Authority' (authorityGetAgents + authorityGetDelegations + 'Delegate' Dialog → authorityDelegate; 'Route and run' Dialog → authorityRouteAndRun; 'Run chain' Dialog → authorityRunChain — all Dialog-confirmed), 'Deep scan' (deepScanResults + 'Run now' → deepScanStart), 'Sysadmin' sub-tab, 'Config' (debugConfig read + setConfig JSON Dialog + updateInitPrefs + 'Reset onboarding' Dialog-confirm → resetOnboarding)"
    - "DiagnosticsSysadminTab ships all 8 sysadmin::* commands (D-184 danger-gated): sysadminDetectHardware + sysadminListCheckpoints + 'Save checkpoint' → sysadminSaveCheckpoint + 'Load checkpoint' Dialog-confirm → sysadminLoadCheckpoint + 'Rollback' Dialog-confirm danger → sysadminRollback + dry-run panels (sysadminDryRunEdit, sysadminDryRunCommand) + 'Sudo exec' ALL-CAPS warning Dialog → sysadminSudoExec"
    - "IntegrationStatus renders integrationGetState() as per-service cards — name + enabled toggle (integrationToggle) + last_poll_at timestamp + error chip; 'Poll now' per service → integrationPollNow({service}) with spinner + auto-refresh; MCP servers sub-section shows mcpGetServers() + mcpServerHealth per server"
    - "McpSettings renders mcpGetServers() list; per-server: connected chip + tool count + 'Discover tools' → mcpDiscoverTools + 'Remove' Dialog-confirm → mcpRemoveServer; 'Add server' Dialog (name/command/args/env) → mcpAddServer; 'Install from catalog' Dialog → mcpInstallCatalogServer; 'Call tool' debug panel → mcpCallTool"
    - "McpSettings 'Tool trust' sub-section: permissionsGetToolOverrides list; per-tool trust pill-select (trusted/ask/blocked) → setToolTrust; 'Reset' → resetToolTrust; classifyMcpTool debug for unknown tools"
    - "ModelComparison renders getTaskRouting() as 4+ task rows (chat/reasoning/agent/vision); per-task: current provider/model chips + 'Change' Dialog (provider + model inputs) → setTaskRouting({task, provider, model}) + saveConfigField('task_routing', …); 'Test provider' button per task → testProvider({provider}) → latency chip or error; 'Switch provider globally' button Dialog → switchProvider(id)"
    - "KeyVault renders getAllProviderKeys() list — provider name + masked key (last-4 visible per D-185 Discretion); 'Store' Dialog with provider + api_key inputs (password field, masked in-UI) → storeProviderKey({provider, apiKey}); honest note about OS keyring backing"
    - "data-testid attributes for Plan 07-07 spec: temporal-root, diagnostics-root, supervisor-health-grid, health-card, integration-status-root, mcp-settings-root, mcp-server-row, mcp-add-server-button, model-comparison-root, task-routing-row, key-vault-root, provider-key-row"
    - "No raw invoke / listen (ESLint no-raw-tauri passes); no ChatProvider re-hoist (D-134); no files_modified overlap with Plans 07-03 / 07-04 / 07-05"
    - "npx tsc --noEmit passes; npm run verify:all passes"
  artifacts:
    - path: "src/features/admin/Temporal.tsx"
      provides: "ADMIN-06 — standup + patterns + meeting prep + exmem"
      exports: ["Temporal"]
    - path: "src/features/admin/Diagnostics.tsx"
      provides: "ADMIN-07 — supervisor health + traces + authority + deep scan + sysadmin + config (SC-4)"
      exports: ["Diagnostics"]
    - path: "src/features/admin/IntegrationStatus.tsx"
      provides: "ADMIN-08 — integration_bridge state + mcp health"
      exports: ["IntegrationStatus"]
    - path: "src/features/admin/McpSettings.tsx"
      provides: "ADMIN-09 — mcp CRUD + tool trust + catalog install"
      exports: ["McpSettings"]
    - path: "src/features/admin/ModelComparison.tsx"
      provides: "ADMIN-10 — task routing + switch provider + test provider"
      exports: ["ModelComparison"]
    - path: "src/features/admin/KeyVault.tsx"
      provides: "synthetic ADMIN-11 (orphan per D-185) — provider key vault with last-4 masking"
      exports: ["KeyVault"]
    - path: "src/features/admin/DiagnosticsSysadminTab.tsx"
      provides: "Diagnostics Sysadmin sub-tab (8 sysadmin::* commands, all Dialog-gated)"
      exports: ["DiagnosticsSysadminTab"]
    - path: "src/features/admin/admin-rich-b.css"
      provides: "Plan 07-06 scoped CSS — task routing grid + mcp server rows + key vault masked style"
  key_links:
    - from: "src/features/admin/Temporal.tsx"
      to: "src/lib/tauri/admin.ts (temporalDailyStandup, temporalWhatWasIDoing, temporalDetectPatterns, temporalMeetingPrep, exmemRecord, exmemSearch, exmemRecent)"
      via: "invokeTyped via wrappers"
      pattern: "temporal|exmem"
    - from: "src/features/admin/Diagnostics.tsx"
      to: "src/lib/tauri/admin.ts (supervisorGetHealth, supervisorGetService, getRecentTraces, authorityGetAgents, authorityGetDelegations, authorityDelegate, authorityRouteAndRun, authorityRunChain, deepScanStart, deepScanResults, deepScanSummary, debugConfig, setConfig, updateInitPrefs, resetOnboarding)"
      via: "invokeTyped via wrappers"
      pattern: "supervisor|getRecentTraces|authority|deepScan|debugConfig|setConfig|updateInitPrefs|resetOnboarding"
    - from: "src/features/admin/DiagnosticsSysadminTab.tsx"
      to: "src/lib/tauri/admin.ts (sysadmin*)"
      via: "invokeTyped"
      pattern: "sysadminDetectHardware|sysadminDryRun|sysadminListCheckpoints|sysadminSaveCheckpoint|sysadminLoadCheckpoint|sysadminRollback|sysadminSudoExec"
    - from: "src/features/admin/IntegrationStatus.tsx"
      to: "src/lib/tauri/admin.ts (integrationGetState, integrationToggle, integrationPollNow, mcpGetServers, mcpServerHealth)"
      via: "invokeTyped"
      pattern: "integration|mcpGetServers|mcpServerHealth"
    - from: "src/features/admin/McpSettings.tsx"
      to: "src/lib/tauri/admin.ts (mcpAddServer, mcpInstallCatalogServer, mcpDiscoverTools, mcpCallTool, mcpGetTools, mcpGetServers, mcpRemoveServer, mcpServerStatus, mcpServerHealth, classifyMcpTool, setToolTrust, resetToolTrust, getToolOverrides)"
      via: "invokeTyped"
      pattern: "mcp[A-Z]|classifyMcpTool|setToolTrust|resetToolTrust|getToolOverrides"
    - from: "src/features/admin/ModelComparison.tsx"
      to: "src/lib/tauri/admin.ts (getTaskRouting, setTaskRouting, switchProvider, testProvider, saveConfigField)"
      via: "invokeTyped"
      pattern: "getTaskRouting|setTaskRouting|switchProvider|testProvider|saveConfigField"
    - from: "src/features/admin/KeyVault.tsx"
      to: "src/lib/tauri/admin.ts (getAllProviderKeys, storeProviderKey)"
      via: "invokeTyped"
      pattern: "getAllProviderKeys|storeProviderKey"
---

<objective>
Ship Admin cluster subset B — the 6 remaining surfaces: Temporal (ADMIN-06), Diagnostics (SC-4 "module health"), IntegrationStatus (ADMIN-08), McpSettings (ADMIN-09), ModelComparison (ADMIN-10), KeyVault (synthetic ADMIN-11 orphan). Each route becomes a real glass-native surface backed by Plan 07-02 wrappers + Plan 07-01 Prefs.

Purpose: ROADMAP Phase 7 SC-4 "Diagnostics view shows module health for all running background tasks" — directly falsified by this plan's Diagnostics hero via `supervisor_get_health`. Plan closes ADMIN-06..10 + KeyVault synthetic-orphan for the remaining 6 Admin routes. Parallel with Plans 07-03/07-04/07-05.

Output: 6 replaced per-route files + 1 sub-component (DiagnosticsSysadminTab — 8 danger-gated sysadmin commands) + 1 scoped CSS partial. ~1300-1600 net new lines. Plan completes under 50% context (highest of the wave-2 plans, still within budget).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-dev-tools-admin/07-CONTEXT.md
@.planning/phases/07-dev-tools-admin/07-PATTERNS.md
@.planning/phases/07-dev-tools-admin/07-02-SUMMARY.md
@.planning/phases/06-life-os-identity/06-06-PLAN.md
@.planning/RECOVERY_LOG.md
@CLAUDE.md
@src/lib/tauri/admin.ts
@src/lib/events/index.ts
@src/hooks/usePrefs.ts
@src/design-system/primitives/index.ts
@src/features/admin/index.tsx
@src/features/admin/types.ts
@src/features/admin/admin.css
@src/lib/context/ToastContext.tsx

<interfaces>
```ts
// Surfaces consumed from Plan 07-02:
import {
  temporalWhatWasIDoing, temporalDailyStandup, temporalDetectPatterns, temporalMeetingPrep,
  exmemRecord, exmemSearch, exmemRecent,
  supervisorGetHealth, supervisorGetService,
  getRecentTraces,
  authorityGetAgents, authorityGetAuditLog, authorityGetDelegations,
  authorityDelegate, authorityRouteAndRun, authorityRunChain,
  deepScanStart, deepScanResults, deepScanSummary,
  sysadminDetectHardware, sysadminDryRunEdit, sysadminDryRunCommand,
  sysadminListCheckpoints, sysadminSaveCheckpoint, sysadminLoadCheckpoint,
  sysadminRollback, sysadminSudoExec,
  debugConfig, setConfig, updateInitPrefs, resetOnboarding,
  integrationGetState, integrationToggle, integrationPollNow,
  mcpAddServer, mcpInstallCatalogServer, mcpDiscoverTools, mcpCallTool,
  mcpGetTools, mcpGetServers, mcpRemoveServer, mcpServerStatus, mcpServerHealth,
  classifyMcpTool, setToolTrust, resetToolTrust, getToolOverrides,
  getAllProviderKeys, storeProviderKey, switchProvider,
  getTaskRouting, setTaskRouting, saveConfigField, testProvider,
} from '@/lib/tauri/admin';

// Plan 07-01 Prefs:
//   prefs['admin.activeTab']
//   prefs['admin.security.expandedAlert']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship Temporal + Diagnostics (+ DiagnosticsSysadminTab) (ADMIN-06..07 — SC-4 falsifier)</name>
  <files>src/features/admin/Temporal.tsx, src/features/admin/Diagnostics.tsx, src/features/admin/DiagnosticsSysadminTab.tsx, src/features/admin/admin-rich-b.css</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §4, §5, §9
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-184
    - /home/arnav/blade/src/lib/tauri/admin.ts (Plan 07-02 output)
    - /home/arnav/blade/src/features/admin/admin.css (danger-banner from Plan 07-02)
    - /home/arnav/blade/src/features/admin/admin-rich-a.css (Plan 07-05)
    - /home/arnav/blade/src/features/identity/PersonaView.tsx (Phase 6 tabbed surface reference)
  </read_first>
  <action>
    **Temporal.tsx** — per D-184:
    - Hero card: on mount `temporalDailyStandup()` → render `TemporalStandup` (summary + highlights bullets + blockers bullets). If no standup available, honest empty-state.
    - Tabs (persisted via `prefs.admin.activeTab` with key `temp:<tab>`): "What was I doing" / "Patterns" / "Meeting prep" / "Execution memory".
      - **What was I doing:** `window_hours` input (default 24) + "Recall" button → `temporalWhatWasIDoing({windowHours})` → result card.
      - **Patterns:** `temporalDetectPatterns()` → list of `TemporalPattern`s.
      - **Meeting prep:** meeting_id input + "Prep" → `temporalMeetingPrep({meetingId})` → briefing card. Note: shared read with Phase 6 MeetingsView (D-184). Add inline note "Shared read with /meetings route — use this tab for debugging / admin."
      - **Execution memory:** `exmemRecent({limit: 50})` feed + "Search" input → `exmemSearch({query})`; debug "Record" form (tool + input + output inputs) → `exmemRecord({...})`.
    - data-testid: `temporal-root`, `temporal-tab`, `temporal-standup-card`, `temporal-recall-button`, `temporal-exmem-feed`.

    **Diagnostics.tsx** — per D-184 (SC-4 falsifier):
    - Hero: on mount `supervisorGetHealth()` → `.admin-health-grid` of service cards (from Pattern §5 recipe). Each card: name + status chip + uptime. Use `data-status="complete"` if running / `"failed"` if error / `"idle"` if stopped.
    - Below hero, tabs (persisted via `prefs.admin.activeTab` with key `diag:<tab>`): "Health" (default) / "Traces" / "Authority" / "Deep scan" / "Sysadmin" / "Config".
      - **Health:** same grid from hero (or repeat with "Refresh" button + per-service detail via `supervisorGetService({name})` on card click).
      - **Traces:** `getRecentTraces({limit: 100})` → reverse-chrono list with span + duration_ms + timestamp.
      - **Authority:** `Promise.all([authorityGetAgents(), authorityGetDelegations()])` → two panels. "Delegate" Dialog (agent + task inputs) → `authorityDelegate({...})` Dialog-confirm. "Route and run" Dialog → `authorityRouteAndRun({...})` Dialog-confirm. "Run chain" Dialog → `authorityRunChain({...})` Dialog-confirm + variant=danger (long-running).
      - **Deep scan:** `deepScanResults()` list + "Run now" → `deepScanStart()` + "Summary" → `deepScanSummary()`.
      - **Sysadmin:** → `<DiagnosticsSysadminTab />`.
      - **Config:** `debugConfig()` read → JSON pre; "Set config" Dialog with JSON textarea → `setConfig({config: JSON.parse(text)})` Dialog-confirm danger. "Update init prefs" Dialog → `updateInitPrefs({...})`. "Reset onboarding" button ALL-CAPS warning → Dialog-confirm → `resetOnboarding()` + toast.
    - data-testid: `diagnostics-root`, `supervisor-health-grid`, `health-card`, `diagnostics-tab`, `diagnostics-traces-list`, `diagnostics-config-pre`, `diagnostics-reset-onboarding-button`.

    **DiagnosticsSysadminTab.tsx** — per D-184 (DANGER ZONE):
    - Top: `<div className="danger-banner">SYSADMIN ACTIONS CAN MODIFY OR DESTROY SYSTEM STATE. USE CHECKPOINTS BEFORE ANY RISKY OPERATION.</div>`
    - Sections:
      - "Hardware" — "Detect hardware" button → `sysadminDetectHardware()` → result card.
      - "Checkpoints" — `sysadminListCheckpoints()` list; "Save checkpoint" Dialog (name input) → `sysadminSaveCheckpoint({name})`; per-checkpoint "Load" Dialog-confirm → `sysadminLoadCheckpoint({id})`; "Rollback" Dialog-confirm danger → `sysadminRollback()`.
      - "Dry run" — two panels: "Dry run edit" (path + content inputs) → `sysadminDryRunEdit({path, newContent})` → diff preview; "Dry run command" (command input) → `sysadminDryRunCommand({command})` → would-change chip.
      - "Sudo exec" — ALL-CAPS warning + Dialog with command input + rationale textarea (required) → `sysadminSudoExec({command, rationale})` Dialog-confirm variant=danger.
    - data-testid: `diagnostics-sysadmin-root`, `sysadmin-checkpoint-row`, `sysadmin-sudo-button`, `sysadmin-rollback-button`, `danger-banner`.

    **admin-rich-b.css** — scoped partial:
    ```css
    /* src/features/admin/admin-rich-b.css — Plan 07-06 scoped partial */

    @layer features {
      .temporal-layout, .diagnostics-layout {
        display: flex;
        flex-direction: column;
        gap: var(--sp-3);
      }

      .temporal-hero {
        padding: var(--sp-3);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
      }

      .integration-service-card {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: var(--sp-2);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        margin-bottom: var(--sp-1);
      }

      .mcp-server-row {
        padding: var(--sp-2);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        margin-bottom: var(--sp-1);
      }

      .model-comparison-table {
        display: grid;
        grid-template-columns: 160px 1fr 140px 100px;
        gap: var(--sp-1);
        align-items: center;
        padding: var(--sp-1);
        border-bottom: 1px solid var(--line);
      }

      .key-vault-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: var(--sp-2);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        margin-bottom: var(--sp-1);
        font-family: var(--font-mono);
      }

      .key-masked {
        font-family: var(--font-mono);
        color: var(--t-3);
        letter-spacing: 0.1em;
      }
    }
    ```

    **CRITICAL:**
    - SC-4 "Diagnostics view shows module health for all running background tasks" — `supervisorGetHealth()` call + grid render is the direct falsifier.
    - All sysadmin actions Dialog-confirmed per D-184 + Pattern §4.
    - `resetOnboarding` is DESTRUCTIVE (clears onboarding state + prefs) — Dialog-confirm + danger variant required.
  </action>
  <verify>
    <automated>test -f src/features/admin/Temporal.tsx && test -f src/features/admin/Diagnostics.tsx && test -f src/features/admin/DiagnosticsSysadminTab.tsx && test -f src/features/admin/admin-rich-b.css && grep -q "temporalDailyStandup\|temporalDetectPatterns\|exmemRecent" src/features/admin/Temporal.tsx && grep -q "supervisorGetHealth\|getRecentTraces\|deepScanResults" src/features/admin/Diagnostics.tsx && grep -q "sysadminSudoExec\|sysadminRollback\|sysadminSaveCheckpoint" src/features/admin/DiagnosticsSysadminTab.tsx && grep -q "danger-banner" src/features/admin/DiagnosticsSysadminTab.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>
    Temporal + Diagnostics + DiagnosticsSysadminTab shipped; SC-4 falsifier in place; all sysadmin actions Dialog-gated; `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship IntegrationStatus + McpSettings + ModelComparison + KeyVault (ADMIN-08..10 + synthetic ADMIN-11)</name>
  <files>src/features/admin/IntegrationStatus.tsx, src/features/admin/McpSettings.tsx, src/features/admin/ModelComparison.tsx, src/features/admin/KeyVault.tsx</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §4
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-184, §D-185
    - /home/arnav/blade/src/lib/tauri/admin.ts
    - /home/arnav/blade/src/lib/events/index.ts (Plan 07-01 — check if INTEGRATION_STATUS_CHANGED exists)
    - /home/arnav/blade/src/features/admin/admin-rich-b.css (just-created Task 1)
  </read_first>
  <action>
    **IntegrationStatus.tsx** — per D-184:
    - On mount: `integrationGetState()` → per-service cards in `.integration-service-card` row style.
    - Each service card: name + enabled toggle (on toggle → `integrationToggle({service, enabled})` + refresh; toggling OFF for production services Dialog-confirms) + last_poll_at timestamp + "Poll now" button (spinner while awaiting `integrationPollNow({service})`).
    - MCP servers sub-section: `mcpGetServers()` + per-server `mcpServerHealth({server})` (reconnect_attempts chip).
    - Optional live subscription: if INTEGRATION_STATUS_CHANGED constant exists (Plan 07-01 audit), `useTauriEvent` to auto-refresh service cards.
    - data-testid: `integration-status-root`, `integration-service-card`, `integration-toggle`, `integration-poll-now`, `mcp-health-chip`.

    **McpSettings.tsx** — per D-185:
    - On mount: `mcpGetServers()` → cards in `.mcp-server-row` style. Per-server: connected chip + `mcpGetTools({server})` count + `mcpServerStatus({server})` detail.
    - Top-actions: "Add server" Dialog (name / command / args / env JSON editor) → `mcpAddServer({name, command, mcpArgs, env})` (note: wrapper uses `mcpArgs` key to avoid TS `args` collision per Plan 07-02). "Install from catalog" Dialog (catalogId input) → `mcpInstallCatalogServer({catalogId})`.
    - Per-server actions: "Discover tools" → `mcpDiscoverTools({server})`; "Call tool" debug Dialog (tool name + args JSON) → `mcpCallTool({server, tool, args})`; "Remove" Dialog-confirm danger → `mcpRemoveServer({server})`.
    - Tool trust sub-section: `getToolOverrides()` → per-tool row with trust pill-select (`trusted`/`ask`/`blocked`) → `setToolTrust({toolName, trust})`; "Reset" button → `resetToolTrust({toolName})`; `classifyMcpTool({toolName})` for unknown tools (debug panel).
    - data-testid: `mcp-settings-root`, `mcp-server-row`, `mcp-add-server-button`, `mcp-remove-button`, `mcp-tool-trust-select`, `mcp-call-tool-input`.

    **ModelComparison.tsx** — per D-185:
    - On mount: `getTaskRouting()` → object like `{ chat: {provider, model}, reasoning: {...}, agent: {...}, vision: {...} }`.
    - Render as `.model-comparison-table` grid — rows: task name + provider chip + model chip + latency chip + "Test" + "Change" buttons.
    - "Test" button per row → `testProvider({provider: row.provider})` → shows latency_ms or error chip.
    - "Change" button per row → Dialog with provider + model inputs → `setTaskRouting({task, provider, model})` then `saveConfigField({field: 'task_routing', value: …})` OR just rely on set_task_routing to persist internally; prefer the second if set_task_routing writes config.rs directly.
    - Top action: "Switch provider globally" button → Dialog with provider select → `switchProvider({provider})` Dialog-confirm (disrupts in-flight chat).
    - data-testid: `model-comparison-root`, `task-routing-row`, `task-test-button`, `task-change-button`, `task-latency-chip`.

    **KeyVault.tsx** — per D-185 (synthetic ADMIN-11 orphan):
    - On mount: `getAllProviderKeys()` → array of `ProviderKey`. Each has `provider` + `key_masked`. If Rust returns full keys (not masked), mask client-side: show first 4 chars + `....` + last 4. If shorter than 8, show `********` (never leak).
    - Each row: provider name + `.key-masked` span + "Copy" button (copies to clipboard — note risk, TBD per D-185 Discretion — planner picks yes for operator ergonomics, since this IS the operator's Mac).
    - Top action: "Store key" Dialog (provider select + api_key password input — masked typed input) → `storeProviderKey({provider, apiKey})` + refresh + toast.
    - Footer: honest note card — "Keys backed by OS keyring (macOS Keychain / Windows Credential Store / Linux libsecret). Keys never written to disk in plaintext."
    - data-testid: `key-vault-root`, `provider-key-row`, `key-vault-store-button`, `key-vault-store-input`.

    **CRITICAL:**
    - D-185 KeyVault mask policy: last-4 only; never expose the whole key in the DOM.
    - `storeProviderKey` input MUST be `type="password"` and the field cleared after submit.
    - McpSettings `mcpAddServer` wrapper uses `mcpArgs` param name (not `args` — TS reserved word collision) per Plan 07-02 D-186 correction.
    - `integrationToggle` OFF for "production-looking" services (gmail, slack, github) Dialog-confirmed; benign off-by-default services (local-only) plain toggle.
    - All invokes through wrappers (no raw invoke).
  </action>
  <verify>
    <automated>test -f src/features/admin/IntegrationStatus.tsx && test -f src/features/admin/McpSettings.tsx && test -f src/features/admin/ModelComparison.tsx && test -f src/features/admin/KeyVault.tsx && grep -q "integrationGetState\|integrationToggle\|integrationPollNow" src/features/admin/IntegrationStatus.tsx && grep -q "mcpGetServers\|mcpAddServer\|mcpRemoveServer\|setToolTrust" src/features/admin/McpSettings.tsx && grep -q "getTaskRouting\|setTaskRouting\|testProvider" src/features/admin/ModelComparison.tsx && grep -q "getAllProviderKeys\|storeProviderKey" src/features/admin/KeyVault.tsx && grep -q "key-masked\|key_masked\|\\.\\.\\.\\." src/features/admin/KeyVault.tsx && npx tsc --noEmit && npm run verify:all</automated>
  </verify>
  <done>
    IntegrationStatus + McpSettings + ModelComparison + KeyVault shipped; KeyVault masks keys to last-4; McpSettings destructive ops Dialog-gated; `npx tsc --noEmit` + `npm run verify:all` pass.
  </done>
</task>

</tasks>

<verification>
- 6 real route components + 1 sub-component + 1 CSS partial shipped.
- Every route invokes at least one admin wrapper (verified via grep per-file).
- `npx tsc --noEmit` passes.
- `npm run verify:all` passes — no regressions.
- ESLint passes (no-raw-tauri rule enforced).
- `files_modified` list is disjoint from Plans 07-03 / 07-04 / 07-05.
</verification>

<success_criteria>
- SC-4 "Diagnostics view shows module health for all running background tasks" — Diagnostics.tsx `supervisorGetHealth()` hero directly falsifies.
- ADMIN-06..10 + KeyVault synthetic orphan mapped to real UI surfaces.
- No route renders a 404 fallback (D-168 policy).
- All dangerous admin ops (sysadmin_sudo_exec, sysadmin_rollback, mcp_remove_server, resetOnboarding, switchProvider, setConfig JSON) Dialog-gated + variant="danger".
- KeyVault masks provider keys to last-4 (D-185).
- Zero Rust file edits.
</success_criteria>

<threat_model>
## Trust Boundaries

- **User input → sysadmin_sudo_exec boundary**: DiagnosticsSysadminTab sudo input flows directly into elevated shell. Dialog-confirm IS authorization.
- **User input → mcp_add_server command/args/env**: McpSettings adds MCP server with user-supplied command/args. If malicious path, Rust-side MCP spawn inherits user's shell.
- **User input → api_key storage**: KeyVault `storeProviderKey` flows into OS keyring. Rust-side keyring plugin handles secure storage.
- **User input → setConfig JSON**: DiagnosticsConfigTab accepts raw JSON config. Malformed JSON throws client-side; malicious config can break app startup (Dialog-confirm mitigates accidents).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Elevation of Privilege | DiagnosticsSysadminTab `sudo_exec` runs arbitrary sudo | mitigate | ALL-CAPS warning banner + required rationale + Dialog-confirm + variant="danger" per D-184. User click IS authorization. |
| T-07-06-02 | Elevation of Privilege | McpSettings arbitrary server command | mitigate | Dialog-confirm required; user IS authorizing. Rust-side MCP spawn inherits user permissions (not elevated). |
| T-07-06-03 | Information Disclosure | KeyVault exposes provider keys in DOM | mitigate | Keys masked to last-4 per D-185. Full keys never rendered. |
| T-07-06-04 | Information Disclosure | KeyVault "Copy" button copies full key to clipboard | mitigate | Operator ergonomics accepted per D-185; clipboard cleared on next copy; user's own machine. |
| T-07-06-05 | Tampering | `resetOnboarding` wipes user progress | mitigate | Dialog-confirm with variant="danger". |
| T-07-06-06 | Denial of Service | `switchProvider` during active chat breaks streaming | mitigate | Dialog-confirm warns about interrupting in-flight requests. |
| T-07-06-07 | Tampering | `setConfig` accepts malformed JSON | mitigate | Client-side JSON.parse try/catch; Dialog-confirm required; toast on parse error. |
| T-07-06-08 | Denial of Service | `deepScanStart` long-running | accept | User-initiated; results pane shows progress. |
</threat_model>

<output>
After completion, create `.planning/phases/07-dev-tools-admin/07-06-SUMMARY.md` with:
- List of 6 real routes + 1 sub-component + 1 CSS partial shipped.
- Command wiring table (which wrapper each route uses).
- SC-4 falsification summary (Diagnostics.tsx + supervisorGetHealth grid).
- Any Rust signature mismatches encountered vs. Plan 07-02 types.
- Confirmation data-testid coverage is complete for Plan 07-07 spec.
- Prefs integration verified (admin.activeTab for Temporal + Diagnostics tabs).
- KeyVault mask strategy (exact client-side masking implementation).
- Any divergences from D-184..D-185 with rationale.
</output>
