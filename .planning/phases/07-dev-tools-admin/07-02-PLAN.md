---
phase: 07-dev-tools-admin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tauri/dev_tools.ts
  - src/lib/tauri/admin.ts
  - src/lib/tauri/index.ts
  - src/features/dev-tools/index.tsx
  - src/features/admin/index.tsx
  - src/features/dev-tools/types.ts
  - src/features/admin/types.ts
  - src/features/dev-tools/dev-tools.css
  - src/features/admin/admin.css
  - src/features/dev-tools/Terminal.tsx
  - src/features/dev-tools/FileBrowser.tsx
  - src/features/dev-tools/GitPanel.tsx
  - src/features/dev-tools/Canvas.tsx
  - src/features/dev-tools/WorkflowBuilder.tsx
  - src/features/dev-tools/WebAutomation.tsx
  - src/features/dev-tools/EmailAssistant.tsx
  - src/features/dev-tools/DocumentGenerator.tsx
  - src/features/dev-tools/CodeSandbox.tsx
  - src/features/dev-tools/ComputerUse.tsx
  - src/features/admin/Analytics.tsx
  - src/features/admin/CapabilityReports.tsx
  - src/features/admin/Reports.tsx
  - src/features/admin/DecisionLog.tsx
  - src/features/admin/SecurityDashboard.tsx
  - src/features/admin/Temporal.tsx
  - src/features/admin/Diagnostics.tsx
  - src/features/admin/IntegrationStatus.tsx
  - src/features/admin/McpSettings.tsx
  - src/features/admin/ModelComparison.tsx
  - src/features/admin/KeyVault.tsx
autonomous: true
requirements:
  - DEV-01  # Terminal — requires runShell, runCodeBlock
  - DEV-02  # FileBrowser — requires fileRead, fileTree, fileIndexSearch, bladeListIndexedProjects
  - DEV-03  # GitPanel — requires gitStyleMine, gitStyleGet, gitStyleClear
  - DEV-04  # Canvas — requires sandboxRun, sandboxDetectLanguage (thin)
  - DEV-05  # WorkflowBuilder — requires workflowList, workflowCreate, workflowRunNow, cronAdd
  - DEV-06  # WebAutomation — requires browserAgentLoop, browserAction, browserSessionStatus
  - DEV-07  # EmailAssistant — requires autoReplyDraft, reminderAddNatural
  - DEV-08  # DocumentGenerator — requires docList, docIngest, docGenerateStudyNotes, docCrossSynthesis
  - DEV-09  # CodeSandbox — requires sandboxRun, sandboxRunExplain, sandboxFixAndRun
  - DEV-10  # ComputerUse — requires computerUseTask, computerUseScreenshot, automation::*, uia::*
  - ADMIN-01 # Analytics — requires dbAnalyticsSummary, dbEventsSince
  - ADMIN-02 # CapabilityReports — requires evolutionGetSuggestions, selfUpgradeCatalog, toolForgeListTools
  - ADMIN-03 # (Reports — synthetic route for P-03) — requires getReports, reportGap, updateReportStatus
  - ADMIN-04 # DecisionLog — requires getDecisionLog, decisionFeedback, authorityGetAuditLog, auditGetLog
  - ADMIN-05 # SecurityDashboard — requires securityOverview, symbolicListPolicies, pentest*
  - ADMIN-06 # Temporal — requires temporalDailyStandup, temporalDetectPatterns, exmemRecent
  - ADMIN-07 # Diagnostics — requires supervisorGetHealth, getRecentTraces, deepScanResults, sysadmin*
  - ADMIN-08 # IntegrationStatus — requires integrationGetState, integrationToggle, mcpGetServers
  - ADMIN-09 # McpSettings — requires mcpGetServers, mcpAddServer, permissionsGetToolOverrides
  - ADMIN-10 # ModelComparison — requires getTaskRouting, setTaskRouting, testProvider
must_haves:
  truths:
    - "src/lib/tauri/dev_tools.ts exports ~90 camelCase wrapper functions spanning 17 Rust modules — native_tools, files, file_indexer, indexer, git_style, code_sandbox, workflow_builder, browser_agent, browser_native, auto_reply, document_intelligence, computer_use, automation, ui_automation, reminders, watcher, cron (D-167 inventory)"
    - "src/lib/tauri/admin.ts exports ~110 camelCase wrapper functions spanning 20 Rust modules — commands (mcp + test_provider + admin helpers), permissions, db_commands (analytics subset), reports, self_upgrade, evolution, immune_system, decision_gate, authority_engine, audit, security_monitor, symbolic, temporal_intel, execution_memory, deep_scan, supervisor, trace, sysadmin, integration_bridge, config (provider/routing), self_critique, tool_forge (D-167 inventory)"
    - "Both wrapper files use invokeTyped<TReturn, TArgs>(command, args); raw invoke NEVER used (ESLint no-raw-tauri passes)"
    - "Each wrapper has JSDoc @see src-tauri/src/<file>.rs citation matching the Rust #[tauri::command] (D-186 / D-38 naming discipline)"
    - "src/features/dev-tools/index.tsx REWRITTEN — 10 lazy imports of per-route files — each component file created with minimal GlassPanel placeholder (Plans 07-03/07-04 fill in the bodies)"
    - "src/features/admin/index.tsx REWRITTEN — 11 lazy imports of per-route files (10 ADMIN + 1 synthetic reports) — each component file created with minimal placeholder"
    - "Both cluster index.tsx files preserve route id order and RouteDefinition[] shape (D-170 single-writer discipline)"
    - "Per-route files are MINIMAL placeholders — each exports a named component that renders a GlassPanel wrapper with route label + 'Ships in Plan 07-NN' placeholder text. Plans 07-03..06 REPLACE these placeholder bodies"
    - "src/features/dev-tools/types.ts + src/features/admin/types.ts re-export cluster-local types from the wrappers + cluster-only UI types — D-188"
    - "src/features/dev-tools/dev-tools.css + src/features/admin/admin.css created with shared layer discipline (D-191) + rgba-bg card classes (D-70) + reuse Phase 5 status tokens (D-132) + Terminal-specific scrollback styling + danger-zone banner"
    - "src/lib/tauri/index.ts barrel re-exports both new modules for convenience (consistent with Phase 5 + 6 pattern)"
    - "npx tsc --noEmit passes; ESLint passes; npm run verify:all passes; npm run dev builds without error (route lazy imports resolve)"
    - "Zero Rust changes (D-167 + D-171)"
  artifacts:
    - path: "src/lib/tauri/dev_tools.ts"
      provides: "Typed wrappers for ~90 Dev Tools commands"
      exports: ["runCodeBlock", "runShell", "askAi", "fileRead", "fileWrite", "fileList", "fileTree", "fileExists", "fileMkdir", "fileIndexScanNow", "fileIndexSearch", "fileIndexRecent", "fileIndexStats", "bladeIndexProject", "bladeFindSymbol", "bladeListIndexedProjects", "bladeReindexFile", "bladeProjectSummary", "gitStyleMine", "gitStyleGet", "gitStyleClear", "sandboxRun", "sandboxRunExplain", "sandboxFixAndRun", "sandboxDetectLanguage", "workflowList", "workflowGet", "workflowCreate", "workflowUpdate", "workflowDelete", "workflowRunNow", "workflowGetRuns", "workflowGenerateFromDescription", "browserAction", "browserAgentLoop", "webAction", "browserDescribePage", "browserSessionStatus", "connectToUserBrowser", "autoReplyDraft", "autoReplyLearnFromEdit", "autoReplyDraftBatch", "docIngest", "docSearch", "docGet", "docList", "docDelete", "docAnswerQuestion", "docCrossSynthesis", "docGenerateStudyNotes", "computerUseTask", "computerUseStop", "computerUseScreenshot", "autoTypeText", "autoPressKey", "autoKeyCombo", "autoMouseMove", "autoGetMousePosition", "autoMouseClick", "autoMouseClickRelative", "autoMouseDoubleClick", "autoMouseDrag", "autoScroll", "autoOpenUrl", "autoOpenPath", "autoLaunchApp", "autoCopyToClipboard", "autoPasteClipboard", "uiaGetActiveWindowSnapshot", "uiaDescribeActiveWindow", "uiaClickElement", "uiaInvokeElement", "uiaFocusElement", "uiaSetElementValue", "uiaWaitForElement", "reminderAdd", "reminderAddNatural", "reminderList", "reminderDelete", "reminderParseTime", "watcherAdd", "watcherListAll", "watcherRemove", "watcherToggle", "cronAdd", "cronList", "cronDelete", "cronToggle", "cronRunNow"]
    - path: "src/lib/tauri/admin.ts"
      provides: "Typed wrappers for ~110 Admin commands"
      exports: ["mcpAddServer", "mcpInstallCatalogServer", "mcpDiscoverTools", "mcpCallTool", "mcpGetTools", "mcpGetServers", "mcpRemoveServer", "mcpServerStatus", "mcpServerHealth", "testProvider", "debugConfig", "setConfig", "updateInitPrefs", "resetOnboarding", "classifyMcpTool", "setToolTrust", "resetToolTrust", "getToolOverrides", "dbTrackEvent", "dbEventsSince", "dbPruneAnalytics", "dbAnalyticsSummary", "reportGap", "getReports", "updateReportStatus", "setReportWebhook", "getReportWebhook", "selfUpgradeInstall", "selfUpgradeCatalog", "selfUpgradeAudit", "pentestAuthorize", "pentestCheckAuth", "pentestRevoke", "pentestListAuth", "pentestCheckModelSafety", "evolutionGetLevel", "evolutionGetSuggestions", "evolutionDismissSuggestion", "evolutionInstallSuggestion", "evolutionRunNow", "evolutionLogCapabilityGap", "immuneResolveGap", "getDecisionLog", "decisionFeedback", "decisionEvaluate", "authorityGetAgents", "authorityGetAuditLog", "authorityGetDelegations", "authorityDelegate", "authorityRouteAndRun", "authorityRunChain", "auditGetLog", "securityScanNetwork", "securityCheckBreach", "securityCheckPasswordHash", "securityScanSensitiveFiles", "securityCheckUrl", "securityOverview", "securityRunAudit", "securityAuditDeps", "securityScanCode", "symbolicCheckPolicy", "symbolicListPolicies", "symbolicAddPolicy", "symbolicVerifyPlan", "temporalWhatWasIDoing", "temporalDailyStandup", "temporalDetectPatterns", "temporalMeetingPrep", "exmemRecord", "exmemSearch", "exmemRecent", "deepScanStart", "deepScanResults", "deepScanSummary", "supervisorGetHealth", "supervisorGetService", "getRecentTraces", "sysadminDetectHardware", "sysadminDryRunEdit", "sysadminDryRunCommand", "sysadminListCheckpoints", "sysadminSaveCheckpoint", "sysadminLoadCheckpoint", "sysadminRollback", "sysadminSudoExec", "integrationGetState", "integrationToggle", "integrationPollNow", "getAllProviderKeys", "storeProviderKey", "switchProvider", "getTaskRouting", "setTaskRouting", "saveConfigField", "selfCritiqueResponse", "selfCritiqueHistory", "selfCritiqueDeepRoast", "selfCritiqueWeeklyMeta", "forgeNewTool", "forgeListTools", "forgeDeleteTool", "forgeTestTool"]
    - path: "src/features/dev-tools/index.tsx"
      provides: "10-route RouteDefinition[] with lazy imports of real components"
    - path: "src/features/admin/index.tsx"
      provides: "11-route RouteDefinition[] (10 ADMIN + synthetic reports) with lazy imports"
    - path: "src/features/dev-tools/types.ts"
      provides: "Cluster-local re-exports + UI types"
    - path: "src/features/admin/types.ts"
      provides: "Cluster-local re-exports + UI types"
    - path: "src/features/dev-tools/dev-tools.css"
      provides: "Cluster-scoped CSS — shared base + terminal scrollback + file tree + tab pills"
      contains: ".dev-surface"
    - path: "src/features/admin/admin.css"
      provides: "Cluster-scoped CSS — shared base + admin health grid + danger banner + tab pills"
      contains: ".admin-surface"
    - path: "src/lib/tauri/index.ts"
      provides: "Extended barrel with dev_tools + admin re-exports"
  key_links:
    - from: "src/lib/tauri/dev_tools.ts"
      to: "src-tauri/src/lib.rs generate_handler! (lines 574..1394)"
      via: "invokeTyped('run_shell' / 'file_tree' / 'workflow_*' / 'browser_agent_*' / 'auto_reply_*' / 'doc_*' / 'computer_use_*' / 'auto_*' / 'uia_*' / 'reminder_*' / 'watcher_*' / 'cron_*' / 'git_style_*' / 'sandbox_*' / 'blade_index_*', ...)"
      pattern: "invokeTyped\\('run_shell|invokeTyped\\('file_|invokeTyped\\('workflow_|invokeTyped\\('browser_|invokeTyped\\('auto_reply_|invokeTyped\\('doc_|invokeTyped\\('computer_use_|invokeTyped\\('auto_|invokeTyped\\('uia_|invokeTyped\\('reminder_|invokeTyped\\('watcher_|invokeTyped\\('cron_|invokeTyped\\('git_style_|invokeTyped\\('sandbox_|invokeTyped\\('blade_"
    - from: "src/lib/tauri/admin.ts"
      to: "src-tauri/src/lib.rs generate_handler!"
      via: "invokeTyped for mcp_* / permissions::* / db_* / reports::* / self_upgrade::* / evolution::* / decision_gate::* / authority_engine::* / audit::* / security_monitor::* / symbolic::* / temporal_* / exmem_* / deep_scan::* / supervisor::* / sysadmin::* / integration_* / config::* / self_critique::* / forge_*"
      pattern: "invokeTyped\\('mcp_|invokeTyped\\('classify_mcp|invokeTyped\\('set_tool_trust|invokeTyped\\('db_|invokeTyped\\('report_|invokeTyped\\('self_upgrade_|invokeTyped\\('pentest_|invokeTyped\\('evolution_|invokeTyped\\('immune_resolve_gap|invokeTyped\\('get_decision|invokeTyped\\('authority_|invokeTyped\\('audit_get_log|invokeTyped\\('security_|invokeTyped\\('symbolic_|invokeTyped\\('temporal_|invokeTyped\\('exmem_|invokeTyped\\('deep_scan_|invokeTyped\\('supervisor_|invokeTyped\\('sysadmin_|invokeTyped\\('integration_|invokeTyped\\('get_task_routing|invokeTyped\\('switch_provider|invokeTyped\\('test_provider|invokeTyped\\('self_critique_|invokeTyped\\('forge_"
    - from: "src/features/dev-tools/index.tsx"
      to: "src/features/dev-tools/Terminal.tsx etc."
      via: "React.lazy + dynamic import"
      pattern: "lazy\\(\\(\\) => import"
    - from: "src/features/admin/index.tsx"
      to: "src/features/admin/Analytics.tsx etc."
      via: "React.lazy + dynamic import"
      pattern: "lazy\\(\\(\\) => import"
---

<objective>
Create the two cluster-level Tauri wrapper modules + rewrite both cluster feature index files with real lazy imports + seed 21 minimal per-route component files that Plans 07-03..06 will fill in. No behavioral UI work in this plan — only plumbing.

Purpose: ROADMAP Phase 7 description "each cluster wires its own lib/tauri module" + D-166 cluster-scoped wrapper discipline + D-170 single-writer index.tsx invariant. After this plan lands, Plans 07-03..06 can ship their route bodies with zero import-path surprises and zero chance of stepping on each other's index.tsx edits.

Output: ~200 camelCase wrapper exports across 2 new TS files; 2 rewritten index.tsx files; 21 new per-route placeholder files; 2 cluster CSS files; 2 type barrel files; 1 barrel edit. Roughly 2000-2800 net new lines — mostly mechanical wrapper declarations. Plan completes under 50% context — large file count but each file is tightly templated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-dev-tools-admin/07-CONTEXT.md
@.planning/phases/07-dev-tools-admin/07-PATTERNS.md
@.planning/phases/06-life-os-identity/06-02-PLAN.md
@.planning/phases/06-life-os-identity/06-02-SUMMARY.md
@.planning/phases/05-agents-knowledge/05-02-PLAN.md
@.planning/RECOVERY_LOG.md
@CLAUDE.md
@src/lib/tauri/_base.ts
@src/lib/tauri/chat.ts
@src/lib/tauri/agents.ts
@src/lib/tauri/knowledge.ts
@src/lib/tauri/life_os.ts
@src/lib/tauri/identity.ts
@src/lib/tauri/index.ts
@src/lib/router.ts
@src/features/dev-tools/index.tsx
@src/features/admin/index.tsx
@src/features/agents/agents.css
@src/features/life-os/life-os.css
@src/styles/tokens.css
@src-tauri/src/lib.rs

<interfaces>
<!-- Existing wrapper pattern (from Phase 5/6 src/lib/tauri/*.ts) -->
```ts
// Established recipe (Phase 5 Plan 05-02; follow identically for dev_tools.ts + admin.ts)

import { invokeTyped } from './_base';

export interface SomeType {
  id: string;
  // ... mirror Rust Serialize shape, snake_case preserved
  [k: string]: unknown;
}

/** @see src-tauri/src/<module>.rs <command_name> */
export function camelCaseWrapper(args: { camelCaseArg: string }): Promise<SomeType> {
  return invokeTyped<SomeType>('snake_case_command', {
    snake_case_arg: args.camelCaseArg,
  });
}
```

<!-- Route definition shape (D-39) -->
```ts
// src/lib/router.ts
export interface RouteDefinition {
  id: string;
  label: string;
  section: 'core' | 'agents' | 'knowledge' | 'life' | 'identity' | 'dev' | 'admin' | 'body' | 'hive';
  component: React.LazyExoticComponent<React.ComponentType<any>>;
  icon?: React.ComponentType;
  shortcut?: string;
  paletteHidden?: boolean;
  description?: string;
  phase?: number;
}
```

<!-- Rust command surface summary (from lib.rs:574-1394 generate_handler!) — D-167 inventory -->
```
DEV TOOLS cluster (~90 commands):
  native_tools (3): run_code_block, run_shell, ask_ai
  files (6): file_read, file_write, file_list, file_tree, file_exists, file_mkdir
  file_indexer (4): file_index_scan_now, file_index_search, file_index_recent, file_index_stats
  indexer (5): blade_index_project, blade_find_symbol, blade_list_indexed_projects, blade_reindex_file, blade_project_summary
  git_style (3): git_style_mine, git_style_get, git_style_clear
  code_sandbox (4): sandbox_run, sandbox_run_explain, sandbox_fix_and_run, sandbox_detect_language
  workflow_builder (8): workflow_list, workflow_get, workflow_create, workflow_update, workflow_delete,
    workflow_run_now, workflow_get_runs, workflow_generate_from_description
  browser_agent (2): browser_action, browser_agent_loop
  browser_native (4): web_action, browser_describe_page, browser_session_status, connect_to_user_browser
  auto_reply (3): auto_reply_draft, auto_reply_learn_from_edit, auto_reply_draft_batch
  document_intelligence (8): doc_ingest, doc_search, doc_get, doc_list, doc_delete,
    doc_answer_question, doc_cross_synthesis, doc_generate_study_notes
  computer_use (3): computer_use_task, computer_use_stop, computer_use_screenshot
  automation (15): auto_type_text, auto_press_key, auto_key_combo, auto_mouse_move, auto_get_mouse_position,
    auto_mouse_click, auto_mouse_click_relative, auto_mouse_double_click, auto_mouse_drag, auto_scroll,
    auto_open_url, auto_open_path, auto_launch_app, auto_copy_to_clipboard, auto_paste_clipboard
  ui_automation (7): uia_get_active_window_snapshot, uia_describe_active_window, uia_click_element,
    uia_invoke_element, uia_focus_element, uia_set_element_value, uia_wait_for_element
  reminders (5): reminder_add, reminder_add_natural, reminder_list, reminder_delete, reminder_parse_time
  watcher (4): watcher_add, watcher_list_all, watcher_remove, watcher_toggle
  cron (5): cron_add, cron_list, cron_delete, cron_toggle, cron_run_now

ADMIN cluster (~110 commands):
  commands (MCP + admin subset): mcp_add_server, mcp_install_catalog_server, mcp_discover_tools,
    mcp_call_tool, mcp_get_tools, mcp_get_servers, mcp_remove_server, mcp_server_status, mcp_server_health,
    test_provider, debug_config, set_config, update_init_prefs, reset_onboarding
  permissions (4): classify_mcp_tool, set_tool_trust, reset_tool_trust, get_tool_overrides
  db_commands (analytics subset — 4): db_track_event, db_events_since, db_prune_analytics, db_analytics_summary
  reports (5): report_gap, get_reports, update_report_status, set_report_webhook, get_report_webhook
  self_upgrade (8): self_upgrade_install, self_upgrade_catalog, self_upgrade_audit,
    pentest_authorize, pentest_check_auth, pentest_revoke, pentest_list_auth, pentest_check_model_safety
  evolution (6): evolution_get_level, evolution_get_suggestions, evolution_dismiss_suggestion,
    evolution_install_suggestion, evolution_run_now, evolution_log_capability_gap
  immune_system (1): immune_resolve_gap
  decision_gate (3): get_decision_log, decision_feedback, decision_evaluate
  authority_engine (6): authority_get_agents, authority_get_audit_log, authority_get_delegations,
    authority_delegate, authority_route_and_run, authority_run_chain
  audit (1): audit_get_log
  security_monitor (9): security_scan_network, security_check_breach, security_check_password_hash,
    security_scan_sensitive_files, security_check_url, security_overview, security_run_audit,
    security_audit_deps, security_scan_code
  symbolic (4): symbolic_check_policy, symbolic_list_policies, symbolic_add_policy, symbolic_verify_plan
  temporal_intel (4): temporal_what_was_i_doing, temporal_daily_standup, temporal_detect_patterns, temporal_meeting_prep
  execution_memory (3): exmem_record, exmem_search, exmem_recent
  deep_scan (3): deep_scan_start, deep_scan_results, deep_scan_summary
  supervisor (2): supervisor_get_health, supervisor_get_service
  trace (1): get_recent_traces
  sysadmin (8): sysadmin_detect_hardware, sysadmin_dry_run_edit, sysadmin_dry_run_command,
    sysadmin_list_checkpoints, sysadmin_save_checkpoint, sysadmin_load_checkpoint, sysadmin_rollback, sysadmin_sudo_exec
  integration_bridge (3): integration_get_state, integration_toggle, integration_poll_now
  config (6): get_all_provider_keys, store_provider_key, switch_provider, get_task_routing, set_task_routing, save_config_field
  self_critique (4): self_critique_response, self_critique_history, self_critique_deep_roast, self_critique_weekly_meta
  tool_forge (4): forge_new_tool, forge_list_tools, forge_delete_tool, forge_test_tool
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/tauri/dev_tools.ts with ~90 typed wrappers + src/features/dev-tools/types.ts barrel</name>
  <files>src/lib/tauri/dev_tools.ts, src/features/dev-tools/types.ts</files>
  <read_first>
    - /home/arnav/blade/src/lib/tauri/life_os.ts (Phase 6 Plan 06-02 output — recipe reference)
    - /home/arnav/blade/src/lib/tauri/_base.ts
    - /home/arnav/blade/src-tauri/src/native_tools.rs (grep '#\[tauri::command\]' to collect all signatures + line numbers)
    - /home/arnav/blade/src-tauri/src/files.rs
    - /home/arnav/blade/src-tauri/src/file_indexer.rs
    - /home/arnav/blade/src-tauri/src/indexer.rs
    - /home/arnav/blade/src-tauri/src/git_style.rs
    - /home/arnav/blade/src-tauri/src/code_sandbox.rs
    - /home/arnav/blade/src-tauri/src/workflow_builder.rs
    - /home/arnav/blade/src-tauri/src/browser_agent.rs
    - /home/arnav/blade/src-tauri/src/browser_native.rs
    - /home/arnav/blade/src-tauri/src/auto_reply.rs
    - /home/arnav/blade/src-tauri/src/document_intelligence.rs
    - /home/arnav/blade/src-tauri/src/computer_use.rs
    - /home/arnav/blade/src-tauri/src/automation.rs
    - /home/arnav/blade/src-tauri/src/ui_automation.rs
    - /home/arnav/blade/src-tauri/src/reminders.rs
    - /home/arnav/blade/src-tauri/src/watcher.rs
    - /home/arnav/blade/src-tauri/src/cron.rs
    - /home/arnav/blade/src-tauri/src/lib.rs (lines 574-1394 — confirm every command in the 17 modules above is registered)
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §1
  </read_first>
  <action>
    Create `src/lib/tauri/dev_tools.ts` (~700-900 lines expected, single file). Structure mirrors Phase 6 `src/lib/tauri/life_os.ts` verbatim:

    1. Header comment block citing D-166, D-167, D-186, D-38.
    2. `import { invokeTyped } from './_base';`
    3. **Types section** — hand-written interfaces for every non-trivial return type. Mirror Rust `#[derive(Serialize)]` struct field names verbatim (snake_case). Every interface has `[k: string]: unknown` index signature. Representative types:
       - `ShellResult` (stdout, stderr, exit_code)
       - `CodeBlockResult`, `AskAiResult`
       - `FileTreeNode`, `FileListEntry`, `FileReadResult`
       - `FileIndexEntry`, `FileIndexStats`
       - `ProjectSummary`, `Symbol`, `IndexedProject`
       - `GitStyle`
       - `SandboxResult` (stdout, stderr, exit_code, duration_ms, language)
       - `Workflow`, `WorkflowRun`, `WorkflowStep`
       - `BrowserActionResult`, `BrowserAgentStep`, `BrowserSessionStatus`, `PageDescription`
       - `AutoReplyDraft`, `AutoReplyBatchItem`
       - `Document`, `DocSearchResult`, `DocAnswer`, `DocSynthesis`, `StudyNotes`
       - `ComputerUseTask`, `ComputerUseScreenshot`
       - `AutomationResult` (for auto_* commands — most return `()` → `void`)
       - `UiaElement`, `UiaSnapshot`
       - `Reminder`, `ReminderParsed`
       - `Watcher`, `WatcherState`
       - `CronJob`, `CronRun`
    4. **Wrapper sections** (one section per Rust module, matching lib.rs order). Each wrapper:
       - One camelCase function per `#[tauri::command]`
       - JSDoc `@see src-tauri/src/<file>.rs <command_name>` (line numbers optional but preferred where discoverable)
       - `invokeTyped<TReturn>('snake_case_command', args)` — raw invoke BANNED
       - camelCase → snake_case at the wrapper's invoke call

    Example section (native_tools — 3 commands):

    ```ts
    // ═══════════════════════════════════════════════════════════════════════
    // native_tools.rs — bash + code block + ask AI (3 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/native_tools.rs run_shell */
    export function runShell(args: { command: string; cwd?: string }): Promise<ShellResult> {
      return invokeTyped<ShellResult>('run_shell', {
        command: args.command,
        cwd: args.cwd,
      });
    }

    /** @see src-tauri/src/native_tools.rs run_code_block */
    export function runCodeBlock(args: { code: string; language: string }): Promise<CodeBlockResult> {
      return invokeTyped<CodeBlockResult>('run_code_block', {
        code: args.code,
        language: args.language,
      });
    }

    /** @see src-tauri/src/native_tools.rs ask_ai */
    export function askAi(args: { prompt: string }): Promise<string> {
      return invokeTyped<string>('ask_ai', {
        prompt: args.prompt,
      });
    }
    ```

    **Repeat for all 17 modules.** Section order matches lib.rs registration order (see D-167 inventory). For every Rust command, EMIT a wrapper. If a Rust signature has arguments, read them and convert camelCase → snake_case at the invoke boundary. Do NOT invent optional args — match Rust exactly.

    **Adjust wrapper signatures to MATCH the actual Rust signatures** — when reading the Rust source, preserve parameter names (converted snake_case → invoke) and return types. If any Rust fn signature differs from an example draft, the ACTUAL Rust signature wins. Add a note in JSDoc explaining any non-obvious correction.

    Then create `src/features/dev-tools/types.ts`:

    ```ts
    // src/features/dev-tools/types.ts
    // Cluster-local barrel — re-exports Tauri wrapper types + UI-only types.
    // @see .planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-188

    export type {
      ShellResult, CodeBlockResult,
      FileTreeNode, FileListEntry, FileReadResult, FileIndexEntry, FileIndexStats,
      ProjectSummary, Symbol, IndexedProject,
      GitStyle,
      SandboxResult,
      Workflow, WorkflowRun, WorkflowStep,
      BrowserActionResult, BrowserAgentStep, BrowserSessionStatus, PageDescription,
      AutoReplyDraft,
      Document, DocSearchResult, DocAnswer, DocSynthesis, StudyNotes,
      ComputerUseTask, ComputerUseScreenshot,
      UiaElement, UiaSnapshot,
      Reminder, ReminderParsed,
      Watcher,
      CronJob, CronRun,
    } from '@/lib/tauri/dev_tools';

    // Cluster-only UI types
    export type DevToolsTabKey = string;

    export interface TerminalLine {
      type: 'cmd' | 'stdout' | 'stderr';
      text: string;
    }
    ```

    **CRITICAL — CLAUDE.md rules:**
    - Raw `invoke()` import is BANNED in this file (ESLint no-raw-tauri). Every wrapper routes through `invokeTyped`.
    - Argument key conversion camelCase → snake_case happens AT EACH wrapper's invoke call.
    - Prefer `[k: string]: unknown` index signature on interfaces for forward-compat (D-38-payload accepted risk).
    - File stays in ONE FILE (no splitting) — makes Plan 07-03/07-04 imports deterministic.
    - Most `automation::*` commands return `()` in Rust (→ `Promise<void>` in TS). Don't invent return types.
  </action>
  <verify>
    <automated>grep -c "export function" src/lib/tauri/dev_tools.ts && grep -c "@see src-tauri" src/lib/tauri/dev_tools.ts && grep -c "invokeTyped" src/lib/tauri/dev_tools.ts && test -f src/features/dev-tools/types.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    dev_tools.ts has ≥85 export function declarations (allow ±10 for signature merge), ≥85 @see JSDoc lines, and ≥85 invokeTyped call sites. types.ts exists. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/tauri/admin.ts with ~110 typed wrappers + src/features/admin/types.ts barrel</name>
  <files>src/lib/tauri/admin.ts, src/features/admin/types.ts</files>
  <read_first>
    - /home/arnav/blade/src-tauri/src/commands.rs (grep '#\[tauri::command\]' — mcp_*, test_provider, debug_config, set_config, update_init_prefs, reset_onboarding)
    - /home/arnav/blade/src-tauri/src/permissions.rs (4 signatures)
    - /home/arnav/blade/src-tauri/src/db_commands.rs (analytics subset — 4 signatures: db_track_event, db_events_since, db_prune_analytics, db_analytics_summary)
    - /home/arnav/blade/src-tauri/src/reports.rs (5 signatures)
    - /home/arnav/blade/src-tauri/src/self_upgrade.rs (8 signatures)
    - /home/arnav/blade/src-tauri/src/evolution.rs (6 signatures)
    - /home/arnav/blade/src-tauri/src/immune_system.rs (1 signature)
    - /home/arnav/blade/src-tauri/src/decision_gate.rs (3 signatures)
    - /home/arnav/blade/src-tauri/src/authority_engine.rs (6 signatures)
    - /home/arnav/blade/src-tauri/src/audit.rs (1 signature)
    - /home/arnav/blade/src-tauri/src/security_monitor.rs (9 signatures)
    - /home/arnav/blade/src-tauri/src/symbolic.rs (4 signatures)
    - /home/arnav/blade/src-tauri/src/temporal_intel.rs (4 signatures — shared read with Phase 6 MeetingsView's temporalMeetingPrep)
    - /home/arnav/blade/src-tauri/src/execution_memory.rs (3 signatures)
    - /home/arnav/blade/src-tauri/src/deep_scan.rs (3 signatures)
    - /home/arnav/blade/src-tauri/src/supervisor.rs (2 signatures)
    - /home/arnav/blade/src-tauri/src/trace.rs (1 signature)
    - /home/arnav/blade/src-tauri/src/sysadmin.rs (8 signatures)
    - /home/arnav/blade/src-tauri/src/integration_bridge.rs (3 signatures)
    - /home/arnav/blade/src-tauri/src/config.rs (6 signatures: get_all_provider_keys, store_provider_key, switch_provider, get_task_routing, set_task_routing, save_config_field)
    - /home/arnav/blade/src-tauri/src/self_critique.rs (4 signatures)
    - /home/arnav/blade/src-tauri/src/tool_forge.rs (4 signatures)
    - /home/arnav/blade/src/lib/tauri/dev_tools.ts (just-created Task 1 — same recipe)
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §1
  </read_first>
  <action>
    Create `src/lib/tauri/admin.ts` (~900-1100 lines expected, single file). Structure mirrors Task 1: header comment + types section + wrapper sections per module.

    Representative types section:

    ```ts
    import { invokeTyped } from './_base';

    // ═══════════════════════════════════════════════════════════════════════
    // Types
    // ═══════════════════════════════════════════════════════════════════════

    export interface McpServerInfo {
      name: string;
      connected: boolean;
      tool_count: number;
      last_error?: string;
      [k: string]: unknown;
    }

    export interface McpTool {
      name: string;
      description?: string;
      input_schema?: Record<string, unknown>;
      [k: string]: unknown;
    }

    export interface McpServerHealth {
      name: string;
      connected: boolean;
      reconnect_attempts: number;
      [k: string]: unknown;
    }

    export interface ProviderTestResult {
      ok: boolean;
      latency_ms?: number;
      error?: string;
      [k: string]: unknown;
    }

    export interface ToolOverride {
      tool_name: string;
      trust: 'trusted' | 'ask' | 'blocked';
      [k: string]: unknown;
    }

    export interface AnalyticsSummary {
      total_events: number;
      by_type?: Record<string, number>;
      window_start?: number;
      window_end?: number;
      [k: string]: unknown;
    }

    export interface AnalyticsEvent {
      id: string;
      type: string;
      payload?: Record<string, unknown>;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface Report {
      id: string;
      summary: string;
      status: 'open' | 'investigating' | 'resolved';
      source?: string;
      created_at: number;
      [k: string]: unknown;
    }

    export interface UpgradeCatalogEntry {
      id: string;
      name: string;
      description?: string;
      version?: string;
      [k: string]: unknown;
    }

    export interface EvolutionSuggestion {
      id: string;
      title: string;
      description?: string;
      priority?: 'low' | 'normal' | 'high';
      [k: string]: unknown;
    }

    export interface DecisionLogEntry {
      id: string;
      decision_type: string;
      classifier?: string;
      confidence: number;
      outcome?: string;
      signal?: string;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface AuthorityAgent {
      name: string;
      role: string;
      permissions?: string[];
      [k: string]: unknown;
    }

    export interface AuthorityAuditEntry {
      id: string;
      agent: string;
      action: string;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface AuditLogEntry {
      id: string;
      category: string;
      message: string;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface SecurityOverview {
      status: 'ok' | 'warn' | 'critical';
      active_alerts: number;
      last_scan_at?: number;
      [k: string]: unknown;
    }

    export interface SecurityScanResult {
      findings: Array<{ severity: string; message: string; source?: string }>;
      [k: string]: unknown;
    }

    export interface SymbolicPolicy {
      id: string;
      name: string;
      rule: string;
      [k: string]: unknown;
    }

    export interface TemporalStandup {
      summary: string;
      highlights?: string[];
      blockers?: string[];
      [k: string]: unknown;
    }

    export interface TemporalPattern {
      pattern: string;
      confidence: number;
      occurrences: number;
      [k: string]: unknown;
    }

    export interface ExecutionMemoryEntry {
      id: string;
      tool?: string;
      input?: string;
      output?: string;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface DeepScanResult {
      category: string;
      findings: Array<{ key: string; value: unknown }>;
      [k: string]: unknown;
    }

    export interface SupervisorHealth {
      services: Array<{ name: string; status: 'running' | 'stopped' | 'error'; uptime_seconds?: number }>;
      [k: string]: unknown;
    }

    export interface SupervisorService {
      name: string;
      status: string;
      details?: Record<string, unknown>;
      [k: string]: unknown;
    }

    export interface TraceEntry {
      id: string;
      span: string;
      duration_ms?: number;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface SysadminCheckpoint {
      id: string;
      name: string;
      created_at: number;
      [k: string]: unknown;
    }

    export interface SysadminDryRun {
      would_change: boolean;
      diff?: string;
      [k: string]: unknown;
    }

    export interface IntegrationState {
      services: Array<{ name: string; enabled: boolean; last_poll_at?: number; error?: string }>;
      [k: string]: unknown;
    }

    export interface ProviderKey {
      provider: string;
      key_masked: string;
      [k: string]: unknown;
    }

    export interface TaskRouting {
      [task: string]: { provider: string; model: string };
    }

    export interface CritiqueEntry {
      id: string;
      subject: string;
      critique: string;
      timestamp: number;
      [k: string]: unknown;
    }

    export interface ForgeTool {
      id: string;
      name: string;
      description?: string;
      definition?: Record<string, unknown>;
      [k: string]: unknown;
    }
    ```

    Wrapper sections (one per module, matching lib.rs order):

    ```ts
    // ═══════════════════════════════════════════════════════════════════════
    // commands.rs — MCP management + admin helpers (14 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/commands.rs mcp_add_server */
    export function mcpAddServer(args: {
      name: string;
      command: string;
      mcpArgs?: string[];
      env?: Record<string, string>;
    }): Promise<void> {
      return invokeTyped<void>('mcp_add_server', {
        name: args.name,
        command: args.command,
        args: args.mcpArgs,
        env: args.env,
      });
    }

    /** @see src-tauri/src/commands.rs mcp_get_servers */
    export function mcpGetServers(): Promise<McpServerInfo[]> {
      return invokeTyped<McpServerInfo[]>('mcp_get_servers', {});
    }

    // ... (continue for all 14)

    // ═══════════════════════════════════════════════════════════════════════
    // permissions.rs — tool trust (4 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/permissions.rs get_tool_overrides */
    export function getToolOverrides(): Promise<ToolOverride[]> {
      return invokeTyped<ToolOverride[]>('get_tool_overrides', {});
    }

    // ... (continue)

    // ═══════════════════════════════════════════════════════════════════════
    // db_commands.rs — analytics subset (4 commands)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // reports.rs — capability_gap_detected surface (5 commands)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // self_upgrade.rs — upgrade catalog + pentest auth (8 commands)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // evolution.rs — capability learning (6 commands)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // immune_system.rs — 1 command
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // decision_gate.rs — 3 commands
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/decision_gate.rs get_decision_log */
    export function getDecisionLog(args: { limit?: number }): Promise<DecisionLogEntry[]> {
      return invokeTyped<DecisionLogEntry[]>('get_decision_log', { limit: args.limit });
    }

    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // authority_engine.rs — 6 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // audit.rs — 1 command
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // security_monitor.rs — 9 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // symbolic.rs — 4 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // temporal_intel.rs — 4 commands (shared read with Phase 6)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // execution_memory.rs — 3 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // deep_scan.rs — 3 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // supervisor.rs + trace.rs — 3 commands total
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // sysadmin.rs — 8 commands (dangerous ops, Dialog-gated in consumers)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // integration_bridge.rs — 3 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // config.rs — provider keys + task routing (6 commands)
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // self_critique.rs — 4 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // tool_forge.rs — 4 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...
    ```

    **Adjust signatures based on actual Rust reads.** Rust wins.

    Then create `src/features/admin/types.ts`:

    ```ts
    // src/features/admin/types.ts
    // Cluster-local barrel.

    export type {
      McpServerInfo, McpTool, McpServerHealth, ProviderTestResult, ToolOverride,
      AnalyticsSummary, AnalyticsEvent,
      Report,
      UpgradeCatalogEntry, EvolutionSuggestion,
      DecisionLogEntry, AuthorityAgent, AuthorityAuditEntry, AuditLogEntry,
      SecurityOverview, SecurityScanResult, SymbolicPolicy,
      TemporalStandup, TemporalPattern, ExecutionMemoryEntry,
      DeepScanResult, SupervisorHealth, SupervisorService, TraceEntry,
      SysadminCheckpoint, SysadminDryRun,
      IntegrationState, ProviderKey, TaskRouting,
      CritiqueEntry, ForgeTool,
    } from '@/lib/tauri/admin';

    export type AdminTabKey = string;

    export interface AdminKpi {
      label: string;
      value: string | number;
      trend?: 'up' | 'down' | 'flat';
    }
    ```

    **CRITICAL**:
    - Raw invoke BANNED.
    - camelCase JS API; snake_case wire args.
    - Every wrapper: one @see JSDoc line + one invokeTyped call.
    - File stays in ONE FILE.
    - `mcp_add_server` accepts `args: string[]` in Rust — to avoid TS `args` collision with the wrapper param object, rename the TS-side key to `mcpArgs` and convert at invoke boundary (see example above).
  </action>
  <verify>
    <automated>grep -c "export function" src/lib/tauri/admin.ts && grep -c "@see src-tauri" src/lib/tauri/admin.ts && grep -c "invokeTyped" src/lib/tauri/admin.ts && test -f src/features/admin/types.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    admin.ts has ≥100 export function declarations (allow ±10 for signature merge), ≥100 @see JSDoc lines, and ≥100 invokeTyped call sites. types.ts exists. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite feature index files + seed 21 per-route placeholder components + create cluster CSS + update tauri barrel</name>
  <files>src/features/dev-tools/index.tsx, src/features/admin/index.tsx, src/features/dev-tools/Terminal.tsx, src/features/dev-tools/FileBrowser.tsx, src/features/dev-tools/GitPanel.tsx, src/features/dev-tools/Canvas.tsx, src/features/dev-tools/WorkflowBuilder.tsx, src/features/dev-tools/WebAutomation.tsx, src/features/dev-tools/EmailAssistant.tsx, src/features/dev-tools/DocumentGenerator.tsx, src/features/dev-tools/CodeSandbox.tsx, src/features/dev-tools/ComputerUse.tsx, src/features/dev-tools/dev-tools.css, src/features/admin/Analytics.tsx, src/features/admin/CapabilityReports.tsx, src/features/admin/Reports.tsx, src/features/admin/DecisionLog.tsx, src/features/admin/SecurityDashboard.tsx, src/features/admin/Temporal.tsx, src/features/admin/Diagnostics.tsx, src/features/admin/IntegrationStatus.tsx, src/features/admin/McpSettings.tsx, src/features/admin/ModelComparison.tsx, src/features/admin/KeyVault.tsx, src/features/admin/admin.css, src/lib/tauri/index.ts</files>
  <read_first>
    - /home/arnav/blade/src/features/dev-tools/index.tsx (current 10-stub version — 29 lines)
    - /home/arnav/blade/src/features/admin/index.tsx (current 11-stub version — 31 lines)
    - /home/arnav/blade/src/features/life-os/index.tsx (Phase 6 Plan 06-02 output — recipe reference)
    - /home/arnav/blade/src/features/life-os/life-os.css (Phase 6 pattern for cluster CSS)
    - /home/arnav/blade/src/lib/router.ts (RouteDefinition interface)
    - /home/arnav/blade/src/design-system/primitives/index.ts (GlassPanel export)
    - /home/arnav/blade/src/lib/tauri/index.ts (barrel — see current exports)
    - /home/arnav/blade/.planning/phases/07-dev-tools-admin/07-PATTERNS.md §2, §9
  </read_first>
  <action>
    **Sub-task 3a — Rewrite src/features/dev-tools/index.tsx (per Pattern §2):**

    ```tsx
    // src/features/dev-tools/index.tsx — Phase 7 rewrite (Plan 07-02, D-170 single-writer)
    // Phase 1 ComingSoonSkeleton stubs replaced with lazy imports of real route components.
    // @see .planning/phases/07-dev-tools-admin/07-CONTEXT.md §D-170
    // @see .planning/REQUIREMENTS.md §DEV-01..10

    import { lazy } from 'react';
    import type { RouteDefinition } from '@/lib/router';

    const Terminal          = lazy(() => import('./Terminal').then((m) => ({ default: m.Terminal })));
    const FileBrowser       = lazy(() => import('./FileBrowser').then((m) => ({ default: m.FileBrowser })));
    const GitPanel          = lazy(() => import('./GitPanel').then((m) => ({ default: m.GitPanel })));
    const Canvas            = lazy(() => import('./Canvas').then((m) => ({ default: m.Canvas })));
    const WorkflowBuilder   = lazy(() => import('./WorkflowBuilder').then((m) => ({ default: m.WorkflowBuilder })));
    const WebAutomation     = lazy(() => import('./WebAutomation').then((m) => ({ default: m.WebAutomation })));
    const EmailAssistant    = lazy(() => import('./EmailAssistant').then((m) => ({ default: m.EmailAssistant })));
    const DocumentGenerator = lazy(() => import('./DocumentGenerator').then((m) => ({ default: m.DocumentGenerator })));
    const CodeSandbox       = lazy(() => import('./CodeSandbox').then((m) => ({ default: m.CodeSandbox })));
    const ComputerUse       = lazy(() => import('./ComputerUse').then((m) => ({ default: m.ComputerUse })));

    export const routes: RouteDefinition[] = [
      { id: 'terminal',           label: 'Terminal',        section: 'dev', component: Terminal,          phase: 7 },
      { id: 'file-browser',       label: 'File Browser',    section: 'dev', component: FileBrowser,       phase: 7 },
      { id: 'git-panel',          label: 'Git',             section: 'dev', component: GitPanel,          phase: 7 },
      { id: 'canvas',             label: 'Canvas',          section: 'dev', component: Canvas,            phase: 7 },
      { id: 'workflow-builder',   label: 'Workflows',       section: 'dev', component: WorkflowBuilder,   phase: 7 },
      { id: 'web-automation',     label: 'Web Automation',  section: 'dev', component: WebAutomation,     phase: 7 },
      { id: 'email-assistant',    label: 'Email Assistant', section: 'dev', component: EmailAssistant,    phase: 7 },
      { id: 'document-generator', label: 'Documents',       section: 'dev', component: DocumentGenerator, phase: 7 },
      { id: 'code-sandbox',       label: 'Sandbox',         section: 'dev', component: CodeSandbox,       phase: 7 },
      { id: 'computer-use',       label: 'Computer Use',    section: 'dev', component: ComputerUse,       phase: 7 },
    ];
    ```

    **Sub-task 3b — Rewrite src/features/admin/index.tsx** (same pattern, 11 routes including synthetic reports):

    ```tsx
    // src/features/admin/index.tsx — Phase 7 rewrite (Plan 07-02)
    // 10 ADMIN requirements + 1 synthetic `reports` route (P-03 capability_gap_detected target).
    import { lazy } from 'react';
    import type { RouteDefinition } from '@/lib/router';

    const Analytics         = lazy(() => import('./Analytics').then((m) => ({ default: m.Analytics })));
    const CapabilityReports = lazy(() => import('./CapabilityReports').then((m) => ({ default: m.CapabilityReports })));
    const Reports           = lazy(() => import('./Reports').then((m) => ({ default: m.Reports })));
    const DecisionLog       = lazy(() => import('./DecisionLog').then((m) => ({ default: m.DecisionLog })));
    const SecurityDashboard = lazy(() => import('./SecurityDashboard').then((m) => ({ default: m.SecurityDashboard })));
    const Temporal          = lazy(() => import('./Temporal').then((m) => ({ default: m.Temporal })));
    const Diagnostics       = lazy(() => import('./Diagnostics').then((m) => ({ default: m.Diagnostics })));
    const IntegrationStatus = lazy(() => import('./IntegrationStatus').then((m) => ({ default: m.IntegrationStatus })));
    const McpSettings       = lazy(() => import('./McpSettings').then((m) => ({ default: m.McpSettings })));
    const ModelComparison   = lazy(() => import('./ModelComparison').then((m) => ({ default: m.ModelComparison })));
    const KeyVault          = lazy(() => import('./KeyVault').then((m) => ({ default: m.KeyVault })));

    export const routes: RouteDefinition[] = [
      { id: 'analytics',          label: 'Analytics',          section: 'admin', component: Analytics,         phase: 7 },
      { id: 'capability-reports', label: 'Capability Reports', section: 'admin', component: CapabilityReports, phase: 7 },
      { id: 'reports',            label: 'Reports',            section: 'admin', component: Reports,           phase: 7, description: 'Backend openRoute target for capability_gap_detected' },
      { id: 'decision-log',       label: 'Decision Log',       section: 'admin', component: DecisionLog,       phase: 7 },
      { id: 'security-dashboard', label: 'Security',           section: 'admin', component: SecurityDashboard, phase: 7 },
      { id: 'temporal',           label: 'Temporal',           section: 'admin', component: Temporal,          phase: 7 },
      { id: 'diagnostics',        label: 'Diagnostics',        section: 'admin', component: Diagnostics,       phase: 7 },
      { id: 'integration-status', label: 'Integration Status', section: 'admin', component: IntegrationStatus, phase: 7 },
      { id: 'mcp-settings',       label: 'MCP Servers',        section: 'admin', component: McpSettings,       phase: 7 },
      { id: 'model-comparison',   label: 'Model Comparison',   section: 'admin', component: ModelComparison,   phase: 7 },
      { id: 'key-vault',          label: 'Key Vault',          section: 'admin', component: KeyVault,          phase: 7 },
    ];
    ```

    **Sub-task 3c — Create 21 per-route placeholder files** (10 dev-tools + 11 admin). Each file is a minimal named export that Plans 07-03..06 will fill in. Template per file (substitute NAME + LABEL + PLAN):

    ```tsx
    // src/features/dev-tools/Terminal.tsx — placeholder shipped by Plan 07-02
    // Real body ships in Plan 07-03.
    import { GlassPanel } from '@/design-system/primitives';
    import './dev-tools.css';

    export function Terminal() {
      return (
        <GlassPanel tier={1} className="dev-surface">
          <div className="dev-placeholder" data-testid="terminal-placeholder">
            <h2>Terminal</h2>
            <p className="dev-placeholder-hint">Ships in Plan 07-03.</p>
          </div>
        </GlassPanel>
      );
    }
    ```

    Create a placeholder for each route:
    - dev-tools (Plan 07-03 fills these 5): Terminal, FileBrowser, GitPanel, Canvas, WorkflowBuilder
    - dev-tools (Plan 07-04 fills these 5): WebAutomation, EmailAssistant, DocumentGenerator, CodeSandbox, ComputerUse
    - admin (Plan 07-05 fills these 5): Analytics, CapabilityReports, Reports, DecisionLog, SecurityDashboard
    - admin (Plan 07-06 fills these 6): Temporal, Diagnostics, IntegrationStatus, McpSettings, ModelComparison, KeyVault

    The placeholder hint must identify which plan ships the body (so Plans 07-03..06 can replace cleanly). For admin routes, substitute `./dev-tools.css` → `./admin.css` and `dev-surface` / `dev-placeholder` → `admin-surface` / `admin-placeholder`.

    **Sub-task 3d — Create src/features/dev-tools/dev-tools.css** (mirror Phase 6 life-os.css):

    ```css
    /* src/features/dev-tools/dev-tools.css — Plan 07-02 shared base
       Plans 07-03 and 07-04 EXTEND this file via scoped partials; never replace.
       @see .planning/phases/07-dev-tools-admin/07-PATTERNS.md §9 */

    @layer features {
      .dev-surface {
        padding: var(--sp-4);
        height: 100%;
        overflow-y: auto;
      }

      .dev-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--sp-2);
        padding: var(--sp-6);
        text-align: center;
      }

      .dev-placeholder-hint {
        color: var(--t-3);
        font-size: 14px;
      }

      .dev-card {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-3);
        display: flex;
        flex-direction: column;
        gap: var(--sp-2);
        transition: border-color 180ms var(--ease-out);
      }

      .dev-card:hover { border-color: rgba(255, 255, 255, 0.18); }

      .dev-card[data-status="running"]  { border-left: 3px solid var(--status-running); }
      .dev-card[data-status="complete"] { border-left: 3px solid var(--status-success); }
      .dev-card[data-status="failed"]   { border-left: 3px solid var(--status-error); }
      .dev-card[data-status="idle"],
      .dev-card:not([data-status])       { border-left: 3px solid var(--status-idle); }

      .dev-tab-row {
        display: flex;
        gap: var(--sp-1);
        padding: var(--sp-2) 0;
      }

      .dev-tab-pill {
        padding: var(--sp-1) var(--sp-2);
        border-radius: var(--radius-pill);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        cursor: pointer;
        font-size: 13px;
        transition: background 140ms var(--ease-out);
        color: var(--t-2);
      }

      .dev-tab-pill[data-active="true"] {
        background: rgba(255, 255, 255, 0.14);
        border-color: rgba(255, 255, 255, 0.22);
        color: var(--t-1);
      }

      /* Terminal scrollback */
      .terminal-topbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: var(--sp-2);
        border-bottom: 1px solid var(--line);
      }

      .terminal-cwd {
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--t-3);
      }

      .terminal-scrollback {
        font-family: var(--font-mono);
        font-size: 13px;
        background: rgba(0, 0, 0, 0.3);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-2);
        max-height: calc(100vh - 260px);
        overflow-y: auto;
        white-space: pre;
        margin: var(--sp-2) 0;
      }

      .terminal-line-cmd    { color: var(--t-1); font-weight: 600; }
      .terminal-line-stdout { color: var(--t-2); }
      .terminal-line-stderr { color: var(--status-error); }

      .terminal-input-row {
        display: flex;
        gap: var(--sp-1);
        align-items: center;
        padding: var(--sp-2);
        background: rgba(0, 0, 0, 0.2);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
      }

      .terminal-prompt {
        font-family: var(--font-mono);
        color: var(--t-1);
        font-weight: 600;
      }

      .terminal-input {
        flex: 1;
        background: transparent;
        border: none;
        color: var(--t-1);
        font-family: var(--font-mono);
        font-size: 13px;
        outline: none;
      }

      /* File tree */
      .file-tree { padding: var(--sp-1); }
      .file-tree-row { font-family: var(--font-mono); font-size: 12px; padding: 2px 0; }
      .file-tree-toggle, .file-tree-file {
        background: none;
        border: none;
        color: var(--t-1);
        cursor: pointer;
        text-align: left;
        padding: 2px 4px;
        border-radius: 3px;
      }
      .file-tree-toggle:hover, .file-tree-file:hover {
        background: rgba(255, 255, 255, 0.06);
      }
    }
    ```

    **Sub-task 3e — Create src/features/admin/admin.css** (parallel structure with danger-zone additions per D-183):

    ```css
    /* src/features/admin/admin.css — Plan 07-02 shared base */

    @layer features {
      .admin-surface {
        padding: var(--sp-4);
        height: 100%;
        overflow-y: auto;
      }

      .admin-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--sp-2);
        padding: var(--sp-6);
        text-align: center;
      }

      .admin-placeholder-hint { color: var(--t-3); font-size: 14px; }

      .admin-card {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-3);
      }

      .admin-card[data-status="running"]  { border-left: 3px solid var(--status-running); }
      .admin-card[data-status="complete"] { border-left: 3px solid var(--status-success); }
      .admin-card[data-status="failed"]   { border-left: 3px solid var(--status-error); }
      .admin-card[data-status="idle"],
      .admin-card:not([data-status])       { border-left: 3px solid var(--status-idle); }

      .admin-card-title { font-weight: 600; font-size: 14px; color: var(--t-1); }
      .admin-card-meta { color: var(--t-2); font-size: 12px; }
      .admin-card-secondary { color: var(--t-3); font-size: 11px; }

      .admin-health-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: var(--sp-2);
      }

      .admin-kpi-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: var(--sp-2);
      }

      .admin-tabs {
        display: flex;
        gap: var(--sp-1);
        padding: var(--sp-2) 0;
        border-bottom: 1px solid var(--line);
        margin-bottom: var(--sp-3);
      }

      .admin-tab-pill {
        padding: var(--sp-1) var(--sp-2);
        border-radius: var(--radius-pill);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        cursor: pointer;
        font-size: 13px;
        transition: background 140ms var(--ease-out);
        color: var(--t-2);
      }

      .admin-tab-pill[data-active="true"] {
        background: rgba(255, 255, 255, 0.14);
        border-color: rgba(255, 255, 255, 0.22);
        color: var(--t-1);
      }

      /* Danger zone (pentest, sysadmin dangerous ops) — D-183 */
      .danger-banner {
        background: rgba(239, 68, 68, 0.08);
        border: 1px solid var(--status-error);
        border-radius: var(--radius-card);
        padding: var(--sp-2);
        color: var(--status-error);
        font-weight: 600;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        font-size: 12px;
        margin-bottom: var(--sp-2);
      }
    }
    ```

    **Sub-task 3f — Update src/lib/tauri/index.ts barrel** to re-export the new modules:

    ```ts
    // Append to existing barrel (follow the file's convention — individual re-exports OR namespace wildcards)
    // If Phase 5/6 used `export * as <cluster> from './<cluster>';` style, mirror:
    export * as devTools from './dev_tools';
    export * as admin    from './admin';

    // If Phase 5/6 used individual re-exports (`export { agentList, ... } from './agents';`),
    // mirror by re-exporting from dev_tools.ts + admin.ts explicitly instead.
    ```

    (Inspect the existing barrel FIRST; match its convention exactly. Do NOT change the existing re-export style.)

    **Sub-task 3g — tokens.css check.** Phase 5 Plan 05-02 introduced `--status-idle/running/success/error` tokens. Phase 7 REUSES — do NOT re-declare. `grep -q "status-running" src/styles/tokens.css` should pass. If missing (shouldn't happen post-Phase-5), that's a regression; flag in SUMMARY.

    **CRITICAL — CLAUDE.md + Phase discipline:**
    - D-170: This plan is the ONLY plan that edits `src/features/dev-tools/index.tsx` and `src/features/admin/index.tsx`. Downstream plans replace the BODY of the 21 per-route files — they do NOT touch the 2 index.tsx files.
    - D-190: Per-route file names match the index.tsx imports exactly. Case-sensitive.
    - D-191: Cluster CSS files are created ONCE here; Plans 07-03..06 append their own rules using scoped partial CSS files (`dev-tools-rich-a.css`, etc. — per Phase 5/6 plan-03/04/05/06 pattern to prevent parallel-edit conflicts).
    - All primitives imported from `@/design-system/primitives` (barrel) — not individual files.
  </action>
  <verify>
    <automated>test -f src/features/dev-tools/Terminal.tsx && test -f src/features/dev-tools/FileBrowser.tsx && test -f src/features/dev-tools/GitPanel.tsx && test -f src/features/dev-tools/Canvas.tsx && test -f src/features/dev-tools/WorkflowBuilder.tsx && test -f src/features/dev-tools/WebAutomation.tsx && test -f src/features/dev-tools/EmailAssistant.tsx && test -f src/features/dev-tools/DocumentGenerator.tsx && test -f src/features/dev-tools/CodeSandbox.tsx && test -f src/features/dev-tools/ComputerUse.tsx && test -f src/features/dev-tools/dev-tools.css && test -f src/features/admin/Analytics.tsx && test -f src/features/admin/CapabilityReports.tsx && test -f src/features/admin/Reports.tsx && test -f src/features/admin/DecisionLog.tsx && test -f src/features/admin/SecurityDashboard.tsx && test -f src/features/admin/Temporal.tsx && test -f src/features/admin/Diagnostics.tsx && test -f src/features/admin/IntegrationStatus.tsx && test -f src/features/admin/McpSettings.tsx && test -f src/features/admin/ModelComparison.tsx && test -f src/features/admin/KeyVault.tsx && test -f src/features/admin/admin.css && grep -q "status-running" src/styles/tokens.css && npx tsc --noEmit</automated>
  </verify>
  <done>
    All 21 per-route placeholder files exist; both cluster index.tsx rewritten with lazy imports; both CSS files created; status tokens confirmed present; barrel updated; `npx tsc --noEmit` passes; `npm run verify:all` passes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes.
- `npm run verify:all` passes — migration-ledger still consistent (routes unchanged), entries check still passes, no-raw-tauri rule still green (every wrapper uses invokeTyped).
- ESLint passes (no-raw-tauri rule enforced).
- `npm run dev` builds the frontend — lazy imports resolve at first load.
- Route count unchanged in migration-ledger (10 dev-tools + 11 admin = 21 Phase 7 routes).
</verification>

<success_criteria>
- `src/lib/tauri/dev_tools.ts` exports ~90 camelCase wrapper functions, each with JSDoc @see citation.
- `src/lib/tauri/admin.ts` exports ~110 camelCase wrapper functions, each with JSDoc @see citation.
- Both cluster feature `index.tsx` files rewritten to lazy-import 10 + 11 real components.
- 21 per-route placeholder files exist and render a glass panel + placeholder hint naming the follow-up plan.
- Two cluster CSS files exist with shared base classes + Terminal-specific + danger-banner styling.
- Types barrel files exist for both clusters.
- Tauri library barrel re-exports both new wrapper modules.
- Zero Rust file edits.
- Zero behavior regressions in existing Phase 1..6 tests.
</success_criteria>

<threat_model>
## Trust Boundaries

- **JS → Rust invoke boundary**: every wrapper crosses this. Args flow JS → serde → Rust param. Untrusted input originates in user-facing components (Plans 07-03..06).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | camelCase → snake_case arg mis-conversion | mitigate | Per-wrapper explicit mapping (not auto-derived). Plan 07-07 Playwright specs test real invoke paths. |
| T-07-02-02 | Information Disclosure | raw invoke bypasses wrappers | mitigate | ESLint `no-raw-tauri` rule enforces invokeTyped-only outside `src/lib/tauri/`. `scripts/verify-no-raw-tauri.sh` CI backstop. |
| T-07-02-03 | Denial of Service | wrapper throws uncaught rejection under load | accept | TauriError class propagates; consumer surfaces to ToastContext. |
| T-07-02-04 | Tampering | route placeholder left shipped if Plans 07-03..06 never run | mitigate | Plan 07-07 Playwright cluster-routes spec asserts real components mount (placeholder hint absent). |
| T-07-02-05 | Elevation of Privilege | `sysadmin_sudo_exec` / `pentest_*` / `kali_*` / `integration_toggle` / `mcp_remove_server` commands fire without user consent | mitigate | Consumer components (Plans 07-05/07-06) use Dialog confirm + ALL-CAPS warning before invoking destructive/lifecycle/pentest commands (D-183 pattern). |
| T-07-02-06 | Information Disclosure | `get_all_provider_keys` returns raw keys | mitigate | KeyVault consumer (Plan 07-06) masks keys to last-4 only per D-185. |
</threat_model>

<output>
After completion, create `.planning/phases/07-dev-tools-admin/07-02-SUMMARY.md` with:
- Exact count of exported wrappers per cluster (dev_tools.ts count + admin.ts count).
- Which Rust module each wrapper group maps to (table format).
- List of the 21 per-route files created (confirming placeholder content).
- Any Rust signature corrections made while reading the source (so Plans 07-03..06 match reality).
- Barrel export update confirmed; status tokens verified present; CSS files created.
- Confirmation that `src/features/dev-tools/index.tsx` and `src/features/admin/index.tsx` were the only index files touched (D-170 single-writer invariant held).
</output>
