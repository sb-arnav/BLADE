---
phase: 09-polish
plan: 03
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - src/styles/motion.css
  - src/design-system/primitives/Dialog.tsx
  - src/design-system/shell/TitleBar.tsx
  - src/design-system/shell/NavRail.tsx
  - src/design-system/shell/CommandPalette.tsx
  - src/features/hud/HudMenu.tsx
  - src/features/hud/HudWindow.tsx
  - src/features/chat/InputBar.tsx
  - src/features/chat/MessageBubble.tsx
  - src/features/settings/SettingsShell.tsx
autonomous: true
requirements:
  - POL-06  # A11y — ARIA labels on icon-only buttons
  - POL-07  # A11y — reduced-motion preference honoured
  - POL-08  # A11y — keyboard navigation + focus management
must_haves:
  truths:
    - "src/styles/motion.css has a @media (prefers-reduced-motion: reduce) block that sets --dur-snap / --dur-fast / --dur-base / --dur-enter / --dur-slow / --dur-float to 0.01ms and disables @keyframes spin"
    - "src/design-system/primitives/Dialog.tsx uses native <dialog> element which provides default focus trap + focus return; if an audit finds a gap (e.g. Dialog does not use native element), the file is updated to restore focus to document.activeElement before open on close"
    - "Every icon-only button in the 10 files listed in files_modified has an aria-label matching the button's semantic purpose (Close, Refresh, Menu, etc)"
    - "Manual audit notes: the scope is shell/* + hud/* + chat InputBar + chat MessageBubble + settings SettingsShell — which are the surfaces with known icon-only affordances (TitleBar window buttons, NavRail icon-only nav, CommandPalette close, HUD tray menu items, Chat send/toolbar buttons, SettingsShell tab buttons)"
    - "tsc --noEmit exits 0 after edits"
    - "verify:all exits 0 — no regression"
    - "No files in Plan 09-04's scope are touched (disjoint wave-2 ownership per D-229): no src/styles/motion-entrance.css, no ListSkeleton, no body/hive/dev-tools/admin feature files"
  artifacts:
    - path: "src/styles/motion.css"
      provides: "Reduced-motion media query override (appended, ~12 LOC)"
      contains: "prefers-reduced-motion"
    - path: "src/design-system/primitives/Dialog.tsx"
      provides: "Focus return on close (verified or added)"
      contains: "aria-modal|showModal|close"
  key_links:
    - from: "src/styles/motion.css"
      to: ":root custom properties"
      via: "media query override"
      pattern: "prefers-reduced-motion"
    - from: "aria-label attributes"
      to: "icon-only buttons"
      via: "added aria-label per button"
      pattern: "aria-label"
---

<objective>
A11y sweep: make icon-only buttons accessible (aria-label), verify Dialog focus trap + focus return, honour prefers-reduced-motion system preference, and keep keyboard navigation matrix functional (Tab, Escape, Enter).

Purpose: ROADMAP Phase 9 POL-06 / POL-07 / POL-08 and SC-5 (WCAG AA + kbd access). No new primitives (ErrorBoundary + EmptyState come from 09-02). Existing Dialog primitive uses native `<dialog>` which provides focus trap by default — this plan verifies that contract, augments where it breaks, and establishes the reduced-motion override.

Output: 1 motion.css append, 1 Dialog.tsx audit edit (touch only if gap found), ~10 TSX edits to add aria-label attributes to icon-only buttons across shell + hud + chat + settings. Plan completes under ~35% context; majority is mechanical aria-label additions per audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-PATTERNS.md
@CLAUDE.md
@src/styles/motion.css
@src/design-system/primitives/Dialog.tsx
@src/design-system/shell/TitleBar.tsx
@src/design-system/shell/NavRail.tsx
@src/design-system/shell/CommandPalette.tsx

<interfaces>
Current motion.css (to append to) — src/styles/motion.css:

```css
:root {
  --ease-spring: cubic-bezier(0.22, 1, 0.36, 1);
  --ease-out:    cubic-bezier(0.16, 1, 0.3, 1);
  --ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
  --dur-snap:   80ms;
  --dur-fast:  150ms;
  --dur-base:  200ms;
  --dur-enter: 280ms;
  --dur-slow:  400ms;
  --dur-float: 6200ms;
  --orb-rms-alpha: 0.55;
  --orb-throttle:  83;
}
@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
```

Target append (see 09-PATTERNS §4):

```css
@media (prefers-reduced-motion: reduce) {
  :root {
    --dur-snap:  0.01ms;
    --dur-fast:  0.01ms;
    --dur-base:  0.01ms;
    --dur-enter: 0.01ms;
    --dur-slow:  0.01ms;
    --dur-float: 0.01ms;
  }
  @keyframes spin { from { transform: none; } to { transform: none; } }
}
```

Dialog primitive — `src/design-system/primitives/Dialog.tsx` uses native `<dialog>` per Phase 2 D-58. Native `<dialog>.showModal()` provides focus trap by default. Focus return on close: browsers restore focus to the invoking element by default when `<dialog>` closes via `close()` or Escape. Audit expected outcome: no edit required. If audit finds `<dialog>` is NOT used (e.g. a CSS div-modal fallback), add manual focus restore via `previouslyFocused.current?.focus()` in useEffect cleanup.

Icon-only button audit scope (inspected at CONTEXT):

- src/design-system/shell/TitleBar.tsx — window buttons (minimize/maximize/close) typically icon-only
- src/design-system/shell/NavRail.tsx — icon-only cluster nav items (each cluster is rendered with just its icon by default)
- src/design-system/shell/CommandPalette.tsx — close button (×) or clear button
- src/features/hud/HudWindow.tsx — HUD tray buttons
- src/features/hud/HudMenu.tsx — context-menu items
- src/features/chat/InputBar.tsx — send / attachment / voice buttons
- src/features/chat/MessageBubble.tsx — copy / thumbs-up / thumbs-down / branch icon buttons
- src/features/settings/SettingsShell.tsx — tab close / tab buttons if icon-only

ARIA label add pattern (see 09-PATTERNS §6):

```tsx
// Before:
<button onClick={close}>×</button>
<button onClick={refresh}><RefreshIcon /></button>

// After:
<button onClick={close} aria-label="Close">×</button>
<button onClick={refresh} aria-label="Refresh"><RefreshIcon /></button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append reduced-motion override to motion.css</name>
  <files>src/styles/motion.css</files>
  <read_first>
    - /home/arnav/blade/src/styles/motion.css (full)
    - /home/arnav/blade/.planning/phases/09-polish/09-PATTERNS.md §4 (full target block)
  </read_first>
  <action>
    Append (at end of file, after the existing @keyframes spin block) the reduced-motion block from 09-PATTERNS §4 verbatim:
    
    ```css
    @media (prefers-reduced-motion: reduce) {
      :root {
        --dur-snap:  0.01ms;
        --dur-fast:  0.01ms;
        --dur-base:  0.01ms;
        --dur-enter: 0.01ms;
        --dur-slow:  0.01ms;
        --dur-float: 0.01ms;
      }
      @keyframes spin { from { transform: none; } to { transform: none; } }
    }
    ```
    
    Do NOT touch the existing :root or @keyframes spin blocks — the media query OVERRIDES them when reduced motion is set.
    
    Do NOT touch Plan 09-04's owned file `src/styles/motion-entrance.css` (that file doesn't exist yet; 09-04 creates it).
    
    Verify: the file grew by ~12 lines. No existing tokens altered.
  </action>
  <verify>
    <automated>grep -q "prefers-reduced-motion" src/styles/motion.css && grep -q "0.01ms" src/styles/motion.css</automated>
  </verify>
  <done>motion.css contains reduced-motion override block; original tokens unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Audit Dialog focus trap + focus return; edit only if gap found</name>
  <files>src/design-system/primitives/Dialog.tsx</files>
  <read_first>
    - /home/arnav/blade/src/design-system/primitives/Dialog.tsx (full)
    - /home/arnav/blade/.planning/phases/02-onboarding-shell/02-CONTEXT.md §D-58 (native dialog decision rationale)
  </read_first>
  <action>
    Read Dialog.tsx. Determine:
    
    1. Does it use `<dialog>` element with `ref.current.showModal()` / `ref.current.close()`? If YES → native focus trap + focus return are automatic. No edit required.
    
    2. If NO (a custom div-modal), add manual focus trap + restore:
       - On open: `previouslyFocused.current = document.activeElement as HTMLElement | null;` and focus first focusable child.
       - On Tab inside modal, loop focus between first and last focusable children.
       - On close: `previouslyFocused.current?.focus();`.
    
    3. In EITHER case, verify the Dialog renders `aria-modal="true"` (native dialog sets this; custom modal must add explicitly) + `role="dialog"` if NOT using native element.
    
    4. Verify the Dialog renders Esc-to-close behavior. Native `<dialog>` handles this; custom modals need a keydown listener.
    
    If audit finds Dialog is clean, leave the file untouched. Document in the plan SUMMARY: "Dialog audit passed — native <dialog> provides focus trap + focus return + aria-modal + Esc close via browser."
    
    If audit finds gaps, edit the file to close them. Keep changes minimal; do not refactor.
  </action>
  <verify>
    <automated>test -f src/design-system/primitives/Dialog.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Dialog primitive audit complete; if edits made, focus trap + focus return + aria-modal verified. If no edits, note in SUMMARY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ARIA label sweep — add aria-label to icon-only buttons across shell + hud + chat + settings</name>
  <files>src/design-system/shell/TitleBar.tsx, src/design-system/shell/NavRail.tsx, src/design-system/shell/CommandPalette.tsx, src/features/hud/HudMenu.tsx, src/features/hud/HudWindow.tsx, src/features/chat/InputBar.tsx, src/features/chat/MessageBubble.tsx, src/features/settings/SettingsShell.tsx</files>
  <read_first>
    - Each file in the files_modified list — read to identify icon-only buttons (buttons whose accessible name would come from a single glyph/svg/emoji with no visible text).
    - /home/arnav/blade/.planning/phases/09-polish/09-PATTERNS.md §6 (ARIA icon-button audit pattern)
  </read_first>
  <action>
    For each of the 8 files:
    
    1. Grep the file for `<button`, `<Button`, or interactive `<a href` / `<div role="button"`. Count buttons.
    2. For each button, check:
       - Does it have visible text content (e.g. "Save", "Close", "Settings")? If YES → already accessible via text, skip.
       - Does it have an existing `aria-label`? If YES → skip.
       - Does its only child render a single glyph, emoji, or svg? If YES → THIS IS ICON-ONLY, add aria-label.
    3. Determine the appropriate aria-label from context:
       - TitleBar.tsx: window controls → `aria-label="Minimize window"`, `aria-label="Maximize window"`, `aria-label="Close window"`
       - NavRail.tsx: cluster nav items → `aria-label="{sectionName}"` (e.g. "Agents", "Body") — if the nav item has a visible text label in a sibling span AND it's inside a group that's visually only-icon in collapsed mode, add aria-label for collapsed mode only (if a toggle exists) or leave alone if always-text.
       - CommandPalette.tsx: close X button → `aria-label="Close palette"`; clear input × → `aria-label="Clear search"`
       - HudMenu.tsx / HudWindow.tsx: menu items → `aria-label="{item name}"` (use the menu entry's intent); tray window buttons → `aria-label="Open main window"` etc.
       - InputBar.tsx: send button (if arrow-only) → `aria-label="Send message"`; attachment button → `aria-label="Attach file"`; voice button → `aria-label="Voice input"`
       - MessageBubble.tsx: copy → `aria-label="Copy message"`; thumbs-up → `aria-label="Good response"`; thumbs-down → `aria-label="Poor response"`; branch → `aria-label="Branch conversation"`
       - SettingsShell.tsx: tab close × → `aria-label="Close settings tab"`; gear/settings icon → `aria-label="Settings"`
    4. Add the aria-label attribute. Do NOT change the visible rendering. Do NOT change the handler logic.
    5. If a button is part of a group (e.g. NavRail cluster icons) AND the component renders a visible label alongside, aria-label is redundant — skip per WCAG best practice (don't double-label).
    
    Verify: `npx tsc --noEmit`. Grep each file after edit to confirm no button with icon-only content is missing aria-label. A reasonable heuristic: `grep -E '<button[^>]*>\s*[×←→✕⋯]\s*</button>' src/...` should produce 0 matches (all such buttons now have aria-label).
    
    Do NOT touch files outside the 8 listed. body + hive + dev-tools + admin clusters are Plan 09-04's territory.
  </action>
  <verify>
    <automated>npx tsc --noEmit && for f in src/design-system/shell/TitleBar.tsx src/design-system/shell/NavRail.tsx src/design-system/shell/CommandPalette.tsx src/features/hud/HudMenu.tsx src/features/hud/HudWindow.tsx src/features/chat/InputBar.tsx src/features/chat/MessageBubble.tsx src/features/settings/SettingsShell.tsx; do test -f "$f" || { echo "missing $f"; exit 1; }; done</automated>
  </verify>
  <done>All 8 files audited; icon-only buttons now carry aria-label; tsc clean; no regression in verify:all.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify keyboard navigation matrix still works (manual audit + grep)</name>
  <files>src/windows/main/useGlobalShortcuts.ts</files>
  <read_first>
    - /home/arnav/blade/src/windows/main/useGlobalShortcuts.ts (full — verify Mod+K / Mod+1 / Mod+/ / Mod+, / Mod+[ / Mod+] all present)
    - /home/arnav/blade/src/design-system/shell/CommandPalette.tsx (verify Escape closes)
    - /home/arnav/blade/src/design-system/shell/NavRail.tsx (verify Tab reaches NavRail + Enter activates)
  </read_first>
  <action>
    This task is an AUDIT, not an edit. Expected outcome: no changes needed (Phase 2 Plan 02-06 + Plan 02-07 already shipped keyboard nav). If audit finds regressions, EDIT useGlobalShortcuts.ts to restore; document in SUMMARY.
    
    Audit checklist:
    
    1. Grep useGlobalShortcuts.ts for shortcut definitions. Confirm:
       - Mod+K opens palette (line ~60)
       - Mod+1 → dashboard
       - Mod+/ → chat
       - Mod+, → settings
       - Mod+[ / Mod+] → back / forward
       - Route-level shortcut registration loop (line ~99-105)
    
    2. Verify Mod+? is NOT yet registered (Plan 09-05 adds it). Do NOT add it here — that's 09-05's responsibility.
    
    3. Verify Escape is handled inside Dialog / CommandPalette (Dialog: native <dialog> handles; CommandPalette: check the open state is toggled on onClose prop).
    
    4. Verify isEditableTarget guard (line ~19) is intact.
    
    If all checks pass, this task is a no-op. Document in SUMMARY: "Keyboard nav audit passed — Phase 2 substrate intact."
    
    If any shortcut is missing, restore per Phase 2 02-06-PLAN.md specification.
  </action>
  <verify>
    <automated>grep -q "Mod+K" src/windows/main/useGlobalShortcuts.ts || grep -q "'k'" src/windows/main/useGlobalShortcuts.ts</automated>
  </verify>
  <done>Keyboard nav audit complete; any regressions restored; SUMMARY notes audit outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS accessibility preference → CSS | prefers-reduced-motion media query consumed by user-style-injected CSS |
| Icon-only button → screen reader | aria-label text is consumed by assistive tech |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information disclosure | aria-label content | accept | aria-label strings are UI labels, not secrets. No sensitive content. |
| T-09-03-02 | Tampering | prefers-reduced-motion overriding app-critical animation | accept | App design explicitly honours reduced motion. No animation carries critical information; every animation is decorative or feedback. |
| T-09-03-03 | Denial of service | Focus trap in Dialog creating infinite tab cycle | mitigate | Native <dialog> trap is browser-managed. Custom trap (if added in Task 2) must limit tab cycling to focusable elements within the dialog; keyboard escape always available via Esc + close button. |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm run verify:all exits 0
- motion.css contains prefers-reduced-motion block
- 8 TSX files in Task 3 scope no longer have icon-only buttons without aria-label (spot-check)
- Dialog audit complete (edit or no-op logged)
- Keyboard nav audit complete (edit or no-op logged)
- Plan 09-04's owned files NOT touched (motion-entrance.css, ListSkeleton.tsx, body + hive + dev-tools + admin features)
</verification>

<success_criteria>
- POL-06 falsified: icon-only buttons in shell + hud + chat + settings carry aria-label
- POL-07 falsified: prefers-reduced-motion media query in motion.css zeroes all durations + disables spin keyframe
- POL-08 falsified: Dialog focus trap + focus return verified or augmented; keyboard nav matrix audited
- Plan 09-04's file ownership respected (no overlap)
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-03-SUMMARY.md`.
</output>
