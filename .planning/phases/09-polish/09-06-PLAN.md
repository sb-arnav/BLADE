---
phase: 09-polish
plan: 06
type: execute
wave: 4
depends_on: ['09-05']
files_modified:
  - tests/e2e/a11y-sweep.spec.ts
  - tests/e2e/error-boundary-recovery.spec.ts
  - scripts/verify-aria-icon-buttons.mjs
  - scripts/verify-motion-tokens.sh
  - scripts/verify-tokens-consistency.mjs
  - scripts/verify-empty-state-coverage.sh
  - package.json
  - CHANGELOG.md
  - src/design-system/primitives/index.ts
autonomous: false
requirements:
  - POL-01  # Motion audit verify script regression guard
  - POL-02  # Empty-state coverage verify script regression guard
  - POL-03  # Error-boundary recovery spec (SC-3 falsifier)
  - POL-04  # Shortcut help spec (SC-4 falsifier)
  - POL-06  # ARIA icon-button verify script
  - POL-07  # Reduced-motion spec
  - POL-10  # Tokens-consistency verify script + CHANGELOG for V1 release
must_haves:
  truths:
    - "src/design-system/primitives/index.ts re-exports ListSkeleton alongside ErrorBoundary + EmptyState (barrel cleanup deferred from Plan 09-04 per D-229)"
    - "tests/e2e/a11y-sweep.spec.ts has 2+ test cases: reduced-motion zeroes --dur-enter (SC-5 partial) + ⌘? opens shortcut-help-grid (SC-4 direct falsifier)"
    - "tests/e2e/error-boundary-recovery.spec.ts simulates a route crash via __TAURI_INVOKE_HOOK__ throwing + asserts role=alert with Route error text visible (SC-3 direct falsifier)"
    - "scripts/verify-aria-icon-buttons.mjs walks src/ + flags icon-only <button> without aria-label; exits 1 on violations"
    - "scripts/verify-motion-tokens.sh greps src/ for rogue transition: linear; exits 1 on violations"
    - "scripts/verify-tokens-consistency.mjs walks src/ + flags raw padding/margin/gap/font-size px literals outside primitive allow-list"
    - "scripts/verify-empty-state-coverage.sh asserts each file in the D-217 coverage table imports EmptyState; exits 1 on violations"
    - "package.json scripts adds 4 new verify:* aliases + extends verify:all to include them"
    - "CHANGELOG.md created following Keep a Changelog format; documents V1 completion by phase (Added/Changed/Fixed/Deferred); version stays 0.7.9 (operator bumps post-phase per D-227)"
    - "verify:all green after extension"
    - "Operator Mac-smoke checkpoint M-41..M-46 documented with explicit verification steps (non-autonomous task — user approves Phase 9 complete before release)"
  artifacts:
    - path: "tests/e2e/a11y-sweep.spec.ts"
      provides: "Reduced-motion + ⌘? specs (SC-4 + SC-5 falsifiers)"
    - path: "tests/e2e/error-boundary-recovery.spec.ts"
      provides: "Crash-route recovery spec (SC-3 falsifier)"
    - path: "scripts/verify-aria-icon-buttons.mjs"
      provides: "A11y regression guard for icon-only buttons"
    - path: "scripts/verify-motion-tokens.sh"
      provides: "Motion audit regression guard"
    - path: "scripts/verify-tokens-consistency.mjs"
      provides: "Cross-route px-literal regression guard"
    - path: "scripts/verify-empty-state-coverage.sh"
      provides: "D-217 coverage regression guard"
    - path: "CHANGELOG.md"
      provides: "V1 release notes — all 9 phases summarized"
      contains: "## [Unreleased]"
  key_links:
    - from: "scripts/verify-*.{mjs,sh}"
      to: "package.json scripts.verify:all"
      via: "composed pipeline"
      pattern: "verify:aria-icon-buttons|verify:motion-tokens|verify:tokens-consistency|verify:empty-state-coverage"
    - from: "tests/e2e/a11y-sweep.spec.ts"
      to: "SC-4 + SC-5 falsification"
      via: "Playwright + page.emulateMedia + keyboard"
      pattern: "prefers-reduced-motion|Meta\\+Shift"
    - from: "tests/e2e/error-boundary-recovery.spec.ts"
      to: "SC-3 falsification"
      via: "Playwright + __TAURI_INVOKE_HOOK__ throw"
      pattern: "Route error|role=\"alert\""
---

<objective>
Close Phase 9 (the FINAL phase) with 2 Playwright specs (a11y + error-boundary recovery), 4 new verify scripts (defensive regression guards for each horizontal concern), verify:all extension, CHANGELOG.md documenting V1 completion, and the non-autonomous Mac-session operator checkpoint (M-41..M-46) that gates the "ready to ship" declaration.

Purpose: ROADMAP Phase 9 SC-1 through SC-5 become falsifiable via automated specs + defensive verify scripts catch regression in Phase 9 polish during any future maintenance. CHANGELOG.md is the human-readable V1 release artifact. Mac-smoke M-41..M-46 closes the loop; operator sign-off is the final gate before Phase 9 can be marked complete + the operator (Arnav) decides on 1.0.0 version bump.

Output: 2 Playwright specs + 4 verify scripts + 1 package.json edit (4 new script entries + verify:all extension) + 1 CHANGELOG.md + 1 operator checkpoint task (non-autonomous). ~600 net new lines — bulk is test + verify scripts (mechanical) + CHANGELOG prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-PATTERNS.md
@.planning/phases/09-polish/09-01-PLAN.md
@.planning/phases/09-polish/09-02-PLAN.md
@.planning/phases/09-polish/09-03-PLAN.md
@.planning/phases/09-polish/09-04-PLAN.md
@.planning/phases/09-polish/09-05-PLAN.md
@CLAUDE.md
@package.json
@scripts

<interfaces>
a11y-sweep.spec.ts pattern (see 09-PATTERNS §10):

```ts
import { test, expect } from '@playwright/test';

test('reduced-motion zeroes durations', async ({ page }) => {
  await page.emulateMedia({ reducedMotion: 'reduce' });
  await page.goto('/');
  const dur = await page.evaluate(() =>
    getComputedStyle(document.documentElement).getPropertyValue('--dur-enter').trim()
  );
  expect(dur).toBe('0.01ms');
});

test('⌘? opens shortcut help panel (SC-4)', async ({ page }) => {
  await page.goto('/');
  await page.keyboard.press('Meta+Shift+/');
  await expect(page.getByTestId('shortcut-help-grid')).toBeVisible();
  await page.keyboard.press('Escape');
  await expect(page.getByTestId('shortcut-help-grid')).toBeHidden();
});
```

error-boundary-recovery.spec.ts pattern (see 09-PATTERNS §10):

```ts
test('route crash shows recovery panel (SC-3)', async ({ page }) => {
  await page.addInitScript(() => {
    (window as any).__TAURI_INVOKE_HOOK__ = (cmd: string) => {
      if (cmd === 'world_get_state') throw new Error('SIMULATED_CRASH');
      return null;
    };
  });
  await page.goto('/#/world-model');
  await expect(page.getByRole('alert', { name: /Route error/i })).toBeVisible();
  await page.getByRole('button', { name: /Back to dashboard/i }).click();
  await expect(page.getByRole('alert', { name: /Route error/i })).toBeHidden();
});
```

Verify script recipes:

- `verify-aria-icon-buttons.mjs` — see 09-PATTERNS §6
- `verify-motion-tokens.sh` — see 09-PATTERNS §7
- `verify-tokens-consistency.mjs` — see 09-PATTERNS §9
- `verify-empty-state-coverage.sh` — see 09-PATTERNS §8

package.json scripts additions:

```json
"verify:aria-icon-buttons": "node scripts/verify-aria-icon-buttons.mjs",
"verify:motion-tokens": "bash scripts/verify-motion-tokens.sh",
"verify:tokens-consistency": "node scripts/verify-tokens-consistency.mjs",
"verify:empty-state-coverage": "bash scripts/verify-empty-state-coverage.sh",
```

Extend `verify:all` to include all 4 new scripts after `verify:phase8-rust` (order: aria → motion → tokens → empty-state).

CHANGELOG.md target structure:

```markdown
# Changelog

All notable changes to BLADE are documented here.

Format: Keep a Changelog (https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added (V1 — BLADE Skin Rebuild)

Phase 0: Pre-Rebuild Audit
- RECOVERY_LOG.md + emit_all → emit_to migration plan

Phase 1: Foundation
- Design tokens, 9 primitives, typed Tauri wrapper, route registry, 5 HTML entries, migration ledger, 14 verify scripts, WCAG audit baseline

Phase 2: Onboarding + Main Shell
- 3 onboarding screens, main shell (TitleBar + NavRail + CommandPalette + ToastContext)

Phase 3: Dashboard + Chat + Settings
- 3 workstreams; 6 Rust event plumbings (hormone_update, blade_message_start, blade_thinking_chunk, blade_token_ratio, blade_quickask_bridged, blade_agent_event emit)

Phase 4: Overlay Windows
- QuickAsk bridge, Voice Orb (OpenClaw math), Ghost Mode (content-protected), HUD bar

Phase 5: Agents + Knowledge
- 10 + 10 requirements; src/lib/tauri/agents.ts + knowledge.ts; rAF-flushed event streams

Phase 6: Life OS + Identity
- 10 + 9 requirements; src/lib/tauri/life_os.ts + identity.ts

Phase 7: Dev Tools + Admin
- 11 + 10 requirements; src/lib/tauri/dev_tools.ts + admin.ts

Phase 8: Body Visualization + Hive Mesh
- 7 + 6 requirements; src/lib/tauri/body.ts + hive.ts; hormone bus dashboard; decision approval queue

Phase 9: Polish Pass (this phase)
- ErrorBoundary + EmptyState + ListSkeleton primitives
- Motion audit; prefers-reduced-motion override
- A11y sweep (icon-only aria-labels; Dialog focus trap verified)
- ⌘? shortcut help panel
- 4 new verify scripts + 5 new Playwright specs
- Closed Phase 8 Rust deferrals: hive_reject_decision, dna_set_identity, delegate_feedback

### Changed
- Full skin rebuild from src.bak (0 imports; D-17 enforced)

### Fixed
- Phase 8 documented deferrals (ApprovalQueue reject, DNA write, AiDelegate feedback)

### Deferred to v1.1
- save_config_cmd unification (save_config_field already covers frontend path)
- Per-pane error boundaries (per-route MVP shipped)
- Mobile responsive layouts (desktop-first)
- ComingSoonSkeleton cards in Canvas + ApprovalQueue dismiss (real features)
- SVG anatomical body diagram (12-card grid shipped)
- HiveMesh DAG visualization (tentacle grid shipped)
- WorldModel git operations
- Full axe-core a11y audit (targeted sweep shipped)

## [0.7.9] — 2026-04-18

Initial V1 tag; version bump to 1.0.0 pending operator sign-off after Mac-smoke M-41..M-46.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 2 Playwright specs (a11y-sweep + error-boundary-recovery)</name>
  <files>tests/e2e/a11y-sweep.spec.ts, tests/e2e/error-boundary-recovery.spec.ts</files>
  <read_first>
    - /home/arnav/blade/tests/e2e/approval-queue.spec.ts (Phase 8 spec reference — __TAURI_INVOKE_HOOK__ pattern)
    - /home/arnav/blade/tests/e2e/shortcut-fallback.spec.ts (Phase 4 keyboard spec reference)
    - /home/arnav/blade/.planning/phases/09-polish/09-PATTERNS.md §10
    - /home/arnav/blade/src/windows/main/ShortcutHelp.tsx (confirm data-testid="shortcut-help-grid") — created in Plan 09-05
    - /home/arnav/blade/src/design-system/primitives/ErrorBoundary.tsx (confirm role="alert") — created in Plan 09-02
  </read_first>
  <action>
    **1. Create `tests/e2e/a11y-sweep.spec.ts`** per 09-PATTERNS §10. Two tests:
    
    - **Test 1: reduced-motion zeroes durations** — `page.emulateMedia({ reducedMotion: 'reduce' })`, navigate to /, read `getComputedStyle(document.documentElement).getPropertyValue('--dur-enter').trim()`, expect `'0.01ms'`. This verifies Plan 09-03 Task 1 motion.css append.
    
    - **Test 2: ⌘? opens shortcut-help-grid** — navigate to /, `page.keyboard.press('Meta+Shift+/')`, assert `getByTestId('shortcut-help-grid')` is visible. Press Escape, assert hidden. This verifies Plan 09-05 Task 1 ShortcutHelp component.
    
    **2. Create `tests/e2e/error-boundary-recovery.spec.ts`** per 09-PATTERNS §10. One test:
    
    - **Test: route crash shows recovery panel** — install `__TAURI_INVOKE_HOOK__` that throws when cmd === 'world_get_state'. Navigate to /#/world-model. Assert `page.getByRole('alert', { name: /Route error/i })` visible. Click Back-to-dashboard button. Assert alert is hidden (boundary resets on route change per resetKey={route.id}).
    
    Both specs should follow the same beforeEach/addInitScript idiom used by Phase 8 approval-queue.spec.ts. No new test dependencies. The route crash spec relies on the ErrorBoundary being wrapped in MainShell (Plan 09-02 Task 2).
    
    Verify: `test -f tests/e2e/a11y-sweep.spec.ts && test -f tests/e2e/error-boundary-recovery.spec.ts`. Don't run the specs here — Mac-smoke M-46 + M-45 are authoritative when dev build is available.
  </action>
  <verify>
    <automated>test -f tests/e2e/a11y-sweep.spec.ts && test -f tests/e2e/error-boundary-recovery.spec.ts</automated>
  </verify>
  <done>Both specs exist; reference existing ShortcutHelp + ErrorBoundary primitives via testid/role selectors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 4 verify scripts + package.json wiring</name>
  <files>scripts/verify-aria-icon-buttons.mjs, scripts/verify-motion-tokens.sh, scripts/verify-tokens-consistency.mjs, scripts/verify-empty-state-coverage.sh, package.json</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/09-polish/09-PATTERNS.md §6 (aria-icon-buttons) + §7 (motion-tokens) + §8 (empty-state-coverage) + §9 (tokens-consistency)
    - /home/arnav/blade/scripts/verify-phase8-rust-surface.sh (bash script reference)
    - /home/arnav/blade/scripts/verify-migration-ledger.mjs (mjs script reference)
    - /home/arnav/blade/package.json (scripts block — append 4 new entries + extend verify:all)
  </read_first>
  <action>
    **1. Create `scripts/verify-aria-icon-buttons.mjs`** per 09-PATTERNS §6:
    - Walk `src/` for `.tsx` files.
    - Regex for `<button[^>]*>\s*([×✕✗←→↑↓⋯])\s*</button>` (single-glyph content).
    - For each match, check if `aria-label=` is present in the match substring.
    - Report violations; exit 1 if any; exit 0 with "OK" otherwise.
    - Guard against false positives: accept an emoji button that DOES have aria-label; reject only the missing ones.
    
    **2. Create `scripts/verify-motion-tokens.sh`** per 09-PATTERNS §7:
    - `set -euo pipefail`
    - `grep -rnE 'transition:[^;]*\blinear\b' src/ 2>/dev/null | grep -v 'ease-linear' || true` → if non-empty, exit 1 with list.
    - Else print OK.
    - `chmod +x`.
    
    **3. Create `scripts/verify-tokens-consistency.mjs`** per 09-PATTERNS §9:
    - Walk `src/` for `.css` and `.tsx` files.
    - Exclude tokens.css / typography.css / motion.css / motion-entrance.css / primitives.css / glass.css (source files).
    - Regex for `\b(padding|margin|gap|font-size)\s*:\s*(\d+)px` — capture numeric.
    - Allow-list: 0/1/2/4/8/12/16/20/24/32 px.
    - Anything outside allow-list → violation.
    - Exit 1 on violations.
    
    **4. Create `scripts/verify-empty-state-coverage.sh`** per 09-PATTERNS §8:
    - REQUIRED_FILES array = the union of Plan 09-02 Task 3 files (17) + Plan 09-04 Task 3 files (~20) from D-217 coverage table. Total ~37 files (minus exclusions — Terminal.tsx is exception per D-217).
    - For each file, check exists + greps for "EmptyState".
    - Exit 1 on any missing.
    
    **5. Update `package.json`:**
    - Add 4 new scripts:
      ```json
      "verify:aria-icon-buttons": "node scripts/verify-aria-icon-buttons.mjs",
      "verify:motion-tokens": "bash scripts/verify-motion-tokens.sh",
      "verify:tokens-consistency": "node scripts/verify-tokens-consistency.mjs",
      "verify:empty-state-coverage": "bash scripts/verify-empty-state-coverage.sh",
      ```
    - Extend `verify:all` to include all 4 (after `verify:phase8-rust`). Order: aria → motion → tokens → empty-state.
    
    Run `npm run verify:all` end-to-end to confirm green.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -x scripts/verify-motion-tokens.sh || chmod +x scripts/verify-motion-tokens.sh; test -x scripts/verify-empty-state-coverage.sh || chmod +x scripts/verify-empty-state-coverage.sh; node scripts/verify-aria-icon-buttons.mjs; bash scripts/verify-motion-tokens.sh; node scripts/verify-tokens-consistency.mjs; bash scripts/verify-empty-state-coverage.sh; npm run verify:all'</automated>
  </verify>
  <done>4 new verify scripts exist + pass; verify:all green with all 18 composed checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create CHANGELOG.md (Keep a Changelog format)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - /home/arnav/blade/.planning/ROADMAP.md (phase-by-phase outline)
    - /home/arnav/blade/.planning/phases/*/·*-SUMMARY.md (if available — pull concrete achievements)
    - /home/arnav/blade/package.json (version is 0.7.9)
    - /home/arnav/blade/src-tauri/tauri.conf.json (version 0.7.9)
    - /home/arnav/blade/.planning/phases/09-polish/09-CONTEXT.md §D-227 (CHANGELOG policy)
  </read_first>
  <action>
    Create `CHANGELOG.md` at the repo root using the template in this plan's <interfaces> block.
    
    Key requirements:
    - Keep a Changelog format (https://keepachangelog.com/en/1.1.0/)
    - Unreleased section at the top — Added / Changed / Fixed / Deferred to v1.1
    - Per-phase bullets under Added (Phase 0 through Phase 9)
    - Version 0.7.9 as the LAST released version (bump to 1.0.0 happens AFTER Mac-smoke M-41..M-46 pass, operator decision per D-227)
    - Do NOT touch package.json / Cargo.toml / tauri.conf.json version fields
    - Link references at the bottom: repository URL placeholder or the `slayerblade.site` pattern
    
    Keep the CHANGELOG concise — bullet form, one line per concrete achievement. No prose paragraphs. Match the template structure from <interfaces> verbatim; adapt phase-specific bullets to reflect actual Phase 0..9 SUMMARYs where available.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q "Unreleased" CHANGELOG.md && grep -q "0.7.9" CHANGELOG.md && grep -q "Polish Pass" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md exists with Unreleased section, per-phase Added bullets, and Deferred v1.1 list.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3b: Primitive barrel cleanup — add ListSkeleton export</name>
  <files>src/design-system/primitives/index.ts</files>
  <read_first>
    - /home/arnav/blade/src/design-system/primitives/index.ts (confirm ErrorBoundary + EmptyState exports from Plan 09-02)
    - /home/arnav/blade/src/design-system/primitives/ListSkeleton.tsx (confirm exists from Plan 09-04)
  </read_first>
  <action>
    Plan 09-04 intentionally skipped editing `src/design-system/primitives/index.ts` to preserve the Wave 2 disjoint-files invariant (D-229 — Plan 09-02 owned that file in Wave 2). Plan 09-06 Wave 4 closes the loop by adding the barrel export.
    
    Append to `src/design-system/primitives/index.ts` (keep existing 11 exports intact):
    
    ```ts
    export { ListSkeleton } from './ListSkeleton';
    ```
    
    Optionally scan the codebase for any `from '@/design-system/primitives/ListSkeleton'` direct imports introduced by Plan 09-04 Task 4 and rewrite them to `from '@/design-system/primitives'` for consistency. If rewrites are done, run `npx tsc --noEmit` to confirm.
    
    Verify: `grep -q "ListSkeleton" src/design-system/primitives/index.ts`.
  </action>
  <verify>
    <automated>grep -q "ListSkeleton" src/design-system/primitives/index.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Barrel now re-exports all 12 primitives (9 original + ErrorBoundary + EmptyState + ListSkeleton).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator Mac-smoke checkpoint (M-41..M-46) + release sign-off</name>
  <what-built>
    - 3 Rust commands backfilled (hive_reject_decision, dna_set_identity, delegate_feedback)
    - ErrorBoundary + EmptyState + ListSkeleton primitives
    - prefers-reduced-motion override in motion.css
    - A11y sweep on shell + hud + chat + settings (icon-only aria-labels)
    - ⌘? shortcut help panel
    - Empty-state sweep across ~37 cluster routes (agents + knowledge + life-os + identity + body + hive + dev-tools + admin)
    - ListSkeleton applied to 6 async-list panels
    - Motion audit pass (no rogue linear transitions)
    - Cross-route token consistency audit
    - 5 new Playwright specs (a11y-sweep, error-boundary-recovery, perf-dashboard-fp, perf-chat-stream, perf-agent-timeline)
    - 4 new verify scripts (aria-icon-buttons, motion-tokens, tokens-consistency, empty-state-coverage)
    - CHANGELOG.md V1 release notes
  </what-built>
  <how-to-verify>
    Per STATE.md multi-phase operator-smoke batching strategy, queue M-41..M-46 onto the bundled Mac session (alongside Phase 1 WCAG + Phase 2..8 Mac items). Do NOT run these checks in the sandbox; they require macOS + working prod build.
    
    **M-41 — Dashboard first paint (SC-5 tight P-01):**
    1. `npm run tauri dev` launches on macOS.
    2. Open about:tracing via developer-tools.
    3. Navigate to /dashboard.
    4. Measure navigation-start → first-contentful-paint.
    5. Confirm ≤ 200ms on integrated GPU (NOT the 250ms loose CI target).
    
    **M-42 — Chat render (SC-5 tight P-01):**
    1. Navigate to /chat.
    2. Send a long message that triggers model streaming at ≥ 50 tokens/sec.
    3. Open React Profiler.
    4. Confirm max render time ≤ 16ms during stream (not 20ms loose CI).
    5. Confirm no full-tree re-renders (flame graph).
    
    **M-43 — Agent timeline rAF stability (SC-5):**
    1. Navigate to /agent-detail (requires a running agent).
    2. Observe 5-minute continuous stream with no dropped rAF callbacks.
    3. Approx 100+ events. Scroll does not stutter.
    
    **M-44 — Prod build bundle (SC-1):**
    1. Run `npm run tauri build` on macOS.
    2. Confirm bundle at `src-tauri/target/release/bundle/macos/Blade.app` (or `.dmg`).
    3. Launch — confirm all 5 windows open without Rust panic.
    4. Navigate to /dashboard + /chat + /body-map + /hive-mesh + /world-model + /agent-detail — no 404 fallbacks; no orphan routes.
    5. Run `node scripts/verify-html-entries.mjs --prod` — passes.
    
    **M-45 — prefers-reduced-motion (POL-07):**
    1. Toggle macOS System Settings → Accessibility → Display → Reduce motion ON.
    2. Launch app.
    3. Confirm no entrance animations (list-entrance class honoured); no GlassSpinner spin rotation; no transitions > 0.01ms visible.
    4. Toggle OFF — animations resume.
    
    **M-46 — ⌘? shortcut help (SC-4):**
    1. Launch app.
    2. Press ⌘? (Cmd+Shift+?) — shortcut help panel opens.
    3. Confirm all global shortcuts render with their labels (⌘K, ⌘1, ⌘/, ⌘,, ⌘[, ⌘], ⌘?, Alt+Space).
    4. Confirm route-scoped shortcuts (if any) render.
    5. Press Escape — panel closes, focus returns to NavRail (visible focus ring).
    6. Click a shortcut label — triggers navigation.
    
    **After M-41..M-46 pass:** Operator decides on version bump to 1.0.0. Typical sequence:
    1. Bump `package.json` version to 1.0.0.
    2. Bump `src-tauri/Cargo.toml` version to 1.0.0.
    3. Bump `src-tauri/tauri.conf.json` version to 1.0.0.
    4. Update CHANGELOG.md: move [Unreleased] → [1.0.0] — {date}.
    5. Commit: `chore: bump v1.0.0 — V1 shipped`.
    6. Tag: `git tag v1.0.0`.
    7. Run `npm run release:prepare-updater` (if release pipeline is wired).
    8. Push + trigger GitHub Actions release workflow.
    
    Expected outcome: all 6 checkpoints pass on the brother's Mac. Report ANY failures for a gap-closure plan before marking Phase 9 complete.
  </how-to-verify>
  <resume-signal>Type "M-41..M-46 approved — ready to ship" OR describe any failing scenarios for a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verify script → filesystem | scripts walk src/ tree; read-only |
| CHANGELOG.md → repository | plain-text document committed to version control |
| Playwright spec → __TAURI_INVOKE_HOOK__ | spec installs a hook that throws to simulate crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Denial of service | Verify scripts walking large repos | accept | src/ tree is ~800 files; linear walk is sub-second. No DoS. |
| T-09-06-02 | Tampering | CHANGELOG.md committed with misleading claims | accept | Developer-facing artifact; review before release tag. |
| T-09-06-03 | Information disclosure | Error spec exposes componentStack | accept | Only in test environment; never in production build. |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm run verify:all exits 0 (18 composed checks: pre-existing 14 + 4 new)
- 2 new Playwright specs exist and compile
- 4 new verify scripts exist, executable, and each passes independently
- CHANGELOG.md exists with Unreleased + per-phase bullets + Deferred list
- Version fields still at 0.7.9 (NOT bumped by planner)
- Operator Mac-smoke checkpoint documented with M-41..M-46 + release sign-off steps
</verification>

<success_criteria>
- SC-1 falsified (partial sandbox + Mac-smoke M-44 closes): verify-html-entries.mjs --prod ready; Mac build produces bundle
- SC-2 falsified: verify-empty-state-coverage.sh regression guard across ~37 files
- SC-3 falsified: error-boundary-recovery.spec.ts + MainShell wrap (Plan 09-02) together make crash-free routes observable
- SC-4 falsified: a11y-sweep.spec.ts ⌘? test + ShortcutHelp component (Plan 09-05) together make shortcut help observable
- SC-5 falsified (partial sandbox + Mac-smoke M-41..M-43, M-45 close): reduced-motion spec + audit-contrast.mjs (existing); perf specs + Mac-smoke confirm
- Phase 9 SUMMARY (09-06-SUMMARY.md) surfaces: all 5 SC falsifiers, Rust deferrals closed (3), primitives added (3), verify scripts added (4), Playwright specs added (5), 37 empty-state swaps, CHANGELOG ready for 1.0.0 bump, Mac-smoke items M-41..M-46 queued
- Plan 09-06 SUMMARY includes "READY TO SHIP" line pending operator Mac-smoke sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-06-SUMMARY.md`.
</output>
