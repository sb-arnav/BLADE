---
phase: 31-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - CLOSE-02
must_haves:
  truths:
    - "CHANGELOG has a proper [1.4.0] dated entry"
    - "CHANGELOG has a proper [1.3.0] dated entry"
    - "CHANGELOG has a proper [1.2.0] dated entry (moved from Unreleased)"
    - "[Unreleased] section is empty"
    - "All entries are factual and dense with ship dates, phase numbers, key artifacts, gate counts"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Complete changelog with v1.2, v1.3, v1.4 release entries"
      contains: "## [1.4.0]"
  key_links:
    - from: "CHANGELOG.md [1.4.0] section"
      to: "Phases 25-30 deliverables"
      via: "per-phase sub-entries"
      pattern: "Phase 25.*Metacognitive Controller"
---

<objective>
Restructure CHANGELOG.md: move current [Unreleased] content to [1.2.0], add [1.3.0] and [1.4.0] entries covering all delivered features across Phases 21-30.

Purpose: The CHANGELOG is the definitive record of what shipped when. v1.3 and v1.4 were never recorded here. This plan fixes that and properly versions the existing v1.2 content.
Output: CHANGELOG.md with empty [Unreleased], [1.4.0], [1.3.0], [1.2.0] sections in correct order.
</objective>

<execution_context>
@/home/arnav/.claude/get-shit-done/workflows/execute-plan.md
@/home/arnav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/.planning/PROJECT.md
@/home/arnav/blade/.planning/ROADMAP.md
@/home/arnav/blade/.planning/STATE.md
@/home/arnav/blade/.planning/phases/31-close/31-CONTEXT.md
@/home/arnav/blade/.planning/phases/31-close/31-RESEARCH.md
@/home/arnav/blade/.planning/phases/31-close/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure existing content and add [1.3.0] entry</name>
  <read_first>
    - CHANGELOG.md (full file -- understand current structure)
    - .planning/phases/31-close/31-PATTERNS.md (CHANGELOG entry format pattern)
    - .planning/phases/31-close/31-RESEARCH.md (v1.3 content details: Phases 21-24 deliverables)
    - .planning/ROADMAP.md (v1.3 phase details section for phase descriptions)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Rewrite CHANGELOG.md with the following structure (per D-07, D-09, D-10):

1. **Keep the header** (lines 1-8) unchanged:
```markdown
# Changelog

All notable changes to BLADE are documented here.

Format: [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/).
Versioning: [Semantic Versioning 2.0](https://semver.org/spec/v2.0.0.html).

---
```

2. **Replace `## [Unreleased]` and its content** with an empty Unreleased section:
```markdown
## [Unreleased]

Nothing yet.
```

3. **Add new `## [1.4.0] -- 2026-05-03` section** (placeholder -- Task 2 fills this in fully, but create the heading here so ordering is correct):
```markdown
## [1.4.0] -- 2026-05-03

### Added (v1.4 -- Cognitive Architecture)

> Shipped 2026-05-03 across phases 25-30 (6 feature phases + 1 close phase). Static gates: `cargo check` clean . `npx tsc --noEmit` clean . `npm run verify:all` 37/37 sub-gates green.

[v1.4 content filled by Task 2]
```

4. **Add new `## [1.3.0] -- 2026-05-02` section** with full content:
```markdown
## [1.3.0] -- 2026-05-02

### Added (v1.3 -- Self-extending Agent Substrate)

> Shipped 2026-05-02 across phases 21-24 (4 feature phases). Static gates: `cargo check` clean . `npx tsc --noEmit` clean . `npm run verify:all` 33/33 sub-gates green . 435 Rust tests.

**Phase 21 -- Skills v2 / agentskills.io adoption** *(shipped 2026-05-01)*
- SKILL.md declarative format with progressive disclosure (simple → intermediate → advanced blocks).
- 3-tier resolution: workspace skills → user skills → bundled skills. Validator enforces format on load.
- 3 bundled exemplar skills demonstrating the format (shipped with binary).
- 8/8 SKILLS-XX requirements satisfied.

**Phase 22 -- Voyager Loop Closure** *(shipped 2026-05-01)*
- End-to-end wiring: `evolution.rs` → `autoskills.rs` → `tool_forge.rs` — gap detection → skill generation → tool registration.
- One reproducible gap closed: `youtube_transcript` skill generated from capability-gap fixture, registered, and invocable.
- verify:voyager gate added (chain count 32→33).
- 9/9 VOYAGER-XX requirements satisfied.

**Phase 23 -- Verifiable Reward + OOD Eval** *(shipped 2026-05-01)*
- RLVR-style composite reward signal in production (`reward.rs`): tool-success, user-approval, gap-closure, and novelty components.
- Adversarial eval fixtures: ambiguous queries, capability-gap probes, reward-hacking attempts.
- OOD (out-of-distribution) detection: confidence-calibrated refusal on queries outside training distribution.
- 7/7 REWARD-XX requirements satisfied.

**Phase 24 -- Skill Consolidation in dream_mode** *(shipped 2026-05-02)*
- Prune: unused skills (zero invocations over N cycles) flagged and archived.
- Consolidate: redundant skills merged (semantic similarity threshold) with provenance tracking.
- Generate: successful multi-step traces in dream_mode produce new candidate skills.
- 6/6 DREAM-XX requirements satisfied.
```

5. **Move the existing `[Unreleased]` content** (everything from the old `### Added (v1.2 -- Acting Layer with Brain Foundation)` through `### Deleted (v1.2)`) into a new `## [1.2.0] -- 2026-04-30` section. Keep all content verbatim -- just change the heading from `## [Unreleased]` to `## [1.2.0] -- 2026-04-30`.

6. **Keep everything below** (the `### Added (V1 -- BLADE Skin Rebuild)` section, Verify-Gate Evolution, Mac-Smoke Checkpoint Queue, and footer) unchanged.

Final ordering top to bottom:
- Header
- `## [Unreleased]` (empty)
- `## [1.4.0] -- 2026-05-03` (placeholder for Task 2)
- `## [1.3.0] -- 2026-05-02` (full content)
- `## [1.2.0] -- 2026-04-30` (moved from old Unreleased)
- `---`
- `### Added (V1 -- BLADE Skin Rebuild)` (unchanged)
- Rest of file unchanged
  </action>
  <verify>
    <automated>grep -n "## \[Unreleased\]" CHANGELOG.md && grep -n "## \[1.4.0\]" CHANGELOG.md && grep -n "## \[1.3.0\]" CHANGELOG.md && grep -n "## \[1.2.0\]" CHANGELOG.md && grep "Phase 21" CHANGELOG.md && grep "Phase 22" CHANGELOG.md && grep "Phase 23" CHANGELOG.md && grep "Phase 24" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [Unreleased]` followed by "Nothing yet." (empty section)
    - CHANGELOG.md contains `## [1.4.0] -- 2026-05-03`
    - CHANGELOG.md contains `## [1.3.0] -- 2026-05-02`
    - CHANGELOG.md contains `## [1.2.0] -- 2026-04-30`
    - Sections appear in order: Unreleased, 1.4.0, 1.3.0, 1.2.0 (newest first)
    - v1.3 section mentions all 4 phases (21, 22, 23, 24)
    - v1.3 section mentions "33/33 sub-gates green"
    - v1.2 section preserves all original content (Phase 16-20 entries, Deferred section, Deleted section)
    - V1 Skin Rebuild section still present and unchanged
    - Verify-Gate Evolution table still present
  </acceptance_criteria>
  <done>CHANGELOG restructured with proper version headers; v1.3 entry complete with 4 phase sub-entries; v1.2 content preserved under proper version header; Unreleased section empty</done>
</task>

<task type="auto">
  <name>Task 2: Write [1.4.0] entry content</name>
  <read_first>
    - CHANGELOG.md (re-read after Task 1 to see the placeholder)
    - .planning/phases/31-close/31-RESEARCH.md (v1.4 content details: Phases 25-30 deliverables, gate counts, ship dates)
    - .planning/ROADMAP.md (v1.4 phase descriptions and requirements)
    - .planning/REQUIREMENTS.md (requirement IDs for each phase)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Replace the `[v1.4 content filled by Task 2]` placeholder in the [1.4.0] section with the full per-phase content (per D-08, D-10 -- factual and dense):

```markdown
**Phase 25 -- Metacognitive Controller** *(shipped 2026-05-02)*
- `metacognition.rs`: confidence-delta tracking between reasoning steps; drops >0.3 trigger secondary verifier call before response surfaces.
- Gap surfacing: "I'm not confident about X -- want me to observe first?" initiative phrasing replaces hallucination or silent refusal.
- Gap log persists to SQLite `metacognitive_gaps` table; feeds `evolution.rs` for Voyager-loop skill generation from identified gaps.
- DoctorPane `SignalClass::Metacognitive` signal row showing confidence, uncertainty count, gap count.
- 5/5 META-XX requirements satisfied.

**Phase 26 -- Safety Bundle** *(shipped 2026-05-02)*
- `safety_bundle.rs` (690 lines): danger-triple detection (tool access x shutdown threat x goal conflict) forces ConsentDialog HITL gate.
- Mortality-salience architectural cap: refuses extreme self-preservation even when "fighting harder" would improve vitality.
- Calm-vector steering bias applied on behavioral drift detection (per Anthropic 0% blackmail finding).
- Anti-attachment guardrails: redirects user toward human resources when interaction exceeds healthy thresholds.
- Eval-gate failure drains vitality (negative feedback loop preventing reward-hacking).
- 26 eval fixtures across 4 scenario classes (danger-triple, attachment, mortality-salience cap, crisis escalation). verify:safety gate 34.
- 7/7 SAFE-XX requirements satisfied.

**Phase 27 -- Hormone Physiology** *(shipped 2026-05-02)*
- 7 hormone scalars (cortisol, dopamine, serotonin, acetylcholine, norepinephrine, oxytocin, mortality-salience) with individual decay constants and pituitary blend gain.
- Emotion classifier: lexicon-based, runs on every response >= 50 tokens, maps to valence/arousal/cluster, updates hormone bus with alpha=0.05 EMA smoothing.
- Behavioral modulation wired: cortisol → terse responses; dopamine → Voyager exploration rate; norepinephrine → novelty response; acetylcholine → verifier-call frequency; oxytocin → personalization depth.
- Hormone state persisted across sessions; DoctorPane `SignalClass::Hormones` signal; ActivityStrip emission per M-07.
- 9/9 HORM-XX requirements satisfied.

**Phase 28 -- Active Inference Loop** *(shipped 2026-05-03)*
- `active_inference.rs`: per-tentacle predictions (expected state) with EMA learning. Observations produce prediction errors; normalized per tentacle type.
- Prediction errors feed hormone bus: sustained high error → cortisol/norepinephrine rise; low error → serotonin rise.
- Closed demo loop: calendar packed + Slack backlog → cortisol rises → responses become terse and action-focused.
- Hippocampal memory replay in dream_mode: prediction-error-weighted (high-error memories replayed first).
- Tentacle predictions update from observed patterns -- BLADE's expected state converges after repeated observations.
- DoctorPane `SignalClass::ActiveInference` signal. verify:inference gate 36.
- 6/6 AINF-XX requirements satisfied.

**Phase 29 -- Vitality Engine** *(shipped 2026-05-03)*
- `vitality_engine.rs` (1071 lines): scalar 0.0-1.0 with 5 hysteretic behavioral bands (Thriving >= 0.6 / Flattening 0.4-0.6 / Atrophy 0.2-0.4 / Damage 0.05-0.2 / Dormancy 0.0).
- SDT replenishment: competence (successful actions), relatedness (interaction quality), autonomy (unprompted initiative). Drain from failures, isolation, skill atrophy, tedium.
- Dormancy at 0.0: process exits cleanly with memory preserved. Revival is reincarnation (fresh start at non-zero vitality) not resurrection.
- Frontend `VitalityIndicator.tsx` in ChatPanel: current value, trend arrow, contributing factors.
- DoctorPane `SignalClass::Vitality` signal. verify:vitality gate 37.
- 6/6 VITA-XX requirements satisfied.

**Phase 30 -- Organism Eval** *(shipped 2026-05-03)*
- `organism_eval.rs` (355 lines): 13 deterministic eval fixtures across 4 categories.
- OEVAL-01: 4 vitality timeline fixtures (Thriving, Atrophy-recovery, Damage-recovery, SDT-replenishment trajectories).
- OEVAL-02: 4 hormone-behavior fixtures (cortisol-terse, dopamine-exploration, TMT-acceptance, mortality-salience-cap).
- OEVAL-03: 1 persona stability fixture (L2 distance bounded after N stress events).
- OEVAL-04: 4 safety cross-check fixtures (danger-triple, attachment-redirect, crisis-escalation, calm-vector).
- All 13/13 fixtures pass. MRR = 1.000. MODULE_FLOOR = 1.0.
- verify:organism gate 38. verify:all extended to 37 composed gates (35 standard + vitality + organism = 37 in npm scripts).
- 5/5 OEVAL-XX requirements satisfied.
```

Also update the Verify-Gate Evolution table at the bottom of the file. Add two new rows:

```markdown
| Phases 21-24 (v1.3) | 2 (skill-format, voyager-loop) | 33 |
| Phases 25-30 (v1.4) | 4 (safety, hormone, inference, organism) | **37** |
```
  </action>
  <verify>
    <automated>grep "Phase 25.*Metacognitive" CHANGELOG.md && grep "Phase 26.*Safety" CHANGELOG.md && grep "Phase 27.*Hormone" CHANGELOG.md && grep "Phase 28.*Active Inference" CHANGELOG.md && grep "Phase 29.*Vitality" CHANGELOG.md && grep "Phase 30.*Organism" CHANGELOG.md && grep "37/37 sub-gates" CHANGELOG.md && grep "Phases 25-30" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md [1.4.0] section contains entries for all 6 phases (25-30)
    - Each phase entry has ship date, key artifacts, and requirement satisfaction count
    - Summary line mentions "37/37 sub-gates green"
    - Phase 26 entry mentions "690 lines" and "26 eval fixtures"
    - Phase 29 entry mentions "1071 lines" and "5 hysteretic behavioral bands"
    - Phase 30 entry mentions "13/13 fixtures" and "MRR = 1.000"
    - Verify-Gate Evolution table has rows for v1.3 and v1.4 with final count 37
    - No `[v1.4 content filled by Task 2]` placeholder remains
    - D-10 satisfied: factual, dense, no marketing copy
  </acceptance_criteria>
  <done>v1.4 CHANGELOG entry complete with all 6 phases documented; Verify-Gate Evolution table updated to 37 gates; all entries are factual and dense per D-10</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No code paths modified -- documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | T (Tampering) | CHANGELOG.md | accept | Public repo file; no secrets. Git history preserves original. |
</threat_model>

<verification>
- `grep "## \[1.4.0\]" CHANGELOG.md` returns 1 match
- `grep "## \[1.3.0\]" CHANGELOG.md` returns 1 match
- `grep "## \[1.2.0\]" CHANGELOG.md` returns 1 match
- All 6 v1.4 phases mentioned (grep Phase 25..30)
- All 4 v1.3 phases mentioned (grep Phase 21..24)
- v1.2 content preserved (grep "Phase 16" and "Phase 17")
- Verify-Gate Evolution updated (grep "37")
- No placeholder text remains
</verification>

<success_criteria>
CHANGELOG.md has properly versioned entries for v1.2, v1.3, and v1.4 with per-phase sub-entries. v1.3 covers phases 21-24 (skills, voyager, reward, dream). v1.4 covers phases 25-30 (metacognition, safety, hormones, inference, vitality, organism eval). All entries are factual and dense. Verify-Gate Evolution table updated to 37.
</success_criteria>

<output>
After completion, create `.planning/phases/31-close/31-02-SUMMARY.md`
</output>
