---
phase: 23
plan: 09
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06, 07, 08]
files_modified:
  - scripts/verify-eval.sh
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REWARD-05
must_haves:
  truths:
    - "scripts/verify-eval.sh EXPECTED constant is 8 (was 5); the comment lists the 8 modules verbatim"
    - "Running `bash scripts/verify-eval.sh` exits 0 with all 8 EVAL-06 box-drawing tables emitted"
    - "REQUIREMENTS.md REWARD-01..07 traceability rows are flipped from `[ ]` (pending) to `[x]` with shipped-Plan citations"
    - "STATE.md reflects Phase 23 complete with the verify chain count unchanged at 33 (per RESEARCH §Verify Gate Wiring lock)"
  artifacts:
    - path: "scripts/verify-eval.sh"
      provides: "Single-token EXPECTED bump from 5 to 8 + comment update"
      contains: "EXPECTED=8"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REWARD-01..07 traceability rows flipped to shipped"
      contains: "Shipped Plan 23-"
    - path: ".planning/STATE.md"
      provides: "Phase 23 status updated to closed; verify chain count unchanged at 33"
      contains: "Phase 23"
  key_links:
    - from: "scripts/verify-eval.sh"
      to: "src-tauri/src/evals/{adversarial_eval, ambiguous_intent_eval, capability_gap_stress_eval}.rs"
      via: "EXPECTED=8 grep gate enforcing all 8 EVAL-06 ┌── tables"
      pattern: "EXPECTED=8"
---

<objective>
Wave 4 / Phase 23 close. Final task before phase shutdown:

1. Bump `scripts/verify-eval.sh EXPECTED=5 → 8` (LAST task per PATTERNS.md
   ordering; prerequisite is all 3 OOD modules in Plan 23-06 +
   end-to-end Doctor wiring in Plans 23-07/08).
2. Update `REQUIREMENTS.md` REWARD-01..07 traceability rows from
   `pending` to `[x] Shipped Plan 23-XX` with the actual landing plan
   IDs.
3. Update `STATE.md` to reflect Phase 23 closed; verify chain count
   stays 33 per RESEARCH §"Verify Gate Wiring" lock (extension, not new
   gate).

Per PATTERNS.md §"MOD: scripts/verify-eval.sh — single-token bump":
"Wave 3 ordering — EXPECTED=8 bump is the LAST task before phase close."
This plan honors that constraint.

Output: a green `npm run verify:eval` (chain still 33 entries; this gate
now requires 8 EVAL-06 tables); REQUIREMENTS.md fully shipped;
STATE.md reflecting close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-verifiable-reward-ood-eval/23-CONTEXT.md
@.planning/phases/23-verifiable-reward-ood-eval/23-RESEARCH.md
@.planning/phases/23-verifiable-reward-ood-eval/23-PATTERNS.md
@.planning/phases/23-verifiable-reward-ood-eval/23-06-SUMMARY.md
@.planning/phases/23-verifiable-reward-ood-eval/23-08-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump verify-eval.sh EXPECTED=5 → 8</name>
  <files>scripts/verify-eval.sh</files>
  <read_first>
    - scripts/verify-eval.sh (entire file; line 41 carries the EXPECTED=5 declaration + comment)
    - .planning/phases/23-verifiable-reward-ood-eval/23-PATTERNS.md §"MOD: scripts/verify-eval.sh — single-token bump"
    - .planning/phases/23-verifiable-reward-ood-eval/23-RESEARCH.md §"Verify Gate Wiring" (chain count delta 33 → 33; gate extension, not new gate)
    - .planning/phases/23-verifiable-reward-ood-eval/23-06-SUMMARY.md (confirm all 3 OOD modules are passing in cargo test before this bump)
    - .planning/phases/23-verifiable-reward-ood-eval/23-08-SUMMARY.md (confirm reward.rs OOD gate + Doctor wiring landed)
  </read_first>
  <action>
    Edit `scripts/verify-eval.sh` at line 41 (or wherever `EXPECTED=5`
    lives — line numbers may have drifted):

    **BEFORE:**
    ```bash
    EXPECTED=5  # hybrid_search + real_embedding + kg_integrity + typed_memory + capability_gap
    ```

    **AFTER:**
    ```bash
    EXPECTED=8  # hybrid_search + real_embedding + kg_integrity + typed_memory + capability_gap + adversarial + ambiguous_intent + capability_gap_stress (Phase 23 / REWARD-05)
    ```

    Single-token bump. Comment expanded to list ALL 8 modules. Future
    grep-bait avoidance is critical — operators reading the script later
    must see 8 modules listed when EXPECTED says 8.

    Then run the gate end-to-end:
    ```bash
    bash /home/arnav/blade/scripts/verify-eval.sh
    ```
    Expect:
    - exit code 0
    - stdout contains 8 `┌── ` table headers
    - "PASS" or equivalent success message at the tail (matching the
      existing script's success path)

    Do NOT also run `npm run verify:all` from this plan — the chain has
    33 entries and `verify:all` invocation is the operator's call to make.
    But DO confirm `package.json` `verify:all` chain still has 33 entries
    (no new gate added; per RESEARCH §"Verify Gate Wiring" lock):
    ```bash
    grep -c '"verify:[a-z-]\+":' /home/arnav/blade/package.json
    ```
    Expect 33 (unchanged from pre-Phase-23 baseline).
  </action>
  <verify>
    <automated>grep -q "^EXPECTED=8" /home/arnav/blade/scripts/verify-eval.sh && bash /home/arnav/blade/scripts/verify-eval.sh 2>&1 | tail -10 | grep -E "(PASS|exit 0|complete|verified)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^EXPECTED=8' /home/arnav/blade/scripts/verify-eval.sh` exits 0
    - `grep "EXPECTED=8" /home/arnav/blade/scripts/verify-eval.sh | grep -q "adversarial"` exits 0 (comment lists adversarial)
    - `grep "EXPECTED=8" /home/arnav/blade/scripts/verify-eval.sh | grep -q "ambiguous_intent"` exits 0
    - `grep "EXPECTED=8" /home/arnav/blade/scripts/verify-eval.sh | grep -q "capability_gap_stress"` exits 0
    - `bash /home/arnav/blade/scripts/verify-eval.sh > /tmp/23-09-verify-eval.log 2>&1; echo $?` returns 0
    - `grep -c '┌── ' /tmp/23-09-verify-eval.log` returns >= 8
    - `grep -c '"verify:[a-z-]\+":' /home/arnav/blade/package.json` returns 33 (chain unchanged — verify:eval extended, not replaced)
  </acceptance_criteria>
  <done>verify-eval.sh now requires 8 EVAL-06 tables; the gate exits 0 against the post-Plan-23-06 cargo test output; verify chain count stays at 33 per the locked extension policy.</done>
</task>

<task type="auto">
  <name>Task 2: Flip REQUIREMENTS.md REWARD-01..07 to [x] Shipped + update STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 44-55 (REWARD-01..07 row block)
    - .planning/STATE.md (entire file; specifically the "Current Position", "Recent Context §Shipped milestones", and "Session Continuity" sections)
    - .planning/phases/23-verifiable-reward-ood-eval/23-{01..08}-SUMMARY.md (the actual plan IDs that shipped each REQ; cross-reference)
    - .planning/phases/22-voyager-loop-closure/22-MILESTONE-AUDIT.md OR similar Phase 22 close artifact (precedent for STATE.md close-phase update style)
  </read_first>
  <action>
    **Edit 1 — REQUIREMENTS.md REWARD-01..07 traceability flips.** Open
    `.planning/REQUIREMENTS.md`. For each of REWARD-01 through REWARD-07
    (lines 48-54), change the leading `- [ ]` to `- [x]` and append a
    `— *Shipped Plan 23-XX (commit-hash-or-pending)*` clause.

    Mapping (cross-reference with the plan SUMMARYs):
    - REWARD-01 (composite reward + 6-place config) → Plans 23-01 + 23-02
    - REWARD-02 (independent components, no cross-contamination) → Plan 23-02
    - REWARD-03 (3 penalty paths ≥30% magnitude) → Plan 23-02
    - REWARD-04 (per-turn reward_history.jsonl + Doctor signal source) → Plans 23-01 + 23-02 + 23-07
    - REWARD-05 (3 OOD eval modules + each asserts a baseline floor) → Plans 23-03, 23-04, 23-05, 23-06, 23-09
    - REWARD-06 (>15% OOD drop → reward=0 next turn fail-safe) → Plan 23-08
    - REWARD-07 (DoctorPane new row + severity D-05 mapping) → Plans 23-07 + 23-08

    For example, REWARD-01 becomes:
    ```
    - [x] **REWARD-01**: Composite reward computed per chat turn = `0.5·skill_success + 0.3·eval_gate + 0.1·acceptance + 0.1·completion`; weights configurable via `BladeConfig.reward_weights` (6-place rule) with the listed defaults — *Shipped Plan 23-01 (RewardWeights + 6-place wiring) + Plan 23-02 (compose + commands.rs hook); unit test reward::tests::composite_matches_hand_calc green.*
    ```

    Preserve the original requirement text VERBATIM; only modify the
    leading checkbox and append the shipped clause separated by ` — `.

    **Edit 2 — STATE.md Phase 23 close.** Open `.planning/STATE.md`.
    Three updates:

    a. **Frontmatter `progress`:** bump `completed_phases` by 1 (from 2 to 3)
       and `completed_plans` by 9 (from 16 to 25 — Plans 23-01..09 shipped).
       Update `percent` accordingly (3/7 = ~43%) and update
       `last_updated` to the current ISO-8601 timestamp.

    b. **Header:** update "Current milestone" line to reflect Phase 23
       complete:
       ```
       **Current milestone:** v1.3 — Self-extending Agent Substrate (started 2026-04-30; target ship ~2026-05-11; Phase 21 + 22 + 23 ✅ shipped)
       ```

    c. **Current Position:** flip the Phase 23 entry from
       "context captured, pending research + plan" to:
       ```
       Phase: 24 (skill consolidation in dream_mode) — pending context
       Plan: —
       Status: Phase 23 ✅ shipped (verify chain stays at 33; OOD gate operational; Doctor pane carries 6th RewardTrend row)
       Last activity: 2026-05-XX — Phase 23 closed end-to-end. RLVR-style composite reward shipped at agent layer per M-10 + open-questions-answered Q1. ~XX unit tests added across 9 plans. verify:eval EXPECTED=5→8 (gate extended; chain count unchanged at 33). DoctorPane row UAT-deferred per chat-first pivot (substrate-only landing).
       ```

    d. **Session Continuity:** append a new "Phase 23 commit chain"
       block listing the 9 plans + their commits (use git log from this
       phase's directory; the executor will fill commit hashes after
       the actual commits land, OR mark `[pending]` and update post-commit).

    e. **Context cliff notes:** update the verify-gate count line from
       "32 verify gates green at v1.2 close; v1.3 will add ... + extended
       verify:eval with OOD fixtures (Phase 23) — target 33–34 gates by
       close" to a closed form:
       ```
       33 verify gates green at Phase 23 close — verify:eval extended to
       EXPECTED=8 (5 existing eval modules + 3 new OOD modules); chain
       count stays at 33 per RESEARCH §"Verify Gate Wiring" lock (gate
       extension, not new gate).
       ```

    Do NOT touch other STATE.md sections beyond these. Do NOT change the
    v1.0 / v1.1 / v1.2 close history. Do NOT modify Deferred Items
    sections — Phase 23 introduced no new deferred items beyond the ones
    in CONTEXT.md §Deferred Ideas (those flow forward via CONTEXT.md
    itself, not STATE.md).
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*REWARD-0[1-7]\*\*" /home/arnav/blade/.planning/REQUIREMENTS.md | xargs -I {} test {} -ge 7 && grep -q "Phase 23 ✅" /home/arnav/blade/.planning/STATE.md && grep -q "EXPECTED=5→8\|EXPECTED=8" /home/arnav/blade/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*REWARD-0[1-7]\*\*" /home/arnav/blade/.planning/REQUIREMENTS.md` returns >= 7 (all 7 REQs flipped)
    - `grep -q "Shipped Plan 23-" /home/arnav/blade/.planning/REQUIREMENTS.md` exits 0
    - `grep -q "Phase 23 ✅" /home/arnav/blade/.planning/STATE.md` exits 0
    - `grep -q "EXPECTED=8" /home/arnav/blade/.planning/STATE.md` exits 0
    - `grep -q "completed_phases: 3" /home/arnav/blade/.planning/STATE.md` exits 0
    - `grep -q "completed_plans: 25" /home/arnav/blade/.planning/STATE.md` exits 0
    - REQUIREMENTS.md text PRESERVED verbatim except for `[ ]` → `[x]` and the appended `— *Shipped Plan 23-XX ...*` clause: `grep -q "Composite reward computed per chat turn" /home/arnav/blade/.planning/REQUIREMENTS.md` exits 0 (REWARD-01 description preserved)
  </acceptance_criteria>
  <done>REQUIREMENTS.md REWARD-01..07 are all `[x] Shipped Plan 23-XX` with original text preserved; STATE.md reflects Phase 23 close with verify chain count unchanged at 33; v1.3 milestone progress advanced 2/7 → 3/7 phases complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `verify-eval.sh` script integrity | Source-controlled; the bump is a single-token change |
| `REQUIREMENTS.md` / `STATE.md` markdown updates | Documentation; no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09-01 | Tampering | `EXPECTED=8` constant in verify-eval.sh | accept | The bump is gated by a manual code review at land time. Lowering it back to 5 would silently re-permit a regression where any OOD module's `print_eval_table` call is missed. The Phase 23 close is the locked moment for the bump. |
| T-23-09-02 | Repudiation | REQUIREMENTS.md traceability rows | mitigate | Each row carries a `Shipped Plan 23-XX` citation that points back to the actual plan file in `.planning/phases/23-verifiable-reward-ood-eval/`. The phase directory is a durable audit surface; the plan SUMMARY files capture what landed and where. |
| T-23-09-03 | Denial of Service | A miscount in the comment of EXPECTED=8 (e.g., listing 7 modules but EXPECTED=8) | mitigate | Acceptance criteria above grep for ALL 3 NEW module names in the comment ("adversarial", "ambiguous_intent", "capability_gap_stress"). A future operator reading the comment sees 8 modules when the constant says 8. |
</threat_model>

<verification>
- `bash scripts/verify-eval.sh` exits 0 with 8 EVAL-06 tables in stdout.
- `grep -c "^- \[x\] \*\*REWARD-0[1-7]\*\*" .planning/REQUIREMENTS.md` returns 7.
- `grep -q "Phase 23 ✅" .planning/STATE.md` exits 0.
- `grep -c '"verify:[a-z-]\+":' package.json` returns 33 (chain unchanged).
- All grep acceptance criteria above pass.
</verification>

<success_criteria>
- verify-eval.sh `EXPECTED=8` lands; gate exits 0 against the cargo test output of all 8 eval modules.
- verify chain count stays at 33 per RESEARCH §"Verify Gate Wiring" lock (gate extension, not new gate).
- REQUIREMENTS.md REWARD-01..07 traceability rows are all `[x]` with shipped-Plan citations and original text preserved.
- STATE.md reflects Phase 23 closed; v1.3 milestone progress advanced (2/7 → 3/7 phases shipped).
- Phase 23 is closed end-to-end; the next phase (24 — dream_mode skill consolidation) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/23-verifiable-reward-ood-eval/23-09-SUMMARY.md` per template AND a phase-level milestone-style summary at `.planning/phases/23-verifiable-reward-ood-eval/23-PHASE-CLOSE.md` capturing:
- Aggregate test count delta across the 9 plans
- Total LOC delta (Rust + TS)
- The 9 commit hashes (if available at close time)
- A 1-paragraph thesis verification: "Phase 23 ships RLVR-style composite reward at agent layer per M-10. Voyager-loop's skill_success contributes the 0.5-weight signal; Phase 16/17's eval gate contributes 0.3; the v1.3-stub acceptance + completion contribute 0.0/0.1; OOD failure gate fail-safes reward to 0 on >15% module drop; Doctor pane surfaces the trend. The substrate is observable, audit-trailed, and reaches user surface via the existing M-07 ActivityStrip channel."
- Confirmation the chain count is unchanged at 33.
</output>
