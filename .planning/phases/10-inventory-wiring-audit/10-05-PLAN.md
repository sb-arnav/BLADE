---
phase: 10-inventory-wiring-audit
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json
  - .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md
  - .planning/phases/10-inventory-wiring-audit/10-MODULES.yaml
  - .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml
  - .planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
tags:
  - audit
  - synthesis
  - tester-pass
  - appendix
  - phase-10
must_haves:
  truths:
    - "10-WIRING-AUDIT.json exists, validates against 10-WIRING-AUDIT.schema.json via verify script"
    - "10-WIRING-AUDIT.md is a single reviewable document with 5 sections + Appendix A + Appendix B"
    - "Every Rust module classified (AUDIT-01), every route classified (AUDIT-02), every config field cataloged (AUDIT-03)"
    - "NOT-WIRED backlog has file:line per backend entry point (AUDIT-04)"
    - "DEAD list carries deletion plan with callers + imports + safe_to_delete (AUDIT-05)"
    - "Appendix A maps all 7 tester-pass symptoms from v1-1-milestone-shape.md §'Why this framing' to catalog rows"
    - "Appendix B enumerates Deferred-to-v1.2 items (acting-tentacle NOT-WIRED, etc.) with one-line rationale each"
    - "verify:wiring-audit-shape passes; npm run verify:all stays green"
    - "Intermediate YAML files deleted (10-MODULES.yaml, 10-ROUTES.yaml, 10-CONFIG.yaml) per Phase 0 precedent"
  artifacts:
    - path: ".planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json"
      provides: "Machine-parseable audit output for Phase 14/15 scripts; zod-validated against schema"
      contains: "\"schema_version\": \"1.0.0\""
    - path: ".planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md"
      provides: "Human-readable monolithic audit; 5 sections + Appendix A + Appendix B"
      contains: "## Summary"
  key_links:
    - from: "10-WIRING-AUDIT.md §Appendix A"
      to: "notes/v1-1-milestone-shape.md §'Why this framing'"
      via: "Symptom-to-row cross-reference table"
      pattern: "Appendix A"
    - from: "10-WIRING-AUDIT.json §not_wired_backlog"
      to: "Phase 14 WIRE2 planning"
      via: "Consumable list of NOT-WIRED items with backend_entry_points"
      pattern: "not_wired_backlog"
    - from: "10-WIRING-AUDIT.json §dead_deletion_plan"
      to: "Phase 14 WIRE2-03 removal backlog"
      via: "DEAD items with safe_to_delete flag + callers/imports audit"
      pattern: "dead_deletion_plan"
    - from: "10-WIRING-AUDIT.json"
      to: "scripts/verify-wiring-audit-shape.mjs"
      via: "zod schema validation gate"
      pattern: "10-WIRING-AUDIT\\.json"
---

<objective>
Synthesize the three Wave 1 subagent YAML outputs (`10-MODULES.yaml`, `10-ROUTES.yaml`, `10-CONFIG.yaml`) into the canonical Phase 10 deliverables: `10-WIRING-AUDIT.json` (machine-parseable) + `10-WIRING-AUDIT.md` (human-readable). Cross-reference against `00-EMIT-AUDIT.md` (247 emit sites) + `verify-phase{5..8}-rust-surface.sh` (462 pre-verified ACTIVE commands). Write Appendix A (Tester-Pass Evidence Map — the 7 symptoms from `v1-1-milestone-shape.md` §"Why this framing") and Appendix B (Deferred-to-v1.2). Run the Wave 0 verify script. Delete the three intermediate YAML files.

Purpose: Ship the one reviewable artifact pair Phase 14 and Phase 15 consume. Phase 14 reads the NOT-WIRED backlog verbatim; Phase 15's `verify:feature-reachability` script parses the JSON sidecar; the human-readable Markdown is what the user reads.

Output: Two committed audit files + 3 deleted intermediate YAML files + updated ROADMAP.md plan count. Single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/notes/v1-1-milestone-shape.md
@.planning/phases/10-inventory-wiring-audit/10-CONTEXT.md
@.planning/phases/10-inventory-wiring-audit/10-RESEARCH.md
@.planning/phases/10-inventory-wiring-audit/10-PATTERNS.md
@.planning/phases/10-inventory-wiring-audit/10-VALIDATION.md
@.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.schema.json
@.planning/phases/10-inventory-wiring-audit/10-MODULES.yaml
@.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml
@.planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml
@.planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md
@.planning/phases/00-pre-rebuild-audit/00-BACKEND-EXTRACT.md
@scripts/verify-wiring-audit-shape.mjs
@scripts/verify-phase5-rust-surface.sh
@scripts/verify-phase6-rust-surface.sh
@scripts/verify-phase7-rust-surface.sh
@scripts/verify-phase8-rust-surface.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse 3 YAML artifacts, cross-reference, and synthesize 10-WIRING-AUDIT.json</name>
  <files>.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json</files>
  <read_first>
    - .planning/phases/10-inventory-wiring-audit/10-MODULES.yaml — full (Subagent A output)
    - .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml — full (Subagent B output)
    - .planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml — full (Subagent C output)
    - .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.schema.json — full (target schema)
    - .planning/phases/10-inventory-wiring-audit/10-RESEARCH.md — read lines 445-556 (§"JSON Sidecar Schema" — the canonical structure)
    - .planning/phases/10-inventory-wiring-audit/10-CONTEXT.md — read lines 23-47 (§D-46 output shape), lines 50-59 (§D-47 per-row depth)
    - .planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md — full (247 emit sites; cross-reference for module ACTIVE/NOT-WIRED classification on event-emitter side)
    - scripts/verify-phase5-rust-surface.sh — full (Phase 5 ACTIVE command list)
    - scripts/verify-phase6-rust-surface.sh — full (Phase 6 ACTIVE command list)
    - scripts/verify-phase7-rust-surface.sh — full (Phase 7 ACTIVE command list)
    - scripts/verify-phase8-rust-surface.sh — full (Phase 8 ACTIVE command list)
    - .planning/REQUIREMENTS.md — read "Inventory & Wiring Audit (AUDIT)" section + v1.2 deferred references (M-03 acting-tentacle)
    - .planning/notes/v1-1-milestone-shape.md — §"Why this framing" (the 7 tester-pass symptoms that seed Appendix A)
  </read_first>
  <action>
    **Synthesis is performed inline by the executor (no new subagent; this is Claude's synthesis work per D-50).**

    Step 1 — Parse YAML manually (no js-yaml install available per canonical_anchors). Use a lightweight Node parse approach: since js-yaml is not in devDependencies, read each YAML file with readFileSync and parse it by running a small inline Node script that shells out to `node --input-type=module -e` with js-yaml from a temporary install IS NOT available. **Use the following approach instead:** read each YAML via Read tool, then reconstruct the row arrays by parsing the YAML structure manually in your synthesis step (YAML is simple enough: `- field:` at column 3 starts a row, `<key>: <value>` at column 5 continues the row). Alternatively, ask Claude to emit a one-shot Node script that parses the YAML using a minimal hand-rolled parser for the subset of YAML the subagents emit (flow-style lists, single-line scalars, double-quoted strings, null). The hand-rolled parser is acceptable because the YAML surface is tiny and schema-constrained.

    A simpler and MORE ROBUST approach: **the executor reads each YAML file with the Read tool, then re-emits the structured data as JSON directly in a single Python one-liner OR a Node inline script** using pattern matching. Recommended: run `python3 -c "import yaml,json,sys; print(json.dumps(yaml.safe_load(open('<path>'))))"` — Python 3 with PyYAML is available on most dev systems. If PyYAML is missing, fall back to: emit a one-off Node script `scripts/_synthesize-audit.mjs` (deleted after the task) that re-implements a minimal YAML→JSON parser tailored to the subagent output schema (listed keys only: `modules`, `routes`, `windows`, `config`, `statics`, `env_vars`, `cargo_features`, `keyring_secrets`, plus the per-row leaf keys). Do not leave `_synthesize-audit.mjs` in the repo; delete after use.

    Step 2 — Load the three YAML documents into in-memory structures.

    Step 3 — Build the canonical JSON sidecar object:
    ```
    audit = {
      schema_version: "1.0.0",
      generated_at: new Date().toISOString(),
      modules: [],
      routes: [],
      config: [],
      not_wired_backlog: [],
      dead_deletion_plan: []
    }
    ```

    Step 4 — Populate `audit.modules`:
    - For each `MODULES.yaml::modules[i]`: copy `file`, `classification`, `purpose`, `trigger`, `ui_surface` (coerce to null if missing); include `commands`, `internal_callers`, `body_registry_entry`, `backend_entry_points`, `reachable_paths` where present.
    - **Cross-reference pass:** for every `commands[].name` (module-qualified), check membership in the union of `verify-phase5-rust-surface.sh` + `verify-phase6-rust-surface.sh` + `verify-phase7-rust-surface.sh` + `verify-phase8-rust-surface.sh` command lists. If a command is in that union AND the YAML classified the module NOT-WIRED, override to ACTIVE and add a `notes:` "Verified ACTIVE via verify-phase{N}-rust-surface.sh". Document the override count in the Plan 05 SUMMARY.
    - **Emit-audit cross-reference:** for every emit site in `00-EMIT-AUDIT.md`, check the module's classification. If module is NOT-WIRED but has an emit in CROSS_WINDOW rows, upgrade to ACTIVE with notes "Active via cross-window emit per 00-EMIT-AUDIT.md:<row>" (Pitfall 2).
    - Validate each row against the schema in the `<acceptance_criteria>` block below.

    Step 5 — Populate `audit.routes`:
    - For each `ROUTES.yaml::routes[i]`: copy all fields. Strip `component_file` (not in schema; fold into `file` if needed). Map `phase: null` → omit key.
    - Do NOT include `ROUTES.yaml::windows[]` in `audit.routes` — windows are not routes; keep them out of the JSON for now but surface under the Markdown's Section 2 (route catalog).

    Step 6 — Populate `audit.config`:
    - For each `CONFIG.yaml::config[i]`: copy `field`, `file`, `struct`, `disk_persisted`, `classification`, `ui_surface`, `control_type`.
    - **Note:** `CONFIG.yaml::statics`, `env_vars`, `cargo_features`, `keyring_secrets` are NOT in the `audit.config[]` schema (they don't match the `{field, file, struct, disk_persisted, ...}` shape). Two options:
      (a) Fold each of those blocks into `audit.config[]` by re-keying: `field: "static::RUNNING"` / `field: "env::ANTHROPIC_API_KEY"` / `field: "cargo_feature::local-whisper"` / `field: "keyring::blade.openai_api_key"`; OMIT the `struct` key entirely for these rows (do NOT set it to null — Plan 01's zod schema declares `struct: z.string().optional()` which accepts omission but rejects explicit null). This keeps a single `config[]` array for the schema.
      (b) Preserve them as Markdown-only content in Section 3 (Config Surface Catalog) without adding to JSON.
      **Chosen path: (a)** — fold everything into `audit.config[]` with the namespaced `field` prefix. This honors AUDIT-03's "every field catalogued" requirement and keeps the JSON schema simple. Set `disk_persisted: false` for statics/env/features/keyring (they're not BladeConfig-persisted). **Critical:** when emitting the JSON object for a non-struct row, DO NOT include a `struct` key at all — the key must be absent from the object literal, not present-with-null-value. Only `BladeConfig`/`DiskConfig` rows carry a `struct: "BladeConfig"` or `struct: "DiskConfig"` value. Use the `field: file` pattern requirement; for statics, use `src-tauri/src/<path>:<line>` which is the match for schema pattern `^src-tauri/.+:[0-9]+$`.

    Step 7 — Build `audit.not_wired_backlog`:
    - For each module with `classification: NOT-WIRED`: add row `{item_type: "module", identifier: <file>, backend_entry_points: <module.backend_entry_points>, phase_14_owner: "WIRE2", deferral_rationale: null}` — unless the module's `notes:` contains "deferred to v1.2", in which case set `phase_14_owner: "DEFERRED_V1_2"` and `deferral_rationale: <the notes text>`.
    - For each route with `classification: NOT-WIRED`: add row `{item_type: "route", identifier: <route.id>, backend_entry_points: [<route.file>:1], phase_14_owner: "WIRE2", deferral_rationale: null}`. (If the route has no line — use `:1` as a placeholder to satisfy the schema pattern `:[0-9]+$`.)
    - For each config with `classification: NOT-WIRED`: add row `{item_type: "config", identifier: <field>, backend_entry_points: [<file>], phase_14_owner: "WIRE2", deferral_rationale: null}`.
    - For each emit site in `00-EMIT-AUDIT.md` classified `single-window` whose event has no `useTauriEvent` subscriber: add row `{item_type: "event", identifier: <event_name>, backend_entry_points: [<emit_file:line>], phase_14_owner: "WIRE2"}`.
    - **Deferred-to-v1.2 gate (per canonical_anchors §6):** Explicitly enumerate acting-tentacle commands (any command matching patterns `*_reply`, `*_post`, `*_deploy`, `*_send`, `*_write`, `*_merge`, plus any module in the v2+ vision doc `notes/v2-vision-tentacles.md`) and flag them `phase_14_owner: "DEFERRED_V1_2"` with `deferral_rationale: "deferred to v1.2 — acting capability (M-03 observe-only guardrail)"`.

    Step 8 — Build `audit.dead_deletion_plan`:
    - For each module with `classification: DEAD`: add row `{identifier: <file>, callers: <module.callers>, imports: <module.imports>, safe_to_delete: <module.safe_to_delete>, deletion_note: <module.deletion_note>}`.
    - Empty arrays are OK if the module is truly orphan.

    Step 9 — Write `audit` as pretty-printed JSON (2-space indent) to `.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json`:
    ```
    writeFileSync(AUDIT_JSON_PATH, JSON.stringify(audit, null, 2) + '\n', 'utf8');
    ```

    Step 10 — Run the verify script:
    ```
    node scripts/verify-wiring-audit-shape.mjs
    ```
    If FAIL, inspect the violation message, fix the offending row(s) in the audit JSON, re-run. Do NOT weaken the schema; fix the data.

    **Constraints:**
    - The JSON is the canonical machine output — EVERY row validates against the schema pattern/enum constraints.
    - Use `python3 -c "import yaml..."` OR a hand-rolled YAML parser; do NOT `npm install js-yaml` — research assumes no npm installs in this phase.
    - If PyYAML is not available, the hand-rolled parser must handle: single-value `key: value`, null (`key: null`), lists (`- key: value`), nested lists (`field: [...]` inline), string quoting (`"..."`), empty string. The subagent output schema is narrow enough to parse reliably.
  </action>
  <verify>
    <automated>node scripts/verify-wiring-audit-shape.mjs && test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json && node -e "const a=JSON.parse(require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json','utf8'));if(a.schema_version!=='1.0.0')throw new Error('wrong schema_version');if(!Array.isArray(a.modules)||a.modules.length<120)throw new Error('module count too low: '+a.modules.length);if(!Array.isArray(a.routes)||a.routes.length<20)throw new Error('route count too low');if(!Array.isArray(a.config)||a.config.length<30)throw new Error('config count too low');console.log('audit.json ok —',a.modules.length,'modules,',a.routes.length,'routes,',a.config.length,'config,',a.not_wired_backlog.length,'not-wired,',a.dead_deletion_plan.length,'dead');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json`
    - Parses as JSON: the automated command exits 0
    - schema_version equals "1.0.0"
    - Module rows ≥120; route rows ≥20; config rows ≥30
    - `verify:wiring-audit-shape` exits 0 (default run covers modules + routes + config + not-wired + dead)
    - All 7 top-level keys present: `node -e "const a=JSON.parse(require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json','utf8'));for(const k of ['schema_version','generated_at','modules','routes','config','not_wired_backlog','dead_deletion_plan'])if(!(k in a))throw new Error('missing '+k);"` exits 0
    - NOT-WIRED backlog non-empty (Phase 14 has work): `node -e "const a=JSON.parse(require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json','utf8'));if(a.not_wired_backlog.length===0)throw new Error('empty not_wired_backlog — seed failed');"` exits 0
    - DEFERRED_V1_2 rows present for acting-tentacle surfaces: `node -e "const a=JSON.parse(require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json','utf8'));const deferred=a.not_wired_backlog.filter(r=>r.phase_14_owner==='DEFERRED_V1_2');if(deferred.length===0)console.warn('no DEFERRED_V1_2 rows — confirm manually that no acting-tentacle commands exist');else console.log(deferred.length,'deferred rows');"` exits 0 (warn-only, not fail — acceptable if v1.1 has no acting-tentacle candidates)
  </acceptance_criteria>
  <done>`10-WIRING-AUDIT.json` exists, validates against the JSON Schema via the zod-based verify script, contains non-empty `modules`, `routes`, `config`, `not_wired_backlog` arrays, and schema_version equals "1.0.0".</done>
</task>

<task type="auto">
  <name>Task 2: Render 10-WIRING-AUDIT.md (5 sections + Appendix A + Appendix B)</name>
  <files>.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md</files>
  <read_first>
    - .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json — full (source of truth for the Markdown tables)
    - .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml — full (windows: block — surfaces in Section 2 only, not in JSON)
    - .planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml — full (statics/env_vars/cargo_features/keyring_secrets — include as Markdown sub-tables in Section 3)
    - .planning/phases/10-inventory-wiring-audit/10-CONTEXT.md — read lines 24-37 (§D-46 section structure)
    - .planning/phases/10-inventory-wiring-audit/10-PATTERNS.md — read lines 35-98 (§"1. 10-WIRING-AUDIT.md" header + section templates)
    - .planning/phases/00-pre-rebuild-audit/00-EMIT-AUDIT.md — read lines 1-30 (header format to mirror)
    - .planning/phases/00-pre-rebuild-audit/00-BACKEND-EXTRACT.md — read lines 1-30 (numbered-section spine format)
    - .planning/notes/v1-1-milestone-shape.md — read §"Why this framing" (7 tester-pass symptoms that seed Appendix A)
    - .planning/REQUIREMENTS.md — read the "Inventory & Wiring Audit (AUDIT)" section (AUDIT-01..05) — cite in header
  </read_first>
  <action>
    Write `.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md` rendering the JSON + YAML content into a single reviewable Markdown document. Mirror the Phase 0 `00-EMIT-AUDIT.md` header-prose-summary-table spine.

    **Document structure:**

    ```markdown
    # Phase 10 — Inventory & Wiring Audit (resolves AUDIT-01..05)

    > Scanned: `src-tauri/src/**/*.rs`, `src/windows/main/router.ts`, `src/features/*/index.tsx`, `src-tauri/src/config.rs`, `src-tauri/Cargo.toml`
    > Policy: D-48 — ACTIVE (invoked + subscribed), WIRED-NOT-USED (UI exists, backend silent), NOT-WIRED (backend exists, no UI), DEAD (no callers + not in v1.1 roadmap)
    > Note: Read-only audit. No code changes. `10-WIRING-AUDIT.json` sidecar is the machine-parseable source of truth; this Markdown is the human view.
    > Sidecar: `.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json` (validates against `10-WIRING-AUDIT.schema.json` via `npm run verify:wiring-audit-shape`).

    ## Summary

    - Total Rust modules classified: {audit.modules.length}
    - Total routes classified: {audit.routes.length}
    - Total config surfaces cataloged: {audit.config.length}
    - NOT-WIRED backlog: {audit.not_wired_backlog.length} items → Phase 14 consumes verbatim
    - DEAD deletion plan: {audit.dead_deletion_plan.length} items → Phase 14 removes safely
    - Classifications:
      - Modules: N ACTIVE, M WIRED-NOT-USED, K NOT-WIRED, J DEAD
      - Routes:  N ACTIVE, M ACTIVE (dev-only), K WIRED-NOT-USED, L NOT-WIRED
      - Config:  N ACTIVE, M WIRED-NOT-USED, K NOT-WIRED, L DEAD
    - Deferred-to-v1.2 (Appendix B): {count of DEFERRED_V1_2 rows} items

    ---

    ## 1. Module Catalog

    Source: `10-WIRING-AUDIT.json::modules[]`. Every `.rs` file under `src-tauri/src/` (excluding `build.rs`). Nested `agents/` subdirectory flat-listed.

    | file | classification | purpose | trigger | ui_surface |
    |------|----------------|---------|---------|------------|
    | src-tauri/src/commands.rs | ACTIVE | Main chat pipeline | `#[tauri::command]` send_message_stream, cancel_chat, ... | src/features/chat/useChat.tsx |
    | ... |

    (emit every row from `audit.modules[]`; sort alphabetically by `file` for scan-ability)

    ---

    ## 2. Route + Command-Palette Catalog

    Source: `10-WIRING-AUDIT.json::routes[]` (50+ rows) + 4 non-main window shells (from `10-ROUTES.yaml::windows[]`, not in JSON).

    Correction vs `10-CONTEXT.md §D-49`: CommandPalette is mounted ONLY in `src/windows/main/MainShell.tsx`. Palette entries = `ROUTE_MAP` filtered by `paletteHidden !== true`. The 4 non-main windows do not host palettes.

    ### 2a. Routes (palette-eligible)

    | id | file | classification | section | palette_visible | shortcut | data_shape | flow_status |
    |----|------|----------------|---------|-----------------|----------|------------|-------------|
    | dashboard | src/features/dashboard/index.tsx | ACTIVE | core | ✓ | Mod+1 | DashSnapshot | data pipes |
    | ... |

    ### 2b. Window Shells (no palette by design)

    | label | file | component | classification | notes |
    |-------|------|-----------|----------------|-------|
    | quickask | src/windows/quickask/main.tsx | src/features/quickask/QuickAskWindow.tsx | ACTIVE | Overlay window; no palette by design |
    | hud | src/windows/hud/main.tsx | src/features/hud/HudWindow.tsx | ACTIVE | HUD strip; no palette |
    | ghost | src/windows/ghost/main.tsx | src/features/ghost/GhostOverlayWindow.tsx | ACTIVE | Content-protected overlay; no palette |
    | overlay | src/windows/overlay/main.tsx | src/features/voice/VoiceOrbWindow.tsx | ACTIVE | Voice orb overlay; no palette |

    ---

    ## 3. Config Surface Catalog

    Source: `10-WIRING-AUDIT.json::config[]` (folded rows) + supplementary tables below for non-field surfaces.

    ### 3a. BladeConfig + DiskConfig struct fields

    | field | file:line | struct | disk_persisted | classification | ui_surface | control_type |
    |-------|-----------|--------|----------------|----------------|------------|--------------|
    | BladeConfig.api_keys | src-tauri/src/config.rs:180 | BladeConfig | ✓ | ACTIVE | src/features/settings/Providers.tsx | map |
    | BladeConfig.vision_provider | src-tauri/src/config.rs:245 | BladeConfig | ✗ | WIRED-NOT-USED (6-place gap) | — | string |
    | ... |

    Rows with `disk_persisted: false` in `BladeConfig` (not `DiskConfig`) are flagged as 6-place-rule violations per Pitfall 8. Phase 14 WIRE2 agenda.

    ### 3b. Static AtomicBool / Lazy toggles (non-field config)

    (from `10-CONFIG.yaml::statics[]`; not in JSON schema but surfaced here for completeness)

    | name | file:line | type | default | toggled_by | classification |
    |------|-----------|------|---------|------------|----------------|
    | RUNNING | src-tauri/src/godmode.rs:42 | AtomicBool | false | godmode::start_god_mode | ACTIVE (internal) |
    | ... |

    ### 3c. Environment variables

    (from `10-CONFIG.yaml::env_vars[]`)

    | name | file:line | read_by | classification | ui_surface |
    |------|-----------|---------|----------------|------------|
    | ANTHROPIC_API_KEY | src-tauri/src/providers/anthropic.rs:38 | providers::anthropic::get_client | WIRED-NOT-USED | — |
    | ... |

    ### 3d. Cargo feature flags

    (from `10-CONFIG.yaml::cargo_features[]`)

    | name | file:line | default_enabled | gated_modules | classification |
    |------|-----------|-----------------|----------------|----------------|
    | local-whisper | src-tauri/Cargo.toml:62 | ✗ | whisper_local.rs | NOT-WIRED |

    ### 3e. Keyring-stored secrets (location only)

    | service | key | file:line | storage_location |
    |---------|-----|-----------|-------------------|
    | blade | openai_api_key | src-tauri/src/config.rs:420 | keyring |
    | ... |

    ---

    ## 4. NOT-WIRED Backlog

    Source: `10-WIRING-AUDIT.json::not_wired_backlog[]`. Phase 14 WIRE2 consumes verbatim.

    | item_type | identifier | backend_entry_points | phase_14_owner | deferral_rationale |
    |-----------|-----------|----------------------|-----------------|--------------------|
    | module | src-tauri/src/self_upgrade.rs | src-tauri/src/self_upgrade.rs:42, ...118 | WIRE2 | — |
    | route | chat-history | src/features/chat/ChatHistory.tsx:1 | WIRE2 | — |
    | config | BladeConfig.vision_provider | src-tauri/src/config.rs:245 | WIRE2 | 6-place gap; add to DiskConfig |
    | event | meeting_ended_ghost | src-tauri/src/ghost_mode.rs:187 | WIRE2 | — |
    | module | src-tauri/src/auto_reply.rs | src-tauri/src/auto_reply.rs:60 | DEFERRED_V1_2 | deferred to v1.2 — acting capability (M-03 observe-only guardrail) |
    | ... |

    Phase 14 owners:
    - `WIRE2` — standard wiring task (add UI surface or fix broken invoke)
    - `A11Y2` — accessibility-related (screen-reader label, focus trap)
    - `LOG` — activity-log instrumentation (LOG-02)
    - `DENSITY` — empty-state polish (DENSITY-05/06)
    - `DEFERRED_V1_2` — deferred per M-03; do NOT wire in v1.1

    ---

    ## 5. DEAD Deletion Plan

    Source: `10-WIRING-AUDIT.json::dead_deletion_plan[]`. Phase 14 removal backlog.

    | identifier | callers | imports | safe_to_delete | deletion_note |
    |-----------|---------|---------|----------------|----------------|
    | src-tauri/src/zzz_example_orphan.rs | (none) | std::sync::atomic::AtomicBool | ✓ | No callers; not in body_registry; not in REQUIREMENTS.md |
    | ... |

    DEAD classification per D-48: no `invoke` callers, no `listen` subscribers, no internal Rust callers, AND not referenced in roadmap/requirements for v1.1 or v1.2. Borderline items remain NOT-WIRED in Section 4 with a `deferred to v1.2` note — they are NOT listed here.

    ---

    ## Appendix A — Tester-Pass Evidence Map

    Cross-references the 7 symptoms from `.planning/notes/v1-1-milestone-shape.md` §"Why this framing" to catalog rows above. Grounds the audit in falsifiable tester-observed reality.

    | # | Symptom | Classification | Catalog row (§/table:row) | Rationale |
    |---|---------|----------------|---------------------------|-----------|
    | 1 | Chat broken for first message (silent failure) | — | §1 `src-tauri/src/commands.rs` (ACTIVE) | Fixed by commit `4ab464c`; `chat_error` BLADE_EVENTS key present + frontend subscriber confirmed in `src/lib/events/index.ts:37` and `src/features/chat/useChat.tsx` |
    | 2 | Deep scan found 1 repo (dumb scanner) | NOT-WIRED (scanner capability) | §1 `src-tauri/src/deep_scan.rs`, `indexer.rs`, `file_indexer.rs` (ACTIVE command registered but scanner logic is single-source-class) + §4 `scan-profile-editor` route NOT-WIRED | Scanner quality is Phase 12 scope; audit flags the under-wired surface for Phase 12 planning |
    | 3 | Dashboard pages feel empty | WIRED-NOT-USED | §2a `dashboard` (ACTIVE — data pipes) + §1 `perception_fusion.rs` (ACTIVE) + `typed_memory.rs` (ACTIVE) | Data source exists but real data is sparse until Phase 12 scan ships; DENSITY-05/07 (Phase 15) + WIRE2-02 (Phase 14) consume |
    | 4 | Background terminal noise, no in-UI activity surface | NOT-WIRED (emit coverage) | §3b 34+ `AtomicBool` control loops + §4 activity-log event surfaces | LOG-02 (Phase 14) consumes; `Activity Log strip` is load-bearing (M-07) |
    | 5 | UI cluttered, no pad/breathing room | — | Out of Phase 10 scope | Phase 15 DENSITY pass |
    | 6 | Options tester expected weren't reachable | NOT-WIRED (config) | §3a rows with `ui_surface: null` + §4 config-type rows | Subagent C primary finding set; Phase 14 WIRE2 wires Settings surfaces |
    | 7 | Groq + llama produced nothing useful (no capability-aware routing) | WIRED-NOT-USED | §1 `router.rs` (ACTIVE — routing logic exists but lacks per-capability tiers) + `providers/mod.rs` (ACTIVE) + §3a `BladeConfig.vision_provider` NOT-WIRED (6-place gap) | PROV-06/09 (Phase 11) consumes; audit pre-seeds the `vision_provider` / `audio_provider` / `long_context_provider` / `tools_provider` fields as 6-place gaps |

    Verified: `4ab464c` is the only tester-pass commit currently on master (per `git log --all --oneline --grep=tester`).

    ---

    ## Appendix B — Deferred-to-v1.2 Rationale

    Items marked `phase_14_owner: "DEFERRED_V1_2"` in Section 4. Phase 14 does NOT wire these in v1.1; Phase 14 planning refers to Appendix B by reference (no re-arguing).

    | identifier | item_type | rationale | v1.1 policy |
    |-----------|-----------|-----------|-------------|
    | src-tauri/src/auto_reply.rs | module | Acting-tentacle capability (write path to external recipient) | NOT-WIRED in v1.1; M-03 observe-only guardrail enforces runtime block |
    | ... (list every DEFERRED_V1_2 row from Section 4) |

    M-03 references: `.planning/STATE.md §"v1.1 Decisions"` + `.planning/notes/v1-1-milestone-shape.md` §"What we're explicitly not doing in v1.1".

    ---

    ## Meta-findings

    - `CLAUDE.md §"New route — 3 places in App.tsx"` is outdated — the single `App.tsx` no longer exists. Per-feature `routes: RouteDefinition[]` export + `src/windows/main/router.ts` aggregation is the current pattern. Recommend a one-line edit to `./CLAUDE.md` in Phase 14 cleanup. (Not in any backlog; meta-note only.)
    - (add any other discoveries the synthesis surfaces — e.g. modules that appear in `body_registry.rs` but are not called from anywhere, emit names that conflict with D-14 routing policy, etc.)

    ---

    *Audit produced: 2026-04-20. No source code modified (read-only phase per D-50). {audit.modules.length} modules + {audit.routes.length} routes + {audit.config.length} config surfaces classified. Sidecar at `10-WIRING-AUDIT.json`.*
    ```

    **Fill rules:**
    - Replace `{audit.modules.length}` etc. with actual counts from the JSON.
    - Sort tables alphabetically by primary key (file for modules, id for routes, field for config) — scan-ability matters.
    - For large tables (>100 rows), no pagination — one long table. Phase 0 precedent: `00-EMIT-AUDIT.md` has 247 rows in one table.
    - Use `—` (em dash) for empty cells, not `null` or blank.
    - Checkmarks: `✓` for true/present, `✗` for false/absent, `—` for N/A.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md && node -e "const m=require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md','utf8');for(const h of ['## Summary','## 1. Module Catalog','## 2. Route + Command-Palette Catalog','## 3. Config Surface Catalog','## 4. NOT-WIRED Backlog','## 5. DEAD Deletion Plan','## Appendix A','## Appendix B'])if(!m.includes(h))throw new Error('missing section: '+h);for(let i=1;i<=7;i++){if(!m.match(new RegExp('\\\\|\\\\s*'+i+'\\\\s*\\\\|')))throw new Error('missing tester-pass symptom '+i);}console.log('MD ok — 5 sections + 2 appendices + 7 tester-pass rows');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md`
    - Has all 5 numbered sections + Summary + Appendix A + Appendix B: the automated command asserts each H2 heading presence
    - Header block cites AUDIT-01..05: `grep -c "AUDIT-01..05" .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md` returns ≥1
    - Appendix A enumerates all 7 tester-pass symptoms (one row per symptom numbered 1-7): automated command checks for `| 1 |` through `| 7 |` row markers
    - §2 includes the correction about palette-being-main-only: `grep -c "CommandPalette is mounted ONLY" .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md` returns ≥1
    - §3 has subsections 3a-3e for struct fields, statics, env vars, cargo features, keyring: `grep -cE "^### 3[a-e]\\." .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md` returns 5
    - §5 only contains DEAD items (not "deferred to v1.2"): `grep -c "deferred to v1.2" .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md` — MUST find matches in §4 and Appendix B, but NOT in §5. Validate by reading §5 content manually during task.
    - Trailing metadata block present: `tail -5 .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md | grep -c "Audit produced:"` returns 1
  </acceptance_criteria>
  <done>`10-WIRING-AUDIT.md` exists with all 5 sections + Appendix A (7 tester-pass symptoms) + Appendix B (deferred-to-v1.2 rationale) + trailing metadata. Every table cell rendered from `10-WIRING-AUDIT.json` + `10-CONFIG.yaml` non-schema blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verify chain, delete intermediate YAMLs, update ROADMAP, commit</name>
  <files>
    .planning/phases/10-inventory-wiring-audit/10-MODULES.yaml,
    .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml,
    .planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/ROADMAP.md — read "Phase 10: Inventory & Wiring Audit" section (update Plans count + plan list)
    - .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json — full (must exist before cleanup)
    - .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md — full (must exist before cleanup)
  </read_first>
  <action>
    Step 1 — Run the full gate chain: `npm run verify:all`. If any gate fails, halt and surface. Expected: 19/19 green (18 baseline + new `verify:wiring-audit-shape`).

    Step 2 — Delete the three intermediate YAML files (Phase 0 precedent — they are not committed long-term; the canonical artifacts are the .md + .json + .schema.json):
    ```
    rm .planning/phases/10-inventory-wiring-audit/10-MODULES.yaml
    rm .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml
    rm .planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml
    ```
    (These were produced by Plans 02-04 and read by Task 1 of Plan 05; they are not part of the long-lived audit output.)

    Step 3 — Update `.planning/ROADMAP.md` Phase 10 section:
    - `**Plans:** {N} plans` → `**Plans:** 5 plans`
    - Replace the `Plans: TBD (generated via /gsd-plan-phase 10)` line with:
      ```
      Plans:
      - [ ] 10-01-PLAN.md — Wave 0 verify script + JSON Schema + package.json wiring
      - [ ] 10-02-PLAN.md — Wave 1 Subagent A: Rust module classifier → 10-MODULES.yaml
      - [ ] 10-03-PLAN.md — Wave 1 Subagent B: Route + command-palette mapper → 10-ROUTES.yaml
      - [ ] 10-04-PLAN.md — Wave 1 Subagent C: Config surface catalog → 10-CONFIG.yaml
      - [ ] 10-05-PLAN.md — Wave 2 Synthesis: merge YAMLs → 10-WIRING-AUDIT.json + .md, Appendix A + B, cleanup
      ```

    Step 4 — Final verify: `npm run verify:all && node scripts/verify-wiring-audit-shape.mjs`. Both exit 0.

    Step 5 — Run the AUDIT-01..05 requirement self-check manually:
    - AUDIT-01: `jq '.modules | length' 10-WIRING-AUDIT.json` returns count > 120 ✓
    - AUDIT-02: `jq '.routes | length' 10-WIRING-AUDIT.json` returns count > 20 ✓
    - AUDIT-03: `jq '.config | length' 10-WIRING-AUDIT.json` returns count > 30 ✓
    - AUDIT-04: `jq '.not_wired_backlog | length' 10-WIRING-AUDIT.json` returns count > 0 AND every entry has `backend_entry_points` non-empty ✓
    - AUDIT-05: `jq '.dead_deletion_plan | length' 10-WIRING-AUDIT.json` returns count ≥ 0 AND every entry has `callers`, `imports`, `safe_to_delete` fields ✓
    (If jq is not available, replace with the node one-liners. These are validations the verify script already enforces, but the self-check confirms the requirement IDs are directly traceable.)

    Step 6 — Commit atomically (single commit per canonical_anchors §Claude's Discretion 3):
    ```
    git add .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json \
            .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md \
            .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.schema.json \
            scripts/verify-wiring-audit-shape.mjs \
            package.json \
            .planning/ROADMAP.md

    # Stage the deletions of the intermediate YAMLs
    git add -u .planning/phases/10-inventory-wiring-audit/

    git commit -m "docs(10): ship inventory & wiring audit (AUDIT-01..05)

    - Add 10-WIRING-AUDIT.md (5 sections + Appendix A/B)
    - Add 10-WIRING-AUDIT.json (machine-parseable sidecar, schema 1.0.0)
    - Add 10-WIRING-AUDIT.schema.json (JSON Schema Draft 2020-12)
    - Add scripts/verify-wiring-audit-shape.mjs (Wave 0 integrity gate)
    - Wire verify:wiring-audit-shape into verify:all chain
    - Update ROADMAP.md Phase 10 plan count

    Appendix A maps all 7 tester-pass symptoms from v1-1-milestone-shape.md.
    Appendix B enumerates Deferred-to-v1.2 items (acting-tentacle surfaces).

    Intermediate YAMLs (10-MODULES.yaml, 10-ROUTES.yaml, 10-CONFIG.yaml) deleted
    per Phase 0 precedent — canonical artifacts are .md + .json + .schema.json.

    No source code modified (read-only phase per D-50)."
    ```

    **Do NOT:** add `Co-Authored-By` line (CLAUDE.md rule — Arnav is sole author).
    **Do NOT:** run `cargo check` (zero Rust changes in this phase).
    **Do NOT:** skip the verify chain (AUDIT-04/05 depend on the shape gate passing).
  </action>
  <verify>
    <automated>npm run verify:all && test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.md && test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.json && test -f .planning/phases/10-inventory-wiring-audit/10-WIRING-AUDIT.schema.json && test ! -f .planning/phases/10-inventory-wiring-audit/10-MODULES.yaml && test ! -f .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml && test ! -f .planning/phases/10-inventory-wiring-audit/10-CONFIG.yaml && grep -c "10-01-PLAN.md" .planning/ROADMAP.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:all` exits 0 (all 19 gates green)
    - Canonical artifacts exist: `test -f 10-WIRING-AUDIT.md && test -f 10-WIRING-AUDIT.json && test -f 10-WIRING-AUDIT.schema.json` exits 0
    - Intermediate YAMLs deleted: `test ! -f 10-MODULES.yaml && test ! -f 10-ROUTES.yaml && test ! -f 10-CONFIG.yaml` exits 0
    - ROADMAP.md updated: `grep -c "10-01-PLAN.md" .planning/ROADMAP.md` returns ≥1
    - Single atomic commit: `git log -1 --format='%s' | grep -c "docs(10)"` returns 1
    - No Co-Authored-By in commit: `git log -1 --format='%B' | grep -c "Co-Authored-By"` returns 0
    - `.git status` clean after commit: `git status --porcelain | wc -l` returns 0 (or only non-tracked items)
  </acceptance_criteria>
  <done>All 19 verify gates green; canonical artifacts exist; intermediate YAMLs deleted; ROADMAP.md updated with 5 plan entries; single atomic commit on master with the docs(10) prefix and no Co-Authored-By.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — synthesis-only plan. Reads YAML/JSON intermediate artifacts and emits Markdown + JSON under `.planning/`. Deletes intermediate YAML files. Updates ROADMAP.md. No network, no IPC, no user input, no auth/authz, no external API.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | N/A | 10-WIRING-AUDIT.{md,json} | accept | Read-only audit produces only Markdown + JSON artifacts in `.planning/`. No code changes, no data exposure surfaces, no auth/authz, no external API contact, no user input handling. Threat model deferred to phases 11+ that ship runtime behavior. |
</threat_model>

<verification>
After Plan 05 completes:
- `npm run verify:all` exits 0 (19/19 gates green — 18 baseline + new `verify:wiring-audit-shape`)
- `10-WIRING-AUDIT.md` + `10-WIRING-AUDIT.json` + `10-WIRING-AUDIT.schema.json` all exist under the phase directory
- `10-MODULES.yaml` + `10-ROUTES.yaml` + `10-CONFIG.yaml` all removed (intermediate artifacts deleted)
- ROADMAP.md Phase 10 section lists all 5 plan files by name
- Single atomic commit on master: `docs(10): ship inventory & wiring audit (AUDIT-01..05)` with NO Co-Authored-By line
- `git status --porcelain` clean
</verification>

<success_criteria>
1. `10-WIRING-AUDIT.json` validates against `10-WIRING-AUDIT.schema.json` via `scripts/verify-wiring-audit-shape.mjs`.
2. `10-WIRING-AUDIT.md` contains 5 sections + Summary + Appendix A + Appendix B + meta-findings + trailing metadata.
3. Appendix A maps all 7 tester-pass symptoms from `notes/v1-1-milestone-shape.md §"Why this framing"` to catalog rows.
4. Appendix B enumerates all DEFERRED_V1_2 items with one-line rationale.
5. Intermediate YAMLs (10-MODULES.yaml, 10-ROUTES.yaml, 10-CONFIG.yaml) deleted post-synthesis.
6. ROADMAP.md Phase 10 plan list updated.
7. `npm run verify:all` exits 0.
8. Single atomic commit on master (no Co-Authored-By).
9. AUDIT-01..05 requirement IDs all traceable to concrete JSON sections + Markdown rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-inventory-wiring-audit/10-05-SUMMARY.md` per the SUMMARY template. Include:
- Full row counts per classification across all 3 dimensions (modules / routes / config)
- Cross-reference override count (how many modules were re-classified ACTIVE due to `verify-phase{5..8}-rust-surface.sh` membership despite subagent A classifying NOT-WIRED)
- Tester-pass evidence rows in Appendix A (confirm all 7 symptoms have at least one row reference)
- Deferred-to-v1.2 count (Appendix B items)
- Verify gate count baseline (18 → 19 → confirm 19/19 green)
- Note: Phase 14 consumes the `not_wired_backlog` array verbatim; Phase 15 `verify:feature-reachability` locks against the schema version 1.0.0
</output>
