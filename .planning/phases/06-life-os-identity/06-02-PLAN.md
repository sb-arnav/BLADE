---
phase: 06-life-os-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tauri/life_os.ts
  - src/lib/tauri/identity.ts
  - src/lib/tauri/index.ts
  - src/features/life-os/index.tsx
  - src/features/identity/index.tsx
  - src/features/life-os/types.ts
  - src/features/identity/types.ts
  - src/features/life-os/life-os.css
  - src/features/identity/identity.css
  - src/features/life-os/HealthView.tsx
  - src/features/life-os/FinanceView.tsx
  - src/features/life-os/GoalView.tsx
  - src/features/life-os/HabitView.tsx
  - src/features/life-os/MeetingsView.tsx
  - src/features/life-os/SocialGraphView.tsx
  - src/features/life-os/PredictionsView.tsx
  - src/features/life-os/EmotionalIntelView.tsx
  - src/features/life-os/AccountabilityView.tsx
  - src/features/identity/SoulView.tsx
  - src/features/identity/PersonaView.tsx
  - src/features/identity/CharacterBible.tsx
  - src/features/identity/NegotiationView.tsx
  - src/features/identity/ReasoningView.tsx
  - src/features/identity/ContextEngineView.tsx
  - src/features/identity/SidecarView.tsx
autonomous: true
requirements:
  - LIFE-01  # HealthView — requires healthGetToday, healthUpdateToday, healthGetStats, healthStreakInfo, streakGetStats
  - LIFE-02  # FinanceView — requires financeGetSnapshot, financeGetTransactions, financeImportCsv, etc.
  - LIFE-03  # GoalView — requires goalList, goalAdd, goalPursueNow
  - LIFE-04  # HabitView — requires habitGetToday, habitList, habitComplete
  - LIFE-05  # MeetingsView — requires meetingList, meetingGet, meetingGetActionItems, temporalMeetingPrep
  - LIFE-06  # SocialGraphView — requires socialListContacts, socialGetContact, socialGetInteractions
  - LIFE-07  # PredictionsView — requires predictionGetPending, predictionAccept, predictionDismiss, learningGetPredictions
  - LIFE-08  # EmotionalIntelView — requires emotionGetCurrent, emotionGetTrend, emotionGetReadings
  - LIFE-09  # AccountabilityView — requires accountabilityDailyPlan, accountabilityGetObjectives
  - IDEN-01  # SoulView — requires soulGetState, getCharacterBible, getUserProfile
  - IDEN-02  # PersonaView — requires personaGetTraits, personaGetRelationship, getUserModel, peopleList
  - IDEN-03  # CharacterBible — requires getCharacterBible, updateCharacterSection, applyReactionToTraits
  - IDEN-04  # NegotiationView — requires negotiationBuildArgument, negotiationStartDebate, etc.
  - IDEN-05  # ReasoningView — requires reasoningThink, reasoningDecompose, reasoningGetTraces
  - IDEN-06  # ContextEngineView — requires contextAssemble, contextClearCache
  - IDEN-07  # SidecarView — requires sidecarListDevices, sidecarPingDevice, kaliRecon, etc.
must_haves:
  truths:
    - "src/lib/tauri/life_os.ts exports ~110 camelCase wrapper functions spanning 14 Rust modules — health_tracker, health, health_guardian, financial_brain, goal_engine, habit_engine, meeting_intelligence, social_graph, prediction_engine, emotional_intelligence, accountability, streak_stats, people_graph, learning_engine, temporal_intel (D-140 inventory)"
    - "src/lib/tauri/identity.ts exports ~40 camelCase wrapper functions spanning 9 Rust modules — character, soul_commands, persona_engine, negotiation_engine, reasoning_engine, context_engine, sidecar, personality_mirror, kali (D-140 inventory)"
    - "Both wrapper files use invokeTyped<TReturn, TArgs>(command, args); raw invoke NEVER used (ESLint no-raw-tauri passes)"
    - "Each wrapper has JSDoc @see src-tauri/src/<file>.rs citation matching the Rust #[tauri::command] (D-159 / D-38 naming discipline)"
    - "src/features/life-os/index.tsx REWRITTEN — 9 lazy imports of per-route files — each component file created with minimal GlassPanel placeholder (Plans 06-03/06-04 fill in the bodies)"
    - "src/features/identity/index.tsx REWRITTEN — 7 lazy imports of per-route files — each component file created with minimal placeholder"
    - "Both cluster index.tsx files preserve route id order and RouteDefinition[] shape (D-143 single-writer discipline)"
    - "Per-route files are MINIMAL placeholders — each exports a named component that renders a GlassPanel wrapper with route label + 'Ships in Plan 06-NN' placeholder text. Plans 06-03..06 REPLACE these placeholder bodies"
    - "src/features/life-os/types.ts + src/features/identity/types.ts re-export cluster-local types from the wrappers + cluster-only UI types — D-161"
    - "src/features/life-os/life-os.css + src/features/identity/identity.css created with shared layer discipline (D-164) + rgba-bg card classes (D-70) + reuse Phase 5 status tokens (D-132)"
    - "src/lib/tauri/index.ts barrel re-exports both new modules for convenience (consistent with Phase 5 pattern)"
    - "npx tsc --noEmit passes; ESLint passes; npm run verify:all passes; npm run dev builds without error (route lazy imports resolve)"
    - "Zero Rust changes (D-140 + D-144)"
  artifacts:
    - path: "src/lib/tauri/life_os.ts"
      provides: "Typed wrappers for ~110 Life OS commands"
      exports: ["healthLog", "healthGetToday", "healthUpdateToday", "healthGetLogs", "healthGetStats", "healthGetInsights", "healthGetContext", "healthCorrelateProductivity", "healthStreakInfo", "healthGetScan", "healthScanNow", "healthSummaryAll", "healthGuardianStats", "healthTakeBreak", "financeAddTransaction", "financeGetTransactions", "financeDeleteTransaction", "financeGetSnapshot", "financeGenerateInsights", "financeGetGoals", "financeCreateGoal", "financeUpdateGoal", "financeInvestmentSuggestions", "financeBudgetRecommendation", "financeGetContext", "financeImportCsv", "financeAutoCategorize", "financeSpendingSummary", "financeDetectSubscriptions", "goalAdd", "goalList", "goalComplete", "goalDelete", "goalUpdatePriority", "goalPursueNow", "habitCreate", "habitList", "habitGet", "habitComplete", "habitSkip", "habitGetLogs", "habitGetToday", "habitInsights", "habitSuggestDesign", "habitGetContext", "meetingProcess", "meetingGet", "meetingList", "meetingSearch", "meetingDelete", "meetingGetActionItems", "meetingCompleteAction", "meetingFollowUpEmail", "meetingCompare", "meetingRecurringThemes", "socialAddContact", "socialGetContact", "socialSearchContacts", "socialUpdateContact", "socialDeleteContact", "socialListContacts", "socialLogInteraction", "socialGetInteractions", "socialAnalyzeInteraction", "socialGetInsights", "socialHowToApproach", "predictionGetPending", "predictionAccept", "predictionDismiss", "predictionGenerateNow", "predictionContextual", "predictionGetPatterns", "emotionGetCurrent", "emotionGetTrend", "emotionGetReadings", "emotionAnalyzePatterns", "emotionGetContext", "accountabilityGetObjectives", "accountabilityCreateObjective", "accountabilityUpdateKr", "accountabilityDailyPlan", "accountabilityCompleteAction", "accountabilityCheckin", "accountabilityProgressReport", "accountabilityGetDailyActions", "streakGetStats", "streakRecordActivity", "streakGetDisplay", "peopleList", "peopleGet", "peopleUpsert", "peopleDelete", "peopleSuggestReplyStyle", "peopleLearnFromConversation", "peopleGetContextForPrompt", "learningGetPredictions", "temporalMeetingPrep"]
    - path: "src/lib/tauri/identity.ts"
      provides: "Typed wrappers for ~40 Identity commands"
      exports: ["consolidateCharacter", "consolidateReactionsToPreferences", "reactionInstantRule", "bladeGetSoul", "getCharacterBible", "updateCharacterSection", "applyReactionToTraits", "soulGetState", "soulTakeSnapshot", "soulDeletePreference", "soulUpdateBibleSection", "soulRefreshBible", "getUserProfile", "personaGetTraits", "personaGetRelationship", "personaUpdateTrait", "personaGetContext", "personaAnalyzeNow", "personaRecordOutcome", "personaAnalyzeNowWeekly", "getUserModel", "predictNextNeedCmd", "getExpertiseMap", "updateExpertise", "personaEstimateMood", "negotiationBuildArgument", "negotiationSteelman", "negotiationFindCommonGround", "negotiationStartDebate", "negotiationRound", "negotiationConclude", "negotiationAnalyze", "negotiationRoleplay", "negotiationCritiqueMove", "negotiationGetDebates", "negotiationGetScenarios", "reasoningThink", "reasoningDecompose", "reasoningTestHypothesis", "reasoningSocratic", "reasoningGetTraces", "contextAssemble", "contextScoreChunk", "contextClearCache", "sidecarListDevices", "sidecarRegisterDevice", "sidecarRemoveDevice", "sidecarPingDevice", "sidecarRunCommand", "sidecarRunAll", "sidecarStartServer", "personalityAnalyze", "personalityImportChats", "personalityGetProfile", "kaliRecon", "kaliCrackHash", "kaliAnalyzeCtf", "kaliExplainExploit", "kaliGeneratePayload", "kaliCheckTools"]
    - path: "src/features/life-os/index.tsx"
      provides: "9-route RouteDefinition[] with lazy imports of real components"
    - path: "src/features/identity/index.tsx"
      provides: "7-route RouteDefinition[] with lazy imports of real components"
    - path: "src/features/life-os/types.ts"
      provides: "Cluster-local re-exports + UI types"
    - path: "src/features/identity/types.ts"
      provides: "Cluster-local re-exports + UI types"
    - path: "src/features/life-os/life-os.css"
      provides: "Cluster-scoped CSS — shared base + life-card classes"
      contains: ".life-surface"
    - path: "src/features/identity/identity.css"
      provides: "Cluster-scoped CSS — shared base + identity-card + tab classes"
      contains: ".identity-surface"
    - path: "src/lib/tauri/index.ts"
      provides: "Extended barrel with life_os + identity re-exports"
  key_links:
    - from: "src/lib/tauri/life_os.ts"
      to: "src-tauri/src/lib.rs generate_handler! (lines 759..1282)"
      via: "invokeTyped('health_*' / 'finance_*' / 'goal_*' / 'habit_*' / 'meeting_*' / 'social_*' / 'prediction_*' / 'emotion_*' / 'accountability_*' / 'streak_*' / 'people_*' / 'learning_*' / 'temporal_*', ...)"
      pattern: "invokeTyped\\('health_|invokeTyped\\('finance_|invokeTyped\\('goal_|invokeTyped\\('habit_|invokeTyped\\('meeting_|invokeTyped\\('social_|invokeTyped\\('prediction_|invokeTyped\\('emotion_|invokeTyped\\('accountability_|invokeTyped\\('streak_|invokeTyped\\('people_|invokeTyped\\('learning_|invokeTyped\\('temporal_"
    - from: "src/lib/tauri/identity.ts"
      to: "src-tauri/src/lib.rs generate_handler!"
      via: "invokeTyped for character_* / soul_* / persona_* / negotiation_* / reasoning_* / context_* / sidecar_* / personality_* / kali_*"
      pattern: "invokeTyped\\('character|invokeTyped\\('soul_|invokeTyped\\('persona_|invokeTyped\\('negotiation_|invokeTyped\\('reasoning_|invokeTyped\\('context_|invokeTyped\\('sidecar_|invokeTyped\\('personality_|invokeTyped\\('kali_"
    - from: "src/features/life-os/index.tsx"
      to: "src/features/life-os/HealthView.tsx etc."
      via: "React.lazy + dynamic import"
      pattern: "lazy\\(\\(\\) => import"
    - from: "src/features/identity/index.tsx"
      to: "src/features/identity/SoulView.tsx etc."
      via: "React.lazy + dynamic import"
      pattern: "lazy\\(\\(\\) => import"
---

<objective>
Create the two cluster-level Tauri wrapper modules + rewrite both cluster feature index files with real lazy imports + seed 16 minimal per-route component files that Plans 06-03..06 will fill in. No behavioral UI work in this plan — only plumbing.

Purpose: ROADMAP Phase 6 description "each cluster wires its own lib/tauri module" + D-139 cluster-scoped wrapper discipline + D-143 single-writer index.tsx invariant. After this plan lands, Plans 06-03..06 can ship their route bodies with zero import-path surprises and zero chance of stepping on each other's index.tsx edits.

Output: ~150 camelCase wrapper exports across 2 new TS files; 2 rewritten index.tsx files; 16 new per-route placeholder files; 2 cluster CSS files; 2 type barrel files; 1 barrel edit. Roughly 1800-2400 net new lines — mostly mechanical wrapper declarations. Plan completes under 45% context — large file count but each file is tightly templated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-life-os-identity/06-CONTEXT.md
@.planning/phases/06-life-os-identity/06-PATTERNS.md
@.planning/phases/05-agents-knowledge/05-02-PLAN.md
@.planning/phases/05-agents-knowledge/05-02-SUMMARY.md
@.planning/phases/05-agents-knowledge/05-PATTERNS.md
@.planning/RECOVERY_LOG.md
@CLAUDE.md
@src/lib/tauri/_base.ts
@src/lib/tauri/chat.ts
@src/lib/tauri/agents.ts
@src/lib/tauri/knowledge.ts
@src/lib/tauri/index.ts
@src/lib/router.ts
@src/features/life-os/index.tsx
@src/features/identity/index.tsx
@src/features/agents/agents.css
@src/features/knowledge/knowledge.css
@src/styles/tokens.css
@src-tauri/src/lib.rs

<interfaces>
<!-- Existing wrapper pattern (from Phase 5 src/lib/tauri/agents.ts) -->
```ts
// Established recipe (Phase 5 Plan 05-02; follow identically for life_os.ts + identity.ts)

import { invokeTyped } from './_base';

export interface SomeType {
  id: string;
  // ... mirror Rust Serialize shape, snake_case preserved
  [k: string]: unknown;
}

/** @see src-tauri/src/<module>.rs <command_name> */
export function camelCaseWrapper(args: { camelCaseArg: string }): Promise<SomeType> {
  return invokeTyped<SomeType>('snake_case_command', {
    snake_case_arg: args.camelCaseArg,
  });
}
```

<!-- Route definition shape (D-39) -->
```ts
// src/lib/router.ts
export interface RouteDefinition {
  id: string;
  label: string;
  section: 'core' | 'agents' | 'knowledge' | 'life' | 'identity' | 'dev' | 'admin' | 'body' | 'hive';
  component: React.LazyExoticComponent<React.ComponentType<any>>;
  icon?: React.ComponentType;
  shortcut?: string;
  paletteHidden?: boolean;
  description?: string;
  phase?: number;
}
```

<!-- Rust command surface summary (from lib.rs:759-1282 generate_handler!) — D-140 inventory -->
```
LIFE OS cluster (~110 commands):
  health_tracker (9): health_log, health_get_today, health_update_today, health_get_logs,
    health_get_stats, health_get_insights, health_get_context, health_correlate_productivity, health_streak_info
  health (3): health_get_scan, health_scan_now, health_summary_all
  health_guardian (2): health_guardian_stats, health_take_break
  financial_brain (15): finance_add_transaction, finance_get_transactions, finance_delete_transaction,
    finance_get_snapshot, finance_generate_insights, finance_get_goals, finance_create_goal, finance_update_goal,
    finance_investment_suggestions, finance_budget_recommendation, finance_get_context,
    finance_import_csv, finance_auto_categorize, finance_spending_summary, finance_detect_subscriptions
  goal_engine (6): goal_add, goal_list, goal_complete, goal_delete, goal_update_priority, goal_pursue_now
  habit_engine (10): habit_create, habit_list, habit_get, habit_complete, habit_skip,
    habit_get_logs, habit_get_today, habit_insights, habit_suggest_design, habit_get_context
  meeting_intelligence (10): meeting_process, meeting_get, meeting_list, meeting_search, meeting_delete,
    meeting_get_action_items, meeting_complete_action, meeting_follow_up_email, meeting_compare, meeting_recurring_themes
  social_graph (11): social_add_contact, social_get_contact, social_search_contacts, social_update_contact,
    social_delete_contact, social_list_contacts, social_log_interaction, social_get_interactions,
    social_analyze_interaction, social_get_insights, social_how_to_approach
  prediction_engine (6): prediction_get_pending, prediction_accept, prediction_dismiss,
    prediction_generate_now, prediction_contextual, prediction_get_patterns
  emotional_intelligence (5): emotion_get_current, emotion_get_trend, emotion_get_readings,
    emotion_analyze_patterns, emotion_get_context
  accountability (8): accountability_get_objectives, accountability_create_objective, accountability_update_kr,
    accountability_daily_plan, accountability_complete_action, accountability_checkin,
    accountability_progress_report, accountability_get_daily_actions
  streak_stats (3): streak_get_stats, streak_record_activity, streak_get_display
  people_graph (7): people_list, people_get, people_upsert, people_delete, people_suggest_reply_style,
    people_learn_from_conversation, people_get_context_for_prompt
  learning_engine (1): learning_get_predictions
  temporal_intel (1): temporal_meeting_prep

IDENTITY cluster (~40 commands):
  character (7): consolidate_character, consolidate_reactions_to_preferences, reaction_instant_rule,
    blade_get_soul, get_character_bible, update_character_section, apply_reaction_to_traits
  soul_commands (6): soul_get_state, soul_take_snapshot, soul_delete_preference,
    soul_update_bible_section, soul_refresh_bible, get_user_profile
  persona_engine (13): persona_get_traits, persona_get_relationship, persona_update_trait, persona_get_context,
    persona_analyze_now, persona_record_outcome, persona_analyze_now_weekly, get_user_model,
    predict_next_need_cmd, get_expertise_map, update_expertise, persona_estimate_mood
    (12 listed — verify count by reading file; any discrepancy use actual Rust count)
  negotiation_engine (11): negotiation_build_argument, negotiation_steelman, negotiation_find_common_ground,
    negotiation_start_debate, negotiation_round, negotiation_conclude, negotiation_analyze,
    negotiation_roleplay, negotiation_critique_move, negotiation_get_debates, negotiation_get_scenarios
  reasoning_engine (5): reasoning_think, reasoning_decompose, reasoning_test_hypothesis,
    reasoning_socratic, reasoning_get_traces
  context_engine (3): context_assemble, context_score_chunk, context_clear_cache
  sidecar (7): sidecar_list_devices, sidecar_register_device, sidecar_remove_device,
    sidecar_ping_device, sidecar_run_command, sidecar_run_all, sidecar_start_server
  personality_mirror (3): personality_analyze, personality_import_chats, personality_get_profile
  kali (6): kali_recon, kali_crack_hash, kali_analyze_ctf, kali_explain_exploit,
    kali_generate_payload, kali_check_tools
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/tauri/life_os.ts with ~110 typed wrappers + src/features/life-os/types.ts barrel</name>
  <files>src/lib/tauri/life_os.ts, src/features/life-os/types.ts</files>
  <read_first>
    - /home/arnav/blade/src/lib/tauri/agents.ts (Phase 5 Plan 05-02 output — recipe reference)
    - /home/arnav/blade/src/lib/tauri/_base.ts
    - /home/arnav/blade/src-tauri/src/health_tracker.rs (grep '#\[tauri::command\]' to collect all signatures + line numbers)
    - /home/arnav/blade/src-tauri/src/health.rs (grep '#\[tauri::command\]')
    - /home/arnav/blade/src-tauri/src/health_guardian.rs
    - /home/arnav/blade/src-tauri/src/financial_brain.rs
    - /home/arnav/blade/src-tauri/src/goal_engine.rs
    - /home/arnav/blade/src-tauri/src/habit_engine.rs
    - /home/arnav/blade/src-tauri/src/meeting_intelligence.rs
    - /home/arnav/blade/src-tauri/src/social_graph.rs
    - /home/arnav/blade/src-tauri/src/prediction_engine.rs
    - /home/arnav/blade/src-tauri/src/emotional_intelligence.rs
    - /home/arnav/blade/src-tauri/src/accountability.rs
    - /home/arnav/blade/src-tauri/src/streak_stats.rs
    - /home/arnav/blade/src-tauri/src/people_graph.rs
    - /home/arnav/blade/src-tauri/src/learning_engine.rs
    - /home/arnav/blade/src-tauri/src/temporal_intel.rs
    - /home/arnav/blade/src-tauri/src/lib.rs (lines 759-1282 — confirm every command in the 14 modules above is registered)
    - /home/arnav/blade/.planning/phases/06-life-os-identity/06-PATTERNS.md §1
  </read_first>
  <action>
    Create `src/lib/tauri/life_os.ts` (~900-1100 lines expected, single file). Structure mirrors Phase 5 `src/lib/tauri/agents.ts` verbatim:

    1. Header comment block citing D-139, D-140, D-159, D-38.
    2. `import { invokeTyped } from './_base';`
    3. **Types section** — hand-written interfaces for every non-trivial return type. Mirror Rust `#[derive(Serialize)]` struct field names verbatim (snake_case). Every interface has `[k: string]: unknown` index signature. Representative types:
       - `HealthLog`, `HealthToday`, `HealthStats`, `HealthInsight`, `HealthContext`, `HealthScan`, `HealthGuardianStats`
       - `FinanceTransaction`, `FinanceSnapshot`, `FinanceInsight`, `FinanceGoal`, `FinanceContext`, `FinanceSpendingSummary`, `FinanceSubscription`
       - `Goal`, `GoalPriority`
       - `Habit`, `HabitLog`, `HabitInsight`, `HabitContext`
       - `Meeting`, `MeetingActionItem`, `MeetingComparison`, `MeetingTheme`
       - `Contact`, `Interaction`, `ContactInsight`
       - `Prediction`, `PredictionPattern`
       - `EmotionReading`, `EmotionTrend`, `EmotionPatternAnalysis`, `EmotionContext`
       - `Objective`, `KeyResult`, `DailyAction`, `DailyPlan`, `CheckIn`
       - `StreakStats`, `StreakDisplay`
       - `Person`, `PersonReplyStyle`
       - `LearningPrediction`
       - `MeetingPrepBriefing`
    4. **Wrapper sections** (one section per Rust module, matching lib.rs order 759..1282). Each wrapper:
       - One camelCase function per `#[tauri::command]`
       - JSDoc `@see src-tauri/src/<file>.rs <command_name>` (line numbers optional but preferred where discoverable)
       - `invokeTyped<TReturn>('snake_case_command', args)` — raw invoke BANNED
       - camelCase → snake_case at the wrapper's invoke call

    Example section (health_tracker — 9 commands):

    ```ts
    // ═══════════════════════════════════════════════════════════════════════
    // health_tracker.rs — daily health log + insights (9 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/health_tracker.rs health_log */
    export function healthLog(args: {
      sleepHours?: number;
      activityMinutes?: number;
      mood?: number;
      energy?: number;
      focus?: number;
      note?: string;
    }): Promise<HealthLog> {
      return invokeTyped<HealthLog>('health_log', {
        sleep_hours: args.sleepHours,
        activity_minutes: args.activityMinutes,
        mood: args.mood,
        energy: args.energy,
        focus: args.focus,
        note: args.note,
      });
    }

    /** @see src-tauri/src/health_tracker.rs health_get_today */
    export function healthGetToday(): Promise<HealthLog | null> {
      return invokeTyped<HealthLog | null>('health_get_today', {});
    }

    /** @see src-tauri/src/health_tracker.rs health_update_today */
    export function healthUpdateToday(args: Partial<HealthLog>): Promise<HealthLog> {
      return invokeTyped<HealthLog>('health_update_today', args);
    }

    // ... 6 more (health_get_logs, health_get_stats, health_get_insights,
    //              health_get_context, health_correlate_productivity, health_streak_info)
    ```

    **Repeat for all 14 modules.** Section order matches lib.rs registration order. For every Rust command, EMIT a wrapper. If a Rust signature has arguments, read them and convert camelCase → snake_case at the invoke boundary. Do NOT invent optional args — match Rust exactly.

    **Adjust wrapper signatures to MATCH the actual Rust signatures** — when reading the Rust source, preserve parameter names (converted snake_case → invoke) and return types. If any Rust fn signature differs from an example draft, the ACTUAL Rust signature wins. Add a note in JSDoc explaining any non-obvious correction.

    Then create `src/features/life-os/types.ts`:

    ```ts
    // src/features/life-os/types.ts
    // Cluster-local barrel — re-exports Tauri wrapper types + UI-only types.
    // @see .planning/phases/06-life-os-identity/06-CONTEXT.md §D-161

    export type {
      HealthLog, HealthToday, HealthStats, HealthInsight, HealthScan, HealthGuardianStats,
      FinanceTransaction, FinanceSnapshot, FinanceInsight, FinanceGoal, FinanceSubscription, FinanceSpendingSummary,
      Goal, GoalPriority,
      Habit, HabitLog, HabitInsight,
      Meeting, MeetingActionItem,
      Contact, Interaction, ContactInsight,
      Prediction, PredictionPattern,
      EmotionReading, EmotionTrend,
      Objective, KeyResult, DailyAction, DailyPlan,
      StreakStats, StreakDisplay,
      Person, PersonReplyStyle,
      LearningPrediction,
      MeetingPrepBriefing,
    } from '@/lib/tauri/life_os';

    // Cluster-only UI types
    export type LifeTabKey = string;

    export interface LifeStatCard {
      label: string;
      value: string | number;
      trend?: 'up' | 'down' | 'flat';
    }
    ```

    **CRITICAL — CLAUDE.md rules:**
    - Raw `invoke()` import is BANNED in this file (ESLint no-raw-tauri). Every wrapper routes through `invokeTyped`.
    - Argument key conversion camelCase → snake_case happens AT EACH wrapper's invoke call.
    - Prefer `[k: string]: unknown` index signature on interfaces for forward-compat (D-38-payload accepted risk).
    - File stays in ONE FILE (no splitting) — makes Plan 06-03/06-04 imports deterministic.
  </action>
  <verify>
    <automated>grep -c "export function" src/lib/tauri/life_os.ts && grep -c "@see src-tauri" src/lib/tauri/life_os.ts && grep -c "invokeTyped" src/lib/tauri/life_os.ts && test -f src/features/life-os/types.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    life_os.ts has ≥100 export function declarations (allow ±10 for signature merge), ≥100 @see JSDoc lines, and ≥100 invokeTyped call sites. types.ts exists. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/tauri/identity.ts with ~40 typed wrappers + src/features/identity/types.ts barrel</name>
  <files>src/lib/tauri/identity.ts, src/features/identity/types.ts</files>
  <read_first>
    - /home/arnav/blade/src-tauri/src/character.rs (grep '#\[tauri::command\]' — 7 signatures)
    - /home/arnav/blade/src-tauri/src/soul_commands.rs (6 signatures; line numbers already known: 31, 55, 170, 178, 184, 210)
    - /home/arnav/blade/src-tauri/src/persona_engine.rs (~12-13 signatures spread across the file — grep to confirm exact count)
    - /home/arnav/blade/src-tauri/src/negotiation_engine.rs (11 signatures)
    - /home/arnav/blade/src-tauri/src/reasoning_engine.rs (5 signatures)
    - /home/arnav/blade/src-tauri/src/context_engine.rs (3 signatures)
    - /home/arnav/blade/src-tauri/src/sidecar.rs (7 signatures — note: `sidecar_start_server` per lib.rs:1013 is a server lifecycle command; Dialog confirm in UI consumer)
    - /home/arnav/blade/src-tauri/src/personality_mirror.rs (3 signatures)
    - /home/arnav/blade/src-tauri/src/kali.rs (6 signatures)
    - /home/arnav/blade/src/lib/tauri/life_os.ts (just-created Task 1 — same recipe)
    - /home/arnav/blade/.planning/phases/06-life-os-identity/06-PATTERNS.md §1
  </read_first>
  <action>
    Create `src/lib/tauri/identity.ts` (~400-500 lines expected, single file). Structure mirrors Task 1: header comment + types section + wrapper sections per module.

    Representative types section:

    ```ts
    import { invokeTyped } from './_base';

    // ═══════════════════════════════════════════════════════════════════════
    // Types
    // ═══════════════════════════════════════════════════════════════════════

    export interface SoulState {
      last_evolved_at?: number;
      trait_count: number;
      preference_count: number;
      [k: string]: unknown;
    }

    export interface UserProfile {
      name?: string;
      interests?: string[];
      bio?: string;
      [k: string]: unknown;
    }

    export interface CharacterBibleDoc {
      sections: Record<string, string>;
      [k: string]: unknown;
    }

    export interface PersonaTrait {
      name: string;
      score: number;
      evidence?: string;
      [k: string]: unknown;
    }

    export interface RelationshipState {
      intimacy: number;
      trust: number;
      moments?: string[];
      [k: string]: unknown;
    }

    export interface UserModel {
      summary?: string;
      archetype?: string;
      interests?: string[];
      expertise?: Record<string, number>;
      [k: string]: unknown;
    }

    export interface ExpertiseMap {
      [topic: string]: number;
    }

    export interface NegotiationArgument {
      claim: string;
      evidence: string[];
      counterpoints?: string[];
      [k: string]: unknown;
    }

    export interface DebateSession {
      id: string;
      topic: string;
      stance: string;
      rounds: Array<{ role: 'user' | 'blade'; text: string; ts: number }>;
      status: 'active' | 'concluded';
      [k: string]: unknown;
    }

    export interface ReasoningTrace {
      id: string;
      kind: 'think' | 'decompose' | 'hypothesis' | 'socratic';
      input: string;
      output: string;
      ts: number;
      [k: string]: unknown;
    }

    export interface AssembledContext {
      text: string;
      tokens?: number;
      chunks?: Array<{ id: string; text: string; score?: number }>;
      [k: string]: unknown;
    }

    export interface SidecarDevice {
      id: string;
      name: string;
      address: string;
      status: 'online' | 'offline' | 'unreachable';
      last_ping?: number;
      [k: string]: unknown;
    }

    export interface PersonalityProfile {
      archetype?: string;
      traits?: Record<string, number>;
      [k: string]: unknown;
    }
    ```

    Wrapper sections (one per module, matching lib.rs order):

    ```ts
    // ═══════════════════════════════════════════════════════════════════════
    // character.rs — bible + reactions (7 commands)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/character.rs consolidate_character */
    export function consolidateCharacter(): Promise<string> {
      return invokeTyped<string>('consolidate_character', {});
    }

    /** @see src-tauri/src/character.rs consolidate_reactions_to_preferences */
    export function consolidateReactionsToPreferences(): Promise<string> {
      return invokeTyped<string>('consolidate_reactions_to_preferences', {});
    }

    /** @see src-tauri/src/character.rs reaction_instant_rule */
    export function reactionInstantRule(args: { rule: string }): Promise<void> {
      return invokeTyped<void>('reaction_instant_rule', { rule: args.rule });
    }

    /** @see src-tauri/src/character.rs blade_get_soul */
    export function bladeGetSoul(): Promise<string> {
      return invokeTyped<string>('blade_get_soul', {});
    }

    /** @see src-tauri/src/character.rs get_character_bible */
    export function getCharacterBible(): Promise<CharacterBibleDoc> {
      return invokeTyped<CharacterBibleDoc>('get_character_bible', {});
    }

    /** @see src-tauri/src/character.rs update_character_section */
    export function updateCharacterSection(args: { section: string; content: string }): Promise<void> {
      return invokeTyped<void>('update_character_section', {
        section: args.section,
        content: args.content,
      });
    }

    /** @see src-tauri/src/character.rs apply_reaction_to_traits */
    export function applyReactionToTraits(args: { reaction: string; context?: string }): Promise<void> {
      return invokeTyped<void>('apply_reaction_to_traits', {
        reaction: args.reaction,
        context: args.context,
      });
    }

    // ═══════════════════════════════════════════════════════════════════════
    // soul_commands.rs — 6 commands
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/soul_commands.rs:31 soul_get_state */
    export function soulGetState(): Promise<SoulState> {
      return invokeTyped<SoulState>('soul_get_state', {});
    }

    // ... (continue for all 6)

    // ═══════════════════════════════════════════════════════════════════════
    // persona_engine.rs — 12-13 commands (verify exact count by reading file)
    // ═══════════════════════════════════════════════════════════════════════

    /** @see src-tauri/src/persona_engine.rs persona_get_traits */
    export function personaGetTraits(): Promise<PersonaTrait[]> {
      return invokeTyped<PersonaTrait[]>('persona_get_traits', {});
    }

    // ... (continue)

    // ═══════════════════════════════════════════════════════════════════════
    // negotiation_engine.rs — 11 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // reasoning_engine.rs — 5 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // context_engine.rs — 3 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // sidecar.rs — 7 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // personality_mirror.rs — 3 commands
    // ═══════════════════════════════════════════════════════════════════════
    // ...

    // ═══════════════════════════════════════════════════════════════════════
    // kali.rs — 6 commands (exposed via SidecarView per D-158)
    // ═══════════════════════════════════════════════════════════════════════
    // ...
    ```

    **Adjust signatures based on actual Rust reads.** Rust wins.

    Then create `src/features/identity/types.ts`:

    ```ts
    // src/features/identity/types.ts
    // Cluster-local barrel.

    export type {
      SoulState, UserProfile, CharacterBibleDoc,
      PersonaTrait, RelationshipState, UserModel, ExpertiseMap,
      NegotiationArgument, DebateSession,
      ReasoningTrace,
      AssembledContext,
      SidecarDevice,
      PersonalityProfile,
    } from '@/lib/tauri/identity';

    export type IdentityTabKey = string;

    export interface IdentitySection {
      key: string;
      label: string;
      content: string;
    }
    ```

    **CRITICAL**:
    - Raw invoke BANNED.
    - camelCase JS API; snake_case wire args.
    - Every wrapper: one @see JSDoc line + one invokeTyped call.
    - File stays in ONE FILE.
  </action>
  <verify>
    <automated>grep -c "export function" src/lib/tauri/identity.ts && grep -c "@see src-tauri" src/lib/tauri/identity.ts && grep -c "invokeTyped" src/lib/tauri/identity.ts && test -f src/features/identity/types.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    identity.ts has ≥38 export function declarations (allow ±5 for signature merge), ≥38 @see JSDoc lines, and ≥38 invokeTyped call sites. types.ts exists. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite feature index files + seed 16 per-route placeholder components + create cluster CSS + update tauri barrel</name>
  <files>src/features/life-os/index.tsx, src/features/identity/index.tsx, src/features/life-os/HealthView.tsx, src/features/life-os/FinanceView.tsx, src/features/life-os/GoalView.tsx, src/features/life-os/HabitView.tsx, src/features/life-os/MeetingsView.tsx, src/features/life-os/SocialGraphView.tsx, src/features/life-os/PredictionsView.tsx, src/features/life-os/EmotionalIntelView.tsx, src/features/life-os/AccountabilityView.tsx, src/features/life-os/life-os.css, src/features/identity/SoulView.tsx, src/features/identity/PersonaView.tsx, src/features/identity/CharacterBible.tsx, src/features/identity/NegotiationView.tsx, src/features/identity/ReasoningView.tsx, src/features/identity/ContextEngineView.tsx, src/features/identity/SidecarView.tsx, src/features/identity/identity.css, src/lib/tauri/index.ts</files>
  <read_first>
    - /home/arnav/blade/src/features/life-os/index.tsx (current 9-stub version — 27 lines)
    - /home/arnav/blade/src/features/identity/index.tsx (current 7-stub version — 25 lines)
    - /home/arnav/blade/src/features/agents/index.tsx (Phase 5 Plan 05-02 output — recipe reference)
    - /home/arnav/blade/src/features/agents/agents.css (Phase 5 pattern for cluster CSS)
    - /home/arnav/blade/src/lib/router.ts (RouteDefinition interface)
    - /home/arnav/blade/src/design-system/primitives/index.ts (GlassPanel export)
    - /home/arnav/blade/src/lib/tauri/index.ts (barrel — see current exports)
    - /home/arnav/blade/.planning/phases/06-life-os-identity/06-PATTERNS.md §2, §7
  </read_first>
  <action>
    **Sub-task 3a — Rewrite src/features/life-os/index.tsx (per Pattern §2):**

    ```tsx
    // src/features/life-os/index.tsx — Phase 6 rewrite (Plan 06-02, D-143 single-writer)
    // Phase 1 ComingSoonSkeleton stubs replaced with lazy imports of real route components.
    // @see .planning/phases/06-life-os-identity/06-CONTEXT.md §D-143
    // @see .planning/REQUIREMENTS.md §LIFE-01..09

    import { lazy } from 'react';
    import type { RouteDefinition } from '@/lib/router';

    const HealthView         = lazy(() => import('./HealthView').then((m) => ({ default: m.HealthView })));
    const FinanceView        = lazy(() => import('./FinanceView').then((m) => ({ default: m.FinanceView })));
    const GoalView           = lazy(() => import('./GoalView').then((m) => ({ default: m.GoalView })));
    const HabitView          = lazy(() => import('./HabitView').then((m) => ({ default: m.HabitView })));
    const MeetingsView       = lazy(() => import('./MeetingsView').then((m) => ({ default: m.MeetingsView })));
    const SocialGraphView    = lazy(() => import('./SocialGraphView').then((m) => ({ default: m.SocialGraphView })));
    const PredictionsView    = lazy(() => import('./PredictionsView').then((m) => ({ default: m.PredictionsView })));
    const EmotionalIntelView = lazy(() => import('./EmotionalIntelView').then((m) => ({ default: m.EmotionalIntelView })));
    const AccountabilityView = lazy(() => import('./AccountabilityView').then((m) => ({ default: m.AccountabilityView })));

    export const routes: RouteDefinition[] = [
      { id: 'health',          label: 'Health',                 section: 'life', component: HealthView,         phase: 6, description: "Today's snapshot + streak" },
      { id: 'finance',         label: 'Finance',                section: 'life', component: FinanceView,        phase: 6 },
      { id: 'goals',           label: 'Goals',                  section: 'life', component: GoalView,           phase: 6 },
      { id: 'habits',          label: 'Habits',                 section: 'life', component: HabitView,          phase: 6 },
      { id: 'meetings',        label: 'Meetings',               section: 'life', component: MeetingsView,       phase: 6 },
      { id: 'social-graph',    label: 'Social Graph',           section: 'life', component: SocialGraphView,    phase: 6 },
      { id: 'predictions',     label: 'Predictions',            section: 'life', component: PredictionsView,    phase: 6 },
      { id: 'emotional-intel', label: 'Emotional Intelligence', section: 'life', component: EmotionalIntelView, phase: 6 },
      { id: 'accountability',  label: 'Accountability',         section: 'life', component: AccountabilityView, phase: 6 },
    ];
    ```

    **Sub-task 3b — Rewrite src/features/identity/index.tsx** (same pattern, 7 routes):

    ```tsx
    // src/features/identity/index.tsx — Phase 6 rewrite (Plan 06-02)
    import { lazy } from 'react';
    import type { RouteDefinition } from '@/lib/router';

    const SoulView          = lazy(() => import('./SoulView').then((m) => ({ default: m.SoulView })));
    const PersonaView       = lazy(() => import('./PersonaView').then((m) => ({ default: m.PersonaView })));
    const CharacterBible    = lazy(() => import('./CharacterBible').then((m) => ({ default: m.CharacterBible })));
    const NegotiationView   = lazy(() => import('./NegotiationView').then((m) => ({ default: m.NegotiationView })));
    const ReasoningView     = lazy(() => import('./ReasoningView').then((m) => ({ default: m.ReasoningView })));
    const ContextEngineView = lazy(() => import('./ContextEngineView').then((m) => ({ default: m.ContextEngineView })));
    const SidecarView       = lazy(() => import('./SidecarView').then((m) => ({ default: m.SidecarView })));

    export const routes: RouteDefinition[] = [
      { id: 'soul',           label: 'Soul',            section: 'identity', component: SoulView,          phase: 6 },
      { id: 'persona',        label: 'Persona',         section: 'identity', component: PersonaView,       phase: 6 },
      { id: 'character',      label: 'Character Bible', section: 'identity', component: CharacterBible,    phase: 6 },
      { id: 'negotiation',    label: 'Negotiation',     section: 'identity', component: NegotiationView,   phase: 6 },
      { id: 'reasoning',      label: 'Reasoning',       section: 'identity', component: ReasoningView,     phase: 6 },
      { id: 'context-engine', label: 'Context Engine',  section: 'identity', component: ContextEngineView, phase: 6 },
      { id: 'sidecar',        label: 'Sidecar',         section: 'identity', component: SidecarView,       phase: 6 },
    ];
    ```

    **Sub-task 3c — Create 16 per-route placeholder files** (9 life-os + 7 identity). Each file is a minimal named export that Plans 06-03..06 will fill in. Template per file (substitute NAME + LABEL + PLAN):

    ```tsx
    // src/features/life-os/HealthView.tsx — placeholder shipped by Plan 06-02
    // Real body ships in Plan 06-03.
    import { GlassPanel } from '@/design-system/primitives';
    import './life-os.css';

    export function HealthView() {
      return (
        <GlassPanel tier={1} className="life-surface">
          <div className="life-placeholder" data-testid="health-view-placeholder">
            <h2>Health</h2>
            <p className="life-placeholder-hint">Ships in Plan 06-03.</p>
          </div>
        </GlassPanel>
      );
    }
    ```

    Create a placeholder for each route:
    - life-os (Plan 06-03 fills these 5): HealthView, FinanceView, GoalView, HabitView, MeetingsView
    - life-os (Plan 06-04 fills these 4): SocialGraphView, PredictionsView, EmotionalIntelView, AccountabilityView
    - identity (Plan 06-05 fills these 4): SoulView, PersonaView, CharacterBible, NegotiationView
    - identity (Plan 06-06 fills these 3): ReasoningView, ContextEngineView, SidecarView

    The placeholder hint must identify which plan ships the body (so Plans 06-03..06 can replace cleanly). For identity routes, substitute `./life-os.css` → `./identity.css` and `life-surface` / `life-placeholder` → `identity-surface` / `identity-placeholder`.

    **Sub-task 3d — Create src/features/life-os/life-os.css** (mirror Phase 5 agents.css):

    ```css
    /* src/features/life-os/life-os.css — Plan 06-02 shared base
       Plans 06-03 and 06-04 EXTEND this file via scoped partials; never replace.
       @see .planning/phases/06-life-os-identity/06-PATTERNS.md §7 */

    @layer features {
      .life-surface {
        padding: var(--sp-4);
        height: 100%;
        overflow-y: auto;
      }

      .life-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--sp-2);
        padding: var(--sp-6);
        text-align: center;
      }

      .life-placeholder-hint {
        color: var(--t-3);
        font-size: 14px;
      }

      .life-card {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-3);
        display: flex;
        flex-direction: column;
        gap: var(--sp-2);
        transition: border-color 180ms var(--ease-out);
      }

      .life-card:hover { border-color: rgba(255, 255, 255, 0.18); }

      .life-card[data-status="running"]  { border-left: 3px solid var(--status-running); }
      .life-card[data-status="complete"] { border-left: 3px solid var(--status-success); }
      .life-card[data-status="failed"]   { border-left: 3px solid var(--status-error); }
      .life-card[data-status="idle"],
      .life-card:not([data-status])       { border-left: 3px solid var(--status-idle); }

      .life-stat-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
        gap: var(--sp-2);
      }

      .life-tab-row {
        display: flex;
        gap: var(--sp-1);
        padding: var(--sp-2) 0;
      }

      .life-tab-pill {
        padding: var(--sp-1) var(--sp-2);
        border-radius: var(--radius-pill);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        cursor: pointer;
        font-size: 13px;
        transition: background 140ms var(--ease-out);
        color: var(--t-2);
      }

      .life-tab-pill[data-active="true"] {
        background: rgba(255, 255, 255, 0.14);
        border-color: rgba(255, 255, 255, 0.22);
        color: var(--t-1);
      }
    }
    ```

    **Sub-task 3e — Create src/features/identity/identity.css** (parallel structure, substituting life → identity + adding tabbed-surface classes for PersonaView / NegotiationView per D-154 / D-156):

    ```css
    /* src/features/identity/identity.css — Plan 06-02 shared base */

    @layer features {
      .identity-surface {
        padding: var(--sp-4);
        height: 100%;
        overflow-y: auto;
      }

      .identity-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--sp-2);
        padding: var(--sp-6);
        text-align: center;
      }

      .identity-placeholder-hint { color: var(--t-3); font-size: 14px; }

      .identity-card {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-3);
      }

      .identity-tabs {
        display: flex;
        gap: var(--sp-1);
        padding: var(--sp-2) 0;
        border-bottom: 1px solid var(--line);
        margin-bottom: var(--sp-3);
      }

      .identity-tab-pill {
        padding: var(--sp-1) var(--sp-2);
        border-radius: var(--radius-pill);
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--line);
        cursor: pointer;
        font-size: 13px;
        transition: background 140ms var(--ease-out);
        color: var(--t-2);
      }

      .identity-tab-pill[data-active="true"] {
        background: rgba(255, 255, 255, 0.14);
        border-color: rgba(255, 255, 255, 0.22);
        color: var(--t-1);
      }

      .identity-edit-textarea {
        width: 100%;
        min-height: 240px;
        background: rgba(0, 0, 0, 0.3);
        border: 1px solid var(--line);
        border-radius: var(--radius-card);
        padding: var(--sp-2);
        color: var(--t-1);
        font-family: var(--font-mono);
        font-size: 13px;
        resize: vertical;
      }
    }
    ```

    **Sub-task 3f — Update src/lib/tauri/index.ts barrel** to re-export the new modules:

    ```ts
    // Append to existing barrel (follow the file's convention — individual re-exports OR namespace wildcards)
    // If Phase 5 used `export * as agents from './agents';` style, mirror:
    export * as lifeOs    from './life_os';
    export * as identity  from './identity';

    // If Phase 5 used individual re-exports (`export { agentList, ... } from './agents';`),
    // mirror by re-exporting from life_os.ts + identity.ts explicitly instead.
    ```

    (Inspect the existing barrel FIRST; match its convention exactly. Do NOT change the existing re-export style.)

    **Sub-task 3g — tokens.css check.** Phase 5 Plan 05-02 introduced `--status-idle/running/success/error` tokens. Phase 6 REUSES — do NOT re-declare. `grep -q "status-running" src/styles/tokens.css` should pass. If missing (shouldn't happen post-Phase-5), that's a regression; flag in SUMMARY.

    **CRITICAL — CLAUDE.md + Phase discipline:**
    - D-143: This plan is the ONLY plan that edits `src/features/life-os/index.tsx` and `src/features/identity/index.tsx`. Downstream plans replace the BODY of the 16 per-route files — they do NOT touch the 2 index.tsx files.
    - D-163: Per-route file names match the index.tsx imports exactly. Case-sensitive.
    - D-164: Cluster CSS files are created ONCE here; Plans 06-03..06 append their own rules using scoped partial CSS files (`life-os-rich-a.css`, etc. — per Phase 5 Plan 05-03/04/05/06 pattern to prevent parallel-edit conflicts).
    - All primitives imported from `@/design-system/primitives` (barrel) — not individual files.
  </action>
  <verify>
    <automated>test -f src/features/life-os/HealthView.tsx && test -f src/features/life-os/FinanceView.tsx && test -f src/features/life-os/GoalView.tsx && test -f src/features/life-os/HabitView.tsx && test -f src/features/life-os/MeetingsView.tsx && test -f src/features/life-os/SocialGraphView.tsx && test -f src/features/life-os/PredictionsView.tsx && test -f src/features/life-os/EmotionalIntelView.tsx && test -f src/features/life-os/AccountabilityView.tsx && test -f src/features/life-os/life-os.css && test -f src/features/identity/SoulView.tsx && test -f src/features/identity/PersonaView.tsx && test -f src/features/identity/CharacterBible.tsx && test -f src/features/identity/NegotiationView.tsx && test -f src/features/identity/ReasoningView.tsx && test -f src/features/identity/ContextEngineView.tsx && test -f src/features/identity/SidecarView.tsx && test -f src/features/identity/identity.css && grep -q "status-running" src/styles/tokens.css && npx tsc --noEmit</automated>
  </verify>
  <done>
    All 16 per-route placeholder files exist; both cluster index.tsx rewritten with lazy imports; both CSS files created; status tokens confirmed present; barrel updated; `npx tsc --noEmit` passes; `npm run verify:all` passes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes.
- `npm run verify:all` passes — migration-ledger still consistent (routes unchanged), entries check still passes, no-raw-tauri rule still green (every wrapper uses invokeTyped).
- ESLint passes (no-raw-tauri rule enforced).
- `npm run dev` builds the frontend — lazy imports resolve at first load.
- Route count unchanged in migration-ledger (9 life-os + 7 identity = 16 Phase 6 routes).
</verification>

<success_criteria>
- `src/lib/tauri/life_os.ts` exports ~110 camelCase wrapper functions, each with JSDoc @see citation.
- `src/lib/tauri/identity.ts` exports ~40 camelCase wrapper functions, each with JSDoc @see citation.
- Both cluster feature `index.tsx` files rewritten to lazy-import 9 + 7 real components.
- 16 per-route placeholder files exist and render a glass panel + placeholder hint naming the follow-up plan.
- Two cluster CSS files exist with shared base classes.
- Types barrel files exist for both clusters.
- Tauri library barrel re-exports both new wrapper modules.
- Zero Rust file edits.
- Zero behavior regressions in existing Phase 1..5 tests.
</success_criteria>

<threat_model>
## Trust Boundaries

- **JS → Rust invoke boundary**: every wrapper crosses this. Args flow JS → serde → Rust param. Untrusted input originates in user-facing components (Plans 06-03..06).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | camelCase → snake_case arg mis-conversion | mitigate | Per-wrapper explicit mapping (not auto-derived). Plan 06-07 Playwright specs test real invoke paths. |
| T-06-02-02 | Information Disclosure | raw invoke bypasses wrappers | mitigate | ESLint `no-raw-tauri` rule enforces invokeTyped-only outside `src/lib/tauri/`. `scripts/verify-no-raw-tauri.sh` CI backstop. |
| T-06-02-03 | Denial of Service | wrapper throws uncaught rejection under load | accept | TauriError class propagates; consumer surfaces to ToastContext. |
| T-06-02-04 | Tampering | route placeholder left shipped if Plans 06-03..06 never run | mitigate | Plan 06-07 Playwright cluster-routes spec asserts real components mount (placeholder hint absent). |
| T-06-02-05 | Elevation of Privilege | `sidecar_start_server` or `kali_*` commands fire without user consent | mitigate | Consumer components (Plan 06-06 SidecarView) use Dialog confirm before invoking lifecycle/pentest commands. |
</threat_model>

<output>
After completion, create `.planning/phases/06-life-os-identity/06-02-SUMMARY.md` with:
- Exact count of exported wrappers per cluster (life_os.ts count + identity.ts count).
- Which Rust module each wrapper group maps to (table format).
- List of the 16 per-route files created (confirming placeholder content).
- Any Rust signature corrections made while reading the source (so Plans 06-03..06 match reality).
- Barrel export update confirmed; status tokens verified present; CSS files created.
- Confirmation that `src/features/life-os/index.tsx` and `src/features/identity/index.tsx` were the only index files touched (D-143 single-writer invariant held).
</output>
