---
phase: 15-density-polish
plan: "05"
type: execute
wave: 2
depends_on:
  - "15-01"
  - "15-02"
  - "15-03"
  - "15-04"
files_modified:
  - src/features/settings/settings.css
  - src/features/chat/chat.css
  - src/features/quickask/quickask.css
  - src/features/providers/providers.css
  - src/features/knowledge/KnowledgeGraph.css
  - src/features/knowledge/knowledge-rich-a.css
  - src/features/agents/agents-dashboard.css
  - src/features/agents/agents-dag-pack.css
  - src/features/agents/SwarmDAG.css
  - src/features/life-os/life-os-rich-a.css
  - src/features/life-os/life-os-rich-b.css
  - src/features/admin/admin-rich-a.css
  - src/features/dev-tools/dev-tools.css
  - src/features/dev-tools/dev-tools-rich-a.css
  - src/features/dev-tools/dev-tools-rich-b.css
  - src/features/body/body.css
  - src/features/hive/hive.css
  - src/features/onboarding/onboarding.css
  - src/features/ghost/ghost.css
  - src/features/hud/hud.css
  - src/features/voice-orb/orb.css
  - src/features/activity-log/activity-log.css
  - src/features/identity/identity.css
  - src/features/identity/identity-rich-a.css
  - src/features/identity/identity-rich-b.css
autonomous: false
requirements:
  - DENSITY-01
  - DENSITY-06
  - DENSITY-02
  - DENSITY-03
must_haves:
  truths:
    - "`node scripts/verify-spacing-ladder.mjs` exits 0 — zero hardcoded spacing px outside whitelist across ALL src/features and src/design-system CSS"
    - "`npm run verify:all` exits 0 — entire verify chain green after Phase 15 polish"
    - "Human operator has visually verified: 5 representative wallpapers show dashboard content dominance; top-bar hierarchy at 1280px is visually correct; no cluttered or crowded card edges on any route"
    - "Human operator has confirmed: zero 'No X' bare-negation empty states visible across a manual sweep of the command palette (every route opened at least once)"
  artifacts:
    - path: "src/features/settings/settings.css"
      provides: "No hardcoded spacing px violations — all rules use --s-* tokens or chip whitelist"
      contains: "var(--s-"
    - path: "src/features/chat/chat.css"
      provides: "No hardcoded spacing px violations"
      contains: "var(--s-"
    - path: ".planning/phases/15-density-polish/15-05-UAT.md"
      provides: "Human verification checkpoint record — 5-wallpaper audit findings, 1280px top-bar screenshot, empty-state sweep log"
      contains: "verified by operator"
  key_links:
    - from: "scripts/verify-spacing-ladder.mjs"
      to: "all files_modified"
      via: "grep for disallowed padding/gap values"
      pattern: "padding:\\s*[0-9]+px"
---

<objective>
Close every remaining hardcoded-spacing violation across the ~24 feature CSS files so `verify:spacing-ladder` exits 0 globally, then pause for a human operator to visually confirm Phase 15's intended polish on a real dev instance against the 5 representative wallpapers used by the v1.0 contrast baseline.

Purpose: Phase 15 success criterion 1 requires "UI review across all 50+ routes reports 0 padding violations against the documented spacing ladder" AND criterion 4 requires "background-image dominance audit on 5 representative wallpapers; contrast + eye-path pass documented." This plan does the remaining sweep + the human visual checkpoint the automated gates cannot substitute for.

Output: All feature CSS files tokenized, `npm run verify:all` green, and a human-verified checkpoint record in `15-05-UAT.md` confirming the polish lands visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-density-polish/SPACING-LADDER.md
@.planning/phases/15-density-polish/15-01-SUMMARY.md
@.planning/phases/15-density-polish/15-02-SUMMARY.md
@.planning/phases/15-density-polish/15-03-SUMMARY.md
@.planning/phases/15-density-polish/15-04-SUMMARY.md

Check the Wave 0/1 summary files to see what's already tokenized and what remains.

<interfaces>
<!-- Tokens from src/styles/tokens.css (allowed layout values) -->

```css
--s-1: 4px;   --s-2: 8px;   --s-3: 12px;  --s-4: 16px;
--s-5: 20px;  --s-6: 24px;  --s-8: 32px;  --s-10: 40px;
--s-12: 48px; --s-16: 64px; --s-20: 80px;
```

<!-- Whitelist (from SPACING-LADDER.md §Whitelist) — allowed outside tokens -->
- padding: 0
- padding: 1/2/3px (sub-token)
- Chip-style: padding: 3px 10px | 4px 10px | 4px 12px | 6px 10px | 6px 12px | 8px 12px | 8px 16px on .chip/.pill/.badge/.status/.tlight selectors
- .voice-orb-window[data-corner] padding: 24px (native overlay placement)
- ::-webkit-scrollbar* width/padding

<!-- Replacement mapping for common hardcoded values encountered in grep -->
- padding: 8px → var(--s-2)
- padding: 12px → var(--s-3)
- padding: 16px → var(--s-4)
- padding: 20px → var(--s-5)
- padding: 24px → var(--s-6) (except .voice-orb-window[data-corner] which is whitelisted)
- padding: 32px → var(--s-8)
- gap: 8px → var(--s-2)
- gap: 12px → var(--s-3)
- gap: 16px → var(--s-4)
- margin: 8px → var(--s-2)
- padding: 8px 12px → var(--s-2) var(--s-3)  (except on chip selectors)
- padding: 12px 16px → var(--s-3) var(--s-4)
- padding: 10px → not tokenizable; nearest is var(--s-2) = 8px OR var(--s-3) = 12px. Choose based on visual intent — usually round UP to --s-3 (12px) for readability.

<!-- Command palette open shortcut for manual sweep -->
⌘K opens CommandPalette → lists every route. Operator visits each route to visually verify.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tokenize remaining feature CSS — sweep until verify:spacing-ladder exits 0</name>
  <read_first>
    - .planning/phases/15-density-polish/SPACING-LADDER.md (canonical + whitelist)
    - Output of `node scripts/verify-spacing-ladder.mjs` (run first to get the current file + line violation list)
    - Each file with violations (per-line context — a value may be intentional chip padding that needs the whitelist selector, or may be a genuine violation)
  </read_first>
  <action>
Systematic sweep. Start by running `node scripts/verify-spacing-ladder.mjs 2>&1 | tee /tmp/ladder.out`. Process the violation list one file at a time.

For each violation:

1. Read the surrounding CSS rule to identify the selector.
2. Check if the selector matches a chip/pill/badge/status/tlight pattern → value likely belongs in the chip whitelist (already in SPACING-LADDER.md). If the value is in `CHIP_WHITELIST_VALUES` from 15-01, no fix needed — confirm the script's regex allows it. If it doesn't, the selector needs adding or the value needs changing.
3. For layout-position padding/margin/gap: map the px value to the nearest token per the Replacement mapping above. Prefer the visually closest token; for 10px default to `var(--s-3)` (12px rounds up, safer for click targets).
4. For `margin: 0 auto` / `margin: auto` / `margin: 0`: leave untouched (whitelisted).
5. For `padding: 0`: leave untouched (whitelisted).
6. Edit the file, save, re-run `node scripts/verify-spacing-ladder.mjs`, confirm the count drops.

Files in scope (in expected sweep order — roughly most-offenders first):
1. src/features/chat/chat.css
2. src/features/settings/settings.css
3. src/features/quickask/quickask.css
4. src/features/providers/providers.css
5. src/features/knowledge/KnowledgeGraph.css
6. src/features/knowledge/knowledge-rich-a.css
7. src/features/agents/agents-dag-pack.css
8. src/features/agents/SwarmDAG.css
9. src/features/agents/agents-dashboard.css
10. src/features/life-os/life-os-rich-a.css
11. src/features/life-os/life-os-rich-b.css
12. src/features/admin/admin-rich-a.css
13. src/features/dev-tools/dev-tools.css
14. src/features/dev-tools/dev-tools-rich-a.css
15. src/features/dev-tools/dev-tools-rich-b.css
16. src/features/body/body.css
17. src/features/hive/hive.css
18. src/features/onboarding/onboarding.css
19. src/features/ghost/ghost.css
20. src/features/hud/hud.css
21. src/features/voice-orb/orb.css
22. src/features/activity-log/activity-log.css
23. src/features/identity/identity.css
24. src/features/identity/identity-rich-a.css
25. src/features/identity/identity-rich-b.css

Also sweep `src/design-system/primitives/primitives.css`, `src/design-system/shell/shell.css` (already partially handled in 15-02), and any remaining files the script flags.

**Guardrails:**
- Do NOT widen the whitelist in SPACING-LADDER.md to avoid fixing a violation. If a value genuinely doesn't fit the scale, investigate whether the layout needs redesigning (likely yes).
- Do NOT break the chat-rgba / hud-chip-count / orb-rgba / ghost-no-cursor gates. Those are color/structural checks independent of spacing, but the CSS files are shared — read each existing verify script's assertions before touching `chat.css`, `hud.css`, `orb.css`, `ghost.css`.
- Do NOT introduce new selectors or rules. This is a value-swap sweep only.
- Do NOT run `cargo check` — this is CSS-only.

**Verification during the task:**
- Re-run `node scripts/verify-spacing-ladder.mjs` after each file. The violation count should monotonically decrease.
- When the count reaches 0, run `npm run verify:all` — the full chain must exit 0.
- If `verify:all` fails, the failing gate identifies which check regressed. Fix the regression (it's usually a background-color token swap that accidentally touched a color-check gate).
  </action>
  <acceptance_criteria>
    - `node scripts/verify-spacing-ladder.mjs` exits 0 with "PASS" message
    - `node scripts/verify-empty-states-copy.mjs` exits 0 (from Plan 15-03, must still pass)
    - `npm run verify:all` exits 0 (the full 27-gate chain)
    - `npx tsc --noEmit` exits 0
    - No file in `files_modified` contains a non-whitelisted hardcoded px padding/margin/gap
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'node scripts/verify-spacing-ladder.mjs && node scripts/verify-empty-states-copy.mjs && npx tsc --noEmit && npm run verify:all'</automated>
  </verify>
  <done>verify:spacing-ladder exits 0, verify:all exits 0, entire verify chain green, TypeScript clean. The automated half of Phase 15 is shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — 5-wallpaper audit + top-bar + route sweep</name>
  <what-built>
Phase 15 automated polish is complete:
- Spacing ladder documented and enforced via `verify:spacing-ladder`
- Empty-state copy rewritten across 29 files, bare-negation gate green
- Top-bar four-tier hierarchy encoded with 1280px + 1100px responsive guardrails
- Dashboard hero pulls ≥ 3 live signals (scan repos + active tentacles + perception state)
- Dashboard CSS uses glass-fill tokens exclusively (background-image dominance rule)
- All remaining 24+ feature CSS files tokenized
- `npm run verify:all` (~27 gates) green

The human verification step covers what automation cannot:
- Visual contrast and eye-path on ACTUAL rendered wallpapers
- Crowded/overlapping card edges that pass token-checks but look wrong
- Empty-state copy reads as human, not template-generated
- Top-bar hierarchy *feels* right, not just passes specificity
  </what-built>
  <how-to-verify>
**Step 0 — Start the dev instance:**
1. Run `npm run tauri dev` in the repo root (kills port 1420 first if needed).
2. Complete onboarding if prompted (use a throwaway key), or clear BLADE config beforehand to land in onboarding.
3. Wait for the main shell to load.

**Step 1 — 5-wallpaper background-image dominance audit (DENSITY-03):**
Swap the desktop wallpaper to each of the 5 baseline wallpapers used in v1.0 contrast testing. Required test wallpapers:
a. Deep indigo / dark (e.g. macOS "Graphite" or `#0a0a1d` solid)
b. Bright warm (e.g. macOS "Sequoia Light" or sunset photo)
c. Bright cool / pastel (e.g. macOS "Iridescence" or pastel gradient)
d. High-contrast photo (any landscape with bright highlights)
e. Mid-tone neutral gray gradient

For each wallpaper:
- Verify Dashboard text is clearly legible (no squinting required)
- Verify card edges/chips remain visible (glass strong enough)
- Capture a screenshot; save to `.planning/phases/15-density-polish/screenshots/wallpaper-{a..e}.png`

**Step 2 — Top-bar hierarchy at 1280px (DENSITY-04):**
1. Resize the main window to exactly 1280×720 (use the Tauri dev tools or drag-resize).
2. Confirm:
   - BLADE brand (tier 1) is visually dominant
   - Status pill (tier 2) is subordinate but readable
   - ⌘K hint (tier 3) is present but clearly tertiary
   - ActivityStrip directly below TitleBar, no gap, no overflow
   - No horizontal scrollbar
3. Resize to 1100×720 — confirm ⌘K hint hides (per 15-02 media query)
4. Capture screenshot at 1280 width → `.planning/phases/15-density-polish/screenshots/topbar-1280.png`

**Step 3 — 50-route empty-state sweep (DENSITY-05 + DENSITY-06):**
1. Open ⌘K command palette.
2. Visit EVERY listed route (use keyboard: up/down arrows + Enter).
3. On each route, confirm:
   - No "No data" / "No X yet" / "No recent X" bare text visible
   - Either real content is rendering (Phase 12/13/14 wiring), OR an EmptyState with timeline copy or a CTA button is visible
   - No visible crowded/overlapping card edges
4. Note any exceptions in the UAT doc — a route that still shows bare negation is a BLOCKER and must be fixed before this checkpoint clears.

**Step 4 — Spacing ladder spot-check (DENSITY-01, DENSITY-06):**
Visit 5 randomly-selected routes. For each, visually check:
- Card padding looks consistent (not tight in one place, loose in another)
- No "wrong" visual weight (e.g. one card clearly padded 20px, another clearly 32px)
If anything looks off, grep the source: all layout padding should be `var(--s-*)` — find the rule, screenshot it, note in UAT.

**Step 5 — Write up `.planning/phases/15-density-polish/15-05-UAT.md`:**
Structure:
```markdown
# Phase 15 UAT — Human Verification Record

Operator: {name or 'Arnav'}
Date: {YYYY-MM-DD}
Platform: {macOS / Windows / Linux} — {version}

## Wallpaper Audit (DENSITY-03)
- [x] Wallpaper A (dark indigo) — dashboard content reads clearly
- [x] Wallpaper B (bright warm) — dashboard content reads clearly
- [x] Wallpaper C (bright cool) — dashboard content reads clearly
- [x] Wallpaper D (high-contrast photo) — dashboard content reads clearly
- [x] Wallpaper E (neutral gray) — dashboard content reads clearly

Screenshots: screenshots/wallpaper-a.png through wallpaper-e.png

## Top-Bar Hierarchy (DENSITY-04)
- [x] 1280px: brand dominant, status secondary, ⌘K tertiary, no overflow
- [x] 1100px: ⌘K hint hides
- Screenshot: screenshots/topbar-1280.png

## Empty-State Sweep (DENSITY-05, DENSITY-06)
- Total routes visited: N
- Routes with real content: N
- Routes with timeline/CTA EmptyState: N
- Routes with bare negation: 0 (blocker if > 0)

## Spacing Spot-Check (DENSITY-01, DENSITY-06)
- Routes spot-checked: {list 5}
- Visually inconsistent padding found: 0 (blocker if > 0)

## Verdict
- [x] Phase 15 polish verified — merge OK
```
  </how-to-verify>
  <resume-signal>
Type "approved" after writing `.planning/phases/15-density-polish/15-05-UAT.md` and confirming all 4 checklist sections pass, or describe any BLOCKER findings (bare-negation route, overflow at 1280px, illegible wallpaper).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None net-new | CSS token swaps only; no new IPC, network, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | CSS rule changes in chat.css / hud.css / orb.css / ghost.css | mitigate | Each of these files has a dedicated verify:* gate (chat-rgba, hud-chip-count, orb-rgba, ghost-no-cursor) that runs in verify:all. Any structural break fails the phase. |
</threat_model>

<verification>
- verify:spacing-ladder exits 0
- verify:empty-states-copy exits 0
- verify:all exits 0 (full chain including all v1.0 gates)
- npx tsc --noEmit exits 0
- 15-05-UAT.md exists and records human verification of wallpaper audit, 1280px top-bar, empty-state sweep, spacing spot-check
</verification>

<success_criteria>
- DENSITY-01 + DENSITY-06 satisfied: 0 hardcoded spacing violations across 50+ routes
- DENSITY-02 satisfied: dashboard + cluster card gaps verified by automated gate + human spot-check
- DENSITY-03 satisfied: 5-wallpaper audit documented
- DENSITY-04 satisfied: top-bar hierarchy confirmed at 1280px
- DENSITY-05 satisfied: zero bare-negation empty states across the full route sweep
- DENSITY-07 satisfied: dashboard hero ≥ 3 live signals verified on cold install
- Phase 15 complete; BLADE v1.1 complete; ready for milestone close via `/gsd-complete-milestone`
</success_criteria>

<output>
After completion, create `.planning/phases/15-density-polish/15-05-SUMMARY.md`. Reference the UAT doc; include the final `npm run verify:all` output tail so the closing state is captured.
</output>
