---
phase: 15-density-polish
plan: "03"
type: execute
wave: 1
depends_on:
  - "15-01"
files_modified:
  - src/features/agents/AgentDashboard.tsx
  - src/features/agents/SwarmView.tsx
  - src/features/agents/AgentDetail.tsx
  - src/features/knowledge/KnowledgeBase.tsx
  - src/features/knowledge/ScreenTimeline.tsx
  - src/features/life-os/HealthView.tsx
  - src/features/life-os/FinanceView.tsx
  - src/features/life-os/GoalView.tsx
  - src/features/life-os/HabitView.tsx
  - src/features/life-os/MeetingsView.tsx
  - src/features/life-os/PredictionsView.tsx
  - src/features/life-os/SocialGraphView.tsx
  - src/features/life-os/AccountabilityView.tsx
  - src/features/life-os/EmotionalIntelView.tsx
  - src/features/admin/Analytics.tsx
  - src/features/admin/CapabilityReports.tsx
  - src/features/admin/DecisionLog.tsx
  - src/features/admin/SecurityDashboard.tsx
  - src/features/admin/Diagnostics.tsx
  - src/features/admin/IntegrationStatus.tsx
  - src/features/admin/McpSettings.tsx
  - src/features/admin/ModelComparison.tsx
  - src/features/admin/KeyVault.tsx
  - src/features/admin/Reports.tsx
  - src/features/admin/Temporal.tsx
  - src/features/hive/HiveMesh.tsx
  - src/features/hive/TentacleDetail.tsx
  - src/features/hive/ApprovalQueue.tsx
  - src/features/hive/AiDelegate.tsx
  - src/features/providers/FallbackOrderList.tsx
autonomous: true
requirements:
  - DENSITY-05
must_haves:
  truths:
    - "Every EmptyState usage in the scope file list either has a meaningful actionLabel + onAction (CTA pattern), OR a description prop matching the timeline regex (learning/still/give me/24h/will appear/once/etc), OR a label that itself includes timeline/CTA phrasing"
    - "After this plan lands, `node scripts/verify-empty-states-copy.mjs` exits 0"
    - "Every rewrite is semantic and human — no placeholder text like 'TBD' or 'Enter description here'"
    - "Empty-state copy for observer-tentacle-fed surfaces references the 24h learning window (ECOSYS observer cadence)"
  artifacts:
    - path: "src/features/life-os/GoalView.tsx"
      provides: "EmptyState with actionLabel='Set a goal' + onAction → openRoute('goal-create') OR description='BLADE is still learning your goals — give me 24h' when no create route exists"
      contains: "actionLabel"
    - path: "src/features/admin/DecisionLog.tsx"
      provides: "EmptyState with timeline description (BLADE is still learning / will appear after) — not 'No decisions logged'"
      contains: "learning"
    - path: "src/features/hive/AiDelegate.tsx"
      provides: "EmptyState with CTA or timeline copy — not bare 'No delegate decisions recorded'"
      contains: "actionLabel"
  key_links:
    - from: "scripts/verify-empty-states-copy.mjs"
      to: "all files in files_modified"
      via: "regex match on EmptyState props"
      pattern: "BLADE is still|learning|give me|will appear|once|after"
---

<objective>
Rewrite every bare-negation EmptyState across feature routes so `verify:empty-states-copy` (shipped in Plan 15-01) exits 0 and every empty surface signals either "there will be content" (timeline) or "you can make content happen" (CTA).

Purpose: DENSITY-05 success criterion. The tester grievance was "dashboard pages empty" — not because data never exists but because empty states said "No X" with no signal that BLADE is still populating. Phase 15's rule: empty states either have real content (handled by Phase 12/13 data wiring in Plan 15-04) OR a CTA + expected-timeline copy. This plan does the copy rewrite sweep.

Output: Every EmptyState in the 31 scope files uses either `actionLabel` + `onAction`, or a description with timeline phrasing, or a label that itself communicates timeline. No bare negation survives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-density-polish/SPACING-LADDER.md

EmptyState primitive signature:
@src/design-system/primitives/EmptyState.tsx

Coverage tracker (file scope reference):
@scripts/verify-empty-state-coverage.sh

<interfaces>
<!-- EmptyState prop shape — preserved from v1.0 Phase 9 -->

```typescript
interface EmptyStateProps {
  label: string;              // short, required
  description?: string;       // optional longer line
  actionLabel?: string;       // CTA button text; only rendered if onAction also present
  onAction?: () => void;      // CTA handler
  icon?: ReactNode;           // optional emoji/icon
  testId?: string;
}
```

<!-- Current bare-negation offenders (from grep inventory in planning) -->

Files with known bare negation:
- agents/AgentDashboard.tsx: label="No agents yet"
- agents/SwarmView.tsx: label="No swarm runs"
- agents/AgentDetail.tsx: label="No events yet"
- knowledge/KnowledgeBase.tsx: label="No matches"
- providers/FallbackOrderList.tsx: label="No providers configured yet"
- life-os/SocialGraphView.tsx: label="No contacts yet"
- life-os/GoalView.tsx: label="No goals yet"
- life-os/EmotionalIntelView.tsx: label="No insights yet"
- life-os/FinanceView.tsx: label="No transactions yet"
- life-os/HabitView.tsx: label="No habits yet"
- life-os/AccountabilityView.tsx: label="No accountability events"
- life-os/MeetingsView.tsx: label="No meetings"
- life-os/PredictionsView.tsx: label="No predictions yet"
- life-os/HealthView.tsx: label="No health data"
- admin/McpSettings.tsx: label="No MCP servers"
- admin/SecurityDashboard.tsx: label="No security alerts"
- admin/Temporal.tsx: label="No activity yet"
- admin/KeyVault.tsx: label="No API keys stored"
- admin/Diagnostics.tsx: label="No diagnostics"
- admin/DecisionLog.tsx: label="No decisions logged"
- admin/IntegrationStatus.tsx: label="No integrations configured"
- admin/ModelComparison.tsx: label="No comparisons yet"
- admin/Reports.tsx: label="No reports filed"
- admin/CapabilityReports.tsx: label="No capability reports"
- admin/Analytics.tsx: label="No analytics yet"
- hive/TentacleDetail.tsx: label="No reports from this tentacle"
- hive/AiDelegate.tsx: label="No delegate decisions recorded"

<!-- Route navigation helper (for CTAs pointing to another route) -->

Navigation is via `useRouterCtx().navigate(routeId)` or the `openRoute()` helper available in components. Existing e.g. in AiDelegate.tsx or HiveMesh.tsx likely already import one. Check per-file; do not invent an import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite bare-negation EmptyStates — Life OS + Admin clusters (16 files)</name>
  <read_first>
    - src/design-system/primitives/EmptyState.tsx
    - scripts/verify-empty-states-copy.mjs (Plan 15-01 output — gives the allow-patterns)
    - Each target file (to understand context: what kind of data is empty, whether a navigation/CTA makes sense)
  </read_first>
  <action>
Edit each of the following files. For EVERY `<EmptyState ...>` whose label matches a bare-negation pattern, rewrite per one of the three allowed forms from SPACING-LADDER.md §Empty-State Copy Rules:

**Allowed form A — Timeline description:**
```tsx
<EmptyState
  label="Habits you track will appear here"
  description="BLADE is still learning your routine — give me 24h of observed activity."
/>
```

**Allowed form B — CTA + handler:**
```tsx
<EmptyState
  label="No habits yet"
  description="Track what matters."
  actionLabel="Add a habit"
  onAction={() => /* existing navigation or create handler */}
/>
```

**Allowed form C — Label with timeline phrasing:**
```tsx
<EmptyState label="BLADE is still learning your habits" />
```

Guidance for choosing form per file:

- **Observer-tentacle-fed surfaces** (Temporal, DecisionLog, Analytics, SecurityDashboard, CapabilityReports, IntegrationStatus, TentacleDetail, AiDelegate, EmotionalIntelView, AccountabilityView, PredictionsView, MeetingsView, Reports, ModelComparison, ScreenTimeline) → use **form A** (timeline) with `"BLADE is still learning — give me 24h"` or `"will appear after N observed sessions"` variants. These populate automatically, no user CTA is correct.

- **User-authored surfaces** (GoalView, HabitView, McpSettings, KeyVault, FallbackOrderList, SocialGraphView, FinanceView, HealthView, Diagnostics, SwarmView, AgentDashboard) → use **form B** (CTA). If the file already has navigation via `useRouterCtx` or an imported `openRoute`, wire the CTA to the logical create/configure route. If no such route exists in-file, fall back to form A. NEVER invent a non-existent route id.

- **Search/filter surfaces** (KnowledgeBase "No matches", AgentDetail "No events yet") → use **form A** with search-aware copy: `"No matches for this filter — clear filters to see everything."` or `"Events will appear here as this agent works."`

For each rewrite, keep the existing `testId`/`icon` props untouched. Adjust only `label`, `description`, `actionLabel`, `onAction`.

Files in scope for Task 1 (16 files — Life OS + Admin):
1. src/features/life-os/HealthView.tsx
2. src/features/life-os/FinanceView.tsx
3. src/features/life-os/GoalView.tsx
4. src/features/life-os/HabitView.tsx
5. src/features/life-os/MeetingsView.tsx
6. src/features/life-os/PredictionsView.tsx
7. src/features/life-os/SocialGraphView.tsx
8. src/features/life-os/AccountabilityView.tsx
9. src/features/life-os/EmotionalIntelView.tsx
10. src/features/admin/Analytics.tsx
11. src/features/admin/CapabilityReports.tsx
12. src/features/admin/DecisionLog.tsx
13. src/features/admin/SecurityDashboard.tsx
14. src/features/admin/Diagnostics.tsx
15. src/features/admin/IntegrationStatus.tsx
16. src/features/admin/McpSettings.tsx

Do NOT:
- Add new imports unless they already exist in the file
- Change component props/state outside the EmptyState JSX blocks
- Reformat unrelated code
- Run cargo check
- Add emoji icons (they are discretionary — only add if the file already uses one elsewhere)

During/after:
- Run `npx tsc --noEmit` after finishing the batch — must pass
- Run `node scripts/verify-empty-states-copy.mjs` — count remaining violations; note in commit message the delta
- Run `bash scripts/verify-empty-state-coverage.sh` — must still pass (EmptyState import preserved)
  </action>
  <acceptance_criteria>
    - `grep -l 'label="No [a-z]* yet"' src/features/life-os/*.tsx src/features/admin/*.tsx` returns 0 files OR each remaining match is followed within 4 lines by `actionLabel=` or `description=`
    - `npx tsc --noEmit` exits 0
    - `bash scripts/verify-empty-state-coverage.sh` exits 0 (EmptyState imports preserved)
    - `node scripts/verify-empty-states-copy.mjs` reports fewer violations than the Wave 0 baseline (captured in 15-01-SUMMARY)
    - Every edited file contains at least one of: `actionLabel=`, `learning`, `still`, `give me`, `will appear`, `once`, `after 24h` (grep across the 16 files)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'npx tsc --noEmit && bash scripts/verify-empty-state-coverage.sh && node scripts/verify-empty-states-copy.mjs > /tmp/es.out 2>&1; CODE=$?; echo "exit $CODE"; cat /tmp/es.out | head -20; for f in src/features/life-os/HealthView.tsx src/features/life-os/GoalView.tsx src/features/life-os/HabitView.tsx src/features/admin/DecisionLog.tsx src/features/admin/Analytics.tsx src/features/admin/Diagnostics.tsx; do grep -E "actionLabel|learning|still|give me|will appear|once|after" "$f" > /dev/null || { echo "MISSING copy in $f"; exit 1; }; done'</automated>
  </verify>
  <done>All 16 Life OS + Admin files rewritten, TypeScript clean, empty-state-coverage still pass, fewer empty-states-copy violations than baseline, every edited file carries timeline/CTA phrasing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite bare-negation EmptyStates — Agents + Hive + Knowledge + Providers (13 files)</name>
  <read_first>
    - Task 1's approach (same rules apply)
    - src/features/agents/* files to understand whether CTAs can wire to existing create/spawn handlers
    - src/features/hive/* for observer-tentacle context (form A)
    - src/features/providers/FallbackOrderList.tsx (provider config — form B makes sense)
    - src/features/knowledge/KnowledgeBase.tsx, ScreenTimeline.tsx
    - src/features/admin/ModelComparison.tsx, KeyVault.tsx, Reports.tsx, Temporal.tsx (remaining admin)
  </read_first>
  <action>
Apply the same rewrite rules from Task 1 to the remaining 13 files:

1. src/features/agents/AgentDashboard.tsx — form B CTA if a "spawn agent" handler exists in-file; else form A
2. src/features/agents/SwarmView.tsx — form B if a "start swarm" path exists; else form A
3. src/features/agents/AgentDetail.tsx — form A (events populate as agent works)
4. src/features/knowledge/KnowledgeBase.tsx — form A ("No matches for this filter — clear to see everything") for the "No matches" case; preserve filter UX
5. src/features/knowledge/ScreenTimeline.tsx — form A (timeline populates from screen capture)
6. src/features/providers/FallbackOrderList.tsx — form B wired to existing provider-add flow if available; else form A
7. src/features/hive/HiveMesh.tsx — check for any EmptyState; form A (hive mesh populates from tentacles)
8. src/features/hive/TentacleDetail.tsx — form A ("Reports from this tentacle will appear as it observes — give me 24h")
9. src/features/hive/ApprovalQueue.tsx — form A ("Decisions needing your approval will appear here as BLADE acts")
10. src/features/hive/AiDelegate.tsx — form A (delegate decisions are observer-fed)
11. src/features/admin/ModelComparison.tsx — form B CTA "Compare a model" if handler exists; else form A
12. src/features/admin/KeyVault.tsx — form B wired to existing "Add key" (usually goes through Providers pane)
13. src/features/admin/Reports.tsx — form A ("Reports will appear as BLADE runs scheduled scans")
14. src/features/admin/Temporal.tsx — form A ("Activity populates as BLADE works — give me 24h")

Same guardrails as Task 1:
- Do NOT invent routes
- Do NOT add new imports unless already present
- Keep icon/testId untouched
- Do NOT reformat unrelated code

After editing:
- `npx tsc --noEmit` must pass
- `node scripts/verify-empty-states-copy.mjs` must exit 0 (this plan closes the remaining violations — if any violations remain from Task 1 files the plan is incomplete)
- `bash scripts/verify-empty-state-coverage.sh` must still pass
- Run full `npm run verify:all` — all existing gates must remain green. Phase 15 notes: "Regressions in v1.0 verify gates fail the phase"
  </action>
  <acceptance_criteria>
    - After Task 2, `node scripts/verify-empty-states-copy.mjs` exits 0 with "PASS — 0 bare-negation empty states"
    - `npx tsc --noEmit` exits 0
    - `bash scripts/verify-empty-state-coverage.sh` exits 0
    - `node scripts/verify-a11y-pass-2.mjs` exits 0 (no regression)
    - `node scripts/audit-contrast.mjs` exits 0 (no regression — contrast could change if text tier shifted)
    - `bash scripts/verify-chat-rgba.sh` exits 0
    - `bash scripts/verify-hud-chip-count.sh` exits 0
    - `bash scripts/verify-ghost-no-cursor.sh` exits 0
    - `bash scripts/verify-orb-rgba.sh` exits 0
    - `bash scripts/verify-motion-tokens.sh` exits 0
    - Every file in files_modified list has at least one of: `actionLabel=`, `learning`, `still`, `give me`, `will appear`, `once`, `after` (grep per file)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'npx tsc --noEmit && bash scripts/verify-empty-state-coverage.sh && node scripts/verify-empty-states-copy.mjs && node scripts/verify-a11y-pass-2.mjs && node scripts/audit-contrast.mjs && bash scripts/verify-chat-rgba.sh && bash scripts/verify-hud-chip-count.sh && bash scripts/verify-ghost-no-cursor.sh && bash scripts/verify-orb-rgba.sh && bash scripts/verify-motion-tokens.sh'</automated>
  </verify>
  <done>Every EmptyState in scope uses form A (timeline) or form B (CTA); verify:empty-states-copy exits 0; all other v1.0 verify gates remain green; no TypeScript regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None net-new | Pure copy + minor prop wiring. No new IPC, network, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | I (Info disclosure) | EmptyState description copy | accept | Copy may mention "BLADE is still learning" — no user PII exposed; generic observation cadence phrasing. |
| T-15-03-02 | Tampering | onAction handlers | accept | CTAs wire only to existing in-file handlers or existing openRoute ids; no new navigation targets created. |
</threat_model>

<verification>
- verify:empty-states-copy exits 0
- All existing verify:all gates remain green
- TypeScript clean
- No bare negation patterns (`No X yet` without description/CTA) in any file under src/features/
</verification>

<success_criteria>
- DENSITY-05 satisfied: every empty state across 29 files has either CTA or timeline copy
- Complements Plan 15-04 (Dashboard hero live signals): 15-04 fills *Dashboard* empty states with real data; 15-03 handles every OTHER route's empty state
</success_criteria>

<output>
After completion, create `.planning/phases/15-density-polish/15-03-SUMMARY.md`.
</output>
