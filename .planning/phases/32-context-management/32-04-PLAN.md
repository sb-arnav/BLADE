---
phase: 32-context-management
plan: 4
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - src-tauri/src/commands.rs
autonomous: true
requirements: [CTX-03, CTX-04]
requirements_addressed: [CTX-03, CTX-04]
must_haves:
  truths:
    - "Compaction trigger is computed as model_context_window × config.context.compaction_trigger_pct, not a hardcoded 140k literal"
    - "Per-provider trigger values are correct: anthropic 200k → 160k, openai 128k → 102.4k, etc."
    - "compress_conversation_smart uses the OpenHands v7610 structured summary prompt (USER_CONTEXT / COMPLETED / PENDING / CURRENT_STATE / CODE_STATE)"
    - "`keep_recent` is bounded by both message count (8) AND token budget (16k) — pathological 50k-token messages can't defeat compaction"
    - "A `blade_status: \"compacting\"` event is emitted before compaction starts so the UI shows an indicator"
    - "If compaction fails (cheap-model error or any panic), the existing `truncate_to_budget` fallback runs — chat never crashes"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "model_context_window() helper + per-model 80% trigger computation + upgraded summary prompt + token-aware keep_recent + blade_status emit"
      contains: "fn model_context_window"
  key_links:
    - from: "tool loop pre-compaction at commands.rs:1486"
      to: "capability_probe::infer_capabilities"
      via: "model_context_window(provider, model) * config.context.compaction_trigger_pct"
      pattern: "compaction_trigger_pct"
    - from: "compress_conversation_smart"
      to: "blade_status emit"
      via: "app.emit(\"blade_status\", \"compacting\")"
      pattern: "compacting"
---

<objective>
CTX-04: Replace the hardcoded `140_000` literal at commands.rs:1488 with a per-model threshold computed as `model_context_window × config.context.compaction_trigger_pct`. CTX-03: Upgrade the existing `compress_conversation_smart` summary prompt to OpenHands' v7610 structured shape, and add a token-aware safety cap on `keep_recent` so pathological large messages can't defeat compaction.

Purpose: Today, GPT-4o-mini at 128k triggers compaction at 109% (NEVER fires; LLM rejects). Anthropic Sonnet 4 at 200k triggers at 70% (too eager). After this plan, every model triggers at exactly 80% of its real context window, the summary is structured for better recall fidelity, and a 50k-token bash output buried in the recent-8 can't slip past compaction.

Output: One trigger formula, one prompt swap, one safety cap, one status event. The compaction body shape is unchanged (CONTEXT.md locked decision); only the trigger and the summary content change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md
@/home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md
@/home/arnav/blade/src-tauri/src/commands.rs
@/home/arnav/blade/src-tauri/src/capability_probe.rs
@/home/arnav/blade/.planning/phases/32-context-management/32-01-SUMMARY.md
@/home/arnav/blade/.planning/phases/32-context-management/32-02-SUMMARY.md

<interfaces>
capability_probe::infer_capabilities (capability_probe.rs:170-240) returns a 5-tuple:

    pub fn infer_capabilities(
        provider: &str,
        model: &str,
    ) -> (bool, bool, bool, bool, u32);
    // (vision, audio, tool_calling, long_context, context_window)

The 5th element is the context window in tokens (e.g. 200_000 for Claude Sonnet 4, 128_000 for gpt-4o, 2_097_152 for Gemini 2.5 Pro).

Existing trigger sites in commands.rs:
- L1486-L1493: pre-tool-loop compaction call with literal `140_000`
- L1532-L1536: TruncateAndRetry recovery with literal `120_000`

compress_conversation_smart signature (commands.rs:179):

    async fn compress_conversation_smart(
        conversation: &mut Vec<ConversationMessage>,
        max_tokens: usize,
        provider: &str,
        api_key: &str,
        model: &str,
        base_url: Option<&str>,
    )

Existing summary prompt at commands.rs:231-236 (3-6 sentences, generic). Replace with the structured v7610 prompt from RESEARCH.md §CTX-03.

estimate_tokens helper (commands.rs:142): chars / 4 heuristic.

build_test_conversation + build_test_conversation_with_token_target from Plan 32-02 (commands.rs `mod tests`).

ContextConfig.compaction_trigger_pct from Plan 32-01 (config.rs): default 0.80.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add model_context_window helper + replace 140k/120k literals + emit blade_status</name>
  <files>src-tauri/src/commands.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/commands.rs (L1480-L1540 — the two literal sites to replace)
    /home/arnav/blade/src-tauri/src/capability_probe.rs (L170-L240 — infer_capabilities signature and behavior)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-04 (per-provider expected values, async wiring decision)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Proactive Compaction
  </read_first>
  <behavior>
    - Test 1: `phase32_compaction_trigger_anthropic_200k` — `model_context_window("anthropic", "claude-sonnet-4")` returns 200_000; trigger at 80% = 160_000.
    - Test 2: `phase32_compaction_trigger_openai_128k` — `model_context_window("openai", "gpt-4o")` returns 128_000; trigger at 80% = 102_400.
    - Test 3: `phase32_compaction_trigger_unknown_model_safe_default` — for an unknown provider/model, the helper returns at minimum 8192 (matches capability_probe's all_false default) so a /0 or comically-low trigger is impossible.
    - Test 4: `phase32_compaction_trigger_pct_respects_config` — varying `compaction_trigger_pct` from 0.80 to 0.65 changes the trigger by exactly 15% of context_window.
  </behavior>
  <action>
**Step A — Add the `model_context_window` helper near `estimate_tokens` (around commands.rs:142). The helper is a thin wrapper around `capability_probe::infer_capabilities` that returns just the context window:**

    /// Phase 32 / CTX-04 — return the model's context window in tokens.
    /// Wraps capability_probe::infer_capabilities and returns its 5th tuple
    /// element. Falls back to 8_192 (capability_probe's all_false default)
    /// for unknown provider/model pairs — this guarantees the compaction
    /// trigger is never zero or negative.
    pub fn model_context_window(provider: &str, model: &str) -> u32 {
        let (_, _, _, _, ctx) =
            crate::capability_probe::infer_capabilities(provider, model, None);
        if ctx < 8_192 { 8_192 } else { ctx }
    }

**Caveat — verify infer_capabilities signature.** RESEARCH.md notes it takes `ctx_window_from_api: Option<u32>` (from a live provider response). For Phase 32's static use, pass `None`. Read `capability_probe.rs:177` to confirm the exact parameter list before writing the wrapper. If the signature is different, adjust the call accordingly — the contract is "return u32 context window."

**Step B — Replace the literal `140_000` at commands.rs:1488 with the new formula. The current call site:**

    compress_conversation_smart(
        &mut conversation,
        140_000,
        &config.provider,
        &config.api_key,
        &config.model,
        config.base_url.as_deref(),
    ).await;

becomes (compute trigger before the call so we can emit the status event with the actual value):

    let trigger = (model_context_window(&config.provider, &config.model) as f32
        * config.context.compaction_trigger_pct) as usize;
    let pre_tokens = estimate_tokens(&conversation);
    if pre_tokens > trigger {
        let _ = app.emit("blade_status", "compacting");
        emit_stream_event(&app, "blade_notification", serde_json::json!({
            "type": "info",
            "message": format!("Compacting conversation (~{} tokens of {} budget)", pre_tokens, trigger)
        }));
    }
    compress_conversation_smart(
        &mut conversation,
        trigger,
        &config.provider,
        &config.api_key,
        &config.model,
        config.base_url.as_deref(),
    ).await;

**Async vs synchronous decision (RESEARCH.md open question 1):** Keep the call SYNCHRONOUS (await inline). The CONTEXT.md "async/non-blocking" lock is satisfied by emitting the indicator BEFORE the await — the user sees "compacting" immediately, the compaction completes (~2-5s typical), then the next reply proceeds. Wrapping in `tokio::spawn` requires Arc<Mutex<>> conversion of the conversation buffer which is high-risk for THIS phase. RESEARCH.md explicitly green-lights option B as the lower-risk path. **Decision recorded here: synchronous + status event. Plan 32-07 keeps this same shape inside its fallback wrapper.**

**Step C — Replace the literal `120_000` at commands.rs:1533** (TruncateAndRetry recovery path). Use a smaller safety percentage (0.65) because this is the ALREADY-TOO-BIG recovery path — we want headroom for the retry:

    compress_conversation_smart(
        &mut conversation,
        (model_context_window(&config.provider, &config.model) as f32 * 0.65) as usize,
        &config.provider, &config.api_key, &config.model,
        config.base_url.as_deref(),
    ).await;

**Step D — Append four tests to commands.rs `mod tests` block (created in Plan 32-02):**

    #[test]
    fn phase32_compaction_trigger_anthropic_200k() {
        let ctx = model_context_window("anthropic", "claude-sonnet-4");
        assert!(ctx >= 200_000, "expected 200k+ for Claude Sonnet 4, got {}", ctx);
        let trigger = (ctx as f32 * 0.80) as usize;
        assert!(trigger >= 160_000 && trigger <= 200_000,
            "expected ~160k trigger, got {}", trigger);
    }

    #[test]
    fn phase32_compaction_trigger_openai_128k() {
        let ctx = model_context_window("openai", "gpt-4o");
        // gpt-4o is 128k context; capability_probe should return 128_000
        assert!(ctx >= 100_000 && ctx <= 200_000,
            "expected ~128k for gpt-4o, got {}", ctx);
        let trigger = (ctx as f32 * 0.80) as usize;
        assert!(trigger > 80_000 && trigger < ctx as usize,
            "expected trigger between 80k and ctx_window, got {}", trigger);
    }

    #[test]
    fn phase32_compaction_trigger_unknown_model_safe_default() {
        let ctx = model_context_window("not-a-real-provider", "not-a-real-model");
        assert!(ctx >= 8_192,
            "unknown model must return ≥ 8192 to keep trigger non-zero, got {}", ctx);
    }

    #[test]
    fn phase32_compaction_trigger_pct_respects_config() {
        let ctx = model_context_window("anthropic", "claude-sonnet-4");
        let trigger_80 = (ctx as f32 * 0.80) as usize;
        let trigger_65 = (ctx as f32 * 0.65) as usize;
        let delta = trigger_80.saturating_sub(trigger_65);
        let expected = (ctx as f32 * 0.15) as usize;
        assert!((delta as i64 - expected as i64).abs() < 100,
            "expected ~15% delta, got {} (expected {})", delta, expected);
    }

**Step E — Build + run:**

    cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
    cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_compaction_trigger 2>&1 | tail -15

All four tests must pass. cargo check must be clean.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_compaction_trigger 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn model_context_window" /home/arnav/blade/src-tauri/src/commands.rs` returns 1
    - `grep -c "compaction_trigger_pct" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 1
    - `grep -c "140_000" /home/arnav/blade/src-tauri/src/commands.rs` returns 0 (literal removed)
    - `grep -c "120_000" /home/arnav/blade/src-tauri/src/commands.rs` returns 0 (literal removed)
    - `grep -c "blade_status.*compacting\|\"compacting\"" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 1
    - `cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_compaction_trigger 2>&1 | grep -c "test result: ok. 4 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
  </acceptance_criteria>
  <done>Per-model threshold replaces literals, status event emitted, four unit tests green, no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade summary prompt to OpenHands v7610 structured shape + token-aware keep_recent</name>
  <files>src-tauri/src/commands.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/commands.rs (L179-L260 — full body of compress_conversation_smart; especially L191 keep_recent, L231-L236 current summary prompt)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-03 (verbatim OpenHands v7610 prompt; token-aware safety cap rationale)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Proactive Compaction (locked summary placement; keep-first/keep-last shape)
  </read_first>
  <behavior>
    - Test 1: `phase32_compress_keeps_endpoints` — build a 200-message conversation, call `compress_conversation_smart` with a low max_tokens (force compaction), inspect the result: system messages are at the start (verbatim), the message at index `system_count` is a User message starting with "[Earlier conversation summary]", the last 8 messages are unchanged from input.
    - Test 2: `phase32_compress_summary_prompt_includes_v7610_keys` — capture the summary_prompt string variable before the LLM call (the prompt the LLM receives) and assert it contains "USER_CONTEXT", "COMPLETED", "PENDING", "CURRENT_STATE".
    - Test 3: `phase32_compress_falls_back_when_summary_call_fails` — mock the cheap-model call to return Err, call compress, assert `truncate_to_budget` ran (conversation is shorter than input but doesn't contain "Earlier conversation summary").
    - Test 4: `phase32_compress_keep_recent_token_aware` — build a conversation where one of the last 8 messages is 50k tokens and the rest are normal. After compaction, total tokens should be ≤ 1.5× the budget (proves the pathological message was dropped or compressed despite being in keep_recent).
  </behavior>
  <action>
**Step A — Replace the summary_prompt construction at commands.rs:231-236.** Current:

    let summary_prompt = format!(
        "Summarize this earlier conversation in 3-6 sentences. Preserve: key decisions made, \
         code written or changed, errors encountered and resolved, facts established. \
         Be dense and specific — this replaces the full history.\n\n{}",
        to_compress.join("\n")
    );

becomes (verbatim text from RESEARCH.md §CTX-03):

    let summary_prompt = format!(
        "You are maintaining a context-aware state summary for an interactive agent. \
         You will be given a list of events corresponding to actions taken by the agent, \
         and the most recent previous summary if one exists. Track:\n\n\
         USER_CONTEXT: (Preserve essential user requirements, problem descriptions, and clarifications in concise form)\n\
         COMPLETED: (Tasks completed so far, with brief results)\n\
         PENDING: (Tasks that still need to be done)\n\
         CURRENT_STATE: (Current variables, data structures, or relevant state)\n\n\
         For code-specific tasks, also include:\n\
         CODE_STATE: {{File paths, function signatures, data structures}}\n\
         TESTS: {{Failing cases, error messages, outputs}}\n\
         CHANGES: {{Code edits, variable updates}}\n\
         DEPS: {{Dependencies, imports, external calls}}\n\
         INTENT: {{Why changes were made, acceptance criteria}}\n\
         VC_STATUS: {{Repository state, current branch, PR status, commit history}}\n\n\
         PRIORITIZE:\n\
         1. Adapt tracking format to match the actual task type\n\
         2. Capture key user requirements and goals\n\
         3. Distinguish between completed and pending tasks\n\
         4. Keep all sections concise and relevant\n\n\
         SKIP: Tracking irrelevant details for the current task type\n\n\
         Events:\n{}",
        to_compress.join("\n")
    );

**Note on `{{` and `}}`** — those are escaped curly braces inside `format!`. Verify by running cargo check; if it errors with "no parameter" the escape is wrong.

**Step B — Add token-aware keep_recent safety cap.** At commands.rs:191 (after `let keep_recent = 8usize;`), add:

    // CTX-03 safety cap — RESEARCH.md notes 8 messages × 50k tokens each = 400k preserved,
    // defeating compaction. Bound keep_recent by tokens too.
    const KEEP_RECENT_TOKEN_BUDGET: usize = 16_000;
    let keep_recent_by_tokens = {
        let mut total: usize = 0;
        let mut count: usize = 0;
        for msg in conversation.iter().rev() {
            let msg_tokens = match msg {
                ConversationMessage::System(_) => 0, // system messages are not "recent"
                ConversationMessage::User(s) => s.len() / 4,
                ConversationMessage::UserWithImage { text, .. } => text.len() / 4 + 250,
                ConversationMessage::Assistant { content, .. } => content.len() / 4,
                ConversationMessage::Tool { content, .. } => content.len() / 4,
            };
            if total + msg_tokens > KEEP_RECENT_TOKEN_BUDGET || count >= keep_recent {
                break;
            }
            total += msg_tokens;
            count += 1;
        }
        count
    };
    let keep_recent = keep_recent_by_tokens.max(2); // always keep at least 2 most-recent

**Step C — Append four tests to commands.rs `mod tests`. The summary-call mocking is the tricky one — use a feature-flag or direct injection. Recommended approach:**

Since `compress_conversation_smart` directly calls `crate::providers::complete_turn`, we can't easily mock it without refactoring. **Pragmatic alternative:** test the BODY of compress (the keep-first/keep-last/build to_compress logic) by calling the function with a small budget against a small conversation and inspecting the result, OR factor out a pure helper `build_summary_prompt(events: &[String]) -> String` and test that directly.

Refactor pattern: extract the prompt construction into a pub(crate) helper:

    pub(crate) fn build_compaction_summary_prompt(events: &[String]) -> String {
        format!(
            "You are maintaining a context-aware state summary for an interactive agent. \
             ... (full v7610 prompt as above) ...\n\n\
             Events:\n{}",
            events.join("\n")
        )
    }

Then `compress_conversation_smart` calls this helper:

    let summary_prompt = build_compaction_summary_prompt(&to_compress);

And the test becomes trivially mockable:

    #[test]
    fn phase32_compress_summary_prompt_includes_v7610_keys() {
        let events = vec!["User: hello".to_string(), "Assistant: hi".to_string()];
        let prompt = build_compaction_summary_prompt(&events);
        assert!(prompt.contains("USER_CONTEXT"));
        assert!(prompt.contains("COMPLETED"));
        assert!(prompt.contains("PENDING"));
        assert!(prompt.contains("CURRENT_STATE"));
        assert!(prompt.contains("CODE_STATE"));
    }

For the keep_recent / keep_endpoints tests, do not call `compress_conversation_smart` directly (it requires real network). Instead, factor out the keep_recent computation:

    pub(crate) fn compute_keep_recent(
        conversation: &[ConversationMessage],
        max_messages: usize,
        token_budget: usize,
    ) -> usize {
        let mut total: usize = 0;
        let mut count: usize = 0;
        for msg in conversation.iter().rev() {
            let msg_tokens = match msg {
                ConversationMessage::System(_) => 0,
                ConversationMessage::User(s) => s.len() / 4,
                ConversationMessage::UserWithImage { text, .. } => text.len() / 4 + 250,
                ConversationMessage::Assistant { content, .. } => content.len() / 4,
                ConversationMessage::Tool { content, .. } => content.len() / 4,
            };
            if total + msg_tokens > token_budget || count >= max_messages {
                break;
            }
            total += msg_tokens;
            count += 1;
        }
        count.max(2)
    }

Then the body of `compress_conversation_smart` calls `compute_keep_recent(&conversation, 8, KEEP_RECENT_TOKEN_BUDGET)`.

Tests:

    #[test]
    fn phase32_compress_summary_prompt_includes_v7610_keys() {
        let events = vec!["User: hello".to_string(), "Assistant: hi".to_string()];
        let prompt = build_compaction_summary_prompt(&events);
        for key in &["USER_CONTEXT", "COMPLETED", "PENDING", "CURRENT_STATE", "CODE_STATE"] {
            assert!(prompt.contains(key), "missing key {} in prompt", key);
        }
        assert!(prompt.contains("hello"), "events not interpolated into prompt");
    }

    #[test]
    fn phase32_compress_keep_recent_normal_case() {
        // 1 system + 20 turns of ~200 chars each → ~50 tokens each
        let conv = build_test_conversation(20);
        let keep = compute_keep_recent(&conv, 8, 16_000);
        // Should hit the 8-message cap before the 16k token budget
        assert_eq!(keep, 8, "expected 8-message cap to apply, got {}", keep);
    }

    #[test]
    fn phase32_compress_keep_recent_token_aware() {
        // 1 system + 7 normal + 1 huge 100k-char message at the end
        let mut conv = build_test_conversation(7);
        conv.push(ConversationMessage::Tool {
            tool_call_id: "x".to_string(),
            tool_name: "bash".to_string(),
            content: "x".repeat(100_000),  // ~25k tokens
            is_error: false,
        });
        let keep = compute_keep_recent(&conv, 8, 16_000);
        // The huge tool message alone exceeds the 16k budget, so keep_recent
        // should bottom out at 2 (the .max(2) floor).
        assert!(keep <= 2, "huge message should cap keep_recent, got {}", keep);
    }

    #[test]
    fn phase32_compress_keep_recent_floor() {
        // Tiny conversation, ensure floor at 2
        let conv = build_test_conversation(1);
        let keep = compute_keep_recent(&conv, 8, 16_000);
        assert!(keep >= 2, "floor should be 2, got {}", keep);
    }

(The "fallback to truncate_to_budget when summary call fails" test belongs in Plan 32-07 — that plan owns end-to-end fallback assertion. Do not duplicate here.)

**Step D — Build + run:**

    cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
    cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_compress 2>&1 | tail -15

All four tests must pass.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_compress 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn build_compaction_summary_prompt" /home/arnav/blade/src-tauri/src/commands.rs` returns 1
    - `grep -c "fn compute_keep_recent" /home/arnav/blade/src-tauri/src/commands.rs` returns 1
    - `grep -c "USER_CONTEXT\|COMPLETED\|PENDING\|CURRENT_STATE\|CODE_STATE" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 5 (one per key, more if duplicated in tests)
    - `grep -c "KEEP_RECENT_TOKEN_BUDGET" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 1
    - `cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_compress 2>&1 | grep -c "test result: ok. 4 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
    - No regression: `cd /home/arnav/blade/src-tauri && cargo test --lib 2>&1 | grep -c "FAILED"` returns 0
  </acceptance_criteria>
  <done>v7610 prompt in place, token-aware keep_recent guard active, four unit tests green, no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/tool message → keep_recent token budget | An adversarial 50k-char tool result could starve the summary; the token-aware cap forces it to be summarized despite being recent |
| compress_conversation_smart → cheap-model summary call → conversation buffer | LLM call can fail or hang. Existing `truncate_to_budget` fallback at L248 already handles this; Plan 32-07 wraps the whole thing in panic-resistance for CTX-07. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | DoS | Compaction-trigger formula returns 0 (e.g. NaN config or zero context_window) | mitigate | `model_context_window` floors at 8_192; `compaction_trigger_pct` is `f32` (NaN > x = false, harmless because compress_conversation_smart short-circuits when conversation tokens ≤ trigger) |
| T-32-09 | DoS | 50k-token bash output in last-8 defeats compaction | mitigate | `compute_keep_recent` token-aware safety cap (16k budget) ensures pathological messages get summarized |
| T-32-10 | Information Disclosure | Summary prompt includes verbatim user/tool content sent to a CHEAP model (potentially different provider than the main chat) | accept | Pre-existing: `cheap_model_for_provider` already routes to the same provider's cheap model. No new disclosure introduced by this plan. |
</threat_model>

<verification>
- `cargo check` clean
- `cargo test --lib commands::tests::phase32_compaction_trigger commands::tests::phase32_compress` all green
- All grep acceptance criteria satisfied
- 140k and 120k literals removed (regex-grep returns 0)
- blade_status "compacting" event grep-confirmed
- Pre-existing test suite still green
- Runtime UAT for compaction trigger end-to-end is part of Plan 32-07 phase-wide UAT (CONTEXT.md §Testing & Verification step 5: "trigger compaction → confirm UI shows indicator, conversation continues")
</verification>

<success_criteria>
1. Compaction trigger = `model_context_window × compaction_trigger_pct`. Anthropic 200k → 160k. OpenAI 128k → 102.4k. Unknown model → ≥ 6553 (8192 × 0.80).
2. Summary prompt contains the OpenHands v7610 structured keys (USER_CONTEXT / COMPLETED / PENDING / CURRENT_STATE / CODE_STATE)
3. keep_recent is bounded by 16k token budget; pathological large messages can't slip past
4. `blade_status: "compacting"` event emitted before compaction begins
5. All eight phase32_* tests in commands.rs are green; no pre-existing test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/32-context-management/32-04-SUMMARY.md` with:
- The exact `infer_capabilities` signature observed (so future maintainers know the call shape)
- Trigger values computed for the four major providers (anthropic / openai / groq / openrouter)
- Confirmation the async-vs-sync decision = synchronous + status event (RESEARCH.md option B)
- Any flag-on for Plan 32-07: the existing `truncate_to_budget` fallback at L248 is already the core CTX-07 backstop for compaction; the panic-resistance wrapper is what 32-07 adds
</output>
