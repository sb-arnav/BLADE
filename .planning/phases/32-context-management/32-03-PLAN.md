---
phase: 32-context-management
plan: 3
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - src-tauri/src/brain.rs
autonomous: true
requirements: [CTX-01, CTX-02, CTX-06]
requirements_addressed: [CTX-01, CTX-02, CTX-06]
must_haves:
  truths:
    - "score_context_relevance recognizes three new context types: identity, vision, hearing"
    - "build_system_prompt_inner gates the heavy parts of sections 0-8 by relevance score, like sections 9+ already do"
    - "A small core (BLADE.md + identity_supplement + role + tools) stays unconditional for coherence"
    - "Per-section token contributions are recorded into a thread_local accumulator (LAST_BREAKDOWN) for CTX-06 to read"
    - "config.context.smart_injection_enabled = false bypasses every new gate (CTX-07 escape hatch)"
  artifacts:
    - path: "src-tauri/src/brain.rs"
      provides: "Extended score_context_relevance keyword sets (identity, vision, hearing) + section gating for 0-8 + LAST_BREAKDOWN accumulator"
      contains: "\"identity\" =>"
    - path: "src-tauri/src/brain.rs"
      provides: "record_section helper + clear_section_accumulator + read_section_breakdown"
      contains: "fn record_section"
  key_links:
    - from: "build_system_prompt_inner sections 0-8"
      to: "score_context_relevance + smart_injection_enabled"
      via: "if config.context.smart_injection_enabled && score_context_relevance(query, label) > gate { ... }"
      pattern: "smart_injection_enabled\\s*&&"
    - from: "build_system_prompt_inner"
      to: "LAST_BREAKDOWN accumulator"
      via: "record_section(label, chars) called after each parts.push"
      pattern: "record_section\\("
---

<objective>
Implement CTX-01 (gate ALL context sections by query relevance), CTX-02 (extend `score_context_relevance` to identity/vision/hearing), and the recording substrate of CTX-06 (per-section token accumulator). Plan 32-06 then surfaces the breakdown to the frontend.

Purpose: Today, "what time is it?" gets the full character bible + OCR + hormones (~30k tokens of waste). After this plan, simple queries inject identity (~1k tokens) only and the rest gates by relevance.

Output: Behavior change. Token consumption on simple queries drops dramatically. The five-must-haves above all become true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md
@/home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md
@/home/arnav/blade/src-tauri/src/brain.rs
@/home/arnav/blade/.planning/phases/32-context-management/32-01-SUMMARY.md
@/home/arnav/blade/.planning/phases/32-context-management/32-02-SUMMARY.md

<interfaces>
brain.rs section gating template (existing pattern at L929-L1152 for sections 9+):

    let gate = thalamus_threshold(current_prompt_chars);
    if !user_query.is_empty() && score_context_relevance(user_query, "code") > gate {
        parts.push(/* heavy section */);
    }

ContextConfig from Plan 32-01 (config.rs):

    pub struct ContextConfig {
        pub smart_injection_enabled: bool,
        pub relevance_gate: f32,
        pub compaction_trigger_pct: f32,
        pub tool_output_cap_tokens: usize,
    }

ContextBreakdown from Plan 32-01 (brain.rs):

    pub struct ContextBreakdown {
        pub query_hash: String,
        pub model_context_window: u32,
        pub total_tokens: usize,
        pub sections: HashMap<String, usize>,
        pub percent_used: f32,
        pub timestamp_ms: i64,
    }

CTX_SCORE_OVERRIDE from Plan 32-02 (brain.rs, #[cfg(test)] only): used by Plan 32-07 fallback test.

score_context_relevance current keyword sets (brain.rs:222-296) already cover: code, schedule, financial, health, security, smart_home, memory, people, system, research. Plan 32-03 adds: identity, vision, hearing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend score_context_relevance with identity / vision / hearing</name>
  <files>src-tauri/src/brain.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/brain.rs (lines 218-319 score_context_relevance — the match block at L222-L296 is what we extend)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-02 (verbatim keyword sets to add)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Selective Context Injection
  </read_first>
  <behavior>
    - Test 1: `phase32_score_identity_high` — `score_context_relevance("who are you", "identity") >= 0.6` (high-keyword: "who are you").
    - Test 2: `phase32_score_identity_low` — query with no identity keyword returns 0.0.
    - Test 3: `phase32_score_vision_high` — `score_context_relevance("what's on my screen", "vision") >= 0.6`.
    - Test 4: `phase32_score_vision_low` — query with no vision keyword returns 0.0.
    - Test 5: `phase32_score_hearing_high` — `score_context_relevance("what was discussed in the meeting", "hearing") >= 0.6`.
    - Test 6: `phase32_score_unknown_type_returns_zero` — unknown context_type returns 0.0 (regression).
  </behavior>
  <action>
**Step A — Add three new arms to the match block at brain.rs:222** (between `"research" =>` at L288 and the catch-all `_ =>` at L295). Verbatim keyword sets per 32-RESEARCH.md §CTX-02:

    "identity" => (
        &["who are you", "your name", "your purpose", "remember me",
          "what do you know about me", "tell me about yourself", "your story",
          "what are you", "who am i to you", "your character"],
        &["you", "your", "yourself", "i", "me", "my"],
        &[],
    ),
    "vision" => (
        &["screen", "see", "looking at", "visible", "showing", "display",
          "on my screen", "what's on", "this page", "this app", "this window",
          "ocr", "read this", "what i'm looking at", "active app", "screenshot"],
        &["this", "that", "here", "above", "below"],
        &[],
    ),
    "hearing" => (
        &["meeting", "conversation", "they said", "what was discussed",
          "transcript", "audio", "call", "spoken", "heard", "listened",
          "voice", "podcast"],
        &["talked", "told", "saying", "speak"],
        &[],
    ),

**Step B — Append six tests to brain.rs `mod tests` block (created in Plan 32-01/32-02):**

    #[test]
    fn phase32_score_identity_high() {
        let s = score_context_relevance("who are you really", "identity");
        assert!(s >= 0.6, "expected high score, got {}", s);
    }

    #[test]
    fn phase32_score_identity_low() {
        let s = score_context_relevance("calculate sqrt of 144", "identity");
        assert_eq!(s, 0.0, "no keyword should produce 0.0, got {}", s);
    }

    #[test]
    fn phase32_score_vision_high() {
        let s = score_context_relevance("what's on my screen right now", "vision");
        assert!(s >= 0.6, "expected high score, got {}", s);
    }

    #[test]
    fn phase32_score_vision_low() {
        let s = score_context_relevance("calculate the integral of x squared", "vision");
        assert_eq!(s, 0.0);
    }

    #[test]
    fn phase32_score_hearing_high() {
        let s = score_context_relevance("what was discussed in the meeting", "hearing");
        assert!(s >= 0.6, "expected high score, got {}", s);
    }

    #[test]
    fn phase32_score_unknown_type_returns_zero() {
        let s = score_context_relevance("anything goes here", "totally-fake-type-name");
        assert_eq!(s, 0.0);
    }

**Step C — Caveat on Test 2 wording.** The medium-keyword set for `identity` includes "i" and "me" (very short tokens). Many queries contain those substrings inside other words (e.g. "calculate" contains "i"). Test 2's query "calculate sqrt of 144" was chosen to avoid both "i " and "me " word boundaries. If a future tweak makes Test 2 flaky, replace its query with a string that demonstrably hits no keyword AND no medium hit (the keyword check uses `q.contains(kw)` not word boundaries). Verify before merging.

**Step D — Build + run:**

    cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_score 2>&1 | tail -15

All six tests must pass. The existing `score_*` tests (any pre-existing) must remain green.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_score 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"identity" =>' /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c '"vision" =>' /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c '"hearing" =>' /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_score 2>&1 | grep -c "test result: ok. 6 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
  </acceptance_criteria>
  <done>Three new context types scored, six unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LAST_BREAKDOWN accumulator + gate sections 0-8 in build_system_prompt_inner</name>
  <files>src-tauri/src/brain.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/brain.rs (full body of `build_system_prompt_inner`: L456-L1200; especially L495-L828 sections 0-8; L929,958,991,1005,1034,1049,1128,1152 — the existing gate template at sections 9+)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-01 (gap list per section), §CTX-06 (record_section pattern)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Selective Context Injection (always-keep core list)
  </read_first>
  <behavior>
    - Test 1: `phase32_section_gate_simple_query` — call `build_system_prompt_inner(&[], "what time is it?", None, &ModelTier::Frontier, "anthropic", "claude-sonnet-4", 1)`, assert returned prompt is shorter than the same call with query "explain this rust trait error in detail" by at least 30%.
    - Test 2: `phase32_section_gate_smart_off_falls_through` — set `BladeConfig.context.smart_injection_enabled = false` (via test config helper or by direct config write), call inner, assert prompt is at LEAST as long as smart=on with a code query (i.e. naive path injects everything).
    - Test 3: `phase32_breakdown_records_per_section` — call inner, then read `LAST_BREAKDOWN.with(|b| b.borrow().clone())` — assert at least 3 entries with non-zero char counts and labels from the documented set.
    - Test 4: `phase32_breakdown_simple_query_excludes_heavy` — for query "what time is it?", `LAST_BREAKDOWN` does not contain entries for "character_bible", "vision", or "hearing" with non-zero counts (or contains them with 0 if labels are pre-registered).
    - Test 5: `phase32_breakdown_clears_each_call` — call inner twice with different queries; assert second call's breakdown does not contain section data from the first call.
  </behavior>
  <action>
**Step A — Add the LAST_BREAKDOWN accumulator + helpers near the top of brain.rs (place near the CTX_SCORE_OVERRIDE block from Plan 32-02, around L215-L220):**

    // ── Phase 32 / CTX-06: Per-section token recorder ─────────────────────────────
    // Populated by build_system_prompt_inner as it pushes each section. Read by
    // get_context_breakdown (Plan 32-06) to render the DoctorPane budget panel.
    //
    // thread_local justification: build_system_prompt_inner runs synchronously
    // inside send_message_stream_inline and the breakdown is read from the SAME
    // task immediately after. If a future async hop crosses worker threads,
    // switch to once_cell::Lazy<Mutex<>> per RESEARCH.md landmine #4.
    thread_local! {
        pub(crate) static LAST_BREAKDOWN: std::cell::RefCell<Vec<(String, usize)>>
            = std::cell::RefCell::new(Vec::new());
    }

    pub(crate) fn clear_section_accumulator() {
        LAST_BREAKDOWN.with(|b| b.borrow_mut().clear());
    }

    pub(crate) fn record_section(label: &str, chars: usize) {
        if chars == 0 { return; }
        LAST_BREAKDOWN.with(|b| b.borrow_mut().push((label.to_string(), chars)));
    }

    pub(crate) fn read_section_breakdown() -> Vec<(String, usize)> {
        LAST_BREAKDOWN.with(|b| b.borrow().clone())
    }

**Step B — At the TOP of `build_system_prompt_inner` (immediately after the `let mut parts: Vec<String> = Vec::new();` line ~L492 and `let config = crate::config::load_config();` line ~L493), insert:**

    clear_section_accumulator();
    let smart = config.context.smart_injection_enabled;
    let gate = config.context.relevance_gate;

**Step C — Wrap each heavy section in the gate, recording its char count after push. The pattern is:**

    if smart && !user_query.is_empty() && score_context_relevance(user_query, "<label>") > gate {
        // (existing push logic)
        record_section("<label>", parts.last().map(|s| s.len()).unwrap_or(0));
    } else if !smart {
        // CTX-07 escape hatch: naive path = unconditional inject
        // (existing push logic)
        record_section("<label>", parts.last().map(|s| s.len()).unwrap_or(0));
    }
    // (when smart=on but score <= gate, section is omitted — record 0 to surface in breakdown)
    else {
        record_section("<label>", 0);
    }

Apply this wrapping to the following sections (line numbers from current file; verify by reading the surrounding context):

| Section | Line range | Label to use | Always-keep? |
|---------|------------|--------------|--------------|
| BLADE.md identity | L499-L503 | `blade_md` | YES — unconditional, just record |
| identity_supplement | L504 | `identity_supplement` | YES — unconditional, just record |
| L0 critical facts | L508-L514 | `memory_l0` | YES — unconditional (small, ≤500 chars) |
| character bible | L516-L529 | `character_bible` | NO — gate by `score(query, "identity") > gate \|\| score(query, "memory") > gate` |
| role injection | L532-L535 | `role` | YES — unconditional (small, identity-coherent) |
| safety modulations | L538-L543 | `safety` | NO — gate by `score(query, "security") > gate` OR `safety_bundle::is_high_priority()` (read existing safety_bundle API; if it exists, OR-combine; if not, default to score gate only) |
| cortisol/oxytocin | L546-L556 | `hormones` | NO — gate: only inject when `physio.cortisol > 0.6 \|\| physio.oxytocin > 0.6` (existing condition) AND `score(query, "identity") > gate \|\| score(query, "memory") > gate` (new gate) |
| identity_extension (deep scan, user model, mirror, virtual contexts, prefrontal, learned prefs) | L558-L610 | `identity_extension` | NO — gate by `score(query, "identity") > gate \|\| score(query, "memory") > gate` |
| vision (perception_fusion / OCR) | L652-L683 | `vision` | NO — gate by `score(query, "vision") > gate` OR has-active-error (preserve existing `visible_errors` carve-out as override) |
| hearing (meeting transcripts) | L685-L711 | `hearing` | NO — gate by `score(query, "hearing") > gate` AND existing `detect_meeting_in_progress()` (AND, not OR — keep meeting precondition) |
| memory_recall | L713-L828 | `memory_recall` | already gated — verify line 713 condition still present, just add `record_section` after each push |

**Always-keep core (NEVER gate, just record):** `blade_md`, `identity_supplement`, `memory_l0`, `role`, and the active tool list (existing path, find via `parts.push` near the tool injection site, label `tools`). This preserves minimum coherence on simple queries (CONTEXT.md locked decision).

**Per-push token recording for sections 9+:** they're already gated. Just add `record_section("<label>", parts.last().map(|s| s.len()).unwrap_or(0));` IMMEDIATELY after each `parts.push(...)` in those sections. Labels: `schedule`, `code`, `security`, `health`, `system`, `financial`, `context_now`, `integrations`, `git`, `world_model`, `misc`, `scaffold`. (Use exact labels — Plan 32-06 reads these.)

**Step D — Append five tests to brain.rs `mod tests` block:**

    #[test]
    fn phase32_section_gate_simple_query() {
        // Reset overrides so production scoring runs.
        CTX_SCORE_OVERRIDE.with(|cell| { *cell.borrow_mut() = None; });
        let simple = build_system_prompt_inner(
            &[], "what time is it?", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let code = build_system_prompt_inner(
            &[], "explain this rust trait error in detail", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let ratio = simple.len() as f32 / code.len().max(1) as f32;
        assert!(ratio < 0.7,
            "simple query should produce a smaller prompt than code query (got ratio {}, simple={} code={})",
            ratio, simple.len(), code.len());
    }

    #[test]
    fn phase32_section_gate_always_keep_core_present() {
        let p = build_system_prompt_inner(
            &[], "abcxyz123", None,  // gibberish — no keyword hits
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        // BLADE.md is always present (small core)
        assert!(p.contains("BLADE") || p.len() > 100,
            "always-keep core (BLADE.md / identity_supplement) must survive gating");
    }

    #[test]
    fn phase32_breakdown_records_per_section() {
        let _ = build_system_prompt_inner(
            &[], "explain this rust function", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let breakdown = read_section_breakdown();
        assert!(breakdown.len() >= 3,
            "expected at least 3 section entries, got {:?}", breakdown);
        // The always-keep core must appear with non-zero count
        let labels: Vec<&str> = breakdown.iter().map(|(l, _)| l.as_str()).collect();
        assert!(labels.iter().any(|l| *l == "blade_md" || *l == "identity_supplement"),
            "always-keep core not recorded: {:?}", labels);
    }

    #[test]
    fn phase32_breakdown_clears_each_call() {
        let _ = build_system_prompt_inner(
            &[], "first query about code", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let first_count = read_section_breakdown().len();
        let _ = build_system_prompt_inner(
            &[], "second query about meetings", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let second_count = read_section_breakdown().len();
        // Second call's breakdown should not include first call's accumulated entries
        // (we can't compare exact counts because they may differ by content; we ensure
        // it didn't grow unboundedly).
        assert!(second_count <= first_count + 5,
            "breakdown grew unboundedly across calls: first={} second={}",
            first_count, second_count);
    }

    #[test]
    fn phase32_breakdown_simple_query_omits_vision() {
        CTX_SCORE_OVERRIDE.with(|cell| { *cell.borrow_mut() = None; });
        let _ = build_system_prompt_inner(
            &[], "what time is it?", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let breakdown = read_section_breakdown();
        let vision_chars = breakdown.iter()
            .find(|(l, _)| l == "vision")
            .map(|(_, c)| *c).unwrap_or(0);
        assert_eq!(vision_chars, 0,
            "vision section should be 0 for simple non-screen query, got {} chars",
            vision_chars);
    }

**Step E — Caveat on test 2 (`smart_injection_enabled = false`).** Toggling config requires a test seam in `crate::config::load_config()` similar to `CTX_SCORE_OVERRIDE`, OR setting an env var that load_config consults. **Decision:** for THIS task, omit the smart=off toggle test (Plan 32-07 owns CTX-07 toggle behavior end-to-end with its own seam). The 5 tests above suffice for CTX-01/02/06.

**Step F — Build and run all phase32 tests:**

    cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -20
    cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32 2>&1 | tail -25

The full pre-existing 435+ tests must remain green. Spot-check by running `cargo test --lib 2>&1 | grep "test result"` and confirming no FAIL lines.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_section_gate brain::tests::phase32_breakdown 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static LAST_BREAKDOWN" /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c "fn record_section" /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c "fn clear_section_accumulator" /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c "fn read_section_breakdown" /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c "smart_injection_enabled" /home/arnav/blade/src-tauri/src/brain.rs` returns at least 1
    - `grep -c "record_section(" /home/arnav/blade/src-tauri/src/brain.rs` returns at least 12 (one per labeled section)
    - `grep -c "score_context_relevance(user_query, \"identity\")" /home/arnav/blade/src-tauri/src/brain.rs` returns at least 1
    - `grep -c "score_context_relevance(user_query, \"vision\")" /home/arnav/blade/src-tauri/src/brain.rs` returns at least 1
    - `grep -c "score_context_relevance(user_query, \"hearing\")" /home/arnav/blade/src-tauri/src/brain.rs` returns at least 1
    - `cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_section_gate_simple_query 2>&1 | grep -c "test result: ok. 1 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_breakdown_records_per_section 2>&1 | grep -c "test result: ok. 1 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
  </acceptance_criteria>
  <done>Sections 0-8 gated, breakdown accumulator populated, simple-query prompt is measurably smaller than code-query prompt, no test regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → score function → injection gate | Untrusted user text reaches keyword matching. Worst case is a query that hits no keyword and gets the smallest prompt — that's the FEATURE, not a bug. |
| build_system_prompt_inner → LAST_BREAKDOWN thread_local | Cross-thread async hop could cause stale data. Mitigated by reading on the same task immediately after assembly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Information Disclosure | Heavy section (character bible, hormones) leaked into prompt for unrelated query | mitigate | Score-gating omits heavy sections when score ≤ gate (per CTX-01 design); always-keep core (BLADE.md, identity_supplement, role, tools) preserves coherence without leaking personal facts |
| T-32-06 | DoS | LAST_BREAKDOWN grows unboundedly across calls | mitigate | `clear_section_accumulator()` invoked at top of every `build_system_prompt_inner`; regression test `phase32_breakdown_clears_each_call` |
| T-32-07 | Tampering | NaN return from score_context_relevance dropped through `> gate` becomes `false`, omitting all context | accept (graceful) | NaN > 0.2 == false, so heavy sections silently omit. Always-keep core preserves identity. CTX-07 explicit panic-handling lives in Plan 32-07. |
</threat_model>

<verification>
- `cargo check` clean
- `cargo test --lib brain::tests::phase32_score brain::tests::phase32_section_gate brain::tests::phase32_breakdown` all green
- Existing 435+ tests show no regressions
- All grep acceptance criteria satisfied
- No new top-level Rust modules
- `safe_slice` used for any new string slicing of user/conversation content (verify via grep: any `&query[..n]` or `&user_query[..n]` introduced in this plan? Must be 0)
- Runtime UAT deferred to Plan 32-07's end-of-phase UAT (the toggle / fallback round-trip is THE verification surface for this entire wave; doing it once at the end is per CLAUDE.md Verification Protocol applied phase-wide rather than per-plan)
</verification>

<success_criteria>
1. score_context_relevance scores identity/vision/hearing queries above 0.6 and unrelated queries at 0.0
2. `build_system_prompt_inner` for "what time is it?" produces a prompt that is ≥ 30% shorter than for "explain this rust trait error in detail"
3. `LAST_BREAKDOWN` accumulator records per-section char counts; CTX-06's Tauri command (Plan 32-06) can read this snapshot
4. Always-keep core (BLADE.md, identity_supplement, role, tools) is present in every prompt regardless of query
5. All eleven phase32_* tests in brain.rs are green; no pre-existing test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/32-context-management/32-03-SUMMARY.md` with:
- Per-section gating decisions made (which `score_context_relevance` type each section was gated on, and any sections that are always-keep)
- Token reduction observed in `phase32_section_gate_simple_query` (paste actual char counts)
- Confirmation no test regressions (paste `cargo test --lib 2>&1 | grep "test result"` summary)
- Any deferred work flagged for Plan 32-06 (e.g. labels chosen for sections 9+)
</output>
