---
phase: 08-body-hive
plan: 03
type: execute
wave: 2
depends_on: ['08-01', '08-02']
files_modified:
  - src/features/body/BodyMap.tsx
  - src/features/body/BodySystemDetail.tsx
  - src/features/body/HormoneBus.tsx
  - src/features/body/OrganRegistry.tsx
  - src/features/body/DNA.tsx
  - src/features/body/WorldModel.tsx
  - src/features/body/body.css
autonomous: true
requirements:
  - BODY-01  # BodyMap — 12 system cards grid + click drill-in (SC-1 falsifier)
  - BODY-02  # BodySystemDetail — per-system module list + vitals drill-in
  - BODY-03  # HormoneBus — 10 hormone bar meters + circadian + HORMONE_UPDATE subscription (SC-2 falsifier)
  - BODY-04  # OrganRegistry — organ list + capabilities + autonomy slider
  - BODY-05  # DNA — 4 tabs (identity/goals/patterns/query)
  - BODY-06  # WorldModel — world state panels + WORLD_STATE_UPDATED subscription
  - BODY-07  # Body cluster wiring (satisfied by 08-02 wrapper + this plan's route implementations)
must_haves:
  truths:
    - "BodyMap renders 12 body-system cards in a responsive grid via bodyGetSummary() + bodyGetMap() — each card click navigates to body-system-detail and persists prefs.body.activeSystem (D-201) (BODY-01 + SC-1)"
    - "BodySystemDetail reads prefs.body.activeSystem, fetches bodyGetSystem(system) for module list, and conditionally renders cardio/urinary/reproductive/joints drill-in panels for systems with matching commands (D-202)"
    - "HormoneBus renders 10 hormone bar meters via homeostasisGet() on mount, subscribes HORMONE_UPDATE for live updates, renders 24-bar circadian histogram via homeostasisGetCircadian(), and Dialog-gates homeostasisRelearnCircadian on confirm (D-203 + SC-2)"
    - "OrganRegistry renders organGetRegistry() list — each organ row expands to show capabilities with per-action autonomy slider calling organSetAutonomy (Dialog-gated for level >= 4 per D-204)"
    - "DNA renders 4 tabs (Identity/Goals/Patterns/Query) using usePrefs 'body.dna.activeDoc' memory; Query tab calls dnaQuery(query) and displays result; Identity edit copies to clipboard + proposes via Brain query (D-203 honest deferral flagged in SUMMARY — no dna_set_* backend)"
    - "WorldModel renders world_get_state() with 5 tabs (Git / Processes / Ports / File changes / Todos), subscribes WORLD_STATE_UPDATED for live updates, and has Refresh button calling world_refresh (D-203)"
    - "All 6 body routes render without 404 in main shell; npm run dev builds; npx tsc --noEmit passes; verify:all passes"
    - "Cross-cluster imports: BodySystemDetail imports supervisorGetHealth from @/lib/tauri/admin (Phase 7); HormoneBus imports homeostasis* from @/lib/tauri/homeostasis (Phase 3). No duplication (D-194 + D-196 last bullet)"
    - "Zero Rust changes (D-196); no new wrapper file changes"
    - "No data-testid renamed from Plan 08-02 placeholders (roots preserved for Playwright specs in Plan 08-05)"
  artifacts:
    - path: "src/features/body/BodyMap.tsx"
      provides: "BODY-01 — 12 system cards + click drill-in"
      contains: "body-map-root"
    - path: "src/features/body/BodySystemDetail.tsx"
      provides: "BODY-02 — per-system module list + vitals drill-in"
      contains: "body-system-detail-root"
    - path: "src/features/body/HormoneBus.tsx"
      provides: "BODY-03 — hormone bars + circadian + HORMONE_UPDATE subscription"
      contains: "hormone-bus-root"
    - path: "src/features/body/OrganRegistry.tsx"
      provides: "BODY-04 — organ list + autonomy controls"
      contains: "organ-registry-root"
    - path: "src/features/body/DNA.tsx"
      provides: "BODY-05 — 4 tab DNA viewer + query"
      contains: "dna-root"
    - path: "src/features/body/WorldModel.tsx"
      provides: "BODY-06 — world state tabs + live update"
      contains: "world-model-root"
  key_links:
    - from: "src/features/body/BodyMap.tsx"
      to: "src/lib/tauri/body.ts bodyGetSummary"
      via: "live data fetch on mount"
      pattern: "bodyGetSummary"
    - from: "src/features/body/HormoneBus.tsx"
      to: "src/lib/events BLADE_EVENTS.HORMONE_UPDATE"
      via: "useTauriEvent"
      pattern: "HORMONE_UPDATE"
    - from: "src/features/body/WorldModel.tsx"
      to: "src/lib/events BLADE_EVENTS.WORLD_STATE_UPDATED"
      via: "useTauriEvent"
      pattern: "WORLD_STATE_UPDATED"
    - from: "src/features/body/BodyMap.tsx"
      to: "src/features/body/BodySystemDetail.tsx"
      via: "setPref('body.activeSystem') + router.openRoute('body-system-detail')"
      pattern: "body-system-detail"
---

<objective>
Fill in the 6 Body cluster routes with real data + interactions on top of the Plan 08-02 placeholders. Every route wires to the body.ts wrapper + existing homeostasis.ts wrapper + cross-cluster admin.ts imports, renders live data, and satisfies the ROADMAP success criteria for BODY-01..06 + the coverage requirement BODY-07 (satisfied collectively).

Purpose: ROADMAP Phase 8 SC-1 (BodyMap interactive visualization + click drill-in) and SC-2 (hormone dashboard live via HORMONE_UPDATE) must be falsifiable. This plan makes them so. The remaining 4 routes (BodySystemDetail, OrganRegistry, DNA, WorldModel) provide the full Body surface with no orphan screens.

Output: 6 component files replaced (each ~100-180 lines) + body.css extensions (~80 lines). ~750 net new lines. Plan completes under 45% context; heaviest content is BodySystemDetail with 5 drill-in branches + WorldModel with 5 tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-body-hive/08-CONTEXT.md
@.planning/phases/08-body-hive/08-PATTERNS.md
@.planning/phases/08-body-hive/08-01-PLAN.md
@.planning/phases/08-body-hive/08-02-PLAN.md
@.planning/phases/07-dev-tools-admin/07-03-PLAN.md
@.planning/phases/07-dev-tools-admin/07-05-PLAN.md
@.planning/phases/06-life-os-identity/06-03-PLAN.md
@CLAUDE.md
@src/lib/tauri/body.ts
@src/lib/tauri/homeostasis.ts
@src/lib/tauri/admin.ts
@src/lib/events/index.ts
@src/hooks/usePrefs.ts
@src/types/hormones.ts
@src/windows/main/useRouter.ts
@src/lib/context/ToastContext.tsx
@src/design-system/primitives/index.ts

<interfaces>
<!-- HormoneState (existing in src/types/hormones.ts — extend per your read) -->
```ts
// HormoneState canonical shape (verify in src/types/hormones.ts before use)
export interface HormoneState {
  arousal: number;
  energy_mode: number;
  exploration: number;
  trust: number;
  urgency: number;
  // + 5 more (confidence, satisfaction, nostalgia, empathy, ...) — verify against
  //   src-tauri/src/homeostasis.rs lines 28-60 and src/types/hormones.ts
  last_updated: number;
}
```

<!-- 12 body systems (from body_registry.rs — planner-picked set) -->
```
nervous vision audio muscular memory identity endocrine cardiovascular hive immune skeleton infrastructure
```

<!-- Lazy usage (from Plan 08-02 placeholders — DO NOT rename data-testids) -->
```tsx
// BodyMap data-testid="body-map-root"
// BodySystemDetail data-testid="body-system-detail-root"
// HormoneBus data-testid="hormone-bus-root"
// OrganRegistry data-testid="organ-registry-root"
// DNA data-testid="dna-root"
// WorldModel data-testid="world-model-root"
```

<!-- Pattern reference: 08-PATTERNS §3 (HormoneBus bar meters) -->
<!-- Tabbed-surface reference: 06-PATTERNS §3 (DNA + WorldModel tabs) -->
<!-- Edit-with-Dialog reference: 06-PATTERNS §4 (homeostasis_relearn_circadian + reproductive_spawn + organ_set_autonomy level >= 4) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BodyMap + BodySystemDetail — SC-1 falsifier + system drill-in</name>
  <files>src/features/body/BodyMap.tsx, src/features/body/BodySystemDetail.tsx, src/features/body/body.css</files>
  <read_first>
    - /home/arnav/blade/src/features/body/BodyMap.tsx (current placeholder from 08-02)
    - /home/arnav/blade/src/lib/tauri/body.ts (bodyGetMap, bodyGetSystem, bodyGetSummary, cardioGetBloodPressure, urinaryFlush, immuneGetStatus, reproductiveGetDna, reproductiveSpawn, jointsListProviders, jointsListStores, bladeVitalSigns)
    - /home/arnav/blade/src/lib/tauri/admin.ts (supervisorGetHealth — cross-cluster import per D-196)
    - /home/arnav/blade/src/windows/main/useRouter.ts (openRoute signature)
    - /home/arnav/blade/src/hooks/usePrefs.ts (setPref + prefs['body.activeSystem'])
    - /home/arnav/blade/src/design-system/primitives/index.ts (GlassPanel, Button, Dialog)
    - /home/arnav/blade/.planning/phases/08-body-hive/08-CONTEXT.md §D-201, §D-202
  </read_first>
  <action>
    **BodyMap.tsx** (replaces placeholder):
    - Fetch `bodyGetSummary()` on mount → array of [systemName, moduleCount].
    - Fetch `bodyGetMap()` once for hover-preview module names.
    - Render hero line: "12 body systems, N modules" (N = sum).
    - Render responsive grid `.body-map` with one `.body-system-card` per system (6 cards per row on wide). Each card:
      - `data-testid="body-system-card-{systemName}"`
      - system name, module count badge (`<Pill>`), up to 3 module names preview (from `bodyGetMap()` filtered client-side).
      - onClick: `setPref('body.activeSystem', systemName)` + `router.openRoute('body-system-detail')`.
    - Keyboard a11y: cards are `<button>` elements. Focus ring via tokens.

    **BodySystemDetail.tsx** (replaces placeholder):
    - Read `prefs['body.activeSystem']` (default 'nervous').
    - Fetch `bodyGetSystem(system)` → ModuleMapping[].
    - Hero: system name + module count + description from first module entry.
    - Tabs via 06-PATTERNS §3 recipe: "Modules" / "Vitals" / "Events".
      - Modules tab: rendered list of modules with `data-testid="module-row-{module}"`.
      - Vitals tab — branch by system name:
        - `cardiovascular`: await `cardioGetBloodPressure()` + `bladeVitalSigns()` + optional `supervisorGetHealth()` (cross-cluster) → show 3 cards.
        - `immune` OR `urinary`: await `urinaryFlush()` + `immuneGetStatus()` → 2 cards.
        - `identity`: await `reproductiveGetDna()` → 1 card; "Spawn child agent" Dialog-gated button calling `reproductiveSpawn({agentType, initialTask})`.
        - `skeleton`: await `jointsListProviders()` + `jointsListStores()` → chips.
        - Other systems: empty-state "No per-system vitals available — Phase 9 polish can add."
      - Events tab — branch by system:
        - `cardiovascular`: `cardioGetEventRegistry()` → table.
        - Others: empty state.
    - data-testid="body-system-detail-root"; tabs have `role="tablist"` + `aria-selected`.

    **body.css** additions (per 08-PATTERNS §9):
    ```css
    @layer features-body {
      .body-map { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: var(--space-4); padding: var(--space-4); }
      .body-system-card { padding: var(--space-4); border-radius: var(--radius-md); background: var(--glass-1-bg); cursor: pointer; border: 1px solid var(--border-1); text-align: left; }
      .body-system-card:hover { border-color: var(--status-running); }
      .body-system-card:focus-visible { outline: 2px solid var(--status-running); outline-offset: 2px; }
      .body-system-detail .tab-list { display: flex; gap: var(--space-2); }
      .body-system-detail .tab-panel { margin-top: var(--space-3); }
    }
    ```

    Ensure: NO `invoke('...')` direct calls — all via wrappers. NO Tailwind @apply, only utilities for layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "bodyGetSummary" src/features/body/BodyMap.tsx && grep -q "body-system-card-" src/features/body/BodyMap.tsx && grep -q "router.openRoute('body-system-detail')" src/features/body/BodyMap.tsx && grep -q "bodyGetSystem" src/features/body/BodySystemDetail.tsx && grep -q "cardioGetBloodPressure" src/features/body/BodySystemDetail.tsx && grep -q "supervisorGetHealth" src/features/body/BodySystemDetail.tsx</automated>
  </verify>
  <done>BodyMap renders summary + clickable cards + router handoff. BodySystemDetail renders tabs + vitals branch. tsc clean. SC-1 path (BodyMap → BodySystemDetail) observable in dev build.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HormoneBus (SC-2 falsifier) + OrganRegistry + DNA</name>
  <files>src/features/body/HormoneBus.tsx, src/features/body/OrganRegistry.tsx, src/features/body/DNA.tsx, src/features/body/body.css</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/08-body-hive/08-PATTERNS.md §3 (HormoneBus bar meter recipe)
    - /home/arnav/blade/src/types/hormones.ts (HormoneState exact field list)
    - /home/arnav/blade/src/lib/tauri/homeostasis.ts (4 wrappers after Plan 08-02)
    - /home/arnav/blade/src/lib/tauri/body.ts (organGetRegistry, organSetAutonomy, organGetRoster, dnaGetIdentity, dnaGetGoals, dnaGetPatterns, dnaQuery)
    - /home/arnav/blade/src/lib/events/index.ts (HORMONE_UPDATE already exists)
    - /home/arnav/blade/.planning/phases/08-body-hive/08-CONTEXT.md §D-203
  </read_first>
  <action>
    **HormoneBus.tsx** (replaces placeholder) — follow 08-PATTERNS §3 verbatim:
    - On mount: `homeostasisGet()` + `homeostasisGetCircadian()`.
    - Subscribe `useTauriEvent(BLADE_EVENTS.HORMONE_UPDATE, (e) => setState(e.payload))`.
    - Render 10 hormone bar meters (verify exact field names against src/types/hormones.ts before coding; if any field is missing from the suggested list in 08-PATTERNS, use the file's exports). Each row: label, meter width=value*100%, numeric 0.00-1.00 value.
    - Color accent per hormone (red/green/blue/purple/neutral mapping in 08-PATTERNS §3).
    - Dominant-hormone chip (largest value) rendered with `data-testid="hormone-dominant"`.
    - 24-bar circadian grid with current hour highlighted (`new Date().getHours()`).
    - "Relearn circadian" Dialog-gated button → `homeostasisRelearnCircadian()` → toast + refresh.
    - "Module directive" input → `homeostasisGetDirective(module)` → JSON table.
    - data-testid="hormone-bus-root". Rows data-testid="hormone-row-{key}".

    **OrganRegistry.tsx** (replaces placeholder):
    - On mount: `organGetRegistry()`.
    - Render list of organ rows. Each row: name chip, health chip (color via status token), summary, observations count, capabilities count. Row click toggles `.expanded` class + reveals per-capability rows.
    - Per-capability row: action + description + mutating badge + autonomy slider (0-5, step=1). Slider change:
      - If new level >= 4 → open Dialog confirm → on confirm call `organSetAutonomy({organ, action, level})`.
      - Else → call immediately.
      - Toast on success/error.
    - "View roster" button → Dialog with `organGetRoster()` raw text in `<pre>`.
    - data-testid="organ-registry-root"; organ rows data-testid="organ-row-{name}".

    **DNA.tsx** (replaces placeholder):
    - 4 pill tabs: "Identity" / "Goals" / "Patterns" / "Query". Active tab persisted via `prefs['body.dna.activeDoc']` (default 'identity').
    - Identity tab: `dnaGetIdentity()` → `<pre>` display. "Edit" button opens textarea (local state). "Copy proposed edits" button copies via `navigator.clipboard.writeText(...)` + toast "Copied — paste into dna.md manually". Honest-deferral card: "Direct write deferred — no backend `dna_set_identity` command yet (Phase 9 polish)."
    - Goals tab: `dnaGetGoals()` → `<pre>`.
    - Patterns tab: `dnaGetPatterns()` → `<pre>`.
    - Query tab: `<input>` + "Ask" button → `dnaQuery({query})` → `<GlassPanel>` result card. Empty state before first query.
    - data-testid="dna-root"; tabs have `role="tablist"` + `aria-selected`.

    **body.css** additions (hormone-row + circadian from 08-PATTERNS §9; organ expanded row; dna tab list).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "HORMONE_UPDATE" src/features/body/HormoneBus.tsx && grep -q "homeostasisGet\\b" src/features/body/HormoneBus.tsx && grep -q "homeostasisRelearnCircadian" src/features/body/HormoneBus.tsx && grep -q "organGetRegistry" src/features/body/OrganRegistry.tsx && grep -q "organSetAutonomy" src/features/body/OrganRegistry.tsx && grep -q "dnaQuery" src/features/body/DNA.tsx && grep -q "body.dna.activeDoc" src/features/body/DNA.tsx</automated>
  </verify>
  <done>HormoneBus renders 10 bars + circadian + relearn Dialog + HORMONE_UPDATE subscription. OrganRegistry renders list + expand + autonomy slider with Dialog-gate. DNA renders 4 tabs + query. tsc clean. SC-2 path observable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: WorldModel — 5 tabs + WORLD_STATE_UPDATED live refresh</name>
  <files>src/features/body/WorldModel.tsx, src/features/body/body.css</files>
  <read_first>
    - /home/arnav/blade/src/lib/tauri/body.ts (worldGetState, worldGetSummary, worldRefresh + WorldState type)
    - /home/arnav/blade/src/lib/events/index.ts (WORLD_STATE_UPDATED added in Plan 08-01)
    - /home/arnav/blade/src/lib/events/payloads.ts (WorldStateUpdatedPayload)
    - /home/arnav/blade/.planning/phases/08-body-hive/08-CONTEXT.md §D-203 (WorldModel section)
  </read_first>
  <action>
    **WorldModel.tsx** (replaces placeholder):
    - On mount: `worldGetState()`.
    - Subscribe `useTauriEvent(BLADE_EVENTS.WORLD_STATE_UPDATED, () => worldGetState().then(setState))` (payload is a summary string; refetch for the full state).
    - Hero: timestamp (rendered as relative time), workspace_cwd (monospace), active_window, network_activity, system_load 3 bars (CPU/RAM/disk).
    - 5 pill tabs: "Git" / "Processes" / "Ports" / "File changes" / "Todos". No prefs persistence needed; local state only.
    - Git tab: table of `git_repos` (path, branch, uncommitted/untracked/ahead counts, last_commit truncated).
    - Processes tab: top 20 by CPU from `running_processes`; columns pid, name, cpu%, mem MB.
    - Ports tab: list `open_ports` (port, service?, local_addr?).
    - File changes tab: `recent_file_changes` (path, kind, timestamp relative).
    - Todos tab: `pending_todos` list.
    - "Refresh" button: `worldRefresh()` → `setState(result)` + toast "World refreshed".
    - "Summary" button → Dialog with `worldGetSummary()` raw text in `<pre>`.
    - data-testid="world-model-root"; tabs data-testid="world-tab-{name}".

    **body.css** additions:
    ```css
    @layer features-body {
      .world-model { display: grid; grid-template-rows: auto 1fr; gap: var(--space-4); padding: var(--space-4); }
      .world-hero { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: var(--space-3); }
      .world-load-bar { height: 6px; background: var(--glass-1-bg); border-radius: 3px; overflow: hidden; }
      .world-load-bar > div { height: 100%; background: var(--status-running); transition: width 250ms ease; }
      .world-table { width: 100%; border-collapse: collapse; font-family: var(--font-mono); font-size: 12px; }
      .world-table th, .world-table td { padding: var(--space-2); border-bottom: 1px solid var(--border-1); text-align: left; }
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "worldGetState" src/features/body/WorldModel.tsx && grep -q "WORLD_STATE_UPDATED" src/features/body/WorldModel.tsx && grep -q "worldRefresh" src/features/body/WorldModel.tsx && npm run verify:all</automated>
  </verify>
  <done>WorldModel renders hero + 5 tabs + live refresh + manual refresh. tsc clean. verify:all passes. All 6 Body routes fully wired.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run verify:all` exits 0
- `npm run lint` passes (no-raw-tauri)
- All 6 body routes have `data-testid="{route-id}-root"` preserved from Plan 08-02 placeholders
- Every wrapper function called exists in src/lib/tauri/body.ts or @/lib/tauri/homeostasis.ts or @/lib/tauri/admin.ts
- No new Rust changes
- All Dialog-gated actions (homeostasis_relearn_circadian, organ_set_autonomy level >= 4, reproductive_spawn) have confirm-cancel buttons
</verification>

<success_criteria>
- ROADMAP Phase 8 SC-1: "BodyMap route renders an interactive visualization of 12 body systems loaded from body_registry.rs; clicking a system drills into BodySystemDetail without error" — observable (bodyGetSummary + click handler + router.openRoute wired)
- ROADMAP Phase 8 SC-2: "The hormone dashboard displays all 10 hormone values and updates in real time when hormone_update events arrive from homeostasis.rs" — observable (homeostasisGet on mount + HORMONE_UPDATE subscription wired; 10 hormone rows rendered)
- BODY-01..06 all implemented; BODY-07 coverage requirement satisfied collectively (flagged in SUMMARY for retrospective)
- Cross-cluster imports documented in file headers (supervisorGetHealth from admin.ts; homeostasis.ts Phase 3)
- Zero Rust changes (D-196)
- `data-testid`s match Plan 08-05 Playwright spec expectations
</success_criteria>

<output>
After completion, create `.planning/phases/08-body-hive/08-03-SUMMARY.md`.
</output>
