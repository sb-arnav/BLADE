---
phase: 08-body-hive
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tauri/body.ts
  - src/lib/tauri/hive.ts
  - src/lib/tauri/homeostasis.ts
  - src/lib/tauri/index.ts
  - src/features/body/index.tsx
  - src/features/hive/index.tsx
  - src/features/body/types.ts
  - src/features/hive/types.ts
  - src/features/body/body.css
  - src/features/hive/hive.css
  - src/features/body/BodyMap.tsx
  - src/features/body/BodySystemDetail.tsx
  - src/features/body/HormoneBus.tsx
  - src/features/body/OrganRegistry.tsx
  - src/features/body/DNA.tsx
  - src/features/body/WorldModel.tsx
  - src/features/hive/HiveMesh.tsx
  - src/features/hive/TentacleDetail.tsx
  - src/features/hive/AutonomyControls.tsx
  - src/features/hive/ApprovalQueue.tsx
  - src/features/hive/AiDelegate.tsx
autonomous: true
requirements:
  - BODY-01  # BodyMap — requires bodyGetSummary, bodyGetMap
  - BODY-02  # BodySystemDetail — requires bodyGetSystem + cardio/urinary/repro/joints drill-ins
  - BODY-03  # HormoneBus — requires homeostasisGet* (wrappers already exist; add 4th: homeostasisRelearnCircadian)
  - BODY-04  # OrganRegistry — requires organGetRegistry, organSetAutonomy, organGetRoster
  - BODY-05  # DNA — requires dnaGetIdentity, dnaGetGoals, dnaGetPatterns, dnaQuery
  - BODY-06  # WorldModel — requires worldGetState, worldGetSummary, worldRefresh
  - BODY-07  # Body cluster wrapper coverage (SATISFIED BY this plan via body.ts)
  - HIVE-01  # HiveMesh — requires hiveGetStatus, hiveSetAutonomy
  - HIVE-02  # TentacleDetail — requires hiveGetStatus, hiveGetReports, hiveSpawnTentacle, organGetAutonomy
  - HIVE-03  # AutonomyControls — requires organGetAutonomy, organSetAutonomy, hiveSetAutonomy
  - HIVE-04  # ApprovalQueue — requires hiveGetStatus (decisions), hiveApproveDecision
  - HIVE-05  # Hive cluster wrapper coverage (SATISFIED BY this plan via hive.ts per D-195 + DP-3)
  - HIVE-06  # AiDelegate — requires aiDelegateCheck, aiDelegateIntroduce
must_haves:
  truths:
    - "src/lib/tauri/body.ts exports ~22 camelCase wrapper functions spanning 10 Rust modules — body_registry, organ, dna, world_model, cardiovascular, urinary, reproductive, joints; plus re-exports homeostasis + admin.supervisorGetHealth via cross-cluster import note (D-194 + D-196)"
    - "src/lib/tauri/hive.ts exports ~11 camelCase wrapper functions spanning 2 Rust modules — hive (8 commands) + ai_delegate (2 commands) + 1 cross-reference to organ_set/get_autonomy in body.ts"
    - "src/lib/tauri/homeostasis.ts EXTENDED with the 4th command wrapper: homeostasisRelearnCircadian() (D-194 — add in-situ not in body.ts)"
    - "Both new wrapper files use invokeTyped<TReturn, TArgs>(command, args); raw invoke NEVER used (ESLint no-raw-tauri passes)"
    - "Each wrapper has JSDoc @see src-tauri/src/<file>.rs citation matching the Rust #[tauri::command] (D-206 / D-38 naming discipline)"
    - "Rust enum types (TentacleStatus, Priority, Decision tagged-union) mirrored as TS union types in hive.ts"
    - "src/features/body/index.tsx REWRITTEN — 6 lazy imports of per-route files — each component file created with minimal GlassPanel placeholder (Plan 08-03 fills in the bodies)"
    - "src/features/hive/index.tsx REWRITTEN — 5 lazy imports of per-route files — each component file created with minimal placeholder (Plan 08-04 fills in the bodies)"
    - "Both cluster index.tsx files preserve route id order and RouteDefinition[] shape (D-199 single-writer discipline)"
    - "Per-route files are MINIMAL placeholders — each exports a named component that renders a GlassPanel wrapper with route label + data-testid=<route-id>-root + 'Ships in Plan 08-NN' placeholder text"
    - "src/features/body/types.ts + src/features/hive/types.ts re-export cluster-local types from the wrappers + cluster-only UI types (BodySystemName union) — D-208"
    - "src/features/body/body.css + src/features/hive/hive.css created with @layer features-body / features-hive scoping (D-210) + reuse Phase 5 status tokens (D-132) + hormone-meter + circadian-grid + tentacle-grid classes"
    - "src/lib/tauri/index.ts barrel re-exports both new modules for convenience (consistent with Phase 5/6/7 pattern)"
    - "npx tsc --noEmit passes; ESLint passes; npm run verify:all passes; npm run dev builds without error (route lazy imports resolve)"
    - "Zero Rust changes (D-196 + D-200)"
  artifacts:
    - path: "src/lib/tauri/body.ts"
      provides: "Typed wrappers for ~22 Body commands across 10 Rust modules"
      exports: ["bodyGetMap", "bodyGetSystem", "bodyGetSummary", "organGetRegistry", "organGetRoster", "organSetAutonomy", "organGetAutonomy", "dnaGetIdentity", "dnaGetGoals", "dnaGetPatterns", "dnaQuery", "worldGetState", "worldGetSummary", "worldRefresh", "cardioGetBloodPressure", "cardioGetEventRegistry", "bladeVitalSigns", "urinaryFlush", "immuneGetStatus", "reproductiveGetDna", "reproductiveSpawn", "jointsListProviders", "jointsListStores"]
    - path: "src/lib/tauri/hive.ts"
      provides: "Typed wrappers for ~11 Hive + AI-Delegate commands"
      exports: ["hiveStart", "hiveStop", "hiveGetStatus", "hiveGetDigest", "hiveSpawnTentacle", "hiveGetReports", "hiveApproveDecision", "hiveSetAutonomy", "aiDelegateIntroduce", "aiDelegateCheck"]
    - path: "src/lib/tauri/homeostasis.ts"
      provides: "Extended with 4th wrapper homeostasisRelearnCircadian"
      contains: "homeostasis_relearn_circadian"
    - path: "src/features/body/index.tsx"
      provides: "6 lazy route imports replacing Phase 1 skeletons"
      contains: "lazy(() => import('./BodyMap')"
    - path: "src/features/hive/index.tsx"
      provides: "5 lazy route imports replacing Phase 1 skeletons"
      contains: "lazy(() => import('./HiveMesh')"
    - path: "src/features/body/types.ts"
      provides: "Cluster-local type exports"
    - path: "src/features/hive/types.ts"
      provides: "Cluster-local type exports"
    - path: "src/features/body/body.css"
      provides: "Body cluster scoped CSS"
    - path: "src/features/hive/hive.css"
      provides: "Hive cluster scoped CSS"
  key_links:
    - from: "src/features/body/index.tsx"
      to: "src/features/body/*.tsx"
      via: "lazy dynamic imports"
      pattern: "lazy\\(\\(\\) => import\\('\\./"
    - from: "src/features/hive/index.tsx"
      to: "src/features/hive/*.tsx"
      via: "lazy dynamic imports"
      pattern: "lazy\\(\\(\\) => import\\('\\./"
    - from: "src/lib/tauri/body.ts"
      to: "src-tauri/src/{body_registry,organ,dna,world_model,cardiovascular,urinary,reproductive,joints}.rs"
      via: "invokeTyped with snake_case command names"
      pattern: "invokeTyped"
    - from: "src/lib/tauri/hive.ts"
      to: "src-tauri/src/{hive,ai_delegate}.rs"
      via: "invokeTyped with snake_case command names"
      pattern: "invokeTyped"
---

<objective>
Ship the typed Tauri wrapper surface for both Phase 8 clusters and rewrite both cluster `index.tsx` files from Phase 1 ComingSoonSkeleton stubs to lazy-imports of real per-route components. Simultaneously seed 11 minimal placeholder component files so Plans 08-03 / 08-04 have file skeletons to fill in (single-writer invariant — later plans never edit index.tsx). This is the substrate plan for Phase 8 — every Body + Hive route must be able to import from `@/lib/tauri/body` or `@/lib/tauri/hive` after this plan lands.

Purpose: body.ts and hive.ts are THE typed contracts between Phase 8 UI and the Rust surface. Zero raw `invoke('...')` calls allowed (ESLint `no-raw-tauri`). Every command from the D-196 inventory gets exactly one wrapper — no multiplexing, no speculative commands.

Output: 2 new wrapper files (~650 lines total), 1 extended wrapper file (+25 lines), 2 index.tsx rewrites (~40 lines each), 11 placeholder component files (~30 lines each), 2 types files, 2 CSS files, 1 barrel update. ~850 net new lines. Plan completes under 40% context — bulk is repetitive wrapper code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-body-hive/08-CONTEXT.md
@.planning/phases/08-body-hive/08-PATTERNS.md
@.planning/phases/07-dev-tools-admin/07-02-PLAN.md
@.planning/phases/07-dev-tools-admin/07-02-SUMMARY.md
@.planning/phases/06-life-os-identity/06-02-PLAN.md
@CLAUDE.md
@src/lib/tauri/_base.ts
@src/lib/tauri/homeostasis.ts
@src/lib/tauri/admin.ts
@src/lib/tauri/index.ts
@src/features/body/index.tsx
@src/features/hive/index.tsx

<interfaces>
<!-- Rust struct shapes to mirror in wrappers (from 08-CONTEXT §D-196 + 08-PATTERNS §1) -->

```
body_registry.rs (lib.rs:1314-1316):
  body_get_map()            -> Vec<ModuleMapping>  { module, body_system, organ, description }
  body_get_system(system)   -> Vec<ModuleMapping>
  body_get_summary()        -> Vec<(String, usize)>

organ.rs (lib.rs:1298-1301):
  organ_get_registry()      -> Vec<OrganStatus>   { name, health, summary, recent_observations, capabilities: OrganCapability[] }
  organ_get_roster()        -> String             (brain-style identity text)
  organ_set_autonomy(organ, action, level: u8)  -> Result<(), String>  // level 0-5
  organ_get_autonomy(organ, action)              -> u8

dna.rs (lib.rs:1289-1292):
  dna_get_identity()        -> String
  dna_get_goals()           -> String
  dna_get_patterns()        -> String
  dna_query(query)          -> String

world_model.rs (lib.rs:967-969):
  world_get_state()         -> WorldState  { timestamp, git_repos, running_processes, open_ports, recent_file_changes, system_load, active_window, workspace_cwd, pending_todos, network_activity }
  world_get_summary()       -> String
  world_refresh()           -> WorldState

cardiovascular.rs (commands @ 303-315):
  cardio_get_blood_pressure()  -> BloodPressure
  cardio_get_event_registry()  -> Vec<EventInfo>
  blade_vital_signs()          -> VitalSigns

urinary.rs (@ 203-209):
  urinary_flush()              -> u64
  immune_get_status()          -> ImmuneStatus

reproductive.rs (@ 216-222):
  reproductive_get_dna()       -> InheritedDna
  reproductive_spawn(agent_type, initial_task?)  -> Result<String, String>

joints.rs (@ 284-293):
  joints_list_providers()      -> Vec<String>
  joints_list_stores()         -> Vec<String>

homeostasis.rs (@ 841-867):  // 3 already wrapped in homeostasis.ts (Phase 3)
  homeostasis_get()                -> HormoneState     ✓ EXISTS
  homeostasis_get_directive(module)-> ModuleDirective  ✓ EXISTS
  homeostasis_get_circadian()      -> Vec<f32>         ✓ EXISTS
  homeostasis_relearn_circadian()  -> Vec<f32>         ← ADD THIS (D-194 — in-situ)

hive.rs (@ 3252..3340):
  hive_start()                     -> Result<HiveStatus, String>
  hive_stop()                      -> Result<(), String>
  hive_get_status()                -> HiveStatus
  hive_get_digest()                -> String
  hive_spawn_tentacle(platform, config)  -> Result<(), String>
  hive_get_reports()               -> Vec<TentacleReport>
  hive_approve_decision(head_id, decision_index)  -> Result<(), String>
  hive_set_autonomy(level: f32)    -> Result<(), String>

ai_delegate.rs (@ 166-177):
  ai_delegate_introduce()          -> Result<String, String>
  ai_delegate_check()              -> serde_json::Value  { name, available, reasoning? }

HiveStatus enum fields (hive.rs:248..275):
  { running, tentacle_count, active_tentacles, head_count, pending_decisions, pending_reports, last_tick, total_reports_processed, total_actions_taken, autonomy, tentacles: TentacleSummary[], recent_decisions: Decision[] }

TentacleStatus = 'Active' | 'Dormant' | 'Error' | 'Disconnected'   // Rust Serialize-as-string
Priority       = 'Critical' | 'High' | 'Normal' | 'Low'
Decision       = tagged union (#[serde(tag = "type", content = "data")]) — 4 variants Reply/Escalate/Act/Inform
```

See 08-PATTERNS.md §1 for the full target body.ts + hive.ts shape. Follow the pattern file verbatim.

<!-- RouteDefinition shape -->

```ts
// src/lib/router.ts (from Phase 1)
export interface RouteDefinition {
  id: string;
  label: string;
  section?: string;
  component: React.LazyExoticComponent<React.ComponentType<unknown>>;
  phase?: number;
  description?: string;
}
```

<!-- Minimal placeholder component shape -->

```tsx
// src/features/body/BodyMap.tsx (Plan 08-02 placeholder — Plan 08-03 replaces)
import { GlassPanel } from '@/design-system/primitives';
import './body.css';

export function BodyMap() {
  return (
    <GlassPanel className="body-map" data-testid="body-map-root">
      <h2>Body Map</h2>
      <p>Ships in Plan 08-03 (BODY-01).</p>
    </GlassPanel>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/tauri/body.ts + extend homeostasis.ts</name>
  <files>src/lib/tauri/body.ts, src/lib/tauri/homeostasis.ts, src/lib/tauri/index.ts</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/08-body-hive/08-PATTERNS.md §1 (full body.ts shape)
    - /home/arnav/blade/src/lib/tauri/_base.ts (invokeTyped signature)
    - /home/arnav/blade/src/lib/tauri/homeostasis.ts (existing 3 wrappers; append pattern)
    - /home/arnav/blade/src/lib/tauri/admin.ts (line 1-120 for reference wrapper-file style)
    - /home/arnav/blade/src-tauri/src/body_registry.rs, organ.rs, dna.rs, world_model.rs, cardiovascular.rs, urinary.rs, reproductive.rs, joints.rs (verify exact command signatures before writing wrappers)
    - /home/arnav/blade/src-tauri/src/lib.rs lines 1284-1338 (confirm all commands registered)
  </read_first>
  <action>
    **Step A — Create `src/lib/tauri/body.ts`:** follow 08-PATTERNS §1 verbatim. ~22 wrappers + ~10 hand-typed interfaces. Structure:

    1. Imports + header comment (cite 08-CONTEXT §D-194, §D-196; note cross-cluster pattern).
    2. `export * as homeostasis from './homeostasis';` re-export (D-194).
    3. All type interfaces (ModuleMapping, OrganStatus, OrganCapability, WorldState, GitRepoState, ProcessInfo, PortInfo, FileChange, SystemLoad, TodoItem, BloodPressure, EventInfo, VitalSigns, ImmuneStatus, InheritedDna). Every interface has `[k: string]: unknown` index signature.
    4. Body registry wrappers (3): `bodyGetMap`, `bodyGetSystem({system})`, `bodyGetSummary`.
    5. Organ wrappers (4): `organGetRegistry`, `organGetRoster`, `organSetAutonomy({organ, action, level})`, `organGetAutonomy({organ, action})`.
    6. DNA wrappers (4): `dnaGetIdentity`, `dnaGetGoals`, `dnaGetPatterns`, `dnaQuery({query})`.
    7. World-model wrappers (3): `worldGetState`, `worldGetSummary`, `worldRefresh`.
    8. Cardiovascular wrappers (3): `cardioGetBloodPressure`, `cardioGetEventRegistry`, `bladeVitalSigns`.
    9. Urinary wrappers (2): `urinaryFlush`, `immuneGetStatus`.
    10. Reproductive wrappers (2): `reproductiveGetDna`, `reproductiveSpawn({agentType, initialTask?})`.
    11. Joints wrappers (2): `jointsListProviders`, `jointsListStores`.

    Each wrapper: `@see src-tauri/src/<file>.rs:<line> <command_name>` JSDoc. camelCase function names; snake_case command strings + arg key conversions. No zod. No codegen.

    **Step B — Extend `src/lib/tauri/homeostasis.ts`:** append a 4th wrapper (D-194 in-situ):

    ```ts
    /**
     * @see src-tauri/src/homeostasis.rs:862
     *   `pub fn homeostasis_relearn_circadian() -> Vec<f32>`
     *
     * Recomputes the circadian profile from recent activity. Returns the fresh 24-hour array.
     * Dialog-gate this at call site (D-205) — it overwrites the cached profile.
     */
    export function homeostasisRelearnCircadian(): Promise<number[]> {
      return invokeTyped<number[]>('homeostasis_relearn_circadian');
    }
    ```

    **Step C — Update `src/lib/tauri/index.ts` barrel:** add `export * from './body';` and `export * from './hive';` AFTER existing cluster exports. Preserve existing exports.

    Cross-check: `grep -c "invokeTyped" src/lib/tauri/body.ts` returns ≥ 22 (one per wrapper). `grep -c "@see src-tauri/src" src/lib/tauri/body.ts` returns ≥ 22.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "invokeTyped" src/lib/tauri/body.ts | awk '$1>=22{exit 0}{exit 1}' && grep -q "homeostasisRelearnCircadian" src/lib/tauri/homeostasis.ts && grep -q "export \\* from './body'" src/lib/tauri/index.ts</automated>
  </verify>
  <done>body.ts exports ≥ 22 camelCase wrappers via invokeTyped. homeostasis.ts has 4th wrapper. Barrel re-exports body. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/tauri/hive.ts</name>
  <files>src/lib/tauri/hive.ts, src/lib/tauri/index.ts</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/08-body-hive/08-PATTERNS.md §1 (full hive.ts shape)
    - /home/arnav/blade/src-tauri/src/hive.rs lines 3252-3340 (all 8 hive commands) + lines 46-280 (enum + struct definitions)
    - /home/arnav/blade/src-tauri/src/ai_delegate.rs lines 166-180 (2 ai_delegate commands)
    - /home/arnav/blade/src-tauri/src/lib.rs lines 656-657, 1284-1288, 1336-1338 (registration verification)
  </read_first>
  <action>
    Create `src/lib/tauri/hive.ts` following 08-PATTERNS §1 verbatim.

    1. Imports + header comment (cite 08-CONTEXT §D-195, §D-196).
    2. TS union types for Rust string-serialized enums:
       - `export type TentacleStatus = 'Active' | 'Dormant' | 'Error' | 'Disconnected';`
       - `export type Priority = 'Critical' | 'High' | 'Normal' | 'Low';`
    3. Decision tagged-union (matches `#[serde(tag = "type", content = "data")]`):
       ```ts
       export type Decision =
         | { type: 'Reply'; data: { platform: string; to: string; draft: string; confidence: number } }
         | { type: 'Escalate'; data: { reason: string; context: string } }
         | { type: 'Act'; data: { action: string; platform: string; reversible: boolean } }
         | { type: 'Inform'; data: { summary: string } };
       ```
    4. Supporting interfaces: `TentacleReport`, `TentacleSummary`, `HiveStatus`, `AiDelegateInfo`. All with `[k: string]: unknown`.
    5. Hive wrappers (8): `hiveStart`, `hiveStop`, `hiveGetStatus`, `hiveGetDigest`, `hiveSpawnTentacle({platform, config})`, `hiveGetReports`, `hiveApproveDecision({headId, decisionIndex})`, `hiveSetAutonomy({level})`.
    6. AI-Delegate wrappers (2): `aiDelegateIntroduce`, `aiDelegateCheck`.

    Each wrapper: `@see src-tauri/src/<file>.rs:<line>` JSDoc. Snake_case args in invokeTyped payload; camelCase exposed to callers.

    Update `src/lib/tauri/index.ts` barrel already done in Task 1 — if missing, add it.

    Cross-check: `grep -c "invokeTyped" src/lib/tauri/hive.ts` returns exactly 10 (8 hive + 2 ai_delegate).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -c "invokeTyped" src/lib/tauri/hive.ts) -eq 10 && grep -q "export type Decision" src/lib/tauri/hive.ts && grep -q "TentacleStatus" src/lib/tauri/hive.ts</automated>
  </verify>
  <done>hive.ts exports exactly 10 camelCase wrappers via invokeTyped. Decision tagged union + TentacleStatus enum declared. Barrel re-exports hive. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite body/hive index.tsx + seed 11 placeholder component files + 2 types + 2 CSS</name>
  <files>src/features/body/index.tsx, src/features/hive/index.tsx, src/features/body/types.ts, src/features/hive/types.ts, src/features/body/body.css, src/features/hive/hive.css, src/features/body/BodyMap.tsx, src/features/body/BodySystemDetail.tsx, src/features/body/HormoneBus.tsx, src/features/body/OrganRegistry.tsx, src/features/body/DNA.tsx, src/features/body/WorldModel.tsx, src/features/hive/HiveMesh.tsx, src/features/hive/TentacleDetail.tsx, src/features/hive/AutonomyControls.tsx, src/features/hive/ApprovalQueue.tsx, src/features/hive/AiDelegate.tsx</files>
  <read_first>
    - /home/arnav/blade/.planning/phases/08-body-hive/08-PATTERNS.md §2 (index rewrite shape) + §9 (CSS template)
    - /home/arnav/blade/src/features/body/index.tsx (current Phase 1 stubs — preserve route id/label order)
    - /home/arnav/blade/src/features/hive/index.tsx (same)
    - /home/arnav/blade/src/features/admin/index.tsx (Phase 7 reference for rewrite shape)
    - /home/arnav/blade/src/design-system/primitives/index.ts (GlassPanel export verified)
  </read_first>
  <action>
    **Step A — Rewrite `src/features/body/index.tsx`** per 08-PATTERNS §2 exactly. Replace the 6 skeleton factory calls with 6 `lazy(() => import('./ComponentName').then((m) => ({ default: m.ComponentName })))` entries. Preserve:
    - Route id order: body-map, body-system-detail, hormone-bus, organ-registry, dna, world-model
    - Labels verbatim from Phase 1
    - `section: 'body'`, `phase: 8`
    - Top file comment updated to reference Plan 08-02 / D-199

    **Step B — Rewrite `src/features/hive/index.tsx`** same pattern. 5 lazy imports. Route id order: hive-mesh, hive-tentacle, hive-autonomy, hive-approval-queue, hive-ai-delegate. Preserve the `description: 'All tentacles overview'` on hive-mesh.

    **Step C — Create 11 placeholder component files**, one per route. Each file MUST:
    - Export a named component (e.g., `export function BodyMap()`).
    - Render a `<GlassPanel data-testid="{route-id}-root">` (e.g., `data-testid="body-map-root"`, `data-testid="hive-mesh-root"`, etc).
    - Include `<h2>{route label}</h2>` + `<p>Ships in Plan 08-0X (REQUIREMENT-ID).</p>` text.
    - Import `./body.css` or `./hive.css` (ensure CSS files are created).

    Component file list (with data-testid values Plans 08-03/04 MUST preserve):
    - BodyMap.tsx → data-testid="body-map-root"
    - BodySystemDetail.tsx → data-testid="body-system-detail-root"
    - HormoneBus.tsx → data-testid="hormone-bus-root"
    - OrganRegistry.tsx → data-testid="organ-registry-root"
    - DNA.tsx → data-testid="dna-root"
    - WorldModel.tsx → data-testid="world-model-root"
    - HiveMesh.tsx → data-testid="hive-mesh-root"
    - TentacleDetail.tsx → data-testid="hive-tentacle-root"
    - AutonomyControls.tsx → data-testid="hive-autonomy-root"
    - ApprovalQueue.tsx → data-testid="approval-queue-root"
    - AiDelegate.tsx → data-testid="ai-delegate-root"

    **Step D — Create `src/features/body/types.ts`** re-exporting cluster-local types:
    ```ts
    export type {
      ModuleMapping, OrganStatus, OrganCapability,
      WorldState, GitRepoState, ProcessInfo, PortInfo, FileChange, SystemLoad, TodoItem,
      BloodPressure, EventInfo, VitalSigns, ImmuneStatus, InheritedDna,
    } from '@/lib/tauri/body';

    // Cluster-only UI types
    export type BodySystemName =
      | 'nervous' | 'vision' | 'audio' | 'muscular' | 'memory'
      | 'identity' | 'endocrine' | 'cardiovascular' | 'hive'
      | 'immune' | 'skeleton' | 'infrastructure';
    ```

    **Step E — Create `src/features/hive/types.ts`** re-exporting:
    ```ts
    export type {
      TentacleStatus, Priority, TentacleReport, TentacleSummary,
      Decision, HiveStatus, AiDelegateInfo,
    } from '@/lib/tauri/hive';

    // Cluster-only UI types
    export type TentaclePlatform =
      | 'github' | 'slack' | 'email' | 'calendar' | 'discord'
      | 'linear' | 'cloud' | 'log' | 'terminal' | 'filesystem';
    ```

    **Step F — Create `src/features/body/body.css`** + `src/features/hive/hive.css`** per 08-PATTERNS §9. Minimum classes needed for placeholders + Plans 08-03/04:
    - body.css: `.body-map`, `.body-system-detail`, `.hormone-bus`, `.organ-registry`, `.dna`, `.world-model`, `.hormone-row.accent-*`, `.hormone-meter`, `.circadian-grid`, `.circadian-bar`, `.dna-tabs`
    - hive.css: `.hive-mesh`, `.tentacle-card.status-*`, `.tentacle-grid`, `.approval-queue`, `.decision-details`, `.autonomy-matrix`

    All CSS wrapped in `@layer features-body {}` or `@layer features-hive {}`.

    Cross-check: `npm run dev` (brief) resolves all 11 lazy imports without 404.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f src/features/body/BodyMap.tsx && test -f src/features/body/HormoneBus.tsx && test -f src/features/hive/HiveMesh.tsx && test -f src/features/hive/ApprovalQueue.tsx && grep -q "BodyMap" src/features/body/index.tsx && grep -q "HiveMesh" src/features/hive/index.tsx && grep -q "@layer features-body" src/features/body/body.css && grep -q "@layer features-hive" src/features/hive/hive.css</automated>
  </verify>
  <done>6 body component files + 5 hive component files exist as minimal placeholders. Both index.tsx files rewritten with lazy imports. 2 types files + 2 CSS files created. tsc clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run verify:all` exits 0
- `grep -c "invokeTyped" src/lib/tauri/body.ts` ≥ 22
- `grep -c "invokeTyped" src/lib/tauri/hive.ts` == 10
- 11 placeholder component files exist with correct data-testid roots
- Both index.tsx rewrites preserve the route id order from Phase 1 substrate
- Zero `#[tauri::command]` additions in Rust (verified by Plan 08-05 verify:phase8-rust script later)
- `npm run lint` passes (no-raw-tauri enforced)
</verification>

<success_criteria>
- src/lib/tauri/body.ts exports ~22 camelCase wrappers with JSDoc Rust cites (D-206)
- src/lib/tauri/hive.ts exports 10 wrappers (8 hive + 2 ai_delegate) + enum types (D-206)
- src/lib/tauri/homeostasis.ts gains 4th wrapper homeostasisRelearnCircadian (D-194)
- src/features/body/index.tsx rewritten with 6 lazy imports; route order preserved (D-199)
- src/features/hive/index.tsx rewritten with 5 lazy imports; route order preserved (D-199)
- 11 placeholder component files exist with `data-testid="{route-id}-root"` that Playwright specs will assert (Plan 08-05)
- 2 types barrels + 2 CSS files created (D-208, D-210)
- src/lib/tauri/index.ts barrel re-exports both new modules
- npx tsc --noEmit + npm run verify:all + npm run lint all pass
- Zero Rust changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-body-hive/08-02-SUMMARY.md`.
</output>
