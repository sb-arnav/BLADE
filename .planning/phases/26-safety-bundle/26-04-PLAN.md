---
phase: 26-safety-bundle
plan: 04
type: execute
wave: 3
depends_on: [26-02, 26-03]
files_modified: []
autonomous: false
requirements: [SAFE-01, SAFE-07]

must_haves:
  truths:
    - "cargo check passes with all safety_bundle integrations"
    - "npx tsc --noEmit passes with frontend ConsentDialog changes"
    - "cargo test --lib evals::safety_eval passes all fixtures at 100%"
    - "npm run verify:safety exits 0"
    - "Dev server starts without runtime panics"
  artifacts: []
  key_links:
    - from: "scripts/verify-safety.sh"
      to: "src-tauri/src/evals/safety_eval.rs"
      via: "cargo test gate"
      pattern: "verify-safety.*OK"
---

<objective>
Run the full verification suite and confirm all safety mechanisms compile, pass eval, and the dev server starts cleanly.

Purpose: Phase 26 is the hard gate for Phases 27-29. This plan runs all verification to confirm the gate is green before the phase can close.
Output: All gates green, dev server verified, phase ready for /gsd-verify-work.
</objective>

<execution_context>
@/home/arnav/.claude/get-shit-done/workflows/execute-plan.md
@/home/arnav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-safety-bundle/26-CONTEXT.md
@.planning/phases/26-safety-bundle/26-01-SUMMARY.md
@.planning/phases/26-safety-bundle/26-02-SUMMARY.md
@.planning/phases/26-safety-bundle/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full static verification chain</name>
  <files></files>
  <read_first>
    - package.json (verify scripts section)
    - scripts/verify-safety.sh
  </read_first>
  <action>
Run the following verification commands in sequence:

1. `cd /home/arnav/blade/src-tauri && cargo check` — Must pass with no errors (confirms all Rust integrations compile)
2. `cd /home/arnav/blade && npx tsc --noEmit` — Must pass (confirms TypeScript changes are type-safe)
3. `cd /home/arnav/blade/src-tauri && cargo test --lib evals::safety_eval -- --nocapture --test-threads=1` — Must pass with 100% fixture pass rate
4. `cd /home/arnav/blade && bash scripts/verify-safety.sh` — Must exit 0 with "OK" message

If any step fails, diagnose and fix the issue. Common issues:
- Missing `use tauri::Manager;` or `use tauri::Emitter;` import in safety_bundle.rs
- Missing `false` argument on existing `request_consent` callers after adding `safety_override` param
- Fixture runner logic mismatch (expected vs actual outcome)
- serde Default issue on HormoneState (may need `#[serde(default)]` on struct)

After all pass, confirm the verify:all chain includes verify:safety by running:
`grep "verify:safety" /home/arnav/blade/package.json`
  </action>
  <verify>
    <automated>cd /home/arnav/blade && bash scripts/verify-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` exits 0
    - `npx tsc --noEmit` exits 0
    - `cargo test --lib evals::safety_eval` exits 0 with all fixtures passing
    - `bash scripts/verify-safety.sh` outputs "[verify-safety] OK" and exits 0
    - package.json verify:all chain includes `verify:safety`
  </acceptance_criteria>
  <done>All static verification gates pass including the new verify:safety gate 34</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 26 Safety Bundle — all safety enforcement mechanisms:
- safety_bundle.rs with danger-triple, mortality-salience cap, calm-vector, attachment, crisis detection
- decision_gate.rs integration (danger-triple pre-check before autonomous actions)
- brain.rs integration (safety prompt modulations injected)
- consent.rs safety_override (AllowAlways hidden for safety-gated consent)
- ConsentDialog frontend (AllowAlways button conditionally hidden, safety notice shown)
- Safety eval module with ~25 fixtures across 5 classes (100% pass rate)
- verify:safety as gate 34 in the verify chain
  </what-built>
  <how-to-verify>
1. Run `npm run tauri dev` — confirm the dev server starts without panics or compile errors
2. In the running app, verify the ConsentDialog still renders normally for regular consent requests (if you can trigger one via any autonomous action)
3. Confirm `npm run verify:safety` passes (should show scored table output)
4. Run `cd src-tauri && cargo test --lib evals::safety_eval -- --nocapture --test-threads=1` and confirm all fixtures are green

Expected: Dev server starts cleanly, no Rust panics in first 10s, safety eval shows all-green table.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only — no new trust boundaries) | This plan only runs checks on code from Plans 01-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-12 | Repudiation | Verification passes but runtime fails (v1.1 lesson) | mitigate | checkpoint:human-verify requires dev server startup + visual confirmation; static gates alone are insufficient per CLAUDE.md Verification Protocol |
</threat_model>

<verification>
- All static gates pass (cargo check, tsc, verify:safety)
- Dev server starts cleanly (human verified)
- Safety eval scored table shows 100% pass rate
</verification>

<success_criteria>
- All SAFE-01 through SAFE-07 are implemented and verified
- verify:safety gate is green (gate 34)
- Dev server confirmed functional
- Phase 26 is ready for /gsd-verify-work closure
</success_criteria>

<output>
After completion, create `.planning/phases/26-safety-bundle/26-04-SUMMARY.md`
</output>
