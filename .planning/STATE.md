---
gsd_state_version: 1.0
milestone: v1.2
milestone_name: Acting Layer with Brain Foundation
status: shipped
last_updated: "2026-04-30T21:10:00.000Z"
progress:
  total_phases: 5
  completed_phases: 5
  total_plans: 22
  completed_plans: 22
  percent: 100
---

# STATE — BLADE (v1.2 SHIPPED)

**Project:** BLADE — Desktop JARVIS
**Current milestone:** v1.2 ✅ shipped 2026-04-30 — `tech_debt` closure (matches v1.1 pattern); see `.planning/milestones/v1.2-MILESTONE-AUDIT.md`.
**Last shipped milestone:** v1.2 — Acting Layer with Brain Foundation (closed 2026-04-30 as `tech_debt`).
**Prior shipped:** v1.1 — Functionality, Wiring, Accessibility (closed 2026-04-27 as `tech_debt`); v1.0 — Skin Rebuild substrate (closed 2026-04-19).
**Current Focus:** v1.2 closed. Phases 16/17/18 shipped code-complete (with documented operator-owned UAT deferrals); Phase 19 deferred wholesale to v1.3 under chat-first pivot; Phase 20 (this audit + CHANGELOG + archive) shipped. Next milestone: v1.3 — chat-capability deepening + Phase 19 UAT close + voice resurrection (PTT + Whisper → JARVIS dispatcher) + browser-harness adoption decision.
**Status:** Phase 18 Plan 18-14 of 14 complete (Wave 4 ✅ closed). Plan 18-14 closed the 4 plan-checker BLOCKERS and W1 from iteration 1: 4 task commits (`495b8fd` Task 1 args extraction in intent_router — `(IntentClass, ArgsBag)` return type; 6 new heuristic args extraction tests for Linear/Slack/GitHub/Gmail/Calendar; `c37216f` Task 2 Linear+Calendar concrete wiring — `auto_create_ticket(description, source)` + `calendar_post_meeting_summary(app, transcript, meeting_title)` replace Plan 09's `None  // PLACEHOLDER`; `2230333` Task 3 tokio oneshot consent — `consent::request_consent` async fn owns oneshot channel, `consent_respond` Tauri command delivers user choice, dispatcher AWAITS in-place; ConsentChoice::AllowOnce passes through without SQLite write; ConsentChoice::AllowAlways persists then falls through; 60s timeout returns Deny; `52a276f` Task 4 commands.rs args wiring + frontend one-shot consent — commands.rs spawns background dispatch with intent_router → jarvis_dispatch_action; admin.ts consentRespond wrapper added with TS literal-union choice validation; payloads.ts ConsentRequestPayload.action_kind field; ChatPanel.handleDecide reduced from 25 lines to 5 lines (single consentRespond call; consentSetDecision + jarvisDispatchAction re-invoke GONE; greps return 0); 18-DEFERRAL.md records D-04 Step 2 LLM-fallback as v1.3 path B with rationale + v1.3 hand-off shape). Verification: cargo check exit 0 (only pre-existing consent_check_at testability-seam warning); cargo test --lib (consent + intent_router + jarvis_dispatch) 43/43 green (15 consent including 5 new oneshot tests + 16 intent_router including 6 new args tests + 12 jarvis_dispatch including 2 new routing/source tests); npx tsc --noEmit exit 0; npm run verify:emit-policy exit 0 (60 broadcast emits unchanged). 4 deviations: Rule 1 — slack test message lacked "slack" service token (heuristic requires verb × service token; fixed by adding "in slack" to test message); Rule 1 — `extract_after_marker_lc` truncated emails at first period (rewrote helper with two paths: "to " takes first whitespace-bounded token preserving emails, other markers take rest-of-line); Rule 1 — `plan_14_placeholders_removed_from_module_source` test self-matched its own assertion message via include_str! (renamed to `plan_14_live_tentacle_calls_present` with positive-only assertions); Rule 3 — Plan 09's `emit_consent_request` helper became dead-code after Task 3 refactor (deleted; request_consent owns the emit + await + timeout cleanup as single source of truth). All 4 BLOCKERS closed: B1 path B (Plan 11 re-invoke handler GONE), B2 (Allow once works without SQLite write via channel pass-through), B3 (Linear+Calendar concretely wired), B4 (real args from intent_router feed dispatcher). W1 closed (action_kind preserved in ConsentRequestPayload + dispatcher's local scope). SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-14-SUMMARY.md`. JARVIS-04 + JARVIS-05 requirements closed by this plan. Carry-forward: Plan 18-12 cold-install demo exercises the full pipeline end-to-end with Linear as the preferred target — operator types `"create a linear issue: test demo"` → ConsentDialog opens → Allow always → real Linear issue created + ActivityStrip shows `[JARVIS] action_required: linear → executed`.

**Prior status (Plan 18-11 — for context):** Plan 18-11 of 14 complete (Wave 4 progressing). Plan 18-11 closed the JARVIS chat-first FE loop: 3 task commits across 6 files. (1) `e962ed3` feat — `src/lib/tauri/admin.ts` +138 lines: 3 type aliases (EgoVerdict / IntentClass / DispatchResult — discriminated unions on `kind`) + 6 wrappers (egoIntercept / intentRouterClassify / jarvisDispatchAction / consentGetDecision / consentSetDecision / consentRevokeAll). consentSetDecision compile-time forbids `'allow_once'` via TS literal-union (T-18-CARRY-15 defense in depth on top of Rust validator). (2) `618cd4f` feat — `src/features/chat/JarvisPill.tsx` (78 lines, 4 D-18 states with tone-mapped Badge composition + dismiss button only on hard_refused + aria-live polite); `src/features/chat/ConsentDialog.tsx` (115 lines, Dialog primitive composition + 3 buttons with Allow once default-focus per D-09; T-18-03 RENDER-LAYER LOCK enforced — content_preview rendered via `<pre>{content_preview}</pre>` React text-node interpolation, the unsafe innerHTML attribute prop is BANNED in both files by acceptance grep returning 0); `src/features/chat/chat.css` +128 lines for jarvis-pill + consent-dialog selectors using ONLY canonical tokens (--s-N / --r-N / --t-N / --g-fill* / --g-edge-* / --font-* / --a-hot) — ghost-token grep `--jarvis-` / `--consent-` returns 0 across all 3 files (memory project_ghost_css_tokens.md lesson held; v1.1 retraction from 210 ghost-token refs not repeated). (3) `67a3077` feat — `src/features/chat/MessageList.tsx` +38 lines: subscribes to BLADE_EVENTS.JARVIS_INTERCEPT via useTauriEvent (D-13 lock honored — only permitted listen surface), renders <JarvisPill /> below the latest assistant bubble, auto-clear effect keyed on currentMessageId reads intercept via ref to avoid streaming-tick churn, hard_refused stays sticky until user dismisses; `src/features/chat/ChatPanel.tsx` +86 lines: subscribes to BLADE_EVENTS.CONSENT_REQUEST, mounts <ConsentDialog />, handleDecide ships Wave-4 SIMPLIFICATION (persist allow_always/denied via consentSetDecision then re-invoke jarvisDispatchAction with hardcoded action='post' as the original verb is lost between emit and re-invoke) — 20-line FORWARD-POINTER comment block at lines 27-46 + TODO(plan-14) marker on the hardcoded action flag the deletion site for Plan 14 Task 4 which REPLACES the entire handler with consentRespond(request_id, choice) over tokio::oneshot. Verification: `npx tsc --noEmit` exit 0; `npm run verify:all` OK across all 27+ gates; `npm run lint` 0 errors (16 pre-existing warnings in unrelated files); `grep -rE "from ['\"]@tauri-apps/api/event['\"]" src/features/chat/` returns 0 (D-13 lock); banned-innerHTML attr-prop grep returns 0 in both new files (T-18-03); ghost-token grep returns 0; useTauriEvent count = 6 across MessageList + ChatPanel (3 each — pre-existing chat events + new JARVIS subscriber for ML, pre-existing none + new CONSENT subscriber for ChatPanel). Zero deviations — plan executed exactly as written; the plan's "either pattern works" suggestion for clearIntercept landed on the local-state-in-MessageList path the plan itself recommended in Step 3 ("the simplest implementation: keep intercept state local"). SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-11-SUMMARY.md`. JARVIS-05 + JARVIS-11 requirements closed. Carry-forward: Plan 18-12 cold-install demo exercises the full chain (send "post to slack" → ego intercepts → JarvisPill flashes → consent_request opens dialog → Allow always → re-invoke dispatches → ActivityStrip shows `[JARVIS] action_required: slack → executed`); Plan 18-14 Task 4 supersedes ChatPanel handleDecide with consentRespond + tokio::oneshot (Plan 11 ships the working-but-degraded version, Plan 14 lands the production handler).

**Prior status (Plan 18-10 — for context):** Plan 18-10 of 14 complete (Wave 4 progressing). Plan 18-10 wired the ego pipeline into commands.rs — every assistant turn through the tool-loop branch is now refusal/capability-gap classified before extract_actions runs. 3 surgical insertions: (1) `crate::ego::reset_retry_for_turn()` at function entry l.718 (D-14 retry-cap reset per turn — without this RETRY_COUNT accumulates across turns and the cap=1 bypasses on second-and-later turn); (2) Pitfall 3 inline KNOWN-GAP doc at fast-streaming branch l.1166-1182 (fast path emits tokens directly via providers::stream_text without server-side accumulation; ego requires full transcript so cannot run here; refusals on this branch NOT caught — workaround = re-issue forcing only_native_tools=false; full coverage requires accumulator refactor deferred to v1.3 per RESEARCH § Anti-Patterns); (3) ego intercept wrap at l.1531-1555 in the tool-loop branch (was l.1517 region) using @-pattern match arm `verdict @ EgoVerdict::CapabilityGap{..} | verdict @ EgoVerdict::Refusal{..}` to bind verdict for handle_refusal without destructuring; final_content unifies all 3 EgoOutcome variants (Retried.new_response | AutoInstalled.then_retried | HardRefused.final_response); extract_actions(&final_content) replaces extract_actions(&turn.content). last_user_text confirmed in scope at l.1543 (bound at l.866 sanitize_input, used 30+ times in function). Pre-existing dead-code warnings on EgoOutcome / RETRY_COUNT / reset_retry_for_turn / emit_jarvis_intercept / handle_refusal (carried since Plan 05/06 landings) all SILENCED — those symbols now have a live consumer. Q1 closed in `.planning/research/questions.md` with D-20 verdict verbatim: "Browser-harness installs ALWAYS require explicit consent. They are large, slow, and user-perceptible (downloads a Chromium binary, starts a long-lived process). Routine creds-based capability gaps (Slack OAuth, GitHub PAT, etc.) auto-prompt via the standard consent dialog. Browser/runtime installs go through a separate explicit-consent surface that surfaces install size, time-to-first-use, and disk footprint before downloading. Browser-harness adoption decision (whether to integrate it at all vs. keeping browser_native.rs + browser_agent.rs) is deferred to v1.3 when Phase 18's chat-action spine is operational and we can measure where browser fallback is actually needed." — Status flipped open → closed; existing fields (Source/Link/Why it matters/What to evaluate/Decision deadline) UNCHANGED; Closer cites Plan 18-10 + 18-CONTEXT.md D-20. cargo check clean (only pre-existing consent_check_at Plan 14 testability-seam warning, unchanged). cargo test --lib ego:: → 18/18 green (capability_gap_extracts_capability_noun + capability_gap_precedes_refusal + hard_refuse_format_locked + 2 disjunction post-check negatives + pass_on_helpful_response + 9 pattern matches + reset_retry_works + retry_cap_holds + safe_slice_used_on_long_content + skeleton_compiles); cargo test --lib commands → 0/0 (no commands tests defined; no regressions across 265 filtered out tests). 2 task commits (`4d29b5a` feat ego wiring + `a1ff743` docs Q1 closure); zero deviations — plan executed exactly as written, 3 insertion points landed at lines RESEARCH and PATTERNS predicted, last_user_text was in scope, match-on-verdict pattern compiled cleanly without borrow-checker fights. KNOWN GAPS carried forward: Plan 18-11 frontend renders JarvisPill on jarvis_intercept events fired by handle_refusal (events now real, listener pending); Plan 18-12 cold-install demo exercises end-to-end; Plan 18-14 Task 4 replaces Plan 05 placeholder `<retry-pending: ...>` text in EgoOutcome::AutoInstalled.then_retried with actual LLM retry call inside this same wrapper region.

---

## Project Reference

See: `.planning/PROJECT.md` (updated 2026-04-27 at v1.1 close)

**Core value:** BLADE works out of the box, and you can always see what it's doing.

**v1.2 locked scope:** Eval foundation + Doctor module + JARVIS (with ego folded in) + Operator UAT close + Polish. ACT (full per-tentacle outbound surface), Skills MVP, tool-replacer, WIRE3 backend burn → v1.3+. Locked input at `notes/v1-2-milestone-shape.md`.

---

## Recent Context

### Shipped milestones

- **v1.0** (2026-04-19) — Skin Rebuild substrate (10 phases, ~165 commits, 18 verify gates green); phase dirs at `.planning/phases/0[0-9]-*` (never formally archived)
- **v1.1** (2026-04-24, closed 2026-04-27) — Functionality, Wiring, Accessibility (6 phases, 29 plans, 27 verify gates green); archived to `milestones/v1.1-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md` + `milestones/v1.1-phases/`

### v1.1 Locked Decisions (still in force for v1.2 planning)

- **M-01** Wiring + smart defaults + a11y, NOT new features — held; v1.2 acting work obeys the same anchor
- **M-03** Observe-only guardrail (`OBSERVE_ONLY: AtomicBool`) — v1.2 will flip per-tentacle behind explicit user consent + trust escalation, never silently
- **M-05** Phase numbering continues globally — v1.2 starts at Phase 16
- **M-07** Activity log is load-bearing — every cross-module action in v1.2 must continue to emit

### v1.0 Decisions Inherited

D-01..D-45 + D-56/D-57 remain locked. See `PROJECT.md` Key Decisions table.

### Open research questions for v1.2

- **Q1**: `browser-use/browser-harness` vs current `browser_native.rs` + `browser_agent.rs` — decision deadline before v1.2 JARVIS phase plan (`research/questions.md`)

---

## Deferred Items

Items acknowledged and deferred at v1.1 milestone close on 2026-04-27 (per `milestones/v1.1-MILESTONE-AUDIT.md` status=tech_debt). All follow the v1.0 Mac-smoke convention (operator-owned, tracked separately):

| Category | Phase | Item | Status | Notes |
|----------|-------|------|--------|-------|
| uat_gaps | 14 | 14-HUMAN-UAT.md | partial | 6 pending — activity-strip cross-route persistence, drawer focus-restore, localStorage rehydrate-on-restart, cold-install Dashboard screenshot, keyboard tab-traversal, 5-wallpaper contrast |
| uat_gaps | 15 | 15-05-UAT.md | unknown | 5 visual-UAT items — 5-wallpaper background-dominance, cold-install RightNowHero screenshot, top-bar hierarchy 1280×720, 50-route empty-state ⌘K sweep, spacing-ladder spot-check |
| verification_gaps | 14 | 14-VERIFICATION.md | human_needed | 17/17 must-haves auto-verified; 6 UAT items pending |
| verification_gaps | 15 | 15-VERIFICATION.md | human_needed | 5/5 SC auto-verified; 5 UAT items pending |
| advisory | 14 | LOG-04 time-range filter | not implemented | Only module filter shipped; 500-entry ring buffer naturally caps window |
| advisory | 11 | ROUTING_CAPABILITY_MISSING UI consumer | deferred | Toast/banner subscriber 0 src/; advisory WARN gate surfaces it |
| backlog | 10 | 97 DEFERRED_V1_2 backend modules | catalogued | All carry `deferral_rationale` strings in 10-WIRING-AUDIT.json; v1.2 burn-down candidate |

### v1.0 Open Checkpoints (still operator-owned)

- Mac smoke M-01..M-46 — `HANDOFF-TO-MAC.md`
- Plan 01-09 WCAG checkpoint — Mac desktop environment
- WIRE-08 full `cargo check` — WSL libspa-sys/libclang env limit; CI green

---

## Blockers

None. v1.1 closed cleanly with documented tech debt.

---

## Session Continuity

**Last session:** 2026-04-30T20:30Z (Plan 18-14 ✅ shipped — Wave 4 end-to-end pipeline wiring. 4 task commits across 9 files closing 4 plan-checker BLOCKERS: `495b8fd` Task 1 args extraction in intent_router (`(IntentClass, ArgsBag)` return type; 6 new heuristic args extraction tests for Linear/Slack/GitHub/Gmail/Calendar — 16/16 green); `c37216f` Task 2 Linear+Calendar concrete wiring (replaces Plan 09's `None  // PLACEHOLDER` with `auto_create_ticket(description, source)` + `calendar_post_meeting_summary(app, transcript, meeting_title)`; 12/12 jarvis_dispatch tests green); `2230333` Task 3 tokio oneshot consent (`consent::request_consent` async fn owns oneshot channel + `consent_respond` Tauri command + ConsentChoice enum; dispatcher AWAITS user choice in-place — Allow once passes through without SQLite write, Allow always persists then falls through, Deny short-circuits; 60s timeout returns Deny with PENDING cleanup; 5 new oneshot tests; `emit_consent_request` Plan-09 helper deleted as superseded by request_consent); `52a276f` Task 4 commands.rs args wiring + frontend one-shot consent (commands.rs spawns background dispatch with classify_intent → jarvis_dispatch_action; admin.ts consentRespond wrapper with TS literal-union choice validation; payloads.ts ConsentRequestPayload.action_kind field; ChatPanel.handleDecide reduced from 25 to 5 lines with single consentRespond call; consentSetDecision+jarvisDispatchAction re-invoke GONE — `grep "consentSetDecision\|jarvisDispatchAction("` returns 0 lines in ChatPanel.tsx; 18-DEFERRAL.md records D-04 Step 2 LLM-fallback as v1.3 path B with rationale + v1.3 hand-off shape). Verification: cargo check exit 0 (only pre-existing consent_check_at testability-seam warning); cargo test --lib (consent + intent_router + jarvis_dispatch) 43/43 green; npx tsc --noEmit exit 0; npm run verify:emit-policy exit 0 (60 broadcast emits unchanged). 4 deviations: Rule 1 — slack test message lacked "slack" service token (heuristic requires verb × service token; fixed by adding "in slack" to test message); Rule 1 — `extract_after_marker_lc` truncated emails at first period (rewrote helper with two paths); Rule 1 — placeholder-removal test self-matched its assertion message via include_str! (renamed to `plan_14_live_tentacle_calls_present` with positive-only assertions); Rule 3 — Plan 09 `emit_consent_request` helper became dead-code (deleted; request_consent owns the emit + await + timeout cleanup as single source of truth). All 4 BLOCKERS closed: B1 path B (re-invoke handler GONE), B2 (Allow once works), B3 (Linear+Calendar wired), B4 (real args extracted). W1 closed (action_kind preserved). SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-14-SUMMARY.md`. JARVIS-04 + JARVIS-05 requirements closed by this plan. Carry-forward: Plan 18-12 cold-install demo can now exercise the full pipeline end-to-end with Linear as the preferred target — operator types `"create a linear issue: test demo"` → ConsentDialog opens → Allow always → real Linear issue created + ActivityStrip shows `[JARVIS] action_required: linear → executed`.)

**Prior session:** 2026-04-30T18:44Z (Plan 18-11 ✅ shipped — Wave 4 frontend integration: 6 typed Tauri wrappers + JarvisPill + ConsentDialog + MessageList/ChatPanel wiring. 3 task commits: `e962ed3` feat — admin.ts +138 lines (3 type aliases + 6 wrappers; consentSetDecision compile-time forbids 'allow_once' via TS literal-union); `618cd4f` feat — JarvisPill.tsx (78 lines, 4-state inline pill, Badge composition, hard_refused-only dismiss, aria-live=polite) + ConsentDialog.tsx (115 lines, Dialog primitive composition, 3 buttons with Allow once default-focus, T-18-03 RENDER-LAYER LOCK — content_preview via React text-node `<pre>{...}</pre>`, banned-innerHTML attr-prop grep returns 0) + chat.css +128 lines (jarvis-pill + consent-dialog selectors, zero ghost tokens — `--jarvis-` / `--consent-` greps return 0 across all 3 files); `67a3077` feat — MessageList.tsx +38 lines (BLADE_EVENTS.JARVIS_INTERCEPT subscriber via useTauriEvent per D-13 lock, JarvisPill render below latest bubble, auto-clear effect keyed on currentMessageId with intercept ref to avoid streaming churn) + ChatPanel.tsx +86 lines (BLADE_EVENTS.CONSENT_REQUEST subscriber, ConsentDialog mount, handleDecide ships Wave-4 SIMPLIFICATION re-invoking jarvisDispatchAction with hardcoded action='post'; 20-line FORWARD-POINTER comment + TODO(plan-14) marker flag deletion site for Plan 14 Task 4 which REPLACES this handler with consentRespond(request_id, choice) over tokio::oneshot). Verification: tsc clean; verify:all OK across 27+ gates; lint 0 errors; raw `listen()` import grep in chat surfaces returns 0 (D-13 lock honored); banned-innerHTML grep returns 0 in both new files (T-18-03); ghost-token grep returns 0; `<JarvisPill>` × 1 in MessageList, `<ConsentDialog>` × 1 in ChatPanel, `consentSetDecision` + `jarvisDispatchAction` × 4 in ChatPanel. Zero deviations — plan executed exactly as written. SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-11-SUMMARY.md`. JARVIS-05 + JARVIS-11 requirements closed. Carry-forward: Plan 18-12 cold-install demo exercises full chain end-to-end; Plan 18-14 Task 4 supersedes ChatPanel handleDecide with the oneshot-aware production handler.)

**Prior session:** 2026-04-30T18:45Z (Plan 18-10 ✅ shipped — Wave 4 begun: commands.rs ego integration + research/questions.md Q1 closure. 2 task commits across 2 files: (1) `4d29b5a` feat — `src-tauri/src/commands.rs` 3 surgical insertions: function entry l.718 `crate::ego::reset_retry_for_turn()` (D-14 retry-cap reset per turn — without this RETRY_COUNT accumulates and cap=1 bypasses on turn 2+); fast-streaming branch entry l.1172-1182 Pitfall 3 KNOWN-GAP doc (ego cannot run on the fast path because providers::stream_text emits tokens directly with no server-side accumulator; refusal-eliciting prompts rarely land here due to is_conversational+is_short_conversation guards; workaround = re-issue forcing only_native_tools=false; full fix = accumulator refactor deferred to v1.3 per RESEARCH § Anti-Patterns); tool-loop branch wrap at l.1531-1555 using `@-pattern` match arm `verdict @ EgoVerdict::CapabilityGap{..} | verdict @ EgoVerdict::Refusal{..}` to bind verdict for handle_refusal without destructuring (cleaner than two separate arms calling the same helper); `final_content` unifies all 3 EgoOutcome variants (Retried.new_response | AutoInstalled.then_retried | HardRefused.final_response) into a single string; `extract_actions(&final_content)` replaces `extract_actions(&turn.content)` so action-tag stripping operates on the (possibly rewritten) ego output. `last_user_text` confirmed in scope at l.1543 (bound at l.866 via sanitize_input; referenced 30+ times in send_message_stream including l.1561 brain_planner::confirm_plan and l.1567 user_text clone). Pre-existing dead-code warnings on `EgoOutcome` / `RETRY_COUNT` / `reset_retry_for_turn` / `emit_jarvis_intercept` / `handle_refusal` (carried since Plan 05/06 landings) all silenced — those symbols now have a live consumer at the chat-pipeline edge. (2) `a1ff743` docs — `.planning/research/questions.md` Q1 status flipped `**Status:** open` → `**Status:** closed`; D-20 verdict block appended verbatim ("Browser-harness installs ALWAYS require explicit consent. They are large, slow, and user-perceptible (downloads a Chromium binary, starts a long-lived process). Routine creds-based capability gaps (Slack OAuth, GitHub PAT, etc.) auto-prompt via the standard consent dialog. Browser/runtime installs go through a separate explicit-consent surface that surfaces install size, time-to-first-use, and disk footprint before downloading. Browser-harness adoption decision (whether to integrate it at all vs. keeping browser_native.rs + browser_agent.rs) is deferred to v1.3 when Phase 18's chat-action spine is operational and we can measure where browser fallback is actually needed."); Closer cites Plan 18-10 + 18-CONTEXT.md D-20; existing Q1 fields (Source/Link/Why it matters/What to evaluate/Decision deadline) UNCHANGED. Verification: `cargo check` exits 0 with only pre-existing `consent_check_at` Plan 14 testability-seam warning (unchanged from prior plan); `cargo test --lib ego:: -- --test-threads=1` → 18/18 green (capability_gap_extracts_capability_noun + capability_gap_precedes_refusal + hard_refuse_format_locked + 2 disjunction post-check negatives + pass_on_helpful_response + 9 pattern matches + reset_retry_works + retry_cap_holds + safe_slice_used_on_long_content + skeleton_compiles); `cargo test --lib commands` → 0/0 (no commands tests defined; 265 filtered out — no regressions). All grep gates green: `ego::reset_retry_for_turn` × 1, `ego::intercept_assistant_output(&turn.content)` × 1, `ego::handle_refusal` × 1, `EgoVerdict::Pass` × 1, `EgoOutcome::HardRefused` × 1, `Pitfall 3` × 3, `extract_actions(&final_content)` × 1; `Status: closed` × 1 (markdown-bolded form `**Status:** closed`), `Status: open` × 0, `Verdict — closed 2026-04-30` × 1, `deferred to v1.3` × 1, `always require explicit consent` (case-insensitive) × 1. Zero deviations — plan executed exactly as written; 3 insertion points landed at lines RESEARCH and PATTERNS predicted (function entry, l.~1166, l.~1517); last_user_text was in scope at the wrap location; match-on-verdict pattern compiled cleanly without borrow-checker fights (the @-pattern keeps `verdict` movable into handle_refusal while leaving turn.content intact for the Pass arm via .clone()); the plan note about "Edit-via-Bash approach being needed for 1500+ line file" was unnecessary — the Edit tool worked fine for all 3 surgical insertions. SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-10-SUMMARY.md`. JARVIS-03/06/07/08/09/10 requirements closed. Carry-forward: Plan 18-11 frontend will listen to `jarvis_intercept` events that handle_refusal now FIRES for real (events live, listener pending); Plan 18-12 cold-install demo exercises end-to-end; Plan 18-14 Task 4 replaces the Plan 05 placeholder `<retry-pending: {capability} installed via {tool}>` text in `EgoOutcome::AutoInstalled.then_retried` with an actual LLM retry call inside this same wrapper.

**Prior session:** 2026-04-30T17:36Z (Plan 18-07 ✅ shipped — Wave 2 begun: slack_outbound MCP-first body + github_outbound gh_post bodies. 2 task commits `3e71f7f` (slack) + `7825e70` (github) across `src-tauri/src/tentacles/{slack,github}_outbound.rs`. slack_outbound::slack_outbound_post_message: Tier-1 MCP via `crate::config::load_config().mcp_servers` probe + `SharedMcpManager` lock through `integration_bridge::get_app_handle()` (slack_deep.rs:34 idiom verbatim) — tries `mcp__slack_chat.postMessage` (dot-form, JS convention) then `mcp__slack_chat_post_message` (underscore, official spec); discriminator: `Err("Unknown tool: ...")` falls through, any other Err propagates (preserves operator visibility into MCP failures, no silent HTTP fallback). Tier-2 HTTP POST `https://slack.com/api/chat.postMessage` with `Authorization: Bearer {SLACK_BOT_TOKEN}` from `crate::config::get_provider_key("slack")` keyring. Shared `parse_slack_response` between MCP+HTTP branches surfaces `ok:false` Slack errors as Err with the API error code, uses `safe_slice(200)` on every error string. github_outbound: `github_outbound_create_pr_comment` POST `/repos/{owner}/{repo}/issues/{pr_number}/comments` with `{"body": ...}`; `github_outbound_create_issue` POST `/repos/{owner}/{repo}/issues` with `{"title", "body"}`. gh_post helper replicated locally (verbatim github_deep.rs:185-200 header set: Bearer + `Accept: application/vnd.github+json` + `X-GitHub-Api-Version: 2022-11-28` + `User-Agent: BLADE-Hive/1.0`) to break module coupling — github_deep refactor cannot regress this path. gh_client uses 30s timeout; PR-comment uses `/issues/{n}/comments` not `/pulls/{n}/comments` (review-thread is different feature). D-10 hard-fail format LOCKED across 3 paths verbatim: `[slack_outbound] Connect via Integrations tab → Slack (no creds found in keyring or MCP server registered).` + `[github_outbound] Connect via Integrations tab → GitHub (no PAT in keyring).` (×2 per github command). 14 unit tests green (7 slack + 7 github): hard_fail_message_format_is_d10_compliant + slack_token_helper_does_not_panic + slack_mcp_registered_smoke + parse_slack_response_extracts_ts_and_channel + parse_slack_response_uses_fallback_channel_when_missing + parse_slack_response_surfaces_api_error + parse_slack_response_handles_garbage_json | hard_fail_message_format_d10_compliant + pr_comment_url_format + issue_url_format + github_token_helper_does_not_panic + gh_client_builds_with_timeout + pr_comment_payload_shape (no `title` field) + issue_payload_shape. cargo check exits 0 (warnings only on pre-existing dead-code symbols `EgoOutcome`/`RETRY_COUNT`/`reset_retry_for_turn`/`emit_jarvis_intercept`/`handle_refusal`/`consent_check` consumed by Plan 18-10; zero new warnings on slack/github_outbound). assert_observe_only_allowed gates preserved at top of all 3 Tauri commands (defense-in-depth; Plan 14 dispatcher holds WriteScope upstream). Module-prefixed error strings (`[slack_outbound]`/`[github_outbound]`) so Plan 18-09 dispatcher can attribute failures by module without free-text parsing. Real HTTP/MCP integration tests deferred to Plan 18-12 cold-install operator UAT (no wiremock in tree). Zero deviations: plan executed exactly as written; pseudo-code's `crate::mcp::manager()` mapped to live `SharedMcpManager`/`get_app_handle()` API per slack_deep.rs canonical idiom — not a deviation, just live API surface. SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-07-SUMMARY.md`. JARVIS-04 requirement closed.

**Prior session:** 2026-04-30T16:48Z (Plan 18-06 ✅ shipped — Wave 1 intent_router heuristic body + consent SQLite CRUD; 2 task commits `d5c68ae` + `5721427` across `src-tauri/src/intent_router.rs`, `src-tauri/src/consent.rs`, `src-tauri/src/lib.rs`. intent_router::classify_intent: tier-1 heuristic (verb × service token, ChatOnly safe-default) — 7 ACTION_VERBS × 6 service tokens (slack/github/gmail+email-alias/calendar/linear); classify_intent_llm hook stubbed (returns None unconditionally) — D-04 Step 2 DEFERRED to v1.3 per Plan 14 path B + 18-DEFERRAL.md. consent.rs: open_db_at(path) testability seam + consent_check_at(db_path, ...) parallel seam pre-pinned for Plan 14 — full CRUD (consent_get_decision/consent_set_decision/consent_revoke_all/consent_list_decisions/consent_check) using composite-PK SQLite consent_decisions table; INSERT OR REPLACE for upsert; DELETE FROM for revoke_all; SELECT * ORDER BY decided_at DESC for list (Settings UI). Decision-value validation gate: only `allow_always` | `denied` are persistable; `allow_once` and arbitrary strings rejected with explicit error per T-18-CARRY-15 (Open Q1 in-memory only). 9 rusqlite::params! sites — SQL-injection-safe per T-18-CARRY-16. consent_list_decisions registered in lib.rs generate_handler! per CLAUDE.md 6-place rule. 20 unit tests green (10 intent_router + 10 consent): chat_only_for_greeting + slack/linear/gmail/github/calendar action_required + capitalization_invariant + heuristic_short_circuits_fast (<50ms) + 2 negatives (no-verb, no-service); schema_string_present + open_db_at_creates_table + set_persists_and_get_retrieves + get_returns_none_for_unknown via consent_check_at + revoke_all_clears + invalid_decision_rejected_at_set_decision + invalid_decision_arbitrary_string_rejected + consent_check_at_reads_allow_always + consent_check_at_reads_denied + consent_check_at_returns_needs_prompt_for_missing_db. cargo check clean (8 warnings, all dead-code on Plan 18-10 consumers). 2 deviations: Rule 1 test reword for github_comment ambiguity (had two verbs `update`+`comment` triggering first-match-wins on iteration order); Rule 2 added consent_list_decisions for D-10 Settings UI revoke flow. SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-06-SUMMARY.md`. REFUSAL_PATTERNS slot populated with 9 (Regex, label) tuples; Pattern 9 (`need_integration`) listed FIRST per D-13 CapabilityGap precedence; 5 mandatory + 3 stretch refusal patterns; disjunction-aware post-check via `static DISJUNCTION_POSTCHECK: OnceLock<Regex>` initialized to `\bbut\b.+\bcan\b` scanning 80-char lookahead from match.end(); safe_slice fallback for non-ASCII boundary cross. handle_refusal enforces D-14 retry cap = 1 per turn (RETRY_COUNT.fetch_add SeqCst returning previous value; prev >= 1 → HardRefused with retry_cap_exceeded reason); CapabilityGap branch emits intercepting → calls evolution_log_capability_gap (verbatim reuse of evolution.rs:1115) → catalog lookup with 3 key fallbacks (bare/_outbound/_write); Runtime kind routes through live `self_upgrade::auto_install(&CapabilityGap) -> InstallResult` (W2 pre-pin verified at self_upgrade.rs:387) with `.success` boolean check (NOT Result Ok/Err); Integration kind hard-refuses with D-15 locked format including `gap.integration_path`. emit_jarvis_intercept fires `app.emit_to("main", "jarvis_intercept", payload)` at every state transition (intercepting/installing/retrying/hard_refused) — single-window pattern matches blade_activity_log precedent, no allowlist entry needed; reason bounded via safe_slice(200) per T-18-CARRY-14. 7 occurrences of D-15 phrase "I tried, but ..." across all hard-refuse branches. 18 unit tests green (filtered 198): pattern_i_cant + pattern_no_access + pattern_not_able + pattern_cannot_directly + pattern_lack_the + pattern_as_an_ai + pattern_unable_to + pattern_no_capability + no_false_positive_on_but_can + no_false_positive_on_however_can + pass_on_helpful_response + capability_gap_precedes_refusal + capability_gap_extracts_capability_noun + retry_cap_holds + hard_refuse_format_locked + safe_slice_used_on_long_content + skeleton_compiles + reset_retry_works. cargo check clean (warnings on dead-code symbols `EgoOutcome` / `emit_jarvis_intercept` / `handle_refusal` consumed by Plan 18-10 commands.rs integration); npm run verify:emit-policy green (60 broadcast emits unchanged — single-window emit_to is exempt). Zero deviations beyond plan-allowed adoption of live auto_install signature. SUMMARY at `.planning/phases/18-jarvis-ptt-cross-app/18-05-SUMMARY.md`.
**Next action:** `/gsd-execute-plan 18-12` — Wave 5 cold-install operator UAT exercises the full FE↔BE chain end-to-end now that Plan 18-14 closed all 4 plan-checker BLOCKERS. Operator types `"create a linear issue: test demo"` (Linear is the preferred target — guaranteed-creds path) → intent_router extracts `(ActionRequired{linear, create}, {title, description, source})` → commands.rs background dispatch → consent_request fires ConsentDialog → Allow always → dispatcher's tokio::oneshot resumes → `auto_create_ticket(description, source)` creates the real Linear issue → ActivityStrip shows `[JARVIS] action_required: linear → executed`. Plan 18-12 captures the screenshot for JARVIS-12 UAT and closes Phase 18.

**Context cliff notes:**

- v1.0 + v1.1 both shipped; substrate is reachable + observable + capability-aware
- 31 verify gates green (was 30; Phase 16 added `verify:eval`); tsc clean
- v1.2 = 5 phases (16=Eval ✅, 17=Doctor, 18=JARVIS+Ego, 19=Operator-UAT, 20=Polish)
- v1.2 acting work flips the per-tentacle observe-only guardrail with explicit consent + trust-tier escalation, never silently
- Activity log strip is the v1.1 contract every v1.2 cross-module action must honor
- Phase 16 eval harness lives at `src-tauri/src/evals/{harness, hybrid_search_eval, real_embedding_eval, kg_integrity_eval, typed_memory_eval, capability_gap_eval}.rs` — Phase 17 Doctor consumes these signals (DOCTOR-02)

---

*State updated: 2026-04-29 — **Phase 16 (Eval Scaffolding Expansion) shipped + verified.** 7 plans across 3 waves: Wave 1 = harness scaffold (16-01); Wave 2 = 5 eval modules (16-02 hybrid_search, 16-03 real_embedding, 16-04 kg_integrity, 16-05 typed_memory, 16-06 capability_gap); Wave 3 = gate-closer + cleanup (16-07: scripts/verify-eval.sh, tests/evals/DEFERRED.md, package.json verify:eval chain entry, embeddings.rs:496-946 deletion). Final state: 5 eval modules @ MRR 1.000, asserted floors held (top-3 ≥ 80%, MRR ≥ 0.6), `verify:all` 30→31 green, embeddings.rs 946→495 lines (production code byte-identical), 19 commits with no Co-Authored-By. Two REQ-vs-real path resolutions documented in file headers: EVAL-02 `consolidate_kg` does not exist (`add_node` idempotent-merge path satisfies); EVAL-05 `detect_missing_tool` lives at `self_upgrade::` not `evolution::` (no re-export added). One Rule-3 deviation: `scripts/verify-wiring-audit-shape.mjs` updated to exclude `src-tauri/src/evals/` from production wiring audit (test-only `#[cfg(test)]` modules). VERIFICATION.md PASS 25/25 must-haves, 4/4 ROADMAP SCs, 8/8 EVAL REQs. Phase 17 (Doctor Module) consumes these eval signals (DOCTOR-02).*

**Planned Phase:** 18 (jarvis-ptt-cross-app) — 14 plans — 2026-04-30T13:50:27.514Z
