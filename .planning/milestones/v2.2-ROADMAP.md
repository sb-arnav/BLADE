# Roadmap: BLADE v2.2 — VISION-Close + Goose-Integrate + Launch-Ready

**Scoped:** 2026-05-14
**Authority:** VISION.md (locked 2026-05-10) > V2-AUTONOMOUS-HANDOFF.md > v2.1-MILESTONE-AUDIT.md carry-forwards > v2.2-REQUIREMENTS.md

## Goal

Bring BLADE from substrate-complete (v2.1 close) to VISION-reached at the wedge. Three workstreams: close load-bearing VISION gaps, integrate Goose Apache-2 foundations (provider trait + session schema) instead of bespoke maintenance, prep the forge launch demo + Show HN + Miessler DM.

## Phase plan (9 phases, dependency-ordered)

```
53 PRESENCE-NARRATE      → sharpest single VISION-distance move (fifth primitive observable in chat)
54 GOOSE-PROVIDER        → adopt Goose Provider trait + canonical_models.json (1,700 models)
55 GOOSE-SESSION         → adopt Goose SQLite session schema (foundation for continuity)
56 HUNT-TELOS            → setup-as-conversation produces optimization target, not just inventory
57 SKILLS-MD             → ~/.blade/skills/ markdown directory (OpenClaw pattern)
58 MEMORY-SIMPLIFY       → kill embeddings.rs vector layer, keep BM25 + KG
59 TRIO-REORG            → held-trio moves to /dev-tools pane (reorganize, don't remove)
60 LAUNCH-PREP           → forge demo script + README rewrite + HN/Miessler/Twitter copy
61 CLOSE                 → CHANGELOG + audit + archive + tag
```

## Dependency analysis

- Phase 53 (PRESENCE-NARRATE) — independent. Touches `evolution.rs`, `homeostasis.rs`, `learning_engine.rs`, `brain.rs`, `useChat.tsx`.
- Phase 54 (GOOSE-PROVIDER) — independent. Touches `providers/`, `router.rs`. Migration safe per-provider.
- Phase 55 (GOOSE-SESSION) — depends on 54 only for context-window lookup from canonical_models.json; can run in parallel if needed. Touches `migrations/`, new `sessions.rs`, `commands.rs`.
- Phase 56 (HUNT-TELOS) — depends on 55 (TELOS frontmatter consumed by brain.rs which reads sessions). Touches `onboarding/hunt.rs`, `onboarding/synthesis.rs`, `brain.rs`.
- Phase 57 (SKILLS-MD) — independent. Touches new `skills/` module, `commands.rs` dispatch.
- Phase 58 (MEMORY-SIMPLIFY) — independent. Touches `embeddings.rs` only (kill path), retrieval callers unchanged in interface.
- Phase 59 (TRIO-REORG) — depends on 53 (vitality badge in chat header uses presence emit substrate). Touches `App.tsx`, `features/body/`, `ghost_mode.rs` routing.
- Phase 60 (LAUNCH-PREP) — depends on 53 + 56 + 57 landing (the demo recording assumes those surfaces). Touches docs only.
- Phase 61 (CLOSE) — depends on all preceding.

## Wave execution shape

**Wave A** (parallel start): 53, 54, 57, 58 — fully independent
**Wave B** (after Wave A foundations): 55 (uses 54's canonical_models), 59 (uses 53's vitality emit substrate)
**Wave C** (after Wave B): 56 (uses 55's session schema for brain.rs context)
**Wave D** (after Wave C): 60 (assembles artifacts from all preceding)
**Wave E** (close): 61

## Verify gates (each phase + close)

- `cargo check` clean
- `tsc --noEmit` clean
- Phase-specific test suite green
- `npm run verify:all` ≥ 37/38 (OEVAL-01c v1.4 carry-forward documented since v2.1)
- New tests added per phase REQ-IDs

## Wake conditions

Per V2-AUTONOMOUS-HANDOFF.md §7:
1. GSD verifier BLOCKED twice on same phase after one self-fix
2. Verify gates regress below 36/38 and code-fixer fails to recover
3. Authority gap that defaulting either way would commit material work hard-to-reverse

None expected under standard execution; the milestone has clear scope-locks and the phases match the v1.6/v2.0/v2.1 dispatch pattern that worked.

## Success criteria

- Presence is observable in chat (Phase 53 ships; user sees vitality/evolution lines)
- Provider routing is canonical-models-aware (Phase 54 ships)
- Session schema matches Goose's (Phase 55 ships)
- Hunt produces TELOS-shaped output (Phase 56 ships)
- `~/.blade/skills/` directory works with 5 seeded skills (Phase 57 ships)
- `embeddings.rs` vector layer dead, BM25 + KG carrying retrieval (Phase 58 ships)
- Held-trio reachable at `/dev-tools`, demoted from main nav (Phase 59 ships)
- Launch artifacts (demo script + README + HN post + Miessler DM + Twitter thread) ready (Phase 60 ships)
- All static gates green (Phase 61 ships)

## What this milestone is NOT trying to do

- Not building the skills marketplace itself — just the directory + loader + 5 seeds
- Not the "second time destroys it" memory continuity rewrite — that needs operator dogfood signal
- Not the Recipes YAML engine — secondary, defer to v2.3
- Not adding the Cline-style approval gate — wait for non-developer feedback
- Not recording/posting the launch demo — operator pulls trigger, this milestone prepares the artifacts
