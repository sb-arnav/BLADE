---
phase: 38-close
plan: 2
type: execute
wave: 1
depends_on: [38-01]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [CLOSE-CHANGELOG]
requirements_addressed: [Phase 38 Close success criterion 2 (ROADMAP line 230) — "CHANGELOG v1.5 entry lists all delivered features and the verify gate count change (37 → 38)". Mirrors v1.4 Phase 25 entry shape verbatim. Adds new row to Verify-Gate Evolution table at file bottom.]
must_haves:
  truths:
    - "CHANGELOG.md has a new `## [1.5.0] -- 2026-05-08` entry inserted between the existing `## [Unreleased]` block (line 10-14) and `## [1.4.0]` (line 16)"
    - "The entry contains a static-gates summary line: 36/38 sub-gates green with OEVAL-01c v1.4 carry-forward note"
    - "The entry contains a dual-mode eval explanation (deterministic CI lane + opt-in operator-runnable lane)"
    - "Per-phase blocks for Phases 32, 33, 34, 35, 36, 37, 38 — each with bold title + shipped/code-complete date + UAT-pending status (where applicable) + 4-7 bullets covering key requirements + their satisfaction count"
    - "Verify-Gate Evolution table at the file bottom (currently around line 308-310) gains a new row: `| Phases 32-37 (v1.5) | 1 (intelligence) | **38** |`"
    - "NO `### Removed` / `### Deprecated` / `### Fixed` sections in the v1.5 entry — purely additive"
    - "NO version bump in package.json / Cargo.toml / tauri.conf.json — operator-controlled per CHANGELOG D-227"
    - "Markdown renders cleanly: ## [1.5.0] sits between [Unreleased] and [1.4.0]; per-phase blocks use existing v1.4 shape"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.5 milestone entry + verify-gate evolution row"
      contains: "## [1.5.0] -- 2026-05-08"
  key_links:
    - from: "CHANGELOG.md ## [1.5.0] entry"
      to: "README.md §Intelligence Layer (v1.5) (Plan 38-01)"
      via: "shared per-phase narrative — CHANGELOG is dated record, README is evergreen"
      pattern: "1\\.5\\.0|Intelligence Layer"
    - from: "CHANGELOG.md Verify-Gate Evolution table"
      to: "scripts/verify-intelligence.sh (Phase 37-07)"
      via: "row count: Phases 32-37 add 1 gate (intelligence) — total 38"
      pattern: "verify:intelligence|\\| 38 \\|"
---

<objective>
Insert a new `## [1.5.0] -- 2026-05-08` CHANGELOG entry between the existing `## [Unreleased]` block and `## [1.4.0]`. Mirror the v1.4 Phase 25 entry shape verbatim — header line, static-gates summary line, dual-mode eval explanation, per-phase bullet groups for Phases 32–38. Add one row to the Verify-Gate Evolution table at file bottom: `Phases 32-37 (v1.5) | 1 (intelligence) | 38`. Per CONTEXT §"CHANGELOG Entry Scope (Plan 38-02)" every shape decision is locked.

Purpose: The CHANGELOG is the dated technical record of v1.5. Where the README's §Intelligence Layer is evergreen narrative, this entry pins the milestone to a date, enumerates per-phase requirements satisfaction counts, surfaces the static-gates posture honestly (36/38 with carry-forward exception), and documents the dual-mode eval design choice (deterministic CI + opt-in operator real-LLM lane). The Verify-Gate Evolution table row makes the 37→38 gate count change auditable.

Output: ~80 LOC of CHANGELOG diff. Single commit `feat(38-02): CHANGELOG v1.5 entry`. Zero source code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/38-close/38-CONTEXT.md
@/home/arnav/blade/CHANGELOG.md

<interfaces>
<!-- Verified anchors at authoring time:

     - CHANGELOG.md line 10: `## [Unreleased]` heading
     - Line 11: blank, line 12: "Nothing yet.", line 13: blank, line 14: "---"
     - Line 15: blank, line 16: `## [1.4.0] -- 2026-05-03`
     - Insert v1.5 entry between line 14's "---" and line 16's `## [1.4.0]`.
       The natural insertion point: after the existing "---" separator at
       line 14, insert a blank line + new `## [1.5.0]` heading + body + a
       trailing "---" separator + blank line. This keeps the existing
       [Unreleased] -> [1.4.0] visual rhythm intact.
     - Verify-Gate Evolution table:
       - Line 298: "## Verify-Gate Evolution" heading
       - Line 300-310: table rows
       - Line 310: "| Phases 25-30 (v1.4) | 4 (safety, hormone, inference, organism) | **37** |"
       - INSERT new row IMMEDIATELY AFTER line 310 (the v1.4 row) and BEFORE
         line 311 (blank line + "Three additional scripts are NOT in `verify:all`").
-->
</interfaces>

</context>

## Tasks

### Task 1 — Confirm exact insertion anchors

Read `CHANGELOG.md` lines 1–80 and lines 295–320. Confirm:
- Line 14 is the `---` separator under `## [Unreleased]`
- Line 16 is `## [1.4.0] -- 2026-05-03`
- The Verify-Gate Evolution table's last row is the v1.4 row showing total 37

If line numbers have drifted (the file is small; unlikely), adjust the Edit anchors below to match the actual file content. The structural truth is "after [Unreleased], before [1.4.0]" and "after the v1.4 row in the table" — line numbers are approximate.

### Task 2 — Insert `## [1.5.0]` entry between [Unreleased] and [1.4.0]

Use the Edit tool. `old_string` matches the exact lines around the [1.4.0] heading; `new_string` inserts the v1.5 entry plus a trailing `---` separator before the [1.4.0] heading.

`old_string`:
```
---

## [1.4.0] -- 2026-05-03
```

`new_string` (verbatim per CONTEXT §"Specifics" → "Concrete Plan 38-02 CHANGELOG entry shape", expanded):
```
---

## [1.5.0] -- 2026-05-08

### Added (v1.5 -- Intelligence Layer)

> Code-complete 2026-05-08 across phases 32-38 (6 feature phases + 1 close phase). Static gates: `cargo check` clean . `npx tsc --noEmit` clean . `npm run verify:all` 36/38 sub-gates green (verify:intelligence + 35 prior; OEVAL-01c v1.4 carry-forward in verify:eval + verify:hybrid_search documented out-of-scope per Phase 32-37 SCOPE BOUNDARY). Phases 32-37 ship at the `checkpoint:human-verify` boundary with operator-deferred runtime UAT.
>
> v1.5's eval is dual-mode: a deterministic CI lane runs 26 fixtures with no LLM calls (`verify:intelligence` is the 38th gate); an opt-in operator-runnable lane (`BLADE_RUN_BENCHMARK=true bash scripts/run-intel-benchmark.sh`) runs 10 multi-step fixtures against real LLMs to populate `eval-runs/v1.5-baseline.json` for regression-only checks.

**Phase 32 -- Context Management** *(code-complete 2026-05-05; UAT pending)*
- `brain.rs` selective injection gating sections 0-8 by query relevance; LAST_BREAKDOWN per-section accumulator surfaces in DoctorPane.
- Proactive compaction at ~80% capacity using OpenHands v7610 structured summary prompt; token-aware keep_recent.
- `cap_tool_output` helper caps individual tool results at ~4k tokens with summary suffix.
- `ContextBreakdown` wire type + `get_context_breakdown` Tauri command + DoctorPane breakdown panel.
- CTX-07 fallback guarantee: any selective-injection / compaction failure degrades to the naive path.
- 7/7 CTX-XX requirements satisfied. checkpoint:human-verify open.

**Phase 33 -- Agentic Loop** *(code-complete 2026-05-06; UAT pending)*
- Mid-loop verifier runs every 3 tool calls; replans when the goal isn't being served.
- Structured `ToolError` feedback: what was tried, why it failed, suggested alternative.
- Plan adaptation on tool failure; iteration cap configurable (default 25).
- Truncation auto-retry with escalated `max_tokens` value.
- Ego intercept ported to the fast-streaming path (closes the Phase 18 known gap).
- 6/6 LOOP-XX requirements satisfied. checkpoint:human-verify open.

**Phase 34 -- Resilience + Session Persistence** *(code-complete 2026-05-06; UAT pending)*
- 5-pattern stuck detection: RepeatedActionObservation, ContextWindowThrashing, NoProgress, MonologueSpiral, CostRunaway.
- Circuit breaker after N consecutive same-type failures; per-conversation cost guard with 80%/100% tiers.
- Provider fallback chain with exponential backoff.
- Append-only JSONL session log; reopen-and-resume from last compaction boundary; branch-from-any-point; one-line preview list.
- 5/5 RES-XX + 4/4 SESS-XX requirements satisfied. checkpoint:human-verify open.

**Phase 35 -- Auto-Decomposition** *(code-complete 2026-05-07; UAT pending)*
- Brain planner detects 5+ independent steps and auto-fans into parallel sub-agents.
- Isolated sub-agent contexts (each sub-agent's 50k bash output doesn't bloat parent).
- Summary-only return to parent; conversation forking into sub-agent branches.
- Sub-agent progress streams into chat with explicit checkpoints.
- 5/5 DECOMP-XX requirements satisfied. checkpoint:human-verify open.

**Phase 36 -- Context Intelligence** *(code-complete 2026-05-07; UAT pending)*
- `tree-sitter` parses TypeScript/JavaScript/Rust/Python source into a symbol-level dependency graph.
- Personalized PageRank scores symbols by current chat mentions.
- Budget-bounded repo map (~1k tokens default) injects at the code-section gate.
- `canonical_models.json` capability registry replaces per-call probes; `router.rs` reads from it.
- `@screen` / `@file:src/main.rs` / `@memory:topic` anchor syntax with chat chip rendering.
- 6/6 INTEL-XX requirements satisfied. REVIEW + REVIEW-FIX commits address 5 critical findings. checkpoint:human-verify open.

**Phase 37 -- Intelligence Eval** *(code-complete 2026-05-08; UAT pending)*
- 26 deterministic fixtures across 4 surfaces: 10 multi-step task completion + 3 context efficiency + 5 stuck + 5 healthy controls + 3 compaction fidelity.
- ScriptedProvider stub + EVAL_FORCE_PROVIDER thread-local seam in `loop_engine.rs` enables zero-LLM CI.
- `verify:intelligence` gate joins `verify:all` as the 38th composed gate.
- Operator-runnable benchmark via `BLADE_RUN_BENCHMARK=true bash scripts/run-intel-benchmark.sh` populates `eval-runs/v1.5-baseline.json`.
- 13 intelligence_eval tests green; driver emits 26 rows, top-1=26/26, top-3=26/26, MRR=1.000.
- 5/5 EVAL-XX requirements satisfied. UAT (b) baseline.json operator-deferred per memory `feedback_deferred_uat_pattern.md`.

**Phase 38 -- Close** *(shipped 2026-05-08; tech_debt)*
- README extends Cognitive Architecture section with Intelligence Layer narrative; Research Foundations cites Claude Code (arxiv 2604.14228), Aider, OpenHands, Goose, mini-SWE-agent.
- This CHANGELOG entry.
- `.planning/milestones/v1.5-MILESTONE-AUDIT.md` written; v1.5 status: `tech_debt`; 42/42 requirements routed; 36/38 verify gates green.
- Phase 32-38 directories archived to `.planning/milestones/v1.5-phases/`.
- 4/4 close success criteria satisfied.

---

## [1.4.0] -- 2026-05-03
```

This insertion adds ~75 lines of content above the existing `## [1.4.0]` heading. The trailing `---` separator before `## [1.4.0]` is the v1.5-to-v1.4 visual divider that mirrors the existing Unreleased-to-1.4 divider.

### Task 3 — Update Verify-Gate Evolution table at file bottom

Locate the Verify-Gate Evolution table (around line 298–310). Find the v1.4 row:
```
| Phases 25-30 (v1.4) | 4 (safety, hormone, inference, organism) | **37** |
```

Use the Edit tool to insert a new row IMMEDIATELY AFTER the v1.4 row.

`old_string`:
```
| Phases 25-30 (v1.4) | 4 (safety, hormone, inference, organism) | **37** |
```

`new_string`:
```
| Phases 25-30 (v1.4) | 4 (safety, hormone, inference, organism) | **37** |
| Phases 32-37 (v1.5) | 1 (intelligence) | **38** |
```

Keep all prior rows (Phase 1, 4, 5, 6, 7, 8, 9, Phases 21-24) unchanged. The new row reflects the single new gate added in Phase 37 (`verify:intelligence`) bringing the total from 37 to 38.

### Task 4 — Verify the rendered markdown via `Read`

Read `CHANGELOG.md`:
- Lines 1–25 — confirm the new ordering: `## [Unreleased]` (line 10) → `---` → `## [1.5.0]` (new heading) → static gates summary line → dual-mode note → first per-phase block (Phase 32)
- Around line 90–100 (where the new entry ends) — confirm `---` separator + `## [1.4.0]` heading immediately follow
- Around line 308–315 — confirm the new v1.5 row sits below the v1.4 row in the Verify-Gate Evolution table

Cite a one-line observation per check in your SUMMARY (e.g. "CHANGELOG ## [1.5.0] entry inserted between Unreleased and 1.4.0; 7 per-phase blocks render; trailing --- separator clean").

### Task 5 — Stage SPECIFIC paths and commit

Run from repo root:
```bash
git add CHANGELOG.md
git status --short | head
```

Confirm `git status --short | head` shows ONLY `M CHANGELOG.md` in the staged set. The 188 pre-existing `.planning/phases/00-31-*/` deletions stay unstaged. The `M README.md` from Plan 38-01 is already committed and won't appear in `git status` (its commit was made by 38-01).

Commit:
```bash
git commit -m "feat(38-02): CHANGELOG v1.5 entry

- Insert ## [1.5.0] -- 2026-05-08 between [Unreleased] and [1.4.0]
- Per-phase blocks for Phases 32-38 with requirements satisfaction counts
- Static gates summary: 36/38 with OEVAL-01c v1.4 carry-forward note
- Dual-mode eval explanation (deterministic CI + opt-in operator real-LLM)
- Verify-Gate Evolution table: add Phases 32-37 (v1.5) row, total 38

Per Phase 38-CONTEXT lock §CHANGELOG Entry Scope (Plan 38-02)."
```

NO Co-Authored-By line. NO `cargo check`.

## Verification

- Markdown renders cleanly per Task 4 reads
- `git status --short` shows ONLY `M CHANGELOG.md` in the staged set
- No source code touched
- Commit message ends without Co-Authored-By line
- `## [1.5.0]` precedes `## [1.4.0]` in version-descending order (the convention this changelog uses; verify by reading lines 16-30 after commit)

## Tests Added

None.

## Anti-patterns (from CONTEXT)

- DO NOT bump `package.json` / `Cargo.toml` / `tauri.conf.json` to 1.5.0 — operator-controlled per CHANGELOG D-227. The `## [1.5.0]` heading is the milestone marker; binary version is unrelated and stays at current 0.7.x
- DO NOT add `### Removed` / `### Deprecated` / `### Fixed` sections — v1.5 is purely additive per CONTEXT §"CHANGELOG Entry Scope (Plan 38-02)"
- DO NOT include an ASCII diagram of the dual-mode eval — text bullets per CONTEXT §"Claude's Discretion (catch-all)"
- DO NOT `git add -A` or `git add .` — 188 pre-existing staged deletions
- DO NOT add Co-Authored-By line in commit message body
- DO NOT touch v1.0..v1.4 prior CHANGELOG entries

## CONTEXT Decisions Cited

- §"CHANGELOG Entry Scope (Plan 38-02)" — every shape decision locked
- §"Backward Compatibility (None — Docs Only)" — no kill switches, docs-only phase
- §"Specifics" → "Concrete Plan 38-02 CHANGELOG entry shape" — header + dual-mode note + per-phase shape locked
- §"Plan Shape (4 Plans, No Waves)" — Phase 38-02 is plan 2 of 4, sequential
- §"Testing & Verification" — Read-tool-based smoke check; no automated tests
- §"Claude's Discretion (catch-all)" — recommendation NO ASCII diagram
