---
phase: 35-auto-decomposition
plan: 11
type: execute
wave: 5
depends_on: [35-01, 35-02, 35-03, 35-04, 35-05, 35-06, 35-07, 35-08, 35-09, 35-10]
files_modified:
  - src-tauri/src/decomposition/executor.rs
autonomous: false
requirements: [DECOMP-01, DECOMP-02, DECOMP-03, DECOMP-04, DECOMP-05]
requirements_addressed: [Phase-wide closure — wires ALL five DECOMP requirements through to runtime UAT per CLAUDE.md Verification Protocol. (1) Adds the panic-injection regression test for execute_decomposed_task — force a panic inside one sub-agent's spawn boundary, assert sibling continues + parent receives a SubagentSummary{success=false} + parent's chat does not crash. (2) Runs the 15-step checkpoint:human-verify UAT — 6-step query triggers fan-out, sub-agent JSONLs created with parent attribution, Merge back UI flow, cost-cap interlock, kill-switch round-trip, screenshots at 1280×800 + 1100×700. (3) Closes Phase 35.]
must_haves:
  truths:
    - "phase35_decomp_panic_in_subagent_spawn_caught_by_outer_wrapper test exists in decomposition::executor::tests — forces panic via DECOMP_FORCE_SUBAGENT_RESULT-adjacent panic seam OR via DECOMP_FORCE_DISTILL_PANIC; asserts execute_decomposed_task returns Ok with at least one SubagentSummary{success=false}"
    - "checkpoint:human-verify task with 15-step UAT script per 35-CONTEXT.md §Testing & Verification — operator-checked"
    - "tsc --noEmit clean; cargo check clean; the existing 37 verify gates remain green"
    - "Screenshots saved at docs/testing ss/ (literal space) for SessionsView Merge back UI + ActivityStrip subagent chips + chat surface with synthetic sub-agent summaries at 1280×800 + 1100×700"
    - "Operator sign-off recorded in 35-11-SUMMARY.md as the phase-closure gate"
    - "All 5 DECOMP requirements verifiable via UAT steps mapped 1:1: DECOMP-01 (steps 2 + 7), DECOMP-02 (steps 2 + 3 + 4 + 6 + 11), DECOMP-03 (step 2 — synthetic AssistantTurns + token-count cap), DECOMP-04 (steps 8 + 9 + 10), DECOMP-05 (steps 2 + 13 + 14 + 15)"
  artifacts:
    - path: "src-tauri/src/decomposition/executor.rs"
      provides: "phase35_decomp_panic_in_subagent_spawn_caught_by_outer_wrapper test"
      contains: "phase35_decomp_panic_in_subagent_spawn_caught_by_outer_wrapper"
    - path: "docs/testing ss/phase-35-uat-*.png"
      provides: "UAT screenshots at 1280×800 + 1100×700 per CLAUDE.md Verification Protocol"
      contains: "phase-35-uat-"
  key_links:
    - from: "Operator (Arnav)"
      to: "Phase 35 closure"
      via: "checkpoint:human-verify with 15-step UAT script + screenshot evidence"
      pattern: "checkpoint:human-verify"
---

<objective>
Close Phase 35 by wiring ALL five DECOMP requirements through to runtime UAT per CLAUDE.md Verification Protocol. The static-gates-only plans (35-01..35-10) prove syntax/types/structure; this plan proves the running binary surfaces auto-decomposition correctly: a 6-step query triggers fan-out, sub-agent JSONLs are created with parent attribution, sub-agent summaries inject as synthetic AssistantTurns into parent chat, ActivityStrip shows subagent_* chips with the throttle rules, SessionsView Merge back UI flows correctly with the confirm modal + auto-route, and the kill-switch + cost-cap interlocks work end-to-end.

The v1.1 lesson is load-bearing: 27 verify gates green + chat broken = retracted milestone. Phase 35 ships 5 requirements that, by design, can mask regressions on the runtime path (e.g. a sub-agent panic that gets swallowed by catch_unwind without a user-visible signal, or chip flooding when throttle breaks). The runtime UAT proves the user sees the right behavior on real flows.

Output: 1 panic-injection regression test added (Task 1); 15-step UAT executed by the operator (Task 2); screenshots saved + read back (Task 3); checkpoint:human-verify gates phase closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/35-auto-decomposition/35-CONTEXT.md
@/home/arnav/blade/.planning/phases/35-auto-decomposition/35-RESEARCH.md
@/home/arnav/blade/.planning/phases/34-resilience-session/34-11-PLAN.md (Phase 34's runtime UAT pattern — Plan 35-11 mirrors)

<interfaces>
<!-- Panic-injection regression test seam:

  Plan 35-06 declared DECOMP_FORCE_DISTILL_PANIC which makes
  distill_subagent_summary panic. Plan 35-11's regression test:
    1. Set DECOMP_FORCE_DISTILL_PANIC = true
    2. Force a 3-group decomposition via DECOMP_FORCE_STEP_COUNT
    3. Inject synthetic SubagentSummary via DECOMP_FORCE_SUBAGENT_RESULT (so
       spawn_isolated_subagent skips the real fork+spawn and returns the
       forced summary BEFORE distillation runs)
    4. Assert execute_decomposed_task returns Ok with summaries that have
       success=true (FORCE_SUBAGENT_RESULT bypasses the panic path)
    5. Then test the OPPOSITE: clear FORCE_SUBAGENT_RESULT, keep
       FORCE_DISTILL_PANIC=true → distill panics → catch_unwind in Plan 35-06
       returns heuristic fallback → SubagentSummary{success=false}

  The Plan 35-06 catch_unwind already converts panic → heuristic fallback.
  Plan 35-11's test asserts the fallback path produces a non-empty summary
  with success=false.

  Alternative: introduce a NEW DECOMP_FORCE_SPAWN_PANIC seam that panics
  inside spawn_isolated_subagent (after fork, before distill). The
  catch_unwind on execute_decomposed_task (Plan 35-05's outer wrapper)
  catches it → returns Internal error. The test asserts Err(Internal)
  AND no zombie tokio tasks.
-->

<!-- 15-step UAT script (35-CONTEXT.md §Testing & Verification verbatim):

  1. Open dev binary (`npm run tauri dev`)
  2. Send a 6-step query: "Find all Rust files modified in the last 7 days,
     summarize each one's purpose, identify the top 3 by complexity, write a
     report to /tmp/blade-rust-modules.md, run cargo check, and post the
     output to a Slack channel" — assert ActivityStrip shows
       subagent_started: 1: researcher,
       2: researcher, 3: analyst, 4: writer, 5: coder, 6: writer
     chips; assert each subagent_complete chip fires; assert parent chat shows
     6 synthetic [Sub-agent summary — step N…] messages
  3. Verify each sub-agent's JSONL exists in `~/.config/blade/sessions/` with
     a `parent` attribute matching the parent's session_id
  4. Open SessionsView; confirm 6 new rows appear with `parent` populated;
     confirm the parent's row's `message_count` reflects only the synthetic
     AssistantTurns added (not the sub-agent inflation)
  5. Send a query that triggers DECOMP, then mid-fan-out cancel — assert
     in-flight sub-agents finish their iteration and their summaries inject;
     assert no zombie tokio tasks
  6. Set `decomposition.max_parallel_subagents = 1` — send a 5-step query —
     assert sub-agents run *serially* (one at a time), all complete, all
     summaries inject in order
  7. Set `decomposition.auto_decompose_enabled = false` — send the same
     6-step query — assert the loop runs sequentially with NO subagent_*
     chips and NO new sub-agent JSONLs created
  8. From SessionsView, click "Branch" on a regular session → pick message
     index 3 → confirm new fork session appears
  9. Click "Merge back" on the fork session → confirm modal appears with
     parent's first_message_excerpt → confirm — assert success toast, fork
     remains in list (not deleted), parent's chat shows a new synthetic
     UserMessage `[Branch merged from fork {fork_id[..8]}…] {summary_text}`
  10. Send a follow-up message in the parent — assert the model's reply
      references the merged summary content (proves the synthetic
      UserMessage entered the conversation correctly)
  11. Set `decomposition.auto_decompose_enabled = true`,
      `cost_guard_per_conversation_dollars = 0.10`, send a 6-step query —
      assert RES-04 80% chip fires somewhere mid-fan-out, assert
      decomposition halts gracefully with
      LoopHaltReason::CostExceeded { scope: PerConversation }, assert
      in-flight sub-agents return their partial summaries
  12. Screenshot SessionsView with merge-back UI at 1280×800 + 1100×700,
      save under `docs/testing ss/`
  13. Screenshot ActivityStrip with subagent_* chips at 1280×800 + 1100×700
  14. Screenshot chat surface with 6 synthetic sub-agent summaries inline
      at 1280×800 + 1100×700
  15. Read back all screenshots via the Read tool and cite a one-line
      observation per breakpoint
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add panic-injection regression test for execute_decomposed_task</name>
  <files>src-tauri/src/decomposition/executor.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/decomposition/executor.rs (DECOMP_FORCE_SUBAGENT_RESULT seam from Plan 35-05; outer catch_unwind from Plan 35-05)
    /home/arnav/blade/src-tauri/src/decomposition/summary.rs (DECOMP_FORCE_DISTILL_PANIC seam from Plan 35-06; heuristic fallback path)
    /home/arnav/blade/.planning/phases/34-resilience-session/34-04-PLAN.md (panic-injection regression test pattern)
  </read_first>
  <behavior>
    - Test verifies the catch_unwind boundary on execute_decomposed_task converts a downstream panic (from distill_subagent_summary) into a heuristic-fallback SubagentSummary.
    - Approach 1: leverage existing seams — DECOMP_FORCE_DISTILL_PANIC=true + DECOMP_FORCE_SUBAGENT_RESULT=None (so the real path runs and panics during distill).
    - Approach 2: add a new DECOMP_FORCE_SPAWN_PANIC seam to executor.rs.
    - Recommend Approach 1: minimal new code; tests the realistic path.
    - Test asserts: execute_decomposed_task returns Ok (heuristic fallback recovers), summaries.len() == groups.len(), at least one summary has success=false (the panicking distillation).
  </behavior>
  <action>
**Step A — Append to the existing `#[cfg(test)] mod tests` block in `src-tauri/src/decomposition/executor.rs`:**

```rust
    #[tokio::test]
    async fn phase35_decomp_panic_in_distill_caught_by_summary_layer() {
        // Plan 35-11 regression — verifies the catch_unwind boundary in
        // distill_subagent_summary (Plan 35-06) converts a forced panic into
        // a heuristic SubagentSummary{success=false} fallback. The parent
        // execute_decomposed_task receives the fallback summary; siblings
        // continue normally; the parent's chat does not crash.
        //
        // This test does NOT exercise the AppHandle path (which lives in
        // the runtime UAT). It directly drives distill_subagent_summary
        // and asserts the catch_unwind contract holds.
        crate::decomposition::summary::DECOMP_FORCE_DISTILL_PANIC.with(|c| c.set(true));
        let mut cfg = crate::config::BladeConfig::default();
        cfg.session.jsonl_log_dir = std::path::PathBuf::from("/tmp/blade-test-decomp-11-panic");

        let result = crate::decomposition::summary::distill_subagent_summary(
            "01ARZ3NDEKTSV4RRFFQ69G5FAV",  // valid Crockford base32 (won't have a real JSONL)
            crate::agents::AgentRole::Researcher,
            &cfg,
        ).await;
        crate::decomposition::summary::DECOMP_FORCE_DISTILL_PANIC.with(|c| c.set(false));

        // catch_unwind in Plan 35-06 converts panic → Ok(heuristic_fallback)
        let s = result.expect("catch_unwind must convert panic to Ok with heuristic fallback");
        assert!(!s.success,
            "panic path produces success=false summary; got success=true");
        assert!(!s.summary_text.is_empty(),
            "heuristic fallback must produce non-empty summary text");
    }

    #[tokio::test]
    async fn phase35_decomp_force_subagent_result_seam_provides_synthetic_summary() {
        // Plan 35-11 regression — verifies the DECOMP_FORCE_SUBAGENT_RESULT
        // seam round-trips a synthetic SubagentSummary correctly. Used by
        // integration tests + Plan 35-04's run_loop trigger verification.
        let synthetic = SubagentSummary {
            step_index: 5,
            subagent_session_id: "01TESTSEAMABCDEFGHJKLMNPQR".to_string(),
            role: "coder".to_string(),
            success: true,
            summary_text: "test seam works".to_string(),
            tokens_used: 42,
            cost_usd: 0.001,
        };
        DECOMP_FORCE_SUBAGENT_RESULT.with(|c| c.set(Some(synthetic.clone())));
        let got = DECOMP_FORCE_SUBAGENT_RESULT.with(|c| c.take());
        assert!(got.is_some(), "seam must round-trip a SubagentSummary");
        let s = got.unwrap();
        assert_eq!(s.step_index, 5);
        assert_eq!(s.role, "coder");
        assert_eq!(s.tokens_used, 42);
    }
```

**Step B — Run cargo check + cargo test:**

```
cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
cd /home/arnav/blade/src-tauri && cargo test --lib decomposition::executor::tests::phase35_decomp_panic decomposition::executor::tests::phase35_decomp_force 2>&1 | tail -15
```
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib decomposition::executor::tests::phase35 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - phase35_decomp_panic_in_distill_caught_by_summary_layer test green
    - phase35_decomp_force_subagent_result_seam_provides_synthetic_summary test green
    - All previous Plan 35-05/06/07 tests still green (regression)
    - cargo check exits 0
  </acceptance_criteria>
  <done>Panic-injection regression test green; catch_unwind discipline confirmed at the summary layer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the static-gate verification rollup (cargo check + tsc + cargo test)</name>
  <files></files>
  <read_first>
    /home/arnav/blade/CLAUDE.md (verification protocol — required gates before runtime UAT)
  </read_first>
  <behavior>
    - cargo check exits 0
    - tsc --noEmit exits 0
    - cargo test --lib decomposition + loop_engine::tests::phase35 + session::list::tests::phase35 + config::tests::phase35 ALL green
    - Existing 37 verify gates remain green (no regressions)
  </behavior>
  <action>
**Step A — Run static gates in sequence:**

```
cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -10
cd /home/arnav/blade/src-tauri && cargo test --lib config::tests::phase35 decomposition loop_engine::tests::phase35 session::list::tests::phase35 2>&1 | tail -30
```

All three commands must exit 0.

**Step B — If the project has a verify:all script, run it:**

```
cd /home/arnav/blade && npm run verify:all 2>&1 | tail -20 || true
```

If the script doesn't exist OR a non-Phase-35 gate fails (e.g. pre-existing OEVAL-01c v1.4 drift): document the result in 35-11-SUMMARY.md but do NOT block UAT. Only Phase-35-introduced regressions are gating.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib config::tests::phase35 decomposition loop_engine::tests::phase35 session::list::tests::phase35 2>&1 | grep -c "FAILED\|test result: FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - cargo check exits 0
    - tsc --noEmit exits 0
    - cargo test passes ALL Phase-35-introduced tests
    - No NEW regressions in the existing 37 verify gates
  </acceptance_criteria>
  <done>Static gates green; runtime UAT clear to begin.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 15-step runtime UAT (operator-checked) + screenshots + read-back</name>
  <files>docs/testing ss/phase-35-uat-*.png</files>
  <read_first>
    /home/arnav/blade/CLAUDE.md (Verification Protocol — required UAT format)
    /home/arnav/blade/.planning/phases/35-auto-decomposition/35-CONTEXT.md §Testing & Verification (15-step script verbatim)
  </read_first>
  <behavior>
    - Operator (Arnav) opens the dev binary and runs the 15-step UAT script.
    - Each UAT step's outcome recorded in 35-11-SUMMARY.md as PASS / FAIL / DEFER.
    - Screenshots at 1280×800 + 1100×700 saved under `docs/testing ss/` (literal space).
    - Read-back: each screenshot's filename + a one-line observation per breakpoint cited in 35-11-SUMMARY.md.
    - Phase closes on operator sign-off when ALL 15 steps PASS or DEFERS are explicitly accepted.
  </behavior>
  <action>
**Step A — Open dev binary:**

```
cd /home/arnav/blade && npm run tauri dev
```

Wait for the app to come up cleanly. No Rust compile errors. No runtime panic in the first 10 seconds.

**Step B — Execute the 15-step UAT script per CONTEXT lock §Testing & Verification:**

1. **Open dev binary** — confirm clean boot, no panic in first 10s.

2. **6-step query** — type and send: `"Find all Rust files modified in the last 7 days, summarize each one's purpose, identify the top 3 by complexity, write a report to /tmp/blade-rust-modules.md, run cargo check, and post the output to a Slack channel"`.
   Assert:
   - ActivityStrip shows subagent_started chips for steps 1-6 with role labels (researcher / researcher / analyst / writer / coder / writer or similar role mix).
   - Each subagent_complete chip fires.
   - Parent chat surface shows 6 synthetic `[Sub-agent summary — step N, ROLE, session ULID[..8]…]` messages.

3. **Sub-agent JSONL inspection** — `ls ~/.config/blade/sessions/` (or wherever jsonl_log_dir resolves). Confirm 6 new ULID JSONL files. For each, run `head -1 ~/.config/blade/sessions/<ULID>.jsonl` and confirm the SessionMeta event has `"parent": "<parent_ULID>"`.

4. **SessionsView inspection** — open SessionsView. Confirm 6 new rows appear with `parent` populated. The parent's row's `message_count` should reflect only the synthetic AssistantTurns added (not the inflation from sub-agent activity).

5. **Cancel mid-fan-out** — send another 6-step query. Mid-fan-out (after 2-3 chips fire), click cancel. Assert:
   - In-flight sub-agents finish their CURRENT iteration and their summaries inject into chat.
   - No zombie tokio tasks (verify via `ps aux | grep blade | wc -l` stable).
   - No spinner stuck.

6. **Serial dispatch** — set `decomposition.max_parallel_subagents = 1` in settings/config. Send a 5-step query. Assert sub-agents run *serially* (one chip fires, completes, then next), all 5 complete, all summaries inject in order.

7. **Kill switch** — set `decomposition.auto_decompose_enabled = false`. Send the same 6-step query. Assert:
   - NO subagent_* chips appear.
   - NO new sub-agent JSONLs created (compare directory listing before/after).
   - Loop runs sequentially as Phase 33 + 34 ship.

8. **Branch flow** — re-enable auto_decompose. From SessionsView, click "Branch" on a regular non-fork session. Pick message index 3. Confirm new fork session appears in the list with `parent` populated.

9. **Merge back flow** — click "Merge back" on the fork session row. Confirm modal appears showing parent's first_message_excerpt. Click "Confirm merge". Assert:
   - Success toast appears.
   - Fork remains in list (NOT deleted).
   - Parent chat opens automatically with a new synthetic UserMessage at the bottom: `[Branch merged from fork {ULID[..8]}…] {summary_text}`.

10. **Merge content propagation** — send a follow-up message in the parent: `"What was discussed in the merged branch?"`. Assert the model's reply references the merged summary content (proves the synthetic UserMessage entered the conversation correctly).

11. **Cost-cap interlock** — set `decomposition.auto_decompose_enabled = true` AND `cost_guard_per_conversation_dollars = 0.10`. Send a 6-step query. Assert:
    - RES-04 80% chip fires somewhere mid-fan-out.
    - Decomposition halts gracefully with `LoopHaltReason::CostExceeded { scope: PerConversation }`.
    - In-flight sub-agents return partial summaries.
    - Future siblings are skipped.

12. **Screenshot SessionsView** — at 1280×800 and 1100×700 viewport sizes. Save as `docs/testing ss/phase-35-uat-sessions-1280x800.png` and `docs/testing ss/phase-35-uat-sessions-1100x700.png`.

13. **Screenshot ActivityStrip** — with subagent_* chips visible. At 1280×800 + 1100×700. Save as `phase-35-uat-activity-1280x800.png` and `phase-35-uat-activity-1100x700.png`.

14. **Screenshot chat surface** — with 6 synthetic sub-agent summaries inline. At 1280×800 + 1100×700. Save as `phase-35-uat-chat-1280x800.png` and `phase-35-uat-chat-1100x700.png`.

15. **Read back screenshots** — use the Read tool on each PNG. Cite a one-line observation per breakpoint:
    - `Sessions 1280×800: 6 fork rows with parent populated; Merge back button visible only on fork rows`
    - `Sessions 1100×700: same — no clipping, scroll preserved`
    - `ActivityStrip 1280×800: subagent_started + subagent_complete chips visible, no flooding`
    - `ActivityStrip 1100×700: same chip set, no overflow`
    - `Chat 1280×800: 6 synthetic [Sub-agent summary — step N…] AssistantTurns rendered inline`
    - `Chat 1100×700: same content; no overlap with input bar`

**Step C — Operator sign-off in 35-11-SUMMARY.md:**

For each of the 15 steps, record PASS / FAIL / DEFER with a 1-line note. If ANY step fails:
- Diagnose the root cause (usually a missing emit, a wiring miss in run_subagent_to_halt's v1 stub, or a frontend filter that drops sub-agent events).
- Fix in a follow-up plan OR document the deferred item in 35-11-SUMMARY.md.
- Re-run the failing step until PASS or accepted DEFER.

**Step D — Final phase-closure commit:**

When ALL 15 steps PASS (or DEFER explicitly accepted), commit 35-11-SUMMARY.md with a phase-close message:

```
docs(35-11): Phase 35 SUMMARY — DECOMP-01..05 closed; UAT 15/15 PASS; phase v1.5 milestone.
```

(Or if some steps DEFER: `docs(35-11): Phase 35 SUMMARY — DECOMP-01..05 closed; UAT 13/15 PASS, 2 DEFER (run_subagent_to_halt real LLM dispatch + chip flooding under N=10 sub-agents); phase v1.5 milestone with documented v1.6 follow-ups.`)
  </action>
  <verify>
    <automated>ls /home/arnav/blade/docs/testing\ ss/phase-35-uat-*.png 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 15 UAT steps execute (PASS / FAIL / DEFER documented)
    - 6 screenshots saved under `docs/testing ss/` (literal space)
    - 6 read-back observations cited in 35-11-SUMMARY.md
    - Operator sign-off recorded
    - DECOMP-01..05 mapped to specific UAT steps with PASS evidence
  </acceptance_criteria>
  <done>Phase 35 closed via runtime UAT; checkpoint:human-verify gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (Arnav) → UAT verdict | Human visual check of runtime behavior — load-bearing per CLAUDE.md Verification Protocol |
| Dev binary → real LLM dispatch | Real provider calls during UAT — sub-agent dispatch path (Plan 35-07's v1 stub may NOT exercise real LLM; UAT confirms or escalates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-40 | Tampering | UAT step 11 (cost cap) silently fires no chip → operator misses regression | mitigate | Step 11 explicitly checks for the RES-04 80% chip; operator sees ActivityStrip live. |
| T-35-41 | Tampering | UAT step 7 (kill switch off) silently fires sub-agents → operator misses recursion gate regression | mitigate | Step 7 explicitly checks "NO subagent_* chips" + "NO new sub-agent JSONLs". |
| T-35-42 | Tampering | UAT screenshots saved to wrong path (typo "testing-ss" instead of "testing ss" with space) | mitigate | Step 12-14 specifies the literal-space path; MEMORY.md documents the trap. |
| T-35-43 | DoS | UAT step 2's 6-step query + the cost cap interlock fires too aggressively → step 2 fails on first run | mitigate | Step 2 uses default cost_guard_per_conversation_dollars=25.0 (way above any 6-step task); step 11 lowers to $0.10 explicitly. |
| T-35-44 | Information disclosure | UAT screenshots may capture API keys / secrets in the dev binary's settings page | mitigate | Operator screenshots SessionsView / ActivityStrip / chat surface — none of which display secrets. Settings page is NOT screenshotted. |
</threat_model>

<verification>
- cargo check + tsc --noEmit + cargo test all green (Task 2)
- Panic-injection regression test green (Task 1)
- 15-step UAT executed by operator (Task 3)
- 6 screenshots saved + 6 read-back observations cited
- Operator sign-off in 35-11-SUMMARY.md
- Phase 35 closes when ALL 15 steps PASS (or DEFER explicitly accepted)
</verification>

<success_criteria>
1. phase35_decomp_panic_in_distill_caught_by_summary_layer test green
2. phase35_decomp_force_subagent_result_seam_provides_synthetic_summary test green
3. cargo check + tsc --noEmit + cargo test ALL exit 0
4. UAT step 1 PASS — dev binary boots cleanly
5. UAT step 2 PASS — 6-step query triggers fan-out, 6 sub-agent chips, 6 synthetic AssistantTurns
6. UAT step 3 PASS — sub-agent JSONLs exist with parent attribution
7. UAT step 4 PASS — SessionsView shows fork rows with parent populated
8. UAT step 5 PASS — cancel mid-fan-out gracefully completes in-flight sub-agents
9. UAT step 6 PASS — max_parallel_subagents=1 forces serial dispatch
10. UAT step 7 PASS — auto_decompose_enabled=false short-circuits the trigger
11. UAT step 8 PASS — Branch creates fork session
12. UAT step 9 PASS — Merge back appends synthetic UserMessage to parent
13. UAT step 10 PASS — model reply references merged content
14. UAT step 11 PASS — cost cap interlock halts decomposition gracefully
15. UAT steps 12-14 PASS — 6 screenshots saved at both viewports under `docs/testing ss/`
16. UAT step 15 PASS — read-back observations cited per breakpoint
17. Operator sign-off in 35-11-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/35-auto-decomposition/35-11-SUMMARY.md` recording:

# Phase 35 SUMMARY — Auto-Decomposition (DECOMP-01..05)

## Status: CLOSED (or DEFER with explicit follow-ups)

## Static gates
- cargo check: PASS
- tsc --noEmit: PASS
- cargo test: PASS (N tests)

## Panic-injection regression
- phase35_decomp_panic_in_distill_caught_by_summary_layer: PASS
- phase35_decomp_force_subagent_result_seam_provides_synthetic_summary: PASS

## UAT 15-step results
| Step | Description | Verdict |
|------|-------------|---------|
| 1 | Open dev binary | PASS |
| 2 | 6-step query → 6 sub-agent chips + summaries | PASS / DEFER |
| ... | ... | ... |
| 15 | Read back screenshots | PASS |

## Screenshots
- docs/testing ss/phase-35-uat-sessions-1280x800.png — observation: ...
- docs/testing ss/phase-35-uat-sessions-1100x700.png — observation: ...
- docs/testing ss/phase-35-uat-activity-1280x800.png — observation: ...
- docs/testing ss/phase-35-uat-activity-1100x700.png — observation: ...
- docs/testing ss/phase-35-uat-chat-1280x800.png — observation: ...
- docs/testing ss/phase-35-uat-chat-1100x700.png — observation: ...

## DECOMP-01..05 verdict
- DECOMP-01 (auto-trigger 5+ steps): PASS via UAT step 2 + 7
- DECOMP-02 (sub-agent isolation): PASS via UAT step 2 + 3 + 4 + 6 + 11
- DECOMP-03 (summary-only return): PASS via UAT step 2 (token cap)
- DECOMP-04 (fork + merge back): PASS via UAT step 8 + 9 + 10
- DECOMP-05 (progress visibility): PASS via UAT step 2 + 13 + 14 + 15

## Deferred follow-ups (if any)
- run_subagent_to_halt real run_loop dispatch wiring (Plan 35-07 v1 stub) — v1.6 if UAT confirms FORCE seam path is sufficient
- Optional get_subagent_summary Tauri command (Claude's discretion lock — recommend YES; defer if not needed for UAT)
- Recursive decomposition (sub-agent triggering own DECOMP-01) — explicitly out-of-scope, v1.6+
- Cross-sub-agent shared state — v1.6+
- Smart agent-role selection ML — v1.6+

## Operator sign-off
[Arnav records here]: PHASE 35 v1.5 CLOSED ✓
</output>
</content>
</invoke>