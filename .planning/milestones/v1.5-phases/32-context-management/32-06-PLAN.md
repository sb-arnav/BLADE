---
phase: 32-context-management
plan: 6
type: execute
wave: 3
depends_on: [1, 3]
files_modified:
  - src-tauri/src/brain.rs
  - src-tauri/src/lib.rs
  - src/lib/tauri/admin.ts
  - src/features/admin/DoctorPane.tsx
autonomous: true
requirements: [CTX-06]
requirements_addressed: [CTX-06]
must_haves:
  truths:
    - "A Tauri command `get_context_breakdown` exists and returns a populated ContextBreakdown after at least one chat turn"
    - "DoctorPane renders a Context Budget section showing per-section token counts as a monospace table"
    - "The breakdown updates per chat turn — refreshing when `chat_done` event fires (no polling)"
    - "Total tokens + model context window + percent_used are visible in the UI"
    - "If the command fails, DoctorPane gracefully omits the panel without breaking the rest of the page"
  artifacts:
    - path: "src-tauri/src/brain.rs"
      provides: "get_context_breakdown Tauri command (reads LAST_BREAKDOWN, computes totals, returns ContextBreakdown)"
      contains: "fn get_context_breakdown"
    - path: "src-tauri/src/lib.rs"
      provides: "get_context_breakdown registered in generate_handler!"
      contains: "get_context_breakdown"
    - path: "src/lib/tauri/admin.ts"
      provides: "getContextBreakdown() typed wrapper"
      contains: "getContextBreakdown"
    - path: "src/features/admin/DoctorPane.tsx"
      provides: "ContextBudgetSection component subscribed to chat_done"
      contains: "ContextBudgetSection"
  key_links:
    - from: "DoctorPane.tsx ContextBudgetSection"
      to: "brain.rs get_context_breakdown"
      via: "invoke<ContextBreakdown>(\"get_context_breakdown\")"
      pattern: "get_context_breakdown"
    - from: "ContextBudgetSection"
      to: "chat_done event listener"
      via: "useTauriEvent(BLADE_EVENTS.CHAT_DONE) OR raw listen('chat_done')"
      pattern: "chat_done"
---

<objective>
CTX-06: Surface the per-section token breakdown to the user. Plan 32-03 already populates `LAST_BREAKDOWN` thread_local in brain.rs. This plan adds the Tauri command that reads it, the typed TS wrapper, and the DoctorPane UI section that displays the breakdown updated per chat turn.

Purpose: ROADMAP.md success criterion 4: "DoctorPane (or a debug view) shows a per-section token breakdown: identity, memory, screen, file, recent, etc." Without this, CTX-01..05's effects are invisible to the user.

Output: Visible budget panel. After sending a chat message, opening the Diagnostics → Doctor pane shows the per-section token tally for that turn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md
@/home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md
@/home/arnav/blade/src-tauri/src/brain.rs
@/home/arnav/blade/src-tauri/src/lib.rs
@/home/arnav/blade/src/features/admin/DoctorPane.tsx
@/home/arnav/blade/src/lib/tauri/admin.ts
@/home/arnav/blade/.planning/phases/32-context-management/32-01-SUMMARY.md
@/home/arnav/blade/.planning/phases/32-context-management/32-03-SUMMARY.md

<interfaces>
ContextBreakdown wire type from Plan 32-01 (brain.rs):

    pub struct ContextBreakdown {
        pub query_hash: String,
        pub model_context_window: u32,
        pub total_tokens: usize,
        pub sections: HashMap<String, usize>,
        pub percent_used: f32,
        pub timestamp_ms: i64,
    }

LAST_BREAKDOWN accumulator + helpers from Plan 32-03 (brain.rs):

    pub(crate) static LAST_BREAKDOWN: thread_local RefCell<Vec<(String, usize)>>
    pub(crate) fn read_section_breakdown() -> Vec<(String, usize)>

DoctorPane existing patterns:
- Uses `lazy` import + `Dialog` + `DoctorRow` + `BLADE_EVENTS` typed events (NOT raw listen())
- ROW_ORDER (line 56) is a typed tuple of SignalClass enum values; adding "context_budget" as a new SignalClass is heavy ceremony
- The CONTEXT.md locked decision is "ship a monospace table inside an existing dialog/drawer" — i.e. DON'T expand the SignalClass enum; render the budget as a SEPARATE section underneath the doctor row list

Tauri command pattern (BLADE convention):

    #[tauri::command]
    pub async fn get_context_breakdown(app: tauri::AppHandle) -> Result<ContextBreakdown, String> {
        // ...
    }

generate_handler! is at lib.rs:607.

BLADE_EVENTS in src/lib/events lists the typed event names; chat_done is emitted from commands.rs (verify via `grep emit_stream_event /home/arnav/blade/src-tauri/src/commands.rs | head` — already widely used).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_context_breakdown Tauri command + register in lib.rs</name>
  <files>src-tauri/src/brain.rs, src-tauri/src/lib.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/brain.rs (LAST_BREAKDOWN + read_section_breakdown helpers added in Plan 32-03)
    /home/arnav/blade/src-tauri/src/lib.rs (line 607 generate_handler! macro — to register the new command at the end of the list)
    /home/arnav/blade/src-tauri/src/capability_probe.rs (infer_capabilities — to populate model_context_window in the breakdown)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-06 (verbatim Tauri command body)
  </read_first>
  <behavior>
    - Test 1: `phase32_get_context_breakdown_after_prompt_build` — call `build_system_prompt_inner` first to populate LAST_BREAKDOWN, then call the breakdown logic (the inner non-Tauri function — see Step A) and assert sections is non-empty + total_tokens > 0 + model_context_window > 0.
    - Test 2: `phase32_get_context_breakdown_empty_when_no_prompt_built` — clear LAST_BREAKDOWN, call breakdown logic, assert sections is empty + total_tokens == 0.
    - Test 3: `phase32_get_context_breakdown_percent_used_clamped` — set up a breakdown where total > model_context_window, assert percent_used is clamped to 100.0 (or saturates somehow — never NaN, never inf).
  </behavior>
  <action>
**Step A — Implement the breakdown logic as a `pub(crate)` non-Tauri function first (testable from `mod tests`), then expose it through a thin Tauri wrapper.** Place near the existing `read_section_breakdown` helper (added in Plan 32-03):

    /// Phase 32 / CTX-06 — pure breakdown computation, callable from tests.
    /// The Tauri wrapper `get_context_breakdown` is a one-line shim around this.
    pub fn build_breakdown_snapshot(provider: &str, model: &str) -> ContextBreakdown {
        let raw = read_section_breakdown(); // Vec<(String, usize)> char counts
        let mut sections: std::collections::HashMap<String, usize> =
            std::collections::HashMap::new();
        let mut total_chars: usize = 0;
        for (label, chars) in raw.iter() {
            *sections.entry(label.clone()).or_insert(0) += chars;
            total_chars += chars;
        }
        // chars / 4 = tokens (matches commands.rs estimate_tokens)
        let total_tokens = total_chars / 4;
        let sections_tokens: std::collections::HashMap<String, usize> = sections
            .into_iter()
            .map(|(k, v)| (k, v / 4))
            .collect();

        let (_, _, _, _, ctx_window) =
            crate::capability_probe::infer_capabilities(provider, model, None);

        let percent_used = if ctx_window == 0 {
            0.0
        } else {
            ((total_tokens as f32 / ctx_window as f32) * 100.0).min(100.0)
        };

        ContextBreakdown {
            query_hash: String::new(), // populated by Tauri wrapper if needed
            model_context_window: ctx_window,
            total_tokens,
            sections: sections_tokens,
            percent_used,
            timestamp_ms: chrono::Utc::now().timestamp_millis(),
        }
    }

**Caveat — capability_probe::infer_capabilities signature.** Plan 32-04's wrapper `model_context_window` lives in commands.rs. To avoid a cross-module dependency, brain.rs calls `capability_probe::infer_capabilities` directly here. Read the actual signature first; pass `None` for the optional API-derived ctx_window param if the third positional argument exists.

**Step B — Add the Tauri command:**

    /// Phase 32 / CTX-06 — Tauri command. Returns the per-section token
    /// breakdown of the most recent build_system_prompt_inner call. Reads
    /// the active config to determine model_context_window.
    #[tauri::command]
    pub async fn get_context_breakdown() -> Result<ContextBreakdown, String> {
        let cfg = crate::config::load_config();
        Ok(build_breakdown_snapshot(&cfg.provider, &cfg.model))
    }

**Step C — Register in lib.rs at the generate_handler! macro (line 607).** Read the existing macro contents first to find the alphabetical or logical place to insert. Append `brain::get_context_breakdown,` to the list (mind the comma; Tauri's macro is comma-separated):

    .invoke_handler(tauri::generate_handler![
        // ... existing 770+ commands ...
        brain::get_context_breakdown,  // Phase 32 / CTX-06
        // ... continued ...
    ])

The exact position matters only for readability. Place near other `brain::*` commands if any exist; otherwise append at the very end. Verify uniqueness:

    grep -c "get_context_breakdown" /home/arnav/blade/src-tauri/src/lib.rs

Should return at least 1 after the change. The file should compile cleanly.

**Step D — Append three tests to brain.rs `mod tests`:**

    #[test]
    fn phase32_get_context_breakdown_after_prompt_build() {
        // Build a prompt to populate LAST_BREAKDOWN
        let _ = build_system_prompt_inner(
            &[], "explain this rust function", None,
            &crate::router::ModelTier::Frontier,
            "anthropic", "claude-sonnet-4", 1,
        );
        let snap = build_breakdown_snapshot("anthropic", "claude-sonnet-4");
        assert!(!snap.sections.is_empty(),
            "expected at least one recorded section, got {:?}", snap.sections);
        assert!(snap.total_tokens > 0, "expected total_tokens > 0");
        assert!(snap.model_context_window >= 100_000,
            "expected anthropic context_window ≥ 100k, got {}", snap.model_context_window);
        assert!(snap.percent_used >= 0.0 && snap.percent_used <= 100.0,
            "percent_used out of bounds: {}", snap.percent_used);
    }

    #[test]
    fn phase32_get_context_breakdown_empty_when_no_prompt_built() {
        clear_section_accumulator();
        let snap = build_breakdown_snapshot("anthropic", "claude-sonnet-4");
        assert!(snap.sections.is_empty());
        assert_eq!(snap.total_tokens, 0);
        assert_eq!(snap.percent_used, 0.0);
    }

    #[test]
    fn phase32_get_context_breakdown_percent_used_clamped() {
        // Force a huge fake breakdown by directly populating LAST_BREAKDOWN
        clear_section_accumulator();
        // 1_000_000 chars = 250_000 tokens — exceeds anthropic 200k window
        record_section("synthetic_overflow", 1_000_000);
        let snap = build_breakdown_snapshot("anthropic", "claude-sonnet-4");
        assert!(snap.percent_used <= 100.0,
            "percent_used must clamp to ≤ 100, got {}", snap.percent_used);
        assert!(snap.percent_used.is_finite(),
            "percent_used must be finite, got {}", snap.percent_used);
    }

**Step E — Build + run + name-collision check:**

    cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
    cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_get_context_breakdown 2>&1 | tail -10
    grep -c "fn get_context_breakdown" /home/arnav/blade/src-tauri/src/

Last command must return 1 (exactly one definition exists — no name collision).
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_get_context_breakdown 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn build_breakdown_snapshot" /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c "pub async fn get_context_breakdown" /home/arnav/blade/src-tauri/src/brain.rs` returns 1
    - `grep -c "#\\[tauri::command\\]" /home/arnav/blade/src-tauri/src/brain.rs` returns at least 1 (the new command)
    - `grep -c "brain::get_context_breakdown" /home/arnav/blade/src-tauri/src/lib.rs` returns 1
    - `grep -rn "fn get_context_breakdown" /home/arnav/blade/src-tauri/src/ | wc -l` returns 1 (no other module defines this name — landmine #5 of RESEARCH.md)
    - `cd /home/arnav/blade/src-tauri && cargo test --lib brain::tests::phase32_get_context_breakdown 2>&1 | grep -c "test result: ok. 3 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
  </acceptance_criteria>
  <done>Tauri command compiled, registered, three tests green, no name collision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed TS wrapper + ContextBudgetSection in DoctorPane</name>
  <files>src/lib/tauri/admin.ts, src/features/admin/DoctorPane.tsx</files>
  <read_first>
    /home/arnav/blade/src/features/admin/DoctorPane.tsx (full file 1-318 — to mirror the section/dialog patterns)
    /home/arnav/blade/src/lib/tauri/admin.ts (find the existing wrapper function pattern — `doctorRunFullCheck` etc.)
    /home/arnav/blade/src/lib/events (to confirm BLADE_EVENTS includes a CHAT_DONE entry; if not, fall back to raw `listen('chat_done', ...)` with proper cleanup)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Context Budget Dashboard
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-06 (DoctorPane integration)
  </read_first>
  <behavior>
    - Test 1 (TS compile): `npx tsc --noEmit` exits 0.
    - Test 2 (visual / manual): the Diagnostics → Doctor sub-tab shows a "Context Budget" header with a monospace table (section, tokens, %) below the existing Doctor row list. (UAT step is in Plan 32-07 phase-wide UAT.)
  </behavior>
  <action>
**Step A — Add the typed wrapper to `src/lib/tauri/admin.ts`. Read the file first** to find the right place (look for existing `export async function doctor*` patterns):

    /**
     * Phase 32 / CTX-06 — fetch the per-section token breakdown from the
     * most recent build_system_prompt_inner call. Returns an object with
     * sections (label → token count), total_tokens, model_context_window,
     * percent_used (0..100), and timestamp_ms.
     */
    export type ContextBreakdown = {
      query_hash: string;
      model_context_window: number;
      total_tokens: number;
      sections: Record<string, number>;
      percent_used: number;
      timestamp_ms: number;
    };

    export async function getContextBreakdown(): Promise<ContextBreakdown> {
      const { invoke } = await import('@tauri-apps/api/core');
      return invoke<ContextBreakdown>('get_context_breakdown');
    }

If the file already imports `invoke` at module top, use that; the dynamic import shown above is safe but unnecessary if a static import already exists.

**Step B — Add ContextBudgetSection to DoctorPane.tsx. The CONTEXT.md decision is "render the budget as a SEPARATE section underneath the doctor row list" — i.e. do NOT expand SignalClass.** Place the new component INSIDE DoctorPane.tsx (not a separate file — keeps it simple).

Add near the imports:

    import {
      getContextBreakdown,
      type ContextBreakdown,
    } from '@/lib/tauri/admin';

(adjust path to match the existing imports at line 24-29).

Add the section component (above or below `function DoctorRow(...)` definition, anywhere in the file):

    function ContextBudgetSection() {
      const [breakdown, setBreakdown] = useState<ContextBreakdown | null>(null);
      const [error, setError] = useState<string | null>(null);

      const refresh = useCallback(async () => {
        try {
          const b = await getContextBreakdown();
          setBreakdown(b);
          setError(null);
        } catch (e) {
          // CTX-07 fallback: never crash the page on a backend hiccup.
          setError(typeof e === 'string' ? e : String(e));
          setBreakdown(null);
        }
      }, []);

      useEffect(() => {
        void refresh();
      }, [refresh]);

      // Refresh on chat_done event (no polling — RESEARCH.md preferred).
      useEffect(() => {
        let unlisten: (() => void) | null = null;
        (async () => {
          const { listen } = await import('@tauri-apps/api/event');
          unlisten = await listen('chat_done', () => {
            void refresh();
          });
        })();
        return () => {
          if (unlisten) unlisten();
        };
      }, [refresh]);

      if (error) {
        // Soft fail — render NOTHING rather than break the page (CTX-07 spirit)
        return null;
      }
      if (!breakdown) {
        return (
          <section className="diagnostics-section" style={{ marginTop: 'var(--s-3)' }}>
            <h4 className="diagnostics-section-title">Context Budget</h4>
            <p className="doctor-row-meta">Loading...</p>
          </section>
        );
      }

      const sortedSections = Object.entries(breakdown.sections)
        .sort(([, a], [, b]) => b - a);

      return (
        <section className="diagnostics-section" style={{ marginTop: 'var(--s-3)' }}>
          <h4 className="diagnostics-section-title">Context Budget</h4>
          <div className="doctor-row-meta" style={{ marginBottom: 'var(--s-2)' }}>
            {breakdown.total_tokens.toLocaleString()} tokens of{' '}
            {breakdown.model_context_window.toLocaleString()} budget (
            {breakdown.percent_used.toFixed(1)}%) — estimate, chars/4 heuristic
          </div>
          <table style={{ width: '100%', fontFamily: 'var(--font-mono, monospace)', fontSize: '12px' }}>
            <thead>
              <tr style={{ textAlign: 'left' }}>
                <th>Section</th>
                <th style={{ textAlign: 'right' }}>Tokens</th>
                <th style={{ textAlign: 'right' }}>% of total</th>
              </tr>
            </thead>
            <tbody>
              {sortedSections.map(([label, tokens]) => {
                const pct = breakdown.total_tokens > 0
                  ? (tokens / breakdown.total_tokens) * 100
                  : 0;
                return (
                  <tr key={label}>
                    <td>{label}</td>
                    <td style={{ textAlign: 'right' }}>{tokens.toLocaleString()}</td>
                    <td style={{ textAlign: 'right' }}>{pct.toFixed(1)}%</td>
                  </tr>
                );
              })}
            </tbody>
          </table>
        </section>
      );
    }

Mount the section inside the main DoctorPane return JSX, AFTER the `<div className="doctor-row-list">` block (around line 244-265 from the earlier read). Find the closing `</section>` of the main diagnostics section and insert `<ContextBudgetSection />` JUST BEFORE it (or as a sibling — the design is whatever fits the existing layout, but per CONTEXT.md "no bespoke design system work" so an inline section is acceptable).

Pragmatic placement — at the very end of the existing `return (...)` JSX:

    return (
      <>
        <section className="diagnostics-section">
          { /* existing content */ }
        </section>
        <ContextBudgetSection />
      </>
    );

Or convert the existing return to a Fragment with two siblings. **Read the existing structure first** to choose the smallest patch.

**Step C — Verify TypeScript compiles:**

    cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -20

Must exit 0. If any new errors appear, fix import paths or type names — the most likely error is a path mismatch on `@/lib/tauri/admin` vs `@/lib/tauri/admin.ts`.

**Step D — UAT smoke (deferred to Plan 32-07).** This task does NOT run a dev server — Plan 32-07 owns the phase-wide UAT including this surface. For now, the only check is `tsc --noEmit` clean.

**Step E — Caveat on CTX-07 spirit.** The component must NOT crash the DoctorPane if the Tauri call errors. The implementation above already handles this (returns `null` on error). Verify by mentally reading: setError(...) → if error → return null. No throw.
  </action>
  <verify>
    <automated>cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function getContextBreakdown" /home/arnav/blade/src/lib/tauri/admin.ts` returns 1
    - `grep -c "export type ContextBreakdown" /home/arnav/blade/src/lib/tauri/admin.ts` returns 1
    - `grep -c "function ContextBudgetSection" /home/arnav/blade/src/features/admin/DoctorPane.tsx` returns 1
    - `grep -c "<ContextBudgetSection />" /home/arnav/blade/src/features/admin/DoctorPane.tsx` returns 1
    - `grep -c "chat_done" /home/arnav/blade/src/features/admin/DoctorPane.tsx` returns at least 1
    - `cd /home/arnav/blade && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0 (Rust still clean — no Rust changes in this task)
  </acceptance_criteria>
  <done>Typed TS wrapper exists, ContextBudgetSection component renders below the Doctor section, listens to chat_done, gracefully handles errors, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain.rs LAST_BREAKDOWN → Tauri IPC → frontend | Section labels are stable enum-like strings owned by us; no untrusted input. |
| chat_done event listener → DoctorPane refresh | Event has no payload validation needed — refresh is a no-op pull from the same Tauri command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-15 | DoS | Backend command panics inside `get_context_breakdown` (e.g. capability_probe failure) | mitigate | Frontend wraps in try/catch and renders null on error (CTX-07 spirit); backend uses `infer_capabilities` which already has a fallback path |
| T-32-16 | Information Disclosure | Section labels could expose internal architecture details to a screenshot shared by the user | accept | Labels are the same as the priority-order comment block at brain.rs:472-490 — already documented in the source. Not sensitive. |
| T-32-17 | DoS | chat_done event fires hundreds of times → React re-render storm | accept | Each `refresh()` is a single Tauri call; React batching handles burst rates fine. If this becomes a problem, debounce in a future plan. |
</threat_model>

<verification>
- `cargo check` clean
- `cargo test --lib brain::tests::phase32_get_context_breakdown` all green
- `npx tsc --noEmit` clean
- All grep acceptance criteria satisfied
- No new Rust modules created (CONTEXT.md §Module Boundaries)
- Tauri command name unique (landmine #5)
- Phase-wide runtime UAT deferred to Plan 32-07 (CONTEXT.md §Testing & Verification step 7: screenshot DoctorPane breakdown panel at 1280×800 + 1100×700)
</verification>

<success_criteria>
1. `get_context_breakdown` Tauri command compiled and registered
2. `getContextBreakdown` TS wrapper typed and importable
3. `ContextBudgetSection` rendered in DoctorPane below the existing doctor row list
4. Section refreshes on `chat_done` event (no polling)
5. On backend error, section renders null (does not break the page)
6. tsc + cargo check both clean
7. Three brain.rs tests green
</success_criteria>

<output>
After completion, create `.planning/phases/32-context-management/32-06-SUMMARY.md` with:
- Insertion point chosen for ContextBudgetSection in DoctorPane (line numbers)
- Whether BLADE_EVENTS.CHAT_DONE was used or raw listen('chat_done') was the fallback path
- Rust + TS check pass times
- Any deferred work for Plan 32-07 (toggle UI to flip smart_injection_enabled at runtime — recommended but not blocking; shipped in 32-07 if cheap)
</output>
