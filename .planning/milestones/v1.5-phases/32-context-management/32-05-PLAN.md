---
phase: 32-context-management
plan: 5
type: execute
wave: 2
depends_on: [1, 2, 4]
files_modified:
  - src-tauri/src/commands.rs
autonomous: true
requirements: [CTX-05]
requirements_addressed: [CTX-05]
must_haves:
  truths:
    - "cap_tool_output(content, 4000) on a 50k-char input returns a string ≤ ~4500 tokens with both head AND tail of the original preserved"
    - "Output contains the marker text \"[truncated from\" so callers / tests / users can recognize truncation"
    - "Outputs at or under the budget pass through unchanged with storage_id None"
    - "Non-ASCII / emoji content does not panic — safe_slice is used for all string slicing"
    - "cap_tool_output is invoked at BOTH conversation-insertion sites: the native tool branch AND the MCP branch in the tool loop"
    - "MAX_TOOL_RESULT_CHARS in format_tool_result is raised so it doesn't silently truncate the input before cap_tool_output can run (RESEARCH.md landmine #9)"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "cap_tool_output helper + ToolOutputCap struct + raised MAX_TOOL_RESULT_CHARS + wiring at both tool-insertion sites"
      contains: "fn cap_tool_output"
  key_links:
    - from: "tool loop native branch (around commands.rs:2237)"
      to: "cap_tool_output"
      via: "content = cap_tool_output(&content, config.context.tool_output_cap_tokens).content"
      pattern: "cap_tool_output\\("
    - from: "tool loop MCP branch (around commands.rs:2247)"
      to: "cap_tool_output"
      via: "content = cap_tool_output(&content, config.context.tool_output_cap_tokens).content"
      pattern: "cap_tool_output\\("
---

<objective>
CTX-05: Cap individual tool outputs at a configurable token budget (default 4000 tokens) before they enter the conversation. Today, `format_tool_result` hard-truncates at 12k chars without preserving the tail — a bash output ending in a critical error message loses that error. After this plan, the head + tail + truncation summary pattern (universal across modern agents per RESEARCH.md) is the canonical path.

Purpose: Phase 32's goal is "a 50k-character bash output produces a tool output capped at ~4k tokens with a summary appended" (ROADMAP.md success criterion 3). This plan ships exactly that.

Output: One new helper function, one bumped constant, two call-site wirings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md
@/home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md
@/home/arnav/blade/src-tauri/src/commands.rs
@/home/arnav/blade/.planning/phases/32-context-management/32-01-SUMMARY.md
@/home/arnav/blade/.planning/phases/32-context-management/32-02-SUMMARY.md

<interfaces>
Existing tool-result wiring in commands.rs:

L336: `const MAX_TOOL_RESULT_CHARS: usize = 12_000;` — used by format_tool_result for hard truncation
L2237: `let (mut content, is_error) = if is_native { native_tools::execute(...).await } else { ... }` — native tool path
L2247: `Ok(r) => (format_tool_result(&r), r.is_error)` — MCP tool path
L2387 (approx): `conversation.push(ConversationMessage::Tool { content, ... })` — final insertion site
L2889-L2918: `fn format_tool_result(result: &McpToolResult) -> String` — the function that currently hard-truncates

`safe_slice` lives at lib.rs:180 (CLAUDE.md mandate for non-ASCII safety):

    pub fn safe_slice(s: &str, max_chars: usize) -> &str

Returns a borrowed slice up to max_chars CHARACTERS (not bytes), respecting char boundaries.

ContextConfig.tool_output_cap_tokens from Plan 32-01 (config.rs): default 4000.

build_test_conversation from Plan 32-02 — used here for the integration of cap_tool_output into the tool loop only via separate unit tests against cap_tool_output directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cap_tool_output helper + ToolOutputCap struct</name>
  <files>src-tauri/src/commands.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/commands.rs (L336 MAX_TOOL_RESULT_CHARS; L2889-L2918 format_tool_result)
    /home/arnav/blade/src-tauri/src/lib.rs (L180 — safe_slice signature)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-05 (verbatim implementation pattern)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Tool Output Caps
  </read_first>
  <behavior>
    - Test 1: `phase32_cap_tool_output_under_budget_passthrough` — content of 200 chars (~50 tokens) with budget 4000 returns content unchanged, storage_id = None.
    - Test 2: `phase32_cap_tool_output_over_budget_truncates` — content of 50_000 chars (~12_500 tokens) with budget 4000 returns a string of less than 20_000 chars (≈ 5000 tokens, allowing for summary text overhead) AND the result contains the marker substring "[truncated from".
    - Test 3: `phase32_cap_tool_output_preserves_head_and_tail` — content "HEAD_MARKER_X" + "x".repeat(50_000) + "TAIL_MARKER_Z", budget 4000. Result must contain both "HEAD_MARKER_X" AND "TAIL_MARKER_Z" — not just one of them.
    - Test 4: `phase32_cap_tool_output_non_ascii_safe` — content of an emoji-heavy string (e.g. "🔥".repeat(20_000)) with budget 4000 must NOT panic. Result is a valid String.
    - Test 5: `phase32_cap_tool_output_storage_id_when_truncated` — content over budget returns storage_id Some(_); under budget returns None.
  </behavior>
  <action>
**Step A — Add the ToolOutputCap struct + cap_tool_output helper. Place these near format_tool_result (around commands.rs:2885, just before the existing function — both are tool-result-related):**

    /// Phase 32 / CTX-05 — return type from `cap_tool_output`.
    /// Original full content reach-back is a Phase 33+ concern; in Phase 32 the
    /// `storage_id` is just a marker that truncation occurred (used by tests).
    #[derive(Debug, Clone)]
    pub struct ToolOutputCap {
        /// The (possibly truncated) content destined for the conversation.
        pub content: String,
        /// Some(id) when truncation occurred, None otherwise.
        pub storage_id: Option<String>,
        /// Approximate token count of the ORIGINAL (untruncated) input. Computed
        /// as chars / 4 to match estimate_tokens.
        pub original_tokens: usize,
    }

    /// Phase 32 / CTX-05 — cap a single tool output at `budget_tokens`. When the
    /// content fits, returns it unchanged with storage_id=None. When it doesn't,
    /// returns the head (~75% of budget) + a truncation marker + the tail
    /// (~12.5% of budget) so callers can see both ends of the output.
    ///
    /// MUST use crate::safe_slice for all char-based slicing — `&s[..n]` panics
    /// on non-ASCII boundaries (CLAUDE.md mandate, MEMORY.md
    /// reference_testing_ss_dir for the v1.1 retraction precedent).
    pub fn cap_tool_output(content: &str, budget_tokens: usize) -> ToolOutputCap {
        let estimated_tokens = content.chars().count() / 4;
        if estimated_tokens <= budget_tokens {
            return ToolOutputCap {
                content: content.to_string(),
                storage_id: None,
                original_tokens: estimated_tokens,
            };
        }

        // Budget split: 75% to the head, 12.5% to the tail, ~12.5% reserved
        // for the truncation marker text. (universal pattern per RESEARCH.md)
        let budget_chars = budget_tokens * 4;
        let head_chars = (budget_chars as f32 * 0.75) as usize;
        let tail_chars = (budget_chars as f32 * 0.125) as usize;

        // SAFE-SLICE: never raw [..n]. safe_slice respects char boundaries.
        let head = crate::safe_slice(content, head_chars);

        // Tail: take the last `tail_chars` characters. char_indices in reverse
        // gives us the boundary, then slice from there. If the tail boundary
        // computation fails, fall back to empty tail.
        let total_chars = content.chars().count();
        let tail = if total_chars > tail_chars {
            // Find the byte index `tail_chars` characters from the end.
            let skip_chars = total_chars.saturating_sub(tail_chars);
            content
                .char_indices()
                .nth(skip_chars)
                .map(|(byte_idx, _)| &content[byte_idx..])
                .unwrap_or("")
        } else {
            content
        };

        let omitted_tokens = estimated_tokens.saturating_sub(budget_tokens);
        let storage_id = format!(
            "tool_out_{}",
            chrono::Utc::now().timestamp_millis()
        );

        let content_out = format!(
            "{}\n\n[truncated from {} tokens; ~{} omitted in middle; storage_id {}]\n\n{}",
            head, estimated_tokens, omitted_tokens, storage_id, tail
        );

        ToolOutputCap {
            content: content_out,
            storage_id: Some(storage_id),
            original_tokens: estimated_tokens,
        }
    }

**Caveat — `chrono::Utc::now()` import.** Verify chrono is already imported at the top of commands.rs (`grep "use chrono" src-tauri/src/commands.rs`). Almost certainly yes (tons of timestamp use elsewhere). If not, use `std::time::SystemTime::now().duration_since(UNIX_EPOCH).unwrap_or_default().as_millis()` as a fallback — simpler, no import needed.

**Step B — Append five tests to commands.rs `mod tests` block (created in Plan 32-02):**

    #[test]
    fn phase32_cap_tool_output_under_budget_passthrough() {
        let small = "tiny output, well under budget";
        let r = cap_tool_output(small, 4000);
        assert_eq!(r.content, small);
        assert!(r.storage_id.is_none());
        assert!(r.original_tokens < 50);
    }

    #[test]
    fn phase32_cap_tool_output_over_budget_truncates() {
        let big = "x".repeat(50_000); // ~12_500 tokens
        let r = cap_tool_output(&big, 4000);
        // Output is shaped roughly like: head(12k chars) + marker(~150 chars) + tail(2k chars) ≈ 14k chars.
        assert!(r.content.len() < 20_000,
            "result should be under ~5000 tokens, got {} chars (~{} tokens)",
            r.content.len(), r.content.len() / 4);
        assert!(r.content.contains("[truncated from"),
            "missing truncation marker in: {}", &r.content[..200.min(r.content.len())]);
        assert!(r.storage_id.is_some());
        assert!(r.original_tokens >= 10_000);
    }

    #[test]
    fn phase32_cap_tool_output_preserves_head_and_tail() {
        let mut content = String::from("HEAD_MARKER_X");
        content.push_str(&"x".repeat(50_000));
        content.push_str("TAIL_MARKER_Z");
        let r = cap_tool_output(&content, 4000);
        assert!(r.content.contains("HEAD_MARKER_X"),
            "head marker missing — head not preserved");
        assert!(r.content.contains("TAIL_MARKER_Z"),
            "tail marker missing — tail not preserved (this is the bug v1.1 lesson teaches)");
    }

    #[test]
    fn phase32_cap_tool_output_non_ascii_safe() {
        // 20_000 fire emojis = ~80_000 bytes (each fire is 4 bytes UTF-8) but
        // ~20_000 chars and ~5000 tokens. With budget 4000, must truncate.
        // Critically, must not panic on char-boundary slicing.
        let emoji = "🔥".repeat(20_000);
        let r = std::panic::catch_unwind(|| cap_tool_output(&emoji, 4000));
        assert!(r.is_ok(), "non-ASCII content caused panic — safe_slice not used");
        let r = r.unwrap();
        assert!(r.content.contains("[truncated from"));
    }

    #[test]
    fn phase32_cap_tool_output_storage_id_when_truncated() {
        let small = cap_tool_output("hello", 4000);
        assert!(small.storage_id.is_none(), "small output got storage_id");
        let big = cap_tool_output(&"y".repeat(50_000), 4000);
        assert!(big.storage_id.is_some(), "big output missing storage_id");
        let id = big.storage_id.unwrap();
        assert!(id.starts_with("tool_out_"), "storage_id format wrong: {}", id);
    }

**Step C — Build + run:**

    cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
    cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_cap_tool_output 2>&1 | tail -15

All five tests must pass. cargo check must be clean.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_cap_tool_output 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub struct ToolOutputCap" /home/arnav/blade/src-tauri/src/commands.rs` returns 1
    - `grep -c "pub fn cap_tool_output" /home/arnav/blade/src-tauri/src/commands.rs` returns 1
    - `grep -c "crate::safe_slice" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 1 NEW occurrence inside cap_tool_output (verify by searching the function body)
    - `grep -c "&content\[\.\.\|&s\[\.\.\|&content\[..n" /home/arnav/blade/src-tauri/src/commands.rs` does NOT increase from baseline (no raw byte slicing introduced)
    - `cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_cap_tool_output 2>&1 | grep -c "test result: ok. 5 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
  </acceptance_criteria>
  <done>cap_tool_output helper compiles, five unit tests green including non-ASCII safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cap_tool_output at both tool-insertion sites + raise MAX_TOOL_RESULT_CHARS</name>
  <files>src-tauri/src/commands.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/commands.rs (L336 MAX_TOOL_RESULT_CHARS; L2237-L2300 native + MCP tool branches; L2300-L2400 the conversation.push site)
    /home/arnav/blade/.planning/phases/32-context-management/32-RESEARCH.md §CTX-05 landmine #9 (ordering: cap_tool_output must run BEFORE format_tool_result truncation, OR format_tool_result's cap must be raised so cap_tool_output sees the full content)
    /home/arnav/blade/.planning/phases/32-context-management/32-CONTEXT.md §Tool Output Caps
  </read_first>
  <behavior>
    - Test 1: `phase32_format_tool_result_no_longer_truncates_at_12k` — synthesize a McpToolResult with 50k-char content, call `format_tool_result(&r)`, assert the returned string is at least 30_000 chars long (i.e. the new MAX is much higher than 12k; cap_tool_output, NOT format_tool_result, is now the budget enforcer).
    - Test 2: `phase32_format_tool_result_still_caps_at_safety_ceiling` — same with 1_000_000-char content; result is bounded (e.g. ≤ 250_000 chars). format_tool_result still has a hard ceiling; just much higher than 12k.
  </behavior>
  <action>
**Decision per RESEARCH.md landmine #9:** Raise `MAX_TOOL_RESULT_CHARS` from 12_000 to 200_000 (a safety net for truly pathological outputs — multi-MB strings — without silently dropping the tail before cap_tool_output runs). The actual per-message budget (4000 tokens / ~16k chars) is enforced by `cap_tool_output`, which runs at the conversation-insertion site and operates on the full output.

**Step A — Bump the constant at commands.rs:336:**

    // Phase 32 / CTX-05: raised from 12_000 to 200_000. The 12k value
    // was silently dropping critical tails before cap_tool_output (the
    // real per-message budget enforcer) could see them. format_tool_result
    // is now a SAFETY net for multi-MB pathological outputs only.
    const MAX_TOOL_RESULT_CHARS: usize = 200_000;

**Step B — Wire cap_tool_output at the native + MCP tool branches.** The current pattern (commands.rs:2237):

    let (mut content, is_error) = if is_native {
        crate::native_tools::execute(&tool_call.name, &tool_call.arguments, Some(&app)).await
    } else {
        let result = { /* manager.call_tool ... */ };
        match result {
            Ok(r) => (format_tool_result(&r), r.is_error),
            Err(e) => { /* autoskills + immune fallback */ }
        }
    };

After this `let (mut content, is_error) = ...;` block (and after any subsequent enrichment that may rewrite `content`, e.g. the explain_tool_failure block at L2302-L2315), insert the cap. The right insertion point is **as late as possible** — AFTER all enrichment and BEFORE the `conversation.push(ConversationMessage::Tool { content, ... })` site. Find the conversation.push call (around L2387 per RESEARCH.md) and insert immediately before it:

    // Phase 32 / CTX-05 — cap the tool output at the configured budget
    // BEFORE inserting into the conversation. The cap runs LAST so any
    // upstream enrichment (explain_tool_failure, immune-system rewrites)
    // gets included in the cap accounting.
    let _capped = crate::commands::cap_tool_output(&content, config.context.tool_output_cap_tokens);
    if _capped.storage_id.is_some() {
        log::info!(
            "[CTX-05] tool '{}' output capped: {} → {} tokens (storage_id {})",
            tool_call.name,
            _capped.original_tokens,
            _capped.content.chars().count() / 4,
            _capped.storage_id.as_deref().unwrap_or("?"),
        );
    }
    let content = _capped.content;
    // (existing conversation.push call follows verbatim, using the new `content`)

**Note: `crate::commands::cap_tool_output` may not need the `crate::commands::` prefix** if the call is in the same module. Use the bare `cap_tool_output(...)` name — `cargo check` will clarify.

**Caveat: variable shadowing.** The existing code uses `let (mut content, is_error) = ...`. The capped value re-binds `content` (no longer mutable). This is intentional — after capping, `content` should be immutable for the conversation.push. If the existing code mutates `content` after this point (e.g. another enrichment), perform the cap call after the LAST mutation. Read carefully.

**Step C — Append two regression tests for format_tool_result. The test must construct a synthetic McpToolResult — see McpToolResult definition first:**

    grep -n "pub struct McpToolResult\|struct McpToolResult" /home/arnav/blade/src-tauri/src/

If it has fields `{ content: Vec<McpContent>, is_error: bool }` and `McpContent { text: Option<String>, ... }`, build like:

    fn make_result(text: &str, is_error: bool) -> crate::mcp::McpToolResult {
        crate::mcp::McpToolResult {
            content: vec![crate::mcp::McpContent {
                text: Some(text.to_string()),
                // ... other fields default
                ..Default::default()
            }],
            is_error,
        }
    }

If McpToolResult or McpContent does NOT implement Default, build manually with all required fields. **READ the actual struct first.** Adjust the test fixture accordingly.

Tests:

    #[test]
    fn phase32_format_tool_result_no_longer_truncates_at_12k() {
        let big = "y".repeat(50_000);
        let r = make_result(&big, false);
        let formatted = format_tool_result(&r);
        // Old behavior: hard-truncated at 12_000 + ~50 chars of marker.
        // New behavior: passes 50_000 through (under the new 200_000 ceiling).
        assert!(formatted.len() >= 30_000,
            "format_tool_result should no longer truncate small-MB outputs (got {} chars)",
            formatted.len());
    }

    #[test]
    fn phase32_format_tool_result_still_caps_at_safety_ceiling() {
        let huge = "z".repeat(500_000);
        let r = make_result(&huge, false);
        let formatted = format_tool_result(&r);
        // 200k ceiling + ~80 chars marker = ~200_080 chars
        assert!(formatted.len() <= 250_000,
            "format_tool_result must still have an outer safety ceiling (got {} chars)",
            formatted.len());
    }

If McpToolResult / McpContent are not constructible from outside the `mcp` module (private fields), make `make_result` a `pub(crate)` helper inside `mcp.rs` for use in tests, OR drop these two tests and rely on the cap_tool_output unit tests + UAT. Document the choice in 32-05-SUMMARY.md.

**Step D — Build + run:**

    cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
    cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32 2>&1 | tail -20

The 7 phase32_cap_tool_output_* + phase32_format_tool_result_* tests must all pass. Pre-existing tests must remain green.

**Step E — UAT considerations.** This plan touches the live tool loop. Plan 32-07 owns the phase-wide UAT (it's the LAST wave and has all of Phase 32 in scope). **For THIS plan, the safety check is purely automated:** verify dev binary still compiles by running `cargo check` (don't run dev server here — Plan 32-07 does that once for everything).
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MAX_TOOL_RESULT_CHARS: usize = 200_000" /home/arnav/blade/src-tauri/src/commands.rs` returns 1
    - `grep -c "MAX_TOOL_RESULT_CHARS: usize = 12_000" /home/arnav/blade/src-tauri/src/commands.rs` returns 0 (old value removed)
    - `grep -c "cap_tool_output(&content" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 1 (wired into the tool loop)
    - `grep -c "tool_output_cap_tokens" /home/arnav/blade/src-tauri/src/commands.rs` returns at least 1
    - `grep -c "\\[CTX-05\\] tool" /home/arnav/blade/src-tauri/src/commands.rs` returns 1 (the log line proves wiring)
    - `cd /home/arnav/blade/src-tauri && cargo test --lib commands::tests::phase32_cap_tool_output 2>&1 | grep -c "test result: ok. 5 passed"` returns 1
    - `cd /home/arnav/blade/src-tauri && cargo check 2>&1 | grep -c "error\["` returns 0
  </acceptance_criteria>
  <done>MAX raised, cap_tool_output wired at the conversation.push site for tool messages, log line confirms wiring, all phase32 commands tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool execution result → conversation buffer | Untrusted tool output (bash, file read, MCP) crosses into the LLM context. cap_tool_output is the bottleneck enforcing the 4k-token budget. |
| non-ASCII / emoji content → string slicing | Raw `&s[..n]` panics; safe_slice respects char boundaries. v1.1 lesson. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | DoS | A 50k-token tool output blows the budget on a single call | mitigate | cap_tool_output enforces 4000-token cap with head + tail + marker; per-message, not aggregate (compaction handles aggregate) |
| T-32-12 | DoS | Non-ASCII content panics during slicing (v1.1 lesson, MEMORY.md project_v11_close_failed_uat) | mitigate | safe_slice mandate; `phase32_cap_tool_output_non_ascii_safe` regression test |
| T-32-13 | Information Disclosure | format_tool_result no longer hard-truncates → MORE tool output flows into the conversation, increasing prompt size | accept | cap_tool_output runs immediately AFTER format_tool_result and BEFORE conversation.push; net flow is bounded by the 4000-token budget. The 200k ceiling is purely a safety net. |
| T-32-14 | Tampering | tool_output_cap_tokens config set to 0 by adversarial / corrupt config | accept | Budget 0 means everything is "over budget", so all outputs return the marker. Worst case: chat is degraded but functional. f32-style validation could be added in a future plan. |
</threat_model>

<verification>
- `cargo check` clean
- `cargo test --lib commands::tests::phase32_cap_tool_output commands::tests::phase32_format_tool_result` all green
- All grep acceptance criteria satisfied
- safe_slice used inside cap_tool_output (no `&content[..n]` raw slice introduced)
- Pre-existing test suite still green
- Phase-wide runtime UAT (50k-char bash output round-trip in a real conversation) handled by Plan 32-07
</verification>

<success_criteria>
1. `cap_tool_output(50k-chars, 4000)` returns ≤ ~5000 tokens with both head and tail preserved
2. Non-ASCII / emoji input does not panic
3. cap_tool_output is wired at the tool-conversation insertion site (single canonical call site that covers both native and MCP branches)
4. `MAX_TOOL_RESULT_CHARS` raised to 200_000; format_tool_result no longer dropping tails before cap_tool_output runs
5. Five phase32_cap_tool_output_* tests + two phase32_format_tool_result_* tests all green
6. No pre-existing test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/32-context-management/32-05-SUMMARY.md` with:
- Insertion site chosen for the cap call (line numbers and surrounding context)
- McpToolResult constructibility outcome (whether the format_tool_result tests landed or were dropped, and why)
- Token cost observed: same 50k-char input before vs after — paste exact char counts
- Any deferred work for Plan 32-06 (storage_id reach-back is explicitly Phase 33+)
</output>
