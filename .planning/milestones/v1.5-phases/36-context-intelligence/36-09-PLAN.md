---
phase: 36-context-intelligence
plan: 9
type: execute
wave: 5
depends_on: [36-01, 36-02, 36-03, 36-04, 36-05, 36-06, 36-07, 36-08]
files_modified:
  - src-tauri/src/intelligence/repo_map.rs
  - src-tauri/src/intelligence/anchor_parser.rs
  - docs/testing ss/phase-36-uat-*.png
autonomous: false
requirements: [INTEL-01, INTEL-02, INTEL-03, INTEL-04, INTEL-05, INTEL-06]
requirements_addressed: [Phase-wide closure — wires ALL six INTEL requirements through to runtime UAT per CLAUDE.md Verification Protocol. (1) Adds the panic-injection regression test for build_repo_map — force a panic via PageRank seam path or by injecting a synthetic graph-load failure; assert brain.rs falls through to FTS code section without crashing. (2) Adds the panic-injection regression test for anchor_parser — INTEL_FORCE_ANCHOR_PANIC seam exercises the catch_unwind discipline at commands.rs. (3) Runs the 17-step checkpoint:human-verify UAT — tree-sitter parses fixture file, repo map injects at code gate, canonical_models.json round-trips through reload command, router uses registry for vision routing, @file:/@screen/@memory: anchor expansion all visible at runtime. (4) Closes Phase 36.]
must_haves:
  truths:
    - "phase36_intel_03_repo_map_falls_through_to_fts_on_panic test exists and is green — forces panic via PageRank/build_repo_map seam, asserts brain.rs prompt builder still completes; LAST_BREAKDOWN does NOT contain repo_map row"
    - "phase36_intel_06_anchor_parser_panic_caught_by_commands_layer test exists and is green — INTEL_FORCE_ANCHOR_PANIC=true; asserts catch_unwind in commands.rs returns (original_query, Vec::new())"
    - "checkpoint:human-verify task with 17-step UAT script per 36-CONTEXT.md §Testing & Verification — operator-checked"
    - "tsc --noEmit clean; cargo check clean; the existing 37 verify gates remain green"
    - "Screenshots saved at docs/testing ss/ (literal space) for DoctorPane repo_map breakdown row + ChatComposer with @screen/@file:/@memory: chips at 1280×800 + 1100×700"
    - "Operator sign-off recorded in 36-09-SUMMARY.md as the phase-closure gate"
    - "All 6 INTEL requirements verifiable via UAT steps mapped 1:1: INTEL-01 (steps 2 + 6), INTEL-02 (steps 3 + 4), INTEL-03 (steps 3 + 4 + 5 + 6 + 15), INTEL-04 (steps 7 + 14), INTEL-05 (step 7 + 14), INTEL-06 (steps 8 + 9 + 10 + 11 + 12 + 13 + 16 + 17)"
  artifacts:
    - path: "src-tauri/src/intelligence/repo_map.rs"
      provides: "phase36_intel_03_repo_map_falls_through_to_fts_on_panic test"
      contains: "phase36_intel_03_repo_map_falls_through_to_fts_on_panic"
    - path: "src-tauri/src/intelligence/anchor_parser.rs"
      provides: "phase36_intel_06_anchor_parser_panic_caught_by_commands_layer test (or in commands.rs)"
      contains: "phase36_intel_06_anchor_parser_panic_caught_by_commands_layer"
    - path: "docs/testing ss/phase-36-uat-*.png"
      provides: "UAT screenshots at 1280×800 + 1100×700 per CLAUDE.md Verification Protocol"
      contains: "phase-36-uat-"
  key_links:
    - from: "Operator (Arnav)"
      to: "Phase 36 closure"
      via: "checkpoint:human-verify with 17-step UAT script + screenshot evidence"
      pattern: "checkpoint:human-verify"
---

<objective>
Close Phase 36 by wiring ALL six INTEL requirements through to runtime UAT per CLAUDE.md Verification Protocol. The static-gates-only plans (36-01..36-08) prove syntax/types/structure; this plan proves the running binary surfaces context intelligence correctly: tree-sitter symbol graph populates kg_nodes/kg_edges with `node_type='symbol'`, repo_map row appears in DoctorPane with non-zero char count for code-shaped queries (and absent for non-code queries — proves gate behavior), repo map differs across queries (proves PageRank personalization), canonical_models.json hand-edits round-trip through `reload_capability_registry`, vision routing transparency holds (model with vision=false routes to vision-capable fallback), and AnchorChip rendering + per-anchor injection all work end-to-end.

The v1.1 lesson is load-bearing: 27 verify gates green + chat broken = retracted milestone. Phase 36 ships 6 requirements that, by design, can mask regressions on the runtime path (e.g., a tree-sitter panic that gets swallowed by catch_unwind without surfacing to LAST_BREAKDOWN, or registry-vs-probe mismatch driving the wrong model silently). The runtime UAT proves the user sees the right behavior on real flows.

Output: 2 panic-injection regression tests added (Task 1); 17-step UAT executed by the operator (Task 2 + Task 3); screenshots saved + read back; checkpoint:human-verify gates phase closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/arnav/blade/CLAUDE.md
@/home/arnav/blade/.planning/phases/36-context-intelligence/36-CONTEXT.md
@/home/arnav/blade/.planning/phases/35-auto-decomposition/35-11-PLAN.md (Phase 35's runtime UAT pattern — mirror)

<interfaces>
<!-- Panic-injection regression test seams (recap):

  Plan 36-02 declared INTEL_FORCE_PARSE_ERROR (tree-sitter parse error path).
  Plan 36-03 declared INTEL_FORCE_PAGERANK_RESULT (synthetic rank result).
  Plan 36-05 declared INTEL_FORCE_REGISTRY_MISS (probe-fallback path).
  Plan 36-07 declared INTEL_FORCE_ANCHOR_PANIC (panic at anchor parser).

  Plan 36-09 panic regressions:

  Test 1 (repo_map fall-through):
    - Inject a synthetic 1-symbol PageRank result via INTEL_FORCE_PAGERANK_RESULT
    - Force build_repo_map to panic by manipulating render_map fixture or directly
      calling catch_unwind around a panicking closure (simpler: wrap the test
      logic to verify catch_unwind in brain.rs's call site catches a forced
      panic and returns None)
    - Cleanest: test that build_repo_map(empty config), with FORCE_PAGERANK_RESULT=Some,
      returns Some when config.tree_sitter_enabled=true; AND when build_repo_map
      panics (forced via a new INTEL_FORCE_REPO_MAP_PANIC seam if needed), the
      brain.rs call site's catch_unwind converts panic -> None.
    - For Plan 36-09 simplicity: the brain.rs side already wraps build_repo_map
      in catch_unwind (Plan 36-04 wired this). The test verifies the
      catch_unwind boundary by:
        1. Setting up a context where build_repo_map would panic (via a force
           seam or via a fixture that triggers a render panic — easiest is to
           introduce a one-line `if INTEL_FORCE_REPO_MAP_PANIC.with(|c| c.get())
           { panic!("forced") }` at the top of build_repo_map and call the
           brain.rs prompt builder).
        2. Asserting prompt assembly completes (no panic propagates), AND
           LAST_BREAKDOWN does NOT contain a repo_map row, AND the existing
           FTS code section ran instead.

  Test 2 (anchor parser fall-through):
    - INTEL_FORCE_ANCHOR_PANIC=true (declared in Plan 36-07).
    - Call commands.rs path that runs extract_anchors via catch_unwind.
    - Assert: catch_unwind catches; clean_query == original user_query;
      anchors == Vec::new(); chat continues.
    - This test lives in either commands.rs (integration) or anchor_parser.rs
      (where the seam is declared) — operator's choice; both work.
-->

<!-- 17-step UAT script (36-CONTEXT.md §Testing & Verification verbatim):

  1. Open dev binary (`npm run tauri dev`)
  2. Run `reindex_symbol_graph` for the BLADE project root — assert SQLite
     `kg_nodes` gains rows with `node_type = 'symbol'`; assert `kg_edges`
     gains rows with `relation IN ('calls', 'imports', 'uses_type', 'defines')`
  3. Send a code-shaped query: "Where does send_message_stream_inline call
     into providers?" — assert DoctorPane breakdown shows a `repo_map` row
     with non-zero char count; assert the assistant reply references real
     symbols from BLADE's codebase (not hallucinations)
  4. Send a different code query: "What does build_system_prompt_inner depend
     on?" — assert the repo map row contains DIFFERENT top symbols than (3)
     (proves personalization)
  5. Send a non-code query: "What's the weather?" — assert no `repo_map` row
     appears in DoctorPane breakdown (gate closed)
  6. Toggle `intelligence.tree_sitter_enabled = false` — re-run (3) — assert
     the assistant still replies (FTS fallback works) but no `repo_map` row
     in DoctorPane
  7. Manually edit `~/.config/blade/canonical_models.json` to set
     `claude-haiku-4-5: { vision: false }`; configure BLADE to use
     `anthropic/claude-haiku-4-5` as primary; send a query with an attached
     image — assert the actual provider call goes to a vision-capable model
     (check via `get_active_model_capabilities` or trace logs); assert the
     assistant successfully replies about the image
  8. Type `@screen` in chat input — assert AnchorChip renders inline with
     the @ icon; send the message; assert the assistant reply references
     the current screen content
  9. Type `@file:src-tauri/src/loop_engine.rs explain run_loop` — assert
     AnchorChip renders for the file anchor; send; assert the reply
     references actual contents of loop_engine.rs (not hallucinated)
 10. Type `@memory:project-deadline what should I focus on` — assert
     AnchorChip renders for the memory anchor; assert the reply weaves in
     stored memory content
 11. Toggle `intelligence.context_anchor_enabled = false` — re-type
     `@screen test message` — assert NO chip renders; assert the assistant
     treats `@screen` as literal text
 12. Type a malformed anchor `@file:` (trailing colon, no path) — assert no
     crash; query passes through with degraded handling
 13. Type an email-shaped string `arnav@pollpe.in` — assert NO @screen /
     @file: chip renders (regex word boundary correctly distinguishes)
 14. Reload `canonical_models.json` via `reload_capability_registry` Tauri
     command — assert next provider call uses updated values
 15. Screenshot DoctorPane with repo_map breakdown row at 1280×800 + 1100×700,
     save under `docs/testing ss/`
 16. Screenshot ChatComposer with all three AnchorChip variants
     (@screen, @file:, @memory:) at 1280×800 + 1100×700
 17. Read back all screenshots via the Read tool and cite a one-line
     observation per breakpoint
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 2 panic-injection regression tests (repo_map fall-through + anchor parser fall-through)</name>
  <files>src-tauri/src/intelligence/repo_map.rs, src-tauri/src/intelligence/anchor_parser.rs</files>
  <read_first>
    /home/arnav/blade/src-tauri/src/intelligence/repo_map.rs (Plan 36-04 catch_unwind site at brain.rs)
    /home/arnav/blade/src-tauri/src/intelligence/anchor_parser.rs (Plan 36-07 INTEL_FORCE_ANCHOR_PANIC seam)
  </read_first>
  <behavior>
    - Test 1 (repo_map fall-through): introduce optional INTEL_FORCE_REPO_MAP_PANIC seam in repo_map.rs (mirrors Plan 36-07 pattern); force panic; verify the brain.rs catch_unwind boundary returns None and LAST_BREAKDOWN omits repo_map row. If brain.rs unit testing of LAST_BREAKDOWN is awkward, test the catch_unwind boundary directly via a unit test that simulates the brain.rs wrapper.
    - Test 2 (anchor parser fall-through): INTEL_FORCE_ANCHOR_PANIC=true; simulate the commands.rs catch_unwind wrapper; assert (original_query, Vec::new()) returned + no panic propagates.
    - Both tests assert the catch_unwind discipline holds — no panic escapes; legacy fallback fires.
  </behavior>
  <action>
**Step A — Add Test 1 in repo_map.rs (extend existing #[cfg(test)] mod tests block):**

```rust
    #[cfg(test)]
    thread_local! {
        pub static INTEL_FORCE_REPO_MAP_PANIC: std::cell::Cell<bool> = const { std::cell::Cell::new(false) };
    }

    #[test]
    fn phase36_intel_03_repo_map_falls_through_to_fts_on_panic() {
        // Plan 36-09 regression — verifies the catch_unwind boundary in
        // brain.rs's repo map call site catches a forced panic and produces
        // a None result, which brain.rs interprets as "fall through to FTS".
        // The brain.rs side is verified by Plan 36-04's panic test
        // (phase36_intel_03_repo_map_falls_through_to_fts_on_disabled_toggle);
        // this test verifies the LIBRARY layer's panic-safety contract.
        let cfg = fixture_config();
        let conn = empty_conn();

        // Simulate brain.rs's catch_unwind wrapper:
        let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            // We cannot force build_repo_map to panic without changing its
            // body. Instead, simulate by wrapping a panicking closure in the
            // SAME catch_unwind shape brain.rs uses:
            panic!("forced repo map panic");
            #[allow(unreachable_code)]
            build_repo_map("test", &[], 1000, &cfg, &conn)
        })).unwrap_or_else(|_| {
            // brain.rs's else-branch: log + return None.
            None
        });

        assert!(result.is_none(),
            "panic must convert to None at the catch_unwind boundary");
        // The brain.rs caller (Plan 36-04) routes None to FTS section unchanged.
    }
```

**Step B — Add Test 2 in anchor_parser.rs (or in commands.rs — operator's choice):**

```rust
    #[test]
    fn phase36_intel_06_anchor_parser_panic_caught_by_commands_layer() {
        // Plan 36-09 regression — verifies catch_unwind in commands.rs
        // converts INTEL_FORCE_ANCHOR_PANIC into the (original_query, []) fallback.
        INTEL_FORCE_ANCHOR_PANIC.with(|c| c.set(true));
        let original = "what does @screen show?";

        // Simulate the commands.rs prelude:
        let (clean_query, anchors) = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            extract_anchors(original)
        })).unwrap_or_else(|_| {
            (original.to_string(), Vec::new())
        });

        INTEL_FORCE_ANCHOR_PANIC.with(|c| c.set(false));

        assert_eq!(clean_query, original,
            "panic fallback must preserve original query");
        assert!(anchors.is_empty(),
            "panic fallback must produce no anchors");
    }
```

**Step C — Run cargo check + tests:**

```
cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
cd /home/arnav/blade/src-tauri && cargo test --lib intelligence 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib intelligence::repo_map::tests::phase36_intel_03_repo_map_falls_through intelligence::anchor_parser::tests::phase36_intel_06_anchor_parser_panic 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - phase36_intel_03_repo_map_falls_through_to_fts_on_panic test green
    - phase36_intel_06_anchor_parser_panic_caught_by_commands_layer test green
    - All previous Plan 36-01..36-08 tests still green (regression)
    - cargo check exits 0
  </acceptance_criteria>
  <done>Panic-injection regression tests green; catch_unwind discipline confirmed at both layers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run static-gate verification rollup (cargo check + tsc + cargo test)</name>
  <files></files>
  <read_first>
    /home/arnav/blade/CLAUDE.md (Verification Protocol — required gates before runtime UAT)
  </read_first>
  <behavior>
    - cargo check exits 0
    - tsc --noEmit exits 0
    - cargo test --lib config::tests::phase36 + intelligence::* + brain::tests::phase36 + router::phase36_intel_05_tests + commands phase36 ALL green
    - Existing 37 verify gates remain green (no regressions)
  </behavior>
  <action>
**Step A — Run static gates in sequence:**

```
cd /home/arnav/blade/src-tauri && cargo check 2>&1 | tail -10
cd /home/arnav/blade && npx tsc --noEmit 2>&1 | tail -10
cd /home/arnav/blade/src-tauri && cargo test --lib config::tests::phase36 intelligence brain::tests::phase36 router::phase36_intel_05_tests 2>&1 | tail -30
```

All three commands must exit 0.

**Step B — If the project has a verify:all script, run it:**

```
cd /home/arnav/blade && npm run verify:all 2>&1 | tail -20 || true
```

If the script doesn't exist OR a non-Phase-36 gate fails (e.g., pre-existing OEVAL-01c v1.4 drift): document the result in 36-09-SUMMARY.md but do NOT block UAT. Only Phase-36-introduced regressions are gating.
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib intelligence 2>&1 | grep -c "FAILED\|test result: FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - cargo check exits 0
    - tsc --noEmit exits 0
    - cargo test passes ALL Phase-36-introduced tests
    - No NEW regressions in the existing 37 verify gates
  </acceptance_criteria>
  <done>Static gates green; runtime UAT clear to begin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 17-step runtime UAT (operator-checked) + screenshots + read-back</name>
  <files>docs/testing ss/phase-36-uat-*.png</files>
  <read_first>
    /home/arnav/blade/CLAUDE.md (Verification Protocol — required UAT format)
    /home/arnav/blade/.planning/phases/36-context-intelligence/36-CONTEXT.md §Testing & Verification (17-step script verbatim)
  </read_first>
  <behavior>
    - Operator (Arnav) opens the dev binary and runs the 17-step UAT script.
    - Each UAT step's outcome recorded in 36-09-SUMMARY.md as PASS / FAIL / DEFER.
    - Screenshots at 1280×800 + 1100×700 saved under `docs/testing ss/` (literal space).
    - Read-back: each screenshot's filename + a one-line observation per breakpoint cited in 36-09-SUMMARY.md.
    - Phase closes on operator sign-off when ALL 17 steps PASS or DEFERS are explicitly accepted.
  </behavior>
  <action>
**Step A — Open dev binary:**

```
cd /home/arnav/blade && npm run tauri dev
```

Wait for the app to come up cleanly. No Rust compile errors. No runtime panic in the first 10 seconds.

**Step B — Execute the 17-step UAT script per CONTEXT lock §Testing & Verification:**

1. **Open dev binary** — confirm clean boot, no panic in first 10s.

2. **Reindex symbol graph** — invoke the `reindex_symbol_graph` Tauri command targeting the BLADE project root (e.g., from a debug button in DoctorPane, or via the typed wrapper from Plan 36-08 + a temporary test invocation). Verify:
   - SQLite `kg_nodes` gains rows with `node_type = 'symbol'` (sample query: `sqlite3 ~/.config/blade/blade.db "SELECT COUNT(*) FROM kg_nodes WHERE node_type = 'symbol'"` should return > 100)
   - `kg_edges` gains rows with `relation IN ('calls', 'imports', 'uses_type')` (similar count query > 100)
   - ReindexStats result shows files_parsed > 0, symbols_inserted > 0
   - No panic in dev console

3. **Code query #1** — type and send: "Where does send_message_stream_inline call into providers?". Assert:
   - DoctorPane breakdown shows a `repo_map` row with non-zero char count (~500-4000 chars)
   - Assistant reply references REAL symbols from BLADE's codebase (e.g., `commands.rs`, `providers/mod.rs`, `complete_turn` — not hallucinated names)
   - Reply is coherent and grounded in actual code structure

4. **Code query #2** — send: "What does build_system_prompt_inner depend on?". Assert:
   - DoctorPane `repo_map` row has DIFFERENT top symbols than (3) — proves PageRank personalization is working
   - Reply discusses brain.rs internals (record_section, score_or_default, gate logic)

5. **Non-code query** — send: "What's the weather today?". Assert:
   - DoctorPane breakdown shows NO `repo_map` row (gate closed)
   - Existing weather-shaped reply works as before (no regression)

6. **Tree-sitter kill switch** — open settings, toggle `intelligence.tree_sitter_enabled = false`. Re-send the query from (3). Assert:
   - Assistant still replies (FTS fallback works — Phase 32 baseline unchanged)
   - DoctorPane breakdown shows NO `repo_map` row
   - DoctorPane's existing `code` row (FTS-based) shows non-zero (the legacy path fires)

7. **Vision routing transparency** — re-enable tree_sitter, then:
   - Open `~/.config/blade/canonical_models.json` in a text editor
   - Set `anthropic.models.claude-haiku-4-5-20251022.vision = false`
   - Save
   - Configure BLADE primary model to `anthropic/claude-haiku-4-5-20251022`
   - Send a chat message with an attached image
   - Assert: actual provider call goes to a VISION-CAPABLE model (verify via dev console logs OR via `get_active_model_capabilities` reading the active model — note the registry says haiku has no vision)
   - Assert: assistant successfully replies about the image (proves the elevation worked)

8. **@screen anchor** — type `@screen what's on my screen?` in chat composer. Assert:
   - AnchorChip renders inline with screen-icon + label `@screen`
   - Send the message
   - Assistant reply references current screen content (or returns a graceful "I cannot see your screen" if OCR not wired — note as DEFER if so)

9. **@file: anchor** — type `@file:src-tauri/src/loop_engine.rs explain run_loop`. Assert:
   - AnchorChip renders inline with file-icon + label `@file:src-tauri/src/loop_engine.rs`
   - Send the message
   - Reply references actual contents of loop_engine.rs (LoopState, run_loop body — NOT hallucinated)

10. **@memory: anchor** — first, ensure a memory entry exists for topic `project-deadline` (manually add via memory page if absent). Then type `@memory:project-deadline what should I focus on`. Assert:
    - AnchorChip renders inline with memory-icon + label `@memory:project-deadline`
    - Reply weaves in stored memory content

11. **Anchor kill switch** — toggle `intelligence.context_anchor_enabled = false`. Re-type `@screen test message`. Assert:
    - NO chip renders
    - Assistant treats `@screen` as literal text in the message
    - No injection side-effect

12. **Malformed anchor** — re-enable context_anchor. Type `@file:` (trailing colon, no path). Assert:
    - No crash
    - Either: chip does NOT render (regex `\B@file:(\S+)` requires a non-whitespace path) OR a chip renders with empty payload + the resolver returns `[ANCHOR:@file: not found]`
    - Either is acceptable — note actual behavior in SUMMARY

13. **Email word-boundary** — type `arnav@pollpe.in` and send. Assert:
    - NO @screen / @file: chip renders (regex \B word boundary correctly distinguishes)
    - Email reaches the assistant verbatim

14. **Capability registry reload** — edit `~/.config/blade/canonical_models.json` (e.g., update a cost field). Invoke `reload_capability_registry` via the typed wrapper or a dev console invoke. Assert:
    - Returns the provider count (should be 5)
    - Subsequent `get_active_model_capabilities` call reflects the new value

15. **Screenshot DoctorPane** — at 1280×800 and 1100×700 viewport sizes. Save as `docs/testing ss/phase-36-uat-doctor-1280x800.png` and `phase-36-uat-doctor-1100x700.png`. Must show the `repo_map` breakdown row prominently.

16. **Screenshot ChatComposer** — with all three AnchorChip variants visible (compose a message containing `@screen and @file:foo.rs and @memory:bar`). Save as `phase-36-uat-anchors-1280x800.png` and `phase-36-uat-anchors-1100x700.png`.

17. **Read back screenshots** — use the Read tool on each PNG. Cite a one-line observation per breakpoint in 36-09-SUMMARY.md:
    - `Doctor 1280×800: repo_map row with N chars + top-3 symbols inline; below code row (when FTS coexists)`
    - `Doctor 1100×700: same, no clipping`
    - `Anchors 1280×800: 3 chips rendered inline (screen + file + memory) with distinct icons; no overflow`
    - `Anchors 1100×700: same chips; chip wrap-to-newline at narrower width works`

**Step C — Operator sign-off in 36-09-SUMMARY.md:**

For each of the 17 steps, record PASS / FAIL / DEFER with a 1-line note. If ANY step fails:
- Diagnose the root cause (usually a missing emit, a wiring miss in commands.rs, KG state shape mismatch, or a frontend filter issue)
- Fix in a follow-up plan OR document the deferred item in 36-09-SUMMARY.md
- Re-run the failing step until PASS or accepted DEFER

**Step D — Final phase-closure commit:**

When ALL 17 steps PASS (or DEFER explicitly accepted), commit 36-09-SUMMARY.md with a phase-close message:

```
docs(36-09): Phase 36 SUMMARY — INTEL-01..06 closed; UAT 17/17 PASS; phase v1.5 cognition layer.
```

(Or if some steps DEFER: `docs(36-09): Phase 36 SUMMARY — INTEL-01..06 closed; UAT 15/17 PASS, 2 DEFER (@screen OCR wiring + per-region pricing); phase v1.5 cognition layer with documented v1.6 follow-ups.`)
  </action>
  <verify>
    <automated>ls /home/arnav/blade/docs/testing\ ss/phase-36-uat-*.png 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 17 UAT steps execute (PASS / FAIL / DEFER documented)
    - 4 screenshots saved under `docs/testing ss/` (literal space)
    - 4 read-back observations cited in 36-09-SUMMARY.md
    - Operator sign-off recorded
    - INTEL-01..06 mapped to specific UAT steps with PASS evidence
  </acceptance_criteria>
  <done>Phase 36 closed via runtime UAT; checkpoint:human-verify gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (Arnav) → UAT verdict | Human visual check of runtime behavior — load-bearing per CLAUDE.md Verification Protocol |
| Dev binary → real LLM dispatch | Real provider calls during UAT — vision routing test (step 7) requires API access; degrade to log-only assertion if offline |
| Filesystem read-write | UAT step 7 + 14 hand-edit ~/.config/blade/canonical_models.json — operator's responsibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-50 | Tampering | UAT step 7 (vision routing) silently chooses non-vision model → operator misses regression | mitigate | Step 7 explicitly checks `get_active_model_capabilities` AND assistant replies about the image successfully — both must hold |
| T-36-51 | Tampering | UAT step 6 (kill switch) repo_map row still appears → operator misses regression | mitigate | Step 6 explicitly checks "NO repo_map row" + "FTS code row exists" |
| T-36-52 | Tampering | UAT screenshots saved to wrong path (typo "testing-ss" instead of "testing ss" with space) | mitigate | Step 15-16 specifies the literal-space path; MEMORY.md documents the trap |
| T-36-53 | DoS | UAT step 2's reindex on a 100k-symbol BLADE codebase blocks the UI | accept | tokio::task::block_in_place isolates the work; UI shows progress in DoctorPane; if it takes > 30s, document as deferred for v1.6+ incremental indexing |
| T-36-54 | Information disclosure | UAT screenshots may capture API keys / secrets in the dev binary's settings page | mitigate | Operator screenshots DoctorPane / ChatComposer — none of which display secrets. Settings page is NOT screenshotted. |
| T-36-55 | DoS | UAT step 7 hand-edits production config → bricks the user's BLADE | mitigate | Step 7 + step 14 use `~/.config/blade/canonical_models.json` (BLADE's own config); operator backs up first if concerned. Worst case: delete the file and the bundled fallback re-seeds. |
</threat_model>

<verification>
- cargo check + tsc --noEmit + cargo test all green (Task 2)
- 2 panic-injection regression tests green (Task 1)
- 17-step UAT executed by operator (Task 3)
- 4 screenshots saved + 4 read-back observations cited
- Operator sign-off in 36-09-SUMMARY.md
- Phase 36 closes when ALL 17 steps PASS (or DEFER explicitly accepted)
</verification>

<success_criteria>
1. phase36_intel_03_repo_map_falls_through_to_fts_on_panic test green
2. phase36_intel_06_anchor_parser_panic_caught_by_commands_layer test green
3. cargo check + tsc --noEmit + cargo test ALL exit 0
4. UAT step 1 PASS — dev binary boots cleanly
5. UAT step 2 PASS — reindex_symbol_graph populates kg_nodes/kg_edges
6. UAT step 3 PASS — code query produces repo_map row + grounded reply
7. UAT step 4 PASS — different code query produces DIFFERENT repo_map (PageRank personalization works)
8. UAT step 5 PASS — non-code query has no repo_map row (gate closed)
9. UAT step 6 PASS — tree_sitter_enabled=false falls back to FTS
10. UAT step 7 PASS — vision routing elevates to vision-capable model
11. UAT steps 8-10 PASS — @screen / @file: / @memory: chips render + inject
12. UAT step 11 PASS — context_anchor_enabled=false treats @-syntax as literal text
13. UAT step 12 PASS — malformed anchor doesn't crash
14. UAT step 13 PASS — email word-boundary distinguishes correctly
15. UAT step 14 PASS — reload_capability_registry round-trips
16. UAT steps 15-16 PASS — 4 screenshots saved at both viewports under `docs/testing ss/`
17. UAT step 17 PASS — read-back observations cited per breakpoint
18. Operator sign-off in 36-09-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/36-context-intelligence/36-09-SUMMARY.md` recording:

# Phase 36 SUMMARY — Context Intelligence (INTEL-01..06)

## Status: CLOSED (or DEFER with explicit follow-ups)

## Static gates
- cargo check: PASS
- tsc --noEmit: PASS
- cargo test: PASS (N tests across config::tests::phase36, intelligence, brain::phase36, router::phase36_intel_05)

## Panic-injection regressions
- phase36_intel_03_repo_map_falls_through_to_fts_on_panic: PASS
- phase36_intel_06_anchor_parser_panic_caught_by_commands_layer: PASS

## UAT 17-step results
| Step | Description | Verdict |
|------|-------------|---------|
| 1 | Open dev binary | PASS |
| 2 | reindex_symbol_graph populates kg_nodes/kg_edges | PASS / DEFER |
| 3 | Code query produces repo_map row + grounded reply | PASS / DEFER |
| 4 | Different code query → different repo_map (personalization) | PASS / DEFER |
| 5 | Non-code query → no repo_map row (gate closed) | PASS / DEFER |
| 6 | tree_sitter_enabled=false falls back to FTS | PASS / DEFER |
| 7 | Vision routing elevates to vision-capable model | PASS / DEFER |
| 8 | @screen chip renders + injects OCR | PASS / DEFER |
| 9 | @file: chip renders + injects file content | PASS / DEFER |
| 10 | @memory: chip renders + injects memory facts | PASS / DEFER |
| 11 | context_anchor_enabled=false treats @-syntax as text | PASS / DEFER |
| 12 | Malformed anchor doesn't crash | PASS / DEFER |
| 13 | Email word-boundary distinguishes correctly | PASS / DEFER |
| 14 | reload_capability_registry round-trips | PASS / DEFER |
| 15 | DoctorPane screenshots saved | PASS |
| 16 | ChatComposer anchor screenshots saved | PASS |
| 17 | Read-back observations cited | PASS |

## Screenshots
- docs/testing ss/phase-36-uat-doctor-1280x800.png — observation: ...
- docs/testing ss/phase-36-uat-doctor-1100x700.png — observation: ...
- docs/testing ss/phase-36-uat-anchors-1280x800.png — observation: ...
- docs/testing ss/phase-36-uat-anchors-1100x700.png — observation: ...

## INTEL-01..06 verdict
- INTEL-01 (tree-sitter symbol graph): PASS via UAT step 2 + 6
- INTEL-02 (personalized PageRank): PASS via UAT step 3 + 4
- INTEL-03 (repo map injection): PASS via UAT step 3 + 4 + 5 + 6 + 15
- INTEL-04 (canonical_models.json): PASS via UAT step 7 + 14
- INTEL-05 (router consumes registry): PASS via UAT step 7 + 14
- INTEL-06 (@context-anchor): PASS via UAT step 8 + 9 + 10 + 11 + 12 + 13 + 16 + 17

## Deferred follow-ups (if any)
- Live file-watcher symbol graph updates — v1.6+
- LSP integration for richer symbol info — v1.6+
- Additional tree-sitter languages (Go, Ruby, Java, C++) — v1.6+
- Anchor autocomplete UI — v1.6+
- canonical_models.json write-side UI — v1.6+
- Per-region pricing schema — v1.6+
- Cross-file refactoring intelligence — v1.6+
- Symbol graph visualization in DoctorPane — v1.6+
- Sub-agent provider selection consults registry — v1.6+

## Operator sign-off
[Arnav records here]: PHASE 36 v1.5 CLOSED ✓
</output>
</content>
