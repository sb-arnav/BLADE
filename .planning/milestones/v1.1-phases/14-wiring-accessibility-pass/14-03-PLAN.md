---
phase: 14-wiring-accessibility-pass
plan: "03"
type: execute
wave: 1
depends_on:
  - "14-01"
files_modified:
  - src/features/dashboard/Dashboard.tsx
  - src/features/dashboard/TentacleSignalsCard.tsx
  - src/features/dashboard/CalendarCard.tsx
  - src/features/dashboard/IntegrationsCard.tsx
  - src/lib/tauri/voice.ts
  - tests/e2e/phase14/dashboard-live-data.spec.ts
  - package.json
autonomous: true
requirements:
  - WIRE2-02
  - WIRE2-03

must_haves:
  truths:
    - "Dashboard shows no ComingSoonCard components when backing data is available"
    - "Hive signals card displays live tentacle list from ecosystemListTentacles()"
    - "Integrations card shows connected services count from ecosystemListTentacles() enabled filter"
    - "Calendar card shows live data from calendar_get_today or a graceful empty state when calendar tentacle is not enabled"
    - "Post-phase, no dashboard card shows placeholder text 'Tentacle reports + autonomy queue', 'Today's events + reminders', or 'Connected services + status'"
  artifacts:
    - path: "src/features/dashboard/TentacleSignalsCard.tsx"
      provides: "Live tentacle signal display using ecosystemListTentacles()"
      min_lines: 40
    - path: "src/features/dashboard/CalendarCard.tsx"
      provides: "calendar_get_today data or graceful empty state"
      min_lines: 30
    - path: "src/features/dashboard/IntegrationsCard.tsx"
      provides: "Connected services count from enabled tentacles"
      min_lines: 30
    - path: "src/features/dashboard/Dashboard.tsx"
      provides: "Updated composition — ComingSoonCard removed for 3 slots"
  key_links:
    - from: "src/features/dashboard/TentacleSignalsCard.tsx"
      to: "src/lib/tauri/ecosystem.ts"
      via: "ecosystemListTentacles() in useEffect"
      pattern: "ecosystemListTentacles"
    - from: "src/features/dashboard/CalendarCard.tsx"
      to: "src/lib/tauri/intelligence.ts OR src/lib/tauri/voice.ts"
      via: "invokeTyped('calendar_get_today')"
      pattern: "calendar_get_today"
---

<objective>
Replace the three ComingSoonCard placeholder components in Dashboard.tsx with
real data-backed components that use Phase 13's ecosystemListTentacles() and
the calendar_get_today command from calendar_tentacle.rs.

All three slots now have backing data: Phase 13 tentacles are the source for
"Hive signals" and "Integrations". Calendar requires wiring calendar_get_today
(NOT-WIRED) via a new TypeScript wrapper — but must gracefully handle the case
where the calendar tentacle is not enabled (sparse scan data or Phase 12 not
yet complete).

Purpose: WIRE2-02 — dashboard cards must not show placeholder text when real
data exists. WIRE2-03 — no dead UI survives v1.1. The three ComingSoonCard
instances are dead UI because Phase 12/13 data now exists.

Output: 3 new card components + Dashboard.tsx updated composition + 1 e2e spec
confirming no placeholder text on dashboard paint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-wiring-accessibility-pass/14-RESEARCH.md
@.planning/phases/13-self-configuring-ecosystem/13-02-SUMMARY.md

<interfaces>
<!-- From src/features/dashboard/Dashboard.tsx (full file is 54 lines — read it): -->
// Current: 3 ComingSoonCard instances with placeholder descriptions
// Target: replace each with a real card component

<!-- From src/lib/tauri/ecosystem.ts (already created in Phase 13): -->
ecosystemListTentacles(): Promise<TentacleRecord[]>
// TentacleRecord: { id: string; enabled: boolean; rationale: string; enabled_at: number; trigger_detail: string }

<!-- calendar_get_today command (from audit — src-tauri/src/tentacles/calendar_tentacle.rs): -->
// Command: calendar_get_today — no args — returns JSON with today's events
// If calendar tentacle is disabled: command returns empty array (safe)
// Wrapper goes in src/lib/tauri/intelligence.ts (already created in 14-02)
// OR create a separate calendar.ts — decision: add to intelligence.ts as calendarGetToday()

<!-- Dashboard grid layout (from Dashboard.tsx + dashboard.css — do not change grid): -->
// 3 × ComingSoonCard occupy col-span-4 slots in the dashboard-grid
// New cards must also use the same col-span-4 slot (apply same CSS class or grid placement)

<!-- Dashboard ComingSoonCard.tsx — read before removing: -->
// ComingSoonCard renders a GlassPanel tier={2} with title, phase badge, description
// New cards should use same GlassPanel tier={2} primitive
import { GlassPanel } from '@/design-system/primitives';

<!-- Empty state pattern (from A4 assumption — Phase 12 may not be complete): -->
// If ecosystemListTentacles() returns [] → show "No tentacles active yet. Run deep scan to auto-configure."
// If calendar_get_today returns [] → show "Calendar not connected. Enable calendar monitoring in Ecosystem settings."
// This is graceful degradation, not a placeholder — it has a CTA.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add calendarGetToday wrapper + build 3 live dashboard card components</name>
  <files>
    src/lib/tauri/intelligence.ts
    src/features/dashboard/TentacleSignalsCard.tsx
    src/features/dashboard/CalendarCard.tsx
    src/features/dashboard/IntegrationsCard.tsx
  </files>
  <read_first>
    - src/lib/tauri/intelligence.ts (from 14-02 — add calendarGetToday to it)
    - src/features/dashboard/ComingSoonCard.tsx (read to understand GlassPanel usage + styling)
    - src/features/settings/panes/EcosystemPane.tsx (pattern for ecosystemListTentacles() usage in useEffect)
  </read_first>
  <action>
In src/lib/tauri/intelligence.ts — append (do not overwrite, just add):
  - calendarGetToday(): Promise<CalendarEvent[]> — invokeTyped('calendar_get_today')
  - Export type CalendarEvent = { id: string; title: string; start: string; end: string; location?: string }

Create src/features/dashboard/TentacleSignalsCard.tsx:
  - On mount: call ecosystemListTentacles() → store in state
  - Render in a GlassPanel tier={2} with class "dash-card" (same as ComingSoonCard slot):
    - Heading: "Hive Signals"
    - When loading: show a single-line skeleton (two gray divs)
    - When tentacles.length === 0: "No tentacles active yet. Run deep scan to auto-configure." with a link (button style) that opens settings-ecosystem route via useRouterCtx openRoute
    - When tentacles.length > 0: list up to 5 enabled tentacles as rows — each row: colored dot (green if enabled, gray if not) + tentacle id + rationale truncated to 60 chars
    - Footer chip: "{enabled count}/{total count} active"
  - Keyboard: card heading is non-interactive; list items are display-only (no interactive elements beyond the empty-state CTA)
  - aria-label on the GlassPanel: "Hive signals — {count} active tentacles"

Create src/features/dashboard/CalendarCard.tsx:
  - On mount: call calendarGetToday() → store in state; catch errors silently (calendar tentacle may be disabled)
  - Render in GlassPanel tier={2} with class "dash-card":
    - Heading: "Calendar"
    - When loading: skeleton row
    - When events.length === 0: "Calendar not connected. Enable monitoring in Ecosystem settings." with openRoute CTA to settings-ecosystem
    - When events.length > 0: list up to 4 events — each row: time range (HH:MM – HH:MM) + event title truncated to 50 chars
  - aria-label on card: "Calendar — {count} events today"

Create src/features/dashboard/IntegrationsCard.tsx:
  - On mount: call ecosystemListTentacles() → filter to enabled only
  - Render in GlassPanel tier={2} with class "dash-card":
    - Heading: "Integrations"
    - When loading: skeleton
    - When enabledTentacles.length === 0: "No integrations active. Run deep scan to connect services." + openRoute CTA
    - When enabledTentacles.length > 0: list enabled tentacle IDs as service chips (short badge labels like "github", "vercel", "slack") — derive from tentacle id
    - Footer: "{count} connected"
  - aria-label on card: "Integrations — {count} connected"

Accessibility for all 3 cards:
  - GlassPanel must have role="region" and aria-label set
  - Empty-state CTAs: <button> element (not <a>) with aria-label describing action
  - No icon-only buttons
  - No unconditional CSS transitions (if loading skeleton animates, gate under prefers-reduced-motion)
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep "calendarGetToday" /home/arnav/blade/src/lib/tauri/intelligence.ts && grep "ecosystemListTentacles" /home/arnav/blade/src/features/dashboard/TentacleSignalsCard.tsx && grep "calendarGetToday" /home/arnav/blade/src/features/dashboard/CalendarCard.tsx && grep "ecosystemListTentacles" /home/arnav/blade/src/features/dashboard/IntegrationsCard.tsx</automated>
  </verify>
  <done>
    - calendarGetToday wrapper added to intelligence.ts
    - 3 new card components exist with live data fetching in useEffect
    - Each card has graceful empty state with CTA (not dead placeholder text)
    - aria-label on each card's container
    - npx tsc --noEmit passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Dashboard.tsx + e2e spec + package.json test script</name>
  <files>
    src/features/dashboard/Dashboard.tsx
    tests/e2e/phase14/dashboard-live-data.spec.ts
    package.json
  </files>
  <read_first>
    - src/features/dashboard/Dashboard.tsx (54 lines — full file already in context)
    - tests/e2e/settings-ecosystem-tentacles.spec.ts (pattern reference for Playwright specs in this project)
    - package.json lines 36-44 (test:e2e:phase13 pattern to mirror for phase14)
  </read_first>
  <action>
In src/features/dashboard/Dashboard.tsx:
  - Remove import of ComingSoonCard
  - Import TentacleSignalsCard from './TentacleSignalsCard'
  - Import CalendarCard from './CalendarCard'
  - Import IntegrationsCard from './IntegrationsCard'
  - Replace the three ComingSoonCard JSX instances with the three new cards
  - Keep all other content (RightNowHero, AmbientStrip, grid structure, CSS classes) EXACTLY as-is
  - Do NOT modify the grid layout (D-76 12-column grid stays)

Create tests/e2e/phase14/dashboard-live-data.spec.ts:
  - 3 specs:
    1. "dashboard renders without ComingSoonCard placeholder text" — navigate to dashboard, assert text "Tentacle reports + autonomy queue" is NOT present, assert text "Connected services + status" is NOT present
    2. "dashboard hive signals card is visible with heading" — assert data-testid or heading "Hive Signals" is visible
    3. "dashboard integrations card is visible with heading" — assert "Integrations" heading is visible
  - Use data-dashboard-surface attribute (from Dashboard.tsx) as container selector
  - Mirror test structure from tests/e2e/settings-ecosystem-tentacles.spec.ts

In package.json:
  - Add "test:e2e:phase14": "playwright test tests/e2e/phase14/" to scripts (after test:e2e:phase13)
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "ComingSoonCard" /home/arnav/blade/src/features/dashboard/Dashboard.tsx && grep "TentacleSignalsCard\|CalendarCard\|IntegrationsCard" /home/arnav/blade/src/features/dashboard/Dashboard.tsx && grep "test:e2e:phase14" /home/arnav/blade/package.json && node -e "require('/home/arnav/blade/tests/e2e/phase14/dashboard-live-data.spec.ts')" 2>&1 | head -3 || true</automated>
  </verify>
  <done>
    - Dashboard.tsx has 0 ComingSoonCard references
    - Dashboard.tsx imports and renders TentacleSignalsCard, CalendarCard, IntegrationsCard
    - tests/e2e/phase14/dashboard-live-data.spec.ts exists with 3 specs
    - test:e2e:phase14 script added to package.json
    - npx tsc --noEmit passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar_get_today → CalendarCard | Calendar event data from Rust crosses IPC into DOM rendering |
| ecosystemListTentacles → card components | Tentacle rationale strings from config.rs rendered in DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Info Disclosure | CalendarCard renders event titles | accept | Calendar events are user's own data rendered locally; no external transmission; titles truncated to 50 chars for layout |
| T-14-03-02 | Tampering | TentacleSignalsCard renders rationale from config | accept | Rationale strings are set by ecosystem.rs probes (filesystem checks, env vars) not by external input; already bounded by safe_slice(200) in emit_activity |
| T-14-03-03 | Denial of Service | calendarGetToday fails if tentacle disabled | mitigate | Errors caught silently; empty state renders instead of crash; no retry loop |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — zero errors
2. `grep -c "ComingSoonCard" src/features/dashboard/Dashboard.tsx` — output must be 0
3. `grep "TentacleSignalsCard" src/features/dashboard/Dashboard.tsx` — match found
4. `grep "calendarGetToday" src/lib/tauri/intelligence.ts` — match found
5. `grep "test:e2e:phase14" package.json` — match found
6. `grep "Tentacle reports + autonomy queue" src/features/dashboard/*.tsx` — NO match (placeholder removed)
</verification>

<success_criteria>
- Dashboard.tsx has zero ComingSoonCard instances
- 3 live data cards replace the 3 placeholder slots
- Each new card has graceful empty state (not blank, not placeholder — has CTA copy)
- e2e spec asserts placeholder text absence
- npx tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-wiring-accessibility-pass/14-03-SUMMARY.md`
</output>
