---
phase: 11-smart-provider-setup
plan: 06
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
  - 04
  - 05
files_modified:
  - scripts/verify-providers-capability.mjs
  - package.json
  - .planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
  - PROV-05
  - PROV-06
  - PROV-07
  - PROV-08
  - PROV-09
tags:
  - phase-11
  - verify-gate
  - integration
  - wave-2
must_haves:
  truths:
    - "scripts/verify-providers-capability.mjs exists and performs 4 structural checks: (1) CAPABILITY_SURFACES has >= 2 entries per capability, (2) >= 1 <CapabilityGap capability=X> usage per capability in src/, (3) all 5 new BladeConfig fields appear in >= 6 places in config.rs, (4) BLADE_EVENTS.ROUTING_CAPABILITY_MISSING exists"
    - "package.json exposes verify:providers-capability + test:e2e:phase11 scripts"
    - "verify:all chain is extended with && npm run verify:providers-capability (gate 20)"
    - "Manual trace document at .planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md captures the goal-backward walkthrough for all 5 ROADMAP success criteria"
    - "npm run verify:providers-capability exits 0 when all gates pass; exits non-zero on structural drift"
  artifacts:
    - path: "scripts/verify-providers-capability.mjs"
      provides: "Phase 11 integrity gate. Node ESM script. Checks CAPABILITY_SURFACES entry count, <CapabilityGap> usage count per capability, 6-place config pattern for 5 new fields, BLADE_EVENTS registry entry. Supports --self-test flag."
      min_lines: 180
    - path: "package.json"
      provides: "Script entries verify:providers-capability + test:e2e:phase11; chain extension to verify:all"
      contains: "verify:providers-capability"
    - path: ".planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md"
      provides: "Human-readable goal-backward trace mapping each of the 5 ROADMAP success criteria to the files + tests + manual verification steps"
      min_lines: 80
  key_links:
    - from: "package.json verify:all chain"
      to: "scripts/verify-providers-capability.mjs"
      via: "&& npm run verify:providers-capability append"
      pattern: "&& npm run verify:providers-capability"
    - from: "package.json test:e2e:phase11"
      to: "tests/e2e/{onboarding-paste-card,settings-providers-pane,fallback-order-drag,capability-gap-*}.spec.ts"
      via: "Playwright test invocation"
      pattern: "test:e2e:phase11"
    - from: "verify-providers-capability.mjs check #3"
      to: "src-tauri/src/config.rs 6-place pattern"
      via: "grep-count assertion >= 6 per new field"
      pattern: "provider_capabilities"
---

<objective>
Ship the Phase 11 integration substrate: (1) a new Node verify gate `scripts/verify-providers-capability.mjs` that structurally validates Phase 11's four falsifiable invariants (capability surfaces, CapabilityGap usage, 6-place config pattern, event registry), (2) `package.json` script entries + chain extension into `verify:all` (becomes gate 20), (3) a manual-trace document mapping each of the 5 ROADMAP success criteria to the Phase 11 artifacts that close it.

Purpose: Lock Phase 11's acceptance surface so any future regression (a deleted surface, a missing CapabilityGap wire, a config field that drifts below 6 places) fails the verify gate at commit time. Provide a goal-backward trace for the `/gsd-verify-work` pass and future auditors.

Output: 2 new files + 1 modified file. `npm run verify:providers-capability` exits 0 on the current Wave 0 + Wave 1 substrate; `npm run verify:all` (extended chain) exits 0; `npm run test:e2e:phase11` runs all 11 Phase 11 Playwright specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-smart-provider-setup/11-CONTEXT.md
@.planning/phases/11-smart-provider-setup/11-RESEARCH.md
@.planning/phases/11-smart-provider-setup/11-PATTERNS.md
@.planning/phases/11-smart-provider-setup/11-VALIDATION.md
@.planning/phases/11-smart-provider-setup/11-01-PLAN.md
@.planning/phases/11-smart-provider-setup/11-02-PLAN.md
@.planning/phases/11-smart-provider-setup/11-03-PLAN.md
@.planning/phases/11-smart-provider-setup/11-04-PLAN.md
@.planning/phases/11-smart-provider-setup/11-05-PLAN.md
@.planning/ROADMAP.md

<interfaces>
<!-- Gate check contracts (PATTERNS.md §18 — all four checks verbatim) -->

Check 1 — CAPABILITY_SURFACES has >= 2 entries per capability:
- Read file `src/features/providers/CAPABILITY_SURFACES.ts` (produced by Plan 11-05).
- For each capability key in ['vision', 'audio', 'long_context', 'tools']: use regex `{cap}:\\s*\\[([\\s\\S]*?)\\]` to capture the array body, then count matches of `route:\\s*['"]` inside.
- Fail if any capability array contains fewer than 2 entries.

Check 2 — >= 1 `<CapabilityGap capability="X">` per capability in src/:
- Walk `src/**/*.{tsx,ts}` excluding `CapabilityGap.tsx` itself and `CAPABILITY_SURFACES.ts`.
- Regex `<CapabilityGap\\s+capability=["'](\\w+)["']` — collect matched capability names in a Set.
- Fail if any of ['vision', 'audio', 'long_context', 'tools'] is not in the Set.

Check 3 — 5 new BladeConfig fields appear in >= 6 places in config.rs:
- NEW_FIELDS = ['vision_provider', 'audio_provider', 'long_context_provider', 'tools_provider', 'provider_capabilities']
- Read `src-tauri/src/config.rs`; for each field, count occurrences of `\\b{field}\\b`.
- Fail if any field occurs fewer than 6 times.

Check 4 — BLADE_EVENTS.ROUTING_CAPABILITY_MISSING exists + has event literal:
- Read `src/lib/events/index.ts`.
- Assert substring `ROUTING_CAPABILITY_MISSING` present.
- Assert substring `blade_routing_capability_missing` present (in single or double quotes).

Analog for Node ESM script shape (scripts/verify-wiring-audit-shape.mjs):
- Uses `import { readFileSync, readdirSync, statSync } from 'node:fs'`
- Uses `process.exit(1)` on failure
- Uses `console.log` with visual prefixes (check / cross)
- Supports `--self-test` flag

Soft-skip pattern (PATTERNS.md §18 soft-skip):
- If `src/features/providers/CAPABILITY_SURFACES.ts` doesn't exist yet, log a WARN and skip check 1 + 2 (useful for partial-landing Wave 0 state). The gate still asserts config.rs (check 3) which lands in Plan 11-02 before Wave 1.

5 ROADMAP success criteria to trace (ROADMAP.md §Phase 11):
1. Paste 3 representative cURL auto-extracts -> covered by Plan 11-01 parser tests + 11-03 onboarding e2e
2. JSON + Python-SDK same auto-fill -> covered by Plan 11-01 parser tests
3. Key save triggers probe; result persists in provider row -> covered by Plan 11-02 probe + 11-03 settings pane e2e
4. Vision-needing surface shows CapabilityGap + CTA opens provider add flow -> covered by Plan 11-05 + 8 e2e specs
5. router.rs consults per-capability; vision task routes to vision_provider -> covered by Plan 11-04 unit tests + manual trace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/verify-providers-capability.mjs with 4 checks + --self-test flag</name>
  <files>scripts/verify-providers-capability.mjs</files>
  <read_first>
    - scripts/verify-wiring-audit-shape.mjs:1-50 (Node ESM skeleton + arg parsing)
    - scripts/verify-wiring-audit-shape.mjs:118-165 (walk + check helper shape)
    - scripts/verify-wiring-audit-shape.mjs:268-298 (checkConfig helper — 6-place pattern analog)
    - scripts/verify-wiring-audit-shape.mjs:346-361 (self-test flag block)
    - scripts/verify-wiring-audit-shape.mjs:367-370 (soft-skip when a target file doesn't exist)
    - scripts/verify-empty-state-coverage.sh:22-98 (file-walk + grep union pattern; alternate analog for check #2)
    - .planning/phases/11-smart-provider-setup/11-PATTERNS.md §18 (complete verify script spec)
    - .planning/phases/11-smart-provider-setup/11-VALIDATION.md Wave 0 requirements §scripts/verify-providers-capability.mjs (required checks list)
    - package.json:8-35 (find verify:wiring-audit-shape entry — analog for new script entry + chain extension)
    - src/features/providers/CAPABILITY_SURFACES.ts (produced by Plan 11-05 — may be absent at time of this plan if plans run out of order; handle gracefully)
    - src-tauri/src/config.rs (for check #3 — 5 new fields should grep-count >= 6 each after Plan 11-02)
    - src/lib/events/index.ts (for check #4 — ROUTING_CAPABILITY_MISSING added by Plan 11-04)
  </read_first>
  <action>
    Create `scripts/verify-providers-capability.mjs` as Node ESM. The script MUST:

    (1) Use only Node built-ins: `node:fs`, `node:path`, `node:url`. No shell invocations, no child processes, no network calls.

    (2) Define these constants at the top of the file:
    ```js
    const CAPABILITIES = ['vision', 'audio', 'long_context', 'tools'];
    const NEW_FIELDS = [
      'vision_provider', 'audio_provider', 'long_context_provider',
      'tools_provider', 'provider_capabilities',
    ];
    ```

    (3) Resolve `ROOT = join(fileURLToPath(import.meta.url), '..', '..')`.

    (4) Parse `--self-test` from process.argv. In self-test mode, log a brief message and exit 0 without running checks (placeholder for future fixture-based self-verification).

    (5) Accumulate results into two arrays: `failed[]` (hard failures) and `warnings[]` (soft-skip info).

    Check 1 — CAPABILITY_SURFACES entry count:
    - Let `surfacesPath = join(ROOT, 'src/features/providers/CAPABILITY_SURFACES.ts')`.
    - If `!existsSync(surfacesPath)`: push warning, skip check 1 AND check 2.
    - Else, read the file. For each cap in CAPABILITIES:
      - Capture block: `src.match(new RegExp(\`${cap}:\\\\s*\\\\[([\\\\s\\\\S]*?)\\\\]\`))?.[1] ?? ''`
      - Count entries: `(block.match(/route:\\s*['"]/g) || []).length`
      - If count < 2: push `check #1: CAPABILITY_SURFACES.${cap} has ${count}/2 entries` to failed.
      - Else: log success with count.

    Check 2 — CapabilityGap usage per capability (only runs if check 1 did):
    - Walk `src/` recursively. Filter to `.tsx` and `.ts` files. Skip `CapabilityGap.tsx` (component definition doesn't count as a consumer) and `CAPABILITY_SURFACES.ts`.
    - Regex scan each file for `<CapabilityGap\\s+capability=["'](\\w+)["']` — collect matched capability names into a Set.
    - For each cap in CAPABILITIES: if not in the Set, push `check #2: no <CapabilityGap capability="${cap}"> in src/` to failed.

    Check 3 — BladeConfig 5-field 6-place:
    - Let `configPath = join(ROOT, 'src-tauri/src/config.rs')`.
    - If `!existsSync(configPath)`: push warning, skip.
    - Else, read the file. For each field in NEW_FIELDS:
      - Count occurrences: `(src.match(new RegExp(\`\\\\b${field}\\\\b\`, 'g')) || []).length`
      - If count < 6: push `check #3: config.rs field ${field} occurs ${count}/6 places` to failed.
      - Else: log success.

    Check 4 — BLADE_EVENTS registry:
    - Let `eventsPath = join(ROOT, 'src/lib/events/index.ts')`.
    - If `!existsSync(eventsPath)`: push warning, skip.
    - Else, read the file. Assert substring `ROUTING_CAPABILITY_MISSING` present AND substring `blade_routing_capability_missing` present (either quote style). Push failure messages per missing piece.

    Summary + exit:
    - If warnings.length > 0: print them under a "Warnings" header.
    - If failed.length > 0: print each failure under a "Failures" header; `process.exit(1)`.
    - Else: print "verify:providers-capability PASSED"; `process.exit(0)`.

    CRITICAL:
    - ESM only. File extension `.mjs` signals ESM to Node.
    - NO shell invocations, NO `child_process`, NO network I/O. Pure read-only filesystem scan.
    - `process.exit(1)` on failure; `process.exit(0)` on pass.
    - Support `--self-test` (no-op in initial version; still exits 0 — matches wiring-audit-shape shape at L346-361).
    - Directory walk MUST skip: `node_modules/`, `src.bak/`, `dist/`, `dist-electron/`, `.planning/`, `tests/`. Only walk `src/` for check 2.
    - DO NOT modify package.json in this task — Task 2 handles that.
    - Output lines are for developer consumption; keep them short. Use ASCII prefixes only.
  </action>
  <verify>
    <automated>cd /home/arnav/blade && node scripts/verify-providers-capability.mjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/verify-providers-capability.mjs` + line count ≥ 180
    - Grep `CAPABILITY_SURFACES` in scripts/verify-providers-capability.mjs returns ≥ 1 match
    - Grep `<CapabilityGap` in scripts/verify-providers-capability.mjs returns ≥ 1 match
    - Grep `NEW_FIELDS` in scripts/verify-providers-capability.mjs returns ≥ 1 match
    - Grep `ROUTING_CAPABILITY_MISSING` in scripts/verify-providers-capability.mjs returns ≥ 1 match
    - Grep `process.exit` in scripts/verify-providers-capability.mjs returns ≥ 2 matches
    - Grep `--self-test` in scripts/verify-providers-capability.mjs returns ≥ 1 match
    - Grep `child_process|require\\(` returns 0 matches (no shell/CJS escape)
    - Script exits 0 when all 4 checks pass (after Plans 11-01..11-05 are landed)
    - Script exits 1 when any required assertion fails (sanity test: temporarily rename one field in config.rs and confirm exit 1)
  </acceptance_criteria>
  <done>
    verify-providers-capability.mjs runs all 4 checks. Exits 0 after Wave 1 substrate is in place; exits 1 on any structural drift. Pure ESM, no shell invocations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add package.json script entries + extend verify:all chain + write manual trace doc</name>
  <files>package.json, .planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md</files>
  <read_first>
    - package.json:8-35 (existing scripts block + verify:all chain — find exact line with `verify:wiring-audit-shape` and the chain end-point for the append)
    - scripts/verify-wiring-audit-shape.mjs (confirm script convention — it's the last gate in verify:all today)
    - .planning/ROADMAP.md §Phase 11 (5 success criteria verbatim — used as the trace matrix)
    - .planning/phases/11-smart-provider-setup/11-{01..05}-PLAN.md (frontmatter must_haves + requirements — used to map each success criterion to files)
    - .planning/phases/11-smart-provider-setup/11-VALIDATION.md §Manual-Only Verifications (mapping for success-criterion trace)
  </read_first>
  <action>
    (1) `package.json` — add 2 new scripts and extend verify:all chain.

    Find the `"scripts"` block (L8-30 approximately). Locate the existing `"verify:wiring-audit-shape"` entry. Add AFTER it:

    ```json
    "verify:providers-capability": "node scripts/verify-providers-capability.mjs",
    "test:e2e:phase11": "playwright test tests/e2e/onboarding-paste-card.spec.ts tests/e2e/settings-providers-pane.spec.ts tests/e2e/fallback-order-drag.spec.ts tests/e2e/capability-gap-vision-screen-timeline.spec.ts tests/e2e/capability-gap-vision-quickask.spec.ts tests/e2e/capability-gap-audio-voice-orb.spec.ts tests/e2e/capability-gap-audio-meeting-ghost.spec.ts tests/e2e/capability-gap-longctx-chat.spec.ts tests/e2e/capability-gap-longctx-knowledge.spec.ts tests/e2e/capability-gap-tools-swarm.spec.ts tests/e2e/capability-gap-tools-web-automation.spec.ts",
    ```

    Find the existing `"verify:all"` chain (the entry that chains multiple `&& npm run verify:*` together). Append to the end:

    ```
    ... && npm run verify:wiring-audit-shape && npm run verify:providers-capability
    ```

    (If verify:wiring-audit-shape is already the last entry, just append `&& npm run verify:providers-capability`. If it's chained earlier, append at the very end.)

    DO NOT modify any other package.json entries.

    (2) Create `.planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md`:

    ```markdown
    # Phase 11 — Goal-Backward Manual Trace

    **Purpose:** Map each of the 5 ROADMAP Phase 11 success criteria to the exact Phase 11 artifacts (files, tests, manual steps) that close them. Consumed by `/gsd-verify-work` and future auditors.

    **Source:** ROADMAP.md §Phase 11 §Success Criteria (locked).

    ---

    ## Criterion 1 — Paste 3 representative cURL snippets auto-extracts provider/model/base_url/headers

    **Artifacts that satisfy this:**
    - `src-tauri/src/provider_paste_parser.rs` — Plan 11-01 — cURL detector (`detect_curl`) implements the regex set from 11-RESEARCH.md §Paste Sample Corpus.
    - `provider_paste_parser::tests::test_curl_openai_single_line` + `test_curl_anthropic_multiline_x_api_key` + `test_curl_groq` — Plan 11-01 — Unit tests for OpenAI / Anthropic / Groq cURL samples (3 representative).
    - `tests/e2e/onboarding-paste-card.spec.ts` — Plan 11-03 — e2e proving paste -> parse -> advance flow for a cURL sample.

    **Automated verify:** `cd src-tauri && cargo test --lib provider_paste_parser::tests::test_curl_*` exits 0 with ≥ 3 passing.
    **Manual verify:** see 11-VALIDATION.md §Manual-Only Verifications row 1 (paste each of the 3 representative samples in dev build).

    ---

    ## Criterion 2 — Pasting a JSON config blob OR Python SDK snippet produces the same auto-fill behavior

    **Artifacts that satisfy this:**
    - `src-tauri/src/provider_paste_parser.rs` — `detect_json` + `detect_python_sdk` functions — Plan 11-01.
    - `provider_paste_parser::tests::test_json_*` (4 JSON tests) + `test_python_*` (5 Python tests) — Plan 11-01.

    **Automated verify:** `cd src-tauri && cargo test --lib provider_paste_parser::tests` — all 17+ tests pass.

    ---

    ## Criterion 3 — Key save triggers one test call; probe result persists; capability pill visible in provider row

    **Artifacts that satisfy this:**
    - `src-tauri/src/capability_probe.rs` — Plan 11-02 — idempotent probe wrapping test_connection + static matrix lookup.
    - `src-tauri/src/config.rs` — Plan 11-02 — 5 new fields in 6-place pattern (provider_capabilities HashMap + 4 Option<String>).
    - `capability_probe::tests::matrix_*` + `auto_populate_*` — Plan 11-02 — 9 unit tests.
    - `config::tests::phase11_fields_round_trip` — Plan 11-02 — round-trip preservation.
    - `src/features/providers/CapabilityPillStrip.tsx` — Plan 11-03 — renders pill strip from ProviderCapabilityRecord.
    - `tests/e2e/settings-providers-pane.spec.ts` — Plan 11-03 — e2e asserting pill strip rendered + re-probe invoked.

    **Automated verify:**
    - `cd src-tauri && cargo test --lib capability_probe::tests config::tests::phase11_fields_round_trip` exits 0
    - `npx playwright test tests/e2e/settings-providers-pane.spec.ts` exits 0
    - `npm run verify:providers-capability` confirms 5 new BladeConfig fields appear ≥ 6 times each in config.rs

    **Manual verify:** 11-VALIDATION.md Manual row 2 — live-probe a real API key, confirm pill strip reflects live response.

    ---

    ## Criterion 4 — Vision-needing UI surface shows CapabilityGap + CTA opens provider add flow — ≥ 2 surfaces; same for audio / long-context / tools

    **Artifacts that satisfy this:**
    - `src/features/providers/CapabilityGap.tsx` — Plan 11-05.
    - `src/features/providers/useCapability.ts` — Plan 11-05.
    - `src/features/providers/CAPABILITY_SURFACES.ts` — Plan 11-05 — ≥ 2 entries per capability.
    - Consumer surfaces (8 files in Plan 11-05 Task 2): ScreenTimeline.tsx, QuickAskView.tsx, VoiceOrbView.tsx, MeetingGhostView.tsx, useChat.tsx (or ChatView consumer), KnowledgeBase.tsx, SwarmView.tsx, WebAutomation.tsx.
    - `src/windows/main/useRouter.ts` — Plan 11-05 — extended openRoute(id, hint?) + routeHint state.
    - `src/features/settings/panes/ProvidersPane.tsx` — Plans 11-03 + 11-05 — routeHint scroll-focus effect.
    - 8 e2e specs `tests/e2e/capability-gap-*.spec.ts` — Plan 11-05.

    **Automated verify:**
    - `npm run verify:providers-capability` confirms (a) ≥ 2 surfaces per capability in CAPABILITY_SURFACES.ts, (b) ≥ 1 <CapabilityGap capability="X"> per capability in src/.
    - `npx playwright test tests/e2e/capability-gap-*.spec.ts` exits 0 with 8 passing.

    **Manual verify:** 11-VALIDATION.md Manual row 3 — cold install + only-Groq key, open Screen Timeline + QuickAsk image input, confirm CapabilityGap renders + CTA opens paste textarea focused.

    ---

    ## Criterion 5 — router.rs routing consults per-capability config; vision task routes to vision_provider with fallback chain (not silently to primary)

    **Artifacts that satisfy this:**
    - `src-tauri/src/router.rs::select_provider` — Plan 11-04 — 3-tier resolution (+ tier-0 base_url escape).
    - `build_capability_filtered_chain` + `build_generic_chain` helpers — Plan 11-04.
    - `router::tests::*` — Plan 11-04 — 7+ unit tests covering all tiers + chain filter + dedup + missing-event signal.
    - `src-tauri/src/commands.rs:~744` — Plan 11-04 — single call-site rewire; emits blade_routing_capability_missing via app.emit_to.
    - `src/lib/events/index.ts` ROUTING_CAPABILITY_MISSING + `src/lib/events/payloads.ts` RoutingCapabilityMissingPayload — Plan 11-04.

    **Automated verify:**
    - `cd src-tauri && cargo test --lib router::tests` exits 0 with ≥ 7 passing
    - `npm run verify:providers-capability` confirms ROUTING_CAPABILITY_MISSING registered
    - Grep audit: exactly 1 `router::select_provider` call site in src-tauri/src/ (commands.rs only)

    **Manual verify:** 11-VALIDATION.md Manual row 4 — enable debug logging, send image-attached message with Groq primary + Anthropic fallback, confirm Anthropic is selected.

    ---

    ## Full Verification Command Set

    ```
    # Rust unit + integration tests
    cd src-tauri && cargo test --lib provider_paste_parser::tests \
                                capability_probe::tests \
                                config::tests::phase11_fields_round_trip \
                                router::tests

    # Batched cargo check
    cd src-tauri && cargo check

    # TypeScript compile
    cd /home/arnav/blade && npx tsc --noEmit

    # Phase 11 Playwright suite (11 specs)
    cd /home/arnav/blade && npm run test:e2e:phase11

    # Phase 11 structural gate
    cd /home/arnav/blade && npm run verify:providers-capability

    # Full chain (existing + new gate)
    cd /home/arnav/blade && npm run verify:all
    ```

    ---

    *Generated 2026-04-20 by Plan 11-06 as the Phase 11 goal-backward trace.*
    ```

    CRITICAL:
    - package.json edits are minimal — 2 new script entries + 1 chain extension. Do NOT reformat other entries.
    - The test:e2e:phase11 script lists exact file paths (not a glob) so CI environments without shell expansion work.
    - Manual trace doc is a `.md` under `.planning/phases/11-smart-provider-setup/` — follows same naming as 11-CONTEXT.md / 11-RESEARCH.md / 11-VALIDATION.md.
  </action>
  <verify>
    <automated>cd /home/arnav/blade && npm run verify:providers-capability 2>&1 | tail -30 && npm run verify:all 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Grep `"verify:providers-capability"` in package.json returns 1 match
    - Grep `"test:e2e:phase11"` in package.json returns 1 match
    - Grep `&& npm run verify:providers-capability` in package.json returns 1 match (chain extension)
    - File exists: `test -f .planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md` + line count ≥ 80
    - Grep `Criterion 1`, `Criterion 2`, `Criterion 3`, `Criterion 4`, `Criterion 5` in 11-MANUAL-TRACE.md each returns ≥ 1 match
    - `npm run verify:providers-capability` exits 0 (after Plans 11-01..11-05 have landed)
    - `npm run verify:all` exits 0 (existing gates + new Phase 11 gate all green)
  </acceptance_criteria>
  <done>
    verify:providers-capability + test:e2e:phase11 script entries in package.json; verify:all chain extended; manual trace doc committed. Full chain runs clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is pure tooling — it does not introduce runtime code. No user input, no network I/O. Therefore the threat model is minimal.

| Boundary | Description |
|----------|-------------|
| Node gate script reading source files | Read-only filesystem access within the repo; never writes |
| package.json script expansion | npm-side execution; commands are fixed strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-30 | Tampering | attacker edits source to bypass check | accept | The gate is advisory during development; CI enforcement happens at PR time; defense-in-depth through code review, not at the gate level |
| T-11-31 | Denial of Service | regex catastrophic backtracking in walker | mitigate | Both regexes used (`route:\\s*['"]` and `<CapabilityGap\\s+capability=...`) are linear-time bounded patterns; file walk is O(n) over src/ |
| T-11-32 | Information Disclosure | gate output leaking file paths | accept | Only repo-relative paths logged; no secrets read or echoed; stdout is intended for developer consumption |
| T-11-33 | Tampering | malicious package.json script | mitigate | package.json edits reviewed via git diff at commit time; script value is a fixed `node scripts/...` invocation with no variable interpolation |
</threat_model>

<verification>
- `npm run verify:providers-capability` exits 0 (after Wave 1 plans landed)
- `npm run verify:all` exits 0 (chain extended, no regression)
- `npm run test:e2e:phase11` runs 11 specs (3 from Plan 11-03 + 8 from Plan 11-05)
- Grep confirms package.json has both new script entries + chain extension
- Manual trace doc at .planning/phases/11-smart-provider-setup/11-MANUAL-TRACE.md covers all 5 ROADMAP success criteria
</verification>

<success_criteria>
- Phase 11 gate shipped: `npm run verify:providers-capability` structurally validates 4 invariants (CAPABILITY_SURFACES entry count, CapabilityGap usage count, 6-place config pattern, event registry) and blocks regressions at commit time
- package.json extended: `verify:providers-capability` becomes gate 20 in the verify:all chain; `test:e2e:phase11` runs the 11 Phase 11 Playwright specs in one command
- Manual trace committed: each of the 5 ROADMAP success criteria is mapped to its closing artifacts (files + automated verify commands + manual verify steps) — /gsd-verify-work can read this doc top-to-bottom to audit Phase 11 acceptance
- All Wave 0 + Wave 1 + Wave 2 artifacts green under one command: `npm run verify:all` + `cargo test --lib` + `npm run test:e2e:phase11` — Phase 11 is DONE when all three exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/11-smart-provider-setup/11-06-SUMMARY.md`.
</output>
