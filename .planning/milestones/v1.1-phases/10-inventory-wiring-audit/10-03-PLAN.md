---
phase: 10-inventory-wiring-audit
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml
autonomous: true
requirements:
  - AUDIT-02
tags:
  - audit
  - subagent-b
  - route-mapping
  - command-palette
  - phase-10
must_haves:
  truths:
    - "Every route in ALL_ROUTES (src/windows/main/router.ts) appears as exactly one row"
    - "Each route has id, file, classification, section, palette_visible, data_shape, data_source[], flow_status"
    - "dev-only routes classified as ACTIVE (dev-only) and excluded from NOT-WIRED backlog (Pitfall 4)"
    - "4 non-main window shells (quickask/hud/ghost/overlay) recorded under a windows: key with palette_surface: null"
    - "Palette-source correction honored: palette lives only in main window, not in 5 windows (RESEARCH.md correction to CONTEXT.md §D-49)"
  artifacts:
    - path: ".planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml"
      provides: "Intermediate subagent output — Route + palette + window catalog ready for synthesis"
      contains: "routes:"
  key_links:
    - from: "10-ROUTES.yaml"
      to: "Plan 05 synthesis"
      via: "YAML load + merge into routes[] array of 10-WIRING-AUDIT.json"
      pattern: "routes:\\s*$"
---

<objective>
Produce `.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml` — the mechanical extraction of every route registered in `src/windows/main/router.ts::ALL_ROUTES` plus the 4 non-main window shells. Classifies routes as ACTIVE / ACTIVE (dev-only) / WIRED-NOT-USED / NOT-WIRED / DEAD with data_shape, data_source, flow_status per D-47.

Purpose: Delegate route + palette extraction to an isolated subagent; Plan 05 merges the YAML into the canonical JSON sidecar. This is the ONLY subagent that needs to correct CONTEXT.md §D-49's inaccurate phrasing (palette is not per-window; it is derived from ROUTE_MAP and lives only in MainShell.tsx).

Output: One intermediate YAML file. Conforms to the subagent contract in 10-RESEARCH.md:380-406 §"Subagent B output schema (YAML)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-inventory-wiring-audit/10-CONTEXT.md
@.planning/phases/10-inventory-wiring-audit/10-RESEARCH.md
@.planning/phases/10-inventory-wiring-audit/10-PATTERNS.md
@src/windows/main/router.ts
@src/windows/quickask/main.tsx
@src/windows/hud/main.tsx
@src/windows/ghost/main.tsx
@src/windows/overlay/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spawn Subagent B (Route + Command-Palette Mapper) and write 10-ROUTES.yaml</name>
  <files>.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml</files>
  <read_first>
    - .planning/phases/10-inventory-wiring-audit/10-RESEARCH.md — read lines 380-406 (§"Subagent B output schema (YAML)")
    - .planning/phases/10-inventory-wiring-audit/10-RESEARCH.md — read lines 183-205 (§"Pattern 3: Route + Palette Single-Source Derivation")
    - .planning/phases/10-inventory-wiring-audit/10-RESEARCH.md — read lines 232-260 (§"Pitfall 1: CommandPalette is Main-Only", §"Pitfall 4: dev-only Routes")
    - src/windows/main/router.ts — read full file (the ALL_ROUTES union is the complete input surface)
    - src/features/dashboard/index.tsx (and every other `src/features/*/index.tsx`) — read when sweeping each cluster's `routes: RouteDefinition[]` export
    - src/windows/quickask/main.tsx — read full (non-palette window shell)
    - src/windows/hud/main.tsx — read full
    - src/windows/ghost/main.tsx — read full
    - src/windows/overlay/main.tsx — read full
  </read_first>
  <action>
    Spawn a single Task-tool subagent with `subagent_type: "general-purpose"` and the exact prompt below. Capture its output to `.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml`. If malformed (does not start with `routes:`), retry once with a specific correction note.

    **Subagent B prompt (verbatim — copy directly into Task({description, prompt, subagent_type}))**:

    ---
    You are Subagent B (Route + Command-Palette Mapper) for BLADE Phase 10 Inventory & Wiring Audit. You are running in an isolated Claude context. Your sole job is to enumerate every route in `src/windows/main/router.ts::ALL_ROUTES` + the 4 non-main window shells, and emit one YAML document classifying each.

    **Output target:** write your final YAML to stdout. Do NOT write files yourself; do NOT touch source code.

    **Authority docs (read these before doing anything):**
    - `.planning/phases/10-inventory-wiring-audit/10-CONTEXT.md` §D-48 (classification heuristic), §D-49 (coverage scope — BUT note the palette correction below)
    - `.planning/phases/10-inventory-wiring-audit/10-RESEARCH.md` §"Subagent Contract Design → Subagent B output schema" (lines 380-406)
    - `.planning/phases/10-inventory-wiring-audit/10-RESEARCH.md` §"Pattern 3: Route + Palette Single-Source Derivation" (lines 183-205)
    - `.planning/phases/10-inventory-wiring-audit/10-RESEARCH.md` §"Pitfall 1: CommandPalette is Main-Only" (lines 234-240)
    - `.planning/phases/10-inventory-wiring-audit/10-RESEARCH.md` §"Pitfall 4: dev-only Routes" (lines 255-260)

    **CRITICAL palette correction (supersedes CONTEXT.md §D-49):**
    CommandPalette is mounted ONLY in `src/windows/main/MainShell.tsx`. The 4 other window shells (quickask, hud, ghost, overlay) have NO palette. Do NOT search them for palette registrations. Palette entries = `ROUTE_MAP` from `src/windows/main/router.ts` filtered by `paletteHidden !== true`. This is a single source. Treat CONTEXT.md's phrasing "palette entries across the 5 window shells" as inaccurate; RESEARCH.md §"Pattern 3" is the corrected authority.

    **Extraction protocol (use Read + Grep + Glob tools; do NOT use bash grep):**

    1. Read `src/windows/main/router.ts` in full. Identify the `ALL_ROUTES` array; it is built from 13 feature-cluster spreads: `dashboardRoutes`, `chatRoutes`, `settingsRoutes`, `agentRoutes`, `knowledgeRoutes`, `lifeOsRoutes`, `identityRoutes`, `devToolsRoutes`, `adminRoutes`, `bodyRoutes`, `hiveRoutes`, `onboardingRoutes`, and `devRoutes` (gated by `import.meta.env.DEV`).
    2. Glob `src/features/*/index.tsx` (13 files). Read each; find the `export const routes: RouteDefinition[] = [ ... ]` literal; parse each entry's `id`, `label`, `section`, `component` / `component_file`, `phase`, `shortcut`, `paletteHidden`.
    3. For each route, determine the component source file (either `component` import path in `routes[]` or the file the cluster's `routes[]` references — typically `src/features/<cluster>/<Component>.tsx`).
    4. For each component file, read it to infer:
       - `data_shape`: the props or top-level state type (e.g. `DashSnapshot { scan_profile, hormones, perception }`)
       - `data_source[]`: list of `invokeTyped` calls and `useTauriEvent` subscriptions found in the component
       - `flow_status`: `data pipes` (invoke + event subs present, data flows), `placeholder` (component renders static/mock content), or `dead` (component throws, returns null, or is a TODO stub)
    5. Classify per D-48:
       - `ACTIVE`: route renders AND data_source has ≥1 invokeTyped/useTauriEvent that resolves to a live backend command/event
       - `ACTIVE (dev-only)`: route is in `devRoutes` array (gated by `import.meta.env.DEV`) — regardless of data flow
       - `WIRED-NOT-USED`: route renders but backend integration is broken (invoke call to non-existent command, or component is a placeholder with no invoke at all despite a real backend existing)
       - `NOT-WIRED`: route is registered in `ALL_ROUTES` but component file doesn't exist, or component is intentionally empty placeholder for future phase
       - `DEAD`: not applicable to routes in this codebase (every ALL_ROUTES entry is at least registered); use NOT-WIRED with a `notes:` explaining deferred status instead
    6. For the 4 non-main window shells, emit one row each under a `windows:` top-level key with `palette_surface: null` and classification based on whether the shell renders a usable component (all 4 are ACTIVE per the v1.0 substrate STATE.md).

    **YAML output schema (exactly this shape — do not add extra top-level keys):**

    ```yaml
    routes:
      - id: dashboard
        file: src/features/dashboard/index.tsx
        component_file: src/features/dashboard/Dashboard.tsx
        classification: ACTIVE
        section: core
        phase: 3
        palette_visible: true
        shortcut: "Mod+1"
        data_shape: "DashSnapshot { scan_profile, hormones, perception }"
        data_source:
          - invoke: perception_get_latest
          - invoke: homeostasis_get
          - event: hormone_update
        flow_status: "data pipes"
        reachable_paths:
          - "route:dashboard → invokeTyped('perception_get_latest') → src-tauri/src/perception_fusion.rs"
        notes: ""
      - id: dashboard-dev
        file: src/features/dev/index.tsx
        component_file: src/features/dev/DevDashboard.tsx
        classification: "ACTIVE (dev-only)"
        section: dev
        phase: null
        palette_visible: false
        shortcut: null
        data_shape: null
        data_source: []
        flow_status: "placeholder"
        notes: "dev-only route; gated by import.meta.env.DEV in router.ts"
      - id: chat-history
        file: src/features/chat/index.tsx
        component_file: src/features/chat/ChatHistory.tsx
        classification: WIRED-NOT-USED
        section: core
        phase: 3
        palette_visible: true
        shortcut: null
        data_shape: "ChatHistory[]"
        data_source:
          - invoke: get_chat_history
        flow_status: "placeholder"
        notes: "invokeTyped('get_chat_history') call exists but backend command not registered in lib.rs::generate_handler!"
    windows:
      - label: quickask
        file: src/windows/quickask/main.tsx
        component: src/features/quickask/QuickAskWindow.tsx
        classification: ACTIVE
        palette_surface: null
        notes: "Overlay window; no palette by design"
      - label: hud
        file: src/windows/hud/main.tsx
        component: src/features/hud/HudWindow.tsx
        classification: ACTIVE
        palette_surface: null
        notes: "HUD strip; no palette by design"
      - label: ghost
        file: src/windows/ghost/main.tsx
        component: src/features/ghost/GhostOverlayWindow.tsx
        classification: ACTIVE
        palette_surface: null
        notes: "Content-protected overlay; no palette by design"
      - label: overlay
        file: src/windows/overlay/main.tsx
        component: src/features/voice/VoiceOrbWindow.tsx
        classification: ACTIVE
        palette_surface: null
        notes: "Voice orb overlay; no palette by design"
    ```

    **Schema rules (enforced by Phase 10 Wave 0 verify script):**
    - `id`: lowercase kebab-case, pattern `^[a-z][a-z0-9-]*$`
    - `file`: starts with `src/`
    - `classification`: one of `ACTIVE` | `ACTIVE (dev-only)` | `WIRED-NOT-USED` | `NOT-WIRED` | `DEAD`
    - `section`: one of `core` | `agents` | `knowledge` | `life` | `identity` | `dev` | `admin` | `body` | `hive`
    - `palette_visible`: boolean (true if `paletteHidden` is absent or false in the RouteDefinition)
    - `flow_status`: `"data pipes"` | `"placeholder"` | `"dead"`
    - `data_source[]`: array of objects, each with either `{invoke: "<cmd_name>"}` or `{event: "<event_name>"}` shape

    **Coverage discipline:**
    - Every entry in `ALL_ROUTES` (evaluated at build time — including both DEV and PROD spreads) gets one row under `routes:`.
    - Include `devRoutes` entries and tag them `ACTIVE (dev-only)` (Pitfall 4).
    - Dialogs/modals triggered from a parent route are NOT enumerated here (D-49: listed under parent with `triggered_from` note).
    - The 4 non-main windows live under `windows:` not `routes:`.

    **Constraints:**
    - Read-only; do NOT modify source code.
    - Do NOT emit Markdown, prose explanation, or summary. Output ONLY the YAML starting with `routes:`.
    - Do NOT use `grep`/`cat`/`find` in Bash. Use Claude Code's Grep + Read + Glob tools.
    - Do NOT search non-main windows for palette entries (Pitfall 1).
    - Do NOT classify `ALL_ROUTES` entries as DEAD — use NOT-WIRED with `notes:` explaining deferred/placeholder status.

    **Final step:** emit the YAML document to stdout. Start with `routes:` on column 1. Include the `windows:` block after the `routes:` block. End with a final newline. No `---` frontmatter. No trailing commentary.

    ---

    **Post-capture steps (executor performs):**
    1. Write the subagent's stdout verbatim to `.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml`.
    2. Run the sanity check: `node -e "const y=require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml','utf8');if(!y.startsWith('routes:'))process.exit(1);const routeCount=(y.match(/^\\s*- id: [a-z]/gm)||[]).length;const windowCount=(y.match(/^\\s*- label: [a-z]/gm)||[]).length;if(routeCount<20)process.exit(2);if(windowCount!==4)process.exit(3);console.log('ROUTES.yaml ok:',routeCount,'routes,',windowCount,'windows')"`
    3. If the sanity check fails, re-spawn the subagent exactly once with a specific correction note. If retry still fails, halt and surface.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml','utf8');if(!y.startsWith('routes:'))throw new Error('missing routes: header');const routeCount=(y.match(/^\\s*- id: [a-z]/gm)||[]).length;const windowCount=(y.match(/^\\s*- label: [a-z]/gm)||[]).length;if(routeCount<20)throw new Error('too few routes: '+routeCount);if(windowCount!==4)throw new Error('expected 4 windows, got '+windowCount);console.log('ROUTES.yaml ok —',routeCount,'routes,',windowCount,'windows');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml`
    - Starts with `routes:`: `head -1 .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml` equals `routes:`
    - Contains at least 20 route rows (tolerance; real count ~50+): the automated command passes
    - Has exactly 4 window entries (quickask/hud/ghost/overlay): automated command asserts `windowCount === 4`
    - Section enum enforced: `grep -E "^\\s*section:" .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml | grep -vE "(core|agents|knowledge|life|identity|dev|admin|body|hive)"` returns empty
    - Classification enum enforced: `grep -E "^\\s*classification:" .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml | grep -vE "(ACTIVE( \\(dev-only\\))?|WIRED-NOT-USED|NOT-WIRED|DEAD)"` returns empty
    - palette_surface: null for every window: `grep -c "palette_surface: null" .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml` returns ≥4
    - No palette entries in non-main windows (Pitfall 1): search for `palette_visible:` under `windows:` must be absent
  </acceptance_criteria>
  <done>`10-ROUTES.yaml` exists; starts with `routes:`; contains ≥20 route rows; has exactly 4 window entries; section/classification enums match schema; every dev-only route tagged `ACTIVE (dev-only)` and excluded from NOT-WIRED backlog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — read-only extraction to a YAML intermediate file. No network, no IPC, no user input, no auth/authz, no external API.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | N/A | 10-ROUTES.yaml | accept | Read-only audit produces only Markdown + JSON artifacts in `.planning/`. No code changes, no data exposure surfaces, no auth/authz, no external API contact, no user input handling. Threat model deferred to phases 11+ that ship runtime behavior. |
</threat_model>

<verification>
After Plan 03 completes:
- `test -f .planning/phases/10-inventory-wiring-audit/10-ROUTES.yaml` exits 0
- YAML starts with `routes:` top-level key
- Contains ≥20 route rows and exactly 4 window rows
- Section and classification enums match the schema allowed sets
- Every dev-only route has `classification: "ACTIVE (dev-only)"`
- No palette registrations found outside the main window (Pitfall 1 honored)
</verification>

<success_criteria>
1. `10-ROUTES.yaml` exists with `routes:` and `windows:` top-level keys.
2. Every entry in `ALL_ROUTES` (including DEV spreads) appears as one row.
3. dev-only routes tagged `ACTIVE (dev-only)`.
4. 4 non-main window shells listed with `palette_surface: null`.
5. Section values restricted to the 9-member enum in the schema.
6. `data_source[]` format is `{invoke: "<name>"} | {event: "<name>"}` object list.
</success_criteria>

<output>
After completion, create `.planning/phases/10-inventory-wiring-audit/10-03-SUMMARY.md`. Include:
- Subagent invocation summary (retry count, malformation)
- Row count per classification
- Count of dev-only routes tagged correctly (Pitfall 4 verification)
- Confirmation that no palette registrations were reported outside `src/windows/main/` (Pitfall 1)
- Note: YAML file will be deleted by Plan 05 after synthesis
</output>
