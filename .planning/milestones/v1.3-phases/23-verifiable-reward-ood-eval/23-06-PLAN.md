---
phase: 23
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - src-tauri/src/evals/mod.rs
autonomous: true
requirements:
  - REWARD-05
must_haves:
  truths:
    - "evals/mod.rs registers all 3 OOD modules behind #[cfg(test)] in lockstep"
    - "Running `cargo test --lib evals -- --nocapture --test-threads=1` exercises all 8 eval modules and prints 8 EVAL-06 box-drawing tables"
    - "All 3 OOD modules pass their respective floors (0.85 / 0.80 / 0.75)"
    - "tests/evals/history.jsonl gains 3 new module rows after first OOD test run"
  artifacts:
    - path: "src-tauri/src/evals/mod.rs"
      provides: "Three #[cfg(test)] mod lines for adversarial_eval, ambiguous_intent_eval, capability_gap_stress_eval"
      contains: "mod adversarial_eval"
  key_links:
    - from: "src-tauri/src/evals/mod.rs"
      to: "src-tauri/src/evals/adversarial_eval.rs"
      via: "#[cfg(test)] mod adversarial_eval;"
      pattern: "mod adversarial_eval"
    - from: "src-tauri/src/evals/mod.rs"
      to: "src-tauri/src/evals/ambiguous_intent_eval.rs"
      via: "#[cfg(test)] mod ambiguous_intent_eval;"
      pattern: "mod ambiguous_intent_eval"
    - from: "src-tauri/src/evals/mod.rs"
      to: "src-tauri/src/evals/capability_gap_stress_eval.rs"
      via: "#[cfg(test)] mod capability_gap_stress_eval;"
      pattern: "mod capability_gap_stress_eval"
---

<objective>
Wave 3 / OOD module registration. Land the 3-line `mod` registration block
in `src-tauri/src/evals/mod.rs` that brings the 3 OOD eval modules
(authored in Plans 23-03/04/05) into the test build.

Critical sequencing: this plan depends on all three OOD module files
existing on disk. It does NOT yet bump `verify-eval.sh EXPECTED=5→8` —
that's the LAST task before phase close (Plan 23-09) per PATTERNS.md
ordering constraint.

Purpose: Once `mod`-registered, each OOD module's `#[test]` becomes
exercisable via `cargo test --lib evals::<module> -- --nocapture --test-threads=1`,
and the floor-pass evidence flows into `tests/evals/history.jsonl` via
the existing `record_eval_run` plumbing.

Output: `src-tauri/src/evals/mod.rs` with 3 new lines appended to the
existing `#[cfg(test)] mod ...` block (currently 5 entries; lines 11-15
of mod.rs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-verifiable-reward-ood-eval/23-CONTEXT.md
@.planning/phases/23-verifiable-reward-ood-eval/23-PATTERNS.md
@.planning/phases/23-verifiable-reward-ood-eval/23-RESEARCH.md
@.planning/phases/23-verifiable-reward-ood-eval/23-03-SUMMARY.md
@.planning/phases/23-verifiable-reward-ood-eval/23-04-SUMMARY.md
@.planning/phases/23-verifiable-reward-ood-eval/23-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 3 OOD modules in evals/mod.rs (lockstep)</name>
  <files>src-tauri/src/evals/mod.rs</files>
  <read_first>
    - src-tauri/src/evals/mod.rs (entire file — currently 5 #[cfg(test)] mod lines at 11-15)
    - .planning/phases/23-verifiable-reward-ood-eval/23-PATTERNS.md §"MOD: src-tauri/src/evals/mod.rs — register 3 new OOD eval modules" (the 3-line append block)
    - .planning/phases/23-verifiable-reward-ood-eval/23-RESEARCH.md §"Harness Integration" (DO NOT raise EXPECTED earlier — that's Plan 23-09)
    - src-tauri/src/evals/adversarial_eval.rs, ambiguous_intent_eval.rs, capability_gap_stress_eval.rs (verify each file exists)
  </read_first>
  <action>
    1. Verify all 3 OOD module files exist on disk first:
       ```bash
       test -f src-tauri/src/evals/adversarial_eval.rs \
         && test -f src-tauri/src/evals/ambiguous_intent_eval.rs \
         && test -f src-tauri/src/evals/capability_gap_stress_eval.rs
       ```
       If any are missing, do NOT proceed — open the corresponding
       upstream plan summary file and fix.

    2. Read `src-tauri/src/evals/mod.rs`. Locate the existing 5-line
       `#[cfg(test)] mod *_eval;` block (currently lines 11-15 per
       PATTERNS.md verified-live):
       ```rust
       #[cfg(test)] pub mod harness;
       #[cfg(test)] mod hybrid_search_eval;
       #[cfg(test)] mod real_embedding_eval;
       #[cfg(test)] mod kg_integrity_eval;
       #[cfg(test)] mod typed_memory_eval;
       #[cfg(test)] mod capability_gap_eval;
       ```

    3. Append 3 new lines IMMEDIATELY AFTER `#[cfg(test)] mod capability_gap_eval;`:
       ```rust
       #[cfg(test)] mod adversarial_eval;            // Phase 23 / REWARD-05
       #[cfg(test)] mod ambiguous_intent_eval;       // Phase 23 / REWARD-05
       #[cfg(test)] mod capability_gap_stress_eval;  // Phase 23 / REWARD-05
       ```

       Order MUST match: adversarial_eval, ambiguous_intent_eval,
       capability_gap_stress_eval (most-stable to least-stable, mirroring
       MODULE_FLOOR descent 0.85 → 0.80 → 0.75).

    4. Do NOT bump `scripts/verify-eval.sh EXPECTED=5` to `EXPECTED=8` in
       this plan. PATTERNS.md §"MOD: src-tauri/src/evals/mod.rs" §Gotchas
       locks this: "DO NOT raise EXPECTED earlier — running verify:eval
       mid-wave with EXPECTED=8 and only some modules wired will fail the
       gate." Plan 23-09 owns the EXPECTED bump as the LAST task before
       phase close.

    5. Run `cd /home/arnav/blade/src-tauri && cargo test --lib evals -- --nocapture --test-threads=1`. Expect:
       - 5 existing eval modules pass (no regression)
       - 3 new eval modules pass their floors (0.85 / 0.80 / 0.75)
       - 8 total `┌── ` table-headers in stdout (verify-eval.sh's grep target)
       - `tests/evals/history.jsonl` gains 3 fresh rows for the new modules
  </action>
  <verify>
    <automated>cd /home/arnav/blade/src-tauri && cargo test --lib evals -- --nocapture --test-threads=1 2>&1 | tee /tmp/23-06-evals.log | grep -c '┌── ' | xargs -I {} test {} -ge 8 && grep -q "test result: ok" /tmp/23-06-evals.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^#\[cfg(test)\] mod " /home/arnav/blade/src-tauri/src/evals/mod.rs` returns >= 8 (5 existing + 3 new)
    - `grep -q "^#\[cfg(test)\] mod adversarial_eval;" /home/arnav/blade/src-tauri/src/evals/mod.rs` exits 0
    - `grep -q "^#\[cfg(test)\] mod ambiguous_intent_eval;" /home/arnav/blade/src-tauri/src/evals/mod.rs` exits 0
    - `grep -q "^#\[cfg(test)\] mod capability_gap_stress_eval;" /home/arnav/blade/src-tauri/src/evals/mod.rs` exits 0
    - `cd /home/arnav/blade/src-tauri && cargo check --tests 2>&1 | tail -3` reports no compile errors
    - `cd /home/arnav/blade/src-tauri && cargo test --lib evals::adversarial_eval -- --nocapture --test-threads=1` exits 0
    - `cd /home/arnav/blade/src-tauri && cargo test --lib evals::ambiguous_intent_eval -- --nocapture --test-threads=1` exits 0
    - `cd /home/arnav/blade/src-tauri && cargo test --lib evals::capability_gap_stress_eval -- --nocapture --test-threads=1` exits 0
    - `cd /home/arnav/blade/src-tauri && cargo test --lib evals -- --nocapture --test-threads=1 2>&1 | grep -c '┌── '` returns >= 8
    - `bash /home/arnav/blade/scripts/verify-eval.sh 2>&1 | tail -5` does NOT yet require 8 (EXPECTED=5 still — Plan 23-09 bumps it)
    - `grep -q '^EXPECTED=5' /home/arnav/blade/scripts/verify-eval.sh` exits 0 (NOT YET bumped — Plan 23-09 owns)
  </acceptance_criteria>
  <done>3 OOD modules are mod-registered behind #[cfg(test)]; cargo test --lib evals exits green with all 8 modules emitting EVAL-06 tables; verify-eval.sh EXPECTED=5 unchanged (intentionally; Plan 23-09 owns the bump).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module registration in mod.rs | Source-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Tampering | `evals/mod.rs` `#[cfg(test)]` gate | mitigate | The `#[cfg(test)]` attribute is mandatory — production builds MUST NOT compile eval modules (CLAUDE.md "Module Map" + PATTERNS.md gotcha). The 3 new lines all carry the same gate; verified by acceptance criteria grep. |
| T-23-06-02 | Denial of Service | A misbehaving OOD module test crashing the cargo test run | mitigate | Each `#[test]` is isolated under `--test-threads=1`; failure of one test surfaces visibly via the `cargo test` exit code. The harness's `record_eval_run` lands a row even on assert failure (Phase 17 D-14), so Doctor stays informed. |
</threat_model>

<verification>
- `evals/mod.rs` carries 8 `#[cfg(test)] mod ...` lines (5 pre-existing + 3 new).
- `cargo test --lib evals -- --nocapture --test-threads=1` exits 0 with all 8 module tests green.
- 8 EVAL-06 box-drawing tables emitted to stdout.
- `tests/evals/history.jsonl` gains 3 new rows on first run.
- `scripts/verify-eval.sh` still says `EXPECTED=5` — Plan 23-09 bumps to 8.
</verification>

<success_criteria>
- 3-line append to `evals/mod.rs` lands in lockstep.
- All 3 OOD module tests pass their floors when run via cargo.
- The 8-table EVAL-06 emission is now achievable; verify-eval.sh remains at 5 until Plan 23-09 LAST-of-phase bump.
</success_criteria>

<output>
After completion, create `.planning/phases/23-verifiable-reward-ood-eval/23-06-SUMMARY.md` per template. Capture: actual cargo test --lib evals output (test count, time, pass rate per module from the harness summary lines), and confirm the 3 new history.jsonl rows landed.
</output>
